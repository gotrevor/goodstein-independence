# HANDOFF — lap 119 · the engine swap is NOT "pure wiring": its O1 (regularity) front landed; descent + orbit-bundle remain

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint unchanged: `peano_not_proves_goodstein` (Statement.lean:22) still a literal `sorry`,
> `[propext, sorryAx, Classical.choice, Quot.sound]` — **0 math axioms**. No sorries added/removed
> (still 8 in `Crux2Blueprint`/related). All edits this lap axiom-clean `[propext, Classical.choice, Quot.sound]`.

## ⭐⭐ THE FINDING — the lap-118 "engine swap = pure wiring" claim was WRONG
I started the engine swap exactly as the lap-118 baton's all-banked table prescribed: re-key `iRK`'s
critical branch `else iRKc d s` → `else iRKcCrit d`, swap `iRKDef`/`iRK_defined` to `iRKcCritDef`, and
simplify `red_zK_crit` to `red (zK s r ds) = iRKcCrit (zK s r ds)`. **The InternalZ half compiles green
standalone** (saved as `scratchpad/lap119-engine-swap.diff`). But changing `red`'s critical value breaks
FAR more than the baton's "2 downstream consumers":

| consumer (file) | what breaks | why the banked table missed it |
|---|---|---|
| `ZRegular_red_zK_crit` (Zsubst) | premises are now `zsubst`/`zInegPrem`/`zAx1`, NOT `zAxReduct∘red` | **the O1 / `ZRegular` front is ENTIRELY ABSENT from the lap-118 table** |
| `ZRegular_red_zK_splice_of_chain` (Zsubst) | reads `iRcritG` premises off `red dᵢ` | same |
| `iord_descent_red_zK_crit` + `iCrit_halves_descend` (RedZKDescent, ~200 lines) | `e0`/`e1` compute `vI = zAxReduct(red ..)`; whole fold changes | descent re-key is real, not a `rw` |
| `not_zKCritical_red_zK` (InternalZ) | trivial (`not_zKCritical_iRKcCrit`) | minor |

`ZRegular_red` is **load-bearing** (feeds `ZDerivesEmptyR`, Crux2Blueprint:1101, toward M3). So the swap
genuinely requires re-establishing O1 for the corrected reduct — a front the baton labelled "pure wiring"
and omitted. I reverted the swap (tree was clean → `git checkout -- src/`) to keep `src/` green, and landed
the missing O1 front additively (it needs only the already-banked `iRKcCrit`, not the swap).

## ⭐ THE ADVANCE — the O1 (regularity) front of the swap, landed (all in `Zsubst.lean`)
- **`zReg_zsubst`** (Zsubst:1224) ALREADY EXISTED — closed-term substitution preserves regularity. Good:
  the hard substitution lemma was already done.
- **`ZRegular_zsubst_zIallPrem`** — the I∀ R-redex's re-principalized child `zsubst (zIallPrem dᵢ) (zIallEig dᵢ)
  (numeral k)` is regular: invert `dᵢ = zIall`, `zReg_zsubst` collapses the substitution, `zReg_zIall`
  gives `zReg d0 = 0`.
- **`ZRegular_zInegPrem`** — the I¬ R-redex's child `zInegPrem dᵢ` is regular (`zReg_zIneg` heredity).
- **`ZRegular_iRKcCrit`** — THE swap's `ZRegular_red_zK_crit` re-proof target: the whole corrected reduct is
  regular. Composes the two slot facts with `ZRegular_zK_of_iCritReductSeq`/`_seqUpdate` (the `zAx1` L-redex
  slots free via `zReg_zAx1`), polarity-dispatched to match `iRKcCrit`'s own `zTag dᵢ = 1` branch. Takes
  hypotheses `hprem` (all chain premises regular — from the chain's own `ZRegular`), `hdI`/`hregI`
  (R-redex premise is a `ZDerivation` + regular), and **`htagI : zTag dᵢ = 1 ∨ = 2`** (the R-redex is an
  I-rule — orbit data, holds because `tp dᵢ = isymR`).

All four axiom-clean `[propext, Classical.choice, Quot.sound]` (verified via `#print axioms`).

## 🎯 NEXT-LAP TARGET — the engine swap, now correctly scoped into THREE fronts
Replay `scratchpad/lap119-engine-swap.diff` (the green InternalZ half: `iRKcCrit`+`iRK`+`iRKcCritDef`
relocated before `iRNextG`; `red_zK_crit` → `iRKcCrit`; `not_zKCritical_red_zK` → `not_zKCritical_iRKcCrit`).
Then close the THREE downstream fronts — the swap stays RED until all land (atomic, don't commit mid-way):

1. **O1 / regularity (Zsubst)** — NOW DE-RISKED. Re-prove `ZRegular_red_zK_crit` via `ZRegular_iRKcCrit`
   (banked this lap). Its `htagI`/`hdI`/`hregI`/`hprem` come from `hZ : ZDerivation (zK s r ds)` + criticality:
   `hprem` from `ZRegular_zK_premise` on `hreg`; `hdI` from `zDerivation_zK_inv`; `htagI` (R-redex is an
   I-rule) from `redexPair_tp` + `tp dᵢ = isymR` ⟹ `zTag dᵢ ∈ {1,2}` (need a `tp_isymR → zTag∈{1,2}` bridge;
   `tp` is `isymR` exactly for tags 1/2 — see `tp` def). `ZRegular_red_zK_crit`'s SIGNATURE must change
   (drop `hI`/`hJ` about `red`; add the redex-structure data) ⟹ its caller `ZRegular_red_zK` (Zsubst:1698)
   adapts. `ZRegular_red_zK_splice_of_chain` similarly re-routes through `iRKcCrit_eq_corr`/`_eq_neg`.
2. **Descent re-key (RedZKDescent)** — the genuinely-heavy front. `iord_descent_red_zK_crit` (line 131) +
   `iCrit_halves_descend` (148) inline the OLD-reduct fold (`vI = zAxReduct(red ..)`, `e0`/`e1`). Re-key to
   the BANKED corrected-reduct descents `iord_descent_iRcritG_critReductCorr` (∀) /
   `iord_descent_iRcritGNeg_critReductNeg` (¬) via `iRKcCrit_eq_corr`/`_eq_neg` + the redex-form data
   (`hdi`/`hdj` zIall/zAxAll or zIneg/zAxNeg, from `tp`-inversion). Budget a whole lap for this front alone.
3. **Soundness (Crux2Blueprint)** — `ZDerivation_red_zK_crit` (517, sorry): restate conclusion to
   `ZDerivation (iRKcCrit (zK s r ds))`, close by `iRKcCrit_eq_corr`/`_eq_neg` + the two soundness capstones
   (`ZDerivation_iRcritG_critReductCorr` ∀ / `ZDerivation_iRcritGNeg_critReductNeg` ¬). Caller (870) adapts.
   The genuine residual: the `redZKReady` orbit bundle (`hdi`/`hdj`/`hIJ`/`hthread`/`hrank`/…) at the
   `redSoundGen` zK-case call site — itself an open sorry.

**Suggested sequencing:** front 1 first (now de-risked, additive-ish), commit-green by adapting only Zsubst;
NO — the swap is atomic (red_zK_crit changes value), so all three must land together. Better: do front 2
(descent) in a SEPARATE prep lap, banking the re-keyed `iord_descent_red_zK_crit` as a lemma stated against
`iRKcCrit` (provable additively once `red_zK_crit ↦ iRKcCrit` is replayed locally), then the atomic swap
becomes a 1-lap wiring of three banked re-proofs.

## Open-sorry inventory (crux-2, 8 sorries — unchanged) — by NAME
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` (THE prize) · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH · M2
`foundation_bot_to_Z_empty` · M3 `false_of_ZDerivesEmpty`.

## Aristotle
Idle — CORRECT. The remaining fronts are engine-internal re-proofs, not isolated lemmas.
