# HANDOFF — 2026-06-23 (lap 46) — ROUTE RESOLVED + internal Thm 3.5 complete

> **Branch** `plan` · **HEAD `74d7496`** · build **green** (`lake build GoodsteinPA`, 1311 jobs) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, untouched).
> Commits this lap: `fd77b94` (bbtail + route resolution), `60db322` (iwtower), `74d7496` (bbeta).
> Untracked (NOT mine, leave alone): `E-ARCHITECTURE-RESPONSE-2026-06-23.md` (Codex), `scratchpad/rathjen.txt` (PDF extract).

## THE BIG RESULT THIS LAP: lap-45's open fork is RESOLVED (memory `route-resolved-prwo-gentzen`)
Operator-directed **Route A**, grounded in **Rathjen 2014 Thm 2.8 + §2-3** (papers/, pdftotext in
`scratchpad/rathjen.txt`). The headline path is Rathjen's Cor 3.7:

  `𝗣𝗔 ⊢ γ →(§3, PRIMREC) 𝗣𝗔 ⊢ PRWO(ε₀) →(Gentzen Thm 2.8(i)) 𝗣𝗔 ⊢ Con(𝗣𝗔)`, then Gödel II.

This IS `Reduction.goodstein_implies_consistency` (its docstring now carries the faithful 2-girder
decomposition; still an honest `sorry`).

**Why the free-X β-wall (`DescentSemantic:582`) was the WRONG target:** Rathjen §3 (Grzegorczyk
domination, Lemma 3.2/3.3) is PRIMREC-ONLY; an oracle-X descent isn't dominated (lap-45's
machine-checked `Grz.not_dominated_of_diag_le`). BUT a **PRWO** descent IS *internally* primrec ⟹
Lemma 3.2 applies internally ⟹ §3 unblocked. The free-X `Thm56.peano_not_proves_TI` does NOT chain
(free-X-TI ⊢ PRWO, so PA⊬PRWO ⟹ PA⊬free-X-TI, WRONG direction) — it's a banked asset, OFF the
headline path. M1 (`goodsteinTerminates_re`) + Phase 0 + Phase 1 (Gödel hook) were already done.

## DONE THIS LAP (all axiom-clean `[propext, Classical.choice, Quot.sound]`) — `src/GoodsteinPA/InternalThm35.lean` (NEW)
The **complete model-internal Rathjen Thm 3.5** over `V ⊧ 𝗜𝚺₁` (codes): from a *slow* internal
descent `α` (`iC αₙ ≤ K(n+1)`), produce a descending `β` indexed from `0` with the tight `iC(βᵣ) ≤ r+1`.
- **block-tail** (`r ≥ K`): `bbtail K α r = ω·α_{(r-K)/K} + (K-(r-K)%K)` (internal division reindex).
  `bbtail_isNF`, `bbtail_C_le` (tight `≤ r+1`), `bbtail_desc` (within/boundary via the existing
  `icmp_betaTail_within/_boundary`, `isNF_iadd_finite`, `iC_betaTail_le`).
- **ω-tower** on codes: `iwtower n = ωₙ` (ω₀=1, ω_{n+1}=ω^{ωₙ}), Σ₁-definable + IΣ₁-total via
  PR.Construction. `iwtower_zero/_succ`, `isNF_iwtower`, `iC_iwtower = 1`, `icmp_iwtower_succ`
  (strict increase), helper `icmp_ocOadd_lt_exp` (smaller leading exponent decides ≺).
- **full sequence** `bbeta K s α r = if r<K then ω_{s+K-1-r} else bbtail K α r`. The prefix is
  SIMPLIFIED from Rathjen's `Σᵢ ω_{s-i}` to SINGLE towers (valid: `C` = max coeff, not term count,
  so `C=1≤j+1`). `bbeta_isNF`, `bbeta_C_le` (`≤ r+1`, all r), `bbeta_desc`.

## THE ONE REMAINING PIECE OF THM 3.5: ω-tower cofinality (next lap, FIRST)
`bbeta_desc` takes a disclosed hypothesis `hbdry : icmp (bbtail K α K) (iwtower s) = 0` (i.e.
`β_K ≺ ωₛ`). Discharge = **ω-tower cofinality in ε₀**: `∀ NF code c, ∃ s, icmp c (iwtower s) = 0`,
with a CONCRETE `s = σ(c)` (need a definable `s` for the β construction). Attack: induction on the
NF code `c` — base `0 ≺ ω₀` (`icmp_zero_ocOadd`); step `c = ocOadd e n t`, by IH `e ≺ ω_{sₑ}`, then
`c < ω^{e+1} ≤ ω^{ω_{sₑ}} = iwtower(sₑ+1)` (needs the `c < ω^{exp+1}` NF bound + `icmp_ocOadd_lt_exp`).

## OPEN DEEP CRUXES (hardest-first; multi-lap; see `PENDING_WORK.md` lap-46 §)
1. **Internal Cor 3.4** — Grzegorczyk hierarchy `f:V→V→V` over `V ⊧ 𝗣𝗔` (internal level `l:V`,
   Ackermann, NOT IΣ₁-total ⟹ needs the PA substrate `ReductModel.reduct_models_PA`, NOT the IΣ₁
   `PR.Construction` toolkit). Produces the slow `α` that `bbeta`/`bbtail` consume. lap-45 path #2
   (parameterize over abstract internal `f` with recursion eqns + Lemma-3.2 domination as hypotheses)
   is the recommended tractable first attack. Existing meta-`l` `InternalCor34` (`ig0`, `iblk`,
   `ibigMul`) is the STANDARD-`l` special case — reused, not dead.
2. **Gentzen Thm 2.8(i) `PRWO(ε₀) → Con(𝗣𝗔)`** — primrec `ord` + reduction `R`, `ord(R D)<ord D`,
   arithmetized in PA over Foundation's `Derivation` / `T.Provable`. The deep ordinal-analysis girder.
   `Con(PA)` = `¬ T.Provable ⌜⊥⌝` (Foundation `Incompleteness/Consistency.lean`).
3. **Formulate `PRWO(ε₀)` as a `Sentence ℒₒᵣ`** (∀ primrec-code descent → finite) — gates 1, 2.
4. Foundation `PA_delta1Definable` axiom rides Gödel II (separate residual to a fully-clean headline;
   lap-6 noted an upstream burndown session). The F-φ `native_decide` in `peano_not_proves_TI` is
   off-path now (free-X banked).

## src/ sorries (unchanged this lap): `Statement.lean:22` (headline, LOCKED), `Reduction.lean`
`goodstein_implies_consistency` (the Route-A girder, now faithfully decomposed in docstring),
`DescentSemantic:582` (free-X β-wall — now KNOWN off-path, candidate for `wip/`), `Thm56` D'
(off-headline), `Embedding.lean:70`, `EmbeddingX:410`, `DescentConstruction:576`.

## LOCKED untouched (anti-fraud): `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
## Aristotle: OFFLINE this run. No jobs. WebFetch dead (use WebSearch / ON-LINE-REQUEST.md if needed).
## NEXT: ω-tower cofinality (finish Thm 3.5), THEN internal Cor 3.4 via abstract-f. Do NOT resume free-X §3-on-X.
