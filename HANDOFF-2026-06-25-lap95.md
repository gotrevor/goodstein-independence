# HANDOFF — lap 95 · crux-2 dispatch gate LANDED in-kernel (ZRegular_red_zK now unconditional)

> Supersedes prior HANDOFF-*.md. Branch `plan`. HEAD = `2026be6`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs).
> Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math
> axioms, re-verified in-kernel this lap); `peano_not_proves_consistency` clean. Lone math `sorry` =
> `goodstein_implies_consistency` (`Reduction.lean:68`) = crux-2 = `redSound`.

## Operator target (unchanged)
Discharge `peano_not_proves_goodstein` AXIOM-FREE = crux-1 (DONE lap 57) ∘ crux-2 (`redSound` =
internalized Buchholz cut-elimination). Path X (finitary, lap-92 DECISION) is the route.

## What this review lap established (direction KEPT, Path X CONFIRMED + SHARPENED)

The lap-92/94 framing of the next step as a "Path X-finitary vs Path C-ω-rule, ~2–3k-line rewrite"
fork is **dissolved** by reading the actual kernel state:

- **O2 is DONE.** `ZDerivation_zsubst` (`Zsubst.lean:1855`, axiom-clean) is exactly the benign,
  criticality-free eigenvariable substitution: `ZDerivation d → maxEigen d < a → ZDerivation
  (zsubst d a t)`. The route-B reducts consume it. The "substitution preserves validity" wall that
  laps 78–91 circled was the *criticality* conjunct, dropped when `ZPhi` moved to `zKValidF`.
- **O1 is DONE except ONE leaf.** `ZRegular_red_zK` (`Zsubst.lean:1788`) is a clean lemma, fully proved
  *modulo the single hypothesis `hseltag`* (splice ⟹ `zTag dᵢ = 4`).
- **The entire remaining regularity wall = that ONE false hypothesis.** Under the current `iRK`
  (`InternalZ.lean:6108`), the splice/replace sub-dispatch is `permIdx dᵢ < lh(zKseq dᵢ)`, which is
  `0 < 0 = false` for non-chain `dᵢ` → the splice fires by default on non-chains (`hseltag` false,
  proved in-kernel `not_permIdx_lt_zKseq_zAtom`).
- **Fix = a SURGICAL gate, not a rewrite.** Splice only when `zTag dᵢ = 4 ∧ ¬ permIdx dᵢ <
  lh(zKseq dᵢ)` (= dᵢ a critical chain); route non-chains to the replace `iRKr` (Buchholz Def 3.2 case
  5.2.2). Identical behaviour on chain selected premises; only non-chains change (junk splice →
  replace). The ω-rule *selection* reading (a critical cut selects the substituted premise whose
  validity is O2) is the SOUNDNESS justification — no new node, no new `Fixpoint` (a naive ω-node
  would break `Fixpoint.StrongFinite`; the bounded-generator design = `zIall` + the derived benign
  substitution = what the repo already has).

## What lap 95 landed (STEP 1 PORTED IN-KERNEL — green 1325, axiom-clean — plus the review synthesis)
The gated dispatch is now IN `src/` (not just spiked):
- **`iRK`** (`InternalZ.lean:6108`) gates the splice on `zTag dᵢ = 4 ∧ ¬ permIdx dᵢ < lh(zKseq dᵢ)`;
  `iRKDef`/`iRK_defined` updated (extra `zTag dᵢ` term + a `zTag dᵢ = 4` case in the definability proof);
  `fstIdx_iRK`/`zTag_iRK` re-proved with `split_ifs <;> simp`.
- **`red_zK_splice`** gains `htag : zTag dᵢ = 4`; **`red_zK_rep`** proof fixed; **NEW `red_zK_rep_nonchain`**
  (non-chain selected premise → replace) — in BOTH `Zsubst.lean` and `Crux2Blueprint.lean`'s local copies.
- **`ZRegular_red_zK` (`Zsubst.lean`) is now UNCONDITIONAL** — `hseltag` DROPPED; `#print axioms =
  [propext, Classical.choice, Quot.sound]`. The lap-94 regularity wall is cleared in-kernel.
- `Crux2Blueprint.ZDerivation_red_zK` dispatch restructured to the gated 3+1-way form (non-chain replace
  = a disclosed `sorry` = the deep validity residual). Headline `#print axioms` unchanged:
  `[propext, sorryAx, choice, Quot.sound]` (0 math axioms, re-verified). Spike `wip/InternalZdispatch.lean`
  REMOVED (superseded — its content is promoted to src/).

Also: `ANALYSIS-2026-06-25-lap95-dispatch-fix-not-pivot.md` (the synthesis), STATUS + PENDING_WORK
refreshed (lap-95 boxes, ledger re-run from real `#print axioms`).

## ▶ NEXT (START HERE) — the validity half (the deep residual)
1. **Validity half** (the genuinely deep residual): rewire the replace branch to emit the
   conclusion-reduced `tpReduce (tp dᵢ) Π n` (`tpReduce` Σ₁-def'd `InternalZ.lean:1064`); prove
   `ZDerivation_red_zK_rep` / `_splice` / `_crit` (Crux2Blueprint sorries) on the reduced conclusions,
   and the non-chain replace case of `ZDerivation_red_zK` (the new disclosed sorry). Lap-90 stands: a
   `red` keeping `fstIdx = Π` is faithful only for `tp = Rep`.
2. **`iord_descent_red`** (`Crux2Blueprint:306`) under the now-faithful dispatch → `false_of_ZDerivesEmpty`
   → `goodstein_implies_consistency` → drop the headline `Statement.lean:22` sorry; confirm `#print
   axioms peano_not_proves_goodstein` is trust-base-clean.

## Aristotle
Idle (all jobs IDLE — none running, verified `aristotle list`). The open lemmas (validity-half
`tpReduce` conclusion-tracking, `iord_descent_red`) are deeply entangled with the repo's bespoke
Σ₁/`Fixpoint` machinery — poor self-contained fodder. Left idle (correct per the charter).

## STOP / don't-repeat
- Do NOT re-derive the "substitution must preserve validity" wall — O2 (`ZDerivation_zsubst`) is DONE.
- Do NOT build a from-scratch `wip/InternalZomega.lean` ω-rule engine — the gate fix reuses the
  finitary engine; the ω-rule is the *justification*, not a rewrite. (A naive ω-node breaks
  `Fixpoint.StrongFinite`.)
- Do NOT supply `hseltag` to `ZRegular_red_zK` against the *current* `iRK` — it is FALSE
  (`not_permIdx_lt_zKseq_zAtom`). Port the gate first.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `wip/InternalZdispatch.lean` elaborates clean via `lake env lean`;
`ZRegular_iRKfix` `#print axioms = [propext, Classical.choice, Quot.sound]`.
