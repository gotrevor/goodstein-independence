# Handoff: lap 144 — ¬-case CLOSED red-free + Ind branch WIRED off red → live path FULLY red-free

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms). **`descent_step_K_critical_neg` AND the whole dispatcher `descent_step_K_critical` are now
`#print axioms` = `[propext, Classical.choice, Quot.sound]`** (sorry-FREE, axiom-clean, off `red`).

## 🎉 MILESTONE — the live `false_of_ZDerivesEmpty` path is now ENTIRELY off `red` (the lap-132 pivot's goal)
Two advances this lap, both within DIRECTION lap-143:
1. **¬-case CLOSED** (step 2) — `descent_step_K_critical_neg` sorry DROPPED (details below).
2. **Ind branch WIRED off `red`** (step 3) — `ZDerivesEmptyR_descent_step`'s Ind root now calls the named
   `descent_step_Ind` (witness = genuine `iIndReductSeqG`), NOT `⟨red d, ZDerivesEmptyR_red hd, …⟩`.

`false_of_ZDerivesEmpty` → `ZDerivesEmptyR_descent_step` → {`descent_step_Ind`, `descent_step_K_majorIdx` →
`descent_step_K_critical` (axiom-clean) / `descent_step_K_noncritical`} references **NO `red`**. The
kernel-FALSE sorries {:80,:1108,:1211,:1384,:1471 = `zKValidF_iIndReduct_of_zInd`/`ZDerivation_red_zK_crit`/
`redSoundGen`/…} are now OFF the live path — only the pre-pivot `red^[n]`-iteration remnants
(`ZDerivesEmptyR_red`:1833, `ZDerivesEmptyR_red_iterate`:1840, `iord_red_iterate_descends`:1851) still
reference them, and those do NOT feed the headline. **The headline's open `sorryAx` now traces ONLY to
honest TRUE-but-unproven lemmas — no kernel-FALSE statement remains on the path.**

## This lap = executed DIRECTION lap-143 step 2 (the ¬-case), a real crux DROP

The lap-142 `redexJ ≤ j0` obstruction is **dissolved**. Before: `descent_step_K_critical_neg` was a named
honest sorry (the lone open critical sub-case). After: both polarities of `descent_step_K_critical` route
through the genuine `iRKcCrit` reduct — the critical-K case is FULLY red-free.

### THE move — route (b): keep-tip `isChainInf` reconstruction (`redexJ ≤ j0`-FREE)
The ¬-case soundness `ZDerivation_iRKcCrit_neg`'s succedent half (`haux0_neg`, which replaces premise
`redexJ` = the `zAxNeg` axiom by the §5 reduct `Ax^1_{Γⱼ→A}` and sets the conclusion succedent to `A`)
needed chain-threading **up to `redexJ`**. From `zKValid`'s `isChainInf` (via `chainInf_redexI_data`) we only
get threading up to the existential tip `j0`, and `redexJ ≤ j0` is NOT free in general. **Fix:** when the
axNeg reduct lands strictly ABOVE the ⊥-orbit tip (`j0 < redexJ`), rebuild chain-validity at the UNCHANGED
tip `j0` — `j0` carries `⊥` (the ⊥-orbit `seqSucc s = ⊥`), so the new `isChainInf` uses the `⊥`-exit
disjunct and the replaced premise (above the tip) never enters the tip-bounded threading/rank. So only
threading up to `j0` is needed — exactly the `chainInf_redexI_data` datum.

### New lemmas
- **InternalZ.lean:** `isChainInf_reduceR_keepTip` (the `j0 < i`, tip-`⊥` keep-tip reconstruction);
  `chainInf_redexI_data` extended to also return the tip exit condition `chainAsucc ds j0 = seqSucc s ∨ = ⊥`
  (2 callers updated with trailing `_`).
- **Crux2Blueprint.lean:** `ZDerivation_corrected_haux0_neg_keepTip` (haux0 via `_general` + keep-tip `hci`),
  `ZDerivation_corrected_haux0_neg_botOrbit` (dispatches `redexJ ≤ j0` → standard / `>` → keep-tip),
  `ZDerivation_iRcritGNeg_corrected_neg_botOrbit`, `ZDerivation_iRKcCrit_neg_botOrbit`. Then
  `descent_step_K_critical_neg` WIRED: soundness = `_neg_botOrbit`, descent = `iord_descent_iRKcCrit_neg`,
  invariants = `ZRegular_/ZFresh_/ZSeqAnt_iRKcCrit_of_zK` (same shape as the lap-143 ∀-case).

## 🎯 NEXT (hardest-first) — the 3 GENUINE open leaves (all honest, none `red`/kernel-false)
1. **`descent_step_Ind` soundness** (`Crux2Blueprint`, the new named sorry). Witness =
   `zK s (irk p) (iIndReductSeqG d0 d1 a k)`, `a = π₁ at'`, `k = value(t)` for the Ind term `t = π₂ at'`.
   - **Soundness** `ZDerivesEmptyR (zK …)`: chain-validity `zKValidF` = `isChainInf_telescope` (banked) +
     per-premise read-offs `chainAnt_/chainAsucc_iIndReductSeqG_*` (banked): base antecedent `= Γ` (`d0`'s),
     step antecedent `Γ,F(i)` threads `F(i) = chainAsucc i`, rank `irk (F(i)) = irk p` (substitution-invariant).
     The **`hexit` clause needs `F(k) = F(t)` i.e. `k = value(t)`** — the lone genuine prerequisite: an
     internal term-evaluation `k = ⟦t⟧` (`substs1 (numeral k) p = substs1 t p = ⊥`). Plus `ZRegular/ZFresh/
     ZSeqAnt` of the reduct (premise-hereditary, like the K-case `*_iRKcCrit_of_zK`).
   - **Descent**: `iord_descent_iIndReduct` (InternalZ:3056, ordinal-shadow descent, real `irk`,
     degree-preserving) via the `iotil`/`idg` congruence `iIndReductSeqG ≅ iIndReductSeq` (corresponding
     premises share `iotil`/`idg`; substitution is ordinal-invariant — same fact behind `iord_descent_red`).
   - **Best first sub-attack:** formalize the internal term-value `k = ⟦t⟧` (the `hexit` blocker), OR prove
     the telescope threading/rank parts modulo `hexit` as a narrower named sorry.
2. **`descent_step_K_noncritical`** (Buchholz 5.2 atomic reduct, GENUINE).
3. **(A) `exists_sigma1_descending_step`** — the `gDef` packaging / concrete `redStep`/witness-bound.

After all three: the dead `red`-soundness sorries {:80,:1108,:1211,:1384,:1471} + the off-path remnants
(`ZDerivesEmptyR_red`/`redSound`/`red^[n]` iteration) → relocate to `wip/` (NOT before — nothing live
references them now, but the relocation is mechanical bookkeeping, do it as a cleanup pass).

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any `ZDerivesEmptyR_descent_step` branch with `red` (the whole path is now off it — keep it so).
- Do NOT attack `redSoundGen`(:1616)/`ZDerivation_red_zK_crit`(:1108)/`zKValidF_iIndReduct_of_zInd`(:80) AS
  STATED — kernel-FALSE/dead, now off-path. Prove `descent_step_Ind` (the genuine replacement) instead.
- Do NOT re-attempt the major-premise-tag {3,4,5,6} split or the `redLeast`/μ-min (A) route (both refuted).

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `descent_step_K_critical_neg` (NEW red-free), `descent_step_K_critical`
  (dispatcher, axiom-clean), `ZDerivation_iRKcCrit_neg_botOrbit`, `ZDerivation_corrected_haux0_neg_botOrbit`,
  `descent_step_Ind` (NEW named sorry — the genuine Ind soundness target), `ZDerivesEmptyR_descent_step`
  (now fully red-free), `false_of_ZDerivesEmpty`:2331 (live endgame entry).
- `src/GoodsteinPA/InternalZ.lean`: `isChainInf_reduceR_keepTip`, `chainInf_redexI_data` (extended);
  `iord_descent_iIndReduct`:3056, `isChainInf_telescope` (Crux2Blueprint), `iIndReductSeqG` read-offs.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-143, still binding) · `PENDING_WORK.md` lap-144 · `STATUS.md` lap-144.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. The ENTIRE live
`false_of_ZDerivesEmpty` path is now off `red` (critical-K ∀+¬ + Ind branch). The 3 remaining open leaves are
all honest/genuine: `descent_step_Ind` (Ind soundness via telescope `zKValidF` + term-value `k = ⟦t⟧`),
`descent_step_K_noncritical` (Buchholz 5.2), (A) `exists_sigma1_descending_step`. FIRST move: attack the
`descent_step_Ind` exit-match `k = value(t)` (the internal term-evaluation prerequisite) or prove the
telescope threading/rank modulo it. NEVER `red`.**
