# HANDOFF — lap 129 · FRESH-MIND REVIEW: the `red`-STALL is the genuine crux; no-stall linchpin LANDED

> **RESUME BATON.** Branch `plan`, HEAD = this lap's commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint unchanged & re-verified in-kernel:
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms). New lemma
> `firstBotPrem_reducible` axiom-clean `[propext, choice, Quot.sound]`; no `sorry` touched (11 disclosed,
> unchanged).

## What this lap DID — a strategic re-orientation + the decisive no-stall lemma

A FRESH-MIND review lap that **corrects the campaign's direction** (laps 121–128 had drifted onto reduct
SOUNDNESS while the lap-120-flagged TERMINATION crux went untouched), grounds the fix in Buchholz §14.25,
and lands the linchpin.

### The finding (resolves lap-120's open question — definitively NO)
The laps 104/107/120 `red`-**STALL** is GENUINE and is NOT killed by `ZRegular`/`ZFresh`:
- `permIdx`/`iperm` selects the first `isymRep` premise; `iperm_isymRep` is UNCONDITIONAL, so it can select
  an atom (`zAtom`, tag 0) or `Ax¹` (`zAx1`, tag 7) LEAF whose succedent ≠ ⊥. Those are `red`-FIXPOINTS
  (`red_zK_fixpoint_of_atom_selected` / `_zAx1_selected`, both PROVEN in repo).
- **Vacuity REFUTED in-kernel:** `zReg (zAtom s) = 0` and `zReg (zAx1 s C) = 0` (`Zsubst.lean:1326,1352`) —
  atoms/`Ax¹` are ALWAYS regular; regularity tracks only eigenvariable freshness. The `zAtom`/`zAx1` `ZPhi`
  disjuncts (`InternalZ:5305,5313`) carry only `inAnt (seqSucc s) (seqAnt s)` — NO atomicity restriction.
  So a chain `∅→∀φ₀, {∀φ₀}→∀φ₁, …, {…}→A` with an all-R-intro (`zIall`/`zIneg`) prefix and an atom whose
  succedent is a prior succedent SATISFIES `isChainInf` and is a valid ⊥-orbit `ZDerivation`. The fixpoint is
  NOT cut-free ⟹ `false_of_ZDerivesEmpty`'s "fixpoint ⟹ cut-free ⟹ absurd" route is unprovable as-is.

### The fix (Buchholz §14.25, `scratchpad/buchholz-gentzen.txt` source-read this lap)
For a chain `Θ→D`, the **major premise** = *the first premise whose succedent IS `D`* (for `∅→⊥`, the first
premise with succedent `⊥`) — NOT the first `iperm`-permissible premise. Buchholz proves the major premise is
NEVER in endform (always reducible); the ordinal STRICTLY drops at every reduction step (Lemma 3.2, NO
fixpoints). The repo's `permIdx` conflates "succedent = D" with "tp = isymRep" and stalls on leaf Reps.

### LANDED — `firstBotPrem_reducible` (`InternalZ.lean`, after `hreroute_of_leaves`; axiom-clean, green)
`ZDerivation (zK s r ds) → seqAnt s = ∅ → seqSucc s = ⊥ → ∃ j < lh ds, chainAsucc ds j = ⊥ ∧ zTag (znth ds
j) ≠ 0 ∧ zTag (znth ds j) ≠ 7`. The faithful major premise (first `⊥`-exit) is `red`-REDUCIBLE — never a
leaf-stall. Proof: the LEAST `⊥`-exit can't be an atom/`Ax¹` leaf, else `chainAsucc_threaded_of_leaf`
re-routes its `⊥` to a strictly earlier exit, contradicting leastness. (Tags `{3,4,5,6}` = zInd/zK/zAxAll/
zAxNeg remain; all reduce. `zIall`/`zIneg` excluded since their succedents ≠ ⊥.)

### Course-correction (a parallel line is a DEAD END on the ⊥-orbit)
The lap-121/122 redex-finder line (`inference_critical_pair_of_chain_reroute`, the "dissolve the stall with
NO engine surgery" dream) is mis-aimed: its Step A (`exit_nonRep_of_reroute`) needs a non-`isymRep` (L-symbol)
exit, but a ⊥-exit is a `zK`/`zInd` sub-derivation of `⊥` (tp = isymRep). Its residual `hreroute` for
non-leaf `isymRep` premises is genuinely FALSE (a `zK` ⊥-exit does NOT re-route — it IS the exit). So engine
surgery (faithful selection) IS required; `firstBotPrem_reducible` is the no-stall guarantee that makes it
safe.

## NEXT (the corrected highest-value target) — the faithful-selection engine re-key
Re-key the selection so `red` of a `∅→⊥` K-node recurses into the MAJOR premise (first `⊥`-exit, reducible by
`firstBotPrem_reducible`) instead of the first `isymRep` premise. Concretely (decomposed in PENDING_WORK
lap-129):
1. Define `majorIdx d` = least `i < lh (zKseq d)` with `chainAsucc (zKseq d) i = seqSucc (fstIdx d)` (the
   Buchholz major premise; on the ⊥-orbit this is the first `⊥`-exit). `𝚺₁`-definable (mirror `permIdxAux`).
2. Prove the no-stall property end-to-end: on a `ZDerivesEmptyR` node, `red` at `majorIdx` is NOT a fixpoint
   (the selected premise has tag ∈ {3,4,5,6}, each `red`-reducible). `firstBotPrem_reducible` is the core.
3. Re-key `iRK`'s replace branch to dispatch on `majorIdx` (was `permIdx`); the critical/splice branch fires
   when the major premise's OWN reduction is an `(R,L)` cut (Buchholz 14.253). Ripples: `permIdxDef`-family,
   the `iord_descent_red` fixpoint branches (atom/`zAx1` cases now UNREACHABLE on the ⊥-orbit → drop), the
   `fstIdx_red`/`tp_selected_*` selection lemmas.
4. Then `false_of_ZDerivesEmpty` closes: no fixpoints on the ⊥-orbit ⟹ `iord_red_iterate_descends` is a
   STRICT descent ⟹ PRWO(ε₀) contradiction. (The PRWO wiring — internalize `n ↦ iord (red^[n] z)` as a `𝚺₁`
   graph, contradict well-foundedness — is the remaining endgame piece, still a bare `sorry`.)

ALTERNATIVE if the re-key is too invasive: keep `permIdx` but prove the ⊥-orbit `red`-fixpoint impossible
DIRECTLY (`no_red_fixpoint_of_ZDerivesEmptyR : ZDerivesEmptyR w → red w ≠ w`) by routing the
atom/`zAx1`-selected case through `firstBotPrem_reducible` (the selected leaf is NOT the major premise, so
the node is not actually `red`-irreducible — needs a `permIdx`-selects-leaf ⟹ contradiction bridge). This
is more contained but its soundness is murkier; the re-key is the faithful path.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open piece is engine-internal selection surgery, not a portable single-lemma target.

## Pointers
`STATUS.md` lap-129 (top) · `PENDING_WORK.md` lap-129 · `NEXT_STEPS.md` lap-129 · no-stall:
`InternalZ.lean` (`firstBotPrem_reducible`, `chainAsucc_threaded_of_leaf:8804`,
`tp_selected_isymRep_of_emptyAnt_botSucc:7621`) · stall fixpoints: `RedZKDescent.lean`
(`red_zK_fixpoint_of_atom_selected:696`, `_zAx1_selected:712`) · Buchholz source:
`scratchpad/buchholz-gentzen.txt` (§13.4 endform, §14.25 chain-rule reduction).
