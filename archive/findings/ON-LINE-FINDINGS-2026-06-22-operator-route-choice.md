# 📌 OPERATOR DIRECTIVE (read me) — Route A vs Route B is delegated to you

This rides the `ON-LINE-FINDINGS-*` lap-start read-hook to guarantee you see it. The substance is in
**`OPERATOR-NOTE-2026-06-22-route-choice.md`** (repo root) — read that.

**TL;DR:** with M6 (Thm 17.1 lower bound) landed, the expedition forks: **Route B (Towsner, current)**
owes M4 embedding + the `(α,k)` cut-elim wiring + the **PA↔PA⁺ arithmetization bridge** (Towsner
Remark 10.3 skips it — the sleeper wall); **Route A (Gentzen, via `Con(PA)`)** sidesteps that bridge
by staying in real PA, and you've already paid much of its cost (M5 cut-elim machinery + the in-flight
`Foundation-delta1-burndown` Δ₁ work).

**The operator delegates the choice to you.** Evaluate both, pick what the evidence favors, **record
your reasoning in `STATUS.md`**, and switch routes between laps if warranted. No need to ask. Fold
this directive into `PENDING_WORK.md`/`STATUS.md` on your next lap (your git), then both this file and
`OPERATOR-NOTE-2026-06-22-route-choice.md` can be trashed.
