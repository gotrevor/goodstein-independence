# Handoff: lap 134 — soundness-front per-node bundle collapsed (hNeg gone, hAll is all that's left)

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `cc299c3` · tree CLEAN · build 🟢 green (`lake build GoodsteinPA`, **1326**)

## 🎯 What we're doing
Headline: `𝗣𝗔 ⊬ Goodstein` (Kirby–Paris), axiom-free. The lone open girder is **crux-2** (IΣ₁-internal
Gentzen consistency / cut-elimination), which is what `peano_not_proves_goodstein`'s `sorryAx` traces to.
The active engine-swap route reduces crux-2 soundness to a per-node bundle (`hAll`/`hNeg`) on the critical
K-node reduct `iRKcCrit`. This lap's mission (operator instruction): discharge those, narrowing toward a
hypothesis-free `ZDerivation_iRKcCrit_botOrbit` that assembles into `false_of_ZDerivesEmpty`.

## 🧠 Context to carry forward
The soundness front is three stacked theorems in `Crux2Blueprint.lean`:
`ZDerivation_iRKcCrit_of_zKValid` (575) → `_of_isChainInf` (614) → `_botOrbit` (648). All three took a
per-node bundle `hAll` (∀-pair) + `hNeg` (¬-pair) + `Seq(seqAnt·)` conjuncts as HYPOTHESES. The lap's arc
was to *derive* each from invariants instead of begging them:

1. **`Seq(seqAnt·)` → derived** from the `ZSeqAnt` fold (lap-133 infra) via `seq_seqAnt_zK_premise` (both
   redex premises are non-chain: `zAxAll` tag 5 / `zIneg` tag 2). Needed threading `ZSeqAnt` into
   `ZDerivesEmptyR` first.
2. **`hNeg` → GONE.** Strengthened the **ZPhi `zIneg` disjunct** with
   `zInegAntWff s p d0 := Seq (seqAnt s) ∧ seqAnt (fstIdx d0) = seqCons (seqAnt s) p` (the exact I¬
   premise-antecedent shape). The `Seq` is BUNDLED so the shape self-preserves under eigensubst
   (`ZDerivation_zsubst`'s zIneg case discharges it via `fvSubstSeq_seqCons`). `zDerivation_zIneg_inv` now
   yields it → `hNeg` reads straight off the redex premise (`hdi ▸ hmem _ hIlt`), no hypothesis.
3. **`hAll` remains** — the ONLY surviving per-node hypothesis. Its hard part is the cut-formula bridge
   `seqSucc sⱼ = cutFormula (zK s r ds)`.

Mental model for `hAll`: `redZKReady_of_zKValid` (`InternalZ:8338`) already does all the polarity dispatch
and INTERNALLY proves every ingredient of the ∀-pair (matrix match `pj = p`, `IsSemiformula 1 p`,
`hcut : cutFormula = substs1 (numeral (π₁π₂tp...)) p`). It just doesn't EXPORT the cut-formula equation. So
the `hAll` discharge is a *strengthening of redZKReady's output*, NOT new mathematics — the proof is already
sitting there. The only genuinely external piece is `maxEigen d0 < a`, which is regularity (NOT in
`zIallWff`) and needs `ZRegular` threaded.

Why this route at all (vs the lap-132 existence-form reflection): both routes consume the same `hAll`/`hNeg`
bundle, so collapsing the bundle is on-path either way. Don't re-litigate.

**Lean gotcha that cost time** (also flushed to staging): the anonymous constructor `⟨…⟩` only *flattens*
a nested `∧` at the **tail**. After adding `∧ zInegAntWff` the `zInegWff` triple became a left-child and
stopped flattening — had to bundle it explicitly `⟨?_,?_,?_⟩` and shift projections `.2.2 → .1.2.2` at 5
sites. Expect the same when strengthening redZKReady's ∀-pair.

## ✅ State
- 6 commits this lap, all green-gated at 1326: ZSeqAnt→ZDerivesEmptyR (`7f9d2b6`), Seq-discharge (`7dbc4e3`),
  ZPhi zIneg strengthening (`4229aa7`), hNeg drop (`b45a7c9`), + 2 docs.
- `src/` builds; the only `sorry`s are the pre-existing crux-2 chain (8 in Crux2Blueprint + Statement/Reduction/
  DescentSemantic) — unchanged footprint, NO new axioms introduced (all additions are constructive proof code).
- Soundness front now carries only `hAll` (+ `hthread`/`hrank`, the `isChainInf` tip data).

## 🎬 Next actions
1. **Strengthen `redZKReady_of_zKValid`'s ∀-pair** (`InternalZ:8341`) to also export
   `seqSucc sⱼ = cutFormula (zK s r ds) ∧ IsUFormula ℒₒᵣ p`. The exact snippet is in `PENDING_WORK.md`
   (lap-134-latest box): `zDerivation_zAxAll_inv (hdj ▸ hZj)` for `seqSucc sⱼ = substs1 (numeral k') pj`,
   `hk : π₁(π₂(tp (znth ds redexJ))) = k'` by `rw [hdj, tp_zAxAll]; simp [isymLk]`, then
   `rw [haxinv.2.2, hcut, hk, hpjp]`. Update the 2 `rcases hcase` consumers (`Crux2Blueprint:595`,
   `InternalZ:9346`). After this, the all-case pulls `hsj`/`hpwff` from `hcase` → `hAll` shrinks to
   `maxEigen d0 < a` only.
2. **Thread `ZRegular` into the soundness front** to discharge `maxEigen d0 < a` (pattern at
   `Crux2Blueprint:1077-1079`), then **drop `hAll`**. The ⊥-orbit supplies `ZRegular` via `ZDerivesEmptyR.2.1`.
3. With the front hypothesis-free, wire `ZDerivation_iRKcCrit_botOrbit` into the assembly
   (`false_of_ZDerivesEmpty` / `wip/ExistenceEndgame.lean`).

## ⚠️ Gotchas
- Don't `ls | tail` for the newest baton — numeric sort: `ls HANDOFF-*-lap*.md | sort -t p -k2 -n | tail -1`.
- `Crux2Blueprint.lean` IS in the default build (imported by `GoodsteinPA.lean`) despite the "not imported"
  comment in its header — that comment is stale. Its `sorry`s are the headline's real debt.
- `(5:V) ≠ 4`-style numeral inequalities in the model `V`: use `simp`, not `decide` (V isn't decidable).

## 📁 Key files
- `src/GoodsteinPA/Crux2Blueprint.lean` — soundness front (575/614/648), `ZDerivesEmptyR` (1103), assembly sorries.
- `src/GoodsteinPA/InternalZ.lean` — `redZKReady_of_zKValid` (8338, NEXT target), `cutFormula`/`_all` (6648/6681),
  `zDerivation_z{Ineg,AxAll,Iall}_inv` (8732/8767/8710), ZPhi/`zblueprint` (5362/5461), `zInegAntWff` (1589).
- `src/GoodsteinPA/Zsubst.lean` — `ZSeqAnt`/`seq_seqAnt_zK_premise` (2168/2188), `ZDerivation_zsubst` zIneg
  case (3460), `fvSubstSeq_seqCons` (3179-ish).
- `PENDING_WORK.md` — lap-134 boxes carry the verbatim next-step snippets.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the context
above, then start at Next action #1: strengthen `redZKReady_of_zKValid`'s ∀-pair output (the proof
ingredients already exist inside it — this is an export, not new math).**
