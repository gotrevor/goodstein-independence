# HANDOFF — lap 131 · zAxAll ZPhi-strengthening LANDED; the shared `Seq(seqAnt)` blocker pinned + flag banked

> **RESUME BATON.** Branch `plan`, HEAD = `fdf5389` (lap-131 final), tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint UNCHANGED & locked:
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms). All 3 new
> lemmas/predicates this lap are axiom-clean `[propext, choice, Quot.sound]`; no `sorry` touched (11
> disclosed, unchanged).

## What this lap DID — drove the lap-130 "turnkey" ZPhi-strengthening, found its hidden depth, completed the tractable half

Three green commits, plus an attempted-then-reverted full atomic change that surfaced the genuine obstruction.

### 1. `a9cf7e3` — `fvSubstSeq_seqCons` substrate + the "self-healing" claim is FALSE
Attempted the full lap-130 ZPhi-strengthening (both `zIneg` + `zAxAll` disjuncts) as one atomic change;
InternalZ went green but `ZDerivation_zsubst` (`Zsubst.lean:~2895`) broke — it is a **genuine SECOND
constructor** of `zIneg`/`zAxAll` `ZDerivation`s that rebuilds the disjunct from *substituted* data
(`fvSubstSeqt a t s`, `fvSubst a t p`, `zsubst d0 a t`), **NOT** via inversion. So lap-130's "construction
sites are SELF-HEALING" is FALSE: strengthening the disjuncts forces `ZDerivation_zsubst` to PROVE the new
conjuncts for the substituted node, each needing an invariant the `ZPhi` skeleton does not record. Reverted
the atomic change (preserved in `scratchpad/lap131-zphi-strengthening-WIP.diff`, 342 lines — the COMPLETE,
correct ZPhi ripple), kept only the reusable substrate **`fvSubstSeq_seqCons`** (`Zsubst.lean` after
`inAnt_fvSubstSeq`): `fvSubstSeq a t (Γ ⁀' A) = (fvSubstSeq a t Γ) ⁀' (fvSubst a t A)` for `Seq Γ`.

### 2. `aa55c3f` — zAxAll ZPhi-strengthening COMPLETE (the completable half)
`zAxAll`'s `zsubst`-transfer obligation (`zAxAllSuccWff`) needs `IsSemiformula ℒₒᵣ 1 p` (for
`fvSubst_substs1`), which the disjunct's `IsUFormula` did not give — but strengthening the disjunct's
formula-hood `IsUFormula → IsSemiformula 1` (matching `zIallWff`, the faithful Buchholz `Ax^{∀p,k}` matrix
IS a 1-formula) makes `hp` **self-supply** it. LANDED:
- `zAxAll` `ZPhi` disjunct now: `IsSemiformula ℒₒᵣ 1 p ∧ inAnt (^∀p)(seqAnt s) ∧ zAxAllSuccWff s p k`.
- rippled: `ZPhi`/`zphi_iff`/`zblueprint` Σ/Π (`!(isSemiformula ℒₒᵣ).sigma/pi 1 p` + `!(zAxAllSuccWffDef…)`)/
  `zPhi_definable` simp (just `+ zAxAllSuccWff_defined.iff`; the raw `isSemiformula` resolved automatically,
  same as `isUFormula`)/`zphi_monotone`/`zphi_strong_finite`.
- `zDerivation_zAxAll_inv` returns all three conjuncts (recovers `k` via `zAxAllK`).
- `ZDerivation_zsubst`'s new zAxAll obligation discharged:
  `seqSucc_fvSubstSeqt ▸ hsucc ▸ fvSubst_substs1 ht (by simp) hp ▸ termFvSubst_numeral`.
- ~7 downstream `IsUFormula` consumers healed with `.isUFormula`; Crux2 construction sites SELF-HEALED via
  the strengthened inversion.
- **Consequence:** `hAll`'s `seqSucc sⱼ = cutFormula` (the ∀-axiom succedent IS the cut instance `F(k)`) is
  now derivable from `zKValid` + `zDerivation_zAxAll_inv`.

### 3. `fdf5389` — THE SHARED BLOCKER pinned + flag banked
Reading the consumption site `ZDerivation_iRKcCrit_of_zKValid` (`Crux2Blueprint:584-663`): the
hypothesis-free `botOrbit'` (lap-130 step 6) must DERIVE both `hAll` and `hNeg`, and **BOTH bundles require
`Seq (seqAnt sⱼ)`/`Seq (seqAnt sᵢ)`** of the chain redex premise nodes (`sⱼ = fstIdx (znth ds redexJ)`,
`sᵢ = fstIdx (znth ds redexI)`) — not just the exact-shape equalities. So even `hAll` (exact-shape now done)
is NOT fully dischargeable: its `Seq (seqAnt sⱼ)` half is missing. `Seq (seqAnt s)` is NOT a `ZDerivation`
consequence (`zAtom`/`zAx1` base disjuncts never record it; `seqAnt q := π₁ q` is not structurally a `Seq`,
`InternalZ:967`). LANDED building block **`seqAntSeqFlag s := if Seq (seqAnt s) then 0 else 1`** +
`seqAntSeqFlagDef`/`_defined`/`_definable`/`_eq_zero_iff` (`Zsubst.lean` after `seqWffFlag`) — the per-node
`Seq` flag the eventual derivation-fold maxes over.

## NEXT (turnkey, in order — full detail in `PENDING_WORK.md` lap 131)
1. **The `seqAntSeq` fold (THE shared blocker — do this FIRST; unblocks BOTH hAll's `Seq` half and hNeg):**
   mirror `ZFresh`'s `freshFlag` machinery (lap 126/127) — a `seqAntSeqFold d` that maxes
   `seqAntSeqFlag (fstIdx ·)` over the derivation tree (= 0 ⟹ every node antecedent is a `Seq`), proved
   preserved by `red`/`iRcrit` (each rule's antecedent op preserves `Seq` — `Seq_seqAnt_seqAddAnt`
   `InternalZ:1057` exists), threaded into `ZDerivesEmptyR` like `ZFresh`/`ZRegular`. ⊥-orbit ROOT
   `seqAnt s = ∅` (Seq trivial) ⟹ fold gives `Seq (seqAnt)` for every chain premise ⟹ supplies
   `hSeqsj`/`hSeqsi`.
2. **zIneg exact-shape (the seqCons equality), once the fold lands:** re-apply the `zInegAntWff` half of the
   WIP diff; discharge `ZDerivation_zsubst`'s zIneg obligation via `fvSubstSeq_seqCons hSeq` (`hSeq` from the
   fold; `fvSubstSeq_seq` makes the substituted antecedent a `Seq` automatically — produced node
   self-satisfies the flag).
3. ⟹ hypothesis-free `ZDerivation_iRKcCrit_botOrbit'` (derive `hAll`/`hNeg` from the orbit); LEFT soundness
   real; tag-4 RIGHT recursion (`iord_descent_red` chain-REPLACE IH) + `false_of_ZDerivesEmpty` PRWO wiring
   remain.

## Backed-up WIP
`scratchpad/lap131-zphi-strengthening-WIP.diff` — the full both-disjunct ZPhi ripple (zAxAll half now landed
clean in `src/`; the zIneg half is the diff's residual, gated on the Seq fold per step 2).

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open work is engine-internal invariant-fold + ZPhi surgery, not a portable lemma.

## Pointers
`STATUS.md` · `PENDING_WORK.md` lap 131 (top, full plan) · zAxAll strengthening:
`InternalZ.lean` (`ZPhi`/`zphi_iff`/`zblueprint`/`zDerivation_zAxAll_inv`) + `Zsubst.lean` (zAxAll branch of
`ZDerivation_zsubst`) · substrate `fvSubstSeq_seqCons` + flag `seqAntSeqFlag`: `Zsubst.lean` · consumption
site (the `Seq(seqAnt)`-needing bundles): `Crux2Blueprint.lean` `ZDerivation_iRKcCrit_of_zKValid:575`,
`ZDerivation_iRKcCrit_botOrbit:648`.
