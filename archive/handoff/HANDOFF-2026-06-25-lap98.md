# HANDOFF — lap 98 · FRESH-MIND REVIEW (direction CONFIRMED) + I∀ conclusion-tracking banked

> Branch `plan`. HEAD = `1008903`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs).
> Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (bare `sorry`;
> the lone math residual `sorryAx` = crux-2). `peano_not_proves_consistency` clean. New
> `red_zIall_tpReduce` axiom-clean (`[propext, Classical.choice, Quot.sound]`).

## ⭐⭐ REVIEW VERDICT: direction CONFIRMED — and the seductive alternative was REFUTED, not assumed
The fresh-mind pass re-litigated the obvious-looking shortcut and killed it:
- **Empirical fact (real `#print axioms`):** `peano_not_proves_TI` (Thm 5.6) IS axiom-clean, and
  `DescentSemantic.peano_not_proves_goodstein_modulo_semantic` is clean **modulo ONE `sorry`** —
  `DescentSemantic.lean:582` (the β-descent). That *looks* like "one sorry from a clean headline via the
  semantic/TI route" — far closer than crux-2's 7 sorries.
- **It is a TRAP.** The lap-45/46 machine-checked finding (`Grz.not_dominated_of_diag_le`) stands:
  Rathjen §3's slow-down is **primrec-only**; a free-`X` (oracle) descent is **not** primrec-dominated, so
  that `sorry` asks for the *impossible*. `peano_not_proves_goodstein_modulo_semantic` is a **vestigial
  theorem built on a FALSE sorry** — wiring it to the headline would be fraud (and free-X-TI ⊢ PRWO is the
  wrong direction anyway). The PRWO descent IS internally primrec ⟹ **crux-2 (Gentzen `PRWO→Con`) is the
  only honest route.** No drift: the lap-46 pivot to crux-2 was correct.
- **HOUSEKEEPING (deferred, harmless):** `DescentSemantic.lean:582` + `peano_not_proves_goodstein_modulo_semantic`
  are off-path/built-on-a-dead-sorry → candidates for `wip/`. Not moved this lap (disclosed sorries, not
  blocking; `Crux2Blueprint` is the live front). Do it on a tidy-up lap.

## ✅ LANDED lap 98 (green 1325, axiom-clean): `red_zIall_tpReduce` (`Zsubst.lean`, after `red_zK_rep_nonchain`)
The **I∀ conclusion-tracking** lemma — the sibling of the proved I¬ `red_zIneg_tpReduce` (`InternalZ.lean:7521`):
```
red_zIall_tpReduce (hZ : ZDerivation (zIall s a p d0))
  (hpfresh : fvSubst ℒₒᵣ a (numeral 0) p = p)              -- a ∉ FV(matrix)
  (hΓfresh : fvSubstSeq a (numeral 0) (seqAnt s) = seqAnt s) -- a ∉ FV(Γ)
  : fstIdx (red (zIall s a p d0)) = tpReduce (tp (zIall s a p d0)) (fstIdx (zIall s a p d0)) 0
```
Certifies the lap-97 eigensubst reduct `red (zIall …) = zsubst d0 a 0` carries EXACTLY the reduced
end-sequent `Γ→F(0)` (= `tpReduce (R_∀xF) Π 0`). The two freshness hyps are the O3 eigenvariable
condition (`a` fresh in matrix + antecedent) — supplied on the ⊥-orbit by the embedding's fresh-eigenvariable
choice. Proof = `fstIdx_zsubst` + `fvSubst_substs1`/`termFvSubst_fvar_self` + the two freshness rewrites.

## ✅ LANDED lap 98 (2nd brick, green 1325, axiom-clean): `isChainInf_seqUpdate_reduceR` (`InternalZ.lean`, after `chainAnt_seqUpdate_of_ne`)
The **R-rule `isChainInf` succedent-reduction** — the Buchholz Thm 3.4(b) chain-validity core for the
I∀/I¬ non-`Rep` replace. Given the selected premise `i` carries the conclusion succedent (so it serves as
the distinguished `j₀`), replacing it by a reduct `v` with same antecedent + reduced succedent (matching
the reduced conclusion `s'`) preserves `isChainInf` — taking `j₀ = i`. Threading/rank up to `i` are
caller-supplied (from `zKValidF` + the permIdx/criticality selection forcing `i ≤ j₀`). This is the
genuine cut-elimination step the keep-`Π` `isChainInf_seqUpdate` cannot do.

## ⭐ KEY DESIGN FINDING (lap 98): the non-`Rep` replace splits into THREE cases (R-rule / L-rule / Rep)
Reading Buchholz Def 3.2 5.2.2 + the repo's `tp`/`red`/`tpReduce` carefully: the conclusion-tracking is
**node-type-dependent** (NOT uniformly `tpReduce (tp d)` — the repo hardcodes `tp(zK)=isymRep`, and `red`
of an axiom is the identity). The replace branch goal `ZDerivation (zK (tpReduce (tp dᵢ) s 0) r (seqUpdate
ds i (red dᵢ)))` decomposes by the selected premise `dᵢ`'s tp:
- **Rep (atom/Ind, `tp dᵢ = isymRep`):** `tpReduce isymRep s 0 = s` → conclusion KEPT. **DONE** —
  `ZDerivation_red_zK_replace` (`Crux2Blueprint:146`) via `iCritAux`/`isChainInf_seqUpdate`.
- **R-rule (I∀/I¬, `tp dᵢ = isymR …`):** conclusion succedent AND premise-`i` succedent reduce in lockstep
  (`∀xF→F(0)`); antecedents unchanged; `i = j₀` (Buchholz: an I-rule premise has `Aᵢ = C` only at `j₀`).
  Pieces NOW BANKED: `red_zIall_tpReduce` (premise conclusion-tracking) + `isChainInf_seqUpdate_reduceR`
  (the chain-validity core). **REMAINING:** assemble them into `ZDerivation` via `zDerivation_zK_intro`
  (need: the off-`i` premise wff conjuncts of `zKValidF` transfer unchanged + `red dᵢ`'s own
  permissibility `iperm (tp (red dᵢ)) (fstIdx (red dᵢ))`; derive `hthread`/`hrank` up to `i` from the
  parent `isChainInf` + the I-rule selection `i = j₀`). I¬ analogue: `red_zIneg_tpReduce` already exists.
- **L-rule (axiom, `tp dᵢ = isymLk …`):** `red dᵢ = dᵢ` (identity) so `seqUpdate ds i (red dᵢ) = ds`
  (premises UNCHANGED); the conclusion gains the instance `F(k)` in its ANTECEDENT
  (`tpReduce (isymLk) s 0 = seqAddAnt F(k) s`). So the goal is `ZDerivation (zK (seqAddAnt F(k) s) r ds)`
  from `ZDerivation (zK s r ds)` — a pure **conclusion-antecedent weakening** (the threading B-condition
  only gets EASIER with more antecedent formulas). **REMAINING:** an `isChainInf`/`zKValidF`
  antecedent-weakening lemma (`isChainInf s r ds → isChainInf (seqAddAnt A s) r ds`, monotone in seqAnt).
  Likely the cleanest of the three. NOTE: must confirm an axiom CAN be hereditarily selected (needs
  `A ∈ Γ`; possible for premises `i>0` whose antecedent carries prior cut-formulas).

## ▶ THE FRONTIER, decomposed (the genuine cut-elimination core)
`ZDerivation_red_zK` (`Crux2Blueprint.lean:190`) has TWO open replace branches (lines 206, 214). After
`rw [red_zK_rep …]` / `red_zK_rep_nonchain` both have the SAME goal:
```
ZDerivation (zK (tpReduce (tp dᵢ) s 0) r (seqUpdate ds i (red dᵢ)))   -- i = permIdx, dᵢ = znth ds i
```
The Rep case (`tp dᵢ = isymRep`) is PROVED (`ZDerivation_red_zK_replace`, via `iCritAux` since
`tpReduce isymRep = id`). The OPEN part is the **non-Rep selected premise** (I∀/I¬/axiom), where the
conclusion genuinely reduces. Two missing pieces, in order:

1. **Conclusion-tracking, all node types** (this lap did I∀; I¬ exists as `red_zIneg_tpReduce`):
   `fstIdx (red dᵢ) = tpReduce (tp dᵢ) (fstIdx dᵢ) 0` for dᵢ ∈ {atom (id, Rep), Ind (id, Rep, via
   `red_rep_of_tp_isymRep`), I∀ (✅ `red_zIall_tpReduce`), I¬ (✅ `red_zIneg_tpReduce`), axAll/axNeg
   (`red = dᵢ`, `tpReduce_isymLk_*`)}. Remaining: the axiom cases (small, `red_zAxAll/Neg = id` + the
   `tpReduce_isymLk_*` end-sequent match — likely an analogous freshness-free brick).
2. **THE NUT — the conclusion-reducing chain-validity constructor.** Build `ZDerivation_iCritReplaceReduce_of`
   (analogue of `ZDerivation_iCritAux_of`, `InternalZ.lean:5387`, but conclusion REDUCED not preserved):
   given `v = red dᵢ` derives `tpReduce (tp dᵢ) (fstIdx dᵢ) 0`, the original chain valid, produce
   `ZDerivation (zK (tpReduce (tp dᵢ) s 0) r (seqUpdate ds i v))`. The content = `isChainInf (tpReduce (tp
   dᵢ) s 0) r (seqUpdate ds i v)` — reducing BOTH the conclusion succedent (`s → F(0)`) and premise i's
   succedent in lockstep (Buchholz Thm 3.4(b)). The existing `isChainInf_congr`/`isChainInf_seqUpdate`
   handle the conclusion-PRESERVING case; this needs the succedent-reduction tracked through `chainAsucc`/
   `chainAnt`. This is the deep, multi-lap residual — start here next.
3. Then: motive-strengthen `redSoundGen` (`Crux2Blueprint.lean:226`) so its IH carries the conclusion-tracking
   (1) per premise (currently `hred` gives only `ZDerivation (red dᵢ)`), feeding (2) into the replace branches.

Downstream (after `ZDerivation_red_zK`): `zKValidF_iIndReduct_of_zInd` (Ind validity), `iord_descent_red`
(ordinal descent over `red`, assemble banked per-branch descents incl. lap-96 `iord_zsubst`),
`foundation_bot_to_Z_empty` (M2 embedding), `false_of_ZDerivesEmpty` (M3 = PRWO well-foundedness), then
wire `goodstein_implies_consistency` → headline.

## Aristotle
Idle — CORRECT. The crux-2 residuals are deeply entangled with the 7777-line `InternalZ` API
(isChainInf/zKValidF/tpReduce/seqUpdate), not self-contained mathlib-style lemmas; inlining hundreds of
defs for a remote prover is not productive. No genuinely-Aristotle-suitable open lemma exists right now.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `#print axioms peano_not_proves_goodstein` =
`[propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms). `red_zIall_tpReduce` axiom-clean.
