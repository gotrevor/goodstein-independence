# HANDOFF — lap 116 · corrected critical-cut inversion PROVEN end-to-end (sound + descends); gate = engine re-key

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint unchanged: `peano_not_proves_goodstein` (Statement.lean:22) is still a literal
> `sorry` — the multi-year target, reduced through crux-2. 8 sorries in `Crux2Blueprint`, unchanged count.
> All new lemmas this lap are axiom-clean `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

## ⭐⭐⭐ THE ADVANCE — the corrected critical-cut inversion is now PROVEN both ways
The lap-114 crux finding (the engine's reduct `ρ = zAxReduct ∘ red` is UNSOUND — instance-`0` not `k` at
the I∀ redex; wrong sequent/payload at the axAll redex) is **fully resolved**. The genuine re-principalized
reduct `critReductCorr` (InternalZ, after `red` ~7313) is proven both SOUND and DESCENDING, axiom-clean:

- **SOUNDNESS** `ZDerivation_iRcritG_critReductCorr` (Crux2Blueprint): `ZDerivation (iRcritG d (critReductCorr d))`.
- **DESCENT** `iord_descent_iRcritG_critReductCorr` (RedZKDescent): `icmp (iord (iRcritG d (critReductCorr d)))
  (iord (zK s r ds)) = 0`.

`critReductCorr d n` emits at the two redexes (else = engine `zAxReduct (red dₙ)`, irrelevant since `iRcritG`
reads ρ only at `redexI`/`redexJ`):
- R-redex (I∀): `zsubst (zIallPrem dᵢ) (zIallEig dᵢ) (numeral k)` — re-principalized at `k = π₁(π₂(tp dⱼ))`.
- L-redex (axAll): `zAx1 (seqAddAnt (cutFormula d) (fstIdx dⱼ)) (cutFormula d)` — the §5 logical axiom `Ax^1`.

### What got built this lap (all banked, axiom-clean, reusable)
1. **`zAx1` is now a `ZDerivation`** — added the 8th `ZPhi` disjunct `(∃ s C, d = zAx1 s C ∧ inAnt (seqSucc s)
   (seqAnt s))`; full ~64-site `rcases zDerivation_iff.mp` ripple + σ/π `zblueprint` arithmetization green.
   New `zDerivation_zAx1_intro`/`_inv`, `red_zK_fixpoint_of_zAx1_selected`.
2. **O-L1 + O-L2 discharged** inside `ZDerivation_corrected_haux1` — it no longer assumes the opaque
   `hZredL`/`hci`; derives them from concrete §5 facts `hSeqsj : Seq (seqAnt sⱼ)`, `hsj : seqSucc sⱼ =
   cutFormula d`. O-L1 via `zDerivation_zAx1_intro`; O-L2 via new `isChainInf_growAnt` (InternalZ) — interior
   antecedent-growth replace keeping the SAME tip `j0` (the §5 reduct preserves the axAll succedent).
3. **`ZDerivation_iRcritG_corrected`** (abstract ρ + `hρI`/`hρJ`) — the soundness core; feeds corrected
   haux0/haux1 to `ZDerivation_iRcritG_of`.
4. **`critReductCorr`** + the soundness/descent specializations (1605915, fada0a6, 9fd70b9).
5. Descent bundles: **`iRedDescent_zsubst_zIall`** (R, instance-invariant via `iotil_zsubst`/`idg_zsubst`,
   NO regularity) + **`iRedDescent_zAx1_zAxAll_of_irk`** (L, `oAtom1 (cutFormula d)` one rank below `∀p`,
   sequent/payload-general). Assembly via `iord_descent_iRcrit_of_chain'` (reads ρ only through 6 bundle facts).

## 🎯 NEXT-LAP TARGET (hardest-first) — the engine re-key, the LAST inversion step
Make the engine `red` actually EMIT `critReductCorr` at critical chains. Self-contained refactor; it WILL
break the build mid-way (def + arithmetization must change together), so do it as one unit:
1. **`critReductCorr` 𝚺₁-definability** (`critReductCorrDef` + instance). Every accessor is already definable
   (`zIallPrem`/`zIallEig`/`cutFormula`/`redexI`/`redexJ`/`zAxReduct`/`red`); only the two `if`-tests +
   branches to encode. Mirror `iRKcDef` (InternalZ:6662).
2. **Re-key `iRKc`** (InternalZ:6656) from `iRcritG d (fun idx => zAxReduct (znth s …))` to
   `iRcritG d (critReductCorr d)`; rewrite `iRKcDef` to match.
3. `red_zK_crit` (InternalZ:7304) then gives `red (zK s r ds) = iRcritG (zK s r ds) (critReductCorr (zK s r ds))`.
4. Close `ZDerivation_red_zK_crit` (Crux2Blueprint:~280 sorry, false-as-stated under current ρ) by
   `red_zK_crit` + `ZDerivation_iRcritG_critReductCorr`; re-point `iord_descent_red_zK_crit` to
   `iord_descent_iRcritG_critReductCorr`. Orbit hyps (`hdi`/`hdj`/freshness/threading/`hirk`/`hsj`/`hrankI`)
   come from the strengthened `redZKReady` motive at the call site.

**CAVEAT:** both corrected theorems handle ONLY the **I∀ R-redex** (`hdi : zIall`). The **I¬ R-redex**
(`zIneg`, the axNeg succedent-replacement) is a separate sub-case, still the `sorry` at Crux2Blueprint:~521.
The engine descent `iord_descent_red_zK_crit` IS unconditional (handles both via the engine bundle); the
corrected versions will need an I¬ analogue (`cutFormula_neg`, `red_zIneg = d0`) before fully replacing it.

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, 8 sorries — by NAME)
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` (THE prize; corrected reduct now sound+descends,
remaining = engine re-key + I¬ sub-case) · axNeg replace validity (I¬ L-redex) · `ZDerivation_red_zK_splice`
· redZKReady-class · `iord_descent_red` chain-REPLACE IH · M2 `foundation_bot_to_Z_empty` · M3
`false_of_ZDerivesEmpty`. (`ZDerivation_corrected_haux0`/`_haux1`, `ZDerivation_iRcritG_corrected`,
`ZDerivation_iRcritG_critReductCorr`, `iord_descent_iRcritG_critReductCorr` are all sorry-FREE; not yet wired
into `ZDerivation_red_zK_crit` pending the engine re-key.)

## Aristotle
Idle — CORRECT. The next piece (engine re-key + `critReductCorrDef` arithmetization) is an engine-internal
change, not an isolated open lemma.
