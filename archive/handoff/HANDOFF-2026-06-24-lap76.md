# HANDOFF — lap 76 · `ZDerivation_zsubst` lands (6/7 cases); zK is the sole remaining hole

> Supersedes prior HANDOFF-*.md. Read order: this file → `PENDING_WORK.md` → `STATUS.md`.
> Branch `plan`. Build 🟢 green `lake build GoodsteinPA` = **1323 jobs**. Tree clean.
> HEAD `73e5f90`. (3 commits this lap from `9d3f554`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**. Sole math
wall = crux 2, localized to `RedSound` (`InternalZ.lean`). Ladder: rung 0.5 ✅ → **rung 1 `zsubst`**
(now 6/7) → rung 2 (Ind reduct) → rung 3 (K/cut reduct) → rung 4 (dispatch).

## ✅ THIS LAP (3 commits)
1. **`bc175db` rung-1 step A** — term commutation helpers in `Zsubst.lean`: `termFvSubst_qqAdd`,
   `termFvSubst_numeral`, `termFvSubst_succVar` (`Sa = ^&e ^+ numeral 1` fixed when `e ≠ a`),
   `isSemiterm_succVar`. (The lap-75 scratchpad WIP draft had been lost; rederived clean.)
2. **`7666224` rung-1 step B** — `zIndWff` now carries `IsSemiterm ℒₒᵣ 0 (zIndTerm d)` (+ matching
   `!(isSemiterm ℒₒᵣ).sigma/.pi 0 t` in `zIndWffDef`). Unary predicate ⟹ only `zIndWff_defined`
   re-proved (simp absorbed it); **no ZPhi cascade** (lap-71 finding confirmed).
3. **`73e5f90` rung-1 step C** — `theorem ZDerivation_zsubst {a t} (ht : IsSemiterm ℒₒᵣ 0 t) : ∀ d,
   ZDerivation d → d ≤ a → ZDerivation (zsubst d a t)` in `Zsubst.lean` (end of file). Proved by
   `zDerivation_induction` (motive Δ₁ via `definability`; uses `ZDerivation_definable` + `zsubst_definable`).
   **atom / zIall / zIneg / zInd / zAxAll / zAxNeg = FULLY PROVEN.** The recipe worked exactly as the
   lap-75 plan predicted: each `ZPhi`-disjunct rebuilt on substituted data via `seqSucc_fvSubstSeqt` /
   `seqAnt_fvSubstSeqt` / `fstIdx_zsubst` + the `fvSubst_*` commutations. The **zInd** case threads all
   six `zIndWff` conjuncts through `fvSubst_substs1` (`numeral 0` / `Sa` / `zIndTerm`) + step-A helpers
   + step-B closedness; eigenvariable `e = π₁ at' ≤ at' < zInd ≤ a ⟹ e ≠ a` discharges freshness.

## ⚠ DESIGN DECISION required before zK can close (lap-76 finding — read PENDING_WORK ⭐ box)
Proving `iperm_tp_zsubst` surfaced a real obstruction: the `zKValid` **criticality** conjunct
`¬ iperm (tp dᵢ) s` does NOT transfer under `fvSubst` (it's not injective on formulas — a critical
chain `^∀F(^&a) ≠ seqSucc s` can collapse to non-critical when `seqSucc s = ^∀F(t)`). And criticality
is **load-bearing**: it powers the lap-70 `not_zKValid_iCritReduct` Option-B refutation
(`InternalZ.lean:4961-4975`), so it can't simply be dropped from `zKValid`. Even `a ∉ FV(s)` alone is
insufficient (the premise principal formula can also carry `^&a`). So the zK case needs a *carefully
chosen* freshness hypothesis on `ZDerivation_zsubst` — best decided fresh. Bricks already landed that the
fix will reuse: `irk_fvSubst`, `iperm_tp_zsubst` (positive), `zTag_zsubst`. The 6 proven cases are
freshness-agnostic, so adding a hypothesis they ignore costs nothing there.

## ▶ NEXT — resolve the design decision, then the **zK case** (`Zsubst.lean`, the lone `sorry`)
Goal in that branch (after `intro hda`): `ZDerivation (zsubst (zK s r ds) a t)`.
`zsubst_zK` gives `zsubst (zK s r ds) a t = zK (fvSubstSeqt a t s) r (tblMapSeq (zsubstTable a t (zK s r ds - 1)) ds)`.
Build via `zDerivation_iff.mpr (Or.inr^4 (Or.inl ⟨s', r, ds', rfl, ?Seq, ?mem, ?valid⟩))` with
`s' = fvSubstSeqt a t s`, `ds' = tblMapSeq (zsubstTable a t (zK s r ds - 1)) ds`:
- **`?Seq`**: `Seq ds'` — `tblMapSeq_seq` (simp, already have).
- **`?mem`** (per-premise IH): `∀ i < lh ds', ZDerivation (znth ds' i)`. `lh ds' = lh ds` (`tblMapSeq_lh`).
  `znth ds' i = znth (zsubstTable a t (zK s r ds - 1)) (znth ds i)` (`znth_tblMapSeq`, hi: i < lh ds).
  Then `= zsubst (znth ds i) a t` via `znth_zsubstTable_eq_zsubst a t _ (znth ds i) (?le)` where
  `?le : znth ds i ≤ zK s r ds - 1` (`znth ds i < ds < zK ⟹ ≤ pred`; mirror `zsubst_zK`'s own bound,
  `le_pred_of_lt` + `lt_of_mem_rng`/`ds_lt_zK`). Finally IH: `(hC (znth ds i) (hmem i hi)).2 (?lea)`
  with `?lea : znth ds i ≤ a` (`< ds < zK ≤ a`). [`hmem i hi : znth ds i ∈ C`.]
- **`?valid`** (THE WALL): `zKValid (fvSubstSeqt a t s) r ds'` from `hvalid : zKValid s r ds`. This is
  `zKValid` **subst-invariance**: the chain truth/permissibility data (`tp`/`iperm`/`isChainInf`,
  the §8.1 critical-pair structure) must transfer under `^&a ↦ t`. Likely needs **new lemmas**
  `tp_fvSubst` / `iperm`-invariance / `isChainInf`-invariance (`grep -rl zKValid` + unfold it first to
  see exactly which projections appear). This is genuine ordinal-analysis content, NOT mechanical —
  budget a full sub-lap; if it resists ~3 honest tries, keep the `sorry` and start **rung 2** in
  parallel (the Ind reduct `K^{rk F}⟨d0, d1(a/0),…,d1(a/k−1)⟩` now CAN consume the proven
  `ZDerivation_zsubst` for the substituted minor-premise copies `d1(a/ν)` — that's the whole point of
  rung 1).

## Parallel / second front
`PA_delta1Definable` (Foundation `Incompleteness/Examples.lean:17`, still an `axiom`) — mandatory for
axiom-free. Advance when the ladder blocks.

ANTI-FRAUD: discharge `Reduction.lean:66` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound`/`zKValid` or fake the reduct.
