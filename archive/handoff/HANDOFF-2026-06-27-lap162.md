# Handoff: lap 162 — ⭐ ex-falso ROUTE 4 REFUTED in-kernel; ROUTE 2 (`zAxBot`) BUILT + obligation isolated

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `acbaa06` · build 🟢 1326 (`lake build GoodsteinPA`, exit 0).
`false_of_ZDerivesEmpty` = `[propext, sorryAx, choice, Quot.sound]` (0 math axioms, no drift, re-verified
in-kernel). **Three green commits** this lap: `c7f09bc` (refute route 4) → `74af3f8` (zAxBot constructor +
projections) → `acbaa06` (exFalsoClose real cert, obligation isolated).

## Lap-162 net: ex-falso went from "design wall" → "ONE precisely-stated, kernel-verified-to-suffice intro lemma"
- **Refuted** the lap-161-mandated ROUTE 4 in-kernel (`wip/ExFalsoOrdinalSpike.lean`, exit 0): an R-intro tower's
  `iotil ≥ 1` can't strict-drop below a degenerate ⊥-exit chain's `iseqNaddIdg = 1`; iord-weaken breaks the splice;
  relaxing zAtom corrupts threading. So a NEW `iotil=0` ⊥-left leaf `zAxBot` (route 2) is required.
- **Built** the `zAxBot` (tag-8) constructor + full projection table + `exFalsoClose` as a REAL `certReplace` cert.
- **Isolated** the entire ⊥-exit ex-falso to the single sorried intro `zDerivation_zAxBot` (`Crux2Blueprint:~3367`):
  `inAnt (^⊥) (seqAnt s) → ZDerivation (zAxBot s)`. **That sorry IS the ZPhi disjunct #9** — discharge it next.

## TL;DR
The lap-161 mandate said: build `exFalsoClose` (`Crux2Blueprint:3477`) by **ROUTE 4** (an R-intro tower over the
succedent `C`), calling it "pure ASSEMBLY, infra all present". **That is REFUTED in-kernel this lap.** The ex-falso
reduct must have `iotil = 0`, which NO existing constructor provides for `⊥∈Γ, C∉Γ`. So the directive's own §2
fallback — a **NEW `iotil=0` ⊥-left leaf `zAxBot` (tag 8)** — is REQUIRED. It is a 2-3 lap structural extension,
not a one-lap assembly.

## The proof (all in `wip/ExFalsoOrdinalSpike.lean`, exit 0)
- `GenReductCert` via `certReplace` demands `iRedDescent v (zK s r ds)` whose `otil_lt` is the STRICT drop
  `icmp (iotil v) (iotil (zK s r ds)) = 0`, with `iotil (zK s r ds) = iseqNaddIdg ds`.
- **`tower_no_drop`**: a degenerate single-leaf ⊥-exit chain has `iseqNaddIdg ds = ω^0 = 1`
  (`leaf_chain_iseqNaddIdg`), and the cheapest route-4 tower `zIneg s p (zAtom s')` has `iotil = ω^0 = 1`
  (`tower_iotil`) — EQUAL, so `icmp = 1 ≠ 0` (`icmp_self`). The strict drop FAILS. Every R-intro adds `+1`
  (`iotil_zIall`/`iotil_zIneg`), so NO tower escapes (general: `n`-leaf chain ⟹ `iseqNaddIdg = n`; `^∀^k⊥` tower
  ⟹ `iotil = k`; `k` ⟂ `n`, so `k ≥ n` always realizable).
- **`leaf_reduct_drops`**: ANY `iotil=0` leaf (the `zAxBot` template) strict-drops below any chain with
  `iseqNaddIdg ds ≠ 0` (`icmp_zero_pos`, the `leafCloseC` move, unconditional via `hposlast`). ✅ route 2 closes.

## Why the obvious escapes are dead (all checked this lap)
- **Weaken to `iord`-level** (`iord = iotower(iotil)(idg)`; tag-8 leaf has `iord 0`): NO — the terminal ex-falso
  reduct is later SPLICED as a producer-premise cert by `certReplace_of_premise_cert` (`:3317-3318`), which
  propagates the parent's strict `iotil` drop from the per-premise strict `iotil` drop via the `#`-fold
  (`iotil_iCritAux_lt`). An idg-dropping premise doesn't shrink that fold. Reduct genuinely needs `iotil = 0`.
- **Relax `zAtom`'s side condition** (fire on `⊥∈Γ`): NO — corrupts `leastSucc_in_ant_or_nonleaf`
  (`InternalZ:9099`) + `chainAsucc_threaded_of_leaf` (`:9047`): an ex-falso leaf has `zTag=0` but its succedent
  doesn't thread, so BOTH conclusion disjuncts fail. Those are load-bearing in the active crux (`collapse`). So
  `zAxBot` must be a NEW tag.

## 🎯 NEXT — execute ROUTE 2 (full per-step plan in PENDING_WORK lap-162)
**De-risker:** `ioNext` (`InternalZ:2261`) and `idgTable` already DEFAULT unknown tags to `0`, so a tag-8
`zAxBot = ⟪s,8,0⟫+1` auto-gets `iotil = idg = iord = 0` with NO dispatcher edits.
1. ✅ **DONE (lap 162, build 🟢 1326).** `zAxBot` constructor + projection simp lemmas
   (`zTag/fstIdx/tp/iotil/idg/iord/isNF_iotil` + `zReg/zFresh/zSeqAnt` + `zAxBotGraph`/`zAxBot_defined`),
   mirroring `zAtom`. `false_of_ZDerivesEmpty` axioms unchanged, no drift.
1b. ✅ **DONE (lap 162, build 🟢 1326).** `exFalsoClose` (`Crux2Blueprint:3477`) rewritten from a `have`-sorry
   into a REAL `certReplace` cert (mirrors `leafCloseC` with `zAxBot`) — kernel-verifies the step-1 projection
   lemmas. The ex-falso obligation is now ISOLATED to the single top-level sorried intro `zDerivation_zAxBot`
   (`:~3367`): `inAnt (^⊥) (seqAnt s) → ZDerivation (zAxBot s)`. That sorry IS the ZPhi disjunct #9.
2. **⏭ NEXT** — discharge `zDerivation_zAxBot` = the ZPhi disjunct #9 + ripple (was step 2 below).
2. **(atomic)** ZPhi disjunct #9 + the ripple: `ZPhi`(:5458), `zphi_iff`, `zphi_monotone`, `zphi_strong_finite`,
   `zblueprint` Σ/Π (:5559), `zPhi_definable` (:5605, add `qqFalsum`/`inAnt` clause), `zDerivation_zAxBot_inv`,
   intro, + a 9th branch through ~125 `zDerivation_iff.mp` rcases (compiler-driven; new leaf behaves like tag-0/7).
   Precedent: lap-116 added `zAx1` as the 8th disjunct (64-site ripple) in a focused lap.
3. `exFalsoClose := fun h => Or.inl ⟨zAxBot s, …, ⟨zero_le, icmp_zero_pos hposlast, isNF_zero⟩⟩` → DROPS the sub-
   sorry. Then `residual` (:3451) still needs (ii) C-exit R-intro replay + (iii) tag-5/6 thread-escape.

## ⚠️ FORBIDDEN / corrections
- **Do NOT re-attempt ROUTE 4** (R-intro tower over `C`) — REFUTED in-kernel (`tower_no_drop`).
- **Do NOT relax `zAtom`** — corrupts the threading invariants.
- **Do NOT weaken `iRedDescent`/`certReplace` to `iord`** — breaks the `#`-fold splice.
- Everything else FORBIDDEN by DIRECTION.md lap-161 still holds (`red`, `iord`-recursion, `redLeast`, the
  `seqUpdate` splice, axMajor/gDef standalone before the port, off-path dead red-soundness sorries, **M2/M3/M4**).
- ALTITUDE (unchanged): `false_of_ZDerivesEmpty` sorry-free is NOT the headline — `goodstein_implies_consistency`
  (`Reduction.lean:68`) is a bare sorry, M2/M4 bridge ~0%. "Only the crux left" ≠ "almost done."

## For the next ALTITUDE lap
DIRECTION.md CURRENT DIRECTIVE (lap-161) mandates ROUTE 4 as primary; that is now refuted. Promote its §2
`zAxBot` fallback to primary. (Grind laps may not edit the CURRENT DIRECTIVE section.)

---
**→ Next session: execute ROUTE 2 step 1 (the additive `zAxBot` constructor + projections), keep build green.
Obey DIRECTION.md lap-161 EXCEPT route 4 (refuted — use route 2 / `zAxBot`).**
