# Handoff: lap 159 — PORT the genReduct_anySucc spike to src + WIRE the {3,4}-producer core of axMajorResidual

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `a128d4e` · build 🟢 green (1326 jobs,
`lake build GoodsteinPA`). In-kernel (real `#print axioms`): `false_of_ZDerivesEmpty` =
`[propext, sorryAx, choice, Quot.sound]` (**0 math axioms**). No drift. No uncommitted edits.

## What this lap did (executed the DIRECTION.md lap-158 port plan — success metric MET)
DIRECTION.md (binding) success metric: *"a src sorry drops, OR axMajorResidual is decomposed into named src
sub-sorries WITH THE SKELETON WIRED."* This lap met the second clause — and explicitly avoided the
forbidden "banking generalized lemmas without wiring them toward axMajorResidual." Three feat/docs commits:

1. **`b7033f7`** — (a) `certFlatten_of_critHalves` generalized off `seqSucc s = ⊥` → `IsUFormula (seqSucc s)`
   (its only ⊥-dependency was the conclusion's well-formedness, now a `zKValidF` component 8). The FLATTEN
   splice is **succedent-agnostic**. Two call sites pass `by rw [hsucc]; simp`. (b) PORTED the lap-158 spike
   `wip/GenReductAnySucc.lean` to src: the **`genReduct_anySucc` family** — the general-succedent {3,4}-Rep-node
   reduction that is the SINGLE key closing both `axMajorResidual` and `descent_step_K_noncrit_axMajor`:
   - `genReduct_anySucc` (entry, `zDerivation_sigma_induction`) — **PROVEN** off ⊥ (the 𝚺₁ motive
     `GenReductCert` never mentions ⊥; tag-3/4 delegate).
   - `genReduct_anySucc_chain` (chain-step dispatcher: `by_cases` a redex below the exit) — **PROVEN** structure.
   - Three NAMED leaf sorries: `ind_reduct_anySucc`, `genReduct_chain_hasRedex_anySucc`,
     `genReduct_chain_noRedex_anySucc`.

2. **`ca8744e`** — **WIRED `genReduct_anySucc` into `axMajorResidual`.** Relocated the family to BEFORE
   `genReduct_chain_noRedex` (so it's callable). Added `repProducerClose`: a {3,4} (`Rep`) producer `m ≤ j0`
   of the cut formula closes by `genReduct_anySucc (znth ds m)` (any succedent) → `certReplace_of_premise_cert`
   (Γ-general splice). Routed the **four clean {3,4}-producer sites** in `tryProducerClose` (tag-3, tag-4, and
   the two `climb_to_rep_producer`→{3,4} landings) through it. The IRREDUCIBLE {3,4}-producer core of
   `axMajorResidual` — Buchholz Thm 2.1, the heart of crux-2 — is now reduced to the proven `genReduct_anySucc`
   entry + its 3 named leaves. Residual `axMajorResidual` now = ONLY the general-`C` leaf/escape/R-intro closes.

3. **`a128d4e`** — PENDING_WORK update (full next-attack tree at top).

## ⭐ NEW finding (lap 159 — kernel-grounded)
The has-redex ¬-case `keepTip` (`ZDerivation_corrected_haux0_neg_botOrbit`, `Crux2Blueprint:~738`) GENUINELY
needs the ⊥-exit: `by_cases redexJ ≤ j0` → the `> j0` branch (`_keepTip`) replaces the succedent by the cut
formula, so ONLY a ⊥-exit re-validates the rebuilt chain. → `genReduct_chain_hasRedex_anySucc`'s ¬-redex
sub-case is a real deep leaf; its ∀-redex sub-case is a clean port (uses the certFlatten generalization).

## 🎯 NEXT (priority order — in PENDING_WORK lap-159 top)
1. **`genReduct_chain_noRedex_anySucc` body** (load-bearing leaf, `Crux2Blueprint:~3407`). Port
   `genReduct_chain_noRedex`'s machinery (collapse / climb / closeZAxNeg / tryProducerClose / tag dispatch) to
   general `C`. Key insight (lap 159): the Z-rules `zAtom`/`zAxAll`/`zAxNeg` ALL allow ANY succedent, so the
   leaf `C∈Γ`→`zAtom s`, the tag-5 sub-(a) `^∀p'∈Γ`→`zAxAll s p' k'`, and tag-6 `¬q,q∈Γ`→`zAxNeg s q` closes
   GENERALIZE cleanly. Use the SAME `repProducerClose` pattern but with the GENERAL IH (not `genReduct_anySucc`)
   for the {3,4}-producer + tag-5/6→producer threads. `rcases hAj0` on the exit disjunct: the ONLY genuinely
   new sorries are (i) the ⊥-exit leaf (`⊥∈Γ`→`Γ→C` ex-falso — no clean Z-rule) and (ii) tag-1/2 R-intro-of-`C`
   (impossible at a ⊥-exit; new at a C-exit).
2. **`ind_reduct_anySucc`** (`:~3382`) — the lap-136 k-fold unfolding `⟨d0, d1[a:=0..k-1]⟩` (k=⟦t⟧); for a
   CLOSED succedent (`p` closed → vacuous induction) the reduct is `d0`. CHECK `zIndWff` admits a closed-`C` zInd.
3. **`genReduct_chain_hasRedex_anySucc`** (`:~3391`) — ∀-redex: clean port (certFlatten general); ¬-redex:
   the keepTip-off-⊥ deep leaf (see finding above).
4. **Swap `genReduct_botSucc` → `genReduct_anySucc`** (thin wrapper, drop the `hsucc` arg; fix call site
   `descent_step_K_noncrit_repMajor`) and retire the ⊥-stack — DROPS `axMajorResidual` outright once the
   general leaves are proven. Then `descent_step_K_noncrit_axMajor` (`:~3857`) closes the same way via the
   PROVEN cut-partner lemmas (`majorPrem_zAxAll_cutPartner`/`_zAxNeg_cutPartner`, Γ=∅, `InternalZ:9592/9620`).

## ⚠️ Forbidden / binding (DIRECTION.md lap-158 — still in force)
Same-degree `õ`-drop for the {3,4} producer (REFUTED lap 157 — must drop the DEGREE / recurse, which the
genReduct_anySucc recursion now does); `red`; `iord`-recursion (CODE-induction ONLY — the spike confirmed NO
outer degree-induction is needed; the degree headroom is LOCAL to each principal-cut flatten); the refuted
single-premise `seqUpdate` splice (`descent_step_K_splice`); attacking `descent_step_K_noncrit_axMajor`:3857 /
gDef:3980 STANDALONE; the off-path dead `red`-soundness sorries {:80,:1253,:1356,:1529,:1616,:1705,:1867} AS
STATED; M2 / M4 wiring. ALTITUDE: M2 (Foundation→Z bridge) ~0% built — "only the crux left" ≠ "almost done."

## 📁 Key anchors (line numbers approx — file shifts as leaves fill)
- `genReduct_anySucc` (entry, PROVEN) `Crux2Blueprint:~3444`; `genReduct_anySucc_chain` `~3422`.
- The 3 named leaves: `ind_reduct_anySucc` `~3382`, `genReduct_chain_hasRedex_anySucc` `~3391`,
  `genReduct_chain_noRedex_anySucc` `~3407`.
- `genReduct_chain_noRedex` (⊥, the WIRED version) `~3484`; `repProducerClose` inside it `~3537`;
  residual `axMajorResidual` `~3536`; `tryProducerClose` `~3615`.
- `certReplace_of_premise_cert` (Γ-general splice, `hexit` disjunction) `~3283`.
- `genReduct_botSucc` (⊥ entry, to be retired) `~3801`; `descent_step_K_noncrit_repMajor` (proven template) `~3916`.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-158, binding) · `PENDING_WORK.md` lap-159 top · `wip/GenReductAnySucc.lean` (the spike).

---
**→ Next session: prove `genReduct_chain_noRedex_anySucc`'s body (port the ⊥-machinery to general `C`,
closing the {3,4}-producer + tag-5/6 via the general IH; only ⊥-exit-ex-falso + tag-1/2-R-intro are new).
Then retire the ⊥-stack to DROP `axMajorResidual`. Keep build green + 0 math axioms.**
