# HANDOFF — lap 133 · `zSeqAnt` invariant BUILT + `ZSeqAnt_red` PROVEN (the shared `Seq(seqAnt)` blocker)

> **RESUME BATON.** Branch `plan`, HEAD = `03454bb`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`,
> **1326**). Headline footprint UNCHANGED (`peano_not_proves_goodstein` still relies on the crux-2 chain of
> `sorry`s; `src/` is sorry-free in the new code). All five lap-133 commits are additive to `Zsubst.lean`.

## What this lap DID — discharged the single named shared blocker's core + preservation

The lap-132 baton named the **`seqAntSeq` fold** as "the single shared blocker" — `ZDerivation_iRKcCrit_*`'s
per-node bundles `hAll`/`hNeg` need `Seq (seqAnt sⱼ)`/`Seq (seqAnt sᵢ)` of the chain redex premises, which
`ZDerivation` does NOT supply (`ZPhi` records only `inAnt`/membership, `InternalZ:5362`; `seqAnt q := π₁ q`
is not structurally a `Seq`). This lap BUILT the full invariant + its `red`-preservation, all sorry-free:

1. **`zSeqAnt` derivation-fold invariant** (`Zsubst.lean`, after `zFresh_zsubst`) — the `Seq` analogue of
   `zFresh`, threaded additively (NOT in `ZPhi`). `zSeqAntNext`/`zSeqAntTable`/`zSeqAnt` + Δ₁ defs +
   structural correctness + per-tag recursion eqns. **Design (important):** the chain (tag-4) node carries
   NO own flag (exactly `zFresh_zK`); the head flag `seqAntSeqFlag (fstIdx d)` fires only at tags ≠ 4. This
   was a deliberate REFACTOR (commit `03454bb`) away from the first design (flag-every-node) — flagging
   K-nodes forced reduct-conclusion `Seq` obligations and broke the `ZFresh_red` mirror.
2. **Extraction:** `seq_seqAnt_zK_premise` (THE `Seq (seqAnt sⱼ)` supplier `hAll`/`hNeg` consume — needs the
   premise be a `ZDerivation` with `zTag ≠ 4`; redex premises are tags 1/2/5/6, always non-chain),
   `seq_seqAnt_of_zSeqAnt` (own-node, guarded `zTag d ≠ 4`), `zSeqAnt_zK_of`/`_zK_premise_zero`,
   `zDerivation_pos`.
3. **`zSeqAnt_zsubst`** — `zsubst` ALWAYS yields antecedent-`Seq` derivations UNCONDITIONALLY (every node
   concludes `fvSubstSeqt a t (·)`, antecedent = `fvSubstSeq` image = always a `Seq`); the I∀-reduct step.
4. **`ZSeqAnt_red` — FULL THEOREM** (`red` preserves `ZSeqAnt`). Almost line-for-line mirror of the proven
   `ZFresh_red`: `ZSeqAnt_red_of_not_zK` (structural+Ind) + the zK dispatch (`ZSeqAnt_zAxReduct` —the one
   genuine delta, `Seq` from the axiom premise's own head flag— `ZSeqAnt_zK_of_seqUpdate`/`_iCritReductSeq`/
   `_seqInsert`, `ZSeqAnt_iRcritG_premise`, `ZSeqAnt_red_zK_replace`/`_crit`/`_splice`/`_splice_of_chain`,
   `ZSeqAnt_red_zK`, assembled `ZSeqAnt_red`).

## NEXT (turnkey) — thread `ZSeqAnt` into the endgame, then discharge `hAll`/`hNeg`

1. **Add `ZSeqAnt` to `ZDerivesEmptyR`** (`Crux2Blueprint:1103`, `def ZDerivesEmptyR d := ZDerivesEmpty d ∧
   ZRegular d ∧ ZFresh d`) → add `∧ ZSeqAnt d`. Then update **`ZDerivesEmptyR_red`** (`Crux2Blueprint:1267`)
   to also produce `ZSeqAnt (red d)` via the now-banked **`ZSeqAnt_red d h.1.1 h.2.2.2`** (mirror the
   `ZFresh_red` slot exactly). Ripple is mechanical: `ZDerivesEmptyR_red_iterate` is unchanged in shape;
   M2 `foundation_bot_to_Z_empty` (`:1302`, already `sorry`) gains a `ZSeqAnt` conjunct in its `∃ z` — free,
   it's still a sorry. Check `ZDerivesEmptyR`'s other consumers compile (grep `.2.1`/`.2.2` projections;
   the new tuple is `⟨_, _, _, _⟩` so 2-3 destructures may need a `.2.2.1`→`.2.2` shift — small).
2. **Discharge the `Seq` parts of `hAll`/`hNeg`** in `ZDerivation_iRKcCrit_botOrbit` (`Crux2Blueprint:648`).
   With `ZSeqAnt (zK s r ds)` now in hand (from `ZDerivesEmptyR`), supply `Seq (seqAnt sⱼ)` (the ∀ axAll
   premise, redexJ) and `Seq (seqAnt sᵢ)` (the I¬ premise, redexI) via `seq_seqAnt_zK_premise hds hsa hi
   hZprem hnK` — the premises are `zAxAll sⱼ pj k'` (tag 5) and `zIneg sᵢ p d0` (tag 2), both non-chain,
   `hZprem` from `(zDerivation_zK_inv hZ).2`, `hnK` from `zTag_zAxAll`/`zTag_zIneg`.
3. **RESIDUAL after that** = the exact-shape ZPhi facts `seqSucc sⱼ = cutFormula` (∀) and
   `seqAnt (fstIdx d0) = seqCons (seqAnt sᵢ) p` (¬) — a SEPARATE blocker (the lap-130/131 ZPhi-strengthening,
   `zAxAllSuccWff`/`zInegAntWff`). NOTE: the `zIneg` half's `Seq (seqAnt sᵢ)` precondition (lap-131's pinned
   blocker for that strengthening) is NOW SUPPLIABLE from this very fold via `seq_seqAnt_zK_premise` — so the
   `zIneg` ZPhi-strengthening is unblocked on its `Seq`-side. (Decide next lap whether to do the
   ZPhi-strengthening for the engine-swap route, or pivot to the existence form `wip/ExistenceEndgame.lean`
   whose K-critical case `descent_step_Kcrit_of_bundle` consumes the same `hAll`/`hNeg`.)

## Open-sorry inventory (unchanged from lap 132) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` · `no_min_descent_absurd_of_goodstein`
(off-path) · 8 Crux2Blueprint: `zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH · `foundation_bot_to_Z_empty`
(M2) · `false_of_ZDerivesEmpty` (M3). Plus `wip/ExistenceEndgame.lean` (3 spike decls, NOT in build).

## Aristotle
Idle — CORRECT. The open work is in-repo plumbing against the `InternalZ`/`iord` substrate Aristotle can't see.

## Pointers
New code: `Zsubst.lean` — `seqAntSeqFlag` (`:1561`, banked lap131), `zSeqAntNext`..`zSeqAnt_zK_of`
(`:1983-2200`), `zSeqAnt_zsubst` (`~:2230`), `ZSeqAnt_red_of_not_zK` (`~:2255`), the `ZSeqAnt_red` family
(after `ZFresh_red`, `~:3100-3270`). Endgame surface: `Crux2Blueprint.lean:648` (`ZDerivation_iRKcCrit_botOrbit`),
`:1103` (`ZDerivesEmptyR`), `:1267` (`ZDerivesEmptyR_red`). `PENDING_WORK.md` lap-133 box. Prior baton
`HANDOFF-2026-06-26-lap132.md` (existence-form reframe + decision gate).
