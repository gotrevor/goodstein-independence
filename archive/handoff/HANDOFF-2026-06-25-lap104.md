# HANDOFF — lap 104 · FRESH-MIND REVIEW + in-kernel endgame CORRECTION

> Branch `plan`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). `src/` UNTOUCHED — headline footprint
> intact, re-verified in-kernel (`lake env lean`): `peano_not_proves_goodstein = [propext, sorryAx, choice,
> Quot.sound]` (0 math axioms, lone `sorryAx` = crux-2), `peano_not_proves_consistency` clean,
> `not_proves_of_implies_consistency` clean. All new work is in `wip/PathCOmega.lean` (NOT imported by
> `GoodsteinPA.lean`), every new decl axiom-clean `[propext, Classical.choice, Quot.sound]`.

## What this lap is (a review lap, not a grind lap)

Fresh-mind strategic pass. Reconstructed the goal, took the real axiom inventory, validated the route, and —
the substantive deliverable — **found and proved in-kernel a correctness flaw in lap 103's endgame
packaging**, which redirects the crux-2 attack. STATUS.md / PENDING_WORK.md / NEXT_STEPS.md refreshed.

## The state (unchanged structurally)

Headline `peano_not_proves_goodstein` is a bare `sorry`, clean modulo crux-2. The whole remaining obligation
is `goodstein_implies_consistency` (`Reduction.lean:68`, `sorry`) = crux-1 (γ→PRWO, **DONE axiom-clean lap
57**) ∘ **crux-2** (PRWO(ε₀)→Con(PA), the Gentzen ordinal-analysis girder — the sole 🟡 frontier). Route =
**Path C** (Buchholz operator-controlled ω-derivations with stored ordinals; lap 102 settled the fork). The
endgame: a single-step `red` strictly drops the stored ordinal; iterated on a ⊥-derivation it yields an
infinite ε₀-descent, contradicting crux-1's PRWO(ε₀).

## The correction (this lap's content — all axiom-clean, `wip/PathCOmega.lean`)

Lap 103 reduced crux-2 to `red_iterate_descends {P} (hinv) (hdrop) (hz)` and framed `hinv` (`∀ w, P w → P
(red w)`, the orbit invariant being `red`-closed) as "tractable via premise selection". **That framing is
false** — proven by 4 new lemmas:

- **`zTag_ne_seven_of_ZDerivation`** — every engine `ZDerivation` carries one of the seven engine tags
  `0..6`; never the stored-ω-∀ tag `7`. (The Path-C ω-nodes, tags 7..10, are a parallel layer the engine
  predicate does not recognize.)
- **`red_redAllEx_eq`** `(hd0 : ZDerivation d0)` — the ∀/∃-cut reduct `redAllEx s d0 a Cnew dR` is a
  `red`-FIXPOINT. Its new left premise is `zsubst d0 a (zExTerm dR)`, whose tag `= zTag d0 ≠ 7` (`zTag_zsubst`
  + `zTag_ne_seven_of_ZDerivation`), so the `red` dispatch `(zTag w = 9 ∧ zTag (zCutL w) = 7 ∧ zTag (zCutR w)
  = 10)` fails and `red` is the identity.
- **`sord_red_iterate_stalls_AllEx`** — on a concrete ∀/∃-cut node `w`, `red w` fires once (`redAllEx …`) but
  every further `red` is the identity, so `sord (red^[n+2] w) = sord (red^[n+1] w)`: the stored ordinal is
  CONSTANT from step 1 — NOT an infinite `≺`-descent.
- **`naive_dispatch_P_not_red_closed`** — ANY `P` that holds on the ∀/∃-cut node and implies the `(9,7,10)`
  dispatch shape fails `hinv` on the reduct.

(Helper `iterate_red_fixed` too.) Verified `#print axioms` clean on all four; `lake env lean wip/PathCOmega.lean`
green; `src/` untouched so `lake build GoodsteinPA` still 1325.

## Why (the genuine `hinv` content) + the corrected NEXT

Reducing a cut on `∀x F` gives a smaller cut on `F(t)` whose premises (`zsubst d0 a t` ⊢ `Γ→F(t)`,
`zExPrem dR` ⊢ `Γ→¬F(t)`) need NOT be **principal** nodes for `F(t)`. The naive `red` only fires on premises
that are *literally* stored ω-nodes, so it stalls. To keep the orbit reducible, `red` must **re-principalize**
the reduct's premises via Schütte/Tait **INVERSION** operators (`redInv∀`/`redInv∧`/`redInv∨`: from any
derivation of `Γ, A` extract a derivation of the immediate subformula instance, stored ordinal `≼`).
Inversion is a recursion over the derivation ⟹ it requires the genuine **Path-C derivation predicate (the
datatype)**. So `hinv` = the Hauptsatz (inversion + reduction) — the irreducible deep content of crux-2,
genuinely multi-month.

**Corrected attack (NEXT_STEPS.md PRIORITY 1, rewritten; hardest-first):**
1. Build the datatype `zcOK : V → Prop` (small isolated Σ₁ `Deriv` predicate; template `InternalZ`'s
   `PR.Construction` Fixpoint; rule set `ZinftyF.Deriv`). Node shapes already coded (ω-∀/ω-ind/cut/∃);
   add ∧/∨/atom-axiom. THE bottleneck — `hinv`, the embedding, and arithmetization all need it.
2. Inversion operators over `zcOK` (∀-inv on the ω-∀-node = premise selection, banked; general case recurses
   through the last rule — `Zinfty.lean` §19.2–19.4 is the META template to port).
3. `red` (Buchholz Def 3.2) calling inversion + `hinv` (`red` preserves `zcOK`-of-∅→⊥) + `hdrop` (bricks 1/3
   + inversion ordinal bounds) → `red_iterate_descends` with the GENUINE `P = zcOK ∧ derives ∅→⊥`.
4. Embedding (M2 analogue): a Foundation/`ZDerivation` ⊥-proof → a `zcOK` ⊥-derivation `z` (`hz`).
5. Arithmetize `red` + `gentzenDescentφ` → discharge `gentzen_descent_of_inconsistent` (`wip/GentzenCon.lean`)
   + crux-1 PRWO → wire to `goodstein_implies_consistency` → headline (ONLY when `#print axioms` clean).

**Do NOT** keep adding `hdrop` cut-shape cases (∧/∨/atom) or wiring more dispatch cases into the current
`red` — those are easy leaves built on an `hinv` that can't hold. The lap-103 bricks remain valid; the
endgame *shape* changes (inversion-driven `red`, genuine `P = zcOK`).

## Kernel state (unchanged this lap)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — bare `sorry`, 0 math axioms.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` = `[propext, choice, Quot.sound]` — clean.

## Aristotle
Idle — CORRECT. The Path-C datatype/inversion work is entangled with the `InternalZ`/`Zinfty` API; not a
self-contained mathlib lemma.

## Brick 5 STARTED this lap (the corrected next brick — `wip/PathCOmega.lean`, axiom-clean)

The datatype as a clean Lean **`inductive ZcOK : V → Prop`** (constructors `leaf` [wraps an engine
`ZDerivation`], `omegaAll` [INFINITARY premise family `∀ t, … → ZcOK (zsubst d0 a t)`, strictly positive so
Lean accepts it], `ex`, `cut`; each carries Buchholz operator-control `sord ≺ α`). The inductive-over-`V`
`cases` dependent-elimination wall (the same one the engine hits) is handled the same way: **`zcOK_iff`** —
the `ZcPhi`-disjunction recursion equation, proved by `cases` on a FREE variable (which Lean CAN eliminate)
— is the clean inversion vehicle. Landed node inversions **`zcOK_cut_inv`/`zcOK_omegaAll_inv`/`zcOK_ex_inv`**
(`zTag`-discriminated) + `zTag_ne_nine/ten_of_ZDerivation`. This is the prototype on which inversion
(`redInv∀`/…), `red`, `hinv`, `hdrop` get developed; the Σ₁-`Fixpoint` port (so the descent is V-internal
for PRWO) is the deferred final brick.

## Brick 5b ALSO landed this lap (principal ∀/∃-cut `hinv`, split — `wip/PathCOmega.lean`, axiom-clean)

`hinv` for the PRINCIPAL ∀/∃-cut (left = ω-∀-node, right = ∃-node) splits cleanly:
- **`zcOK_redAllEx_premises` (STRUCTURAL closure — the genuine soundness content):** the reduct's two
  premises (`zsubst d0 a tE`, the ∃-premise `dE`) are `ZcOK`, via `zcOK_cut_inv` ⟶ `zcOK_omegaAll_inv`
  (premise family at the witness) + `zcOK_ex_inv`.
- **`zcOK_redAllEx_of_ctrl` (full closure GIVEN the reduct's operator-control):** isolates the remaining
  obligation to the ordinal assignment alone.
- **⚠ 2nd in-kernel finding:** the lap-103 `imax` stored-ordinal is INSUFFICIENT for the cut node — the
  `cut` constructor needs `sord premise ≺ stored`, but the reduct stores `imax (sord dL') (sord dR')` and the
  max-ACHIEVING premise EQUALS `imax` (never `≺`, `icmp` irreflexive). `imax` worked for the parent-cut
  *descent* (`sord_redAllEx_lt`) but NOT the reduct's own *operator-control*. The fix is Gentzen's RANK-AWARE
  ordinal (`o(cut) = ω^{rank} ⊕ …`, strictly above premises AND ≺ parent, carrying the single-step descent) —
  the deep Gentzen-Hauptsatz content, now isolated to the ORDINAL assignment.

## NEXT (two isolated fronts — the deep crux-2 core, sharply pinned)
1. **General ∀-inversion `redInv∀ : V → V → V`** + `ZcOK d → ZcOK (redInv∀ d t)` with `sord (redInv∀ d t) ≼
   sord d` — the recursion that RE-PRINCIPALIZES a non-ω-∀-node left premise. Principal ω-∀ case =
   `zcOK_omegaAll_inv` (premise selection); recursive cases (cut/∃/leaf) push the inversion through the last
   rule (Schütte; `Zinfty.allInvAux` is the META template).
2. **Rank-aware `sord`** (replace `imax`): a cut node's stored ordinal must be strictly above its premises
   AND strictly below the parent — so `zcOK_redAllEx_of_ctrl`'s `hLctrl`/`hRctrl` AND `sord_redAllEx_lt` hold
   together. This is Gentzen's ordinal assignment; the single-step descent rides on it.

(Both feed the GENUINE `red_iterate_descends` with `P = ZcOK ∧ derives ∅→⊥`. The Σ₁-`Fixpoint` port of
`ZcOK` — so the descent is V-internal for PRWO — is the deferred final brick, after the math closes.)

## This lap's commits (all `wip/PathCOmega.lean`, axiom-clean, `src/` untouched + doc refresh)
(1) `3e839e9` — 4 lemmas certifying the `hinv` closure failure + helper; STATUS/PENDING_WORK/NEXT_STEPS
refreshed with the corrected datatype-first plan. (2) `20b79f3` — Brick 5: `ZcOK` inductive predicate +
`zcOK_iff` + node inversions. (3) `2d95819` — Brick 5b: principal-cut structural closure + the rank-aware
ordinal finding. `lake build GoodsteinPA` green 1325 throughout; `lake env lean wip/PathCOmega.lean` clean,
all new decls `[propext, Classical.choice, Quot.sound]`.
