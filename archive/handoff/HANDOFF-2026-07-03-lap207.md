# Handoff: lap 207 — route-(c) steps (2)+(3) CLOSED sorry-free; chain assembled; 2b majorization STARTED

**FINAL LAP STATE (8 green commits)**: `f883dd8` (root discharge `gated_of_sigma1`) ·
`5b26b4b` (**`readoffVTC_core` SORRY-FREE — the `allω` trap is DISSOLVED**) · `22e5d2a`
(PENDING record) · `2f4ff2e` (pipeline `rankToZeroAuxTC ∘ readoff_value_Zef2TC`) · `23c2353`
(chain assembled at `goodsteinBodyE` + Σ₁ certificate + `gated_root_of_sigma1`) · `d92f514`
(2b prep: `ewIter_mono_slot` + `ewIterTower_rel1_le`) · `e636550` (norm/Nlog bridge) ·
`6e1487e` (2b step engine `hardy_step`). Tree CLEAN. **Resume = §NEXT item 1** (the 2b master
WF wrapper).

**Date**: 2026-07-03 · **Branch**: `plan` · **Build**: 🟢 bare `lake build` (1342, run at
`f883dd8`; only wip files touched since) + `lake env lean` green on `wip/E1EmbeddingGrind.lean`,
`wip/ReadoffValueGate.lean`, `wip/HardyMajorization.lean`, `wip/GateRootProbe.lean`,
`wip/BodyShapeProbe.lean`. Headline untouched this lap (no src edits at all).

**Directive (OUTRANKS this)**: `DIRECTION.md` lap-206 block. Steps (1)(2)(3) now ALL closed;
work remaining = its step (4): piece-2a residue + **2b growth conversion** (in progress), then
the ONE judge package. Do NOT self-ratify into `src`.

## ✅ This lap

1. **`gated_of_sigma1`** (`wip/ReadoffValueGate.lean`): `Hierarchy 𝚺 1 ψ → (∀ B, gvb ψ B ≤
   P (max V B)) → Gated P V ψ`, kernel-clean. Pieces: `sigma1_all_inv` (𝚺1 ∀-head = ball ONLY),
   `gate_extract` (false ball instance pins `k < tvB t 0`; `valm_env_irrel_of_positive`),
   `tvB_le_gvb_ball`, WF-recursion w/ coupled hypothesis self-threading (`gvb_substs_le` +
   max-algebra). Also `gated_root_of_sigma1`: Σ₁-ness alone ⟹ `∃ P, Monotone P ∧ Gated P V ψ`
   (P := gvb-based). Probes: `wip/GateRootProbe.lean`.
2. **`readoffVTC_core` + `readoff_value_Zef2TC`** (`wip/E1EmbeddingGrind.lean`) — **SORRY-FREE,
   the ONE wip trap sorry is superseded**. V-threaded invariant (every member `Gated P V`,
   non-tracked false; slot frame `rel1 f₀ j`, `j ≤ V`); bound `ewIter (Sslot f₀ P) α (Sslot f₀
   P V)`. The `allω` case needs NO branch-0 split (gate always yields false branch `k₀ ≤ P V`;
   `rel1_rel1` collapses frames; `T3_descent'` absorbs all bumps incl. `exI`'s `max V n`).
   Old `readoffTC_core` (trap sorry) left in place as superseded.
3. **Pipeline + assembly**: `readoff_value_pipeline` (rank-d root-slot derivation + certificate
   → true instance under the tower bound, via `rankToZeroAuxTC` EwLow entry);
   `goodsteinBodyE_inst_shape` (instance IS `∃⁰ χ` by rfl + `Hierarchy 𝚺 1` by double rew +
   simp); **`readoff_value_goodstein`** = the whole rung-E chain modulo the `Gated` certificate
   hypothesis (certificate ↔ Σ₁ seam composes at judge/src assembly — the two wip modules can't
   import each other).
4. **2b started** (`wip/HardyMajorization.lean` + E1 prep): m-uniformization (`ewIter_mono_slot`
   pointwise slot-domination — NEW general lemma; `ewIterTower_rel1_le` commutes the stage
   pre-max K_m out of the whole tower); **norm/Nlog bridge** `norm β < 2^(Nlog β + 1)` (sharp:
   fails at coeff 5 without the +1); **`hardy_step`**: `H_{ω^β'}(H_{ω^β'}(H_{ω^e'}(z))) ≤
   H_{ω^α'}(z)` (exact composition `hardy_coeff_add`+`hardy_add_comp` right-associated, then
   linear-norm-gated `hardy_le_of_lt` raise). All kernel-clean.
5. ⚠️ **REFUTED on paper** (recorded PENDING lap-207; kernel-probe optional): Nlog-gated hardy
   ordinal-monotonicity is FALSE (β=ω·4, α=ω², x=2 — coefficient 2^x beats argument x). The
   majorization MUST route through the linear norm + bridge, as `hardy_step` does.

## 🚦 NEXT (in order)

1. **The 2b master WF wrapper** (`wip/HardyMajorization.lean`): `ewIter f α m ≤ hardy
   (Wpow (g α)) (seed)` by WF induction on α, branch case = `hardy_step`. Design decisions
   settled: assignment exponent `g α ≈ e₀ + 1 + α`-shape (ONote add, no mul/opow needed);
   seed must be PRE-INFLATED past `2^(K+1)` (K = f (Nlog α + m)) so `hnorm` is paid by
   `norm_lt_of_Nlog_le`; the `Nlog β + ·` additions and the engine `H_{ω^{e'}}` absorb the
   f-domination hypothesis (`∀x, f x ≤ hardy (Wpow e') x`-form, plus f mono/infl/low). Watch:
   the recursive IH's seed grows (inner call at bumped `Nlog β`) — bound form should be
   `B(α,m) := H_{Wpow (gα)}(u (Nlog α + m))` with u fixed; the small-β (β ≤ e') branches
   don't compose exactly — raise β to e'+1 FIRST via gated mono (norm small ✓) or fold into
   the final raise.
2. **2b remaining glue**: (a) gvb numeral contraction instance `gvb ((Rew.subst ![nm m]).q ▹ ψ)
   B ≤ gvb ψ (max B m)` (ReadoffValueGate; direct `gvb_rew_le` instance) — makes P_m ≤ P*
   (max m ·) with FIXED P*; (b) `Sslot`-level assembly of the m-uniformized bound; (c) the
   semantic link `atomTrue (χ_m/[nm n]) → goodsteinLength m ≤ n` (igoodstein faithfulness —
   scope vs `Dom.goodsteinLength` / `DescentSemantic`, grep `igoodstein_nat`); (d) final
   EventuallyLE: one fixed `fastGrowing o` dominates `m ↦ H_{...}(u(...))` — ordinal room is
   free (`o` arbitrary NF).
3. Then: judge package = rung-E statement + `Zef2TC` + (Ax2) + route-(c) read-off + the
   certificate seam, as ONE package. **Do NOT self-ratify into `src`.**

## Gotchas fresh this lap
- `Semiformula.all.inj` closes the ball-inversion where `simp [ball_eq, all.injEq]` makes no
  progress (RHS not syntactically `all` until whnf).
- Identifiers may not contain `Σ` (`hχΣ` → parse error at next use site).
- `fin_cases` is NOT available in this import closure — use `rcases Fin.eq_zero i with rfl`.
- `ω`/`ω^·` on Ordinal needs `open Ordinal`; ONote sums are best proved by `repr_inj.mp` +
  `repr_add` + a `show` at the raw `ω ^ _ * _ + _` shape.
- `NF.below_of_lt (lt_def.mp h) hNF |>.repr_lt` is the "repr < ω^b from head-exponent <" idiom
  (`below_of_lt'` is the converse direction and its field notation collides).
- `hardy_add_comp` exactness needs the RIGHT summand strictly below `ω^(lastExp LEFT)` — you
  CANNOT stack same-level principals with it; merge same-level via `hardy_coeff_add` FIRST,
  then append the small tail.
