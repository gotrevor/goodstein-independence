# HANDOFF — 2026-06-22 (lap 3)

> **NEXT LAP FIRST ACTION:** start **M4 — the embedding `PA⁺ ↪ Z_∞`** (Towsner §16, Thm 16.7).
> Cut-elimination (M5) is DONE and axiom-clean; the missing link to a *bounded* `Z_∞` proof is the
> embedding. See `PENDING_WORK.md` O2 "NEXT" and `STATUS.md`.

Branch `plan`. Headline build **green** (`lake build GoodsteinPA`, 1243 jobs). Headline
`Statement.peano_not_proves_goodstein` is **still a literal `sorry`** (anti-fraud — M4+M6+M7 are
not yet built, so it does not chain). `Defs.lean`/`Bridge.lean` RHS/`goodsteinTerminates` LOCKED.

## What this lap did — COMPLETED the Z_∞ cut-elimination (Towsner §19), zero sorries ✅

`src/GoodsteinPA/Zinfty.lean` (promoted from `wip/ZinftyF.lean`) now machine-checks the **entire**
Gentzen-style cut-elimination for the infinitary ω-rule calculus `Z_∞`, `#print axioms`-clean
(`[propext, Classical.choice, Quot.sound]` on `cutElim`, `cutElimStep`, `atomCut`). Built this lap:

1. **Cut reductions.** `cutReduceConj`/`cutReduceDisj` (§19.5, ∧/∨) — *no new induction*: invert
   both premises (`andInvL/R` + `orInv`, already proved) and close with two ordinary cuts.
   `cutReduceAll` (§19.6, ∀/∃) — `∃` is not invertible, so invert the ∀-side (`allInv` → numeral
   family) and **induct on the ∃-side**, cutting at the witness numeral. Used a fixed inverted
   family + `Δ.erase(∃∼φ)∪Γ` frame so the IHs need no family-threading.
2. **`cutElimStep` (§19.7)** = `cutElimStepAux` (transfinite induction over the derivation, rank
   `c+1→c`, bound `ω^(o d)`) + `cutElimPrincipal` (8-way dispatch on the cut formula: ∧/∨ →
   reductions, ∀/∃ → `cutReduceAll`, atomic → `atomCut`, `⊤`/`⊥` → `removeFalsum`).
3. **Atomic + `⊥` cuts WITHOUT a truth layer** — the lap's nicest surprise. `atomCut` removes a
   `rel`/`nrel` cut by induction on one premise: the atom is never principal, and the one
   interesting case (`axL` clashing on the cut atom) is dissolved by set-sequent idempotence (the
   negation is already in `Γ`, so the other premise proves `Γ`). `removeFalsum` deletes an
   incidental `⊥`. So §19.2's truth content was never needed.
4. **`cutElim` (§19.9)** — full elimination to cut-free, `ω`-tower bound — already followed from
   `cutElimStep`, now fully real.
5. **ε₀-closure** (commit `5048d8f`): `omega0_opow_lt_epsilon0` + `omegaTower_lt_epsilon0` — the
   cut-free ordinal `ω_c^α` stays `< ε₀` when `α < ε₀` (M7 prerequisite, pure ordinal, axiom-clean).
6. **M4 path validated** (commits `f2db847`, `a62418c`): `atomTrue φ := Semiformula.Evalm ℕ … φ`
   typechecks (needs `import Foundation.FirstOrder.Arithmetic.Basic.Model`). The full M4 truth-axiom
   design + the `trueAtom`↔`atomCut` interaction is in `PENDING_WORK.md` **O2′** — execute next lap
   on a `wip/` copy (it re-touches the §19 inductions; do NOT risk the clean `src/Zinfty.lean`).

Engineering resolutions (record for the corpus):
- `Ordinal.nadd` (natural sum) is **absent from mathlib v4.31.0** (no `NaturalOps.lean`). Used
  ordinary `+` with a `+1` slack throughout; bounds stay below `ω^(·+1)` via
  `Ordinal.isPrincipal_add_omega0_opow` (additive principality).
- The **Hardy `k` index is NOT needed for cut-elimination** — pure Schütte `(α,c)` suffices.
  (Towsner's `k` is a §17 lower-bound device.) Resolves last lap's open question.
- `exI` restricted to **numeral witnesses** `nm n` (so §19.6 matches the ∀-inversion family);
  faithful for the arithmetic term model, with the evaluation obligation deferred to M4.
- mathlib gotcha: `add_le_add_left h 1` strips a *right* `+1`; `(add_le_add_iff_left α).mpr h` adds
  on the left; `zero_le` (no arg); `Finset.erase_insert_eq_erase`.

## State of the headline (ANTI-FRAUD)
`peano_not_proves_goodstein` is still `sorry` (`#print axioms` shows `sorryAx`). The whole `Zinfty`
layer is a verified *component* (M3+M5) that does not yet reach the headline. Discharge only when
`#print axioms` is clean AND it genuinely chains (after M4+M6+M7).

## Next lap — start here (hardest-first)
1. **M4 embedding** (`PA⁺ ↪ Z_∞`, Towsner §16/§18). New file `src/GoodsteinPA/Embed.lean`. Sub-
   targets M4.1 (Lemma 16.1), M4.2 (Cor 16.6, induction axioms), M4.3 (Thm 16.7). Reuse
   Foundation's finitary `Derivation` (`FirstOrder/Basic/Calculus.lean`) — map each rule across,
   finitary `∀`→ω-rule. The numeral-substitution of free vars matches our `exI`/`allω` shape.
2. **M6 lower bound** in parallel (independent of M4): §17 Thm 17.1 + Hardy hierarchy (overlaps
   Track 1 `~/src/lean-formalizations` `Logic/FastGrowing`). M6.4 likely needs the Hardy `k` index
   threaded through a *cut-free* `Provable₀` — design then.
3. **M7 assembly** once M4+M6 land.

No open `ON-LINE-REQUEST.md` (Towsner self-contained). No Aristotle job (offline).

## Build / file map
- `src/GoodsteinPA/{Defs,Encoding,Bridge,Statement}.lean` — Phase 0 (headline `sorry`).
- `src/GoodsteinPA/{Computability,Reduction}.lean` — M1 (axiom-clean) + M2 (Gödel II hook).
- **`src/GoodsteinPA/Zinfty.lean` — Z_∞ calculus + COMPLETE cut-elimination (M3+M5), axiom-clean.**
- `wip/Zinfty.lean` — superseded `AForm` prototype (history; keep).
- `STATUS.md` (living overview), `PHASE2-DECOMPOSITION.md`, `PENDING_WORK.md` — plan + open-work.
