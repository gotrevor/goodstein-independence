# HANDOFF — 2026-06-23 (lap 37, **internal ONote substrate: codes + iC + ievalNat + iCanon; route-(b) decided**)

> **Branch** `plan` · HEAD = `38b0c5e` · build **green** (`lake build GoodsteinPA`, **1307 jobs**) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, **0 math axioms**). **Tree clean.**

## ✅ Done this lap (6 green commits)

The lone wall is `hbound` in `DescentSemantic.no_min_descent_absurd_of_goodstein` — the Rathjen §3
slow-down run inside a model `M` on the `≺`-descent. This lap **mapped the deep obstruction and built
the internal-ONote foundation it requires.**

1. **`InternalLog.ilog_mono`** — `2≤b, 0<n≤n' ⟹ ilog b n ≤ ilog b n'` (numeric bridge).
2. **`DescentCore.evalNat_succ_base`** — `Canon b o → o.NF → evalNat (b+1) o = bump (b+1) (evalNat b o)`
   (via `canon_round_trip`+`evalNat_toONote`). Raising the eval base by one IS the numeric `ibump`, so the
   internal `ibump` substrate realizes `evalNat`'s base-change in `M` directly.
3. **STRATEGIC FINDING** (`ANALYSIS-2026-06-23-lap37-order-reflection-opacity.md`, paper-grounded in
   Rathjen 2014 §3 on disk): the descent is `Mlt f y x = M ⊧ precφ(y,x)` with `precφ =
   codeOfREPred₂(natCode a < natCode b)` — the **opaque r.e. blob** (same opacity that was wall B);
   `natCode` is an arbitrary `Denumerable.eqv` inverse. Rathjen's βₖ construction (Cor 3.4 / Thm 3.5)
   manipulates the **CNF** of descent elements, so the descent must be decodable to CNF in `M`.
   **Route decision = (b): transparent HFS-CNF coding** (a code IS its CNF) → transparent `precT`,
   re-wire seam + F (`epsilon0_le_orderType_ltPull` holds for ANY `e : ℕ≃NONote`, so order-type transfers).
4. **`src/GoodsteinPA/InternalONote.lean` (NEW, sorry-free, in the lib build):**
   - CNF **codes** as nested HFS pairs: `ocOadd ec n rc := ⟪⟪ec,n⟫,rc⟫+1` (`0 ↦ 0`); decode projections
     `ocExp`/`ocCoeff`/`ocTail` (`𝚺₀`) + round-trip simp lemmas; **subterm bounds** `ocExp_lt`/`ocCoeff_lt`/
     `ocTail_lt` (+`_of_pos`) — the course-of-values strict-decrease facts.
   - **`iC`** (Rathjen `C` max-coefficient) via a CofV table; `𝚺₁`-definable; `iC_zero`; recursion
     **`iC_ocOadd : iC (ocOadd ec n rc) = max (max (iC ec) n) (iC rc)`**.
   - **`ievalNat`** (`T̂^b_ω` evaluation) via a binary CofV table (param = base `b`); `𝚺₁`-definable;
     `ievalNat_zero`; recursion **`ievalNat_ocOadd = n * ipow (b+1) (ievalNat b ec) + ievalNat b rc`**
     (mirrors `Domination.evalNat_oadd`).
   - **`iCanon b c := iC c ≤ b`** (internal `Canon`, free via `Canon_iff_C_le`); `iCanon_zero`;
     recursion `iCanon_ocOadd`; `Γ-Relation` definability.

## Aristotle
**`ibump_mono` RUNNING** (UUID `7c8bb0e8-23cc-4118-9bab-70b37a2debbc`, `scratchpad/ibump_mono.lean`):
goal `2≤b → n≤n' → ibump b n ≤ ibump b n'` over ℕ with the true `ibump`/`ipow`/`ilog` laws axiomatized
(algebra identical to the V-model, so a clean proof PORTS to `InternalBump`). **HARVEST NEXT LAP:**
`aristotle list`; on COMPLETE, verify + `#print axioms` + port to `src/GoodsteinPA/InternalBump.lean`.
Its digit-direct technique informs the internal order-reflection (the next deep piece).

## NEXT (hardest-first) — the deep piece is internal order-reflection
See `PENDING_WORK.md` top for the full decomposition. In order:
1. **Harvest `ibump_mono`** from Aristotle → port to `InternalBump`.
2. **Internal `evalNat_lt_iff` / `icmp`** — the order-reflection the descent consumes:
   `iCanon b o → iCanon b p → isNF o → isNF p → (ievalNat b o < ievalNat b p ↔ o ≺ p)`. Structural
   induction + ievalNat arithmetic + the NF "tail value < leading power" bound. SAME family as
   `ibump_mono` — reuse its technique. Or build `icmp` (binary CofV table indexed by `⟪o,p⟫`).
3. **Internal `isNF`** (exponents strictly decreasing — needs `icmp`) + internal `evalNat_succ_base`
   (`ievalNat (b+1) c = ibump (b+1) (ievalNat b c)` for `iCanon ∧ isNF`, by structural induction).
4. **Seam/F re-wire to `natCodeT`** (transparent coding) ⟹ transparent `precT`; re-validate
   `peano_not_proves_TI` with `#print axioms` at every step (must stay girder-clean).
5. **Slow-down `βₖ`** from the descent (`descent_seq_exists`) on internal codes; `C(βₖ)≤k+1`; feed
   `nonterminating_internal` ⟹ `hbound` ⟹ `hCD` ⟹ headline axiom-clean.

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`. Route (b)
re-touches the proven `peano_not_proves_TI` — re-`#print axioms` it after every seam/F change.

## src/ sorries (unchanged): `Statement.lean:22` (headline, locked), `Reduction.lean:52` (off-path
Route-A), `DescentSemantic.lean` `hbound` (the lone genuine wall). `InternalONote.lean` is sorry-free.
