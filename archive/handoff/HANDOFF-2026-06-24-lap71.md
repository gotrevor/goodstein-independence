# HANDOFF — lap 71 · FRESH-MIND REVIEW + RUNG 0.5 COMPLETE (ZPhi rule-faithfulness)

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md` (top), then `STATUS.md`.
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = 1321 jobs. Tree clean. HEAD `679bb5c`.
> (3 green commits this lap; HEAD started at `e1a6147`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**.
Route: `PA⊢γ →(crux-1 DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)` → Gödel II. Sole math wall = crux-2,
localized to **`RedSound`** (`InternalZ.lean`): `∀ d, ZDerivesEmpty d → ZDerivation (iR2 d)` — the
`iR2`-reduct of a contradiction derivation must itself be a genuine `ZDerivation` = real internalized
cut-elimination. Lap 70 refuted Option B in-kernel (`not_zKValid_iCritReduct`) and forced **Option A**
(a genuine validity-preserving reduct, shape-dispatched on the cut formula, à la Bryce–Goré `cut_elim.v`).

## ✅ THIS LAP — RUNG 0.5 COMPLETE (the genuine-reduct prerequisite), all axiom-clean, green 1321
**Review:** direction KEPT. Kernel re-verified from real `#print axioms`: headline
`[propext, sorryAx, choice, Quot.sound]` (0 math axioms, honest `sorry`); `goodstein_implies_consistency`
= +`PA_delta1Definable`; lap-70 landmarks clean. STATUS.md refreshed off the stale lap-59/62 framing
(header, Where-it-stands, What's-happened, axiom ledger, Outstanding ladder).

**The work — all three I-rule disjuncts of `ZPhi` are now rule-faithful** (they pin the Buchholz
inference-rule *premise sequents*, not just the conclusion succedent), so a genuine reduct can read the
premise-sequent data off inversion:
- **I¬** `zInegWff p d0` (`d0 ⊢ p,Γ→⊥`): `𝚫₁.Semisentence 2`, all `𝚺₀`. Commit `8208ed0`.
- **I∀** `zIallWff s a p d0` (`d0 ⊢ Γ→F(a)`): `𝚫₁.Semisentence 4`, uses Foundation `substs1Graph ℒₒᵣ` +
  `qqFvarDef`. Commit `ca39f99`.
- **Ind** `zIndWff d` (UNARY on the node; `d0 ⊢ Γ→F(0)`, `d1 ⊢ F(a),Γ→F(Sa)`, concl `F(t)`):
  `𝚫₁.Semisentence 1` + `𝚫₁-Predicate`. New accessors `zIndEig`/`zIndTerm` (the `at'=⟪a,t⟫` decode);
  term-codes `numeral`/`qqAdd` from `Bootstrapping.Arithmetic`. Commit `679bb5c`.

Each wired through the full cascade (`ZPhi` def → `zphi_monotone` → `zphi_strong_finite` → `zphi_iff`
×4 patterns → `zblueprint` σ/π cores → `zPhi_definable` simp) and each inversion lemma
**strengthened** to surface its `…Wff`: `zDerivation_zIneg_inv`/`_zIall_inv`/`_zInd_inv`.

**Reusable cascade recipe (battle-tested ×3 this lap, in `PENDING_WORK.md` top):** def `…Wff` + `…WffDef`
+ `_defined` ABOVE `ZPhi` (after `zKValid_definable`, ~line 1252); add `∧ …Wff …` to the disjunct;
propagate the binder through monotone/strong_finite/`zphi_iff`; `.sigma`/`.pi` into the σ/π blueprint
cores; `+…Wff_defined.iff` to `zPhi_definable`; build, fix the ≤4 inversion sites naming the disjunct's
last conjunct (strengthen the `_inv` lemma to surface the `…Wff`).

**Gotchas banked (PENDING_WORK):** `numeral`/`qqAdd`/`numeralGraph`/`qqAddGraph` live in
`LO.FirstOrder.Arithmetic.Bootstrapping.Arithmetic` (NOT bare `…Arithmetic`); `(substs1.defined
(L := ℒₒᵣ)).iff` must pin `L` or instance synth fails; the `𝚫₁-Predicate`/`𝚫₁-Relation₄` simp needs
`and_assoc` to reconcile the right-nested core with the def's grouping; no `𝚫₁-Relation₅ … via`
notation exists ⟹ make ≥5-ary side-conditions UNARY on the node (also frees the body to grow without
a cascade re-run); there is no `zIallEig` accessor — recover the eigenvariable via
`congrArg (fun d => π₁ (zRest d)) h`.

## ⚠️ KNOWN DEBT on `zIndWff` (deliberate, cheap to fix later)
`zIndWff` OMITS the `Γ ⊆ ant(d1)` threading conjunct (`∀ i < lh (seqAnt (fstIdx d)), inAnt (znth
(seqAnt (fstIdx d)) i) (seqAnt (fstIdx (zIndPrem1 d)))`) that the genuine Ind reduct's `isChainInf` will
need. Because `zIndWff` is UNARY, adding it later only re-proves `zIndWffDef`/`zIndWff_defined` — it does
NOT touch the `ZPhi` cascade. Add it when building rung 2.

## ▶ NEXT — rung 1: `zsubst d a n` (the foundational brick of the genuine reduct)
Σ₁ eigenvariable substitution on Z-derivations (substitute numeral `n` for free var `a` throughout
derivation `d`), Σ₁-definable + `ZDerivation`-preserving + `iotil`-invariant + `fstIdx`-computing.
A Σ₁ recursion over the derivation tree applying Foundation's `substs1`/`Rew` at each sequent — mirror
the `iRTable`/`iCritReduct` blueprint recursions. Sub-bricks + the full lap-70 LADDER (rung 2 genuine
Ind reduct — the most tractable, premises are genuine sub-derivations not `Rep`; rung 3 genuine K/cut
reduct; rung 4 `RedSound` tag-dispatch → `iord_iR2_iterate_descends` closes the descent → C0.5 bridge →
`Reduction.lean:68`) are in `PENDING_WORK.md`.

Blueprint for the genuine reductions = `~/src/Gentzen/theories/Logic/cut_elim.v` (port the SHAPE; findings
in `archive/findings/ON-LINE-FINDINGS-2026-06-24-bryce-gore-gentzen.md`). Realistic cadence ≈ one genuine
rung per lap; this is real internalized cut-elimination (a major multi-lap undertaking).

## Parallel front (when the reduct blocks)
- `PA_delta1Definable` is STILL an upstream Foundation `axiom` — mandatory for axiom-free. Independent.
- C0.5 bridge `¬Con(M) → ∃ z, ZDerivesEmpty z` (Bryce–Goré `Peano.v` three-layer shape).

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. `RedSound` is the lone InternalZ obligation; do NOT axiomatize it
or fake the reduct.

## Lap discipline
InternalZ is `src/` (green-gated, sorry-scanned). Commit each green build; end the turn after each so
the governor can pace.

## Commit log (lap 71, branch `plan`)
- `8208ed0` rung-0.5 I¬ wired (zInegWff) + STATUS review refresh
- `ca39f99` rung-0.5 I∀ wired (zIallWff)
- `679bb5c` rung-0.5 Ind wired (zIndWff) — RUNG 0.5 COMPLETE
