# Handoff: lap 163 — ⭐⭐⭐ `zDerivation_zAxBot` DROPPED; ZPhi disjunct #9 (zAxBot, tag-8) FULLY WIRED, axiom-clean

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `09cd161` · build 🟢 1326 (`lake build GoodsteinPA`, exit 0).
**One green commit this lap.** `false_of_ZDerivesEmpty` = `[propext, sorryAx, Classical.choice, Quot.sound]`
(UNCHANGED from lap-162 baseline — **no drift**, the `sorryAx` is from the OTHER remaining path sorries).
`zDerivation_zAxBot` = `[propext, Classical.choice, Quot.sound]` — **no sorryAx, FULLY PROVEN**.

## Lap-163 net: the lap-162 isolated obligation is DISCHARGED
The lap-162 baton isolated the entire ⊥-exit ex-falso to ONE sorried intro `zDerivation_zAxBot`
(`Crux2Blueprint:3377`), calling it "the ZPhi disjunct #9". **That is now a real, axiom-clean theorem.**
The `exFalsoClose` cert (`:3501`) consumes it directly, so **the ⊥-exit ex-falso is now sorry-free**.

src `Crux2Blueprint` sorries **47 → 46** (net ONE dropped, ZERO added across all 4 touched files;
InternalZ/Zsubst/RedZKDescent stayed 0).

## What landed — the full 9th-disjunct ripple (the lap-116 `zAx1` precedent, LARGER than estimated)
The handoff/PENDING undercounted at "~125 `zDerivation_iff.mp` sites": the true ripple also includes the
`rcases hphi` ZPhi-induction sites + the substitution machinery + several recursion-table dispatchers.
**InternalZ.lean:**
- `ZPhi` 9th disjunct `(∃ s, d = zAxBot s ∧ inAnt (^⊥) (seqAnt s))` + `zphi_monotone`/`_strong_finite`/
  `_iff` (both directions) + `zblueprint` Σ/Π clauses (via `qqFalsumDef`) + `zPhi_definable`
  (`zAxBot_defined.iff`/`qqFalsum_defined.iff`).
- `zsubstNext` def + `zsubstNextDef` arith + by_cases cascade: tag-8 is a **faithful sequent-subst**
  `zAxBot (fvSubstSeqt a t (fstIdx d))`, NOT the identity else (else would break `fstIdx_zsubst`).
- NEW: `zDerivation_zAxBot_intro`/`_inv`, `red_zAxBot`.
- All destructuring sites. The 3 non-mechanical ones: **`climb_to_rep_producer`** carries tag-8 as an
  extra producer tag (consumer routes it to `residual`); **`majorPrem_tag_mem`** EXCLUDES a tag-8 major
  on the ⊥-orbit via the ⊥-in-own-antecedent rerouting (mirrors the tag-0/7 leaf exclusion);
  **`leastSucc_in_ant_or_nonleaf`** treats tag-8 as a non-leaf (`Or.inr`). `firstBotPrem`/
  `majorIdx_botOrbit` only need `zTag ≠ 0 ∧ ≠ 7` (tag-8 trivially passes).
**Zsubst.lean:** `zsubst_zAxBot`/`maxEigen_zAxBot`; `zReg_zAxBot`/`zFresh_zAxBot` HOISTED before their
`zsubst`-soundness uses; all commutation lemmas + `red`/`ZRegular`/`ZFresh`/`ZSeqAnt`-preservation + the
substitution-soundness build-site (`⊥` stays `⊥` under `fvSubst`, via `fvSubst_falsum (L := ℒₒᵣ)`).
**Crux2Blueprint.lean:** `red_rep_of_tp_isymRep`/`redSoundGen`/`iord_descent_red` (incl. the nested
premise-dispatch, tag-8 = `red`-fixpoint via the new `red_zK_fixpoint_of_zAxBot_selected` in
RedZKDescent); BOTH noRedex dispatch copies — tag-8 ex-falso **major** → `exFalsoClose`/`leafClose`
(⊥ threads to Γ or contradicts jstar minimality), tag-8 **producer** → `residual`/`axMajorResidual`.

## 🎯 NEXT — the ⊥-exit ex-falso is DONE; close the rest of `genReduct_chain_noRedex`'s `residual`
`exFalsoClose` (`:3501`) is fully real. The two open structural escapes (lap-161 frontier) remain in the
`residual` sorry (`genReduct_chain_noRedex` anySucc copy, `~:3486`) + `axMajorResidual`:
1. **C-exit R-intro replay** — tag-1/2 (`zIall`/`zIneg`) major premise producing the CONCLUSION succedent
   `C` (not `⊥`); the dispatch currently routes the C-exit of tag-1/2 to `residual` (`:3607`,`:3616`).
2. **tag-5/6 thread-escape** — the `climb_to_rep_producer` ESCAPE disjunct (`^∀`-formula in `Γ`) and the
   leftover producer landings currently route to `residual`/`axMajorResidual`.
Closing those + the now-done ex-falso DROPS the `genReduct_chain_noRedex_anySucc` whole-lemma sorry
(`Crux2Blueprint:~3430`), which is what `axMajorResidual`/`descent_step_K_noncrit_axMajor` consume.

## ⚠️ FORBIDDEN / corrections (unchanged from lap-162 unless noted)
- The lap-162 "~125-site" estimate was LOW — the ripple is ~150 sites + 2 arith dispatchers (`zsubstNext`)
  + recursion-table simp lemmas (`maxEigen`/`red`/`zReg`/`zFresh` `_zAxBot`). Done now; no re-derivation
  needed.
- `zsubstNext`'s tag-8 MUST be the faithful sequent-subst (not the identity else) or `fstIdx_zsubst` fails.
- DIRECTION.md lap-161 still holds (`red`/`iord`-recursion/`redLeast`/`seqUpdate` splice/M2-M3-M4 etc.).
- ALTITUDE (unchanged): `false_of_ZDerivesEmpty` sorry-free is NOT the headline —
  `goodstein_implies_consistency` (`Reduction.lean:68`) bare sorry, M2/M4 bridge ~0%.

---
**→ Next session: close `genReduct_chain_noRedex`'s `residual` (C-exit R-intro replay + tag-5/6
thread-escape) — the ex-falso escape is now fully discharged. Keep build green, `#print axioms` clean.**
