# Handoff: SERIES-3 lap 200 — E-1 block 3: master predicate + case ladder, 7/10 sorry-free; `∃ K` statement amendment

**Date**: 2026-07-03 · **Branch**: `plan` · **HEAD**: `4b8a8b3` · **Tree**: CLEAN ·
**Build**: 🟢 bare **`lake build`** (1342 jobs). **Headline**:
`GoodsteinPA.peano_not_proves_goodstein = [propext, Classical.choice,
goodstein_implies_consistency, Quot.sound]` (sorryAx OFF, undrifted). `blueprint_audit` ✓ 16.

**Order in force**: `REBUILD-Z-SERIES-3-ORDER-2026-07-03.md`. **Ledger**:
`REBUILD-Z-SERIES-3-LEDGER.md` (block 8 this lap).

## ✅ This lap (1 green commit) — E-1 block 3 in `wip/E1EmbeddingGrind.lean`

- **STATEMENT DISCOVERY (judge amendment input)**: the fixed-slot DRAFT
  (`embedding_Zef2TC_DRAFT`) is unprovable as stated — `Zef2TC.exI` demands witness
  `n ≤ f 0` with `f = ewRootSlot e B` structural, but the `exs` witness value (and the root
  goodstein witness `N(m)`) is env-/instance-dependent and unbounded.  Fix = the W3 `SomeK`
  discipline on the slot: env-local `∃ K`, slot `rel1 (ewRootSlot e B) K` (composes with the
  ω-rule via `rel1_rel1`, keeps `EwF1/EwF2` via `rel1_low`).  `embedding_Zef2TC_DRAFT2` =
  amended rung-E statement (typechecked, sorry; DRAFT1 kept verbatim as flagged input).
- **`BudgetedEmbedsTC`** master predicate (W3 shape + `∃ K`; operator fixed at `Cl ⊤`).
- **7/10 `Derivation2` cases SORRY-FREE** (standard triple, `#print axioms` anchors):
  `closed` (via `em_Zef2TC'`), `verum`, `wk`, `shift`, `or`, `and`, `cut`.  Two-premise
  joins: control `osucc (e₁+e₂)` with `norm eᵢ` absorbed into structural `B`
  (`hardy_le_of_lt` gate), root `osucc (α₁+α₂)` with `Nlog_add_le_max_succ` + one `K`-rung
  of gate slack (`relSlot_succ_gap`).
- **`budgetedEmbedding_Zef2TC`** assembled by REAL induction; `sorryAx` only through the
  three disclosed hard leaves `axm` / `all` / `exs`.
- Banked toolkit (all sorry-free): `Zef2TC.mono_f/mono_c/change_H/change_e` (e is a phantom
  index — trivial-induction transport), `Nlog_osucc_le`, `relSlot_le`, `relSlot_mono`,
  `relSlot_succ_gap`, `le_relSlot_zero`.

## ✅ Also this lap (2nd green commit, `58ab26e`) — E-1 block 4: `exs` DISCHARGED, ladder 8/10

`em_cong_Zef2TC` (value-congruent budgeted EM; atomic split on `atomTrue` closes via
`trueRel`/`trueNrel` — the (Ax2)-load-bearing step) + `em_cong1_Zef2TC` +
`budgetedEmbedsTC_exs` all SORRY-FREE: cut at rank `complexity+1` against the congruent-EM
premise `(nm m, asg env t)`, then `exI` at `m`, witness absorbed into `K := max K₁ m + 3`.
Mechanically validates the `∃ K` amendment. Master now sorryAx-clean except `axm` + `all`.
Ledger block 9. Gotcha: omega atomizes `rel1 … (M+1+1)` vs `rel1 … (M+2)` — normalize the
index with `rw [show M+1+1 = M+2 from rfl]`; unfold `set`-bound slots (`rw [hF, hK]`) before
gate omegas.

## ✅ Also (3rd green commit, `e1f9f6f`) — E-1 block 5: the GROWTH KIT

Key analysis: the `all` case cannot close under the block-8 predicate — per-branch `K_n`
carries no growth bound, killing ω-rule uniformization.  V3 fix = witness budget bounded by
a structural function of env, paid by the control tower.  Banked SORRY-FREE: `Gexp = H_{ω²}`
closed form (`2^(x+1)(x+1)−1`), add/mul absorption, iterate kit, `Gexp^[c] = H_{ω²·c}`,
`envSup` + cons law, and **`term_val_le_Gexp_iter`** (every ℒₒᵣ term value ≤
`Gexp^[c](envSup env N)`, structural `c,N`) + `stdClosedVal_asg` bridge.  Ledger block 10.

## 🚦 NEXT (E-1 block 6 = V3)

1. **V3 predicate**: bound the env-local budget structurally (e.g. slot index
   `Gexp^[c] (envSup env N)` or side condition `K ≤ hardy e (max B (envSup env N))`);
   re-close the 8 landed cases (joins unchanged), re-prove `exs` against
   `stdClosedVal_asg_le_Gexp_iter`, then **`all`** via `envSup_cons_le` + `rel1_rel1`.

2. **`axm`** — W1/W2 proper (𝗣𝗔⁻ finite axioms via `trueRel` bounded truth à la
   `provable_true`; induction schema = cut-tower).

Then W-1 (splice composition) per the order; keep `Zef2T`/`Zef2TC` OUT of src until the
judge rules on the block-6/8 amendments.

## ⚠️ Gotchas fresh from this lap

- `Embedding.asg` needs `import GoodsteinPA.Embedding` (OperatorZef2/WainerRoute don't pull
  it in).
- `.mono_f (relSlot_le … (by omega) …)` inline fails — the target slot is a metavariable, so
  omega can't see `B`; bind `hff : ∀ x, … ≤ …` with the explicit target slot first.  Same
  for `.mono_c`: pass `(c' := …)` explicitly when the target rank is only fixed later.
- `clog 1 = 1` is `by decide` (not `rfl`/`norm_num [clog]`).
- `zero_add` is ambiguous under `open ONote` — use `Nat.zero_add` in simp sets.
- Multi-line `exact Foo (by simp) (by simpa using …)` with a line break inside the second
  `by` can produce a bogus parse error — hoist the membership into a named `have`.

## 📁 Key files
- `wip/E1EmbeddingGrind.lean` — Zef2TC + em + block-3 ladder (~790 lines; sorries: DRAFT1,
  DRAFT2, axm, all, exs — all disclosed/designated).
- `REBUILD-Z-SERIES-3-LEDGER.md` block 8 — full claim set.
