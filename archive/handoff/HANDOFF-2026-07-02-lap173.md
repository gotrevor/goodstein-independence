# HANDOFF — 2026-07-02, lap 173 (SPIKE W4B executed: **FAIL — T-W4B FIRES**)

## What this lap did

Executed `SPIKE-W4B-BUDGET.md` (the binding work order) to completion:

- **`wip/SpikeW4BBudget.lean`** — the kernel probe. Green, ONE `sorry` (the §1 running-family
  reduction statement pin, by design), no new `axiom` declarations, no `src/` edits.
  - §1 pins `cutReduceAllAuxRunning` (fixed `k₀` → running `max k₀ n`, control raised per
    SPIKE-W4's `raise`, output `osucc (α + γ)` at budget `dd + norm α + 1` — rail-minimal).
  - §2 rail: `rail_norm_genuinely_carried` (equal-exponent CNF merge is head-coefficient
    additive) + `dodge_a_norm_not_sheddable` (parametric in `C` — kills dodge (a)).
  - §3 `two_level_config`: the cut-under-`allω` configuration with branch-unbounded premise
    norms is a REAL sorry-free `Zekd` derivation (non-vacuity).
  - §4 `probe_cut_all_arm` (REAL proof, `sorryAx` only via the pin): the ∀/∃ arm CAN emit —
    but only at slot `dd_in + norm αf + 1` and control `raise (raise e B) αf`;
    `seam1_uniform_slot_unpayable` + `seam2_no_uniform_slot` kernel-refute both demands.
  - §5 dodges (b) (two seams: `norm` not `<`-monotone; ω-family premise norms unbounded) and
    (c) (`allω` exact-index discipline) + control seams (unconditional raise overshoot;
    fam-exponent escape) — all kernel-checked.
- **`SPIKE-W4B-VERDICT.md`** — verdict **FAIL**: no statement-level fix exists in the `(k,d)`
  numeric budget calculus; every candidate shape's failing inequality reduced to a kernel
  counterexample or a named unsatisfiable side condition. **T-W4B fires**: the Buchholz
  operator-controlled (`Zᵉ`) fallback fork is PINNED (what replaces `(k,d)`, what carries over,
  what rebuilds, ~7–11 laps) — NOT built.

## The decision in front of the operator

Green-light the `Zᵉ` redesign per the verdict's pin. First `Zᵉ` lap = re-run this probe's two
seams as `Zeh` statement forms (deciding experiment before any grind). Do NOT grind the seven
mechanical W4 cases / rank-0 twins / running-family port on the `(k,d)` motive — all would be
re-keyed by the fork.

## Resume pointers

`SPIKE-W4B-VERDICT.md` (the full result + `Zᵉ` pin) → `SPIKE-W4B-BUDGET.md` (the mandate) →
`SPIKE-W4-VERDICT.md` §"the residual" (what this confirmed) → `wip/SpikeW4BBudget.lean`
(the kernel artifacts). Durable batons: `JUDGE-HANDOFF.md`, `STATUS.md`, `PENDING_WORK.md`.
