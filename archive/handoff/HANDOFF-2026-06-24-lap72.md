# HANDOFF — lap 72 · RUNG 1 `zsubst` DEFINED (eigenvariable substitution on Z-derivations)

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md` (top), then `STATUS.md`.
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = **1323 jobs**. Tree clean. HEAD `d9cb91c`.
> (5 green commits this lap; HEAD started at `06c7f15`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**.
Route: `PA⊢γ →(crux-1 DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)` → Gödel II. Sole math wall = crux-2,
localized to **`RedSound`** (`InternalZ.lean`): `∀ d, ZDerivesEmpty d → ZDerivation (iR2 d)` = real
internalized cut-elimination. Lap 70 forced **Option A** (a genuine validity-preserving reduct,
shape-dispatched). The lap-70 LADDER: rung 0.5 (✅ lap 71) → **rung 1 `zsubst`** → rung 2 (genuine Ind
reduct) → rung 3 (genuine K/cut reduct) → rung 4 (`RedSound` tag-dispatch).

## ✅ THIS LAP — RUNG 1 `zsubst` DEFINED + Σ₁-definable, all axiom-clean, green 1323
The Buchholz I∀/Ind reducts need **eigenvariable substitution** `d[n] := d0(a/n)` (replace free var
`^&a` by numeral `n` throughout a derivation). PENDING_WORK's open design question — "free-var subst is
NOT `substs1`" — is now **resolved + built from scratch**, bottom-up:

**New file `src/GoodsteinPA/FvSubst.lean`** (a `module`; general Foundation-level infra over `ℒₒᵣ`/`L`):
- `termFvSubst a t u` — replace `^&a` by term `t` in a coded TERM (`Language.TermRec`, params `a,t`:
  identity on bvar/func, `if x=a then t else ^&x` on fvar). Definability `𝚺₁-Function₃/₄`, vec
  accessors, `IsSemiterm(Vec)` preservation. Commit `f4d9d2c`.
- `fvSubst a t p` — the same on a coded FORMULA (`UformulaRec1`, param `⟪a,t⟫`: atoms call
  `termFvSubstVec`; **`allChanges`/`exsChanges` are identity** — sound because our `t` is always a
  CLOSED numeral, so no `termBShift` needed under binders). 8 constructor rewrite lemmas +
  `𝚺₁-Function₃` definability. Commit `8696c1d`.
- `fvSubst_isSemiformula` — `fvSubst a t` preserves `IsSemiformula L n` for closed `t`
  (`bounded_all_sigma1_order_induction` + `IsSemiformula.case_iff`, mirror `IsSemiformula.subst`;
  `isSemiterm_weaken` for the `n→n+1` under-binder step). Commit `cce617c`.

**New file `src/GoodsteinPA/Zsubst.lean`** (plain import of `InternalZ` + `FvSubst`; namespace
`GoodsteinPA.InternalZ`):
- `fvSubstSeq a t Γ` — map `fvSubst a t` over a coded formula SEQUENCE (`PR.Construction` over a length
  counter, param `⟪a,t⟫`, mirror `tpSeqAux`/`iseqMaxAux`) + `Seq`/`lh`/`znth` read-out lemmas.
- `fvSubstSeqt a t s` — substitute a whole sequent `s = ⟪Γ,C⟫` (antecedent = `fvSubstSeq` over the
  sequence; succedent = single `fvSubst`). `seqAnt`/`seqSucc` simp lemmas. Commit `7ea3849`.
- `tblMapSeq tbl ds` — map a value-table read over a premise sequence (the zK case; `seqCons`-collect
  variant of `iseqMaxAux`) + read-out lemmas.
- `zsubstNext d s a t` — the table STEP, dispatching all **7 tags** (zAtom/zIall/zIneg/zInd/zK/zAxAll/
  zAxNeg): rebuilds each constructor with substituted end-sequent (`fvSubstSeqt a t (fstIdx d)`),
  substituted principal formula(s) (`fvSubst`), substituted Ind induction term (`termFvSubst`),
  eigenvariable index left INTACT, children read (already-substituted) from table `s` (zK premises via
  `tblMapSeq`). `𝚺₁-Function₄` via the 7-way tag blueprint `zsubstNextDef`.
- `zsubst d a t = znth (zsubstTable a t d) d` — course-of-values `<`-recursion (`zsubstTable`/`PR.Blueprint 2`,
  mirror `iRTable`/`iR2`). `𝚺₁-Function₃`. Commit `d9cb91c`.
- Added accessors `zIallEig` (`= π₁(zRest d)`) + `zAxAllK` Def (both were missing). All axiom-clean
  `[propext, Classical.choice, Quot.sound]`.

## ▶ NEXT — rung 1 CORRECTNESS (the `zsubst` is defined; now prove it does the right thing)
Three properties (lap-70 LADDER step 1), in dependency order:
1. **`fstIdx_zsubst : fstIdx (zsubst d a t) = fvSubstSeqt a t (fstIdx d)`** — the end-sequent computes.
   Easiest: `zsubst d = znth (zsubstTable a t d) d`; need `znth (zsubstTable a t d) d = zsubstNext d
   (zsubstTable a t d) a t` (the diagonal read-out, mirror `iR2`'s `znth_iRTable` lemmas — find them in
   `InternalZ.lean` ~line 4380+), then `fstIdx (zsubstNext …) = fvSubstSeqt …` by the 7-tag case split
   (each constructor's `fstIdx (z* s' …) = s'`).
2. **`ZDerivation_zsubst : ZDerivation d → IsSemiterm ℒₒᵣ 0 t → ZDerivation (zsubst d a t)`** (rung-1
   correctness, the genuine content). Needs: the child `<` bounds (each premise code `< d`, so the
   table read hits the right value — check `InternalZ` for `zIallPrem_lt`/strict-bound lemmas, mirror
   how `iR2` correctness reads premises), the per-rule validity under substitution (each Z rule commutes
   with `fvSubst`), and likely an **eigenvariable-freshness hypothesis** (`a ∉` the inner eigenvariables/
   conclusion — Buchholz's regularity). Drive by `ZDerivation`-induction (`zDerivation_iff`/the lap-70
   inversions `zDerivation_z*_inv`).
3. **`iotil_zsubst = iotil`** (õ substitution-invariance — the ordinal side already assumes it). `iotil`
   is shape+rank-based and `irk` is subst-invariant (`irk_substs1`; prove the `fvSubst`/`termFvSubst`
   analog `irk_fvSubst`), so this should follow structurally — likely the EASIEST of the three.

Then **rung 2** (genuine Ind reduct `iRInd' = zK s (irk p) ⟨d0, zsubst d1 at' 0,…, zsubst d1 at' (k−1)⟩`,
`k = ⟦induction term⟧`; the most tractable wall — premises are genuine sub-derivations) becomes reachable.
Blueprint for the genuine reductions: `~/src/Gentzen/theories/Logic/cut_elim.v` (port the SHAPE; findings
in `archive/findings/ON-LINE-FINDINGS-2026-06-24-bryce-gore-gentzen.md`).

## Parallel front (independent of the reduct)
- `PA_delta1Definable` is STILL an upstream Foundation `axiom` (`Incompleteness/Examples.lean:17`) —
  mandatory for axiom-free. PA = 𝗣𝗔⁻ + `InductionScheme ℒₒᵣ Set.univ`; need a Δ₁ `ch` for "φ is the
  univ-closure of `succInd` of some 1-var formula" (`Theory.Δ₁` class, `Theory.lean:13`). Bounded
  arithmetization, no deep math — independent of the cut-elimination wall. (Likely poor Aristotle fit:
  heavy Foundation-specific syntax.)
- C0.5 bridge `¬Con(M) → ∃ z, ZDerivesEmpty z` (Bryce–Goré `Peano.v` three-layer shape).

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. `RedSound` is the lone InternalZ obligation; do NOT axiomatize it
or fake the reduct.

## Lap discipline
`FvSubst.lean`/`Zsubst.lean` are in `src/` (green-gated, sorry-scanned), both sorry-free. Commit each
green build, end the turn after each so the governor can pace. Aristotle available (`aristotle list`
worked) but no clean self-contained open lemma was architected this lap (the rung-1 correctness lemmas
all depend on the new defs; not a good remote fit).

## Commit log (lap 72, branch `plan`)
- `f4d9d2c` term-level `termFvSubst`
- `8696c1d` formula-level `fvSubst`
- `cce617c` `fvSubst_isSemiformula` (preservation, closed t)
- `7ea3849` `fvSubstSeq` + `fvSubstSeqt`
- `d9cb91c` `zsubst` DEFINED (tblMapSeq + zsubstNext + zsubstTable + zsubst, Σ₁-definable)
