# HANDOFF — 2026-06-23 (lap 38, **internal-ONote substrate COMPLETE: icmp + isNF + order-reflection**)

> **Branch** `plan` · build **green** (`lake build GoodsteinPA`, **1307 jobs**) · headline
> `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, **0 math axioms**). **Tree clean.**

## ✅ Done this lap (3 green commits) — the internal ε₀-notation arithmetic is now fully in place

All in `src/GoodsteinPA/InternalONote.lean` (sorry-free, axiom-clean `[propext, choice, Quot.sound]`):

1. **`icmp` (internal CNF comparison)** — `ONote.cmp`/`ONoteComp.cmpStep` internalized via a
   **pair-indexed** course-of-values table (`icmpTable ⟪c1,c2⟫`; sub-comparisons at `⟪ocExp,ocExp⟫`/
   `⟪ocTail,ocTail⟫`, both `< ⟪c1,c2⟫` by `pair_lt_pair`). `𝚺₁`-definable. Recursion law `icmp_ocOadd
   = thenV (icmp e1 e2) (thenV (cmpV n1 n2) (icmp r1 r2))` + base cases. Helpers `thenV`/`cmpV`.
2. **`isNF` (internal CNF well-formedness)** — `isNF c := isNFb c = 1`, a `0/1` CofV flag built as a
   **product** of four definable indicators (coeff≠0, exp NF, tail NF, tail-exp ≺ head-exp). Product
   form keeps the step `𝚺₁` with **no negated existentials** (the De Morgan trap). `𝚺₁`-Predicate.
   Recursion `isNF_ocOadd : isNF (oadd e n r) ↔ n≠0 ∧ isNF e ∧ isNF r ∧ (r=0 ∨ icmp (ocExp r) e = 0)`.
3. **Order-reflection `ievalNat_lt_of_icmp_eq_zero`** (Rathjen 2.3(iii)) — **THE crux** the descent's
   `ineq6_step` consumes: on `isNF`/`iCanon b`, `icmp o p = 0 ⟹ ievalNat b o < ievalNat b p`. Proved
   **digit-direct** (no ordinals ⟹ it internalizes; the ℕ-level `evalNat_lt_iff`'s `toOrdinal` route
   does NOT). The value-bound `TB` and monotonicity `MONO` are mutually recursive, so carried in ONE
   strong induction on a single code measure. `icmp_eq_imp_eq` (separate induction) gives the eq case.
   Support: `ocOadd_destruct`, `ievalNat_pos`, `tb_imp_bd`, subterm NF/Canon projections.

**Substrate inventory (all in `InternalONote.lean`, all axiom-clean):** codes `ocOadd`/`ocExp`/
`ocCoeff`/`ocTail` + bounds · `iC` (max-coeff) · `ievalNat` (T̂) · `iCanon` · `icmp` · `isNF` ·
**order-reflection**. This is the ε₀-notation arithmetic re-formalized inside `IΣ₁` — the deep heart.

## Aristotle
**`ibump_mono` COMPLETE** (UUID `7c8bb0e8`), downloaded + extracted to
`scratchpad/ibump_x/ibumpjob_aristotle/ibump_mono.lean` (axiomatized-ℕ form, `Nat.strong_induction_on`,
same combined upper-bound+strict-mono technique I used for order-reflection). **NOT yet ported to V** —
order-reflection didn't need it. Port to `InternalBump` when a bump-monotonicity step needs it (the
Nat→V port must replace `omega`/`Nat.*` with V ordered-semiring lemmas — see the no-omega-ring memory).
No job currently running; next OPEN lemma to feed = internal `evalNat_succ_base` (below) once scoped.

## NEXT (hardest-first) — from substrate to `hbound`
The lone wall is `hbound` in `DescentSemantic.no_min_descent_absurd_of_goodstein:392`. Remaining:

1. **Internal `evalNat_succ_base`** — `ievalNat (b+1) c = ibump (b+1) (ievalNat b c)` for `isNF`/
   `iCanon b` c. Structural induction; needs `ilog (b+1) (n·(b+1)^E + tail) = E` (peel leading digit),
   which uses the **tail bound** `tail < (b+1)^E` — now available inside order-reflection's `TB`
   (extract it as a standalone `ievalNat_tail_lt` lemma first). Connects `ievalNat` base-change to the
   Goodstein bump. May need ported `ibump_mono`/`ibump` digit facts.
2. **Internal `ineq6_step`** — port `DescentCore.ineq6_step` onto codes in `M`, using
   `ievalNat_lt_of_icmp_eq_zero` (have it) + `evalNat_succ_base` (piece 1). This is `hbound`'s `step`.
3. **Seam/F re-wire to transparent `natCodeT`** (route (b), the risky girder touch): make the descent
   order `Mlt`/`precφ` decode to `icmp` on codes. Re-validate `peano_not_proves_TI` with `#print
   axioms` after EVERY change (must stay girder-clean). `epsilon0_le_orderType_ltPull` holds for ANY
   `e : ℕ≃NONote`, so the order-type half transfers.
4. **`βₖ` slow-down** (Rathjen Thm 3.5) from `descent_seq_exists` on internal codes (`C(βₖ) ≤ k+1` via
   `iC`), `b k = ievalNat (k+2) βₖ`; assemble `hbound` (`base`/`step`/`hpos` + `𝚺₁`-definability).

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`. Route (b)
re-touches the proven `peano_not_proves_TI` — re-`#print axioms` after every seam/F change.

## src/ sorries (unchanged): `Statement.lean:22` (headline, locked), `Reduction.lean:50` (off-path
Route-A), `DescentSemantic.lean:392` `hbound` (the lone genuine wall). `InternalONote.lean` sorry-free.
