# Handoff: lap 160 — WIRE the load-bearing leaf `genReduct_chain_noRedex_anySucc`

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `d4ce7e3` · build 🟢 green (1326 jobs,
`lake build GoodsteinPA`). In-kernel (real `#print axioms`): `false_of_ZDerivesEmpty` =
`[propext, sorryAx, Classical.choice, Quot.sound]` (**0 math axioms**, no drift). No uncommitted edits.

## What this lap did (executed lap-159 NEXT priority #1)
Ported `genReduct_chain_noRedex_anySucc` (`Crux2Blueprint:3407`) — the **load-bearing leaf** of
`genReduct_anySucc` — from a bare whole-lemma `sorry` to a **fully-wired §14.254 dispatch** with ONE narrow
internal `residual` sorry. Modelled faithfully on the proven ⊥-version `genReduct_chain_noRedex`. One commit:

- **`d4ce7e3`** — `genReduct_chain_noRedex_anySucc` body:
  - **§14.25 selection GENERALIZED**: least exit `jstar` over `{seqSucc s, ⊥}` (was: least ⊥-exit).
  - **{3,4}-PRODUCER CORE WIRED** (the irreducible heart of crux-2 = Buchholz Thm 2.1): closes via the
    GENERAL `IH` + Γ/exit-general `certReplace_of_premise_cert` (`repProducerClose`, passing `hAj0`).
  - **C-exit leaf** → `zAtom s` (`leafCloseC`); **tag-6** → succedent-AGNOSTIC `zAxNeg s q` (`axNegCloseGen`,
    ports VERBATIM); **tag-5/6 cut-partner CLIMB** → `tryProducerClose`/`closeZAxNeg` (lap-157
    `climb_to_rep_producer` + `rightSym_producer_redex`, confirmed succedent-agnostic → port free).
  - **`residual`** isolates the genuinely-NEW anySucc content (see NEXT).

## ⭐ Kernel-grounded findings (lap 160)
1. **The 8 ZPhi rules' succedent constraints** (`InternalZ:5458`): `zAxNeg` (tag 6) is the LONE
   succedent-AGNOSTIC rule (ex-falso: `¬q,q∈Γ ⟹ Γ→C` for ANY `C`); `zAtom`/`zAx1` require `seqSucc s∈Γ`;
   `zAxAll` forces `seqSucc s = substs1 k p`. ⟹ the C-exit escapes close cleanly; the ⊥-exit leaf/zAxAll
   escapes (`⊥∈Γ`/`^∀⊥∈Γ ⟹ Γ→C`) have NO single-node Z-rule.
2. **The ⊥-exit ex-falso has no cheap reduct**: a single-premise chain wrapper `zK s 0 [w]` has
   `iotil = ω^(õ w)` which can EXCEED `iseqNaddIdg ds` (fails when the chain is a single leaf,
   `iseqNaddIdg = 1`). So ex-falso to general `C` is genuine new infra (needs an internal ⊥-elim / weakening
   rule, or a witness-bounded reduct). This is the deep core of `residual`.
3. **`iRedDescent`** (`InternalZ:6169`) is just `idg≤ ∧ icmp(iotil) descent ∧ isNF(iotil)` —
   `iord_descent_of_iRedDescent` (`:6194`) converts to full `iord` descent given `isNF(iotil d)`.
4. **`genReduct_chain_noRedex` (⊥-version) returns `Or.inl` (certReplace) in EVERY non-sorry branch**
   (escapes + producer-closes); the WHOLE-CHAIN `certFlatten` does NOT descend standalone
   (`ω^õa # ω^õb` vs `iseqNaddIdg`). ⟹ delegating `descent_step_K_noncrit_axMajor` to it would need a
   `certFlatten → ∃d'` converter (missing infra: build the critical reduct from arbitrary halves + the
   `iRKcCrit` descent). The clean route for axMajor is the CUT-PARTNER climb (reuse `tryProducerClose`).

## 🎯 NEXT (priority order)
1. **Split + attack `residual` (`Crux2Blueprint:3407`)**. It absorbs exactly: (i) **⊥-exit ex-falso**
   (`⊥∈Γ` from tag-0/7 leaf at a ⊥-exit, or `^∀⊥∈Γ` from a tag-5 ⊥-exit escape) ⟹ `Γ→C`; (ii) **C-exit
   R-intro replay** (tag-1/2 major producing the conclusion succedent `C` directly); (iii) the **tag-5
   climb-escape** (`^∀G∈Γ`) + **tag-6 partial thread** (= the ⊥-version's own `axMajorResidual`,
   `:3683` — these are NOT new). Decompose `residual` into these named sub-sorries; (iii) is shared with
   `axMajorResidual` so prove ONCE. (i) needs new infra (finding #2); (ii) likely needs internal weakening.
2. **`genReduct_chain_hasRedex_anySucc`** (`:3391`) — ∀-redex sub-case is a CLEAN port
   (`certFlatten_of_critHalves` generalized off ⊥, lap 159, `:2986`); ¬-redex sub-case = the `keepTip`
   (`ZDerivation_corrected_haux0_neg_botOrbit` `:~738`) which GENUINELY needs the ⊥-exit (lap-159 finding).
3. **`ind_reduct_anySucc`** (`:3382`) — lap-136 `k`-fold unfolding `⟨d0, d1[a:=0..k-1]⟩` (k=⟦t⟧); for a
   CLOSED succedent vacuous → `d0`. Note: the caller `genReduct_anySucc` (`:3465`) passes NO closedness, so
   the GENERAL statement is needed (the hard unfolding). CHECK `zIndWff` admits a closed-`C` zInd.
4. **`descent_step_K_noncrit_axMajor`** (`:4180`, Γ=∅ twin) — cut-partner lemmas
   (`majorPrem_zAxAll_cutPartner`/`_zAxNeg_cutPartner`, `InternalZ:9592/9620`) give `i'` with
   `chainAsucc ds i' = cutFormula`; climb to its tag-3/4 producer `m` (reuse `tryProducerClose`-style),
   reduce `znth ds m` via the now-wired `genReduct_anySucc` (general succedent), splice via
   `descent_step_K_replace`/`descent_step_K_spliceHalves` at `m` (mirror the PROVEN `repMajor` `:3930`).
5. **Retire the ⊥-stack** (handoff lap-159 step 4) — only AFTER the 3 anySucc leaves are proven:
   `genReduct_botSucc` → `genReduct_anySucc` thin wrapper ⟹ DROPS `axMajorResidual` (`:3683`).

## ⚠️ Forbidden / binding (DIRECTION.md lap-158 — still in force)
Same-degree `õ`-drop for the {3,4} producer (REFUTED lap 157); `red`; `iord`-recursion (CODE-induction
ONLY — confirmed by lap-158 spike); the refuted single-premise `seqUpdate` splice (`descent_step_K_splice`);
attacking `axMajorResidual`:3683 / `descent_step_K_noncrit_axMajor`:4180 / gDef:4309 in a way that ADDS
net sorries instead of reducing; the off-path dead `red`-soundness sorries {`:80,:1253,:1356,:1529,:1616,
:1705,:1867`} AS STATED; M2 / M4 wiring. ALTITUDE: M2 (Foundation→Z bridge) ~0% built — "only the crux
left" ≠ "almost done."

## 📁 Key anchors (line numbers as of HEAD d4ce7e3)
- `genReduct_chain_noRedex_anySucc` (WIRED, `residual` sorry inside) `Crux2Blueprint:3407`;
  `genReduct_anySucc` (PROVEN entry) `:3444`; `genReduct_anySucc_chain` (dispatcher) `:3425`.
- The other 2 anySucc leaves: `ind_reduct_anySucc` `:3382`, `genReduct_chain_hasRedex_anySucc` `:3391`.
- `genReduct_chain_noRedex` (⊥-version, `axMajorResidual` `sorry`) `:3683`; `certReplace_of_premise_cert`
  (Γ/exit-general splice) `:3283`; `certFlatten_of_critHalves` (off-⊥, lap 159) `:2986`.
- `descent_step_K_noncrit_axMajor` `:4180`; `descent_step_K_noncrit_repMajor` (PROVEN template) `:3930`;
  `descent_step_K_replace` `:2477`; `descent_step_K_spliceHalves` `:3871`.
- `exists_sigma1_descending_step` / gDef `:4309`; `false_of_ZDerivesEmpty` `:4423`(approx, shifted).
- Helpers (succedent-agnostic): `leastSucc_in_ant_or_nonleaf` `InternalZ:9076`, `climb_to_rep_producer`
  `:9179`, `rightSym_producer_redex` `:9151`; cut-partner `:9592/:9620`; `iRedDescent` `:6169`.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-158, binding) · `PENDING_WORK.md` lap-160 top.

---
**→ Next session: split `residual` (`:3407`) into the 3 named sub-cases (i ⊥-exit ex-falso, ii C-exit
R-intro, iii = shared `axMajorResidual`); attack (iii) once for both, scope (i)'s ex-falso infra. Keep
build green + 0 math axioms.**
