# Handoff: SERIES-3 lap 202 — E-1 blocks 7b–9a: V3 ladder 9/10, PA⁻ `axm` DONE, induction kit part 1

**Date**: 2026-07-03 · **Branch**: `plan` · **HEAD**: `d807e08` · **Tree**: CLEAN ·
**6 green commits this lap** (`8489701` V3 and/cut, `9e2a3f4` V3 exs, `68c6626` ExFree truth
engine, `d9dd9d3` PA⁻ dispatcher, `a212451` addEqOfLt, `d807e08` closure peel).
**Build**: 🟢 bare **`lake build`** (1342 jobs). **Headline** unchanged/undrifted
(`peano_not_proves_goodstein = [propext, Classical.choice, goodstein_implies_consistency,
Quot.sound]`).

**Order in force**: `REBUILD-Z-SERIES-3-ORDER-2026-07-03.md`. **Directive (OUTRANKS this baton)**:
`DIRECTION.md` CURRENT DIRECTIVE = lap-201 block (MANDATE = Lane E rung E / V3 — unchanged, this
lap worked strictly inside it).

## ✅ This lap (all in `wip/E1EmbeddingGrind.lean`, all `[propext, Classical.choice, Quot.sound]`)

1. **V3 `and`/`cut`** (`budgetedEmbedsV3_and/_cut`) — two-premise joins, SIMPLER than block-8:
   the structural invariant `Nlog α ≤ B` + `le_relSlot_zero` pays the root gate directly (no
   succ-gap rungs). `B := max B₁ B₂ + norm e₁ + norm e₂ (+ complexity) + 2`, `N := max N₁ N₂`.
2. **V3 `exs`** (`budgetedEmbedsV3_exs`) — the env-dependent witness `m = stdClosedVal (asg env t)`
   paid STRUCTURALLY: control tower `e := osucc (e₁ + ω²·(c+1))`;
   `m ≤ Gexp^[c](envSup env Nt) ≤ hardy(ω²·(c+1)) M ≤ hardy e (max B M) ≤ F 0`
   (`Gexp_iter_eq_hardy` + `hardy_le_of_lt`, norm gate absorbed into `B`). No domination field.
3. **W1 truth engine** (`ExFree`, `ExFree.rew`, `truth_exFree_Zef2TC`) — TRUE ∃-free closed
   formulas are cut-free `Zef2TC`-derivable at rung `ofNat(2k+1)`; no `exI`, no witness budget.
4. **PA⁻ `axm` DONE** (`budgetedEmbedsV3_axm_PAminus`) — all 17 PA⁻ axioms + 5 EQ shapes
   (funcExt ×4 symbols, relExt ×2) dispatch through `budgetedEmbedsV3_of_exFree_true`
   (+ `atomTrue_asg_emb`, `asg_emb_fix`). Concrete `ExFree` by simp with
   `Operator.eq_def/lt_def/LE.def_of_Eq_of_LT, imp_eq, allClosure-unfold, Matrix.vecTail`.
5. **`addEqOfLt` DISCHARGED** (`budgetedEmbedsV3_addEqOfLt`) — the sole ∃-carrying PA⁻ axiom,
   bespoke: two `allω` peels with **cons-vector collapse** (`embedding_subst_q_cons_app` — NO
   bShift residue; this was the key trick), `orI`, trichotomy (`trueNrel` if `a ≥ b`, else `exI`
   at witness `b−a ≤ b ≤` branch slot).
6. **Induction kit part 1** (`osuccs`, `osuccs_succ_shift`, `Cl_osuccs`, `Nlog_osuccs_le`,
   `allClosure_peel`) — the `univCl` layer: numeral-instance families at `α` (quantified over
   `(H, f)` em_cong-style) lift to `∀⁰*` closures at `osuccs α ℓ`.

**V3 ladder: 9/10.** Sole remaining case = `axm` for the **induction schema**
(`univCl (succInd φ)`, `φ` arbitrary).

## 🚦 NEXT — the succInd cut-tower (the last rung-E leaf)

Target: `budgetedEmbedsV3` for `↑(univCl (succInd φ)) ∈ Γ`. Plan (designed this lap):
- `univCl X` coerces to `univCl' X = ∀⁰* (Rew.fixitr 0 X.fvSup ▹ X)` (simp
  `coe_univCl_eq_univCl'`); `asg_emb_fix` kills the env. Feed `allClosure_peel` with
  `ℓ := fvSup`, `χ := fixitr ▹ succInd φ`.
- Per numeral instance `w`: `succInd` unfolds (`imp_eq`) to `∼A ⋎ (∼B ⋎ C)` =
  `{∼ψ/[‘0’], ∃x(ψ(x) ⋏ ∼ψ(x+1)), ∀x ψ(x)}` after two `orI`s (ψ := the w-substituted φ).
- `allω` on `C`; branch `n` = **cut tower** `D_k` (`k ≤ n`), induction on `k`:
  - `D_0`: value-congruent EM `em_cong1_Zef2TC` pair `(nm 0, ‘0’)` (term-vs-numeral mismatch!).
  - `D_{k+1}` from `D_k` by `cut` on `ψ/[nm k]` (rank `complexity+1`, absorbed into `d`/`B`):
    right premise fires the step disjunct: `exI` witness `k` (≤ branch slot), `andI`, two EM
    leaves — the `x+1` leaf via `em_cong1` pair `(nm (k+1), ‘nm k + 1’)`, values equal.
  - Branch ordinal grows LINEARLY in `n` (`ofNat(c₀ + c₁·n)`-shaped) → `allω` root must be
    ω-level: `expTower (ofNat 1) = ω` is in every `Cl` (`Cl.expTower (Cl.ofNat 1)`);
    `Nlog ω` is small. Use `Zef2TC.weak` to raise tower nodes below the uniform ω root.
- Then assemble `budgetedEmbedsV3_axm` (PA⁻ ∨ induction), swap into the master ladder
  (`budgetedEmbedding` over V3), rung-E statement + E-0 probe → **judge pass**.
  **Do NOT self-ratify into `src`** (directive).

## ⚠️ Gotchas fresh this lap
- **`embedding_subst_q_cons_app` is THE tool** for `(subst w).q ▹ ·)/[nm m]` — collapses to
  `subst (nm m :> w)`, no bShift. Bespoke-axiom grinds become mechanical with it.
- `Rew` composition identities: prove `(asg env).comp Rew.emb = Rew.emb` by `ext x` with
  bullets `x.elim0` / `x.elim` (Fin 0 / Empty) — `congr 1` alone mis-splits.
- Concrete-axiom `ExFree`/shape sims need: `Semiformula.Operator.eq_def/lt_def`,
  `LE.def_of_Eq_of_LT`, `imp_eq`, `Matrix.vecTail/vecHead/comp_vecCons`, `Matrix.empty_eq`,
  `Rew.func`. `fin_cases` is NOT available; avoid `show (1 : Fin 2) = (0:Fin 1).succ` simp
  loops (maxRecDepth).
- `atomTrue` is DEFEQ-transparent through ⋏/⋎/⊥ (use `htrue.1`, `htrue.elim` directly);
  for ∀-instances go through `eval_substs` + `Embedding.valm_nm` + `Matrix.constant_eq_singleton,
  Matrix.empty_eq`.
- `Semiformula.rec'/cases'` constructor is `exs` not `ex`; rew-of-∀ lemma = `Rewriting.app_all`.

## 📁 Key files
- `wip/E1EmbeddingGrind.lean` (~2450 lines) — everything above at the tail; `#print axioms`
  block at EOF re-verifies the whole set.
- `REBUILD-Z-SERIES-3-LEDGER.md` — ledger through block 11 (lap-201; this lap = blocks 7b–9a,
  ledger entry pending next altitude pass).
