# Handoff: SERIES-3 lap 201 — FRESH-MIND REVIEW + E-1 block 6–7a: V3 predicate, `all` DISCHARGED, ladder 6 cases sorry-free

**Date**: 2026-07-03 · **Branch**: `plan` · **HEAD**: `59ee78c` · **Tree**: CLEAN ·
**3 green commits this lap** (`a7ee836` review, `eea0709` V3+all, `59ee78c` V3 ladder 6 cases).
**Build**: 🟢 bare **`lake build`** (1342 jobs). **Headline**:
`GoodsteinPA.peano_not_proves_goodstein = [propext, Classical.choice,
goodstein_implies_consistency, Quot.sound]` (sorryAx OFF, undrifted, re-verified this lap).
`blueprint_audit` ✓ 16.

**Order in force**: `REBUILD-Z-SERIES-3-ORDER-2026-07-03.md`. **Directive (OUTRANKS this baton)**:
`DIRECTION.md` CURRENT DIRECTIVE = **lap-201 block** (MANDATE = Lane E rung E / V3). **Ledger**:
`REBUILD-Z-SERIES-3-LEDGER.md` (block 11 this lap).

## ✅ This lap — two parts

### Part 1 — FRESH-MIND REVIEW (docs commit `a7ee836`)
Re-verified terminal state (real `#print axioms`): headline undrifted; rungs **P**
(`cutElimPass_Zef2`), **R** (`rankToZero_Zef2`), **D** (`readoff_delta0_Zef2`) all REAL,
axiom-clean `[propext, choice, Quot.sound]`; splice `wainer_splice_Zef2` (`WainerLadder.lean:41`)
carries the sole live `sorryAx`. Direction SOUND (laps 198→200 walked N→D→E crux-first). Retargeted
the stale lap-192 `DIRECTION.md` directive → **lap-201 CURRENT DIRECTIVE block**: MANDATE = Lane E
rung E, E-1 block 6 = V3, close `all` (decisive) + `axm`. Refreshed STATUS.md, PENDING_WORK.md.

### Part 2 — E-1 block 6: V3 + `all` DISCHARGED (green commit)
`wip/E1EmbeddingGrind.lean`. `budgetedEmbedsV3_all` SORRY-FREE, `[propext, Classical.choice,
Quot.sound]`.

- **`BudgetedEmbedsV3`** — the refined V3 predicate. The block-8 predicate bound BOTH the node
  ordinal `α` AND the witness index `K` *per assignment*; that is what forced `all` to uniformize
  unbounded per-branch `(K_n, α_n)`. **V3 moves `α` and the budgets `B,d,N,c` OUTSIDE `∀ env`**
  (they are env-independent in fact — every case builds them from `complexity`), leaving the ONLY
  env-dependence in the slot index, fixed as `envSup env N`. Predicate carries `Nlog α ≤ B`
  (absorbing-norm gate invariant) + `∀ x, Gexp^[c] x ≤ ewRootSlot e B x` (the `exs` domination field).
- **`budgetedEmbedsV3_all` — THE DECISIVE CASE — DISCHARGED.** Both uniformizations dissolve:
  ordinal `β n := α` uniform (root `osucc α`), budget index paid by the branch relativization via
  `envSup_cons_le` + `rel1_rel1` + `relSlot_mono`. `free`/`shift`/`nm` sequent identities port
  verbatim from `Embedding.lean`'s `Provable` `all` case; operator via `change_H`; gate via
  `Nlog_osucc_le` + `le_relSlot_zero`. Banked `ewRootSlot_mono_B` (one line).
- **Significance**: `all` was the route-critical sub-crux of rung E (the sole remaining hard rung).
  Its failure would have forced a redesign of the SomeK/W3 shape. The block-8 "the `all` case
  cannot close" obstruction is confirmed a **predicate-shape artifact**, now compiler-refuted.

### Part 3 — E-1 block 7a: V3 ladder, 6 cases sorry-free (green commit `59ee78c`)
`BudgetedEmbedsV3` finalized **field-free** — dropped the `Gexp`-domination field (it is a LOCAL
`exs`/`axm` witness concern, not a global merge-able invariant; removing it makes the two-premise
joins trivial). New predicate:
`∃ B d N, ∃ e α, e.NF ∧ α.NF ∧ Nlog α ≤ B ∧ ∀ env, Zef2TC α e ⊤ (rel1 (ewRootSlot e B) (envSup env N)) d (Γ.image …)`.
Migrated 6 cases from the block-8 `BudgetedEmbedsTC` ladder to V3, all `[propext, choice, Quot.sound]`:
`closed` (`em_Zef2TC'`, structural `α = ofNat(2·complexity+1)`, `envSup env 0 = 0`), `verum`, `wk`,
`or` (`osucc` root, `B+1` for `Nlog`/gate slack via `relSlot_mono` + `le_relSlot_zero`), `shift`
(index absorbed by `N+1`; banked `envSup_shift_le`), `all` (re-proved field-free). The block-8 ladder
(proving `BudgetedEmbedsTC`) stays intact above — V3 is the parallel replacement.

## 🚦 NEXT (E-1 block 7b — finish the V3 ladder, then `axm`)

Migrate the remaining cases to `BudgetedEmbedsV3`:
- **`and`/`cut`** (two-premise joins) — port from block-8 `budgetedEmbedsTC_and/_cut`: control
  `osucc (e₁ + e₂)` (`relSlot_le he_i heNF hlt_i` with `norm e_i` absorbed into `B`), structural
  root `osucc (α₁ + α₂)` (`Nlog_add_le_max_succ` + slack), `B := max B₁ B₂ + norm e₁ + norm e₂ + 2`
  (covers the `Nlog (osucc (α₁+α₂)) ≤ B` invariant), `N := max N₁ N₂` (`envSup_mono_N` per premise),
  `d := max d₁ d₂` (+ `φ.complexity+1` for `cut`). Transport each premise by `change_e` + `mono_f`
  (`relSlot_le`) + `mono_c`. These are the structural-α analogues — simpler than block 8 (no per-env
  ordinal). Then:
1. **`exs`** — re-prove against `stdClosedVal_asg_le_Gexp_iter` + the `Gexp`-domination field:
   `m ≤ Gexp^[c](envSup env N) ≤ ewRootSlot e B (envSup env N) = f 0`. To DISCHARGE the domination
   field itself, choose the control tower `e > ω²·c` (NF) and `B ≥ norm(ω²·c)` so
   `Gexp^[c] x = hardy(ω²·c) x ≤ hardy e (max B x) ≤ ewRootSlot e B x` (via `hardy_le_of_lt` with
   argument `≥ B ≥ norm(ω²·c)`). `Gexp_iter_eq_hardy` gives `Gexp^[c] = H_{ω²·c}`.
2. **`axm`** — W1/W2 finite 𝗣𝗔⁻ axioms via `trueRel` bounded truth; induction schema = cut-tower.
3. Reassemble `budgetedEmbedding` over V3, then the rung-E statement + E-0 Ax2 probe → next judge
   pass for ratification. **Do NOT self-ratify the rung-E statement into `src`.**

The 8 block-8 cases + `exs` stay in the file (proving `BudgetedEmbedsTC`); migration to V3 is the
block-7 work. `axm` is the genuinely-remaining content after that.

## ⚠️ Gotchas fresh from this lap
- `Embedding.asg` unfolds to `ZinftyF.nm`; `Zef2TC`'s scope uses `OperatorZinfty.nm` (defeq). In
  the `free`/`nm` `hRew` identity, unfold BOTH in the simp set:
  `simp [Embedding.asg, Rew.comp_app, ZinftyF.nm, GoodsteinPA.OperatorZinfty.nm]`.
- `envSup`/`max` chains: `omega` can choke; prefer an explicit `calc` with `envSup_mono_N` +
  `envSup_cons_le` + `Nat.max_comm`.
- The `all` root ordinal `osucc α` needs `+1` gate slack — carry the structural invariant
  `Nlog α ≤ B` and pay via `B+1` at the root (`ewRootSlot_mono_B` transports the domination field).

## 📁 Key files
- `wip/E1EmbeddingGrind.lean` — `BudgetedEmbedsV3` + `budgetedEmbedsV3_all` (new, sorry-free) at
  the tail; the block-8 `BudgetedEmbedsTC` ladder (8/10) + growth kit above.
- `REBUILD-Z-SERIES-3-LEDGER.md` block 11 — full claim set.
