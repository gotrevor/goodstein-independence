# HANDOFF — lap 87 · crux-2: 5.2.1 splice VALIDITY object COMPLETE (object-level)

> Supersedes prior HANDOFF-*.md. Read order: this → `STATUS.md` → `PENDING_WORK.md` (lap-87 box) →
> `ANALYSIS-2026-06-25-lap87-splice-order-sensitivity.md`. Branch `plan`. HEAD `7a54718`.
> Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Tree clean.

## ✅ 6 GREEN COMMITS THIS LAP (axiom base untouched, headline still 0 math axioms)
All new lemmas axiom-clean `[propext, Classical.choice, Quot.sound]`. All in `InternalZ.lean`.
1. `fcbb15f` — **5.2.1 validity reductions (order-independent)** `isChainInf_iSpliceEnd` +
   `zKValidF_iSpliceEnd` (+ splice read-outs `chainAsucc`/`chainAnt`_{`seqCons_seqUpdate_{top,lt}`,
   `seqUpdate_{self,of_ne}`}). Reduce splice validity to local threading hyps. NB: these are over the
   END-APPEND model `seqCons (seqUpdate ds i a) b` — kept as the ordinal-side interface + `key` template;
   their `isChainInf` hyp is unsatisfiable for the genuine halves (see finding).
2. `0085ed4` — **FINDING doc** (`ANALYSIS-…-lap87-splice-order-sensitivity.md`): confirmed vs Buchholz
   Def 3.2 (paper md line 75–76) that 5.2.1 validity needs the ORDERED insert object, not the banked
   end-append model (which serves only the order-independent `#`-fold ordinal descent).
3. `6d9132d` — **THE hard 5.2.1 math** `isChainInf_seqInsert_spec` + `zKValidF_seqInsert_spec`: the
   ordered-insert splice `cs = d₀…d_{i−1} a b d_{i+1}…dₗ` is `isChainInf`/`zKValidF`-valid from the
   original chain's unpacked validity at `j₀` (`i ≤ j₀`) + the Thm-3.4(a) genuine half end-sequents.
   Full order-sensitive threading worked out (new distinguished `j₀+1`; region-by-region). Abstract over
   the insert read-outs.
4. `8f8f308` — PENDING update.
5. `63817c2` — **concrete `seqInsert` op** (`seqInsertAux`/`seqInsert`, `PR.Construction`,
   `𝚺₁-Function₅`, mirror `seqUpdateAux`) + ite-free read-outs `seqInsert_lh`,
   `znth_seqInsert_{pre,at,at1,suf}`, AND the corollaries **`isChainInf_seqInsert`** /
   **`zKValidF_seqInsert`** (specs instantiated on the concrete object, read-out hyps discharged, given
   `i < lh ds`). The genuine 5.2.1 reduct's validity is now a usable object-level fact.
6. `7a54718` — PENDING update.

## 🎯 OBJECTIVE (operator, binding)
Discharge `peano_not_proves_goodstein` (`Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`). Headline re-verified clean this lap: `[propext, sorryAx,
choice, Quot.sound]` (0 math axioms). SOLE blocker = **crux-2** = `redSound` = internalized
cut-elimination (Buchholz §3/§6). The lap-86 finding fixed the target: `red`'s tag-4 must DISPATCH
5.1 / 5.2.1 / 5.2.2; the genuinely-new content is each branch's `zKValidF` validity.

## State of the three dispatch branches (validity at the lemma level)
- **5.1 critical** — `iCritReductG` / `ZDerivation_iCritReductG_of` (laps 84–85). Built.
- **5.2.2 replace** — `ZDerivation_iCritAux_of`/`_of_zK` + `zKValidF_seqUpdate` (lap 86). Built.
- **5.2.1 splice** — VALIDITY object COMPLETE this lap (`isChainInf_seqInsert` / `zKValidF_seqInsert`
  on the concrete `seqInsert`). ⚠ Not yet connected to descent or to the genuine halves (see NEXT).

## ▶ NEXT (remaining 5.2.1, then the dispatch — `PENDING_WORK.md` lap-87 box)
1. **Descent transfer for `seqInsert`.** The banked descent (`iord_descent_iSpliceEnd`,
   `iotil_iSpliceEnd_lt`, `idg_iSpliceEnd_le`) is on the END-APPEND model. Prove
   `õ(seqInsert ds i a b) = õ(seqCons (seqUpdate ds i a) b)` (same `#`-multiset; `iseqNaddIdg` is
   permutation-invariant — likely needs an `iseqNaddIdg`-reindex lemma) ⟹ `iord_descent` on `seqInsert`.
   Alternatively prove the descent directly on `seqInsert` mirroring `iotil_iSpliceEnd_lt` (the `#`-fold
   over `seqInsert` puts the two new ω-powers at indices `i`,`i+1` instead of `i`,end — same value).
2. **Wire the genuine halves.** Supply `a = dᵢ{0}`, `b = dᵢ{1}` from `inference_critical_pair` (L3.1,
   the redex finder) applied to the critical premise `dᵢ`, + the Thm-3.4(a) end-sequent facts to
   discharge `ha_ant`/`ha_rank`/`hb_succ`/`hb_ant` of `isChainInf_seqInsert`. (`a`/`b` are themselves
   `iCritReductG`-style recombinations of `dᵢ`'s own halves — `tp = isymRep`, so own-perm + tag
   formula-hood collapse, cf. `ZDerivation_iCritAux_of_zK`.)
3. **The `red` tag-4 DISPATCH def** (`iRNextG` `~InternalZ.lean:4900`): branch on `zKCritical`
   (Δ₁, lap 86) → 5.1 `iRcritG`; else bounded first-hit minimal `i` with `tp(dᵢ) ◁ Π`, then
   sub-dispatch on `dᵢ` critical → 5.2.1 `seqInsert`-reduct / 5.2.2 `iCritAux`-reduct. Re-prove
   `red_zK`, `red`'s definability (`iRNextGDef`), and re-check the lap-86 `not_zKCritical_*` lemmas
   (now apply only to the 5.1 sub-case).
4. **`redSound`** = `zDerivation_induction`, tag-4 split 5.1/5.2.1/5.2.2 → each `zKValidF`; then
   `iord_descent_red` UNCONDITIONAL (drops `hcrit`). Wire `Crux2Blueprint.redSound`/`iord_descent_red`
   → `false_of_ZDerivesEmpty` → headline (the blueprint's M2/M3 leaves remain too:
   `foundation_bot_to_Z_empty`, the C0.5 Foundation→Z bridge).

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. src/ sorries unchanged from lap 86: `Statement.lean:22`,
`Reduction.lean:68`, `DescentSemantic.lean:582` + the 4 `Crux2Blueprint` leaves (`redSound`,
`iord_descent_red`, `foundation_bot_to_Z_empty`, `false_of_ZDerivesEmpty`). No uncommitted edits.
Aristotle: idle (Foundation not in its v4.28 env).
