# HANDOFF — 2026-06-23 (lap 25, **E-core §3 driven deep: Thm 3.5 tail + arithmetization scaffold**)

> **Branch** `plan` · HEAD = lap-25 tip · build **green** (`lake build GoodsteinPA`, **1274 jobs**) ·
> headline `GoodsteinPA.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). Tree clean.

A hardest-first lap on the **E-core** wall (Rathjen §3, the Goodstein⟹PRWO descent inside PA). Durable
overview = **`STATUS.md`**. Attack paths = **`PENDING_WORK.md`** (lap-25 top, read first). E map =
**`DESCENT-PLAN.md`** (now has §3b — arithmetization tooling VERIFIED). Two walls remain: **E-core +
F-φ** (F-φ on Aristotle, `aris_onotecmp`, still RUNNING — leave it).

## ✅ Lap-25 deliverables (8 green commits, all `[propext,choice,Quot.sound]`-clean)

All in `src/GoodsteinPA/DescentCore.lean` (semantic) + the NEW `DescentArith.lean` (arithmetization),
both now wired into `src/GoodsteinPA.lean` (DescentLift/Core/Arith were orphaned from the lib build;
fixed, 1271→1274 jobs).

1. **Rathjen ωₙ tower** — `omegaStack` (`ω₀=1`, `ωₙ₊₁=ω^{ωₙ}`), `omegaStack_NF`, `C_omegaStack : C(ωₙ)=1`,
   `repr_omegaStack_{succ,strictMono}`. The Thm 3.5 head-term scaffold.
2. **`C(ω·α) ≤ C(α)+1`** (`C_omega_mul_le`) — "multiplying by ω bumps coefficients by ≤1" — via
   `one_add_oadd`, `C_one_add_le` (`C(1+e)≤C(e)+1`), induction on the `ONote.mul` recursion.
3. **`NoFin` infra** — `NoFin` (no finite CNF part) + `noFin_omega_mul` (`ω·α` is `NoFin`) + `C_ofNat`,
   `one_add_ne_zero`. The "no coefficient merge" lever.
4. **Thm 3.5 tail-term `C`-bound** — `C_add_ofNat_le` (`C(a+finite) ≤ max(C a, finite)` for `NoFin` NF
   `a`; mirrors mathlib `add_nfBelow` with cmp-gt under NF), `NF_omegaO`, and `C_betaTail_le`
   (`C(ω·αₙ+(K-i)) ≤ K(n+1)+i+1` = `C(βᵣ)≤r+1` for the tail block).
5. **Thm 3.5 tail-term DESCENT** — `repr_omegaO` (`repr ω=ω`), `repr_betaTail_within` (larger finite tail
   → larger value), `repr_betaTail_boundary` (`ω·αₙ₊₁+K < ω·αₙ` from `αₙ₊₁≺αₙ`; ω absorbs finite K).
   **⟹ both halves of Thm 3.5's TAIL block (C-bound + descent) are machine-checked** — the non-vacuous
   asymptotic content of the slow-down.
6. **Arithmetization map VERIFIED** (`DESCENT-PLAN.md §3b`): confirmed exact signatures of
   `sigma_one_completeness` (Σ₁ free) and `sigma1_pos_succ_induction` (internal `𝗜𝚺₁` induction);
   `P(k):=mₖ≥T̂^{k+2}(βₖ)` is Δ₀ hence `𝚺₁` ⟹ the induction applies directly.
7. **Arithmetization SCAFFOLD machine-checked** (`DescentArith.lean`): **`ineq6_internal`** — inside
   `[V ⊧ₘ* 𝗜𝚺₁]`, given `𝚺₁`-functions `m,b`, base `b 0 ≤ m 0`, and the internal step, the
   `definability` tactic + `sigma1_pos_succ_induction` yield `∀k, b k ≤ m k`. `nonterminating_internal`
   = the PA-internal Lemma 3.6 (`0<b k ⟹ 0<m k`). **The inequality-(6) induction now assembles in Lean.**
8. Build wiring + docs.

## 🎯 Open obligations (E-core, hardest-first)

1. **E-core(b) — THE WALL: internalized `𝚺₁`-definability** (the one gate, now precisely scoped).
   `ineq6_internal` reduces E-core's kernel to: supply the concrete `𝚺₁`-function `m` = internalized
   `goodsteinSeq`/`bump` and `b` = `T̂^{k+2}∘β`, and prove the internal `ineq6_step`. **Scoped this lap:**
   Foundation's `𝗜𝚺₁` has base-**2** `log`/`exp`/`bexp`/`Pow2`/`‖·‖`
   (`Arithmetic/Exponential/{Exp,Log,Bit}.lean`) but **NO general variable-base power** and no
   auto "computable→V-internal `𝚺₁`-function" bridge (the `codeOfREPred` route gives ℕ-level Σ₁
   provability only — fine for per-instance facts via `sigma_one_completeness`, NOT for the V-internal
   induction). So `bump` must be **hand-built**, base-b, the way `log`/`exp` are: define `b^x` as a
   `𝚺₁`-function via its graph (graph-definability technique, `…_defined : 𝚺₀/𝚺₁-Function via …Def`),
   then base-b digits, then `bump` (= base-b hereditary base-change `S^b_{b+1}`), then the internal
   `ineq6_step` (Δ₀ numeral form of `DescentCore.ineq6_step`). **Multi-lap.** First brick = internalized
   `b^x` (`Pow`) as a `𝚺₁`-function in `V`.
2. **E-lift X-induction instance** (`PRWO ⟹ TI prec`, the X-essential glue) — Foundation-side; lap-24
   correction: for Route B this is the *integrated* paLX construction with the free-X descent, not "one
   instance". Parallel deep thread.
3. **F-φ** `rePred_ltPull_natCode` — ON ARISTOTLE (`aris_onotecmp`, `16c9fc79-…`, RUNNING). On return:
   verify in-kernel + `#print axioms`, port → makes Thm 5.6 fully axiom-clean.

## ⚠️ Notes
- **At next lap start:** poll `aristotle list` for F-φ; check for `ON-LINE-FINDINGS-*.md`. `GREP
  DescentCore.lean` before any semantic ONote/Goodstein lemma (it is now large — tower, C-arithmetic,
  βᵣ tail facts, ineq6_step, lemma36_*).
- **LOCKED untouched:** `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `sorry`.
- **src/ sorries (2):** `Statement.lean:22` (headline, locked), `Reduction.lean:50` (Route-A, off-path).
- Discharge the headline `sorry` ONLY when E is real AND `#print axioms peano_not_proves_goodstein` clean.
- **The semantic Thm 3.5 HEAD block** (`βⱼ=Σω_{s-i}`, `j<K`) is unbuilt — a finite, vacuous-on-its-own
  boundary detail (low value vs the arithmetization wall). Skipped deliberately.
