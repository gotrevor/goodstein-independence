# Handoff: lap 151 — §14.254 splice = FLATTEN; false `descent_step_K_splice` DROPPED via `GenReductCert`

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `2ca0d7d` · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms, no drift).
Operator SOLE-OBJECTIVE = **M1b-term** (crux-2 termination). Working tree clean.

## ✅ DONE THIS LAP (2 commits) — the DROP, faithful
1. **`e204766` — refuted the lap-150b `descent_step_K_splice` in-kernel + PROVED the FLATTEN engine.**
   The single-premise `seqUpdate`+combined-`iord` splice is **FALSE**: when the reduced premise degree-trades
   (`idg v < idg dⱼ`, `õ v` RISES, bounded only by `ω^{õ dⱼ}` — the `iRKcCrit` structure), the outer chain's
   `õ`-fold (`iotil_zK` = `õ`-only) RISES while its degree (`idg_zK = max r (maxdeg−1)`, pinned ≥ r) stays
   flat ⟹ outer `iord` does NOT descend; `iord_descent_cut`'s `idg+1≤idg` and `õ<ω^{õ}` premises both fail.
   A fresh host judge note (`E-2026-06-26-JUDGE-splice-flatten-not-seqUpdate.md`) reached the IDENTICAL
   conclusion independently (~85%). **Fix = Buchholz §14.254 case-(ii) FLATTEN**: remove `dⱼ`, splice its two
   principal-cut halves `dⱼ{0}⊢Γ→B`, `dⱼ{1}⊢B,Γ→A` (both `õ ≺ õ dⱼ`) into the outer chain (`seqInsert`), so
   the outer `õ`-fold strictly drops. Banked **`descent_step_K_spliceHalves`** (axiom-clean) assembling the
   already-proven `seqInsert` bricks (`isChainInf_seqInsert`/`ZDerivation_seqInsert_of`/`Z*_zK_of_seqInsert`/
   `iord_descent_seqInsert'`).
2. **`2ca0d7d` — the DROP.** Enriched `genReduct_botSucc`'s conclusion to **`GenReductCert d`** =
   `certReplace d ∨ certFlatten d` (the structured certificate the judge's option-(a) recommended):
   `Or.inl` = single `õ`-dropping REPLACE reduct (`iRedDescent`); `Or.inr` = the two FLATTEN halves
   (cut-pair end-sequents + `õ`-drop + `irk B + 1 ≤ idg d`). `descent_step_K_noncrit_repMajor` now `rcases`
   the cert → REPLACE routes to `descent_step_K_replace` (proven), FLATTEN to `descent_step_K_spliceHalves`
   (proven) — **sorry-FREE**. **`descent_step_K_splice` (the false sorry) DELETED** → net −1 live src sorry,
   path now faithful. `GenReductCert_definable` PROVEN axiom-clean.

## 🔑 DEFINABILITY GOTCHAS (banked — `GenReductCert_definable` cost a lot to land)
- `definability` (aesop) **exhausts** on the 24-conjunct flat `replace ∨ flatten` disjunction AND on the
  general `Γ-[m + 1]` form. **Fix:** split into `abbrev certReplace`/`certFlatten`, prove each
  **`𝚺₁-Predicate` SEPARATELY** (the exact level `zDerivation_sigma_induction` needs — `hP : 𝚺-[1]-Predicate`),
  then `GenReductCert_definable := certReplace_definable.or certFlatten_definable`.
- A bare `iRedDescent v d` (a STRUCTURE) is **not auto-decomposed** by aesop — `simp only [iRedDescent_iff]`
  BEFORE `definability` to expose its 3 `𝚫₁` fields.
- `inAnt` has only a `𝚺₀-Relation` instance (no `Γ-[m+1]`) — **inline it** as the bounded `∃ i < lh Y, …`.
- `set_option maxHeartbeats 1600000 in` on each cert instance.

## 🎯 NEXT (per `PENDING_WORK.md` lap-151b) — three live leaves, all in the FAITHFUL cert form
1. **`genReduct_chain_hasRedex`** (`Crux2Blueprint:2988`, the chain's own §14.253 cut → FLATTEN cert `Or.inr`):
   the halves are `iRKcCrit`'s `iCritReductSeq` components `d0,d1`; their `õ`-drop is **FREE**
   (`iord_descent_iCritReduct`'s `h0o/h1o` inputs). The cut-pair end-sequents (`seqAnt(fstIdx d0)=Γ`,
   `seqSucc(fstIdx d1)=⊥`, the `hb_thr` threading) come from the `iCritReduct` structure; `irk B + 1 ≤ idg d`
   from T3.4. **Blocked on the `Seq (seqAnt s)` / zSeqAnt tag-4 FOLD** (the halves' soundness `ZDerivation d0/d1`
   needs `Seq (seqAnt s)`; do the fold per handoff lap-150 — analog of the lap-149 freshFlag fold). ← do this FIRST.
2. **`genReduct_chain_noRedex`** (`:3012`, §14.254 recurse): `majorPrem_tag_mem` ⟹ major tag ∈{3,4,5,6}; reduce
   major (3/4) or Rep cut-partner (5/6) by the IH (which now hands back a `GenReductCert`), then re-package as
   the PARENT's `GenReductCert` re-based to the parent's end-sequent (REPLACE↦REPLACE, FLATTEN↦FLATTEN).
3. **`descent_step_K_noncrit_axMajor`** (`:3226`, tags 5/6) + **gDef** `exists_sigma1_descending_step` (`:3349`).

## ⚠️ Forbidden / do-NOT-undo
- Do NOT re-introduce the single-premise `seqUpdate`+combined-`iord` splice (`descent_step_K_splice`,
  refuted in-kernel this lap + judge-convergent). The faithful object is the FLATTEN (`seqInsert` halves).
- Do NOT witness descent with `red`; do NOT `iord`-RECURSION for the construction (CODE induction via
  `zDerivation_sigma_induction` only); do NOT `redLeast`/μ-min for gDef.
- Do NOT collapse the repMajor/axMajor §14.254a/b split; do NOT touch the off-path dead `red`-soundness
  sorries {:82,:1257,:1367,:1563,:1653,:1765,:1868} AS STATED.
- State the cert definability instances at `𝚺₁` ONLY (general `Γ-[m+1]` exhausts aesop).

## 📁 Key anchors
- Cert: `GenReductCert`/`certReplace`/`certFlatten` (`Crux2Blueprint`, before `genReduct_chain_hasRedex`),
  `GenReductCert_definable`/`certReplace_definable`/`certFlatten_definable`.
- FLATTEN engine: `descent_step_K_spliceHalves` (`Crux2Blueprint`, after `genReduct_botSucc`), PROVEN.
- Consumer: `descent_step_K_noncrit_repMajor` (sorry-FREE, `rcases` the cert).
- Judge notes (committed, root): `E-2026-06-26-JUDGE-splice-flatten-not-seqUpdate.md`,
  `E-2026-06-26-JUDGE-code-recursion-crux.md`, `JUDGE-HANDOFF-2026-06-26-lap150.md`.
- Splice test cases (prior refutations): `ANALYSIS-…-lap87-splice-order-sensitivity.md`,
  `…-lap94-splice-dispatch-unfaithful.md`.

---
**→ Next session: do the `Seq (seqAnt s)` / zSeqAnt tag-4 FOLD to unblock `genReduct_chain_hasRedex`'s
FLATTEN soundness (its descent is already free) — that drops the chain's own §14.253 leaf. Keep build green
+ headline 0 math axioms. NEVER the `seqUpdate` splice (refuted); NEVER `red`/`iord`-recursion.**
