# Handoff: lap 210 — SERIES-4 Lane S COMPLETE (S-1…S-5 all GREEN); rung-W composition assembled at the axiom's verbatim type

**Branch**: `plan` · **HEAD**: `649b1e4` · **Date**: 2026-07-03 · Tree CLEAN.
**Build**: 🟢 all three wip modules re-verified via `lake env lean` this lap (src untouched).
Headline unchanged/undrifted.

**Directive (OUTRANKS this)**: `DIRECTION.md` SERIES-3 judge block — MANDATE was the SERIES-4
FINAL-DISCHARGE order, Lane S. **Lane S is now COMPLETE**; remaining per that order = Lane B
(B-1 native_decide burndown · B-2 the 15 src sorries re-point/retirement PROPOSALS) + stamping
the ONE judge package. Do NOT self-ratify into src (axiom swap is judge-gated).

## ✅ This lap (12 green commits) — the whole SERIES-4 Lane S

All `[propext, Classical.choice, Quot.sound]`, verified by real `#print axioms` output:

- **S-1** `ewIterTower_dom_pad` (`564b51f`, `wip/HardyMajorization.lean`): the d-fold tower is
  padded-Hardy-dominated at ONE existential level (per-pass double-Hardy collapse + `ω^{A+1}` raise;
  gate paid FROM the growing pad).
- **S-2** (`15f6617`, `b22dbc3`): `gvb_le_iter` (RVG — `gvb` of a fixed formula ≤ `G^[k]`, engine
  abstract), `hardy_Wpow_iter_dom_pad` (HM — iterates of `H_{ω^{E₀}}` padded-dominated, mirror of
  the tower proof), `dom_pad_max` (joint-level `E₁+E₂+1` combiner), `Sstar_dom_pad`.
- **S-3** (`b22d33f`, `d5f044a`, `4511346`, `5370cf0`, `64902f1`):
  - `ewIter_dom_pad_levelcap` (HM): the α'-UNIFORM level cap at `ω^{e₀+2+1+γ+1}` — the outer
    norm gate `norm α' < 2^{Nlog α'+1}` is paid by the inner Hardy value (`two_pow_le_hardy_Wpow2`,
    needs `e₀ ≠ 0`). THE structural risk of the assembly, now closed.
  - `readoff_value_pipeline'`/`embedding_Zef2TC_V3_linearK`/`readoff_value_goodstein'` (E1):
    the Nlog-primed, LINEAR-stage (`max K₀ m`) read-off (Zef2TCProv's discarded certificate
    re-threaded; the embedding's stage is literally `max (envSup 0 N) m`).
  - `dom_pad_comp` (HM): padded-dominated functions compose (level `E₁+E₂+2`).
  - `gated_certificate_uniform` (RVG): ONE `k` certifies the whole numeral family
    (`gvb_substs_q_le` + `gvb_le_iter`); P monotone + `Gated` + `G^[k]`-bounded at `max(V,m)` shift.
  - `master_conversion` (HM, S-3 capstone): slot-abstract — ANY padded-dominated inflationary `S`,
    per-m `α' ≤ γ` with `Nlog` certificate, `n ≤ ewIter S α' (S(max K₀ m))` ⟹ `n ≤ f_{osucc E₅}(m)`
    eventually. Chain: levelcap → certificate absorbs inner arg into `Gexp(S(·))`
    (`two_mul_add_le_hardy_omega_sq`, `m ≥ q`) → 3× `dom_pad_comp` → `hardy_pad_lt_fastGrowing_osucc`.
  - Also `Scirc_dom_pad` (HM): concrete rel1-free `S°` domination, E1-legible (no `Wpow`).
- **S-4** (`19065e7`): `hardy_pad_lt_fastGrowing_osucc` (`H_{ω^L}(m+C) < f_{osucc L}(m)`, m ≥ C+3)
  + `dom_pad_eventuallyLE`.
- **S-5** (`649b1e4`): **`wainer_bound_witness`** (E1) — `(𝗣𝗔 ⊢ goodstein) → ∃ o, o.NF ∧
  EventuallyLE goodsteinLength (fastGrowing o)` — the VERBATIM type of the sole route axiom
  (`WainerRoute.lean:119-121`). Three hypotheses, each the VERBATIM statement of a kernel-clean
  theorem in a sibling wip module (they cannot import each other):
  `Hcert` = `ReadoffValueGate.gated_certificate_uniform` · `HSdom` = `HardyMajorization.Scirc_dom_pad`
  · `Hconv` = `HardyMajorization.master_conversion`. Read-off + m-uniformization
  (`ewIterTower_rel1_le`/`ewIter_mono_slot`/`ewIter_rel1_le`, V := 0) + semantic link inline.

## 🚦 NEXT

1. **The ONE judge package** (per DIRECTION §4): stamp a SERIES-4 evidence doc — the S-1…S-5
   chain, the three-module hypothesis pairing (statements match verbatim modulo `Gated` dup and
   `Wpow x = oadd x 1 0` spelling), `#print axioms` transcript for: `wainer_bound_witness`,
   `gated_certificate_uniform`, `Scirc_dom_pad`, `master_conversion`, `readoff_value_goodstein'`.
   The judge executes the splice (concatenate/import in src + apply the three theorems to the
   witness) and the axiom→theorem swap. Grind laps MUST NOT touch `WainerRoute.lean`/`Statement.lean`.
2. **Lane B** (interleave): B-1 W7 native_decide burndown (statements FROZEN) · B-2 the 15 src
   sorries (re-point to landed theorems or retirement PROPOSALS).

## Gotchas fresh this lap
- `ring` is useless on ordinal `repr` goals (non-comm) — explicit-arg `ONote.repr_add` rewrites,
  `push_cast`, `rfl`. Bare `le_add_right` resolves to the wrong order lemma — use
  `Ordinal.le_add_right`/`Ordinal.le_add_left`; left-mono add is `(add_le_add_iff_left _).mpr`.
- Inequalities feeding `hardy_monotone`/`hardy_le_of_lt` must be standalone `have`s — inline
  `by omega` sees metavariables (also pass `dom_pad_comp`'s pads `c₁ c₂` EXPLICITLY, else
  "stuck Preorder ?m").
- `hΣ` is not a valid identifier (Σ token); `show T from e` chokes next to `∃⁰ χ` — prefer
  direct `Semiformula.exs.inj hχeq` (the unifier whnf's `goodsteinBodyE/[nm m]` fine).
- `omega` DOES handle `max` on ℕ; it does NOT multiply atoms — provide product expansions
  (`mul_add` by `ring`) + `Nat.le_mul_of_pos_left` facts, then omega.
- `Zef2TCProv` (E1:3424) carries `Nlog α' ≤ f 0` — don't re-derive; re-thread.
