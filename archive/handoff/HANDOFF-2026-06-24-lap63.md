# HANDOFF — 2026-06-24 (lap 64) — irk real; LH4 FULL iord descent; õ-NF on real derivations

> **Branch** `plan` · **HEAD** `956fc1d` · `lake build GoodsteinPA` **green (1320 jobs)** · headline
> `peano_not_proves_goodstein` untouched (honest `sorry`, 0 math axioms, anti-fraud intact).
> All lap-64 work is in `wip/InternalZ.lean` (compiles standalone; `src/` unchanged except the new
> `import Foundation.FirstOrder.Incompleteness.Second` is a built package — no `src/` files touched).

## What landed (3 commits, all axiom-clean `[propext, choice, Quot.sound]`)
1. **`irk` — formula rank, STUB→THEOREM** (`d0fc982`). The lap-63 stub `irk d := 0` is now the genuine
   Buchholz logical-complexity rank, built on Foundation's `UformulaRec1.Construction` (the recursion
   engine behind `bv`): `rk(atom)=rk(⊤)=rk(⊥)=0`, `rk(A∧B)=rk(A∨B)=max+1`, `rk(∀F)=rk(∃F)=rk F+1`.
   Compute lemmas `irk_{rel,nrel,verum,falsum,and,or,all,ex}`. Discharges lap-63 NEXT #1 (the `irk`
   prereq for the LH4 degree side AND T3.4/T4). Required `import …Incompleteness.Second` for the formula
   coding (`UformulaRec1`, `^rel`/`^⋏`/`^∀`, `IsUFormula`). Downstream graph `def`s that reference the
   now-noncomputable `irkDef` were marked `noncomputable` (idgNextDef/idgTable.blueprint/idgTableDef/
   idgDef/iseqMaxIdgAux.blueprint/iordDef) — harmless (graphs are never executed; Foundation does the same).
2. **LH4 — FULL Ind-rule `iord` descent on the genuine reduct object** (`2beac51`), now with real `irk`:
   - idg-fold machinery mirroring the õ-fold: `iseqMaxIdgAux_congr`, `iseqMaxIdg_seqCons`,
     `iseqMaxIdgAux_const`, `iseqMaxIdg_iRepeatSeq`, `iseqMaxIdg_iIndReductSeq`.
   - **`idg_zK_iIndReduct`** — the reduct `K^{rk p}(d0, d1×k)` has the SAME degree as `Ind^{a,t}_F d0 d1`
     (`max{rk p, (max dg)∸1} = max{(dg∸1)s, rk p}`; ∸-over-max distrib `max_sub_one_distrib` + `ac_rfl`).
   - **`iord_descent_iIndReduct`** — degree preserved (above) + õ strictly dropping
     (`icmp_iotil_iIndReduct`, lap 63) ⟹ `iord_descent_samedeg` gives `o(d[0]) ≺ o(Ind…)`. **LH4 is now a
     complete `iord` descent on a real reduct object, both sides, real `irk`.** (lap-63 NEXT #0(a) DONE.)
3. **`isNF_iotil_of_zDerivation`** (`956fc1d`) — `õ(d)` is a valid CNF code for EVERY `ZDerivation d`,
   by structural induction over the C0 Fixpoint (each rule via its per-constructor NF lemma
   `isNF_iotil_z{Atom,Iall,Ineg,Ind}` + `isNF_iseqNaddIdg`/`iotil_zK` for K^r). **Populates the
   `isNF (iotil ·)` side-condition of `iord_descent_dgdrop`/`iord_descent_cut` on GENUINE objects** —
   retires the "discharged later via ZDerivation" caveat the descent templates carried.

## NEXT (hardest-first) — the residual is OBJECT CONSTRUCTION + the bridge
0. **Wire LH4 into `iR`/`iord_iR_descent` (broaden to `zTag = 3`).** Blocker = the Ind reduct's two
   faithful ingredients, both genuine (don't smuggle): **(a) numeral-value `k` extraction** from `t`
   (`at' = ⟪a,t⟫`) — needs a term-`value` function; Buchholz: `k=0 ⟹ reduct=d0` (use `iord_descent_cut`,
   degree may drop), `k>0 ⟹` the K^r chain (`iord_descent_iIndReduct`, DONE). **(b) eigenvariable
   substitution `sub : V→V→V`** on derivation codes with `õ(sub d1 j)=õ d1`, `dg(sub d1 j)=dg d1`
   (Buchholz Remark p.10) — a recursion over the derivation structure; for *validity*/`derivesEmpty`-
   preservation, swap `sub d1 j` for the bare `d1` copies in `iIndReductSeq` (the õ/idg folds already
   support the swap via `iseqNaddIdgAux_congr`/`iseqMaxIdgAux_congr`). The ORDINAL descent is already
   k-independent (any k>0) and substitution-invariant, so LH4's `iord` content is banked; what (a)/(b)
   add is faithful WELL-FORMEDNESS of the reduct as a Z-derivation.
1. **L3.1 redex finder** (judge §8.1) — Σ₁ search over the chain's premise inference-symbols for the least
   critical `(i,j,k)`; finite combinatorics, NO ordinals. Needs inference-symbol structure (now that `irk`
   is real, T3.4's `rk(A(d))<r` is statable).
2. **T3.4 rank bound + iR critical branch** (the nut's object side) — `d{0}=K^r(i/dᵢ[k])`,
   `d{1}=K^r(j/d_j[0])`, `d[0]=K^{r−1}d{0}d{1}`; then N3a/N3b feed `iord_descent_cut` (DONE).
3. **C0.5 Foundation→Z bridge** (parallelizable, worktree) — `(𝗣𝗔).DerivationOf d ⊥ → ∃ z, ZDerivesEmpty z`
   (B1/B2/B3 per `E-CRUX2-DECOMPOSITION §5`; Z's native `Ind` skips Bryce–Goré's biggest sub-tower).
4. **PARALLEL:** `PA_delta1Definable` (mandatory per hardened endpoint; still upstream `axiom`).

## Ordinal+structural inventory — DONE this lap (don't rebuild)
`irk` (real formula rank), `iseqMaxIdg_{seqCons,iRepeatSeq,iIndReductSeq}` + `iseqMaxIdgAux_{congr,const}`,
`max_sub_one_distrib`, `idg_zK_iIndReduct` (degree preserved), `iord_descent_iIndReduct` (LH4 full),
`isNF_iotil_of_zDerivation` (NF on real derivations). Plus all of lap-63's ordinal-side inventory.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
Aristotle OFFLINE. Crux 1 DONE (axiom-clean lap 57). All crux-2 work stays in `wip/` until the cruxes are
real (anti-fraud). No `src/` files were modified this lap.
