# HANDOFF — 2026-06-23 (lap 35, **wall-C `hDdef` DISCHARGED; descent-existence complete in `src/`**)

> **Branch** `plan` · HEAD = `66574bf` · build **green** (`lake build GoodsteinPA`, **1306 jobs**) ·
> headline `Statement.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). **Tree clean.**

Durable overview = **`STATUS.md`**. The lone deep wall is unchanged:
**`DescentSemantic.no_min_descent_absurd_of_goodstein`** (Rathjen §3 inside a model `M ⊧ paLX`), split
into `hCD` (wall C+D) and `hB` (wall B, opaque `codeOfREPred` bridge — literature-gated,
`ON-LINE-REQUEST.md`). This lap **completed wall C's descent-existence brick** (the lap-34 scaffold's
lone `sorry`).

## Lap-35 deliverable — `hDdef` DISCHARGED (5 commits; new file promoted to `src/`)

`wip/DescentConstruction.lean` (lap-34 scaffold, one disclosed `sorry` = `hDdef`) is now **sorry-free,
axiom-clean, and promoted to `src/GoodsteinPA/DescentConstruction.lean`** (imported in `src/GoodsteinPA.lean`).
`#print axioms descent_seq_exists = [propext, choice, Quot.sound, ONoteComp…native_decide.ax_1_5]` (the
accepted F-φ finite-witness artifact, inherited via `prec`/`Thm56`) — **no `sorryAx`, no new axiom.**

The discharge of `hDdef` (`D(k) := ∃ W, IsDescent f a₀ W ∧ lh W = k+1` is `LX`-definable):
1. **`isDescent_iff_mem`** — membership/`Seq`-graph form of `IsDescent` (over the reduct, `0<lh W`):
   `Seq W ∧ ⟪0,a₀⟫∈W ∧ (∀ i x x', ⟪i,x⟫∈W → ⟪i+1,x'⟫∈W → Mlt f x' x) ∧ (∀ i x, ⟪i,x⟫∈W → ¬MX x)`. **Key
   move:** the `X`-atom now sits on a *bound variable* `x`, not a `znth`-function-term — so the
   definability is a bounded `∀` over a reduct-definable `∈`-guard + a primitive `Xsym`-atom.
2. **`lxDef_exists` / `lxDef2_and`** — binary `LX`-definability combinators (`∃`-close the first var via
   Foundation `eval_ex`; `∧`-close via even/odd free-assignment merge). Match the `∃W` shape.
3. **`skel_lxDef` (A) / `descentMlt_lxDef` (B) / `xclause_lxDef` (C)** — the three definability pieces:
   - **A** `Seq W ∧ ⟪0,a₀⟫∈W ∧ lh W=k+1`: pure `ℒₒᵣ`-on-reduct via Foundation `seqDef`/`memRelOpr`/`lhDef`
     (`Defined.iff` reads them off `inst.lMap Φ = standardModel reductORing`; `a₀` carried by `e 0=a₀`).
   - **B** the `Mlt`-descent clause: `∀⁰∀⁰∀⁰ (g1 🡒 g2 🡒 prec⇜![#0,#1])`; two `memRelOpr` guards
     (g2 on successor term `‘#2+1’`) + the X-free fvar-free `prec` atom (`eval_substs`+`eval_iff_of_funEqOn`).
   - **C** the `¬MX` clause: `∀⁰∀⁰ (memRelOpr-guard 🡒 ∼Xsym ![#0])` — the genuinely-`X` piece.
4. **`descent_seq_exists`** (`∀ k, ∃ W, IsDescent f a₀ W ∧ lh W=k+1`, by `lx_succ_induction`) — now
   sorry-free; `hDdef` = `lxDef_exists (lxDef2_and A (lxDef2_and B C))`, predicate-converted via
   `isDescent_iff_mem`.

GOTCHA banked (lap 35): to build a *mixed* `ℒₒᵣ`-guard + `X`-atom `LX` formula, write the guard in `ℒₒᵣ`
(Foundation `memRelOpr`/`seqDef`/`lhDef` operators/Semisentences), `lMap Φ` it, conjoin the `Xsym`/`prec`
atom in `LX`; eval via `eval_lMap` + `rw [reduct_eq_standardModel]` then `Defined.iff`/`eval_memRel`
(`@[simp]`). Vector args: `funext j; fin_cases j <;> simp [Semiterm.val_bvar]` (needs
`import Mathlib.Tactic.FinCases`). `fin_cases` is NOT auto-imported in a fresh file.

## NEXT — wall C, step 2 (the βₖ slow-down), then step 3 (the run) → `hCD`

`hCD : ∃ m₀ : M, ∀ k : M, 0 < InternalPow.igoodstein m₀ k` (`DescentSemantic.lean:409`). With
`descent_seq_exists` in hand, the remaining wall-C content is **Rathjen §3 Thm 3.5 internalized in `M`**:
1. **Extract the descent function `a : M → M`** from `descent_seq_exists`: `a k := znth W_k k` where
   `W_k` is the (unique-up-to-prefix) length-`k+1` descent. Need coherence (`W_k` prefixes `W_{k+1}`) OR
   re-extract a single coded sequence; then `Mlt f (a (k+1)) (a k)` and `¬MX (a k)` hold.
2. **Slow-down `βₖ`** with `C(βₖ) ≤ k+1` (Rathjen Thm 3.5 — `papers/rathjen-2014…§3`, transcribed in
   `scratchpad/rathjen.txt` this lap; the `βₖ = ω·αₙ + (K−i)` block construction). Substrate is route-
   neutral in `DescentCore.lean` (`omegaStack`, `C_betaTail_le`, `repr_betaTail_*`, **`ineq6_step`**,
   `lemma36_nonterminating`); the missing piece is constructing `βₖ` from an arbitrary descent +
   internalizing the ONote/`C` facts into `M`'s reduct as an `LX`-definable function.
3. **Define `b k = T̂^{k+2}(βₖ)`, `m₀ = T̂²(β₀)`; prove `(hPdef, base, step, hpos)`** and feed
   **`DescentSemantic.lx_nonterminating`** (already built, `X`-essential run side) ⟹ `hCD`. `step` =
   internalized `DescentCore.ineq6_step` (Rathjen Lemma 3.6, inequality (6)).

Wall B (`hB`, `DescentSemantic.lean:418`) stays literature-gated + independent (`ON-LINE-REQUEST.md`,
the `codeOfREPred goodsteinTerminates ↔ ∃k igoodstein·k=0` IΣ₁-internal bridge). Good Aristotle candidate
once isolated.

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`.

## src/ code sorries (3, unchanged)
`Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A hook, off path),
`DescentSemantic.no_min_descent_absurd_of_goodstein` (**THE wall**, disclosed `hCD`+`hB` at 409/418).
`DescentConstruction.lean` is sorry-free.

## Aristotle
Idle. Wall B is the next self-contained candidate once the `codeOfREPred`↔`igoodstein` equivalence is
isolated as a bounded lemma.
