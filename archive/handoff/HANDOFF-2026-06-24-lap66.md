# HANDOFF — lap 66 · CRUX-2 ISLAND GREEN-GATED + iR2 NOW DESCENDS ON THE K-RULE

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md` (top section has the live
> next-step plan + the one open gap), then `STATUS.md`. Branch: `plan`. HEAD: `bcfcdbc`.
> Build: 🟢 green, `lake build GoodsteinPA` = 1321 jobs. Working tree: clean (all committed).

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`). Route: `PA⊢γ →(crux-1, DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)`
→ Gödel II. **crux-2 (`PRWO→Con`, Buchholz-Z ordinal analysis in PA) is the sole remaining math wall.**
The 3 `src/` sorries: `Statement.lean:22` (headline), `Reduction.lean:68` (`goodstein_implies_consistency`
= crux-2's girder), `DescentSemantic.lean:582` (the slowed code-descent β).

## ✅ WHAT LANDED THIS LAP (9 green commits, all axiom-clean)
1. **P0+P1a — crux-2 island promoted into the build.** Farmed goodstein-ab-xhigh's recursive-iR2 spine
   (the architectural keystone, 3937→now ~4090 lines) OUT of the un-built `wip/InternalZ.lean` island
   INTO `src/GoodsteinPA/InternalZ.lean`, imported by the `src/GoodsteinPA.lean` aggregator. `lake build`
   now type-checks it every lap AND the sorry-gate scans it (it is sorry-free). This was P0 in the lap-65
   baton — the prior A/B arms' "axiom-clean wip" was UNVERIFIED because nothing built it.
2. **P1b — med arm NOT merged (correctly).** goodstein-ab-med uses an incompatible symbol encoding
   (`iRsym=⟪0,C,0⟫+1` vs xhigh's `isymR=⟪0,A⟫`); its atomic chain bottoms out on med's `tp`/`ZPhi` over
   that encoding, so it does not compile against the xhigh spine — grafting = re-derivation, not a merge.
   xhigh already carries the §5 atomic layer (`zAxAll`/`zAxNeg`/`zAx1`/`oAtom1`). Do NOT spend laps
   porting med's encoding. (Verified by attempting the graft: 60-decl closure failed on `iRsym` et al.)
3. **THE K-case architectural fix (the heart of the lap).** The recursive `iR2` did NOT descend on
   tag-4 (the K/cut rule): `iR2` is the identity on atomic axioms, so the j-side redex premise had no
   õ-drop. Fixed by:
   - `zAxReduct : V → V` — the §5 atomic-reduct function (`zAxAll s p k ↦ zAx1 s p`, `zAxNeg s p ↦
     zAx1 s p`, id off tags 5/6) + `zAxReductDef` (Σ₁-definability) + rewrite/bundle lemmas.
   - **Rewired `iRNext` tag-4** so the critical reduct installs `zAxReduct` on both premise reducts:
     `iCritReduct d i j (zAxReduct (iR2 premᵢ)) (zAxReduct (iR2 premⱼ))`. Identity on the i-side
     (I-rule sub-derivation), §5 `Ax^1` reduct on the j-side L-axiom. `iR2_zK`/`iR2_zK_eq_iRcrit`
     updated to `ρ = zAxReduct ∘ iR2`. Verified path-2 (weaken j-side to `≤`) is IMPOSSIBLE: the nut's
     N3b (`icmp_omega_pow_nadd_lt`) needs BOTH premises' õ to strictly drop.
4. **Extended `ZDerivation`/`ZPhi` with the §5 L-axiom base cases (tags 5,6).** Before: ZPhi had only
   zAtom/zIall/zIneg/zInd/zK, so chain premises were tags 0-4, `tp=isymLk` never occurred, and **the
   redex finder could never fire on a genuine ZDerivation** (K-case unreachable). Now 7 disjuncts incl.
   `zAxAll`/`zAxNeg` (each carrying `IsUFormula p`). Full fixpoint cascade fixed: def, monotone,
   strong_finite, zphi_iff, zblueprint (Σ/Π embed `(isUFormula ℒₒᵣ).sigma`/`.pi`), zPhi_definable,
   isNF_iotil_of_ZDerivation (+ `isNF_iotil_zAxAll/_zAxNeg`), 6 rcases sites, `zAxReduct_of_tp_isymR`
   (replaces the now-false `zAxReduct_of_ZDerivation`), `k_lt_zAxAll`.

## ▶ NEXT STEP (THE prize — see PENDING_WORK.md top for full detail)
**Assemble `iord_descent_iR2_struct` for tag 4 (the K-case)** → then the unconditional
`ZDerivation d → icmp (iord (iR2 d)) (iord d) = 0` for ALL tags. All leaf bundles are banked &
axiom-clean (i-side `iRedDescent_iR2_of_tp_isymR` + `zAxReduct_of_tp_isymR`; j-side
`iRedDescent_zAxReduct_zAxAll/_zAxNeg`; the nut `iord_descent_iRcrit_of_chain'`; `iR2_zK_eq_iRcrit`).

⚠️ **ONE OPEN GAP to resolve first:** the nut `iord_descent_iRcrit_of_chain'` consumes chain-structure
predicates (`chainAsucc`/`chainAnt`/`hchain`/`hAj0`/`hrank`/`hperm`/`hnperm`). The ZPhi `zK` disjunct
only gives `Seq ds ∧ ∀ i<lh ds, znth ds i ∈ ZDerivation` — NOT the antecedent-threading those
predicates need. Decide: (a) strengthen the `zK` ZPhi disjunct to a genuine `isChainInf`-style chain
condition (another fixpoint-cascade edit — but now you've done one, the pattern is known), or (b) derive
`chainAnt`/`chainAsucc` from the premise sequents. Inspect `inference_critical_pair_of_chain`'s and
`iord_descent_iRcrit_of_chain'`'s exact hypotheses vs. what `zDerivation_zK_inv` provides.

Then: no-infinite-descent (ε₀ well-founded) → `ZDerivesEmpty d → False` → C0.5 Foundation→Z bridge
(`foundation_bot_to_Z_empty`, blueprint Bryce-Goré `Peano.v`, Z's native Ind maps PA-induction directly)
→ discharge `Reduction.lean:68` → headline falls out. ANTI-FRAUD: only discharge `Reduction.lean:68`
once `#print axioms peano_not_proves_goodstein` is clean.

## Pointers
- `E-CRUX2-DECOMPOSITION-2026-06-24.md` §8 = leaf grind-list. `papers/` Buchholz [6] (§3 reduction, §4
  assignment, §5 atomic), Rathjen 2014, Kirby-Paris.
- Reference corpus (read-only, NOT auto-loaded): `~/personal/claude/knowledge/core/projects/lean-journey/reference/`.
  Used this lap: `lean-build-skips-unimported-modules.md`, `lean-treadmill-sorry-gate-scope-is-srcdir.md`,
  `reference_lake_build_zero_jobs.md` (srcDir is package-level → promoted InternalZ into src/ rather than
  fighting a per-lib srcDir).
- A/B arms (READ-ONLY, harvested what's mergeable): `~/src/goodstein-ab-xhigh` (spine, MERGED),
  `~/src/goodstein-ab-med` (incompatible encoding, NOT merged).

## Lap discipline reminder
Every lap must compile crux-2 work under the green-gate (now it does — InternalZ is in `src/`) or shrink
distance to a `src/` hole. Commit each green build, end the turn after each so the governor can pace.
