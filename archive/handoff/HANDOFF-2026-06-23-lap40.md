# HANDOFF — 2026-06-23 (lap 40, **internal ordinal arithmetic for the Rathjen §3 slow-down**)

> **Branch** `plan` · HEAD `af13718` · build **green** (`lake build GoodsteinPA`, **1307 jobs**) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, **0 math axioms**).
> **Tree clean** (only `scratchpad/` untracked). All new lemmas `#print axioms`-clean `[propext, choice, Quot.sound]`.

## Context: the lone wall is `hbound` (`DescentSemantic.lean:416`)
The entire ordinal-analysis girder + Goodstein-faithfulness is done & axiom-clean. The one open
`sorry` is `hbound` inside `no_min_descent_absurd_of_goodstein`: an existential producing a
non-terminating internal special-Goodstein run from the M-internal ≺-descent (`no_min`). Its `step`
is the proven `InternalONote.ineq6_step_internal` (lap 39). The remaining content is the **Rathjen §3
slow-down** internalized on codes: build `βₖ = ω·αₖ + (K−i)` with `iCanon (k+1) βₖ`, icmp-descending,
isNF, 𝚺₁-definable in k, from the descent αₖ. Read Rathjen 2014 §3 (Thm 2.6 proof + Def 3.1) on disk —
the slow-down is irreducible and needs `ω·α` mult + CNF add on codes.

## ✅ Done this lap (7 feat commits) — all in `src/GoodsteinPA/InternalONote.lean`, axiom-clean
- `47c267b` **`iadd`** — internal CNF ordinal addition `a+b` on codes (CofV table indexed by 1st
  summand, param=b, 3-way `icmp` leading-exp branch). `iadd_zero_left`, `iadd_ocOadd`.
- `1af80bc` **`iomul`** — internal ω-multiplication `ω·c` (exponent bump `e↦1+e = iadd (ocOadd 0 1 0) e`,
  recurse tail). `iomul_zero`, `iomul_ocOadd`.
- `8a546d9` **`iC_one_add`** (`iC(1+e) ≤ iC e + 1`), **`iC_iomul`** (`iC(ω·c) ≤ iC c + 1`, Rathjen Thm 3.5).
- `e94a30a` **`iC_iadd_finite`** (`iC(ω·c + m) ≤ max(iC(ω·c)) m`, internal `C_add_ofNat_le`) + helper
  `iadd_one_ne_zero`. → with `iC_iomul`, the full **`C(βₖ) ≤ k+1` canonicity bound**.
- `02a4750` **`icmp_self`** (`icmp a a = 1`) + `cmpV_self`; **`icmp_iadd_iomul_finite`**
  (`icmp(ω·α+p)(ω·α+q) = cmpV p q`); **`icmp_betaTail_within`** (within-block descent `ω·α+p ≺ ω·α+(p+1)`).
- `a03945d` **`icmp_one_add`** (`icmp(1+e1)(1+e2) = icmp e1 e2` for NF exponents — the **boundary-descent
  crux**) + a reusable helper layer: `iadd_one_eq`/`fin`/`inf`, `ocExp_zero`, `cmpV_one_add`,
  `icmp_pos_pos`, `icmp_zero_pos`/`pos_zero`, `icmp_finHead_infHead`/`infHead_finHead`.

## KEY SIMPLIFICATION (don't re-derive `ievalNat` laws)
`ineq6_step_internal` keeps `ievalNat βₖ` **symbolic** — it needs only `isNF`, `iCanon`, the
`icmp`-descent of the *codes*, NOT computed `ievalNat` values. So the messy `ievalNat_iadd`/
`ievalNat_iomul` interaction laws are **NOT needed** for the assembly. Define `b k := ievalNat (k+1) βₖ`
and leave it opaque.

## NEXT (hardest-first) — toward assembling `hbound`
1. **`icmp_iomul`** : `icmp (iomul a)(iomul b) = icmp a b` (ω-mult order-preserving). Structural
   induction: head via `icmp_one_add` (needs isNF on the exponents — bundle isNF hyps), tail via IH.
   Use `icmp_pos_pos` to expose heads.
2. **boundary descent** : `icmp (ω·αNext + s)(ω·α + t) = 0` from `icmp αNext α = 0` — the decision
   happens inside the `iomul` part (via icmp_iomul), before the appended finite tails matter. Likely
   need an `icmp_iadd_finite_mono`: appending finite tails to two NoFin codes already in `lt` preserves it.
3. **`isNF_iomul`, `isNF_iadd_finite`** — isNF preservation (iomul: exponents `1+e` strictly decreasing
   via `icmp_one_add`; iadd_finite: append a positive finite below a NoFin NF code). For step's isNF hyps.
4. **βₖ assembly** — 𝚺₁-def `βₖ` in k from the M-internal descent (the SEAM, route b: make `Mlt`/`precφ`
   decode to `icmp`/codes; touches the proven `peano_not_proves_TI` girder — re-`#print axioms` after
   every seam edit). Then `iCanon (k+1) βₖ` (iC bounds, HAVE), icmp-descent (within+boundary), isNF,
   `b k = ievalNat (k+1) βₖ`, `step`=`ineq6_step_internal` (HAVE), `base`/`hpos` (need `βₖ≠0`,
   `ievalNat_pos`), assemble. **Consider building the slow-down as a STANDALONE lemma over an abstract
   code-descent first** (so it doesn't block on the seam).

## Aristotle
Idle. Next genuinely-open self-contained lemma to feed = `icmp_iomul` (spec: `icmp (iomul a)(iomul b) =
icmp a b` with the `icmp`/`iomul`/`icmp_one_add` laws as axioms over ℕ; clean proof PORTS). Spec
precisely (faithful axiomatization) before submitting.

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`.

## src/ sorries (unchanged): `Statement.lean:22` (headline, locked), `Reduction.lean:52` (off-path
Route-A), `DescentSemantic.lean:416` `hbound` (the lone genuine wall). `InternalONote` sorry-free.
