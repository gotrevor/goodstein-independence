# HANDOFF — lap 103 · Path-C datatype advanced: all node types + the ∀-cut ordinal drop

> Branch `plan`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). `src/` unchanged (headline footprint
> intact: `peano_not_proves_goodstein` = 0 math axioms, lone `sorryAx` = crux-2). ALL new work is in
> `wip/PathCOmega.lean` (NOT imported by `GoodsteinPA.lean`), every new decl axiom-clean
> `[propext, Classical.choice, Quot.sound]`.

## What this lap built (the Path-C stored-ordinal datatype, continuing lap 102's bricks)

Lap 102 settled the fork → Path C (Buchholz operator-controlled derivations with STORED ordinals) and
landed bricks 1 (ω-∀-node), 3-kernel (induction limit ordinal), 4-skeleton (abstract descent). This lap
filled in the datatype's node types + the cut-elimination ordinal drop, all axiom-clean:

1. **Induction ω-node PACKAGED** (brick 3 → a node). `zIndOmega` (tag 8) + `zIndOmegaValid` (premise
   family uniformly valid ∧ every depth-`k` unfolding `iord ≺` stored limit) + `zInd_realizes_zIndOmegaValid`
   — a regular finitary `zInd` realizes it, stored ord = `indOmegaStoredOrd` (the genuine limit the computed
   `iord` cannot assign). Per-premise validity is the motive-free `znth_iIndReductSeq_ZDerivation` (NO
   `zKValid` chain wall); the bound is brick 3's `iord_iIndReduct_lt_storedBound`.

2. **`sord` — the stored-ordinal projection** the Path-C `red` descent reads. 4-way `zTag` dispatch
   (7=∀ → α; 8=ind → α; 9=cut → α; else `iord`), `sord_z*Omega` compute α off the coding. **`sord` is
   `𝚺₁`-definable** (`sordDef` + `sord_defined`, the disjunction-form + `by_cases`/`numeral_eq_natCast`
   idiom from `tpReduceDef`) — the prerequisite for arithmetizing `gentzenDescentφ` (GentzenCon C5).

3. **The Path-C CUT NODE** (the genuinely-new ω-rule content). `zCutOmega` (tag 9) storing
   conclusion/stored-α/two premises/cut-formula; `zCutOmegaValid` = Buchholz operator-control (both premises
   `ZDerivation` ∧ each `sord ≺ α`); `sord_drop_zCutOmega_left/right` (each premise `sord ≺` cut `sord = α`).

4. **The ∀-cut REDUCTION STEP — the cut-elimination ordinal DROP** (this lap's headline content).
   - `imax` (icmp-max) + **`icmp_imax_lt`**: `max(a,b) ≺ α` whenever both `≺ α` — the linear-order fact,
     NO additive-principality of `α` needed (the whole reason a plain max works where `#` wouldn't).
   - `redCutAll`: the `cut`-vs-`∀x F` reduct — selects the witness premise `zsubst d0 a t` (brick 1) + the
     `∃`-side witness sub-derivation, rebuilds a SMALLER cut on `F(t)` with stored ord = `imax` of the two.
   - **`sord_redCutAll_lt`**: the reduction STRICTLY drops the stored ordinal `≺ α` (left bound from brick
     1's `zAllOmega_cut_descends`, right from the cut's operator-control). Pure premise selection — NO chain,
     NO `zKValid` reduct (the Path-X wall, lap 101/102, is avoided structurally).

5. **∃-introduction node + self-contained ∀/∃-cut reduction.** `zExOmega` (tag 10) storing
   conclusion/α/matrix/witness/premise + `zExTerm`/`zExPrem` projections + `zExOmegaValid`. `icmp_trans'`
   (unbounded `≺`-transitivity, `a+b+c` bound over the bounded `icmp_trans`). `redAllEx` +
   **`sord_redAllEx_lt`** — the reduction reads the witness `t` and right premise OFF the ∃-node (no
   external parameter), selects the ∀-node's premise at `t`, rebuilds the smaller cut, drops `sord`
   (`iord(selected) ≺ αAll ≺ α` via transitivity; ∃-premise `≺ α` via operator-control).

6. **The cut-orbit `red : V → V` (first dispatch case) + end-to-end drop.** Node projections
   `zAllD0`/`zAllEig`/`zCutL`/`zCutR`/`zCutC` + `fstIdx_zCutOmega`. `red` reduces a tag-9 cut whose premises
   are (ω-∀-node, ∃-node) via `redAllEx`; identity elsewhere. **`sord_red_lt_AllEx`** — on a concrete
   `zCutOmega s α (zAllOmega…) (zExOmega…) C`, `red` fires the dispatch and `sord` strictly drops below
   `α = sord w`. The genuine cut-elim step end to end (dispatch + selection + ordinal drop).

7. **`red_iterate_descends` — the realistic invariant-relativized infinite descent.** Carrying an orbit
   invariant `P`: if `P` is `red`-closed (`hinv`) and `red` drops `sord` on `P` (`hdrop`), then
   `n ↦ sord(red^[n] z)` descends forever. **This is the exact endgame shape** — reduces crux-2 to: define
   `P` + prove `hinv` (orbit closure) + `hdrop` (per-step drop, ✔ for ∀/∃ via `sord_red_lt_AllEx`).

## Where this sits in the endgame

The descent `n ↦ sord(red^[n] d₀)` contradicts crux-1's PRWO(ε₀). The per-step drops are now in hand for
the two genuine cases: **∀-cut** (`sord_redCutAll_lt`) and **induction** (brick 3 / `sord_drop_zIndOmega`).
`stored_ord_iterate_descends` (brick 4) packages the iteration abstractly. `sord` is Σ₁-definable. The
target remains `GentzenCon.gentzen_descent_of_inconsistent` (the lone deep axiom feeding crux-2; the
object-level `gentzen_reduction_internalized` is already PROVED via completeness).

## NEXT (Path-C build, in `wip/PathCOmega.lean` then promote) — precise obligations

The infinite descent is now packaged as `red_iterate_descends` (P-relativized). Crux-2 reduces to
**three** obligations (in difficulty order):

1. **`hinv` — the orbit invariant `P` is `red`-closed (THE hard one, structural cut-elim soundness).**
   Define `P` = "valid reducible ⊥-derivation" (a cut node whose premises derive the cut formula via
   decomposable top rules — ω-∀/∃/∧/∨/ind, with operator-control). Prove `red w` is again such a node: for
   the ∀/∃ case the reduct is a cut on the SMALLER `F(t)` whose premises (`zsubst d0 a t`, `zExPrem dR`)
   derive `F(t)` / `¬F(t)` via the appropriate top rule. This is the ω-rule analogue of the old `redSound`
   wall — but should be tractable here because premise SELECTION (not chain reduction) keeps validity (lap
   102 (A)). The reduct premises being the right node shapes is where the work is.
2. **`hdrop` — per-step drop on `P`.** ✔ for BOTH genuine cases now: ∀/∃-cut (`sord_redAllEx_lt` /
   `sord_red_lt_AllEx`) AND **induction-cut** (`sord_redIndEx_lt` — the depth-`t` unfolding `iord ≺` stored
   limit, brick 3, then `≺ α` by operator-control + `icmp_trans'`). Remaining for `hdrop`: the **∧/∨/atom**
   cut cases (each a smaller cut, `imax` drop via `sord_zCutOmega_imax_lt`) + fold all cases into the total
   `red` dispatch (currently `red` wires only the ∀/∃ case; `redIndEx` is proven standalone, wire it next).
3. **`red` is `𝚺₁`-definable** (`sord`/`imax` already are; `zsubst`/projections/`zCutOmega` are — compose),
   then **`gentzenDescentφ` := graph of `n ↦ sord(red^[n] d₀)`** (bounded iteration) → discharge
   `gentzen_descent_of_inconsistent` (C2 iterate keeps ⊥-derivations = `hinv`; C3 per-step `icmp` drop =
   `hdrop`/`red_iterate_descends`). `_realized`/`_dominated` = the C4/Lemma-3.2 bookkeeping.
4. **Wire** crux-1 ∘ crux-2 → `Reduction.goodstein_implies_consistency` → headline (ONLY when `#print
   axioms` clean — anti-fraud; `Statement.lean` `sorry` untouched until then).

Keep `InternalZ`/`Crux2Blueprint` (Path X) green in `src/` as fallback until Path C reaches the headline.

## Kernel state (unchanged this lap)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — bare `sorry`, 0 math
  axioms (lone `sorryAx` = crux-2).
- `peano_not_proves_consistency = [propext, choice, Quot.sound]` — clean. `goodsteinSentence_faithful` — clean.

## Aristotle
Idle — CORRECT. The Path-C `red`/cut-elimination work is entangled with the `InternalZ`/`Zinfty` API; not a
self-contained mathlib lemma.

## This lap's commits (all `wip/PathCOmega.lean`, axiom-clean, src/ untouched)
(1) brick 3 packaged (`zIndOmega`+validity+realization). (2) `sord` projection + per-node drops. (3) `sord`
`𝚺₁`-definability. (4) cut node (`zCutOmega`+validity+drops). (5) ∀-cut reduction step (`imax`/`redCutAll`/
`sord_redCutAll_lt` — the ordinal drop). `lake build GoodsteinPA` green 1325 throughout.
