# HANDOFF — 2026-06-24 (lap 61) — crux-2 C1 COMPLETE + C3 descent framework in place

> **Branch** `plan` · HEAD at lap end · `lake build GoodsteinPA` **green** (wip is off-target).
> Headline `peano_not_proves_goodstein` untouched (honest `sorry`, 0 math axioms, anti-fraud intact).
> All lap-61 work is in **`wip/InternalZ.lean`** (one consolidated module), every def/lemma
> axiom-clean `[propext, Classical.choice, Quot.sound]`.

## ⭐⭐ This lap: the entire system-Z ordinal **assignment (C1)** is built + the **Thm 4.2 descent templates (C3)** are proved

Buchholz's consistency proof (`CRUX2-ORD-ASSIGNMENT-2026-06-24.md`) runs over his finitary system Z;
crux-2 internalizes Thm 4.2 (`o(d[n]) ≺ o(d)`). Lap 60 built the C0 constructor codes + the `src/`
order theory (F1–F4 + ω-tower mono). This lap built **everything from the codes up to the descent
templates**:

**C1 — assignment functions (total `𝚺₁`, course-of-values tables mirroring `iC`/`iaddTable`):**
- **Internal variadic folds** `iseqMaxTab` / `iseqNaddTab` (the K^r premise *sequence* needs an
  internal fold; `InternalCor34.ibigMul` is a meta-iterate `k:ℕ` and can't reach internal arity
  `lh ds`). Plus the dual "apply-the-function" folds `iseqMaxIdg` / `iseqNaddIdg`.
- **Sub-derivation projections** `zRest`/`zIallPrem`/`zInegPrem`/`zIndP`/`zIndPrem0`/`zIndPrem1`/
  `zKrank`/`zKseq` — each `𝚺₀`, with compute-lemma on its own code + `≤d` bound.
- **`idg`** (degree) — all 5 §4 equations: `idg_zAtom=0`, `idg_zIall/zIneg=idg(prem)`,
  `idg_zInd=max(idg d0∸1, idg d1∸1, irk F)`, `idg_zK=max r (iseqMaxIdg ds ∸1)`.
- **`iotil`** (`õ`, pre-ordinal) — all 5 equations: `=0`; `=iadd(õ prem)(1)`;
  `=inadd ω^{õd0} ω^{õd1+1}`; `iotil_zK = iseqNaddIdg ds`.
- **`iord`** (`o`) `:= iotower (iotil d) (idg d) = ω_{dg(d)}(õ(d))` — the [KB81] assignment.
- Fold-congruence lemmas `iseqMaxAux_idgTable_eq` / `iseqNaddAux_ioTable_eq` (table-fold ↔
  direct-fold under dominance) drive the variadic `_zK` equations.

**C3 — Thm 4.2 descent, the deep crux (templates DONE, per-rule arithmetic remains):**
- **`iord_descent_samedeg`** — `dg(e)=dg(d) ∧ õ(e)≺õ(d) ⟹ o(e)≺o(d)` (via `icmp_iotower_mono`).
- **`iord_descent_dgdrop`** — `dg(d)=dg(e)+1 ∧ õ(e)≼õ(d) ∧ isNF(õ d) ⟹ o(e)≺o(d)` (via
  `icmp_iotower_lt_succ_of_le`). **These two cover EVERY Thm 4.2 descent shape.**
- I-rule case proved concretely (`iord_descent_I`, `iord_descent_zIneg/zIall`).
- NF building blocks `isNF_omega_pow`, `isNF_iseqNaddIdg` (discharge the dgdrop `isNF(õ d)` premise
  once derivations are characterized).

## NEXT LAP (hardest-first) — the remaining C3 content + wiring

The deep crux is now **decomposed**: "prove `o(d[n])≺o(d)`" reduced to "per rule, establish the
`õ`/`dg` comparison the matching template needs". Order of attack:

1. **C0 Fixpoint `ZDerivation : V → Prop`** (the key unblocker). Mirror Foundation's
   `Theory.Derivation` (`FirstOrder/Bootstrapping/Syntax/Proof/Basic.lean`) via
   `HFS/Fixpoint.lean`'s `Fixpoint.Construction`, with Z's rules 2–5 + atomic (the `z*` codes).
   Unblocks: `isNF (iotil d)` by structural induction (compose the per-constructor NF steps),
   `iR` well-definedness, and the ⊥-characterization (C2/C5).
2. **Ind / K^r per-rule `õ`-comparisons** (the deep Buchholz arithmetic, uses `src/` F1–F4):
   the Ind→Rep reduct is a `K^r`; need `# of k copies of ω^β ≺ ω^{β+1}` (F3 `icmp_term_lt_omega_succ`)
   + assignment **substitution-invariance** (`õ(d1(a/i))=õ(d1)` — a parallel brick, needs a coded
   substitution operator).
3. **C2 `iR`** (reduction `d↦d[0]`, Def 3.2) — to state the descent on concrete reducts.
4. **`irk` faithful rank** — currently STUB `=0` (real total `𝚺₀` fn, flagged); the faithful version
   is a course-of-values recursion on Foundation's coded formulas (qqAnd/qqAll/…). Off the descent
   *direction* (only sets the degree bound); needed for a fully-faithful `dg`.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
Aristotle OFFLINE. Crux 1 (`seqDescent_dominated`) untouched — see lap-56 STATUS box. `iotil`=`õ`
named `iotil` (avoid unicode-identifier risk).
