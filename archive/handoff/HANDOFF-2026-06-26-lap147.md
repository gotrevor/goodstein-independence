# Handoff: lap 147 — §5.2 has-redex half PROVEN (criticality decoupled); residual = general Γ→⊥ recursion

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline UNCHANGED `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
(0 math axioms). FOUR commits: major-premise dispatch wiring → criticality decoupling → `descent_step_K_hasRedex`
→ has-redex/no-redex split (+ docs).

Operator SOLE-OBJECTIVE = **M1b-term** (crux-2 termination). Attacked the hardest-first live sorry
`descent_step_K_noncritical` (Buchholz §5.2). Got off `red` AND off criticality, PROVING the Buchholz §14.253
principal-cut (has-redex) half for ALL ⊥-orbit chains; the lone §5.2 residual is now the §14.254 general
reduction.

## 🎉 HEADLINE — the has-redex half of §5.2 is PROVEN, criticality-free
`descent_step_K_hasRedex` (`Crux2Blueprint:2346`): a regular `∅→⊥` chain with the `isChainInf` exit data
(`j0`/⊥-exit/threading/rank) and ANY in-region redex pair `⟪i0,j1⟫` (`i0<j1≤j0`) has the genuine `iRKcCrit`
reduct as a strictly-`iord`-descending `ZDerivesEmptyR`. **NO `red`, NO criticality.** `descent_step_K_noncritical`
is now a sorry-FREE dispatcher: extract the exit `j0` from `zKValidF`, then `by_cases` on a redex below `j0` —
YES → `descent_step_K_hasRedex` (PROVEN); NO → `descent_step_K_noncrit_recurse` (the lone residual).

**Key insight (verified in-kernel):** the `iRKcCrit` soundness/descent/invariants are ALL criticality-free
GIVEN the redex; criticality entered ONLY at `isRedexPair_redexCode_of_zKValid` →
`inference_critical_pair_of_chain` (to PROVE a redex exists). Decoupling lemmas landed (`Crux2Blueprint`,
after `descent_step_K_critical`):
- `isRedexPair_redexCode_of_exists` — `redexCode` finds a redex from bare existence.
- `redexI_lt_of_redexPair` — pair-monotone bound `redexI<j0` from any in-region redex pair.
- `redZKReady_of_zKValidF_exists` — the redex ∀/¬ disjunction from `zKValidF`+existence (no crit).
- `iord_descent_iRKcCrit_corr_of_redex` / `_neg_of_redex` — descent via `iord_descent_iRcrit_of_redex`.
(Soundness `ZDerivation_iRKcCrit_all`/`_neg_botOrbit` + invariants `ZRegular/ZFresh/ZSeqAnt_iRKcCrit` were
already criticality-free; just fed the redex data.)

This also covers the NON-critical-but-has-redex chains the lap-141 critical/non-critical split left open, and
subsumes the redex-finding of `descent_step_K_critical` (now obtainable without criticality).

## 🎯 THE ONE remaining §5.2 sorry — `descent_step_K_noncrit_recurse` (`Crux2Blueprint:2470`)
No redex below the exit `j0` ⟹ by `majorPrem_tag_mem` the major premise (first ⊥-exit, `majorIdx ≤ j0`) is a
`Rep` node (`zInd`/sub-`zK`, tag 3/4 — Buchholz §14.254 REPLACE) or a tag-5/6 L-axiom whose cut-partner is a
chain (no direct R-intro). All cases REDUCE THE MAJOR PREMISE, a derivation of `Γₘ→⊥` with `Γₘ` possibly
NONEMPTY — the GENERAL `Γ→⊥` Z-derivation reduction. **This is the genuinely deep piece** (the ⊥-orbit
collapses of `descent_step_Ind`/`_K_critical`/`hasRedex` all needed `Γ=∅`/a redex; here neither holds).

**Concrete attack:** generalize the one-step descent to `Γ→⊥` and prove by STRONG INDUCTION on derivation
STRUCTURE (NOT `iord` — that would be PRWO, Gödel-barred inside 𝗜𝚺₁; structural/Σ₁ induction on the code is
fine), i.e. Buchholz Theorem 2.1 / Corollary 2.1:
  `descent_step_general : regular ZDerivation of Γ→⊥, not in endform ⟹ ∃ same-sequent reduct, iord-descending`.
Then `recurse` reduces the major premise by the IH (smaller derivation), REPLACES it in the chain
(`isChainInf_congr` keeps validity; `iord_descent_red_zK_replace_eq` gives descent). Per-tag reducts of
`descent_step_general`: `zInd`→Ind unfolding (generalize `descent_step_Ind` off `Γ=∅`); `zK`→recursion/splice
(`iord_descent_red_zK_chain_*` banked); `zAxAll`/`zAxNeg` with redex→`hasRedex`; atoms/`Ax1`→endform/§5 atomic.
This effectively rebuilds the reduction engine CORRECTLY (the broken `red`'s replacement).

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any `ZDerivesEmptyR_descent_step` branch with `red`.
- Do NOT re-introduce criticality into the `iRKcCrit` path (the decoupling is the lap's win).
- Do NOT attack the off-path dead `red`-soundness sorries {:80,:1253,:1356,:1529,:1616,:1705} AS STATED.
- Do NOT attempt `descent_step_general` by `iord`-recursion (PRWO/Gödel-barred); use STRUCTURAL induction.

## 📁 Key anchors
- `descent_step_K_hasRedex` `Crux2Blueprint:2346`; decoupling lemmas `:2122`-`:2325`; dispatcher `:2487`;
  residual `descent_step_K_noncrit_recurse` `:2470`.
- (A) `exists_sigma1_descending_step` `Crux2Blueprint:2811` (the other live-path sorry).
- Major-premise machinery (InternalZ): `majorPrem_tag_mem` :9369, `majorIdx_botOrbit_reducible` :9239.
- Banked replace descent (`RedZKDescent.lean`): `iord_descent_red_zK_replace_eq` :363, `_chain_replace` :625.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-146, binding) · `STATUS.md` lap-147 · `PENDING_WORK.md` lap-147.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. §5.2 has-redex
half PROVEN (criticality-free `iRKcCrit`); the ONE residual = `descent_step_K_noncrit_recurse` = the general
`Γ→⊥` reduction, closure via `descent_step_general` by STRUCTURAL induction (Buchholz Thm 2.1). NEVER `red`;
NEVER `iord`-recursion for the general step.**
