# HANDOFF — 2026-06-23 (lap 56) — FRESH-MIND REVIEW: crux-1 redirect + honest decomposition

> **Branch** `plan` · **HEAD `5751d79`** · tree **clean** · `lake build GoodsteinPA` **green (1316 jobs)** ·
> headline `peano_not_proves_goodstein` = honest `sorry` (`#print axioms` = `[propext, sorryAx, choice,
> Quot.sound]`, 0 math axioms, anti-fraud intact). **3 commits** `182a4e2`, `b814271`, `5751d79`.
> `wip/GentzenCon.lean` verified `lake env lean` green (2 expected sorries). `StdCor34` promoted to `src/`.

## What this lap did — crux 1 from "opaque + hidden bug" to a clean, honest decomposition

Fresh-mind review. Re-verified the kernel (headline 0 math axioms; `goodsteinSentence_faithful` clean;
M1+Phase 1 long done). **Direction VALIDATED:** crux 1 (`γ→PRWO`) is the right hardest-but-tractable
target; crux 2 (Gentzen eq-5) stays 🟠 generational, parked. Three verified commits:

### (1) `182a4e2` — opacity DISSOLVED → natCode↔NF bridge gone
Lap-55 built `prwoInstance` on the **opaque** `precφ` (`codeOfREPred₂`, std-model-only spec → wall-B
opacity in nonstandard `M`). Rebuilt on the **transparent** `InternalONote.icmp`:
`prec_internal := “z y. ∃ c, !icmpDef c z y ∧ c = 0”`, `eval_prec_internal : M⊧prec_internal[z,y] ↔
icmp z y = 0` (every `M⊧IΣ₁`). ⟹ the lap-55 "natCode↔NF order bridge" sub-target **dissolves** — PRWO
now uses the SAME coding `igoodstein` does. `prwoInstance_faithful` is now a clean `M=ℕ` corollary that
**shed its F-φ native_decide artifact** (axiom-clean). Memory `prwo-transparent-icmp-not-opaque-precphi`.

### (2) `b814271` — promote StdCor34 to src/, wire the girder seam
`wip/StdCor34.lean` → `src/GoodsteinPA/StdCor34.lean` (sorry-free, axiom-free; build 1316). Added
`SeqDominated M` (the girder inputs) + `nonterminating_of_dominated` (**axiom-clean** — the
certificate→girder seam type-checks end-to-end, the project's historical bug source).
`nonterminating_of_seq_descent` is now PROVED (was a bare sorry through lap 55).

### (3) `5751d79` — honest threading (fixed a false-for-arbitrary-seq sorry)
`seqDescent_dominated`'s conclusion `SeqDominated M` is seq-free, so "any descent ⟹ a standard-dominated
descent exists" is FALSE in general (`F_diag_not_dominated`). Fixed (lap-50 prescription): thread the
seq-specific certificate **`SeqStdBounded seq M := ∃ l₀:ℕ, ∀ n y, (M⊧/![y,n] seq) → iC y ≤ iF l₀ n`**
(Rathjen Lemma 3.2) through `seqDescent_dominated` / `prwoInstance_models_of_goodstein` /
`goodstein_implies_prwo` (now `(hstdom : ∀ M⊧IΣ₁, SeqStdBounded seq M) → 𝗣𝗔⊢γ → 𝗣𝗔⊢prwoInstance seq`),
supplied at `gentzenDescentφ` by the disclosed axiom `gentzenDescentφ_dominated`.

## State of the crux-1 chain (`wip/GentzenCon.lean`)
`goodstein_implies_consistency = goodstein_implies_prwo gentzenDescentφ ∘ gentzen_prwo_implies_consistency`.
- **`goodstein_implies_prwo`** (crux 1) — PROVED modulo the ONE sorry `seqDescent_dominated`; honest
  (certificate is a hypothesis). `nonterminating_of_dominated` (girder seam) axiom-clean.
- **`seqDescent_dominated`** — TRUE conditional lemma, lone crux-1 sorry = the Cor-3.4 construction:
  from a `SeqStdBounded` + `icmp`-descending `seq`, build `SeqDominated M` (β = a descending NF branch;
  the standard-level slowdown gives `wseq`/`Cβ`/`l₀` + bounds + width-domination via `hstdom`).
- **`gentzen_prwo_implies_consistency`** (crux 2) — bare sorry + `ord`/`R`/`ord_R_descends`(eq-5, icmp
  form)/`derivesEmpty`/`gentzenDescentφ`/`gentzenDescentφ_dominated` placeholder axioms. 🟠 generational.

## NEXT (hardest-first)
1. **Prove `seqDescent_dominated` in the GENERAL (abstract-`SeqStdBounded`) form** — the independent
   crux-1 work, NOT gated on crux 2. Substrate: `StdCor34.crux1_internal_run_of_width_dom` consumes
   `β`/`wseq`/`l₀`/bounds; the gap is constructing them from the descent. Sub-steps:
   (a) `β` = a descending NF branch of `seq` (needs functionality/branch extraction; for `gentzenDescentφ`
       a genuine function); (b) `wseq` block code from `β`'s complexity (`BlkRec`, "mechanical 𝚺₁
       recursion"); (c) `hβC` from `wseq`'s construction; (d) `hwdom` (`znth wseq n ≤ iF l₀ n`) from
       `hstdom` — the part that genuinely USES the certificate. Blueprint: sorry-free ℕ-template
       `Grzegorczyk.lean` `corAlpha_*` + memory `crux1-headline-needs-only-standard-level`.
2. **Crux 2 (`ord`/`R` arithmetization in PA)** — the shared bottleneck: `gentzenDescentφ` (and hence its
   Lemma-3.2 certificate `gentzenDescentφ_dominated`) cannot be DISCHARGED until `ord`/`R` exist. 🟠
   generational (Buchholz [6]; Foundation Hauptsatz is meta-level, no shortcut). Chip opportunistically:
   read `papers/buchholz-on-gentzens-first-consistency-proof.pdf` + `papers/siders-*`, decompose eq-(5).

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/GentzenCon.lean` is wip. Aristotle OFFLINE. WebFetch dead. Honest endpoint unchanged: best-case
headline `[propext, choice, Quot.sound, PA_delta1Definable]`.
⚠️ NEVER `git checkout <wip file>` to undo edits (reverts to HEAD, drops them) — `cp` from scratchpad.
