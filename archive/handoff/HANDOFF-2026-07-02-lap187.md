# Handoff: REBUILD-Z lap 8 (global 187) — judged src port + ladder erection; STOP for judge

**Date**: 2026-07-02 · **Branch**: `plan` · **Build**: 🟢 `lake build` 1341 jobs · **Headline**:
`peano_not_proves_goodstein` = `[propext, Classical.choice, goodstein_implies_consistency,
Quot.sound]` (UNDRIFTED). Full verdict: `REBUILD-Z-LAP8-VERDICT.md` (written for the judge).

## 🎯 What this lap did (executed `REBUILD-Z-LAP8-ENTRANCE-2026-07-02.md`)

Ported the ratified lap-7 `Zef2` statement layer into `src/`, discharged what was mechanical,
erected the wainer ladder as named pins, and **isolated two genuine escalation points** (both
architect-owned, NOT self-ratified).

## ✅ State — DISCHARGED / BANKED this lap

- `src/GoodsteinPA/EwIter.lean` (P-a) — verbatim iterate port; `src/GoodsteinPA/OperatorZef2.lean`
  (P-b) — `Zef2` calculus. Both wired into `GoodsteinPABlueprint`.
- **Read-off pins (P-c) DISCHARGED** via `Zef2.toZef` (forgetful map, mandated route; also the
  conservativity witness) — `readoff_sigma1_Zef2` / `headline_readoff_Zef2` are real theorems.
- **`allInv_Zef2` (P-d) DISCHARGED** — real proof; the new `Zef2.gate` projection (every
  constructor exposes `ewN α ≤ f 0`) + `gate_rel1` thread the size gate through inversion.
- **ewN arithmetic BANKED (kernel-verified):** `ewN_addAux_le`, `ewN_add_le`, `ewN_osucc_le`,
  `ewN_osucc_add_le` (`ewN(osucc(α+γ)) ≤ ewN α + ewN γ + 1`).
- **`collapseIter_NF` PROVEN** (rung-R ordinal-tower NF preservation, real content).

## 🚧 Named disclosed pins in `src` (the sorry delta — EXACTLY these)

`OperatorZeh.lean:1907` old pin 3 `cutElimPass_Zf` (unchanged, superseded). In
`OperatorZef2.lean`: `cutElimPass_Zef2` (mandated pass pin), `cutReduceAllAuxRunning_Zf2` +
`stepAllω_Zf2` (P-d reduction/step), rungs `rankToZero_Zef2` (L-R) / `readoff_delta0_Zef2` (L-D) /
`embedding_Zef2` (L-E) / `wainer_splice_Zef2` (L-W).

## 🧠 TWO ESCALATIONS for the judge (the real payload — read the verdict §2, §3, §5)

1. **Reduction-level GATE-COMPOSITION trap (P-d, `cutReduceAllAuxRunning_Zf2`).** The reduction's
   synthesized `osucc(α+γ)` roots need `ewN(osucc(α+γ)) ≤ g(f 0)`, i.e. (by `ewN_osucc_add_le`)
   `ewN α + ewN γ + 1 ≤ g(f 0)`. Available gates: `ewN γ ≤ f 0` (∃-side base `f`) and `ewN α ≤ g 0`
   (∀-family, ∀-side base `g`). With `EwF1 g` this closes **iff `ewN α ≤ f 0`** — the ∀-family
   ordinal gated at the **∃-side** base. But `fam` gates α only at the ∀-side base `g`, and `ewN`
   is NOT ordinal-monotone (trap-8 pathology), so no witness-slack recovers it. The cross-gate is
   not `f.1`-class → statement change → escalation; and it is entangled with how the FORBIDDEN
   `cutElimPass_Zef2` wires the two premise slots. **This is the precise kernel-grounded reason
   pins 1–2 don't re-thread over `Zef2` as-stated.**
2. **L-E embedding trap-9 (entrance-pre-authorized).** A faithful `embedding_Zef2` must bind the
   target sequent to the concrete `𝗣𝗔`-goodstein translation + a `𝗣𝗔 ⊢ ↑goodsteinSentence`
   source; that machinery lives cross-module (`Statement`/`WainerRoute`) and is unavailable at
   `Zef2`-statement level. Per "escalate rather than improvise," it is stated PARAMETRICALLY over
   `Γ_G`; binding `Γ_G` is the escalation locus.

**Representation finding:** sorry rung pins CANNOT be `@[goodstein_blueprint]`-tagged
(`BlueprintAudit` computes `broken` for any `sorryAx` footprint) and an `axiom` is FORBIDDEN this
lap, so the rungs live on the tex dep-graph (NODE_NOTE), not the machine ledger. `annotate_
depgraph.py`'s own comments confirm this. Ledger-14 citation updated to name the rungs. The tex
`\lean{}`-binding + web regen is host-side bookkeeping (left for the host/judge; not a hard gate).

## 🎬 Next actions (laps 9+, AFTER the judge rules)

1. **Judge ruling on the two escalations** (verdict §5). Until then the pass grind stays FORBIDDEN.
2. If the reduction cross-gate is ruled admissible: `cutReduceAllAuxRunning_Zf2` closes with
   `EwF1 g` + `ewN α ≤ f 0` threaded (the proof skeleton mirrors `cutReduceAllAuxRunning_Zf`;
   `Zef2.gate`/`gate_rel1`/`ewN_osucc_add_le` are the levers, all banked). Then `stepAllω_Zf2` via
   `allInv_Zef2` (already proven) + the reduction.
3. Rung grind order (post-ratification): R (plain induction over the pass) is quickest; D
   parallelizable; E after its translation target is supplied.

## ⚠️ Gotchas

- `Zef2` gate is on EVERY node (`hαN : ewN α ≤ f 0`); `Zef2.gate` extracts it uniformly. `ewN` is
  NOT ordinal-monotone — do not assume `α' ≤ α ⟹ ewN α' ≤ ewN α`.
- Old pin 3 / old `Zef` / `iterSlot` in `OperatorZeh.lean` are FROZEN evidence (docstring-superseded
  only). `wip/EwIter.lean` + `wip/*Probe.lean` are the freeze reference — read-only.
- Newest baton by numeric lap sort: `ls HANDOFF-*-lap*.md | sort -t p -k2 -n | tail -1`.

## 📁 Key files

- `src/GoodsteinPA/OperatorZef2.lean` — the whole `Zef2` layer + 7 named pins + ewN arithmetic.
- `src/GoodsteinPA/EwIter.lean` — the iterate.
- `REBUILD-Z-LAP8-VERDICT.md` — the judge verdict (statement of both escalations + gate status).
- `REBUILD-Z-LAP8-ENTRANCE-2026-07-02.md` — the order this lap executed.

---
**→ Next session: this lap is COMPLETE and STOPPED for the judge. Do NOT grind the pass or the P-d
reduction until the judge rules on the two escalations (verdict §5). If resuming before a ruling,
the productive open work is rung R (`rankToZero_Zef2`) — a plain induction over the pass pin — but
it consumes `cutElimPass_Zef2` so its footprint stays sorry-tainted regardless.**
