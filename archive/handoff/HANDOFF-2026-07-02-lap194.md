# Handoff: lane-D read-off DECOMPOSED (lap 194) — `sound0` + falsity-invariant `readoffD_aux` proven in `src`; rung-D reduced to ONE named residue

**Date**: 2026-07-02 · **Branch**: `plan` · **Build**: 🟢 `lake build GoodsteinPA` 1328 jobs ·
**Headline**: `GoodsteinPA.peano_not_proves_goodstein = [propext, Classical.choice,
goodstein_implies_consistency, Quot.sound]` (sorryAx OFF, UNDRIFTED — re-verified this lap via
`#print axioms`). Tree CLEAN.

**Order in force**: `REBUILD-Z-SERIES-1-ORDER-2026-07-02.md` · **Binding directive**: `DIRECTION.md`
CURRENT DIRECTIVE **lap-192 block** (MANDATE = lane D; top-rank cut is JUDGE-GATED, wip-only probes).
**Ledger**: `REBUILD-Z-SERIES-1-LEDGER.md` (lap-194 row at end).

## ✅ What this lap did (1 green commit) — mandated lane D

Rung D `readoff_delta0_Zef2` (`OperatorZef2.lean`) went from a single monolithic `sorry` to a
`readoffD_aux` reduction, and `readoffD_aux` is PROVEN in `src` for every rule but one. All new,
axiom-clean except the disclosed residue:

- **`sound0`** — rank-0 `Zef2` soundness (`∃ ψ ∈ Γ, atomTrue ψ`), the reusable truth core; ported
  from `wip/Lap13ReadoffDeltaProbe.lean`. The hard content is the `allω` (Π) combination.
- **`valm_nm`** — `Semiterm.valm ℕ ![] f (nm n) = n` for `OperatorZinfty.nm`, `by simp [nm]`.
  (⚠️ importing `GoodsteinPA.Embedding` to reuse ITS `valm_nm` is a DEAD END — it surfaces `Γ₀`
  parse-token clashes in existing `OperatorZef2` proofs. Prove numeral-eval locally instead.)
- **`atomTrue_all_iff` / `atomTrue_ex_iff`** — `atomTrue(∀⁰χ) ↔ ∀k, atomTrue(χ/[nm k])` + ∃ dual.
- **`readoffD_aux`** — the strengthened **FALSITY invariant** (the re-framing that makes it go):
  `Zef2 α e H f 0 Γ → (∀ ψ∈Γ, ψ = ∃⁰φ ∨ ¬atomTrue ψ) → ∃ n ≤ f 0, atomTrue(φ/[nm n])`.
  PROVEN: `axL` (a true literal ⊥s the all-false hyp), `wk`/`weak` (subset-inherit, slot `f` fixed),
  `exI` (slot stays `f`; χ=φ ⇒ use the introduced `n ≤ f 0` or recurse; χ≠φ ⇒ `χ/[nm n]` false ⇒
  recurse), `cut` (rank-0 vacuous), `allω` NON-trapped (`∃⁰φ ∉ Γ₀` ⇒ false branch ⇒ `sound0` ⊥).
- **`readoff_delta0_Zef2 := readoffD_aux dd rfl (Or.inl on the singleton)`** — proven modulo residue.

## 🔑 Route-decisive facts to carry forward

- **The SOLE residue = `readoffD_trapped`** (named `sorry` in `src/GoodsteinPA/OperatorZef2.lean`).
  The `allω` node with the goal existential `∃⁰φ` TRAPPED in the shared context `Γ₀` by a lower-`exI`
  contraction. Branches run at `rel1 f n` (`rel1 f n 0 = f n`, NOT `f 0`) ⇒ the branch witness bound
  is `≤ f n`, breaking the outer `≤ f 0`. **Monotone `f` does NOT fix it** (`f n ≥ f 0`, wrong way).
  This is Towsner §5.4 / Thm 17.1 clause-(ii) **growth-coupled** witnessing (`𝒢(n) > h_α(k)`), not a
  pure structural read-off — the bound is re-derived per level via the ordinal descent, not
  propagated. Concrete worst-case in PENDING_WORK.md lap-194 (finding #2).
- **The Δ₀ hypothesis `hφbdd` is NOT consumed** by the read-off as proven (`atomTrue` total on ℕ ⇒
  falsity invariant self-maintains, no syntactic Δ₀ descent). Kept verbatim (unused-var warning
  only). Judge-relevant: `<BoundedInstance>=DeltaZero` is harmless but not load-bearing here.

## 🚧 NEXT (attack `readoffD_trapped` — still lane D)

1. **Option A (statement-shape / caller) — likely the intended factoring.** Add a no-trap SHAPE
   hypothesis to the read-off that FORBIDS `∃⁰φ` from entering an `allω` context (mirrors how
   `readoff_sigma1`'s `ReadoffShapeF` makes `allω` dead), then have the reduction-exit caller supply
   it. **Check first:** is the `rankToZero` exit derivation contraction-free / principal enough to
   establish such a shape? If yes, this closes rung D cleanly.
2. **Option B (growth-coupled):** thread the banked `hardy`/`fastGrowing` separation into the
   invariant à la Thm 17.1 clause (ii). Heavier; couples the read-off to the Part-2 lower bound.
3. If neither is clean, the read-off statement may need a contraction-free / principal-derivation
   hypothesis — a JUDGED amendment (do not self-ratify), record as escalation input.

## 📁 Key files
- `src/GoodsteinPA/OperatorZef2.lean` — `sound0`, `valm_nm`, `atomTrue_all_iff/ex_iff`, `readoffD_aux`
  (PROVEN), `readoffD_trapped` (residue `sorry`), `readoff_delta0_Zef2` (wired). Other sorries:
  `:655` top-rank cut (judge-gated), `embedding_Zef2` rung E (architect-gated).
- `PENDING_WORK.md` — lap-194 block (advance + 2 findings + Option A/B attack) at top.
- `REBUILD-Z-SERIES-1-LEDGER.md` — lap-194 row + the 3 series-judge hygiene items.
- `wip/Lap11CutFloorProbe.lean` — header hardened to ruling-grade (refuter satisfies the pass kit).
- `DIRECTION.md` — CURRENT DIRECTIVE lap-192 block (BINDING; lane D mandate; crux judge-gated).
