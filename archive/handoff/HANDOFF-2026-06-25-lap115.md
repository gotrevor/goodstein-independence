# HANDOFF — lap 115 · the inversion's L-half PROVEN; gate = `zAx1`-as-ZDerivation

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint unchanged: `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice,
> Quot.sound]` (0 math axioms). New lemmas `ZDerivation_corrected_haux1` + `tp_zAx1` axiom-clean
> `[propext, choice, Quot.sound]`. No sorries added (still 8 in `Crux2Blueprint`).

## ⭐⭐⭐ THE ADVANCE — BOTH halves of the corrected critical-cut inversion are now proven
Lap 114 proved the R-half `ZDerivation_corrected_haux0`. **This lap proves the L-half
`ZDerivation_corrected_haux1`** (`Crux2Blueprint.lean`, after `haux0`), grounded in the verbatim Buchholz
§5 (scratchpad `buchholz-gentzen.txt`, NOW READ — was the missing source for the L-side).

### The Buchholz §5 grounding (the piece the prior laps were missing)
- **Def 3.2 case 5.1** (`buchholz-gentzen.txt:653`+): for `Aᵢ = ∀xF`, `d{1} := K^r_{A(d),Π} (j/dⱼ[0])` — the
  L-redex premise `j = redexJ` (an `axAll` axiom, `tp = L^k_{∀xF}`) is replaced by `dⱼ[0]`, conclusion
  antecedent gains `A(d) = F(k) = cutFormula d`.
- **§5 case 2.1** (`buchholz-gentzen.txt:903`): `dⱼ = Ax^{∀xF,k}` has endsequent `Π = Γⱼ→F(k)` (succedent
  **is** `F(k)`!), and `tp(dⱼ) = L^k_{∀xF}`, so `dⱼ[0] = Ax^1_{F(k),Γⱼ→F(k)}` — the §5 **logical axiom**
  `Ax^1` (rule 1, `:844`: `Γ→C` with `C∈Γ`), antecedent gains `F(k)`, succedent `F(k)`.
- So the engine's faithful L-reduct is `v = zAx1 (seqAddAnt (cutFormula d) sⱼ) C` (antecedent `F(k),seqAnt sⱼ`,
  succedent `seqSucc sⱼ = F(k)`), valid because succedent ∈ antecedent (the just-added `F(k)`).

### What `ZDerivation_corrected_haux1` proves (axiom-clean)
Replacing premise `redexJ` of a critical chain `zK s r ds` (with `znth ds redexJ = zAxAll sⱼ p k'`) by the
corrected reduct `v`, and growing the conclusion antecedent by `cutFormula d` (`seqAddAnt`), is a
`ZDerivation`. Discharged via `ZDerivation_iCritReplaceReduce_general` (the antecedent-growth replace
constructor — same one the I¬ replace `ZDerivation_zK_replace_zIneg_of` uses); since `tp v = isymRep` and
`zTag v = 7`, the `iperm` conjunct is `iperm_isymRep` and all four tag-formula conjuncts (`hf1/2/5/6`) are
vacuous. The well-formedness conjuncts come from the parent chain's `zKValidF` (`hcf` gives `IsUFormula
(seqSucc sⱼ)`; `hss`/`hsa` give the conclusion wff via `forall_IsUFormula_seqCons`).

### The TWO genuine residuals — isolated as named hypotheses (exactly like `haux0`'s orbit data)
- **(O-L1) `hZredL : ZDerivation (zAx1 (seqAddAnt (cutFormula d) sⱼ) C)`** — that the §5 logical axiom is
  itself a `ZDerivation`. **This is the gate, and `zAx1` (tag 7) is NOT currently a `ZDerivation` at all**
  (`ZPhi`, `InternalZ:5231`, has 7 disjuncts; tag 7 is absent). L-side analogue of the R-side
  `ZDerivation_zsubst_zIall_premise`.
- **(O-L2) `hci : isChainInf (seqAddAnt (cutFormula d) s) r (seqUpdate ds redexJ v)`** — the threading
  reconstruction. L-side analogue of `haux0`'s `hthread`/`hrank`; built from the parent `isChainInf`
  restricted to `≤ j₀` with the `F(k)`-weakened antecedent (the lap-113 `irk_chainAsucc_redexJ` reasoning,
  `isChainInf_reduceR_membership` / `isChainInf_seqAddAnt`).

## ⭐ THE CRUX FINDING (extends lap-114's R-side mismatch to the L-side)
The engine's L-redex critical reduct `zAxReduct (zAxAll sⱼ p k') = zAx1 sⱼ p` (`InternalZ:6343`,
`red_zK_crit`'s `ρ`) is unfaithful in **THREE** ways vs Buchholz §5 case 2.1:
1. **payload** `p` (the open matrix) vs `F(k) = substs1 (numeral k) p` (the closed instance);
2. **sequent** `sⱼ` vs `seqAddAnt (F(k)) sⱼ` (the antecedent must gain `F(k)`);
3. **`zAx1` is not a `ZDerivation`** (tag 7 missing from `ZPhi`) — so even with payload/sequent fixed,
   `ρ(redexJ)` is not a valid premise.
All three are descent-invariant (the õ uses `oAtom1 = 2·rk(C)`, and `rk(p)=rk(F(k))` — substitution of the
bound var preserves rank), which is why laps 108–113 never exercised the defect (mirror of why instance-0
hid on the R-side).

## 🎯 NEXT-LAP TARGET (hardest-first, the gate for the whole inversion) — make `zAx1` a sound ZDerivation
Add the §5 `Ax^1` logical axiom as the **8th `ZPhi` disjunct**:
`(∃ s C, d = zAx1 s C ∧ inAnt (seqSucc s) (seqAnt s))` — validity = succedent ∈ antecedent, mirroring `zAtom`
(`InternalZ:5232`). Then `hZredL` (O-L1) discharges via a `zDerivation_zAx1_intro`. Ripple (add the new arm
at the END to minimise disruption — existing 7-arm `rcases` just gain a trailing tag-7 pattern + bullet):
1. `ZPhi` (`:5231`), `zphi_monotone` (`:5241`), `zphi_strong_finite` (`:5256`), `zphi_iff` (`:5273`).
2. `zblueprint` σ-core (`:5318`) + π-core (`:5337`): mirror the `zAxNeg` disjunct
   (`!zAx1Graph d s C ∧ ∃ ss,!seqSuccDef ss s ∧ ∃ sa,!seqAntDef sa s ∧ !inAntDef ss sa`). `zAx1Graph` exists
   (`:653`). Then `zPhi_definable` (`:5356`) simp — add `zAx1_defined.iff`.
3. ~64 `rcases zDerivation_iff.mp` sites (`grep -rn zDerivation_iff.mp src/GoodsteinPA/*.lean`): Zsubst 24,
   InternalZ 26, Crux2Blueprint 12, RedZKDescent 2. Each gains one trailing `| ⟨s, C, h, _⟩` pattern and one
   trailing bullet — almost all are tag-mismatch (`absurd (congrArg zTag h) (by simp)` or `simp at htag`).
   The few that genuinely case on every tag (`tp_eq_isymRep_of_zTag`, `red_rep_of_tp_isymRep`,
   `tag_uformula_of_ZDerivation`, `zDerivation_iff.mpr` builders) need a real tag-7 arm (trivial: `zAx1` is a
   `Rep` leaf — `tp = isymRep`, `red zAx1 = zAx1` presumably, no I/Ax formula).
4. Add `zDerivation_zAx1_intro` + `zDerivation_zAx1_inv` (mirror `zDerivation_zAtom_inv`, `InternalZ:8171`).
**Risk:** the σ/π arithmetization + `zPhi_definable` simp is the delicate part; budget a full lap. The 64-site
ripple is mechanical but error-prone — do it compile-driven, one file at a time, commit per green file if
possible. Once `zAx1` is a ZDerivation, (O-L1) closes; (O-L2) `hci` is the lap-113 threading; then assemble
`ZDerivation_red_zK_crit` = `ZDerivation_iRcritG_of` fed `haux0`/`haux1` — **but only after re-keying `red`'s
tag-4 critical branch (`iRNextG`/`iRKc`) to emit the corrected reducts** (R: `numeral k`; L: `zAx1 (seqAddAnt
F(k) sⱼ) (F(k))`), the lap-114 step-2 work, re-checking each `red_zK_*` descent rewrite (instance-invariant ⟹
should transfer).

**Banked, axiom-clean, reusable:** `ZDerivation_corrected_haux0` (R-half), `ZDerivation_corrected_haux1`
(L-half), `tp_zAx1`, + the lap-114 `Zsubst` linchpins.

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, 8 sorries — by NAME, lines drift)
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` (THE prize; FALSE for current `ρ` — **BOTH halves
now proven** `haux0`/`haux1`, remaining = `zAx1`-as-ZDerivation (O-L1) + threading (O-L2) + `red`
redefinition) · `ZDerivation_red_zK_splice` · axNeg replace validity · redZKReady-class · `iord_descent_red`
chain-REPLACE IH · M2 `foundation_bot_to_Z_empty` · M3 `false_of_ZDerivesEmpty`.
(`ZDerivation_corrected_haux0`/`_haux1` are sorry-FREE; not yet wired into the `ZDerivation_red_zK_crit`
sorry, pending O-L1/O-L2 + the corrected `red`.)

## Aristotle
Idle — CORRECT. The next piece (`zAx1`-as-ZDerivation + 64-site ripple) is an engine-internal change, not an
isolated open lemma.
