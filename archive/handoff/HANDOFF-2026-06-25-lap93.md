# HANDOFF — lap 93 · Path-X O1: `maxEigen`/`zReg` freshness machinery landed

> Supersedes prior HANDOFF-*.md. Branch `plan`. HEAD = `3df7d68`. Build 🟢 green
> (`lake build GoodsteinPA`, 1325 jobs). Headline `peano_not_proves_goodstein =
> [propext, sorryAx, choice, Quot.sound]` (0 math axioms, unchanged); the lone math `sorry` is still
> `goodstein_implies_consistency` (`Reduction.lean:68`) = crux-2 = `redSound`.

## Operator target (unchanged)
Discharge `peano_not_proves_goodstein` AXIOM-FREE = crux-1 (DONE lap 57) ∘ crux-2 (`redSound` =
internalized cut-elimination). Path X (finitary, lap-92 DECISION) is the chosen route. O1 =
eigenvariable-freshness tracking maintainable through `red`.

## What lap 93 landed (6 commits, all axiom-clean `[propext, choice, Quot.sound]`, green 1325)
All in `src/GoodsteinPA/Zsubst.lean` (namespace `GoodsteinPA.InternalZ`):

1. **`maxEigen` recursion equations + `maxEigen_zsubst`** (`2e9ea8d`). `maxEigen_zAtom`/`_zIall`(`max a
   (maxEigen d0)`)/`_zIneg`/`_zInd`(`max (π₁ at') (max …)`)/`_zAxAll`/`_zAxNeg`/`_zAx1`/`_zK`(`iseqMaxEigen
   ds`) via the `idg` table-correctness template. `maxEigen_zsubst (a t) : ∀ d, ZDerivation d →
   maxEigen (zsubst d a t) = maxEigen d` (closed-term subst preserves eigenvariable indices).

2. **`ZDerivation_zsubst` reformulated** (`6331b3a`) from the code bound `d ≤ a` to the genuine,
   substitution-stable freshness bound **`maxEigen d < a`**. The `maxEigen` block was RELOCATED above
   `ZDerivation_zsubst` so it can reference it. Added fold-bound `le_iseqMaxEigen`. Corollaries
   `ZDerivation_zsubst_zIall_premise`/`_zInd_premise1` now take `maxEigen d0 < a` / `maxEigen d1 < π₁ at'`.
   (Dead `hsa : s ≤ a` removed — never used.)

3. **`zReg` hereditary-freshness invariant** (`6774f10`). ADDITIVE architecture (NOT a `zIallWff` edit —
   that reshapes the `ZDerivation` fixpoint; large blast radius). Standalone `𝚺₁` violation-count
   `zReg d` (`0` iff regular) on the `maxEigen` template: `ltFlag`/`zRegNext`/`zRegTable` + recursion eqs.
   `ZRegular d := zReg d = 0`. Route-B bridges `maxEigen_lt_of_regular_zIall`/`_zInd` (regular node ⟹ the
   `maxEigen d0 < a` the reformulated `ZDerivation_zsubst` consumes). `zReg_zsubst` (regularity preserved
   by closed-term subst).

4. **`red` preserves `ZRegular` — structural + Ind cases** (`73b79ee`): `ZRegular_red_of_not_zK` covers
   atom/zIall(→d0)/zIneg(→d0)/zInd(→`iRInd`)/zAx*. KEY finding: `iRInd (zInd …) = zK s (irk p)
   (iIndReductSeq d0 d1 1)` is a chain over the **literal** premises `⟨d1,d0⟩` — NO substitution at the Ind
   level. Added `iseqReg` fold lemmas (`_seqCons`/`_const`/`_iRepeatSeq`/`_iIndReductSeq`, `_znth_congr`).

5. **`zK`-case closing blocks** (`da3b40a`): `ZRegular_zK_of_premises` (a chain with all-regular premises
   is regular) + `ZRegular_zAxReduct` (per-premise atomic reduct preserves regularity).

6. **`zK` reduct-regularity helpers** (`3df7d68`): `le_iseqReg`, `ZRegular_zK_premise` (premise of a regular
   chain is regular), `ZRegular_zK_of_seqUpdate` (5.2.2 `iRKr` + 5.1 `iRKc` halves),
   `ZRegular_zK_of_iCritReductSeq` (5.1 `iRKc` outer chain).

## ⚠ KEY STRUCTURAL FINDING (lap 93) — reorients the next phase
**`red`-preserves-`ZRegular` for the `zK` case is NOT a standalone lemma — it needs `zKValidF`.** The
genuine cut-elim substitution lives ONLY in the `zK` dispatch (`red (zK …) = iRK …`, Buchholz 5.1
`iRKc`=`iCritReductG` / 5.2.1 splice `iRKs` / 5.2.2 replace `iRKr`); `red` of `zIall`/`zIneg` just strips to
the premise. The 5.2.1 splice `iRKs` reads halves `a,b = znth (zKseq (red dᵢ)) 0/1` of `dᵢ = znth ds permIdx`,
and `zReg a ≤ zReg (red dᵢ)` holds ONLY when `red dᵢ` is a genuine tag-4 chain — which requires `zTag dᵢ = 4`,
a fact that follows from `zKValidF`'s validity data, NOT from `ZDerivation`+`ZRegular` alone. **⟹ regularity
must be threaded INSIDE the `redSound` induction**, where `zKValidF` is in scope, not as a separate
`red_preserves_ZRegular`. The lap-93 helpers above are exactly the tools that combined induction will use.

## ▶ NEXT (START HERE)
1. **Inspect the existing `redSound`/`RedSound` scaffolding first** (InternalZ.lean, laps 82–90: search
   `RedSound`, `iord_descent_red`, the 5.2.1/5.2.2 validity leaves `iord_descent_iCritAux`/`iSpliceEnd`,
   `zKValidF_iCritReductSeq`). Understand the current shape of the tag-4 DISPATCH assembly in `iRNextG`
   and what `redSound` must produce.
2. **Prove "red of a valid + regular derivation is valid + regular"** by the `redTable`/`zDerivation_induction`:
   - non-`zK`: reuse `ZRegular_red_of_not_zK` (regularity) + existing validity leaves.
   - `zK` `iRKr` (5.2.2): chain over `seqUpdate ds permIdx (red dᵢ)` (permIdx d < lh ds in this branch);
     regularity via `ZRegular_zK_of_seqUpdate` + `ZRegular_zK_premise` (premises) + IH `ZRegular (red dᵢ)`.
   - `zK` `iRKc` (5.1): chain over `iCritReductSeq` of two `seqUpdate` halves; `ZRegular_zK_of_iCritReductSeq`
     + `ZRegular_zK_of_seqUpdate` + `ZRegular_zAxReduct` (the `ρ`-reducts). Needs redex range facts (use
     `zKValidF` if needed).
   - `zK` `iRKs` (5.2.1): use `zKValidF` to pin `zTag dᵢ = 4` ⟹ `red dᵢ` is a tag-4 chain ⟹ its halves
     `a,b` are regular premises (`ZRegular_zK_premise` on `red dᵢ`). Then `ZRegular_zK_of_premises` over
     `seqInsert ds permIdx a b` (need `znth_seqInsert` entry lemmas — check they exist near line 3661).
   - The I∀/Ind validity steps consume the route-B bridges `maxEigen_lt_of_regular_zIall`/`_zInd` +
     `ZDerivation_zsubst_zIall_premise`/`_zInd_premise1`.
3. **Embedding ⟹ `ZRegular`** of the contradiction derivation (eigenvariables chosen fresh/increasing).
4. Wire `Crux2Blueprint` → `false_of_ZDerivesEmpty` → headline.

## STOP
- Do NOT bake freshness into `zIallWff`/`zIndWff` (reshapes the fixpoint; large blast radius — lap-93
  rejected this for the additive `zReg`).
- Do NOT attempt a standalone `red_preserves_ZRegular` for the `zK` case (needs `zKValidF` — see finding).

## Build / verify
`lake build GoodsteinPA` 🟢 1325. New lemmas `#print axioms` = `[propext, Classical.choice, Quot.sound]`
(verified via `lake env lean` scratch this lap). `redSound`/`goodstein_implies_consistency` still the
lone open math `sorry`.
