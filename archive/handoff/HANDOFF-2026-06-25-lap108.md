# HANDOFF — lap 108 · review reorientation (prototypes are a dead end) + the K-case CRITICAL-branch `red` descent banked

> **RESUME BATON.** Branch `plan`, HEAD `b764472`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`,
> **1326**). Headline footprint intact, re-verified in-kernel (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms; lone
> `sorryAx` = crux-2); `peano_not_proves_consistency` / `not_proves_of_implies_consistency` axiom-clean. The
> whole reduction wrapper is clean — only crux-2 stands between us and the axiom-free headline. 3 commits this
> session (`3e60132` review, `7ca88af` narrowing, `b764472` the new brick).

## What landed this session (laps 107–108)

### Review / reorientation (lap 107, `3e60132`) — two kernel-verified findings
1. **`ZInf.allInv` is VACUOUS** — provable by ONE weakening (`ZInf.weaken_top d.seq d`), ignoring `ht` + the
   `^∀φ` membership (verified by elaborating the one-liner in place of the lap-106 40-line `induction`;
   `wip/PathCInf.lean`, now `ZInf.allInv_vacuous`). `ZInf : V → Prop` has no ordinal index and doesn't erase
   `^∀φ`, so it can't carry the META `Zinfty.allInvAux`'s content (ordinal-preservation + erasure). The
   planned `permCongr` perf fix would polish nothing. **STOP it.**
2. **External inductives are NON-LOAD-BEARING** — `ZInf`/`ZcOK`/`ZcDer` are all external Lean inductives
   (PathCOmega.lean:701 admits a deferred-arithmetization PROTOTYPE). The headline `IΣ₁ ⊢ Con(PA)` needs the
   descent in EVERY `V ⊧ IΣ₁` incl. non-standard models whose coded ⊥-proof is non-standard (no external
   well-founded tree exists) ⟹ `foundation_bot_to_Z_empty` is unprovable on them. The load-bearing carrier
   is the Σ₁ CODE engine `red`/`iord` (`InternalZ.lean`).

### Concrete advance (lap 108, `7ca88af` + `b764472`)
- **`iord_descent_red` (`Crux2Blueprint.lean`) Ind branch PROVEN in place** (banked `iord_descent_red_zInd`),
  residual `sorry` isolated to exactly the **K/cut case** (tag 4).
- **NEW `src/GoodsteinPA/RedZKDescent.lean`** (sorry-free, axiom-clean `[propext, choice, Quot.sound]`,
  green-gated): **`iord_descent_red_zK_crit`** — the K-case CRITICAL-branch ordinal descent for the GENUINE
  reduct `red`: `icmp (iord (red (zK s r ds))) (iord (zK s r ds)) = 0` for a critical (`¬ permIdx < lh ds`)
  valid `K^r` chain. Ports the banked `iord_descent_iR2_zK_of_valid` (`iR2`-ρ) to the `red`-ρ via two new
  bundle lemmas (`iRedDescent_zAxReduct_red_of_tp_isymR/_isymLk`) + `iord_iRcritG_eq_iRcrit`. Key content: the
  I∀ eigensubst `red = zsubst d0 a 0` preserves `iotil`/`idg` (`iotil_zsubst`/`idg_zsubst`), so the descent
  bundle transfers, costing only the regularity `maxEigen d0 < a` (the `hreg` hypothesis).

### Correction folded in
The lap-107 baton conflated two distinct `red`s: the lap-104 STALL is the `wip/PathCOmega` PROTOTYPE `red`
(tags 7-10, non-load-bearing), NOT the headline-wired `src/InternalZ` finitary engine `red` (tags 0-6). The
finitary engine's blockers are the K-case branch descents + the `redZKReady` "spine" motive + `axNeg`.

## NEXT (hardest-first) — finish `iord_descent_red`'s K-case on the finitary engine
The K-case dispatches three Buchholz Def-3.2 case-5 branches (`red_zK_crit`/`_splice`/`_rep`). Now:
1. **Non-critical SPLICE/REPLACE branch descents** — the genuine open ordinal core. `red (zK) = iRKs`/`iRKr`
   (`red_zK_splice`/`red_zK_rep[_nonchain]`, `Zsubst.lean:1430/1413/1453`); prove their `iord` descents
   (mirror `RedZKDescent`'s critical assembly; the splice halves are two `iRcritG`-like reducts, the replace
   updates one premise via `tpReduce`). The regularity bundle lemmas in `RedZKDescent` are reusable.
2. **`zKValid` from the ∅→⊥ orbit** — even the critical brick can't fire from `ZDerivesEmptyR` alone: the bare
   `ZDerivation` `zK` disjunct does NOT carry `zKValid` (`InternalZ.lean:7517`). Need the "wire `zKValid` into
   `ZPhi`" phase (`InternalZ.lean:7519`), OR derive the needed validity from the orbit's `ZRegular` + the
   chain structure. THIS is the gating prerequisite for assembling the K-case from the branch descents.
3. Assemble `iord_descent_red`'s K-case from (1)+(2)+`iord_descent_red_zK_crit`, closing the K `sorry`.
4. Then `false_of_ZDerivesEmpty` (`Crux2Blueprint.lean:600`) via `iord_red_iterate_descends` + PRWO; the
   embedding `foundation_bot_to_Z_empty` (`587`); the validity leaves (`78/95/196/369`); the `redSoundGen`
   `redZKReady` spine motive (`455`, lap-101 flagged shaky — settle it in-kernel, prove or refute). Then wire
   crux-1 ∘ crux-2 → `Reduction.goodstein_implies_consistency` → headline. ONLY when `#print axioms` clean.

**Escalation note:** if the `redZKReady` spine motive proves genuinely broken (settle it like lap-104 settled
the prototype stall), the finitary engine is dead and the route becomes the Σ₁-Fixpoint ARITHMETIZATION of
the ω-rule cut-elim (the node shapes `zAllOmega s d0 a α` already code the ω-family via `zsubst d0 a t`; the
deferred work is making the validity predicate a `PR.Blueprint`/`Construction` Fixpoint, not an external
inductive). The math is doubly-proven (Bryce-Goré Coq + the repo's axiom-clean META `Zinfty.lean`).

## Aristotle
Idle — CORRECT. The K-branch descents + `zKValid`-from-orbit are entangled with the `InternalZ` HFS engine
API, not self-contained mathlib lemmas.

## Kernel state
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms.
- `iord_descent_red_zK_crit` / the two `red` bundle lemmas = `[propext, Classical.choice, Quot.sound]`.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` = `[propext, Classical.choice, Quot.sound]`.
