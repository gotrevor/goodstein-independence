# HANDOFF — lap 74 · DEEP REFLECTION + rung-1 well-formedness gap (B) CLOSED + `fvSubst_substs1`

> Supersedes prior HANDOFF-*.md. Read: `REFLECTION-2026-06-24-lap74.md` (lap's primary deliverable) →
> `PENDING_WORK.md` (top: lap-74 grind box, then lap-73 plan) → `STATUS.md`.
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = **1323 jobs**. Tree clean. HEAD `cb102fb`.
> (3 commits this lap; HEAD started at `ab772d0`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**. Sole math
wall = crux 2, localized to **`RedSound`** (`InternalZ.lean`): genuine internalized cut-elimination via
the Option-A validity-preserving reduct. Ladder: rung 0.5 ✅ → **rung 1 `zsubst`** (DEFINED lap 72,
`fstIdx_zsubst` lap 73, **CORRECTNESS in progress lap 74**) → rung 2 (Ind reduct) → rung 3 (K/cut reduct)
→ rung 4 (`RedSound` tag-dispatch).

## ✅ THIS LAP
1. **DEEP REFLECTION (`de94b14`)** — direction KEEP, re-validated from altitude. **PROVED crux 2 is
   unavoidable** (the banked free-X monument is the wrong shape; the specific-instance route still needs
   Gentzen — no ε₀-strength-free proof exists). Flagged the lap-70 reduct re-work (stop extending
   `iR2`/`iCritReduct`; `iord`/ω-tower assignment reusable). Named `PA_delta1Definable` the second front.
   Refreshed STATUS (header/Where-it-stands/ledger/lap-74 box; trimmed What's-happened ~12).
2. **Rung-1 well-formedness gap (B) CLOSED (`e243e02`)** — the lap-73 blocker. Strengthened all three
   I-rule premise-sequent predicates with principal-formula formula-hood (lap-71 cascade recipe; the
   `isUFormula`/`isSemiformula` σ/π splice auto-discharges under `val_sigma`, no extra `.iff`):
   `zInegWff p d0` += `IsUFormula ℒₒᵣ p`; `zIallWff s a p d0` += `IsSemiformula ℒₒᵣ 1 p`; `zIndWff d`
   += `IsSemiformula ℒₒᵣ 1 (zIndP d)`. Inversions surface it for free; no construction site ⟹ zero
   ZPhi-cascade churn.
3. **General `fvSubst_substs1` (`cb102fb`)** — `fvSubst a t (substs1 v p) = substs1 (termFvSubst a t v)
   (fvSubst a t p)` for closed `v` (`IsSemiterm 0 v`), `IsSemiformula 1 p`. The Ind-case succedent transfer.

## ▶ NEXT — finish `ZDerivation_zsubst` (rung-1 step 2). Plan in PENDING lap-74 box. Order:
1. **Term helpers** (Zsubst.lean, ℒₒᵣ): `termFvSubst_numeral a t x = numeral x` (mirror Foundation
   `numeral_bShift`, `ISigma1.sigma1_succ_induction` + `qqFunc_absolute`/`qqFuncN_eq_qqFunc`);
   `termFvSubst_qqAdd` (via `termFvSubst_func` + `LOR_func_addIndex`); closedness `IsSemiterm 0 (^&e ^+ numeral 1)`.
2. **Add `IsSemiterm ℒₒᵣ 0 (zIndTerm d)` to `zIndWff`** (cascade — unary, only re-proves its Def; needed
   for the Ind conclusion transfer `fvSubst_substs1` with `v = zIndTerm d`).
3. **State & prove** `ZDerivation_zsubst {a t} (ht : IsSemiterm ℒₒᵣ 0 t) : ∀ d, ZDerivation d → d ≤ a →
   ZDerivation (zsubst d a t)`. ⚠ Motive must FIX `a,t` (so `P d := d ≤ a → ZDerivation (zsubst d a t)`
   stays Δ₁; the naive ∀a∀t motive is Π₁ and fails `zDerivation_induction`). Drive by `zDerivation_induction`,
   7-tag `rcases` (mirror `isNF_iotil_of_ZDerivation` at `InternalZ.lean:3792`), build each via
   `zDerivation_iff.mpr`. Per-case recipe (worked out in PENDING):
   - atom: `inAnt_fvSubstSeq` + `seqSucc/seqAnt_fvSubstSeqt`.
   - zIall: IH(d0) [`d0<zIall≤a`] + `seqSucc`/`fvSubst_all` (needs `IsUFormula p` = `hwff.2.2.isUFormula`)
     + `fvSubst_substs1_fvar` (`e≠a` from `a_lt_zIall`+`hda`) + `fvSubst_isSemiformula`.
   - zIneg: IH + `fvSubst_inegF` (`ht.isUTerm`,`hwff.2.2`) + `fvSubst_falsum` + `inAnt_fvSubstSeq` + `IsUFormula.fvSubst`.
   - zInd: 2×IH + `fvSubst_substs1` ×3 (numeral 0 / Sa / `zIndTerm`) using the step-1 term helpers + the new closedness conjunct.
   - zAxAll/zAxNeg: `IsUFormula.fvSubst` + `inAnt_fvSubstSeq`.
   - **zK (the hard one, do last):** per-premise IH via `znth_zsubstTable_eq_zsubst`; then `zKValid` transfer
     under subst — needs `tp`/`iperm`/`isChainInf` subst-invariance (CHECK; likely `tp_fvSubst`/`iperm` lemmas).
   Then step 3 `iotil_zsubst = iotil`, then **rung 2** (genuine Ind reduct).

## Parallel / second front
- `PA_delta1Definable` (Foundation `Incompleteness/Examples.lean:17`, still an `axiom`) — mandatory for axiom-free.

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound` or fake the reduct.
