# Handoff: lap 16 — C₂ structural embedding complete; one renaming lemma + hax discharge remain

**Date**: 2026-06-22 20:18 UTC · **Branch**: `plan` · **HEAD**: `4bf9576`

> **Read `HANDOFF.md` (the live baton, refreshed this lap) for the full technical state + the
> critical-path table + the lap-17 step list.** This dated doc is the archive + the mentor context
> that isn't in HANDOFF.md.

## 🎯 What we're doing
Headline: `Statement.peano_not_proves_goodstein` (Kirby–Paris, still an honest `sorry`, untouched).
Route: Buchholz §5 boundedness. The remaining girder is **C₂** (embed `𝗣𝗔(LX) ⊢ TI_≺(X)` into the
`XFreeAx` `Z∞` carrier `PXFc`), then **D** fires (`orderType_le_of_TIprovable`, already proven) ⟹
Thm 5.6, then **E** (Goodstein⟹TI) + **F** (arithmetization seam, `‖≺‖=ε₀`) ⟹ headline.

## 🧠 Context to carry forward (the bootstrap payload)
- **The lap-15 HANDOFF mis-scoped C₂'s `exs` case as "mechanical". It is not.** Faithfully embedding
  Foundation's `Derivation2.exs` (arbitrary-term ∃-intro) over **X-atom bodies** needs Buchholz's
  *value-congruent X-pair axiom* `{Xs, ¬Xt}` (sᴺ=tᴺ, `AX(Z∞)`, lecture notes p.27) — our same-atom
  `Deriv.axL` is insufficient, and `axTrue` on an X-literal breaks `XFreeAx`. **I verified this by
  reading the Buchholz PDF (pp.26–30), not by reasoning from memory.** Methodological lesson: ground
  each "remaining case" in `papers/` before trusting a prior lap's label.
- **The fix that worked:** generalise the literal axiom to value-congruent pairs (`Deriv.axLv (r) (v v')
  (hval : ∀i, valm(v i)=valm(v' i)) …`, generic over `L`, sound). Buchholz's Boundedness *case 1.2*
  (p.29) and cut-elim's *literal-cut* (Remark p.27) already handle value-congruent pairs — our
  same-atom versions are the special case. I threaded `axLv` through all three files and it's green.
- **`provable_em_cong_gen_x` is CLEANER than the ℒₒᵣ original** (`Embedding.provable_em_cong_gen`):
  every atomic case closes uniformly via `axLv` (no `LitTrue` split, no `axTrue`), so it's
  `XFreeAx`-safe for X-atoms automatically. That's what discharged `exs`.
- **Why `embedC_LX_gen` is clean despite the `atomCut_x` sorry:** the sorry lives in `PXFc.cutElim`
  (C₁). `embedC_LX_gen` builds a `PXFc` at some finite cut rank and never calls `cutElim` — D applies
  cutElim *after* embedding. So `embedC_LX`/`embedC_LX_gen` are axiom-clean now; only the *D-end* needs
  the sorry cleared.
- **Two `nrel_value_subst` attempts, both reverted (kept green checkpoint each time).** The lemma's
  structure = `removeFalseLitAux_x` with frame `Γ.erase Lit → insert Lit' (Γ.erase Lit)`. Compound
  cases + `axTrue`/`verumR`/`weak` leaves compiled. **The wall is the matched `axLv` leaf**: leaf
  `{rel r va, nrel r vb}` with `nrel rr vv = nrel r vb`; to pair the surviving `rel r va` with
  `nrel rr ww` via `axLv` you need `r = rr` *as `Rel` elements* (after `kk=k`). `congrArg (∼·)` only
  gives the *formula* eq `rel rr vv = rel r vb`; the Rel-element eq needs `injection`/`subst` with HEq
  on the arg vector. That dependent dance is the only thing left and it's why I deferred it — it's a
  contained, known-shape fresh-lap task, not a design problem.

## ✅ State (all committed, `lake build GoodsteinPA` green = 1266 jobs, verified this session)
- **`embedC_LX_gen` — C₂ structural embedding, COMPLETE + axiom-clean** (`#print axioms =
  [propext, Classical.choice, Quot.sound]`, verified via scratch file). All 10 `Derivation2` cases,
  `axm` abstracted as `hax`.
- `provable_true_x`, `provable_em_cong_gen_x`, `PXFc.exI_closed`, `XFreeForm` — all axiom-clean (`src/GoodsteinPA/EmbeddingX.lean`).
- `Deriv.axLv` value-congruent literal axiom retrofitted across `ZinftyGen` (incl. hard `atomCutAux` +
  3-case `removeFalseLitAux`), `Boundedness` (case 1.2), `XFreeCutElim` (7/8 `_x` sites).
- `paLX := lMap(𝗣𝗔⁻) + InductionScheme LX univ` defined; `embedC_LX = embedC_LX_gen` at `↑paLX` stated
  (resolves "what is `Z ⊢ TI(X)`").
- **Three `src/` sorries:** `Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A,
  off-path), `XFreeCutElim.lean:1048` (`atomCut_x` value-cong X-atom-cut — the one new, disclosed).
  C₁/D temporarily carry the last one.

## 🎬 Next actions
1. **`PXFc.nrel_value_subst`** (clears `atomCut_x` → restores C₁/D to clean). Reconstruct from the
   `removeFalseLitAux_x` template; the only hard part is the matched `axLv` leaf (`r=rr` extraction —
   see Context). Then wire into `atomCutAux`'s `axLv`/`hrel` case (transport `hNC`, no truth split).
2. **Discharge `hax` for `paLX`** (independent of #1; fires `embedC_LX`): X-free axioms (`lMap 𝗣𝗔⁻`
   image + X-free induction) via `provable_true_x` — needs `XFreeForm (lMap (ORing.embedding LX) ψ)`
   (rel case: `(ORing.embedding LX).rel r ∈ {Sum.inl Eq.eq, Sum.inl LT.lt}`, `isLeft = true`) and
   `LitTrue (asgX e ▹ lMap Φ ↑σ)` from `ℕ ⊧ₘ σ` (Foundation `eval_lMap`); X-induction instances via
   `metaInduction` (the Foundation-DSL glue: build `step` from `ψ` by successor subst, prove `hstep`).
3. After both: **D fires** ⟹ Thm 5.6 (modulo F). Then attack **F** (the 2nd real wall) + **E**.

## ⚠️ Gotchas
- `Semiterm.valm ℕ ![] id (nm n)` with a *bare* `nm n` → "typeclass instance problem is stuck" (the
  language metavar isn't pinned). Pin it: `(nm n : Semiterm LX ℕ 0)`. (Fixed in `valm_nm`.)
- A new `Deriv` constructor (or generalising one) makes *every* `match`/`induction` on `Deriv`
  non-exhaustive across ZinftyGen/Boundedness/XFreeCutElim — no intermediate green until all branches
  land. The leaf branches are mostly mechanical (mirror `axL` with `v'` on the negative); budget for it.
- `ANALYSIS-2026-06-22-lap16-exs-axLv.md` has the full retrofit recon + which sites are mechanical.

## 📁 Key files
- `src/GoodsteinPA/EmbeddingX.lean` — C₂ (new this lap): `embedC_LX_gen`, `provable_true_x`,
  `provable_em_cong_gen_x`, `PXFc.exI_closed`, `paLX`, `embedC_LX`.
- `src/GoodsteinPA/XFreeCutElim.lean` — C₁; `atomCut_x` sorry at ~line 1048; `axLv` builder.
- `src/GoodsteinPA/Boundedness.lean` — Thm 5.4 + corollary B; `XFreeAx`, case 1.2.
- `HANDOFF.md` — live baton (full detail). `ANALYSIS-2026-06-22-lap16-exs-axLv.md` — retrofit recon.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Read
`HANDOFF.md` for full detail, then start at Next action #1: reconstruct `PXFc.nrel_value_subst` (the
matched-`axLv`-leaf `r=rr` extraction is the only hard part), OR pick up the independent `hax`
discharge (#2). Both are green-checkpointable.**
