# HANDOFF — lap 85 · crux-2 M1: R1 DISCHARGED + genuine reduct `red` DEFINED + descent bridge

> Supersedes prior HANDOFF-*.md. Read order: this → `PENDING_WORK.md` (lap-85 box at top) →
> `Crux2Blueprint.lean` (leaves) → `E-CRUX2-ROADMAP-2026-06-24.md` (M1a/M1b/M2/M3).
> Branch `plan`. HEAD `63f8288`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Tree clean.

## 🎯 OBJECTIVE (operator, binding)
Discharge `peano_not_proves_goodstein` (`Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`). SOLE blocker = **crux-2**: the 3 src sorries
(`Statement.lean:22`, `Reduction.lean:66`, `DescentSemantic.lean:550`) + the blueprint leaves, all
reducing to the cut-elimination nut **`redSound`** (M1b).

## ✅ THIS LAP (7 green commits, axiom base untouched) — the M1 wall materially advanced
The keystone re-point landed and the genuine reduct now **exists** (was the multi-lap blocker).

- **R1 — the `ZPhi` re-point (DISCHARGED).** `ZPhi`'s `zK` disjunct now carries `zKValidF` (Buchholz's
  faithful, criticality-free `K^r` validity, §3 clause 5) instead of `zKValid = zKValidF ∧ zKCritical`.
  Criticality was never a derivation property — only the *reduction* uses it. New theorem
  `zDerivation_zK_intro` (was the `hZPhiK` hypothesis); `zKValidF_of_ZDerivation_zK` replaces
  `zKValid_of_ZDerivation_zK`. The dead iR2-orbit descent (`iord_descent_iR2_struct`/`_of_emptyAnt`/
  `_of_ZDerivesEmpty`/`iord_iR2_iterate_descends`) now takes an explicit `zKCritical` hypothesis (honest;
  iR2 is the dead reduct). `Zsubst.lean` zK-case drops the criticality conjunct. (commit `eedad5e`)
- **M1a — `red` DEFINED (DONE).** Genuine validity-faithful reduct (Buchholz §6 / Def 3.2), replaces iR2:
  - `iRcritG d ρ` = genuine closed critical branch on CORRECT reduced endsequents `Θ→A(d)`/`A(d),Θ→D`
    (`A(d)=chainAsucc (zKseq d) (redexI d)`), via `iCritReductG`; `ZDerivation_iRcritG_of` (R1 done, only
    R2 left). (commit `b565740`)
  - `iRNextG` 5-case tag dispatch (I/Ind = iR2's reducts; K-case = `iRcritG`), `redTable` PR-recursion,
    `red := znth (redTable d) d`, `𝚺₁`-definable (`redDef`). Per-rule eqs `red_zAtom/zIall/zIneg/zInd/
    zAxAll/zAxNeg/red_zK`. `fstIdx_red_of_tag_Ind_or_K`. Genuine endsequent ops `seqSetSuccDef`/
    `seqAddAntDef`. Blueprint `red`/`fstIdx_red` wired to the reals (blueprint sorries 6→4). (commit `bab0922`)
- **M1b descent side (started).** `iord_iRcritG_eq_iRcrit` (via `iotil_zK`/`idg_zK` being independent of
  the conclusion sequent) — red's K-case carries the SAME `iord` as iR2's, so the banked descent transfers.
  (commit `dc991bf`). `red_eq_iR2_of_tag_ne_four` — red = iR2 off tag 4, isolating the entire difference to
  conclusion sequents. (commit `63f8288`)

## ▶ NEXT (priority order — M1b `redSound` is the nut)
1. **`iord (red x) = iord (iR2 x)` UNCONDITIONAL** (strong induction on `x`). tag≠4: `red_eq_iR2_of_tag_ne_four`.
   tag 4 (`x=zK s r ds`): `iord(red x)=iord(iRcrit x ρ_red)` (`red_zK`+bridge), `iord(iR2 x)=iord(iRcrit x ρ_iR2)`
   (`iR2_zK_eq_iRcrit`); `iCritReduct` iotil/idg depend on `ρ` only through `iotil/idg` of `ρ(redexI/redexJ)`;
   for redex premise `p`: if `zTag p≠4` then `red p=iR2 p` so `ρ_red=ρ_iR2`; if `zTag p=4` then `zAxReduct=id`
   (reduct tag 4) and `iotil(red p)=iotil(iR2 p)` by IH (`p<x`). Needs: `iCritReduct` iotil/idg congruence in
   `(v,w)` through `iseqNaddIdgAux_congr_iotil` (3150) + `znth_seqUpdate` read-outs. ⟹ `iord_descent_red`
   reduces to the criticality question (below).
2. **Criticality of ⊥-chains** — does a `ZDerivesEmpty` K-chain always have a critical redex (positive rank ⟹
   L3.1 `inference_critical_pair`)? If YES: critical-only `red` suffices, `iord_descent_red` closes and the
   non-critical 5.2-splice gap is moot. If NO: `red`'s tag-4 must branch on `zKCritical` (add the 5.2 splice
   producer — banked `iord_descent_iSpliceEnd`/`zKValidF_seqUpdate_iR2`). **Decide this first — it gates whether
   `red` is complete.**
3. **R2 (auxiliary IH)** — discharge `haux0`/`haux1` of `ZDerivation_iRcritG_of`: the two auxiliaries are
   `ZDerivation`s. (a) premises: unchanged from `d`'s ZDerivation; replaced one `ρ idx = zAxReduct (red (znth ds idx))`
   by `redSound` IH + `ZDerivation_zAxReduct`. (b) aux chain `zKValidF` via banked `zKValidF_iCritReductSeq`/
   `isChainInf_iCritReductSeq` threading (Thm 3.4(a)).
4. ⚠ **`zAx1` is TAG 7, NOT a `ZPhi` rule** (tags 0–6). The redex j-premise is an L-AXIOM (`tp = isymLk` ⟹
   tag 5/6 = `zAxAll`/`zAxNeg`), so `ρ(redexJ d) = zAxReduct(red(axiom)) = zAx1` (tag 7) — currently NOT a
   ZDerivation. **Before R2:** add a tag-7 disjunct to `ZPhi` for `zAx1` (Buchholz's `Ax^1` logical axiom,
   docstring at def `zAx1` line ~645) with the faithful side condition, then re-bless the Fixpoint
   (`zphi_monotone`/`zphi_strong_finite`/`zphi_iff`/`zblueprint` sigma+pi/`zPhi_definable`) and extend the
   ~15 `zDerivation_iff` rcases sites (7→8 branches each). Blast radius is large but mechanical. ⚠ ALSO note:
   the genuine I∀ auxiliary may need the eigenvariable-SUBSTITUTED premise `d_i[k]` (`Zsubst` machinery), not
   `zAxReduct∘red` — VERIFY the lap-84 `iCritReductG`/`A(d)=chainAsucc` interface is self-consistent with
   `ρ=zAxReduct∘red` before grinding R2 (it gives the right ORDINAL; check it gives the right ENDSEQUENT).
5. **`redSound`** = `zDerivation_induction` over `d` (ZDerivesEmpty ⟹ tag 3/4 via `zTag_Ind_or_K_of_ZDerivesEmpty`):
   tag 3 (Ind) needs the Ind-reduct `zKValidF` (deep residual, parallels old `ZDerivation_iR2_zInd_of_zKValid`
   but `zKValidF` now); tag 4 = `ZDerivation_iRcritG_of` + R2.

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. src/ sorries: `Statement.lean:22`, `Reduction.lean:66`,
`DescentSemantic.lean:550` + 4 blueprint leaves (`redSound`, `iord_descent_red`, `foundation_bot_to_Z_empty`,
`false_of_ZDerivesEmpty`). `#print axioms` clean on all lap-85 lemmas (`red`/`red_zK`/`iord_iRcritG_eq_iRcrit`/
`zDerivation_zK_intro`/`ZDerivation_iRcritG_of`/`fstIdx_red_of_tag_Ind_or_K`).

Aristotle: idle (Foundation not in its mathlib-v4.28 env; all M1 work is Foundation-internal).

## Commits this lap (branch `plan`, 891bbf8..63f8288)
re-point ZPhi→zKValidF (R1) · iRcritG + ZDerivation_iRcritG_of · seq-op definability + red DEFINED + recursion
eqs + fstIdx_red · ordinal bridge iord_iRcritG_eq_iRcrit · red_eq_iR2_of_tag_ne_four · (+ PENDING docs).
