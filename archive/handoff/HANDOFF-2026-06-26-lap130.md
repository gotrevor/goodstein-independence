# HANDOFF — lap 130 · re-orient the endgame onto the lap-124 `iRcrit` dichotomy; pin & start the per-node fix

> **RESUME BATON.** Branch `plan`, HEAD = `40bf387` (lap-130 final), tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint UNCHANGED & still locked:
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms). All 7 new
> lemmas/predicates this lap are axiom-clean `[propext, choice, Quot.sound]`; no `sorry` touched (11
> disclosed, unchanged).

## What this lap DID — walked the crux end-to-end, then started the genuine fix

Seven green commits. The through-line: the lap-129 "re-key `permIdx ↦ majorIdx`" plan was shown
INCOMPLETE, the campaign re-oriented onto the **already-banked lap-124 `iRcrit` dichotomy**, and the
real remaining obstruction (per-node exact-shape facts) was pinned and its fix started.

### 1. The lap-129 majorIdx-replace plan is INCOMPLETE (commits `1543589`, `e01daa6`)
`majorIdx_botOrbit_reducible` excludes only tag-0/7 leaves from the major-premise slot, but **tags 5/6
(`zAxAll`/`zAxNeg`, L-axioms) are ALSO `red`-FIXPOINTS** (`red_zAxAll = id`, `red_zAxNeg = id`,
`InternalZ:7374,7377`) and are NOT excluded — their inversions put the *active* L-formula (`^∀p`/`inegF p`),
not the succedent, in the antecedent, so the leaf-rerouting doesn't apply. A naive `replace` into a tag-5/6
major premise stalls too. **Pinned the fix:** `majorPrem_zAxAll_cutPartner` / `majorPrem_zAxNeg_cutPartner`
(the active L-formula threads back to a strictly-earlier R-intro = the principal-cut partner) +
`majorPrem_tag_mem` (⊥-orbit major premise tag ∈ {3,4,5,6}; 1,2 excluded since their succedent is an
R-principal formula ≠ ⊥, via `qqAll_ne_falsum`/`inegF_ne_falsum`).

### 2. RE-ORIENTATION onto the lap-124 dichotomy (commits `c5098c7`, `6db012a`)
**KEY:** lap 124 ALREADY banked `iRcrit_descends_or_nonleaf_isymRep` (`RedZKDescent:194`): for ANY regular
`∅→⊥` chain, **either** `iRcrit (zK s r ds) (zAxReduct∘red)` strictly `iord`-descends (LEFT, stall-tolerant,
NO `permIdx`/`red`-fixpoint dependence), **or** there's a non-leaf `isymRep` premise. The lap-129 "DEAD END"
verdict was only about *dissolving* the RIGHT via reroute — but the RIGHT is RECURSED, not dissolved. New
lemma `isymRep_nonleaf_zInd_or_zK`: the four `isymRep`-carrying constructors are `zAtom`/`zInd`/`zK`/`zAx1`,
so a non-leaf `isymRep` premise is exactly a `zInd` (3) or `zK` (4) — = `majorPrem_tag_mem`'s tag-3/4 cases.
Assembled the tag-explicit entry **`iRcrit_descends_or_zInd_zK_premise`** (`RedZKDescent.lean` end): the
clean dichotomy `false_of_ZDerivesEmpty` should be restructured around. Dispatch:
- **LEFT** (all `isymRep` leaves) → `iRcrit` descends (banked).
- **RIGHT tag 3** (`zInd`) → `red_zInd`/`iord_descent_red_zInd` strict descent (banked).
- **RIGHT tag 4** (`zK`) → recurse into the sub-⊥-chain (the genuine residual).
- Tags 5/6 (`isymLk`) never on the RIGHT — they're the LEFT redex L-side.

### 3. LEFT-soundness plumbing — discharged 2 of 6, PINNED the obstruction, STARTED the fix (commits `bb138b0`, `abfc2f4`, `40bf387`)
The LEFT case needs `iRcrit` to be a `ZDerivation` to iterate. That is `ZDerivation_red_zK_crit`
(`Crux2Blueprint:655`, FALSE as stated under current `ρ`), fixed post-engine-swap by the PROVEN
`ZDerivation_iRKcCrit_of_zKValid` (`:575`) modulo its plumbing `hCwff`/`hSeqs`/`hAll`/`hNeg`/`hthread`/`hrank`.
- ✅ `hCwff` = **`cutFormula_wff_of_zKValid`** (`InternalZ.lean`): `IsUFormula (cutFormula)` from the orbit.
- ✅ `hSeqs` = `seq_empty` on the ⊥-orbit (`seqAnt s = ∅`). Both packaged into
  **`ZDerivation_iRKcCrit_botOrbit`** (`Crux2Blueprint.lean`), residual = `hAll`/`hNeg`/`hthread`/`hrank`.
- ⚠️ **THE OBSTRUCTION (verified):** `hAll`'s `seqSucc sⱼ = cutFormula` (∀-axiom succedent IS `F(k)`) and
  `hNeg`'s `seqAnt (fstIdx d0) = seqCons (seqAnt sᵢ) p` (I¬ premise ant = exactly `Γ,A`) are EXACT-SHAPE
  equalities the current loose `zAxAll`/`zIneg` `ZPhi` disjuncts (only `inAnt`/membership) do NOT supply.
- ✅ **FIX STARTED — building blocks banked:** **`zAxAllSuccWff s p k`** := `seqSucc s = substs1 (numeral
  k) p` and **`zInegAntWff s p d0`** := `seqAnt (fstIdx d0) = seqCons (seqAnt s) p`, each `𝚫₁`-definable
  (`…Def`+`_defined`+`_definable`, `InternalZ.lean` after `zInegWff_definable`). Additive (don't yet touch
  ZPhi); these splice into the disjuncts.

## NEXT (highest-value, turnkey) — the atomic ZPhi strengthening
Splice `zAxAllSuccWff`/`zInegAntWff` into the `zAxAll`/`zIneg` `ZPhi` disjuncts so `hAll`/`hNeg` become
derivable from `zKValid`. Mirrors the lap-118 `zAxNeg` `A∈Γ` strengthening. **Exact site list in
`PENDING_WORK.md` lap-130** (the "TURNKEY ZPhi-STRENGTHENING PLAN", 6 steps):
1. `ZPhi` (`InternalZ:5310/5307`) — add the conjunct to each disjunct.
2. `zblueprint` Σ/Π (`:5415/5435`, `:5407/5427`) — append `!(zAxAllSuccWffDef.sigma/pi) …` /
   `!(zInegAntWffDef.sigma/pi) …`; add `…_defined.iff` to the `zPhi_definable` simp (`:5446`).
3. `zphi_iff`/`zDerivation_iff` both dirs (`:5319-5392`) + `zphi_monotone` (`:5317`) + Construction-field
   destructures (`:5504/5529`) — mechanical pattern `_`/`hnew` additions.
4. `zDerivation_zAxAll_inv`/`zDerivation_zIneg_inv` — return the new conjunct.
5. Construction sites (`ZDerivation_red` zAxAll/zIneg, `Crux2Blueprint:1089/1092`) — SELF-HEALING (they
   reconstruct from the same disjunct they destructured via the inversion).
6. ⟹ derive `hAll`/`hNeg` from `redZKReady_of_zKValid` + the strengthened inversions ⟹ a hypothesis-free
   `ZDerivation_iRKcCrit_botOrbit'`. Then the LEFT soundness is real; the tag-4 RIGHT recursion +
   `false_of_ZDerivesEmpty` PRWO wiring remain.

⚠️ **RISK:** this is a ~10-site atomic change (the build goes red mid-way). The riskiest piece is the
`zPhi_definable` simp at `:5446` — the two building-block predicates already proved their own `_defined`
green on the first try, so `…_defined.iff` should slot in. Budget a full lap; do NOT commit non-green.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH (`:1167`) ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open work is engine-internal ZPhi surgery + the tag-4 recursion, not a portable lemma.

## Pointers
`STATUS.md` · `PENDING_WORK.md` lap-130 (top, full plan) · dichotomy entry: `RedZKDescent.lean`
(`iRcrit_descends_or_zInd_zK_premise`, `iRcrit_descends_or_nonleaf_isymRep:194`) · cut-partners +
`majorPrem_tag_mem` + `isymRep_nonleaf_zInd_or_zK` + the two `…Wff` building blocks: `InternalZ.lean` ·
soundness plumbing: `Crux2Blueprint.lean` (`ZDerivation_iRKcCrit_botOrbit`, `ZDerivation_iRKcCrit_of_zKValid:575`)
· `cutFormula_wff_of_zKValid` (`InternalZ.lean` after `redZKReady_of_zKValid:8268`).
