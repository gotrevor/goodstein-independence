# HANDOFF — lap 79 · `PA_delta1Definable` front A · internal scheme-recognizer machinery built, crux bridge banked

> Supersedes prior HANDOFF-*.md. Read order: this file → `PENDING_WORK.md` (lap-79 top box) →
> `src/GoodsteinPA/PADelta1.lean` (bricks 2a–2d). Branch `plan`. Build 🟢 green
> `lake build GoodsteinPA` = **1196 jobs** for `PADelta1`. Tree clean. HEAD `036fc46`.
> (9 commits this lap: 6 feat bricks + 3 docs.)

## 🎯 OBJECTIVE (operator, unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**
(trust base `[propext, Classical.choice, Quot.sound]` only). TWO headline blockers:
(1) crux 2 = the `RedSound` internalized cut-elimination — **deep-reflection-blocked**, see
lap-78 box in `PENDING_WORK.md` + `ANALYSIS-2026-06-24-lap78-criticality-substitution-wall.md`;
(2) **`PA_delta1Definable`** = Foundation's upstream `axiom 𝗣𝗔.Δ₁` that Gödel II for `𝗣𝗔` rides.
This lap drove front (2).

## ✅ THIS LAP — internal induction-scheme recognizer machinery (all axiom-clean)
Goal: build `inductionSchemeUnivDelta1 : (InductionScheme ℒₒᵣ Set.univ).Δ₁` (the lone `sorry` in
`PADelta1.lean`, line ~497) → assemble `paDelta1 : 𝗣𝗔.Δ₁` → rewire `Reduction.lean`, dropping
`PA_delta1Definable` from the headline. `𝗣𝗔 = 𝗣𝗔⁻ + InductionScheme ℒₒᵣ Set.univ`; `paMinusDelta1`
(finite) was already axiom-clean (lap 78). The scheme half needs an internal Δ₁ recognizer for codes
of `univCl (succInd φ)`. Bricks landed:

- **2a `qqAllItr p k = ^∀^[k] p`** (PR.Construction) — `𝚺₁-Function₂`; `qqAllItr_succ'` (front-peel) +
  **`qqAllItr_quote`** (`qqAllItr ⌜φ⌝ n = ⌜∀⁰* φ⌝`).
- **2b `freeToBound`** — the forward `Rew.fixitr 0 m` arithmetization: term-level `termFreeToBound d t`
  (`^&x↦^#(x+d)`, `TermRec`) + formula-level `freeToBound d p` (`UformulaRec1`, depth-threaded, full
  simp set). **BANKED but UNUSED by the recognizer** (the pivot below uses backward `subst` instead).
- **2c `fvarSeq m = ⟨^&0,…,^&(m-1)⟩`** (PR, `qVec` pattern w/ `^&0` head + `termShiftVec`) —
  `𝚺₁-Function₁`; `nth_fvarSeq` (`.[i]=^&i`), `len_fvarSeq`, `IsSemitermVec_fvarSeq`.
- **2d `subst_fvarSeq_fixitr`** ⭐ **THE crux bridge** — `subst ℒₒᵣ (fvarSeq (0+φ.fvSup)) ⌜fixitr 0 φ.fvSup ▹ φ⌝
  = ⌜φ⌝`. Proof lifts Foundation's `Semiformula.subst_comp_fixitr` through `typed_quote_substs` +
  `Semiformula.val_substs`; helper `fvarSeqVec_val` matches the typed free-var subst vector
  `fun i ↦ ⌜&i⌝` to the raw `fvarSeq`. **This was the hardest piece and is done.**

## ⭐ KEY FINDING — the recognizer pivot (avoids a dependent-arity wall)
The "obvious" route (match `freeToBound ⌜φ⌝ = ⌜fixitr 0 m ▹ φ⌝` by induction) is BLOCKED: it needs
`(Rew.fixitr n m).q = Rew.fixitr (n+1) m`, which is **ill-typed** (codomains `n+m+1` vs `n+1+m`, not
defeq) — Foundation omits it on purpose. So the recognizer goes **BACKWARD**: given `y`, peel `m`
leading `^∀`s to a body, recover the fv-version by `subst (fvarSeq m) body`, and check it is
`succIndCodeRaw` of a 1-ary semiformula. This reuses `subst_comp_fixitr` (existing) and dodges the
wall. `IsFVFree`/`IsSemiformula`-pin forces `m = fvSup` (no internal `fvSup` function needed).

Target recognizer (raw, over V; `ch` evaluates this):
`ch(y) := ∃ p ≤ y, IsSemiformula 1 p ∧ ∃ m ≤ y, ∃ body ≤ y, y = qqAllItr body m ∧ IsFVFree m body ∧`
`(m = 0 ∨ ¬ IsFVFree (m-1) body) ∧ subst ℒₒᵣ (fvarSeq m) body = succIndCodeRaw p`.

## ▶ NEXT — finish `inductionSchemeUnivDelta1` (assembly only; the hard bridge is banked)
Pieces, in order (full detail in `PENDING_WORK.md` lap-79 box):
1. **univCl↔qqAllItr bridge**: `(⌜univCl ψ⌝ : V) = qqAllItr ⌜Rew.fixitr 0 ψ.fvSup ▹ ψ⌝ ψ.fvSup` —
   combine `qqAllItr_quote` with `coe_univCl_eq_univCl'` (`univCl ψ = ∀⁰* (fixitr 0 ψ.fvSup ▹ ψ)` as
   SyntacticFormula) + `Sentence.quote_def`. Mind the `0 + fvSup` arity (keep it; `subst_fvarSeq_fixitr`
   already uses `0 + φ.fvSup`).
2. **build `ch : 𝚫₁.Semisentence 1`** = the bounded-∃ recognizer above (HierarchySymbol semisentence
   over the Σ₁/Δ₁ graphs: `succIndCodeRaw`/`subst`/`qqAllItr`/`IsFVFree`/`IsSemiformula`).
3. **`mem_iff`** at ℕ. (⇐) witness p=⌜ψ⌝, m=fvSup, body=⌜fixitr..⌝; close via (1)+(2)+
   `succIndCodeRaw_quote`+`subst_fvarSeq_fixitr`. (⇒) decode p,m,body; the ONE nontrivial sub-argument:
   from `subst (fvarSeq m) body = ⌜succInd ψ⌝` + `body` fv-free m-ary ⟹ `body = ⌜fixitr 0 m ▹ succInd ψ⌝`
   (injectivity of subst-by-fvarSeq on fv-free m-ary codes / inverse of fixitr).
4. **`isDelta1`** = `ProvablyProperOn.ofProperOn` + properness of the bounded ∃.
5. Rewire `Reduction.lean`: `peano_not_proves_consistency := @consistent_unprovable 𝗣𝗔 paDelta1 _ _`.
   **ANTI-FRAUD**: do this ONLY when `inductionSchemeUnivDelta1` is sorry-free (else it just swaps
   `PA_delta1Definable` for `sorryAx`). Discharge `Statement.lean:22` only when `#print axioms
   peano_not_proves_goodstein` is clean (still needs crux 2 too).

## Front B (crux 2 `RedSound`) — untouched this lap, DEEP-REFLECTION-blocked
Do not resume the rung-1→2 grind until the criticality-design fork is decided (recommend structural
criticality, option 2). See lap-78 box. `ZDerivation_zsubst` (`d≤a` form) stays banked + axiom-clean.

## Build / verify
`lake build GoodsteinPA` green (PADelta1 = 1196 jobs). All lap-79 lemmas `#print axioms`-verified
`[propext, Classical.choice, Quot.sound]`. Lone `sorry` = `inductionSchemeUnivDelta1`.
Aristotle: idle this lap (no clean self-contained OPEN lemma to feed — the remaining pieces are deep
Foundation-internal arithmetization). `WebFetch` untested (worked locally throughout).
