# HANDOFF — lap 120 · DEEP REFLECTION — the genuine open crux is the SELECTION/STALL defect, not the inversion

> **RESUME BATON.** Branch `plan`, HEAD `<this commit>`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`,
> **1326**). Headline footprint re-verified in-kernel (`lake env lean`): `peano_not_proves_goodstein`
> + `goodstein_implies_consistency` = `[propext, sorryAx, Classical.choice, Quot.sound]` — **0 math axioms**;
> `peano_not_proves_consistency` clean. Statement re-audited vs paper — no drift. No proof code changed this
> lap (reflection synthesis only): 11 disclosed sorries unchanged (1 headline + 1 girder + 1 DescentSemantic
> + 8 Crux2Blueprint).

## ⭐⭐⭐ THE FINDING (read FIRST) — `red` STALLS, so the endgame can't close
The cut-elimination engine's endgame `false_of_ZDerivesEmpty {z} (hz : ZDerivesEmptyR z) : False`
(`Crux2Blueprint:1144`, bare sorry) **cannot close as currently architected.** `red` can be a FIXPOINT on
the ⊥-orbit: `permIdx` selects the first premise with `iperm isymRep` (unconditionally true), so an ATOM or
`zAx1` leaf can be selected → `red (zK s r ds) = zK s r ds` (`red_zK_fixpoint_of_atom_selected` /
`_zAx1_selected`, RedZKDescent) → a tag-4 K-node that is **not cut-free**. The repo flags this in
`RedZKDescent.lean`'s own docstring. Lap-111's disjunctive `iord_descent_red` (`red d = d ∨ iord ≺`) did NOT
fix the stall — it RELOCATED it into the unbuilt `false_of_ZDerivesEmpty` (the stall branches close on
`Or.inl`). **Same defect as laps 104/107, still open.** The inversion work (112–119) solved a DIFFERENT
sub-problem — reduct SOUNDNESS — which does not make the orbit terminate at a contradiction.

## 🎯 NEXT-LAP TARGET — (A) the fixpoint-absurdity lemma; the fix is AXIOM-CUT ELIMINATION
**`no_red_fixpoint_of_ZDerivesEmptyR : ZDerivesEmptyR w → red w ≠ w`** (equivalently
`red w = w ∧ ZDerivesEmptyR w → False`).

**⚠️ KERNEL-GROUNDED (lap-120 scouting) — the VACUITY route FAILS; do not pursue it.** I checked the engine:
- On the ⊥-orbit, Cor 2.1 (`tp_selected_isymRep_of_emptyAnt_botSucc`, InternalZ:7593) forces the selected
  premise `tp = isymRep`; atom & `zAx1` satisfy it (axAll/axNeg are already vacuous, `tp=isymLk≠isymRep`).
- Leaf-soundness (`zTag_reducible_of_emptyAnt`/`zTag_Ind_or_K_of_ZDerivesEmpty`, InternalZ:8377/8480) kills
  axiom leaves with EMPTY antecedent. But the SELECTED PREMISE has its own sequent `sᵢ`, and `isChainInf`
  (InternalZ:1177) permits premise antecedents to contain CUT FORMULAS: `∀ B, inAnt B (chainAnt ds i) →
  inAnt B (seqAnt s) ∨ ∃ i'<i, B = chainAsucc ds i'`. With `seqAnt s = ∅`, premise antecedents = the
  `chainAsucc` (cut) formulas. So a selected atom `zAtom sᵢ` can be a **valid axiom `B→B`** (`seqAnt sᵢ={B}`,
  `seqSucc sᵢ=B=chainAsucc`), satisfying atom-validity `inAnt (seqSucc sᵢ) (seqAnt sᵢ)`. **The atom's
  antecedent is NOT forced empty — the stall is genuinely reachable; `false_of_ZDerivesEmpty` is unprovable
  as the engine stands.**

**THE RESOLUTION (faithful Gentzen; = the deferred lap-104/107 "find+reduce the lowest cut").** A selected
axiom premise `B→B` means the K-node's cut on `B` is against an AXIOM, which Gentzen ELIMINATES (the cut
vanishes, leaving the matching `B`-succedent premise). The engine's Rep-reduce is the IDENTITY here
(`tpReduce_isymRep`), so it STALLS. **Fix = extend `red` to reduce the axiom-cut** (selected genuine axiom
`B→B` → splice in the matching `B`-succedent premise, drop the cut) → guaranteed progress. Selection-refinement
alone (skip atoms) is INSUFFICIENT (the B-cut still needs reducing); an M2 atom-free-spine invariant is fragile.
PROBE whether `ZRegular w` constrains it, but expect the axiom-cut reduction to be the real fix.

**Properties:** on the M3 critical path; the SAME defect as laps 104/107, now pinned to the precise mechanism.
This is the deepest unverified assumption in crux-2 — does the internalized cut-elimination actually terminate
to a contradiction?

**⚠️ SHARPENED SUB-TARGET (lap-120, `iperm` scouting — see `PENDING_WORK.md` lap-120 for the full analysis).**
At the ⊥-orbit conclusion `s`, `iperm (tp ·) s` holds **iff `tp = isymRep`** (I-rules need succedent `⊥`,
L-axioms need their formula `∈ ∅` — both impossible). So `permIdx` = first `isymRep` premise. **Key:** if that
premise is an atom at **index 0**, threading forces its antecedent EMPTY ⟹ atom-validity fails ⟹ vacuity
fires. The stall needs the atom first-`isymRep` at **index > 0** (earlier premises all non-permissible
I-rules/L-axioms, atom antecedent = a `chainAsucc` cut formula). **CONCRETE NEXT-LAP TARGET:** prove the
first-`isymRep` premise of a valid ⊥-orbit chain is forced to index 0 (or to a vacuity-firing shape). If
provable → (A) closes by vacuity, NO engine surgery needed; if a real index-`>0` atom-stall is constructible
→ the axiom-cut elimination is forced. Sharp + in-kernel decidable — start by attacking
`red_zK_fixpoint_of_atom_selected`'s reachability, not the abstract claim.

## ⭐⭐ SECOND FINDING (lap-120 cont) — `zKValidF_iIndReduct_of_zInd` is FALSE as stated
Broadened off the stall thread (per `how-to-get-unblocked.md`) to the Ind-case validity sorry
(`Crux2Blueprint:79`). KERNEL-GROUNDED: `iIndReductSeq d0 d1 1 = ⟨d1,d0⟩` (verified, `scratchpad/indtest2.lean`),
`seqSucc d1 = F(a+1)` (free eigenvar `a`), `seqSucc d0 = F(0)`, `seqSucc s = F(t)` (`t` a CLOSED term, from
`zIndWff`). The `isChainInf` exit (`chainAsucc j0 ∈ {F(t), ⊥}`) is UNSATISFIABLE for any `zInd` concluding
`F(t)`, `t ≠ 0` — so the theorem is false. **SAME root cause as lap-114** (ordinal-invariant reduct loses the
instance; the verbatim-repeat reduct never re-instantiates the step at `0..t`). **FIX:** instance-correct Ind
reduct (eigenvar-substituted step chain to `t`, à la lap-114's `zsubst`-re-principalization), then restate +
prove. **⚠️ Audit splice/replace reducts for the same latent defect.** See `PENDING_WORK.md` lap-120 (cont).

## After (A): the rest of the endgame (unchanged priorities)
2. Finish the atomic engine swap (lap-119 recipe in `PENDING_WORK.md` lap-119; `scratchpad/lap119-engine-swap.diff`)
   → `redSound` → the RIGHT (infinite-descent) disjunct.
3. (C) `gentzenDescentφ` as the real Σ₁ graph of `n ↦ iord(red^[n] z)`; assemble `false_of_ZDerivesEmpty`
   from (A) [LEFT] + descent+PRWO [RIGHT]; then M2 `foundation_bot_to_Z_empty` (the Foundation→Z embedding).

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED, anti-fraud) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path free-X) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
M2 `foundation_bot_to_Z_empty` · M3 `false_of_ZDerivesEmpty` (← (A) is its LEFT-disjunct closer).

## Aristotle
Idle — CORRECT. (A) is engine-internal (sequent-shape absurdity), not an isolated portable lemma.

## Pointers
`REFLECTION-2026-06-26-lap120.md` (primary) · `STATUS.md` (refreshed) · `NEXT_STEPS.md` (reordered, lap-120
banner) · `PENDING_WORK.md` lap-120 (the decomposition + attack).
