# HANDOFF — 2026-06-23 (lap 55) — crux-1 collapsed: 3 named hyps → ONE isolated bridge lemma

> **Branch** `plan` · **HEAD `5ee9589`** · tree **clean** · `lake build GoodsteinPA` **green (1315 jobs)** ·
> headline `peano_not_proves_goodstein` = honest `sorry` (`#print axioms` = trust base only, anti-fraud
> intact). **11 commits** `d23a60b..5ee9589`. All new src lemmas axiom-clean `[propext, choice, Quot.sound]`,
> sorry-free. `wip/{StdCor34,GentzenCon}.lean` checked via `lake env lean <file>` (green).

## What this lap did — crux 1 (`goodstein_implies_prwo`) driven from 3 hyps to ONE bridge

At lap start crux 1 = `crux1_internal_run` modulo THREE named hyps (`hdef`/`hdom`/input `β`). This lap
discharged/reduced all three AND wired the model-theoretic route, leaving a single isolated lemma.

### (1) `hdef` — definability — FULLY DISCHARGED (`d23a60b`, `ce74007`)
- `src/GoodsteinPA/InternalIg.lean`: `ig0_definable`, **`ig_definable`** (meta-induction on level,
  proved at `𝚺₁`), `igtTot_definable`.
- `wip/StdCor34.lean`: `bbtail_definable`/`bbeta_definable`/`salpha_definable` → **`hdef_of_beta_definable`**
  (whole `bbeta∘salpha` tower `𝚺₁`-definable from one `𝚺₁-Function₁ β`) → **`crux1_internal_run_of_beta_def`**.

### (2) `hdom` — reduced to WIDTH-domination (`325d4f1`)
- `src/GoodsteinPA/BlkRec.lean`: `off_succ_lt_width_of_blk_eq`.
- `wip/StdCor34.lean`: `hdom_of_width_dom` + **`crux1_internal_run_of_width_dom`** — domination premise is
  now exactly `∀ n, znth wseq n ≤ iF l₀ n` (Rathjen 2014 Lemma 3.2).

### (3) iF growth bricks (Lemma 3.2 substrate) — `src/GoodsteinPA/IIter.lean` (`47b66a1`, `c9ca087`)
- `self_le_iIter`/`self_le_iF` (inflationary `n ≤ iF l n`), `le_iIter_add`, `iF_le_succ_level`,
  `iF_mono_level` (`l≤l' ⟹ iF l n ≤ iF l' n`, n≥1), `iF_one` (`iF 1 n = n+n`).

### (4) Model-theoretic route for `goodstein_implies_prwo` (`533bd31`, `855b2df`, `5ee9589`)
`wip/GentzenCon.lean`: `goodstein_implies_prwo` is **no longer a bare sorry**. It chains through
Foundation's arithmetic completeness `provable_of_models` + soundness `models_of_provable`, reducing to
a per-model obligation `prwoInstance_models_of_goodstein` (`[ORingStructure M] [M ⊧ₘ* 𝗣𝗔]`, `M ⊧ γ ⟹
M ⊧ prwoInstance seq`). That obligation is **fully machine-checked** modulo ONE lemma:
- `prwoInstance_models_iff` — general-model unfolding of `prwoInstance` (analog of ℕ-only `_faithful`).
- γ model-unfolding (`∀m ∃N, igoodstein m N = 0`) via Bridge-style closure eval.
- contradiction: descent ⟹ `nonterminating_of_seq_descent` ⟹ refutes γ at `m₀`.

## NEXT (hardest-first) — THE single remaining crux-1 lemma
**`GoodsteinPA.GentzenCon.nonterminating_of_seq_descent`** (`wip/GentzenCon.lean`, lone `sorry`):
```
(seq) (M) [ORingStructure M] [M ⊧ₘ* 𝗜𝚺₁]
  (hdesc : ∀ n y z : M, (M ⊧/![y,n] seq) → (M ⊧/![z,n+1] seq) → (M ⊧/![z,y] precφ)) :
    ∃ m₀ : M, ∀ k : M, 0 < igoodstein m₀ k
```
Discharge plan: from the everywhere-≺-descending `seq`-graph build the inputs to
`StdCor34.crux1_internal_run_of_width_dom`:
1. **`β : M → M`** — the NF ordinal-code sequence. seq's values are `precφ`/`natCode`-ordered; need the
   **natCode ↔ internal NF-code (icmp/ocOadd) order bridge** so a `precφ`-descent becomes an `icmp`-descent
   of NF codes (`hβNF`/`hβ0`/`hβdesc`). β is `𝚺₁` because seq is a formula (`hβdef`).
2. **`wseq`** + **width-domination** `∀ n, znth wseq n ≤ iF l₀ n` — Rathjen Lemma 3.2. For the headline
   only the concrete `seq = gentzenDescentφ` is needed, whose `C(ord(Rⁿd₀))` IS standard-`l₀` dominated
   (see `[[crux1-headline-needs-only-standard-level]]`); for arbitrary seq this can fail
   (`Grz.F_diag_not_dominated`), so likely thread a domination certificate as a hypothesis, OR specialize.
3. **`hβC`** (`iC(β(blk j)) ≤ Cβ + j`) — from seq's growth bound.
- iF growth bricks (this lap) are the Lemma-3.2 substrate. The natCode↔NF bridge is the new sub-target;
  check `SeamDefinability.precφ` / `Epsilon0Complete.natCode` for the order correspondence.

## Hardest wall (unchanged) — crux 2 eq (5) `ord(R d) ≺ ord d` (🟠 generational, cited)
`wip/GentzenCon.lean` isolates it to the single `ord_R_descends` axiom + `gentzen_prwo_implies_consistency`
sorry. Not the active thread.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/StdCor34.lean`/`wip/GentzenCon.lean` are wip. Aristotle OFFLINE. WebFetch dead.
⚠️ Reminder (cost me ~2 min this lap): NEVER `git checkout <wip file>` to undo uncommitted edits — it
reverts to HEAD and drops them. Use `cp` from a scratchpad backup. (memory `never-git-checkout-uncommitted-edits`)
