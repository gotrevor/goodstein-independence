# HANDOFF — lap 82 (OPERATOR REDIRECT) · crux-2: criticality ≠ validity; descent fully banked

> Supersedes prior HANDOFF-*.md. Read order: this → `ANALYSIS-2026-06-25-lap82-criticality-not-validity.md`
> → `PENDING_WORK.md` (lap-82 box, top) → `STATUS.md`. Branch `plan`. Build 🟢 green
> (`lake build GoodsteinPA`). HEAD = lap-82 crux-2 commit. Tree clean after final doc commit.

## 🎯 OPERATOR REDIRECT (this session)
Front 2 (`PA_delta1Definable`) moved to a **parallel box** — accept Foundation's axiom as a tracked
rest-point, **STOP touching `PADelta1.lean`** (it merges later). Drive **crux 2 (`RedSound`)** ONLY,
using Buchholz (both papers — validate, don't trust).

## ✅ THIS LAP (all green; axiom-clean)
1. **(pre-redirect) `PADelta1.lean` code-size bounds** — `lt_qqAll`, `self_le_qqAllItr`,
   `count_le_qqAllItr` (3 axiom-clean helpers; harmless, stay banked). DO NOT extend PADelta1 further.
2. **crux-2 root cause found + validated against Buchholz (both papers, read in full):** `zKValid`
   bakes a spurious *criticality* conjunct `(∀ i < lh ds, ¬ iperm (tp (znth ds i)) s)` into chain
   validity. Buchholz §3 clause 5 (= the repo's `isChainInf`: j₀ + threading + rank) carries NO
   criticality; criticality is a Def-3.2 *reduction* property. Baking it in forces `ZDerivation` to be
   *only-critical* chains → the genuine reduct's `Rep`-tagged recombined premises spuriously fail
   validity → `RedSound`-on-`iR2` false (`not_zKValid_iCritReduct`, NOT refuted — it's about the OLD
   over-strong validity).
3. **Decoupling banked** (`InternalZ.lean` after `zKValid_definable`): `zKCritical`, `zKValidF`
   (= `zKValid` − criticality), `zKValid_iff_zKValidF_and_zKCritical` (build accepting this split =
   in-kernel confirmation criticality is a separable conjunct), `zKValidF_of_zKValid`.
4. **⭐ KEY FINDING: the DESCENT side is ALREADY FULLY BANKED.** Every Buchholz reduction case has its
   closed `iord_descent_*` proved (`InternalZ.lean` 2529–3293): I-rules, Ind (LH4), non-critical chain
   `iord_descent_iCritAux` (5.2.2), splice `iord_descent_iSpliceEnd` (5.2.1), critical
   `iord_descent_iRcrit_of_chain` (5.1). `iord_iR2_iterate_descends` assembles the ε₀-descent modulo
   `RedSound`. **Crux 2 is NOT blocked on descent.**
5. **`zKValidFDef` + `zKValidF_defined`/`_definable`** — Δ₁ arithmetization of faithful validity
   (`zKValidDef` minus the `¬(!ipermDef ti s)` line), green first try. The re-point prerequisite.
6. **Measured re-point blast radius** (re-pointed `ZPhi`, built, reverted): exactly 6 lemma sites break,
   all mechanical except the descent capstone. Full list in `PENDING_WORK.md` lap-82 box.

## ▶ NEXT (revised redesign — descent done; arithmetization ready; wall = validity + dispatch)
1. Re-point `ZPhi`'s `zK` disjunct (`InternalZ.lean:3694`) `zKValid` → `zKValidF`; ALSO `zPhiBounded_iff`
   (3741, spots 3754/3768), `zblueprint` (3790/3808: `zKValidDef.sigma/.pi` → `zKValidFDef.sigma/.pi`),
   `zPhi_definable` proof. Then fix the 6 lemma sites (PENDING lap-82 box): `zKValid_of_ZDerivation_zK`
   (~4000) → returns `zKValidF`; constructors 5094/5125 → take `zKValidF` (become PROVABLE);
   ⚠️ `iord_descent_iR2_zK_of_valid` (4780) needs `by_cases zKCritical` (the HARD one — needs step 2).
2. Make `iR2_zK` dispatch critical (5.1) / non-critical (5.2.2 `iCritAux`) / sub-critical (5.2.1 splice)
   — currently always `iRcrit` (`iR2_zK_eq_iRcrit`). Each case's descent is already banked: just wire.
3. **Prove `RedSound` = Thm 3.4(b)/D₁** (THE new content): reduct is a genuine `ZDerivation` under
   `zKValidF`, by the same `ZDerivation` induction as descent. Critical recombination `K^{r−1}_Π d{0}d{1}`
   via Thm 3.4(a); peeling primitives `zDerivation_z*_inv` + `inference_critical_pair` in place.
Fallback: Siders' Howard vector (cross-check only).

## Commits this lap (branch `plan`, all green)
- `feat(lap 82): code-size bounds for recognizer` (PADelta1 — pre-redirect; banked, don't extend)
- `feat(lap 82): crux-2 root cause — criticality ≠ chain-validity; decouple zKValid`
- `docs(lap 82): crux-2 KEY FINDING — descent fully banked; wall = validity+dispatch`
- `feat(lap 82): zKValidFDef — Δ₁ arithmetization of faithful validity (re-point prereq)`
Tree clean after this HANDOFF update. No uncommitted proof edits.

## Build / verify
`lake build GoodsteinPA` green. `src/` sorries unchanged: `Statement.lean:22` (headline),
`Reduction.lean:66`, `DescentSemantic.lean:550` (crux-2 chain), `PADelta1.lean:803`
(`inductionSchemeUnivDelta1` — parallel-box rest-point, leave alone). Crux-2's `RedSound` is a `Prop`
hypothesis (not a `sorry`), the genuine remaining math content.

Aristotle: idle — Foundation not in its mathlib-v4.28 env; all work is Foundation-internal.
