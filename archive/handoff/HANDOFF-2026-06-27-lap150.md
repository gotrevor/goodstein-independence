# Handoff: lap 150 — code-recursion FRAME landed; descent measure CORRECTED `iRedDescent`→`iord`; §14.254 splice isolated

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `02f37cc` · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
`false_of_ZDerivesEmpty` = `[propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms, no drift).
Operator SOLE-OBJECTIVE = **M1b-term** (crux-2 termination). Working tree clean.

## ✅ DONE THIS LAP (4 commits)
1. **`759a5b7`/`5dea9ba` + `561f3d8`(prev) → the code-recursion FRAME.** `genReduct_botSucc` is now proven
   **by `zDerivation_sigma_induction`** (NEW `𝚺₁`-motive variant of `zDerivation_induction`, `InternalZ`).
   Validated the open meta-question: the existence-form §14.254 recursion **IS** legitimate in `𝗜𝚺₁` — the
   motive `∃ v, … ∧ icmp (iord v) (iord d) = 0` is `𝚺₁`-definable (`definability` passes). tags ∉{3,4}
   vacuous; **tag-3 (`zInd`) PROVEN**; tag-4 delegates to `genReduct_botSucc_chain` WITH the per-premise IH.
   `genReduct_botSucc_chain` is a sorry-FREE dispatcher (extract `isChainInf` ⊥-exit `j0` from chain
   validity — `Γ`-agnostic — then `by_cases` redex) → two faithful leaves `genReduct_chain_hasRedex`
   (§14.253 principal cut) / `genReduct_chain_noRedex` (§14.254 recurse-via-IH).
2. **`02f37cc` → `iRedDescent`→`iord` CORRECTION (the lap's key finding).** In-kernel proof that the chain
   principal cut (`iRKcCrit`) drops via the **DEGREE** (`idg_zK_iCritReduct_lt`), NOT `iotil`
   (`iord_descent_cut`'s `iotil` premise is against `ω^{õ d}`, weaker than `õ d`), so `iRedDescent`
   (`õ`-strict-drop) is **FALSE** for the chain cut. The host-dropped judge review
   (`E-2026-06-26-JUDGE-code-recursion-crux.md`) reached the identical conclusion INDEPENDENTLY. Corrected
   `genReduct_botSucc`/`_chain`/`_chain_hasRedex`/`_chain_noRedex` conclusion+IH to the COMBINED **`icmp
   (iord v) (iord d) = 0`**; tag-3 converts via `iord_descent_of_iRedDescent`. Isolated the genuine
   Buchholz **§14.254 SPLICE** as NEW `descent_step_K_splice` (the `iord`-monotone premise replacement;
   `descent_step_K_noncrit_repMajor` routes through it instead of the `iotil`-based `descent_step_K_replace`).

## 🎯 NEXT (per `PENDING_WORK.md` lap-150b — read it, it has the full attack) — three `iord` leaves
1. **`descent_step_K_splice`** (Crux2Blueprint, the §14.254 SPLICE) — the genuine engine the judge flags as a
   **bug-magnet**. Re-read `scratchpad/buchholz-gentzen.txt` §14.254 (lines 480-535) AND
   `ANALYSIS-…-lap87-splice-order-sensitivity.md` + `…-lap94-splice-dispatch-unfaithful.md` (prior in-kernel
   refutations = the test cases) BEFORE grinding. Need: `iord`-fold-monotonicity for `iCritAux`/`seqUpdate`
   that does NOT assume the `iotil` drop (the degree-traded case `iotil_iCritAux_lt` can't cover).
2. **`genReduct_chain_hasRedex`** (principal cut) — descent is **FREE** from the EXISTING
   `iord_descent_iRKcCrit_corr_of_redex`/`_neg_of_redex` (`Crux2Blueprint:2245`/`:2282`, they already give
   `icmp (iord (iRKcCrit …)) (iord (zK s r ds)) = 0`). Residual = soundness `ZDerivation_iRKcCrit_all`/
   `_neg_botOrbit` + invariants `ZRegular/ZFresh/ZSeqAnt_iRKcCrit` — blocked on **`Seq (seqAnt s)`** (next).
3. **`genReduct_chain_noRedex`** — `majorPrem_tag_mem` ⟹ major tag ∈{3,4,5,6}; reduce major (3/4) or Rep
   cut-partner (5/6) by IH, then splice (#1).

## 🔑 THE `Seq (seqAnt s)` GAP (tag-4 chains) — fix = zSeqAnt tag-4 FOLD (its own focused lap)
Soundness of the principal cut needs `Seq (seqAnt s)`; for a tag-4 CHAIN this is NOT derivable from
`ZSeqAnt` (`zSeqAntNext` tag-4 clause flags **`0`**, `Zsubst:2164`; `seq_seqAnt_of_zSeqAnt` works only for
`zTag ≠ 4`). Fix = change `zSeqAntNext`'s tag-4 branch to ALWAYS `seqAntSeqFlag (fstIdx d)` (analog of the
PROVEN lap-149 freshFlag fold / lap-146 zIndWff fold): ripple `zSeqAntNextDef` σ-clause + `_defined` simp +
`zSeqAnt_zK` + the ~6 `ZSeqAnt_zK_*`/`_iRKcCrit` sites (`Seq (seqAnt s)` for the UNCHANGED conclusion comes
from the original chain) + the orbit build sites. Then `Seq (seqAnt s)` falls out of `hseqant` with NO
threading. **gDef** (`exists_sigma1_descending_step` :3128) stays SEPARATE — the `𝚺₁` induction gives `∃ v`,
not the explicit `𝚺₁` graph; build the constructive single-step reduct (NOT `redLeast`, refuted lap-139).

## ⚠️ Forbidden / do-NOT-undo
- Do NOT re-introduce `iRedDescent` as the CHAIN descent measure (refuted in-kernel this lap; it's `iord`).
- Do NOT witness any descent with `red`; do NOT use `iord`-RECURSION for the construction (PRWO/Gödel-barred
  — CODE induction via `zDerivation_sigma_induction` only, now wired); do NOT `redLeast`/μ-min for gDef.
- Do NOT collapse the repMajor/axMajor §14.254a/b split. Do NOT touch the off-path dead `red`-soundness
  sorries {:82,:1257,…} AS STATED.
- `descent_step_K_replace` (`:2475`, `iotil`/`iRedDescent`-based) is STILL VALID infra for `iotil`-dropping
  reducts (Ind) — not deleted, just no longer the chain consumer. Don't confuse it with `descent_step_K_splice`.

## 📁 Key anchors
- Frame: `genReduct_botSucc` (`Crux2Blueprint:3012`), `genReduct_botSucc_chain` (:2979 dispatcher),
  `zDerivation_sigma_induction` (`InternalZ`, after `zDerivation_induction`).
- Leaves (all `iord`-descent): `genReduct_chain_hasRedex` (:2932), `genReduct_chain_noRedex` (:2952),
  `descent_step_K_splice` (:3067), `descent_step_K_noncrit_axMajor` (tags 5/6, :3110), gDef (:3128+).
- Descent FREE for hasRedex: `iord_descent_iRKcCrit_corr_of_redex`/`_neg_of_redex` (`Crux2Blueprint:2245`/`:2282`).
- Judge reviews (host, committed): `E-2026-06-26-JUDGE-code-recursion-crux.md`,
  `JUDGE-HANDOFF-2026-06-26-lap150.md` (the binding outside-judge baton).
- Flawed bundle (dead, reverted): `scratchpad/lap150-flawed-iRedDescent-bundle.diff`.

---
**→ Next session: attack `descent_step_K_splice` (§14.254 SPLICE) FIRST after reading lap87/lap94, OR do the
zSeqAnt tag-4 fold to unblock hasRedex soundness. Keep build green + headline 0 math axioms. NEVER `red`;
NEVER `iord`-recursion for the construction; NEVER `iRedDescent` for the chain descent (refuted).**
