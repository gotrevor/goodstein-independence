# Handoff: lap 143 — DEEP REFLECTION + WIRE iRKcCrit into the critical ∀-case (red-free)

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms). **`descent_step_K_critical_all` `#print axioms` = `[propext, Classical.choice, Quot.sound]`**
(sorry-FREE, axiom-clean — NO false `:1108`/`:80`).

## This lap = altitude reflection THEN grind (both delivered)

### Reflection (the synthesis — primary deliverable)
`REFLECTION-2026-06-26-lap143.md`. **Finding:** the lap-132 existence-form pivot was right and KEPT, but laps
141-142 half-abandoned it — `descent_step_K_critical` re-witnessed the bare-`∃` step with `red`
(`ZDerivesEmptyR_red` → the kernel-FALSE/incomplete `redSoundGen` :1471 → `zKValidF_iIndReduct_of_zInd` :80 +
open zK :1508 + `ZDerivation_red_zK_crit` :1108), and the genuine red-free `ZDerivation_iRKcCrit_critical_all`
(:1847, lap-142) was banked but UNWIRED. Binding directive set in `DIRECTION.md` (lap-143): FINISH the wiring,
witness with genuine reducts, NEVER `red`.

### Grind (executed the directive's step 1+2 — a real crux drop)
- **NEW (Zsubst.lean):** `ZSeqAnt_iRKcCrit` + `ZSeqAnt_iRKcCrit_of_zK` (the one missing `ZDerivesEmptyR`
  conjunct), `ZFresh_iRKcCrit_of_zK` (uniform `_of_zK`), helpers `ZSeqAnt_zsubst_zIallPrem` /
  `ZSeqAnt_zInegPrem`. The `zAx1`/`zInegPrem` leaves get antecedent-`Seq` from the orbit (cut-partner is an
  L-axiom: `tp = isymLk` ⟹ `zTag ∈ {5,6} ≠ 4`, via `tp_isymLk_tag` + `seq_seqAnt_zK_premise`).
- **SPLIT `descent_step_K_critical`** (Crux2Blueprint.lean) on the R-redex polarity:
  - `descent_step_K_critical_all` (I∀) — witnesses `∃ d'` with `iRKcCrit (zK s r ds)`: soundness =
    `ZDerivation_iRKcCrit_critical_all`, invariants = `ZRegular_/ZFresh_/ZSeqAnt_iRKcCrit_of_zK`, descent =
    `iord_descent_iRKcCrit_corr`. **sorry-free, axiom-clean, red-free.**
  - `descent_step_K_critical_neg` (I¬) — NEW named honest sorry (residual = `redexJ ≤ j0`).
  - dispatcher case-splits on `redZKReady_of_zKValid`'s ∀/¬ disjunction.

## 🎯 NEXT (hardest-first; `DIRECTION.md` lap-143 steps 3-5)
1. **Re-witness the Ind branch** of `ZDerivesEmptyR_descent_step` (`Crux2Blueprint:1946`) with the corrected-Ind
   reduct `iIndReductSeqG` (lap-136), not `red d`, dropping its `redSoundGen`/false-:80 dependence. (Needs the
   Ind soundness + descent for `iIndReductSeqG`; the soundness shell is the lap-136 `iIndReductSeqG` chain.)
2. **Close `descent_step_K_critical_neg`** (the ¬-case): pin `j0 = lh ds − 1` for genuine ⊥-orbit chains
   (`isChainInf_of_last`) so `redexJ < lh ds = j0+1`; OR weaken `ZDerivation_corrected_haux0_neg` to thread
   only up to `min(redexJ, j0)`. Then `ZDerivation_iRKcCrit_neg` + `iord_descent_iRKcCrit_neg` discharge it
   exactly like the ∀-case.
3. **(A)** `exists_sigma1_descending_step` (:1992) — the `gDef` packaging (concrete `redStep`).
4. **`descent_step_K_noncritical`** (:1924) — Buchholz 5.2 atomic reduct (the genuine non-critical leaf).
5. After 1+2, the DEAD `red`-soundness sorries {:80,:1108,:1211,:1384,:1471} are off-path → relocate to `wip/`.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any `ZDerivesEmptyR_descent_step` branch with `red` (the lap-141 regression this lap undid).
- Do NOT attack `redSoundGen`(:1471)/`ZDerivation_red_zK_crit`(:1108)/`zKValidF_iIndReduct_of_zInd`(:80) AS
  STATED — all kernel-FALSE/dead; they leave the live path as the witnesses switch.
- Do NOT re-attempt the major-premise-tag {3,4,5,6} split (abandoned lap-141) or the `redLeast`/μ-min (A) route
  (refuted lap-139).

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `descent_step_K_critical_all` (NEW, red-free ∀), `descent_step_K_critical_neg`
  (NEW named ¬-sorry), `descent_step_K_critical` (dispatcher), `ZDerivesEmptyR_descent_step` (:1943, Ind branch still `red`).
- `src/GoodsteinPA/Zsubst.lean`: `ZSeqAnt_iRKcCrit`(_of_zK), `ZFresh_iRKcCrit_of_zK`, `ZSeqAnt_zsubst_zIallPrem`, `ZSeqAnt_zInegPrem`.
- `src/GoodsteinPA/RedZKDescent.lean`: `iord_descent_iRKcCrit_corr`/`_neg`.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-143) · `REFLECTION-2026-06-26-lap143.md` · `PENDING_WORK.md` lap-143 · `STATUS.md` lap-143.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. The critical ∀-case
is now red-free (`descent_step_K_critical_all`, sorry-free, axiom-clean). FIRST move: re-witness the Ind branch
of `ZDerivesEmptyR_descent_step` with `iIndReductSeqG` (off `red`/false-:80), OR close the ¬-case
`descent_step_K_critical_neg` via `j0 = lh ds−1`. Witness with genuine reducts, NEVER `red`.**
