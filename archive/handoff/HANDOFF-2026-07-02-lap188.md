# Handoff: REBUILD-Z lap 9 (global 188) — crux SHARPENED to a kernel-proven binary; all-gated, STOP for judge

> ✅ **RESOLVED 2026-07-02 — this handoff is HISTORICAL.** The judge ruled
> (`E-2026-07-02-JUDGE-rebuild-z-lap8-validation.md`): ruling #1 = PAPER-LITERAL, the binary
> below was a false dichotomy (E–W Lemma 25 concludes at `α + β`, no successor bump; fix (b)
> refused as a Def-16/Def-23 conflation). **The order in force is
> `REBUILD-Z-SERIES-1-ORDER-2026-07-02.md`** (via DIRECTION's current binding block) — start
> there, not here.

**Date**: 2026-07-02 · **Branch**: `plan` · **Build**: 🟢 `lake build` 1341 jobs · **Headline**:
`peano_not_proves_goodstein` = `[propext, Classical.choice, goodstein_implies_consistency,
Quot.sound]` (UNDRIFTED). No `src/` touched this lap.

## 🎯 What this lap did

Resumed with lap 8 STOPPED for the judge on two escalations (`REBUILD-Z-LAP8-VERDICT.md`).
Rather than run the low-value fallback (rung R = plumbing over the forbidden pass `sorry`),
attacked the **route-decisive crux** — escalation #1, the reduction gate-composition trap — and
**sharpened it to a kernel-proven binary choice for the judge.**

## 🔬 The advance (`wip/Lap9GateProbe.lean`, both facts `#print axioms`-clean)

Pin-1 `cutReduceAllAuxRunning_Zf2`'s fresh `allω`/`cut`/`exI` node sits at `osucc (α + γ)` over
slot `g ∘ f`; its `Zef2` gate is `ewN (osucc (α+γ)) ≤ g (f 0)` = (saturating) `ewN α + ewN γ + 1 ≤
g (f 0)`. Available: `ewN α ≤ g 0`, `ewN γ ≤ f 0`. The entrance permits adding "(f.1)-class
hypotheses as needed" to pins 1–2, so the question was: does `EwF1 f ∧ EwF1 g` close it?

- **`noOsucc_closes`** — WITHOUT the `+1` (additive-norm output), `StrictMono g` **alone** suffices
  (`g(f0) ≥ g0 + f0`, the exact additive budget). So the growth bounds ARE enough for the sum.
- **`osucc_plus_one_refutes`** — WITH the `+1`, concrete `EwF1` slots refute it: `gBad m = if m=0
  then 2 else 2m+1`, `fBad m = 2m+1`; `g0=2, f0=1, g(f0)=g1=3`, `a=2≤g0, b=1≤f0`, yet
  `a+b+1 = 4 > 3`. The `EwF1` minimal step `g0→g1` leaves no room for the successor `+1`.

**Conclusion:** the obstruction is the **successor `+1`** (a single cut strictly grows the ordinal
against a *fixed* base with zero slack), NOT a missing growth bound. It is NOT fixable by any
`(f.1)-class` hypothesis. Also checked (analysis, grounded in `Zef2Prov`'s `≤`-slack + `ewN`
non-monotonicity + `ewN_add_le`): no in-statement re-rooting dodges it — the `osucc(α+γ)` ceiling
is too tight when a premise saturates (successor case `γ = osucc βφ`, IH witness reaches `α+γ`).

**vs lap 8:** lap 8 said "cross-gate `ewN α ≤ f 0` unavailable → escalate." Lap 9 proves the deeper
fact: *even granting the growth content via `EwF1 f ∧ EwF1 g`, the pin is kernel-false.*

## 🧭 The judge's ruling #1 is now a precise BINARY (no third option survives the counterexample)

- **(a) additive-norm output ordinal** (natural-sum/Hessenberg shape, `ewN` exactly additive → no
  `+1`; then `noOsucc_closes` closes via `StrictMono g`). Cost: `Ordinal.nadd` was DELETED at v4.31
  (`mathlib-nadd-removed-v431.md`) and ONote has no natural sum → ~bespoke; AND the pass's ordinal
  collapse consumes the output shape, so it can't be validated end-to-end without the (forbidden)
  pass. Ordinary `+` does NOT substitute (successor case still saturates).
- **(b) argument-absorbing gate** (`f (ewN α + ·)`, the genuine E–W `ewStep` design: `K = f(ewN α +
  m)`) instead of comparing to the fixed base `f 0`. Trades the invariant; must re-reconcile the
  headline read-off (`headline_readoff` needs the base-0 witness bound `n ≤ f 0`).

## 🚧 State — every remaining `src/` sorry is governance-gated (why this lap SELF-STOPS)

- pins 1–2 over `Zef2` — architect-owned statement redesign (self-ratify VOID). **Gated.**
- `cutElimPass_Zef2` (the pass) — "grinding the pass" FORBIDDEN. **Gated.**
- `cutElimPass_Zf` (old pin 3) — frozen/superseded token. **Gated.**
- rung D `readoff_delta0_Zef2` — "Δ₀ extension" FORBIDDEN. **Gated.**
- rung E `embedding_Zef2` — escalated trap-9, architect-owned. **Gated.**
- rungs R/W — laps-9+, gated behind "STOP for the judge"; R = plumbing over the pass sorry, W
  consumes E/R/D. **Gated.**

DIRECTION's binding block (lap-7 judge pass) mandates "STOP for the judge" after lap 8; the judge is
unavailable (asleep). All paths forward need the architect/judge. Run is `--allow-stop`-armed for
exactly this all-gated condition → `box done --green`.

## 🎬 Next actions (AFTER the judge rules #1)

1. **If ruling = fix (b) [likely, it's the genuine E–W design]:** redesign `Zef2`'s gate to
   argument-absorbing; re-reconcile the read-off exit bound (probe: does `n ≤ f(ewN α)` still give
   a concrete Σ₁ bound? — yes, but check `headline_readoff`). Then pins 1–2 re-thread with `EwF1`.
2. **If ruling = fix (a):** prototype an ONote additive combinator `α ⊕ γ` with `ewN` additivity +
   strict domination in `wip/` (PENDING_WORK lap-9 §NEXT); needs architect sign-off on the pass's
   ordinal accounting too.
3. Only after #1/#2 lands do rungs R/D/E/W and the pass grind (laps 9+) re-open.

## 📁 Key files

- `wip/Lap9GateProbe.lean` — the kernel-clean sharpening (noOsucc_closes / osucc_plus_one_refutes).
- `PENDING_WORK.md` (lap-9 section) — full analysis + next attack.
- `REBUILD-Z-LAP8-VERDICT.md` — the two escalations (this lap sharpens #1).
- `src/GoodsteinPA/OperatorZef2.lean` — the `Zef2` layer + 7 named pins.

---
**→ Next session: STILL STOPPED for the judge. Ruling #1 is now a precise binary (fix a vs fix b);
make it, then the pins/pass/ladder re-open. Do NOT self-ratify a `Zef2` redesign (VOID).**
