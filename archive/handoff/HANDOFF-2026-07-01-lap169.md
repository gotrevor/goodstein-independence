# Handoff — lap 169 (2026-07-01 04:08 UTC)

**Branch** `plan` · **HEAD** `52ea77a` · Build 🟢 green (`lake build GoodsteinPA` = 1326 jobs,
observed this lap). `wip/M2Probe.lean` typechecks 0 `sorry` (`lake env lean wip/M2Probe.lean`, exit 0).

## READ FIRST — DIRECTION owns the route
`DIRECTION.md` CURRENT DIRECTIVE (lap-167, operator-ratified) is BINDING and outranks this doc: a
**bounded 5-lap M2 feasibility probe** (Foundation→internal-Z bridge), laps 167–171, **HARD GATE lap
171**. The objective is a VERDICT (`M2-PLAUSIBLE` vs `PIVOT-B`) written to
`ROUTE-ESCALATION-2026-06-28.md`, handed to the operator — NOT a build, NOT headline wiring. FORBIDDEN
this window: grinding `residual`/`ind_reduct_anySucc`; wiring M2 into the headline; starting lap 172 on
Route A. This lap (169) is a probe-grind lap inside that window.

## 🎯 What we're doing
Probing whether internal Z can simulate the decisive M2 case — the **PA induction-axiom leaf** (directive
step 4). Foundation's PA carries induction as a closed theory-axiom formula (`axm s p`); the M2 bridge
must produce a Z derivation of its `toZ`-translated two-sided sequent. Lap-168 finding A claimed this is
"bounded — every rule needed is present." This lap tested that claim by making it concrete.

## 🧠 Context to carry forward
- **The two-sided induction shell is `zIall s a p (zInd q at' p d0 d1)`.** Peel `^∀ p` with `zIall`
  (eigenvar `a`); native `zInd` at conclusion-term `qqFvar a` gives base premise `d0 : Γ → φ(0)` and
  step premise `d1 : Γ, φ(b) → φ(b+1)` (b = zInd's own eigenvar). Γ = {φ(0), ∀x(φ(x)→φ(x+1))} from the
  `→`-peel. I verified this composition typechecks with all invariants (`twoSidedInductionShell_R`).
- **KEY FINDING (contradicts lap-168's optimism): the STEP premise is NOT closable by internal Z's nine
  rules.** Two independent gaps: (1) Z's only `∀`-left is `zAxAll`, succedent `substs1 (numeral k) p`
  (`zAxAllSuccWff`, `InternalZ.lean:1576`) — NUMERAL instantiation only, never the free eigenvar
  `qqFvar b`; (2) no `∨`-/`→`-left rule — `φ(b)→φ(b+1) = ∼φ(b) ^⋎ φ(b+1)` (`Bootstrapping.imp`) is a
  general disjunction, NOT the `_ ^⋎ ^⊥` shape `zIneg`/`zAxNeg` handle. Enumerated all 9 `ZPhi`
  constructors (`InternalZ.lean:5512`) — no free-var ∀-left, no general implication-left.
- **This is the finitary-`zInd` vs `PA_∞`-ω-rule design fork.** A real PA_∞ derives the induction axiom
  via NUMERAL instances only (matches `zAxAll`) + the ω-rule; the current finitary `zInd`
  (eigenvariable) needs a free-var ∀-left that is absent. So the induction leaf's STRUCTURE is bounded,
  but simulating the closed axiom as a standalone Z sequent hits a genuine expressiveness gap. **My
  read: this leans PIVOT-B** for the induction leaf as the calculus stands.
- **Two escape hypotheses to test before the verdict** (either reopens M2-PLAUSIBLE): (α) an admissible
  free-variable `∀`-left / substitution lemma that lifts a numeral-instantiated derivation family to a
  free-var one; (β) whether the bridge can avoid the standalone axiom sequent by translating the axiom's
  USE-in-proof rather than the leaf in isolation — but Foundation's Hilbert `axm` leaf enters as a
  closed formula, so the standalone sequent looks required. I did NOT find either escape this lap; they
  are the honest open question for lap 170.
- **Tension for the gate (also flagged lap 168):** the escalation's "crux-2 = false summits" premise is
  contradicted by lap-168's prefix-reduct evidence (crux-2 `residual` looked tractable). Now lap-169
  adds a signal the OTHER way (M2 induction leaf looks harder than lap-168 thought). The lap-171 review
  should weigh BOTH.

## ✅ State
- 3 new theorems in `wip/M2Probe.lean`, all `#print axioms` = `[propext, Classical.choice, Quot.sound]`
  (verified this lap), 0 `sorry`:
  - `zDerivation_of_zIall_R` — ∀-peel carries the 3 `ZDerivesEmptyR` invariants (bounded ✓).
  - `zDerivation_of_zAx1_R` — identity leaf w/ invariants; discharges the induction BASE `Γ → φ(0)`.
  - `twoSidedInductionShell_R` — the full shell composes with invariants given base+step premises.
  - `PAInductionStepObligation Γ φ b` — the isolated step obligation `Γ, φ(b) → φ(b+1)`.
- `PENDING_WORK.md` lap-169 section written with the full finding + the two escape hypotheses.
- Committed `52ea77a`, green.

## 🎬 Next actions (lap 170, toward the lap-171 verdict)
1. **Test escape (α):** search Z for an admissible free-variable `∀`-left or a substitution/specialization
   lemma (`zsubst` machinery, `maxEigen`/eigenvariable regularity) that could convert a numeral-instance
   derivation into a `qqFvar b` one. Grep `src/GoodsteinPA/Zsubst.lean` for substitution-admissibility.
   If none exists and none is cheaply buildable, that closes off (α).
2. **Test escape (β):** check how the M2 bridge (`FoundationToZSimulationR` in `wip/M2Probe.lean`)
   actually consumes an `axm` leaf — does it require a standalone Z sequent for the leaf, or can it
   compose with the surrounding proof? Read Foundation's `Theory.Derivation`/`axm` interface.
3. **Write the lap-171 verdict** (if 170 is the last probe lap) to `ROUTE-ESCALATION-2026-06-28.md`:
   `PIVOT-B` unless (α)/(β) land. Do NOT start lap 172 on Route A.

## ⚠️ Gotchas
- `wip/` files are NOT in the `lean_lib` (srcDir=`src`). Typecheck with `lake env lean wip/M2Probe.lean`,
  NOT `lake build GoodsteinPA.wip.M2Probe` (that path doesn't exist → error 4294967294).
- **A `-/` sequence inside a Lean docstring closes the comment early.** My finding-doc string had
  `` `∨`-/`→` `` which terminated the `/-- … -/` mid-comment → parse error. Rephrase to avoid literal
  `-/`. (Flushed to auto-memory.)
- Newest baton = numeric sort on lap field, not `ls | tail`:
  `ls HANDOFF-*-lap*.md | sort -t p -k2 -n | tail -1`.

## 📁 Key files
- `wip/M2Probe.lean` — the M2 probe surface; new decls at the tail (~line 717+).
- `DIRECTION.md` — the binding lap-167 M2-probe directive + ROUTE GUARD.
- `PENDING_WORK.md` lap-169 — this lap's full finding.
- `M2-PROBE-FINDING-2026-07-01-lap168.md` — lap-168's (now-partly-superseded) optimistic finding.
- `ROUTE-ESCALATION-2026-06-28.md` — where the lap-171 verdict goes.
- `src/GoodsteinPA/InternalZ.lean:1576` (`zAxAllSuccWff`), `:5512` (`ZPhi` 9 rules) — the obstruction
  evidence.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then pick up at "Next actions" #1: test M2 escape hypothesis (α) by searching Z for an
admissible free-variable ∀-left, working strictly inside the lap-167 M2-probe directive (verdict due
lap 171).**
