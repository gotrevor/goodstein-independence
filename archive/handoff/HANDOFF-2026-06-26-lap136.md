# Handoff: lap 136 — Ind-reduct obstruction REFUTED in-kernel; corrected reduct (`iIndReductSeqG`) built

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `a537d33` · tree CLEAN · build 🟢 green
(`lake build GoodsteinPA`, **1326**). Headline axiom footprint UNCHANGED
(`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms).

## 🎯 Mandate this lap: M1b-term (tag-3 / Ind soundness front of `descent_step_K_majorIdx`)
The lap-135 handoff recommended `zKValidF_iIndReduct_of_zInd` (Crux2Blueprint:79) as the "tractable" entry
to tag-3 soundness. **It is FALSE as stated** — proved in-kernel — and the lap pivoted to building the
genuine corrected reduct. Four green commits, all on M1b-term.

## ✅ What landed (all sorry-free, green)
1. **`0756f98` — KERNEL-VERIFIED OBSTRUCTION.** `zKValidF_iIndReduct_of_zInd` is false. The engine's Ind
   reduct `iIndReductSeq d0 d1 1 = ⟨d1,d0⟩` (step first, base last) cannot be a valid `zK` chain:
   `isChainInf`'s exit clause forces a premise succedent `∈ {F(a+1), F(0)}` to equal the conclusion
   succedent `F(t)` — degenerate-term only. Theorems: `isChainInf_iIndReduct_exit` +
   `zKValidF_iIndReduct_forces_degenerate` (Crux2Blueprint, right after the :79 sorry). The `⟨d1,d0⟩`
   reduct is the *ordinal shadow* (descends via `iord_descent_iRInd_zInd` but is not valid); `iotil_zInd`
   ignores the term, so no finite single-step reduct is both valid and descending.
2. **`2a1fb9b` — `isChainInf_telescope`** (Crux2Blueprint): the k-step generalization of the proven
   `isChainInf_iCritReductSeq`. Any length-`k+1` sequence whose antecedents telescope (premise 0 ⊆ Γ;
   premise `i+1` ⊆ Γ∪{succ of premise `i`}; last premise carries `seqSucc s`/`⊥`; ranks bounded) is
   `isChainInf`-valid (exit `j0=k`). THE validity core.
3. **`9a18a72` — `iIndReductSeqG`** (Crux2Blueprint): the corrected substituted reduct
   `⟨d0, d1[a:=0], …, d1[a:=k-1]⟩`. Full PR-construction + recursion eqs (`iIndReductSeqG_zero/_succ`) +
   `𝚺₁-Function₄` definability (`iIndReductSeqGDef`, rew `![#0,#4,#1,#2,#3]`) + structural lemmas
   (`iIndReductSeqG_seq`, `iIndReductSeqG_lh = k+1`) + read-outs `znth_iIndReductSeqG_zero` (premise 0 =
   `d0`) and `znth_iIndReductSeqG_step` (premise `i+1` = `zsubst d1 a (numeral i)`, `i<k`).
4. **`a537d33` — `seqSucc_zsubst_zInd_step`** (Crux2Blueprint): the telescoping step succedent —
   `seqSucc (fstIdx (zsubst d1 (π₁ at') t)) = substs1 (t ^+ 𝟏) p`, modulo eigenvar freshness
   `fvSubst (π₁ at') t p = p`. Ind-step analog of `seqSucc_zsubst_zIall_premise`.

## ⚠️ KEY findings to NOT relitigate
- **Do NOT attack `zKValidF_iIndReduct_of_zInd` as stated** — it is false (kernel-verified). Fix the reduct
  (use `iIndReductSeqG`) instead. Memory: `ind-reduct-false-target-lap136`.
- **Numeral-representation subtlety:** the chain telescopes via successor `^+ 𝟏` (= `qqAdd · (numeral 1)`);
  `numeral_succ_pos` gives `numeral i ^+ 𝟏 = numeral (i+1)` for `i>0`, but at `i=0`,
  `numeral 0 ^+ 𝟏 = 𝟎 ^+ 𝟏 ≠ 𝟏 = numeral 1`. So the cut formulas align via successor, and the reduced
  Ind node's term `t = π₂ at'` must be the successor-tower `𝟎 ^+ 𝟏 ^+ … ^+ 𝟏` — thread through M2's
  Ind-node setup, or substitute the successor-tower term rather than `numeral i`.

## 📌 NEXT (hardest-first, on M1b-term) — assemble `zKValidF_iIndReductSeqG`
The remaining obligation is now finite/mechanical:
1. **Antecedent read-out** (the analog of `seqSucc_zsubst_zInd_step`): `seqAnt (fstIdx (zsubst d1 (π₁ at') t))`
   threads into `seqAnt s ∪ {F(t)}` — via `fstIdx_zsubst` + `seqAnt_fvSubstSeqt` + `zIndWff`'s
   `inAnt (F(a)) (seqAnt (fstIdx d1))` + Γ-freshness `fvSubstSeq (π₁ at') t (seqAnt s) = seqAnt s`. Pattern:
   `fstIdx_zsubst_zIall_premise` (Zsubst:2680) does the I∀ analog.
2. **`chainAsucc`/`chainAnt` of `iIndReductSeqG`** from the znth read-outs (1-line `unfold chainAsucc/chainAnt` +
   `znth_iIndReductSeqG_zero/_step`), feeding the telescope hyps. Watch the numeral boundary (above).
3. **`zKValidF_iIndReductSeqG`** = `isChainInf_telescope` (chain) ∧ per-premise conjuncts. Per-premise
   `iperm`/tag-UFormula come from each premise being a `ZDerivation`: `d0` (given) and
   `zsubst d1 (π₁ at') (numeral i)` via `ZDerivation_zsubst` (Zsubst:3430, needs `IsSemiterm 0 (numeral i)`).
   Conclusion succedent/antecedent UFormula from `zIndWff` (`IsSemiformula 1 p`) + substs1-UFormula.
4. **Then** re-key `iRInd`/`red_zInd` to `iIndReductSeqG` at `k = value of t`, and ripple-fix the descent
   infra (`iotil_zsubst` keeps each step's `õ = õ d1`, so the fold `ω^{õd1}·k ⊕ ω^{õd0}` still descends).

Alternative (cheaper, if orbit is Ind-free): make `ZDerivesEmptyR` carry an Ind-free invariant and discharge
tag-3 by vacuity (pushes induction-realization to M2). Parallel UNBLOCKED M1b-term pieces:
`descent_step_K_majorIdx` tag-5/6 (banked `majorPrem_zAx{All,Neg}_cutPartner` + `hAll` bridge) and tag-4.

## 📁 Key files / anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: obstruction theorems + `isChainInf_telescope` + the
  `iIndReductSeqG` block + `seqSucc_zsubst_zInd_step` (all between :79 and the "Branch recursion equations"
  section). Open crux sorries: `descent_step_K_majorIdx`, `prwo_forbids_existence_descent` (existence
  endgame near `false_of_ZDerivesEmpty`), `redSoundGen` K-case, `zKValidF_iIndReduct_of_zInd` (false; keep).
- `src/GoodsteinPA/Zsubst.lean`: `ZDerivation_zsubst` (3430), `fstIdx_zsubst` (147), `fstIdx_zsubst_zIall_premise`
  (2680), `seqSucc_zsubst_zIall_premise` (2648), `isSemiterm_succVar` (260), `termFvSubst_qqAdd` (219).
- `src/GoodsteinPA/InternalZ.lean`: `zIndWff` (1681), `zDerivation_zInd_inv` (8681), `iotil_zInd` (2414),
  `iRInd`/`iIndReductSeq` (5624/2955), `isChainInf`/`zKValidF` (1177/1415).
- `PENDING_WORK.md` lap-136 box (full plan); memory `ind-reduct-false-target-lap136`.

---
**→ Next session: HEAD `a537d33`, build green. The corrected Ind reduct `iIndReductSeqG` + its validity core
`isChainInf_telescope` + the step succedent are all banked sorry-free. Assemble `zKValidF_iIndReductSeqG`
(antecedent read-out → telescope → per-premise conjuncts), minding the successor-vs-numeral boundary. Don't
re-attack the false `zKValidF_iIndReduct_of_zInd`.**
