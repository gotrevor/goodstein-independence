# HANDOFF — lap 94 · Path-X O1: `red`-preserves-`ZRegular` zK-dispatch branches landed

> Supersedes prior HANDOFF-*.md. Branch `plan`. HEAD = `2a5d86b`. Build 🟢 green
> (`lake build GoodsteinPA`, 1325 jobs). Headline `peano_not_proves_goodstein =
> [propext, sorryAx, choice, Quot.sound]` (0 math axioms, unchanged); the lone math `sorry` is still
> `goodstein_implies_consistency` (`Reduction.lean:68`) = crux-2 = `redSound`.

## Operator target (unchanged)
Discharge `peano_not_proves_goodstein` AXIOM-FREE = crux-1 (DONE lap 57) ∘ crux-2 (`redSound` =
internalized cut-elimination). Path X (finitary, lap-92 DECISION) is the chosen route. O1 =
eigenvariable-freshness tracking (`zReg`/`ZRegular`) maintainable through `red`.

## What lap 94 landed (2 commits, all axiom-clean `[propext, choice, Quot.sound]`, green 1325)
All in `src/GoodsteinPA/Zsubst.lean` (namespace `GoodsteinPA.InternalZ`), after the lap-93 zReg blocks:

1. **zK-dispatch regularity branches** (`0bba42e`): `ZRegular_zK_of_seqInsert` (5.2.1 building block) +
   `red_zK_rep`/`red_zK_splice` (the iRK dispatch recursion eqns, ported from the uncompiled
   `Crux2Blueprint` INTO the build) + the three branch lemmas:
   - `ZRegular_red_zK_replace` (5.2.2 `iRKr`) — **unconditional** from the IH (`ZRegular_zK_of_seqUpdate`).
   - `ZRegular_red_zK_crit` (5.1 `iRKc`/`iRcritG`) — takes the two redex-premise reducts
     `ZRegular (red (znth ds (redexI/redexJ …)))` (`ZRegular_zK_of_iCritReductSeq` over two `seqUpdate`s).
   - `ZRegular_red_zK_splice` (5.2.1 `iRKs`) — takes the two splice halves' regularity.

2. **splice-of-chain + extraction** (`2a5d86b`): `ZRegular_iRcritG_premise` (a regular `iRcritG` has both
   `iCritReductSeq` halves regular) + `ZRegular_red_zK_splice_of_chain` — **derives** the 5.2.1 halves'
   regularity from `zTag dᵢ = 4` (selected premise is a chain) + IH, via `red_zK_crit` (the splice branch
   ⟹ `dᵢ` critical ⟹ `red dᵢ = iRcritG …` 2-premise reduct). The interface `redSound` consumes: the
   `zTag dᵢ = 4` fact comes from `zKValidF` validity data (lap-93 finding).

⟹ **All three zK-dispatch regularity branches now reduce to data `redSound` has in scope** (IH +
per-premise `ZDerivation`/`zTag`/`zKValidF`).

## ▶ NEXT (START HERE)
1. ✅ **DONE (`710b469`, `52c9af3`).** `ZRegular_red_zK` assembled — the complete zK-case of "red preserves
   ZRegular", dispatching the two `iRK` sentinels into `ZRegular_red_zK_replace`/`_splice_of_chain`/`_crit`.
   - ✅ **`hredex` DISCHARGED INTERNALLY** (`52c9af3`): `redexI_redexJ_lt_of_zKValid` (extracts the
     redex-in-range half of the `iord_descent_iR2_zK_of_valid` recipe, InternalZ) +
     `zKCritical_of_not_permIdx_lt` (criticality from the `permIdx` sentinel) ⟹ the critical branch reads
     its redex bounds off the chain's own `zKValidF` (`zKValidF_of_ZDerivation_zK`) + criticality. No longer
     a hypothesis.
   - ⛔ **`hseltag` is FALSE — proven in-kernel** (`67feecd`, `not_permIdx_lt_zKseq_zAtom`). See
     `ANALYSIS-2026-06-25-lap94-splice-dispatch-unfaithful.md`. `iRK`'s inner sentinel
     `permIdx dᵢ < lh(zKseq dᵢ)` is `0 < 0 = false` for ANY non-chain `dᵢ` (atom/I-rule/axiom all have
     `lh(zKseq) = 0`), so the SPLICE branch fires by default for non-chain selected premises — the branch
     does NOT imply `zTag dᵢ = 4`. NOT cured by the ⊥-orbit (a selected `Rep` can be an atom). So
     `ZRegular_red_zK`'s splice leaf AND the validity-half `ZDerivation_red_zK_splice` are blocked at the
     SAME unfaithful dispatch. The current `red`/`iRK` cannot close them.
2. **▶ THE REAL NEXT STEP — route-B dispatch redesign.** `iRK` must gate the 5.2.1 splice on `zTag dᵢ = 4`
   (genuine chain) and route every non-chain selected premise through a CONCLUSION-REDUCING replace that
   emits `tpReduce (tp dᵢ) Π n` (already Σ₁-def'd) instead of keeping `Π`. This is a `def` change with
   blast radius on the banked `iord`-descent lemmas → scope via a `wip/` spike first (lap-92 de-risk
   doctrine). The lap-93/94 regularity bricks + `redexI_redexJ_lt_of_zKValid` + eigensubst reducts are
   reused by the redesigned dispatch. Until then `hseltag`/`ZDerivation_red_zK_splice` stay disclosed as
   the route-B frontier.
3. (Deferred) fold regularity INTO the validity induction: the combined invariant
   `∀ d, ZDerivation d → ZRegular d → ZDerivation (red d) ∧ ZRegular (red d)` and prove by
   `zDerivation_induction`. The `ZRegular` half is `ZRegular_red_of_not_zK` (lap 93) ∪ `ZRegular_red_zK`
   (step 1). The `ZDerivation` half is the deep cut-elim core (the Crux2Blueprint `ZDerivation_red_zK_*`
   sorries) — **still route-B-blocked** (see below), but the regularity half being in scope is what
   unblocks the I∀/Ind eigensubst conclusion-tracking (`ZDerivation_zsubst_zIall_premise` consumes
   `maxEigen_lt_of_regular_zIall`).
3. **Route-B reality check (read `ANALYSIS-2026-06-25-lap90-…`)**: the repo's `red` keeps `fstIdx = Π`
   (`fstIdx_iRK = fstIdx d`), so it equals Buchholz `d[0]` only for `tp = Rep`. The validity half
   `ZDerivation_red_zK` is FALSE in full generality; the headline-true target is `redSound` over
   `ZDerivesEmpty` with the conclusion-reduction (`tpReduce`, already Σ₁-def'd) tracked. Regularity (O1,
   this lap) is the eigensubst PREREQUISITE for route B's I∀ branch, NOT a replacement for the
   conclusion-reduction. Decide next: rewire `red` to emit `tpReduce`, vs prove the ⊥-orbit Rep-selection
   invariant hereditarily (lap-90 refuted the naive version — needs the regularity/validity coupling).

## Reusable interfaces (lap 93–94)
`maxEigen`/`maxEigen_zsubst`, `zReg`/`ZRegular`/`zReg_zsubst`, route-B bridges
`maxEigen_lt_of_regular_zIall/_zInd`, premise eigensubst `ZDerivation_zsubst_zIall_premise/_zInd_premise1`,
zK-branch regularity blocks (`ZRegular_zK_of_seqUpdate/_seqInsert/_iCritReductSeq/_premises`,
`ZRegular_zAxReduct`, `ZRegular_zK_premise`, `ZRegular_iRcritG_premise`), red dispatch eqns
(`red_zK_crit` [InternalZ], `red_zK_rep`/`red_zK_splice` [Zsubst]).

## STOP
- Do NOT bake freshness into `zIallWff`/`zIndWff` (reshapes the fixpoint; large blast radius — lap-93
  rejected this for the additive `zReg`).
- Do NOT attempt a standalone `red_preserves_ZRegular` for the `zK` SPLICE case (needs `zTag dᵢ = 4` from
  `zKValidF` — use `ZRegular_red_zK_splice_of_chain`, thread the tag from `redSound`).
- Do NOT "prove" the validity-half `ZDerivation_red_zK`/`redSoundGen` in full generality — FALSE (route B,
  lap-90). The regularity half is honest; the validity half needs conclusion-reduction.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. New lemmas `#print axioms` = `[propext, Classical.choice, Quot.sound]`
(verified via `lake env lean` scratch this lap). `redSound`/`goodstein_implies_consistency` still the
lone open math `sorry`.
