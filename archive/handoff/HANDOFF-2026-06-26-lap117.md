# HANDOFF — lap 117 · ¬-case critical-cut inversion SOUNDNESS proven; BOTH critical sub-cases complete

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint unchanged: `peano_not_proves_goodstein` (Statement.lean:22) is still a literal
> `sorry` (the multi-year target, reduced through crux-2), `[propext, sorryAx, Classical.choice,
> Quot.sound]` — **0 math axioms**. 8 sorries in `Crux2Blueprint`, unchanged count. All new lemmas this
> lap are axiom-clean `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

## ⭐⭐⭐ THE ADVANCE — the ¬-case inversion is now PROVEN (the second/last critical sub-case)
Lap 116 proved the **∀-case** critical-cut inversion (`iRcritG` + `critReductCorr`) but its CAVEAT was that
it handled ONLY the I∀ R-redex; the **I¬ R-redex** was open. This lap closes it — and the review found the
hidden prerequisite for the engine re-key (see NEXT): a clean re-key needs the ¬-case soundness too, else the
currently sorry-free descent (`iCrit_halves_descend`) and regularity (`ZRegular_red_zK_crit`) lemmas, which
read the corrected reduct's halves, would regress.

### THE structural finding (why ¬ is NOT a mechanical mirror of ∀)
Buchholz Def 3.2 case 5.1, ¬-subcase (`buchholz-gentzen.txt:657-660`): for a cut on `¬A` the two
half-derivations **SWAP** which redex they replace —
- `d{0}` (succedent half `Π.A`) replaces the **L**-redex `j` (the `axNeg` axiom),
- `d{1}` (antecedent half `A,Π`) replaces the **R**-redex `i` (the `I¬` rule),

the OPPOSITE of the ∀-case (`d{0}↦i`, `d{1}↦j`). The repo's `iRcritG` hardwires the ∀-convention
`seqUpdate` slots, so it CANNOT express the ¬-case — a new **swapped-half constructor `iRcritGNeg`** is
required.

### What got built this lap (all banked, axiom-clean, reusable)
1. **`iRcritGNeg` + `ZDerivation_iRcritGNeg_of`** (`InternalZ`, after `iRcritG` ~6648 / after
   `ZDerivation_iRcritG_of` ~8521): the swapped-half critical reduct (`iCritReductG` with `redexI`/`redexJ`
   swapped in the two `seqUpdate` slots) + its assembly via the same `ZDerivation_iCritReductG_of`.
   `fstIdx_iRcritGNeg`/`zTag_iRcritGNeg` simp lemmas included.
2. **`ZDerivation_corrected_haux0_neg`** (`Crux2Blueprint`): the SUCCEDENT half — `redexJ = axNeg sⱼ A` ↦
   the §5 reduct `Ax^1_{Γⱼ→A}` = `zAx1 (seqSetSucc sⱼ A) A` (Buchholz Lemma 5.1 case 2.2), conclusion
   succedent set to `A = cutFormula d`, antecedent KEPT — via `ZDerivation_iCritReplaceReduce_of`.
3. **`ZDerivation_corrected_haux1_neg`** (`Crux2Blueprint`): the ANTECEDENT half — `redexI = I¬ sᵢ A d0` ↦
   the child `d0` (deriving `A,Γᵢ→⊥`), conclusion antecedent grown by `A`, endform succedent KEPT — via
   `ZDerivation_iCritReplaceReduce_general` + **`isChainInf_reduceR_membership`**. KEY: because `d0`'s
   succedent is `⊥`, the `isChainInf` re-points its distinguished tip to `redexI` (the `⊥`-endform), which is
   exactly why an arbitrary (non-`⊥`) conclusion succedent `D` is fine here (the `Or.inr hbot` branch).
4. **`ZDerivation_iRcritGNeg_corrected_neg`** (`Crux2Blueprint`): the ¬-case soundness capstone — feeds the
   two halves to `ZDerivation_iRcritGNeg_of`; cut-rank drop is `irk_cutFormula_lt`'s ¬-branch
   (`rk(A) < rk(¬A) ≤ r`); conclusion well-formedness from `zKValidF_of_ZDerivation_zK`.

### The ONE documented §5 residual (the ¬-side analogue of lap-116's `zAx1` gate)
`haux0_neg`/the capstone carry an explicit hypothesis `hpmem : inAnt A (seqAnt sⱼ)` — Buchholz 2.2's side
condition that **both** `A,¬A ∈ Γ` in the axNeg axiom. The repo's `zAxNeg` ZPhi disjunct pins only `¬A ∈ Γ`
(`zDerivation_zAxNeg_inv` returns `IsUFormula A ∧ inAnt (inegF A) (seqAnt s)`). DISCHARGE = strengthen the
`zAxNeg` (tag-6) ZPhi disjunct to also require `inAnt A (seqAnt s)` (the L-side twin of lap-116's `zAx1`
8th-disjunct ripple: `zphi_*`/`zblueprint` σ+π/`zPhi_definable` + the ~64 `rcases zDerivation_iff.mp` sites,
each axNeg arm gains one conjunct) — OR derive `A∈Γⱼ` from the redex-pair chain context (the cut formula `¬A`
threaded by the chain; less certain). This is the next prerequisite to make `haux0_neg`/the ¬ capstone
side-condition-free.

## 🎯 NEXT-LAP TARGET (hardest-first) — the engine re-key, now de-risked on BOTH polarities
With ∀ (`iRcritG`/`critReductCorr`, lap 116) AND ¬ (`iRcritGNeg`, this lap) soundness proven, re-key `red`'s
tag-4 critical branch (`iRKc`, `InternalZ:6656`) to **DISPATCH on the redex polarity** and emit the
polarity-correct constructor:
- `zTag (znth (zKseq d) (redexI d)) = 1` (I∀) → `iRcritG d (critReductCorr d)` (lap-116 reduct), else
- (I¬) → `iRcritGNeg d (ρ_neg d)` with `ρ_neg (redexI) = red dᵢ` (= `d0`) and `ρ_neg (redexJ) =
  zAx1 (seqSetSucc sⱼ A) A`.
Steps (mirror lap-116's planned re-key, now two-armed; will break the build mid-way — do as one unit):
1. **𝚺₁-definability** of the dispatched reduct (`critReductCorrDef` for ∀ + a `critReductNegDef` for ¬, or
   one combined def). Template = `cutFormulaDef` (`InternalZ:6558`, already arithmetizes
   `numeral (π₁(π₂(tp dⱼ)))`) + `iRKcDef` (`6662`). All accessors definable (`zsubst`/`zAx1`/`seqAddAnt`/
   `seqSetSucc`/`zIallPrem`/`zIallEig`/`tp`/`pi₁`/`pi₂`/`numeralGraph`).
2. **Re-key `iRKc`** + `iRKcDef` + `iRKc_defined` to the dispatched form.
3. **`red_zK_crit`** (`InternalZ:7304`) then gives `red (zK s r ds) = (iRcritG / iRcritGNeg) …` per polarity.
4. Close **`ZDerivation_red_zK_crit`** (`Crux2Blueprint:~383` sorry) by `red_zK_crit` + the two soundness
   capstones (`ZDerivation_iRcritG_critReductCorr` ∀ / `ZDerivation_iRcritGNeg_corrected_neg` ¬); orbit hyps
   come from a strengthened `redZKReady` motive at the call site (incl. `hpmem` once the `zAxNeg` disjunct
   is strengthened — see residual above).
5. **Descent re-point:** `iord_descent_red_zK_crit` (`RedZKDescent:86`) survives via `iord_iRcritG_eq_iRcrit`
   (ρ-invariant, ∀-side). For ¬ need an **`iord_iRcritGNeg_eq_iRcrit` twin** (iCritReductG's `iord` is
   symmetric in `ds0`/`ds1` since `#` is commutative — should be a short mirror of the ∀ proof).
   `iCrit_halves_descend` + `ZRegular_red_zK_crit` re-prove with the corrected bundles per polarity.

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, 8 sorries — by NAME)
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` (THE prize; BOTH critical sub-cases' soundness now
proven (∀ lap116, ¬ lap117), remaining = engine re-key + the `zAxNeg` `A∈Γ` strengthening) · axNeg replace
validity · `ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH · M2
`foundation_bot_to_Z_empty` · M3 `false_of_ZDerivesEmpty`. (`iRcritGNeg`, `ZDerivation_iRcritGNeg_of`,
`ZDerivation_corrected_haux0_neg`/`_haux1_neg`, `ZDerivation_iRcritGNeg_corrected_neg` are all sorry-FREE,
axiom-clean; not yet wired into `ZDerivation_red_zK_crit` pending the engine re-key.)

## Aristotle
Idle — CORRECT. The next piece (engine re-key + arithmetization + `zAxNeg` disjunct strengthening) is an
engine-internal change, not an isolated open lemma. The `zAxNeg`-strengthening (a `ZPhi` ripple) could be
spun off to Aristotle as a self-contained sub-task if desired, but it is mechanical (mirror lap-116 `zAx1`).
