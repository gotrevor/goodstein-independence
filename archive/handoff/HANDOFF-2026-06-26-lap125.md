# HANDOFF — lap 125 · the `red`-stall is ROUTED AROUND; LEFT-branch soundness pinned to ONE encoding gap

> **RESUME BATON.** Branch `plan`, HEAD = this lap's commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint unchanged across laps 122–125 (only additive
> lemmas): no `sorry` touched, 11 disclosed sorries as in lap 121. `peano_not_proves_goodstein` still
> `[propext, sorryAx, choice, Quot.sound]` (0 math axioms); all new lemmas verified `[propext,
> Classical.choice, Quot.sound]`.

## The session arc (laps 122–125): the 13-lap `red`-stall is dissolved as a blocker
The lap-120 reflection named the `red`-stall (`permIdx` selects an atom/`zAx1` leaf ⟹ `red d = d`, not
cut-free) as THE open crux, unresolved for 13+ laps. This session routed around it entirely.

**Banked (all axiom-clean `[propext, choice, Quot.sound]`):**
1. `inference_critical_pair_of_chain_reroute` (InternalZ) — generalized redex finder; `hnperm` isymRep
   clause replaced by `hreroute`. **Refuted the lap-121 "L-axiom caveat"** (least-left-symbol ordering ⟹
   the cut-formula source is always below `j`, never an L-axiom).
2. `iord_descent_iRcrit_of_chain_reroute` (InternalZ) — stall-free K-descent wiring.
3. `hreroute_of_leaves` (InternalZ) — reduces `hreroute` to `hleaves` (every isymRep premise is a leaf).
4. `inference_critical_pair_of_botChain` (InternalZ) — leaf-stall redex from a genuine ⊥-chain via JUST
   `hleaves`; `hnperm2` is FREE for a ⊥-chain.
5. `redex_or_nonleaf_isymRep_of_botChain` (InternalZ) — the concrete Buchholz case-5.1/5.2 dichotomy.
6. `iord_descent_iRcrit_botChain_leaves` (RedZKDescent) — **the stall-bypassing descent**: `iRcrit`
   descends regardless of `red`'s `permIdx`, modulo `hleaves`. Never touches `red (zK s r ds)`.
7. `iRcrit_descends_or_nonleaf_isymRep` (RedZKDescent) — the DESCENT dichotomy (LEFT = actual descent /
   RIGHT = non-leaf isymRep), the case split the restructured endgame consumes.

## ⭐ THE remaining LEFT-branch gap — ONE precise encoding hole (lap 125 finding)
The SOUND LEFT successor is `iRcritG (zK s r ds) (critReductCorr …)` (NOT `zAxReduct∘red` — lap-114 proved
that unsound, instance-0 ≠ cut-instance k; but `iord` is instance-invariant so the descent transfers).
BOTH its soundness (`ZDerivation_iRcritG_critReductCorr`, Crux2Blueprint:470) and descent
(`iord_descent_iRcritG_critReductCorr`, RedZKDescent:457) are banked. The ONLY un-suppliable input is
**O3 freshness** `hpfresh`/`hΓfresh` (`fvSubst a (numeral k) p = p`, `fvSubstSeq … (seqAnt sᵢ) = seqAnt sᵢ`)
= the I∀ **eigenvariable condition** (`a ∉ FV(p)`, `a ∉ FV(seqAnt sᵢ)`). `hfresh_eig` (✅
`maxEigen_lt_of_regular_zIall`), threading/rank (✅ from `zKValidF`) are fine. **The current `ZPhi` I∀
disjunct does NOT track the eigenvariable condition** (`⟨s,a,p,d0,rfl,hd0,hsucc,hwff⟩` — no freshness), and
`zReg`/`ZRegular` only tracks `maxEigen d0 < a`. So `hpfresh`/`hΓfresh` are un-derivable from the present
encoding — that is the hole.

## Next-lap targets (in order)
1. **[lap 126] Strengthen the `ZPhi` I∀ disjunct with the eigenvariable freshness.** Add the conjunct
   (`fvSubst a · p = p ∧ fvSubstSeq a · (seqAnt s) = seqAnt s`) to ZPhi's I∀ case, then ripple:
   `ZPhi`/`zphi_monotone`/`zphi_strong_finite`/`zphi_iff`/`zblueprint` (σ+π)/`zPhi_definable` + EVERY
   `rcases zDerivation_iff` I∀-destructure site (InternalZ + Crux2Blueprint + RedZKDescent + Zsubst). SAME
   pattern as lap-115 (`zAx1` 8th disjunct) / lap-118 (`zAxNeg`). **ATOMIC ripple — build is red mid-way;
   do it in one turn, start at a fresh-budget turn head.** Then `zDerivation_zIall_inv` returns the freshness,
   supplying `hpfresh`/`hΓfresh` to `ZDerivation_iRcritG_critReductCorr` ⟹ LEFT-branch soundness for ∀-case
   closes (¬-case via `_haux1_neg`, half-built).
2. Assemble the LEFT successor as a `ZDerivesEmptyR` (soundness + `fstIdx`/`ZRegular` preservation) ⟹
   recursive descent step; restructure `false_of_ZDerivesEmpty` off the `red`-orbit onto the dichotomy
   successor + WF(`iord`).
3. RIGHT splice branch (case 5.2). Then `foundation_bot_to_Z_empty` (M2) + `gentzenDescentφ`.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open pieces (ZPhi ripple, endgame restructure) are engine-internal, not portable.

## Pointers
`PENDING_WORK.md` lap-124/125 (full descent dichotomy + the O3-freshness/eigenvar-condition residual) ·
`HANDOFF-2026-06-26-lap122.md` (the finder-assembly detail) · `STATUS.md`.
