# HANDOFF — lap 107 · FRESH-MIND REVIEW: the external-inductive prototype track is a DEAD END (kernel-verified); pivot to the Σ₁ engine `red` redesign

> **RESUME BATON.** Branch `plan`, tree CLEAN after this lap's commit. Build 🟢 green (`lake build
> GoodsteinPA`, 1325). `src/` UNTOUCHED — headline footprint intact, re-verified in-kernel (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms; lone
> `sorryAx` = crux-2 = `goodstein_implies_consistency`'s `sorry`); `peano_not_proves_consistency` /
> `not_proves_of_implies_consistency` = `[propext, Classical.choice, Quot.sound]` (axiom-clean — the whole
> reduction wrapper is clean; only crux-2 stands between us and the axiom-free headline). This was a REVIEW
> lap: no new proof bricks, two decisive in-kernel findings that change direction, + doc reorientation +
> honest cleanup of one vacuous lemma. 1 commit.

## ONE-LINE STATE
The lap-102→106 plan (prototype cut-elimination on external Lean inductives `ZInf`/`ZcOK`/`ZcDer`, then
arithmetize) is a DEAD END for the headline — proven by two kernel-verified findings — so the frontier pivots
to redesigning the **Σ₁ code engine `red`/`iord`** (`InternalZ.lean`), the only carrier that can host a
descent over non-standard codes, whose sole open crux is the lap-104 stall.

## The two findings (kernel-verified)

### Finding 1 — `ZInf.allInv` is VACUOUS
The lap-106 ∀-inversion lemma `ZInf Γ → inAnt (^∀φ) Γ → ZInf (seqCons Γ φ(t))` is provable by a SINGLE
weakening — `exact ZInf.weaken_top d.seq d` — using NEITHER `ht` (that `t` is a closed term) NOR the `^∀φ ∈ Γ`
hypothesis. Verified by replacing the entire 40-line `induction` with the one-liner and elaborating
(`lake env lean wip/PathCInf.lean`): accepted, only an "`ht` unused" linter warning (which itself confirms the
hypothesis is dead weight). The lemma is now `ZInf.allInv_vacuous` with the one-liner + the finding in its
docstring; the 6 commuting `sorry`s are GONE (deleted, not filled — they were on a content-free statement).

**Why vacuous.** The META `Zinfty.allInvAux` (`src/Zinfty.lean:429`) concludes
`Provable (o d) c (insert (χ/[nm n]) (Γ.erase (∀⁰χ)))`. Its entire content is (1) **ordinal preservation**
(same bound `o d`, same cut-rank `c`) and (2) **erasure** of `∀⁰χ`. `ZInf : V → Prop` has **no ordinal index**
and the statement **keeps `^∀φ`** (output ⊇ input), so the conclusion is a mere weakening. Cut-elimination IS
an argument about ordinals; a carrier with no ordinal cannot express it. ⟹ the lap-106 "principal case
proven" + the planned `permCongr` perf fix were polishing nothing. **Do NOT resume the `permCongr` fix.**

### Finding 2 — external inductives are NON-LOAD-BEARING for the headline
`ZInf` (PathCInf.lean), `ZcOK` and `ZcDer` (PathCOmega.lean:708/1338) are all external Lean
`inductive … : V → Prop`. PathCOmega.lean:701-702 says so explicitly ("PROTOTYPE the cut-elimination math …
the Σ₁ port … is the deferred final brick"). But the headline is `IΣ₁ ⊢ Con(PA)`: reasoning inside IΣ₁, the
ε₀-descent must hold in EVERY model `V ⊧ IΣ₁` — including non-standard models where `¬Con(PA)` holds and the
coded ⊥-proof `z : V` is a NON-STANDARD object. No external (Lean, well-founded) derivation tree exists for a
non-standard `z` (the PA→Zω embedding of a non-standard proof is externally ill-founded). So the embedding
`foundation_bot_to_Z_empty` (`Crux2Blueprint.lean:576`) is **unprovable** for the prototype carriers. They can
GUIDE the inversion combinatorics but can never be wired into the headline.

## The load-bearing carrier + the real crux
The Σ₁ engine `red : V → V` / `iord : V → V` (`InternalZ.lean`) IS arithmetized and TOTAL on all codes
(standard + non-standard) — that's why `iord_red_iterate_descends` (`Crux2Blueprint.lean:559`) already builds
the ℕ-indexed descent `n ↦ iord(red^[n] z)` on codes. The remaining gap is `iord_descent_red`
(`Crux2Blueprint.lean:533`, `sorry`) — and lap-104 proved (in-kernel) it is **false for the current `red`**:
`red d = znth (redTable d) d` steps via `iRNextG d s` (`InternalZ.lean:6915`), which dispatches ONLY on the
conclusion's top `zTag` (1→eigensubst, 2→peel, 3→`iRInd`, 4→`iRK` cut-reduct, else→identity). After one
K/cut reduction the reduct's top node is no longer a cut, so `red` becomes the identity and the orbit STALLS
(lap-104: `red_redAllEx_eq`, `sord_red_iterate_stalls_AllEx`, `wip/PathCOmega.lean`).

## NEXT (hardest-first) — the engine `red` redesign (Gentzen's reduction on codes)
1. **Redesign `red`/`iRNextG` to locate the relevant redex anywhere in the derivation code, not just the top
   node.** For an empty-sequent (∅→⊥) derivation the endsequent has no logical content ⟹ the lowest inference
   must be a cut; reduce THAT cut (key-reduction) and the conclusion stays ∅→⊥ with strictly smaller `iord`.
   This is a Σ₁ tree-search (`redTable`-style construction) for the lowest/topmost cut + a local key-reduction.
   The prototype inversion cases (which premise/witness `t` to select; how `#` `inadd` / `iotower` ordinals
   combine — banked axiom-clean in `InternalNadd`/`InternalTower`/`wip/PathCOmega.lean`) are the GUIDE; port
   them onto codes.
2. **Prove `iord_descent_red`** (K/cut case; the Ind case `iord_descent_red_zInd` is already done) for the
   redesigned `red`: `icmp (iord (red d)) (iord d) = 0` for a regular ∅→⊥ orbit `d`.
3. **`false_of_ZDerivesEmpty`** (`Crux2Blueprint.lean:588`): the ℕ-indexed `iord`-descent contradicts
   `PRWO(ε₀)`. Wire crux-1's PRWO + the embedding.
4. Discharge the remaining `Crux2Blueprint` validity `sorry`s (`78/95/196/369/455`) + `foundation_bot_to_Z_empty`
   (`576`), then wire crux-1 ∘ crux-2 → `Reduction.goodstein_implies_consistency` (`Reduction.lean:68`) →
   headline `Statement.lean:22`. ONLY when `#print axioms` is clean (anti-fraud).

## What this lap changed
- `wip/PathCInf.lean`: `ZInf.allInv` → `ZInf.allInv_vacuous` (one-line proof + the vacuity finding in the
  docstring); deleted the 6 commuting `sorry`s. Elaborates clean (`lake env lean`).
- `NEXT_STEPS.md`, `PENDING_WORK.md`, `STATUS.md`: prepended the lap-107 DIRECTION CHANGE box; refreshed
  STATUS header / Where-it-stands / Short-term / headline ledger row off the stale prototype framing.
- No `src/` edit. Headline footprint unchanged.

## Aristotle
Idle — CORRECT. The next target (redesigning a Σ₁ code-engine reduction with an ε₀-descent over the repo's
`InternalZ` HFS API) is deeply entangled with in-repo definitions, not a self-contained mathlib lemma.

## Kernel state (unchanged this lap)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` = `[propext, Classical.choice, Quot.sound]`.

---

## lap-108 continuation (same session, post-checkpoint resume) — narrowing + two-engine correction

**Build 🟢 green 1325; headline footprint unchanged.** Two things:

1. **`iord_descent_red` (`Crux2Blueprint.lean`) Ind branch PROVEN in place.** A `∅→⊥` derivation has top tag
   3 (Ind) or 4 (K) (`zTag_Ind_or_K_of_ZDerivesEmpty`); the Ind branch closes via the banked
   `iord_descent_red_zInd`, isolating the residual `sorry` to exactly the **K/cut case**. Genuine narrowing of
   the open core on the headline-WIRED finitary engine.

2. **Correction to lap-107 Finding (two distinct `red`s).** The lap-107 baton conflated the headline-wired
   finitary engine `red` (`src/InternalZ`, tags 0-6) with the `wip/PathCOmega` prototype `red` (tags 7-10).
   The lap-104 STALL is the PROTOTYPE one; the finitary engine's blocker is instead the K-case dispatch
   (`iRK` splice/replace branch ordinal descents unbanked — only critical `iord_descent_iR2_zK_of_valid` is)
   + the `redZKReady` "spine" motive (lap-101 flagged shaky, never settled) + the `axNeg` sub-residual. The
   lap-107 conclusions stand (prototypes non-load-bearing; `ZInf.allInv` vacuous); only the engine
   attribution is fixed. See `PENDING_WORK.md` lap-108 for the full corrected two-engine map + NEXT options.

**NEXT (hardest-first):** (A) finish `iord_descent_red`'s K case on the finitary engine — re-point
`iord_descent_iR2_zK_of_valid` to the `red`-ρ + bank the splice/replace branch descents; settle the
`redZKReady` spine motive in-kernel (prove it holds or prove it broken — don't leave it "shaky"). Escalate
to (B) the Σ₁-Fixpoint arithmetization of the ω-rule cut-elim only if (A)'s spine motive is genuinely broken.
