# HANDOFF — lap 112 · cutFormula strip LANDED + WIRED INTO iRcritG (engine now cuts the stripped A(d))

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint re-verified in-kernel this lap (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms;
> lone `sorryAx` = crux-2). Same footprint as laps 109–111.

## 🎯 IMMEDIATE NEXT STEP (resume here — the wire-in is DONE, only the close remains)
The splice `hr'` sorry (`Crux2Blueprint.lean:614`, inside `iord_descent_red`) is now CLOSABLE with
banked lemmas — the engine rewire (below) made its obligation `max (irk (cutFormula dᵢ)) r ≤
idg (zK s r ds)`. To close it:
1. Extend `iCrit_halves_descend` (`RedZKDescent.lean:146`) to ALSO return
   `irk (cutFormula (zK s r ds)) < zKrank (zK s r ds)` — it already extracts the redex `hRi` (=`tp(znth
   ds redexI)=isymR(chainAsucc ds redexI)`, via `redexPair_tp hrc` / the `inference_critical_pair`
   data `hRi` at `:172`) and the premise `ZDerivation` (`hmem _ hIlt`), so feed them to
   **`irk_cutFormula_lt`** (InternalZ, this lap; NO closedness arg). NB align `chainAsucc ds redexI`
   vs the lemma's `chainAsucc (zKseq (zK s r ds)) (redexI (zK s r ds))` — `zKseq_zK` makes them defeq.
2. In the splice branch (`Crux2Blueprint:601–614`), the `?_` obligation is exactly
   `iord_descent_red_zK_chain_splice`'s `hr'`. Rewrite `seqSucc (fstIdx (znth (zKseq (red dᵢ)) 0))`
   to `cutFormula dᵢ` (via the e0 readout `RedZKDescent:198` + `seqSucc_seqSetSucc`), then close with
   `max_le`: `r ≤ idg` by `r_le_idg_zK`; `irk (cutFormula dᵢ) ≤ idg(parent)` from
   `irk (cutFormula dᵢ) < zKrank dᵢ ≤ idg dᵢ` (`r_le_idg_zK`) `⟹ ≤ idg dᵢ − 1 ≤ iseqMaxIdg ds − 1 ≤
   idg(zK s r ds)` (`idg_zK`). This drops the FIRST of `iord_descent_red`'s 2 residual sorries.

## ✅ THE ENGINE WIRE-IN — DONE THIS LAP (commit `feat(crux-2/lap112): WIRE-IN`)
`iRcritG` now cuts the critical reduct on **`cutFormula d`** (stripped `A(d)`), not the principal
`chainAsucc`. Changed: `iRcritG` def, `iRKcDef`/`iRKc_defined` (new `cutFormulaDef` Σ₁ graph),
`ZDerivation_iRcritG_of` statement, `RedZKDescent.iCrit_halves_descend` e0/e1. `Zsubst` regularity
was parametric in the conclusion C → survived verbatim. `iotil`/`idg` immune (read premises). Green.

## ⭐ WHAT LANDED THIS LAP (one green commit, `InternalZ.lean` ~:6422, before `iRcritG`)
The lap-110/111 NEXT-step-2 deliverable (the **cut-formula strip**, Buchholz Thm 3.4(a)) is now
PROVED machinery in the kernel — the genuine degree-drop content, not scaffolding:

- **`cutFormula (d : V) : V`** — the STRIPPED cut formula `A(d)`. `A_i := chainAsucc (zKseq d)
  (redexI d)` (principal of the R-redex premise). `A_i = ∀xF ⟹ A(d) = F(k)` (instance `k =
  π₁(π₂(tp d_redexJ))` off the `L^k` premise); `A_i = ¬A = inegF A ⟹ A(d) = A`. Branches on
  `π₁(A_i − 1) = 6` (the `qqAll` tag). Mirrors `tpReduce`'s formula extraction.
- **`tp_isymLk_form`** — an `L^k_{A_i}` inference symbol forces `A_i ∈ {∀xF, ¬A}` (the only two
  `tp`-cases — tags 5/6 — that emit an `isymLk`). The structural lever that case-splits cutFormula.
- **`cutFormula_all` / `cutFormula_neg`** — readouts (`= F(k)` / `= A`).
- **`irk_cutFormula_lt`** — **THE strict rank bound** `rk(A(d)) < r`, vs the principal's NON-strict
  `rk(A_i) ≤ r`. This is exactly the degree drop the splice `hr'` and the critical `hCrk` need
  (lap-110 root cause: `iCritReductG` cuts on principal `A_i`, off-by-one in rank).

All PROVED, axiom-clean, green.

## ✅ CLOSEDNESS GAP DISSOLVED (2nd grind commit) — `irk_cutFormula_lt` now SELF-CONTAINED
Initial `irk_cutFormula_lt` needed `hsf : IsSemiformula ℒₒᵣ 0 (chainAsucc …)` (principal closed) —
forced by the ∀-case `irk_substs1` needing `IsSemiformula 1` matrix; `IsUFormula` (all `zKValidF`
tracks) does NOT suffice. **But that closedness is FREE from the R-redex premise**, now banked as:
- **`tp_isymR_form_wff`** (`InternalZ` ~:6450): a `ZDerivation` with `tp = R_{A_i}` is an I∀/I¬
  inference ⟹ `A_i = ∀xF` carries `IsSemiformula 1` (matrix, via `zIallWff`) or `A_i = ¬B` carries
  `IsUFormula B` (via `zInegWff`). The closedness lives in the **I-rule premise side condition**, not
  the chain validity.
- **`irk_cutFormula_lt`** restated: hypotheses are just `(ZDerivation dᵢ, tp dᵢ = R_{A_i}, rk A_i ≤ r)`
  — no external closedness. **Directly consumable in `iCrit_halves_descend`** (it has the premise
  derivations + the redex `hRi`/`hrank` already, `RedZKDescent:172–192`).

So strengthening `zKValidF` is **NOT needed** — the wire-in is a clean 2-step pipeline.

## NEXT — ordered (closedness no longer a blocker)
1. **(do FIRST) Wire `cutFormula` into `iRcritG`.** Replace `chainAsucc (zKseq d)
   (redexI d)` → `cutFormula d` in `iRcritG` (`:6427`), `iRKcDef`/`iRKc_defined` (`:6448/:6459` —
   swap `chainAsuccDef C ds i` for a new `cutFormulaDef C d`; cutFormula IS Σ₁-definable, template
   `tpReduceDef` `:6108`), and `ZDerivation_iRcritG_of`'s statement (`:8126`, proved generically →
   typechecks under uniform C-swap). The regularity ripple (`Zsubst.ZRegular_red_zK_crit` `:1505`,
   `iCrit_halves_descend` `:198`) is PARAMETRIC in the conclusion sequent C → survives verbatim.
2. **(the payoff) Close `iord_descent_red`'s splice `hr'`** (`Crux2Blueprint:614`). After the
   wire-in `seqSucc (fstIdx (znth (zKseq (red dᵢ)) 0)) = cutFormula dᵢ` (via `seqSucc_seqSetSucc`).
   The obligation `max (irk (cutFormula dᵢ)) r ≤ idg (zK s r ds)` closes as: `r ≤ idg` by
   `r_le_idg_zK`; `irk (cutFormula dᵢ) < zKrank dᵢ` by **`irk_cutFormula_lt`** (this lap, fed the
   redex `hRi` + premise `ZDerivation` + `hrank` from `iCrit_halves_descend`'s existing extraction at
   `RedZKDescent:172–192` — NO closedness needed now), then `< r' ≤ idg dᵢ ⟹ ≤ idg dᵢ − 1 ≤
   iseqMaxIdg ds − 1 ≤ idg(zK s r ds)`. Extend `iCrit_halves_descend` to ALSO return
   `irk (cutFormula (zK s r ds)) < r` (it has `hRi`/`hrank`/`hmem` in scope already). FIRST residual
   `iord_descent_red` sorry to fall.

4. **(THE PRIZE, still) `ZDerivation_red_zK_crit` ∀/¬-INVERSION** (`Crux2Blueprint:96`). The
   cut-formula strip is its prerequisite (now half-built). The two halves must DERIVE the stripped
   endsequents `d{0} ⊢ Θ→A(d)`, `d{1} ⊢ A(d),Θ→D` — Buchholz Thm 3.4(a) inversion. Template
   `Zinfty.allInv`/`andInv`/`orInv` (META, axiom-clean). Multi-year core; START after the strip lands.

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, build-line numbers)
- `:79` `zKValidF` Ind-reduct validity · `:96` `ZDerivation_red_zK_crit` (critical SOUNDNESS = inversion)
- `:197` splice validity · `:370` axNeg replace validity · `:456` (redZKReady-class) · `:541`
  `iord_descent_red` (2 internal sorries: chain-REPLACE IH `:600` + splice `hr'` `:614`) ·
  `:682` M2 embedding · `:695` M3.

## Aristotle
Idle — CORRECT. Everything open is entangled with the `InternalZ` HFS engine API
(`red`/`iRK`/`iord`/`zKValidF`/`cutFormula`). The closedness-strengthen + wire-in is engine-internal.

## Kernel state (re-verified in-kernel lap 112)
- `peano_not_proves_goodstein` / `goodstein_implies_consistency` = `[propext, sorryAx, choice, Quot.sound]`.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` / `goodsteinSentence_faithful`
  = `[propext, choice, Quot.sound]` (clean).
