# Handoff: lap 208 — 2b GROWTH CONVERSION CRUX CLOSED (master majorization + concrete engine); items (b)+(d) glue banked

**FINAL LAP STATE (6 green commits)**: `69dbdb0` (lemma bank: `hardy_succ_ge`/`hardy_arg_add`
argument super-additivity, `Wpow_lt`, `normSum`/`norm_add_le`, coeff-3 chain
`hardy_chain3_eq`/`stepOrd3_lt_Wpow`) · `3f8e3f2` (**`ewIter_hardy_le` SORRY-FREE — THE 2b
master majorization**, WF wrapper w/ abstract engine `hEng`) · `b58fdc8` (**concrete engine**:
`hEng_of_dom` at `e' := e₀+2` + end-to-end `ewIter_hardy_le_of_dom`) · `f7104a3` (PENDING) ·
`4810396` (2b(b) `gvb_substs_q_le`) · `df520d7` (2b(d) `goodsteinBodyE_semantic_link`).
Tree CLEAN. All new lemmas `#print axioms` = `[propext, Classical.choice, Quot.sound]`.

**Date**: 2026-07-03 · **Branch**: `plan` · **Build**: 🟢 `lake env lean` green on
`wip/HardyMajorization.lean`, `wip/ReadoffValueGate.lean`, `wip/E1EmbeddingGrind.lean`
(E1 now also imports `GoodsteinPA.InternalBridge`). No src edits this lap (bare `lake build`
last verified at lap-207 `f883dd8`; only wip touched since).

**Directive (OUTRANKS this)**: `DIRECTION.md` lap-206 block, step (4) — 2b growth conversion.

## ✅ This lap (the 2b majorization, closed end-to-end at the abstract-slot level)

1. **`ewIter_hardy_le`** (`wip/HardyMajorization.lean`):
   `ewIter f α m ≤ H_{ω^{e'+1+α}}(H_{ω^{e'}}(Nlog α + m + p))`, all NF α, sorry-free.
   Branch chain = outer IH → inner IH → `hardy_arg_add` pushes `Nlog δ` costs innermost →
   middle engine raised to branch level (`Wpow_lt`+`hardy_le_of_lt`, gate ≤ p) →
   `hardy_chain3_eq` (H³∘engine = `oadd gδ 3 (Wpow e')`) → final raise at the ENGINE-INFLATED
   argument (norm gate = `norm_add_le` + `norm_lt_of_Nlog_le` bridge + `hEng`). Single growth
   hypothesis `hEng`; NO f-mono/infl needed. Assignment `g α := e' + 1 + α` (ONote add).
2. **`hEng_of_dom` + `ewIter_hardy_le_of_dom`**: engine instantiated at `e' := e₀+2` from bare
   pointwise `∀z, f z ≤ H_{ω^{e₀}}(z)` (e₀ ≠ 0 NF), explicit pad; chain = closed form
   `H_{ω²}(y)+1 = 2^{y+1}(y+1)` (`hardy_omega_pow_ofNat`+`fastGrowing_two`) at
   `y := H_{ω^{e₀}}(x+p)` → raise ω²→ω^{e₀+1} (eq-split at e₀=1 via `repr_inj`) →
   `hardy_add_comp` exact → raise under ω^{e₀+2}. End-to-end:
   `ewIter f α m ≤ H_{ω^{e₀+3+α}}(H_{ω^{e₀+2}}(Nlog α + m + p))`.
3. **2b(b)** `gvb_substs_q_le` (ReadoffValueGate): one-binder numeral contraction
   `gvb ((Rew.subst ![nm m]).q ▹ χ) B ≤ gvb χ (max B m)` — P_m contracts to fixed P*.
4. **2b(d)** `goodsteinBodyE_semantic_link` (E1EmbeddingGrind):
   `goodsteinBodyE/[nm m] = ∃⁰ χ → atomTrue (χ/[nm n]) → Dom.goodsteinLength m ≤ n`.

## 🚦 NEXT (in order) — resume = item 1

1. **2b(c) Sslot-level assembly** (new wip section, likely E1EmbeddingGrind since it needs both
   readoff + majorization — HardyMajorization imports only OperatorZef2, E1 can't import it;
   either add `import` of nothing-cyclic (HardyMajorization imports OperatorZef2 only, E1
   imports OperatorZef2/WainerRoute/Embedding/InternalBridge — E1 CAN import wip file? wip
   files aren't in the lake tree; copy-or-move: cleanest = give HardyMajorization no deps it
   lacks and have the ASSEMBLY live in a NEW wip file importing both via `lake env lean` with
   both paths — wip files can't import each other (lap-207 note). Options: (i) do the assembly
   in HardyMajorization by RESTATING the two needed E1 facts as hypotheses (hypothesis-style
   seam, like readoff_value_goodstein did with Gated); (ii) judge-package time anyway).
   Content: fix m: from `readoff_value_goodstein` get bound
   `n ≤ ewIter (Sslot tower_m P_m) α' (Sslot tower_m P_m V)`; uniformize
   (i) `ewIterTower_rel1_le` commutes the K_m pre-max out, (ii) `gvb_substs_le`/`gvb_substs_q_le`
   contract P_m ≤ P*(max m ·); Sslot monotone-in-both ⟹ slot ≤ fixed S* with argument max-shift
   (needs a small `Sslot_mono_slot` + `ewIter_mono_slot` chain — both banked); then
   `ewIter_hardy_le_of_dom` with f := S*, e₀ := any NF exponent dominating S* pointwise —
   **S*-domination `S* ≤ H_{ω^{e₀}}` is the ONE genuinely new obligation**: S* = max(tower,P*);
   tower = ewIterTower of ewRootSlot (concrete, elementary per fixed B,d,α — likely dominated
   at some finite/ω-level e₀ via `ewIter_hardy_le` ITSELF applied to the tower! d-fold
   composition of the majorization); P* = gvb goodsteinBodyE (a FIXED formula — linear-ish,
   grep gvb growth; worst case take e₀ big enough).
   Then semantic link (d): goodsteinLength m ≤ n ≤ H_{ω^{e₀+3+α}}(H_{ω^{e₀+2}}(Nlog α' + g(m)))
   — ONE fixed ordinal, m only in the argument.
2. **2b(e)**: contradiction shape: `WainerRoute.goodsteinLength_eventually_dominates_fixed_fastGrowing
   (o, NF) : EventuallyLEWithSlack (fastGrowing o) goodsteinLength 2`. Need: the (c) bound
   `goodsteinLength m ≤ F(m)` with F ≤ fastGrowing o' eventually for SOME fixed NF o'
   (`hardy_le_fastGrowing` + `hardy_omega_pow_lt_fastGrowing` banked in Hardy.lean give
   H→fastGrowing brackets). Compose → contradiction with domination ⟹ `𝗣𝗔 ⊬ goodstein`
   modulo the judge-gated seams.
3. **Judge package** (unchanged): rung-E statement + Zef2TC + (Ax2) + route-(c) read-off +
   certificate seam + 2b majorization as ONE package. **Do NOT self-ratify into src.**

## Gotchas fresh this lap
- `omega` chokes silently on `2^(f x + 1)` atoms (prints an infeasible "counterexample");
  `generalize 2^(...) = Q at *` first, and keep `Wpow` folded/unfolded CONSISTENTLY across
  hyps and goal (a goal-only `simp only [Wpow]` splits the hardy atom).
- `set` inside a WF-recursive proof branch breaks the IH application (δ✝/δ mismatch) — inline.
- `⊧/` (`Evalbm`) is an abbrev over `Eval _ _ Empty.elim`: simp can't RE-FOLD a raw `Eval`;
  transport the ε to `Empty.elim` via a generic `∀ ε₁ ε₂` funext lemma (all `Empty → ℕ` equal),
  then apply `Defined.iff.mp` directly (no explicit v arg).
- `Semiformula.exs.inj` on a rewrite-application equality works (whnf), but the extracted body
  is in `rewAux` normal form — restate via a defeq `have` with `▹` before eval simp; the
  residual `(Rew.subst ![nm m]).q #1` val needs `Rew.q_bvar_succ` at `#(Fin.succ 0)` + a plain
  `simp at hkey` BEFORE the rw (deep env lambdas normalize only under full simp).
- ordinal `ω^{s+1}·ω = ω^{s+2}`: rw [opow_add] hits the INNER `s+1` first; do it in a
  standalone `have` with `conv_lhs`.
