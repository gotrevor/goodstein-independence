# HANDOFF — lap 110 · splice branch 6/7 closed (`iCrit_halves_descend`); `hr'` degree-drop crux isolated

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**). Headline
> footprint intact, re-verified in-kernel (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms; lone
> `sorryAx` = crux-2); `iCrit_halves_descend = [propext, Classical.choice, Quot.sound]`. 1 code commit
> + this doc commit.

## What landed this lap (110)

### `iCrit_halves_descend` — the splice halves' descent, BANKED (`RedZKDescent.lean`)
For a valid critical `K^r` chain `dᵢ = zK s r ds` (`¬ permIdx < lh ds`), the two critical-reduct halves
`a,b = znth (zKseq (red dᵢ)) {0,1}` satisfy the per-half `õ`/`idg`/NF bounds below `dᵢ`
(`ha`/`hb`/`hag`/`hbg`/`hNFa`/`hNFb`). **Key in-kernel fact:** the critical 5.1 reduct's `õ`-JUMP lives in the
OUTER `K^{r-1}` rank-drop, NOT the individual halves — each half is a `K`-chain over
`seqUpdate ds (redexI/J) (red·)` (i.e. `dᵢ`'s OWN premise sequence with the redex R/L premise swapped for its
strictly-descending genuine reduct), so each premise-fold descends below `dᵢ` via `iotil_iCritAux_lt` /
`idg_iCritAux_le` (`iotil`/`idg` ignore the half's reset conclusion/rank). Proof mirrors
`iord_descent_red_zK_crit`'s redex extraction, then the `iCritAux` fold-monotonicity lemmas.

### Wired into `iord_descent_red`'s SPLICE branch (`Crux2Blueprint.lean:595`)
The chain-splice sub-case (`dᵢ` a critical chain) now feeds the 6 bounds to
`iord_descent_red_zK_chain_splice`, **closing 6 of its 7 residual `sorry`s.** Only the rank bound `hr'`
remains.

## State of `iord_descent_red`'s K-case (end of lap 110)
- critical branch → `iord_descent_red_zK_crit` ✅
- non-critical, selected = I¬/Ind/I∀ → banked `iRedDescent` bundles ✅
- non-critical, selected = chain, `dᵢ` non-critical (REPLACE) → residual = the IH `iRedDescent (red dᵢ) dᵢ`
  (1 `sorry`; **blocked by the atom-fixpoint wall** — the IH is FALSE at atom-selecting fixpoints, see
  lap-109 box / PENDING)
- non-critical, selected = chain, `dᵢ` critical (SPLICE) → **6/7 closed via `iCrit_halves_descend`**; residual =
  the rank bound `hr'` only (1 `sorry`)
- non-critical, selected = atom/axAll/axNeg → the FIXPOINT defect (`red dᵢ = dᵢ`, 3 `sorry`s)

## ⭐ ROOT-CAUSE FINDING (lap-110) — `iCritReductG` cuts on the PRINCIPAL, not the STRIPPED, formula
See `ANALYSIS-2026-06-25-lap110-iCritReductG-cut-formula-strip.md`. The splice `hr'` AND the critical-case
soundness `ZDerivation_red_zK_crit` (`Crux2Blueprint:100`) share ONE root cause: `iCritReductG`/`iRcritG`
use cut formula `C = chainAsucc(redexI)` = the redex **principal** `Aᵢ`, but Buchholz Thm 3.4(a)
(`buchholz-gentzen.txt:690/705/808`) cuts on the **stripped** `A(d)` with `rk(A(d)) < r` STRICT
(`= rk(Aᵢ) - 1`). With the principal, `rk(C) ≤ r` only — `hCrk : irk C ≤ r-1` and `hr'`'s strict bound both
FAIL. `irk_cut_lt_rank_forall`/`_neg` (`InternalZ:409/415`) give the strict drop for the stripped formula.

**FIX (next lap target — supersedes the two-paths below):** redefine `iCritReductG`'s cut formula to the
stripped `A(d)` (def `cutFormula d` by cases on `Aᵢ = ^∀F`/`¬A` from the redex, via `substs1 k`/negand).
The ordinal-DESCENT lemmas (`iord_descent_red_zK_crit`, `iCrit_halves_descend`) are IMMUNE (`iotil`/`idg`
read only the premise sequence, never `C`); only `ZRegular`/`ZDerivation` end-sequent readers + the splice
rank `irk C` change. **`hr'` closes with ONLY the stripped rank bound (no inversion) — do this FIRST.** Full
`ZDerivation_red_zK_crit` additionally needs the ∀/¬-inversion `d{0}⊢Θ→A(d)`/`d{1}⊢A(d),Θ→D` (blueprint
`wip/PathCInf.lean` `Zinfty.allInv`). Full open-sorry inventory + 3-paths-each is in `PENDING_WORK.md` lap-110.

## NEXT (historical framing — the `hr'` degree-drop crux, now subsumed by the root-cause fix above)
`hr' : max (irk C) r ≤ idg(parent)` where `C = chainAsucc dsᵢ (redexI dᵢ)` is the redex PRINCIPAL formula
(`= seqSucc (fstIdx half0)`, established in-kernel). `r ≤ idg(parent)` is `r_le_idg_zK`. The hard half
`irk C ≤ idg(parent)`:
- `idg(parent) = max(r, iseqMaxIdg ds - 1)`; `irk C ≤ r'ᵢ ≤ idg(dᵢ) ≤ iseqMaxIdg ds` (`≤` only) — **off by
  one**, FAILS in the edge case `irk C = r'ᵢ = idg(dᵢ) = iseqMaxIdg ds` with `r < iseqMaxIdg ds`.
- `red_zK_splice`'s principal rank `irk C` is CORRECT (the spliced parent cuts on `C`) — NOT a stripping bug.
- **Resolution (PENDING lap-110, two paths):** (1) a chain-rank invariant giving STRICT
  `irk(chainAsucc ds i) < idg` (parent degree strictly dominates premise cut-ranks; hereditary via
  `zKValidF`/embedding) ⟹ `irk C ≤ iseqMaxIdg ds - 1 ≤ idg(parent)`, closing `hr'` via `le_iseqMaxIdgAux` +
  `idg_zK`; or (2) a measure/splice-rank refinement reflecting the degree-determining premise's reduction.
  Shares the bookkeeping with `redZKReady`'s motive (`Crux2Blueprint:340/493`).

After `hr'`: the chain-REPLACE IH (atom-fixpoint wall, lap-109 path 1 engine refinement) + atom/axAll/axNeg
fixpoints remain. Then `false_of_ZDerivesEmpty` (`Crux2Blueprint:673`) via PRWO + `iord_red_iterate_descends`;
embedding `foundation_bot_to_Z_empty` (`660`); wire crux-1∘crux-2 → headline. ONLY when `#print axioms` clean.

## Aristotle
Idle — CORRECT. The `hr'` chain-rank invariant + the atom wall are entangled with the `InternalZ` HFS engine
API (`zKValidF`/`idg`/`iseqMaxIdg`/`isChainInf`), not self-contained mathlib lemmas.

## Kernel state
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms.
- `iCrit_halves_descend` / `iord_descent_red_zK_crit` / `_replace_eq` / `_splice_eq` =
  `[propext, Classical.choice, Quot.sound]`.
