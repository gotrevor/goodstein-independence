# HANDOFF — lap 100 · the crux-2 validity assembly is COMPLETE modulo two named obligations

> Branch `plan`. HEAD = `c452155`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). 15 commits this lap,
> all green + axiom-clean.
> **NEXT LAP START HERE:** the entire K-case cut-elimination VALIDITY layer (`ZDerivation_red_zK`) is now
> sorry-free in its body; the whole orbit obligation is consolidated into ONE predicate `redZKReady`
> (Crux2Blueprint). The single remaining math residual for the K-case validity is the `redZKReady`-supplying
> **motive** (one `sorry` in `redSoundGen`'s K-case) plus the lone **axNeg** residual (Path C). Both deep.
> Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (bare `sorry`;
> designated audit surface, not yet wired). `peano_not_proves_consistency` clean.

## ✅ LANDED lap 100 (7 commits, all green 1325; new bricks axiom-clean `[propext, choice, Quot.sound]`)
1. **`ZDerivation_zK_replace_zIneg_of`** — I¬ non-`Rep` replace FULLY ASSEMBLED modulo orbit invariants
   (membership-`isChainInf` via `ZDerivation_iCritReplaceReduce_general`; antecedent grows by `p`).
2. **`ZDerivation_zK_replace_zAxAll_of`** — axAll non-`Rep` replace FULLY ASSEMBLED (cleanest; pure
   antecedent monotonicity, NO threading needed).
3. **`thread_rank_restrict_of_le`** (InternalZ) — wiring piece B: restrict parent threading/rank up-to-`j₀`
   down to `i ≤ j₀`. Trivial `le_trans`, exactly the capstones' `hthread`/`hrank`.
4. **`ZDerivation_red_zK_nonRep`** — the non-`Rep` replace DISPATCH assembled for 3/4 tags (I∀/I¬/axAll
   route to the capstones; atom/Ind excluded by `htp`, chain by `htag`). **axNeg (tag 6) is the lone
   internal `sorry`** (Path C).
5. **`redZKReady`** (def) + **`ZDerivation_red_zK` refactor** — the two replace `sorry`s in
   `ZDerivation_red_zK` are GONE; its body is sorry-free. `redZKReady s r ds` bundles EXACTLY the per-node
   orbit data (chain-Rep tracking + Seq-wff + selection-bounded threading/rank + per-tag freshness/faithful-
   ant/wff). The single residual moved up to `redSoundGen`'s K-case (`sorry : redZKReady …`).

**STATUS of the four non-Rep cases:** I∀ ✅ (lap 99), I¬ ✅, axAll ✅, **axNeg ❌ OPEN (Path C)**.

## ▶ THE remaining K-case math content, sharply localized to TWO obligations
### A. The motive — supply `redZKReady s r ds` (one `sorry` in `redSoundGen`)
`ZDerivation_red_zK` is a clean conditional on `redZKReady`. To CLOSE the K-case validity, strengthen
`redSound`'s induction (NOT `redSoundGen`, "false in general") to carry `redZKReady` per chain node. Lap-100
findings that sharpen this:
- **`redZKReady`'s `tp` facts are FREE** (`tp_zK = isymRep` unconditional + `zTag_iRK = 4` ⟹ `red` of a
  chain is a chain ⟹ `tp (red dᵢ) = isymRep`). So the chain-Rep field reduces to **`fstIdx (red dᵢ) =
  fstIdx dᵢ`** only. (Optional cleanup: slim the def + derive the tp facts in-place; LOW priority — they're
  trivially dischargeable by the motive too, so slimming doesn't reduce hard content.)
- **The genuine hard residuals are exactly TWO:** (i) `fstIdx (red dᵢ) = fstIdx dᵢ` for a non-critical
  chain `dᵢ` — TRUE only when `dᵢ` is "Rep-reducing" (its own selected premise is `Rep`, route-B
  `fstIdx_red`); this HEREDITARY Rep-reduction is the core of Buchholz Thm 3.4's conclusion bookkeeping.
  (ii) the `permIdx ≤ j₀` selection bound feeding threading/rank (banked half `permIdx_le_of_isPermPrem`;
  the orbit must supply "∃ permissible premise at index `≤ j₀`").
- **On a ∅→⊥ chain the non-Rep tag fields are VACUOUS** (Cor 2.1 `tp_selected_isymRep_of_emptyAnt_botSucc`:
  selected premise is `Rep`, so `znth ds permIdx = zIall/zIneg/zAxAll …` is FALSE — and even out-of-range
  `permIdx` gives `znth = 0 ≠ z…`). `Seq (seqAnt s) = Seq ∅` trivial there. First sub-lemma to attempt:
  `redZKReady_of_emptyAnt_botSucc` (∅→⊥ special case ⟹ just residuals (i)+(ii)).

### ⭐⭐ THE motive invariant is now pinned: hereditary all-`Rep` selected-premise spine
Read Buchholz `papers/buchholz-on-gentzens…md:98-104`. **Thm 3.4(b): `d[n] ⊢ tp(d)(Π,n)`** (reduct derives
the REDUCED endsequent, by simultaneous induction). The conclusion-tracking is the motive's second conjunct.
**KEY SUBTLETY:** repo `tp (zK) = isymRep` ALWAYS, but Buchholz `tp(d)` for a non-critical chain is
`tp(dᵢ)` (selected premise) — so a chain reduct's conclusion is `tpReduce (tp dᵢ) (fstIdx d) 0`, per-tag, not
a single formula. `redZKReady` already encodes exactly this per-tag structure.
**The genuinely-open core (does NOT dissolve via tracking):** `redZKReady`'s chain-`Rep` `fstIdx (red dᵢ) =
fstIdx dᵢ` needs `dᵢ`'s OWN selected premise to be `Rep` (so `red dᵢ` keeps Π — `fstIdx_red_zK_of_selected_
Rep`). For `dᵢ` a chain that recurses ⟹ **the invariant is "the selected-premise spine is hereditarily
all-`Rep`."** Cor 2.1 gives the FIRST level (`∅→⊥` chain's selected premise is `Rep`); the motive must extend
it down the spine. **Next-lap target:** does the selected (minimal-permissible) premise of an `∅→⊥` chain
itself have an `∅→⊥`-like conclusion (so Cor 2.1 reapplies)? Examine the `isChainInf` threading
(`Γᵢ ⊆ Γ ∪ {A₀..A_{i-1}}`) at the selected index — this is the precise next question. Two banked bricks
ready to consume: `tp_red_isymRep_of_zTag_4`, `fstIdx_red_zK_of_selected_Rep`.

### B. axNeg (Path C) — the succedent-replacement constructor
`ZDerivation_red_zK_nonRep`'s lone `sorry`. The axNeg reduct gives conclusion `Γ→p` (`seqSetSucc s p`), but
the reduct premise `red dᵢ = dᵢ` keeps succedent `D ≠ p`, so NO premise carries succedent `p` — the
membership-`isChainInf` route fails. Needs Buchholz's genuine ¬-axiom cut (premise restructuring;
`papers/buchholz-on-gentzens…md:80-95`, the `A,Θ→⊥ / Θ→A / Θ→D` triple). OR prove axNeg is unreachable as the
minimal-permissible non-chain premise on the ⊥-orbit (Path C1 — `iperm (L⁰_{¬p}) s ⟺ ¬p ∈ Γ`).

## ✅ ALSO LANDED lap 100 — ordinal-side `iord_descent_red` dispatch started + 4 motive bricks
- `thread_rank_restrict_of_le`, `permIdx_le_of_exists_isPermPrem` (InternalZ) — threading/selection-bound
  wiring combinators (piece B).
- `tp_red_isymRep_of_zTag_4` (Crux2Blueprint), `fstIdx_red_zK_of_selected_Rep` (InternalZ) — the chain-Rep
  conclusion-tracking bricks: tp facts FREE, Rep-reduction = "selected premise Rep" (off ∅→⊥).
- `iord_descent_red_zInd` (Crux2Blueprint) — Ind leaf of the ORDINAL-side descent (parallel to the validity
  dispatch). K-case ordinal residual mirrors `redZKReady`; per-branch descents banked
  (`iord_descent_iCritAux`/`_seqInsert`/`_iRcrit_of_chain`). `iord_descent_red` (Crux2Blueprint) still
  `sorry` — only the K-case left, shares the motive.

## Downstream (after the K-case validity closes)
`ZDerivation_red_zK` ⟹ `redSoundGen`/`redSound` ⟹ `iord_descent_red` (Crux2Blueprint, parallel motive) ⟹
`foundation_bot_to_Z_empty` (M2, the independent Bryce–Goré port) + `false_of_ZDerivesEmpty` (M3) ⟹ wire to
`Reduction.goodstein_implies_consistency` ⟹ headline.

## Aristotle
Idle — CORRECT. The residuals (hereditary Rep-reduction, `permIdx ≤ j₀`, axNeg ¬-cut) are entangled with the
7800-line `InternalZ` API, not self-contained mathlib lemmas.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `ZDerivation_red_zK = [propext, sorryAx, choice, Quot.sound]` (sorryAx =
the redZKReady residual + axNeg + the older iIndReduct/crit/splice). Headline footprint unchanged.
