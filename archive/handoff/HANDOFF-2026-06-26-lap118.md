# HANDOFF — lap 118 · ¬-case inversion's `hpmem` residual DISCHARGED; capstone now unconditional

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint unchanged: `peano_not_proves_goodstein` (Statement.lean:22) is still a literal
> `sorry`, `[propext, sorryAx, Classical.choice, Quot.sound]` — **0 math axioms**. No sorries added/removed
> (still 8 in `Crux2Blueprint`). All edits this lap are axiom-clean `[propext, Classical.choice, Quot.sound]`.

## ⭐⭐⭐ THE ADVANCE — the ¬-case soundness capstone is now UNCONDITIONAL
Lap 117 proved `ZDerivation_iRcritGNeg_corrected_neg` (the ¬-case critical-cut inversion soundness) SOUND but
with one orbit hypothesis `hpmem : inAnt A (seqAnt sⱼ)` — Buchholz 2.2's side condition that **both** `A,¬A∈Γ`
in the axNeg axiom. This lap discharges it.

### What was done (faithful, not just convenient)
Buchholz §5 case 2.2 (`scratchpad/buchholz-gentzen.txt:903`): `d = Ax^{¬A,0}_Π` requires `Π=Γ→⊥` **and
`A,¬A∈Γ`**. The repo's `zAxNeg` ZPhi disjunct pinned only `¬A∈Γ` — i.e. it was UNDER-specified. The fix makes
it faithful:
- **`ZPhi` 7th disjunct** (`InternalZ:5283`) gains a 4th conjunct `inAnt p (seqAnt s)`.
- Ripple (all mechanical, mirror the existing `inegF p` conjunct): `zphi_monotone`, `zphi_strong_finite`,
  `zphi_iff` (bounded restatement + both rcases/construction directions), `zblueprint` σ (`∧ !inAntDef p sa`,
  reusing the in-scope `sa = seqAnt s`) and π (`(!inAntDef nb sa ∧ !inAntDef p sa)`), `zPhi_definable` (the
  simp proof matched with no change).
- **`zDerivation_zAxNeg_inv`** (`InternalZ:8279`) now returns `IsUFormula p ∧ inAnt (inegF p) (seqAnt s) ∧
  inAnt p (seqAnt s)`.
- Construction sites threaded the new conjunct (all are *preservations*, never fresh introductions — the
  embedding M2 is still a sorry, so no new proof obligation lands today): `redSoundGen` (`Crux2Blueprint:890`),
  `ZRegular_red` (`Zsubst:1737`), `ZDerivation_zsubst` (`Zsubst:1940`, via `inAnt_fvSubstSeq hin2`), and the
  destructure in `iperm_tp_fstIdx_of_ZDerivation` (`InternalZ:5664`).
- **`ZDerivation_corrected_haux0_neg`** recovers `hpmem` in-proof from the axNeg premise's own derivation
  (`zDerivation_zK_inv hZ` → `zDerivation_zAxNeg_inv`), so its `hpmem` hypothesis is GONE.
- **`ZDerivation_iRcritGNeg_corrected_neg`** drops the `hpmem` hypothesis entirely.

This is the ¬-side twin of lap-115's `zAx1` 8th-disjunct discharge (which closed the ∀ L-half's `hZredL`).
Verified axiom-clean: `ZDerivation_iRcritGNeg_corrected_neg`, `ZDerivation_corrected_haux0_neg`,
`zDerivation_zAxNeg_inv` all `[propext, Classical.choice, Quot.sound]`.

## ALSO banked this lap (the ¬-branch re-key prerequisites — all axiom-clean)
1. **`iord_iRcritGNeg_eq_iRcrit`** (+ `idg_`/`iotil_` halves, `InternalZ` after `iord_iRcritG_eq_iRcrit`):
   the ¬-branch DESCENT re-point. `iRcritGNeg` swaps the two `seqUpdate` halves vs `iRcritG`; since
   `iotil = inadd(ω^õ d0)(ω^õ d1)` (commutative on NF via `inadd_comm`) and `idg = max(idg d0)(idg d1)`
   (commutative unconditionally via `max_comm`), the swap leaves `iord` INVARIANT. `idg_` half is
   unconditional; `iotil_` half needs the two folds NF (supplied by `hNF`/`hρNF` at the descent call site).
   This is HANDOFF lap-117 §NEXT step 5's "need an `iord_iRcritGNeg_eq_iRcrit` twin" — DONE.
2. **`critReductNeg`** (`InternalZ` after `critReductCorr`): the concrete ¬-case `ρ` the re-keyed branch
   emits — `redexJ ↦ zAx1 (seqSetSucc sⱼ A) A` (§5 axNeg reduct, succedent REPLACE not antecedent grow),
   `redexI ↦ zInegPrem dᵢ = d₀` (I¬ child, red-free 𝚺₁ accessor — arithmetization-ready, no
   re-principalization). Read-offs `critReductNeg_redexI`/`_redexJ`.
3. **`ZDerivation_iRcritGNeg_critReductNeg`** (`Crux2Blueprint` after the ¬ capstone): the concrete-`ρ`
   soundness — `ZDerivation (iRcritGNeg d (critReductNeg d))`, the `critReductNeg` twin of
   `ZDerivation_iRcritG_critReductCorr`. **Both polarity reduct suppliers are now soundness-certified
   against their concrete engine `ρ`** — exactly the objects the re-keyed `red` must produce.

## 🎯 NEXT-LAP TARGET (hardest-first) — the engine re-key, now FULLY de-risked on BOTH polarities
Both soundness capstones are now hypothesis-light AND concrete-`ρ`-certified (∀:
`ZDerivation_iRcritG_critReductCorr`; ¬: `ZDerivation_iRcritGNeg_critReductNeg`), and BOTH descent
re-points are banked (`iord_iRcritG_eq_iRcrit` / `iord_iRcritGNeg_eq_iRcrit`).

### ✅ Steps 1–2 DONE this lap — the dispatched reduct is DEFINED + ARITHMETIZED (the hardest piece)
- **`iRKcCrit d`** (`InternalZ`, after `critReductNeg_redexI`): the fully-explicit polarity-dispatched
  critical reduct (`zTag dᵢ = 1` → ∀ `iCritReductG` with re-principalized `zsubst`+`Ax^1`-antgrow; else ¬
  swapped-slot `iCritReductG` with `Ax^1`-succset+`zInegPrem`). Standalone (depends only on `d`, NOT the
  engine table) — red-free, table-free, no nested `if`. Bridges **`iRKcCrit_eq_corr`/`_eq_neg`** prove it =
  the soundness-certified `iRcritG (critReductCorr)` / `iRcritGNeg (critReductNeg)`.
- **`iRKcCritDef` + `iRKcCrit_defined`** (`InternalZ`): the `𝚺₁` graph + definability — closed first try
  (`by_cases` on `ti = zTag dᵢ`; reuses `cutFormulaDef`'s `k=π₁(π₂(tp dⱼ))`+`numeralGraph` threading).
  **THE arithmetization wall is down.**

### Remaining — the engine SWAP (steps 3–6, one atomic unit; breaks build mid-way, do NOT commit until green)
3. **Re-key `iRK`'s critical branch** `else iRKc d s` → `else iRKcCrit d` (`InternalZ:6719`), update `iRKDef`
   (`!iRKcDef y d s` → `!iRKcCritDef y d`, `InternalZ:6728`) + `iRK_defined` (swap `iRKc_defined.iff` →
   `iRKcCrit_defined.iff`). `iRKc`/`iRKcDef`/`iRKc_defined` then become DEAD (can delete or leave unused).
4. **`red_zK_crit`** (`InternalZ:~7327`) SIMPLIFIES to `red (zK s r ds) = iRKcCrit (zK s r ds)` — proof
   `rw [red_zK, iRK, zKseq_zK, if_neg hcrit]` (NO more `iRKc`-unfold / `iRcritG_congr` / `znth_redTable_eq_red`
   table substitution — `iRKcCrit` is table-free). This is a NET SIMPLIFICATION of the equation. **But it
   changes `red`'s critical value, so the 2 downstream consumers below break and must re-prove in the same
   unit.**
5. Close **`ZDerivation_red_zK_crit`** (`Crux2Blueprint:~517` sorry) via `red_zK_crit` (new form) +
   `iRKcCrit_eq_corr`/`_eq_neg` (case on `zTag dᵢ = 1`) + the two concrete-`ρ` soundness lemmas
   (`ZDerivation_iRcritG_critReductCorr` ∀ / `ZDerivation_iRcritGNeg_critReductNeg` ¬). **The real work here
   is the orbit data:** the soundness lemmas need `hdi`/`hdj`/`hIJ`/`hthread`/`hrank`/`hCwff`/`hd0ant`/Seq —
   a strengthened `redZKReady` motive at the call site (in `redSoundGen`'s zK-case, `Crux2Blueprint:~870`).
   This `redZKReady` orbit bundle is the genuine remaining residual (it is ALSO an open sorry independently).
6. **Descent re-point:** `iord_descent_red_zK_crit` (`RedZKDescent`, uses `red_zK_crit`+`iord_iRcritG_eq_iRcrit`)
   re-proves: `rw [red_zK_crit]` now exposes `iRKcCrit`; case on `zTag dᵢ=1` via `iRKcCrit_eq_corr`/`_eq_neg`,
   then the corrected-reduct descents **`iord_descent_iRcritG_critReductCorr` (∀) /
   `iord_descent_iRcritGNeg_critReductNeg` (¬) — BOTH banked** (the ¬ twin landed this lap). Both need the
   explicit redex forms (`hdi`/`hdj` zIall/zAxAll or zIneg/zAxNeg) + `hcut`/`hp`/`hirk`, derived at the call
   site by inverting `tp dᵢ = isymR` / `tp dⱼ = isymLk` (the redex-pair `tp` facts `redexPair_tp`) into the
   constructor forms (`tp_isymR_form_wff` etc.).

### ✅ ALL corrected-reduct lemmas are now BANKED (both polarities, both halves) — the swap is pure wiring
| | ∀ (I∀ R-redex) | ¬ (I¬ R-redex) |
|---|---|---|
| reduct supplier | `critReductCorr` | `critReductNeg` (red-free) |
| dispatched object | `iRKcCrit` (`zTag dᵢ=1` branch) | `iRKcCrit` (else branch) |
| 𝚺₁ arithmetization | `iRKcCrit_defined` (combined, lap 118) | ″ |
| bridge to supplier | `iRKcCrit_eq_corr` | `iRKcCrit_eq_neg` |
| SOUNDNESS | `ZDerivation_iRcritG_critReductCorr` | `ZDerivation_iRcritGNeg_critReductNeg` |
| iord re-point | `iord_iRcritG_eq_iRcrit` | `iord_iRcritGNeg_eq_iRcrit` |
| DESCENT | `iord_descent_iRcritG_critReductCorr` | `iord_descent_iRcritGNeg_critReductNeg` |

The remaining engine swap (steps 3–6) is now ONLY the in-place `iRK`/`iRKDef`/`iRK_defined`/`red_zK_crit`
edit + re-proving the 2 consumers by dispatching to the banked lemmas above + the `tp`-inversion plumbing
+ the `redZKReady` orbit bundle (the genuine residual to CLOSE `ZDerivation_red_zK_crit`).

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, 8 sorries — by NAME)
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` (THE prize; BOTH critical sub-cases' soundness now
proven AND hypothesis-light (∀ lap116, ¬ lap117–118); remaining = engine re-key only) · axNeg replace
validity · `ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH · M2
`foundation_bot_to_Z_empty` · M3 `false_of_ZDerivesEmpty`.

## Aristotle
Idle — CORRECT. The next piece (engine re-key + arithmetization) is engine-internal, not an isolated lemma.
