# HANDOFF — 2026-06-23 (lap 22, **D' DISCHARGED** — Thm 5.6 axiom-clean modulo only F-φ)

> **Branch** `plan` · HEAD `c1619f1` · build **green** (`lake build GoodsteinPA`, **1271 jobs**) ·
> headline `GoodsteinPA.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). Tree clean.

## ✅ Lap-22 deliverable — D' (`embed_TI_bounded`) fully discharged (8 green commits)
The lap-21 disclosed `sorry` **`Thm56.embed_TI_bounded`** (the embedded ordinal `< ε₀` — *the* Gentzen
content) is now a real machine-checked proof. **`#print axioms Thm56.peano_not_proves_TI =
[propext, Classical.choice, Quot.sound, rePred_ltPull_natCode]`** — the `sorryAx` is GONE. The single
remaining math axiom on Thm 5.6 is **F-φ** (`rePred_ltPull_natCode`, a definitional dep via the seam).

All new work is in **`src/GoodsteinPA/EmbeddingBound.lean`** (~600 lines, axiom-clean). The chips,
in dependency order:
1. **`PXFcFin` / `PXFcLt` bound-tracking layer** + ε₀-closure helpers (`add_one_lt_epsilon0`,
   `omega0_lt_epsilon0`, `add_lt_epsilon0` via ε₀'s additive principality `ω^ε₀=ε₀`, `natCast_lt_epsilon0`).
2. **`provable_em_cong_gen_x_bdd`** / **`provable_true_x_bdd`** — the complexity-driven leaf engines,
   re-proved returning **explicit** uniform heights `↑(2k)` / `↑k` (so their ω-rule families are literally
   constant).
3. **`subst_value_subst_bdd`**, **`exI_closed_bdd`**, **`provable_em_x_fin`** — finite-preserving helpers.
4. **`metaInduction_cong_bdd`** — THE single transfinite jump. The cut-tower chain is built at finite
   height per `n`; the ω-rule over the *non*-constant finite family lands at `⨆ₙ↑Nₙ + 1 ≤ ω+1 < ε₀`
   (`PXFc.allω_omega`). Returns the explicit v-uniform `ω+1`.
5. **`PXFc_allClosure_omega`** — strips `n` universal closures at explicit `B + n` (constant families).
6. **`exI_closed_lt`** — `<ε₀`-preserving (not finite) closed-∃ for the embedding's `exs` case.
7. **`hax_paLX_bdd`** — axiom discharge, uniform-over-`e` bound: X-free axioms at `↑complexity`,
   X-induction at `(ω+3)+↑fvSup`, all `e`-independent.
8. **`embedC_LX_gen_bdd`** + **`embedC_LX_bdd`** — the 10-case structural embedding threading a single
   `B < ε₀` (pushed up via `mono`); transfinite content enters only through `hax_paLX_bdd`.
9. **Wired** `Thm56.embed_TI_bounded` ⟹ Thm 5.6 `sorryAx`-free.

**Key insight that made it tractable:** structural rules add only finite bumps (`+1`/`max+1`/`⨆const+1`);
the height goes transfinite *only* through the induction axiom's cut-tower (`metaInduction`), where a
linearly-growing chain of *finite* heights sups to `ω`. So the whole bound is `< ε₀` and **uniform over
the closing assignment `e`** (it depends only on the derivation's structure).

## 🎯 Open obligations (priority order) — TWO walls left
1. **F-φ** `rePred_ltPull_natCode` (`SeamDefinability.lean`). **ON ARISTOTLE** (`aris_onotecmp`, UUID
   `16c9fc79-ae8b-4b04-8b83-2e8e9e5f38db`, still RUNNING). On return: VERIFY in-kernel + `#print axioms`,
   port. Crux = `Primcodable ONote` (structural) + `Primrec₂ ONote.cmp`. If still running next lap and
   blocked, attempt the `Primcodable.ofDenumerable` recursion-framework proof locally (see
   `ON-LINE-REQUEST.md`). Discharging it makes the **entire F girder** axiom-clean.
2. **E** `DescentE` (interface pinned in `Thm56.lean`): `𝗣𝗔 ⊢ γ → Nonempty (Derivation2 paLX {TI prec})`
   — the Goodstein→`TI(ε₀)` descent in PA, the deep crux. **Needs `papers/` reading**
   (Cichoń/Rathjen-2014/Agboola/Towsner). Map the wall, state per-step lemmas, formalize the first
   prerequisite. ~2–4 laps.
3. **G — done** (`peano_not_proves_goodstein_of_descent`). Discharge the headline `sorry` ONLY when E is
   real AND `#print axioms peano_not_proves_goodstein` is clean.

## ⚠️ Locked / notes
- **LOCKED untouched:** `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `sorry`.
- **src/ sorries (2):** `Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A, off-path).
  `EmbeddingBound.lean` + `Thm56.lean` are now **sorry-free**.

## 📊 Lap estimate to headline
F-φ ~0–1 (Aristotle) · E ~2–4 (dominant, needs literature) · G done. Everything from D' back +
C₂ + the §5 assembly is machine-checked + `sorryAx`-free.
