# Handoff: lap 137 — ALTITUDE REVIEW: existence-form termination half was MIS-TYPED (Gödel-barred); FIXED

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `ddda795` · build 🟢 green
(`lake build GoodsteinPA`, **1326**). Headline footprint UNCHANGED
(`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms).

## 🎯 This was a fresh-mind ALTITUDE review lap (per the review protocol)
Reviewed the lap-135 existence-form pivot + lap-136 Ind-reduct work. Set the binding `DIRECTION.md`
CURRENT DIRECTIVE, refreshed `STATUS.md`, prepended `PENDING_WORK.md` lap-137 — AND landed a real
structural proof-fix (one green commit).

## ⭐ THE finding (decisive, structural — drove the lap)
The lap-135 `prwo_forbids_existence_descent` concluded `False` in bare `[V ⊧ₘ* 𝗜𝚺₁]` with **NO PRWO/γ
hypothesis** — and is **UNPROVABLE as so typed**:
- its hypothesis `hstep` = `ZDerivesEmptyR_descent_step` (the per-step cut-reduction descent) is a genuine
  `𝗜𝚺₁` fact (cut-reduction lowering the ordinal is primrec/arithmetizable);
- so if `prwo_forbids_existence_descent` were ALSO `𝗜𝚺₁`-provable, then `𝗜𝚺₁ ⊢ ¬∃z, ZDerivesEmptyR z`
  ≈ `Con(𝗣𝗔)` (via M2 `Z ⊇ 𝗣𝗔`) — **Gödel-barred**.

`PRWO(ε₀)` is exactly the PA-unprovable principle and **must enter as a hypothesis**. The lap-136 grind
sank into the *other* (legitimately-`𝗜𝚺₁`) sub-sorry's Ind-reduct redesign (`iIndReductSeqG`) while this
structural hole in the termination half sat undiagnosed.

## ✅ FIXED this lap (green, banked — `Crux2Blueprint.lean`, after the `false_of_ZDerivesEmpty` block)
- **`InternalPRWO V`** (`def`): `∀ f : V→V, (𝚺₁-Function₁ f) → (∀ n, isNF (f n)) → ¬(∀ n : V, icmp (f(n+1)) (f n)=0)`
  — no `𝚺₁`-definable internal NF sequence is everywhere `icmp`-descending = **PRWO(ε₀)**. The honest crux-1
  deliverable (from `V ⊧ γ` via `bbeta`/`nonterminating_internal`). NOT an `𝗜𝚺₁` theorem.
- **`prwo_forbids_existence_descent (hprwo : InternalPRWO V) (hstep) {z} (hz)`** + **`false_of_ZDerivesEmpty
  (hprwo : InternalPRWO V) {z} (hz)`** — now sorry-FREE compositions threading `hprwo`.
- **`exists_sigma1_descent_of_step (hstep) {z} (hz) : ∃ f : V→V, (𝚺₁-Function₁ f) ∧ (∀ n, isNF (f n)) ∧
  (∀ n : V, icmp (f(n+1)) (f n)=0)`** — the NEW named sub-sorry = the genuine remaining termination content.

(⚠️ `𝚺₁-Function₁` notation has low arg precedence — wrap each application in parens: `(𝚺₁-Function₁ f)`.
And `def InternalPRWO` uses `variable (V) in` with the docstring placed AFTER the `in`.)

## 📌 NEXT — `exists_sigma1_descent_of_step` DECOMPOSED (A)+(B); (B) PROVEN modulo (B0) (lap 137, green)
`exists_sigma1_descent_of_step` = sorry-free `(A) ∘ (B)`. **(B) is now FULLY PROVEN modulo the single linchpin
(B0)** — descent, NF, membership-induction, and both definability side-goals all closed this lap. **Two open:**
- **(B0) `exists_sigma1_iterate`** (ATTACK NEXT — the Foundation-engineering linchpin): `𝚺₁-Function₁ g →
  ∃ orbit, 𝚺₁-Function₁ orbit ∧ orbit 0 = z ∧ ∀ n:V, orbit(n+1)=g(orbit n)`. Manual `PR.Construction`
  (`iotower`/`zRegTable` template, `Blueprint 1` param `z`, `succ := g`). ⚠️ **Subtlety:** `Blueprint.succ` is
  PARAMETER-FREE `Semisentence`, but `𝚺₁-Function₁ g` (`Definable`) gives a PARAMETRIZED `Semiformula V k`.
  **Fix: strengthen the hyp to parameter-free `DefinedFunction₁ g φ` (explicit `φ : 𝚺₁.Semisentence 2`)** —
  supplied by the concrete (A) step (iord/icmp/ZDerivesEmptyR all param-free); then `Blueprint.succ := φ` rewired.
  No generic Foundation iteration combinator exists (HFS checked).
- **(A) `exists_sigma1_descending_step`** — the total `𝚺₁` descending step `g`. ⚠️ definability crux:
  `μ d'. [ZDerivesEmptyR d' ∧ icmp(iord d')(iord w)=0]` has a `𝚫₁` matrix (minimality `𝚫₁`) but a `𝚺₁` totality
  guard `∃d', P w d'` (unbounded witness) = wrong polarity. Fix = primrec **witness bound** ⟹ bounded-`μ` `𝚫₁`,
  OR a **constructive** reduct from `ZDerivesEmptyR_descent_step` (Ind=`red d`, K=critical) → `g` directly (no `μ`).

NB (A) and (B0) likely SHARE the parameter-free-`φ` need: the constructive (A) step gives `g` via a param-free
`φ`, which is exactly what (B0) wants — so resolving (A) constructively may hand (B0) its `φ` for free.

(Background — the original 3-step plan, now realized as (A)+(B0)+(B): the never-built M3 internalization of
`iord_iR2_iterate_descends` `InternalZ:9816` over internal `n : V`.)
1. **`redLeast : V → V`** = `μ d'. [ZDerivesEmptyR d' ∧ icmp (iord d') (iord d) = 0]`. The matrix is `𝚫₁`
   (`ZDerivesEmptyR` is `𝚫₁`: `ZDerivation` `𝚫₁` `InternalZ:5542`; `ZRegular`/`ZFresh`/`ZSeqAnt` = `zReg`/
   `zFresh`/`zSeqAnt` `= 0`, each `𝚺₁`-`Function₁` `Zsubst:1298`+; `iord` `𝚺₁` `InternalZ:2536`; `icmp _=0` `𝚫₁`).
   Totality on the orbit from `hstep`. Extract via the `𝚺₁` least-number principle (`sigma1_pos_succ_induction`
   is already used in `DescentArith`; look for Foundation's LNP / `findLeast` analog in `ISigma1`).
2. **internal `𝚺₁` orbit `redOrbit z : V → V`** = `n ↦ redLeast^[n] z` over internal `n : V`, built as a
   **`PR.Construction`** course-of-values recursion (the repo's mechanism — see `idgTable`/`zRegTable`/
   `maxEigenTable` in `Zsubst.lean:779`+, `InternalZ`). THIS is the load-bearing internalization. Likely its
   own sub-tower (decompose further) — but it is the RIGHT multi-lap piece (the termination internalization).
3. **`f n := iord (redOrbit z n)`** — `𝚺₁` (iord ∘ `𝚺₁`); NF via `isNF_iotower` on `isNF_iotil_of_ZDerivation`
   (orbit points are `ZDerivesEmptyR` ⟹ `ZDerivation`); descends via `hstep` at each orbit point (kept in
   `ZDerivesEmptyR` by closure under the chosen reduct).

**Why PRIMARY:** closing this VALIDATES the existence-form pivot end-to-end. If it WALLS (e.g. the `𝚺₁`-orbit
can't be built), the pivot is hollow and we must know that BEFORE more `descent_step_K_majorIdx` grind.

## Secondary front (genuinely required; resume after the termination half is locked)
`descent_step_K_majorIdx` (`Crux2Blueprint:1613`) — the per-step `𝗜𝚺₁` cut-reduction descent. tag-3 Ind
soundness via the corrected `iIndReductSeqG` (lap-136, mid-redesign; assemble `zKValidF_iIndReductSeqG` per
the lap-136 handoff). tag-4 (chain) = the deep core, untouched. tag-5/6 = banked `cutPartner` + `hAll`.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT undo the PRWO-seam type-correction (re-stating `→ False` in bare `𝗜𝚺₁` is Gödel-barred).
- Do NOT re-derive the deterministic `red`-fixpoint stall — the existence form RETIRED it.
- Do NOT attack M2 `foundation_bot_to_Z_empty` / M3 wiring / `InternalPRWO`-from-crux-1 now (directive).

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `InternalPRWO` + `exists_sigma1_descent_of_step` +
  type-corrected `prwo_forbids_existence_descent`/`false_of_ZDerivesEmpty` (right after the
  `ZDerivesEmptyR_descent_step` block, ~:1640-1690). Other open crux sorries: `descent_step_K_majorIdx`
  (:1613), `descent_step_K_majorIdx`'s Ind residual `zKValidF_iIndReduct_of_zInd` (:81, FALSE — keep).
- `src/GoodsteinPA/InternalZ.lean`: `iord_iR2_iterate_descends` (:9816, the external-ℕ descent to internalize),
  `iord_defined` (:2536), `ZDerivation_defined` (:5542), `isNF_iotil_of_ZDerivation`, `iord` (:2531).
- `src/GoodsteinPA/Zsubst.lean`: `zReg_defined` (:1298), `PR.Construction` examples (:779+).
- `DIRECTION.md` CURRENT DIRECTIVE (lap-137) · `PENDING_WORK.md` lap-137 · `STATUS.md` lap-137 box.

---
**→ Next session: HEAD `ddda795`, build green. The existence-form termination half is now CORRECTLY TYPED
(PRWO is a hypothesis, not a Gödel-barred goal). PRIMARY = discharge `exists_sigma1_descent_of_step` (build
the `𝚺₁` `ε₀`-descent: `redLeast` μ-witness → internal `𝚺₁` orbit via `PR.Construction` → `iord∘orbit`).
Do NOT re-state the termination half as `→ False` in bare `𝗜𝚺₁`.**
