# Handoff: lap 209 — FRESH-MIND REVIEW + `S*`-domination half-built (padded engine + base slot + collapse helpers GREEN)

**Branch**: `plan` · **HEAD**: `3d1ab04` · **Date**: 2026-07-03 · Tree CLEAN, nothing uncommitted.
**Build**: 🟢 bare `lake build` 1342 (src UNCHANGED this lap — all edits in `wip/HardyMajorization.lean`,
verified green via `lake env lean`). Headline `[propext, Classical.choice, goodstein_implies_consistency,
Quot.sound]` sorryAx OFF, undrifted (re-verified real `#print axioms`).

**Directive (OUTRANKS this)**: `DIRECTION.md` CURRENT DIRECTIVE **lap-209 block** — MANDATE = the 2b
COMPOSITION, `S*`-domination FIRST. Do NOT self-ratify rung-E artifacts into `src`.

## ✅ This lap (7 green commits)

1. **Review** (`a792f13`): route (c) read-off (lap 207) + 2b growth-conversion crux (lap 208) both
   confirmed CLOSED and kernel-clean, hardest-first, no drift. Set the binding DIRECTION lap-209 block;
   refreshed STATUS (header/where-it-stands/what's-happened/outstanding/ledger stamps).
2. **Decisive-case finding** (`4f1f695`): the `S*`-domination hits a **z=0 constant-floor wall** —
   `hEng_of_dom` needs bare `∀z, f z ≤ H_{ω^{e₀}}(z)`, but the pipeline slot has a big constant floor at
   0 (`ewRootSlot e B K ≥ 3`, preserved by the tower's `ewIter` floor + `Sslot`'s max) while
   `hardy ω 0 = 1`. Fix = a PADDED engine.
3. **Padded engine** (`20c232d`): `hEng_of_fx` (abstract core = `hEng_of_dom`'s proof parameterized by
   the raised-arg domination `∀x, f x ≤ H_{ω^{e₀}}(x+p)`), `hEng_of_dom_pad` (hyp `∀z, f z ≤ H_{ω^{e₀}}(z+c)`,
   needs `c ≤ p`), `ewIter_hardy_le_of_dom_pad` (end-to-end, pad `= norm-const + 8 + c`). Banked bare
   `hEng_of_dom`/`ewIter_hardy_le_of_dom` UNTOUCHED.
4. **Base slot** (`46134d3`): `ewRootSlot_dom_pad` — `ewRootSlot e m x ≤ H_{ω^{(e+1)+2}}(x + P_R)`,
   `P_R = norm-const(e+1) + 8 + (m + norm e)`. Via `e_lt_Wpow_succ` (`e < ω^{e+1}`) + `hardy_maxpad`
   (`hardy e (max m ·) ≤ H_{ω^{e+1}}(· + (m+norm e))`) → feed `f z := hardy e (max m z)` to
   `hEng_of_dom_pad`; `2x+2f+3 ≤ engine LHS` since `x ≤ f ≤ 2^{f+1}`.
5. **Tower-collapse helpers** (`3d1ab04`): `rel1_dom_pad` (`max K` folds into pad),
   `Wpow_add_lt_Wpow_succ` (`ω^A+ω^B < ω^{A+1}` for `B<A`), `hardy_double_collapse`
   (`H_{ω^A}(H_{ω^B} y) = H_{ω^A+ω^B} y` for `B<A`, via `hardy_add_comp`).
   All new lemmas `[propext, Classical.choice, Quot.sound]`.

## 🚦 NEXT — resume here (full design in `PENDING_WORK.md` lap-209 top)

**`ewIterTower_dom_pad`** (the remaining hard piece; all its bricks are now green + in hand:
`ewIter_hardy_le_of_dom_pad`, `rel1_dom_pad`, `Wpow_add_lt_Wpow_succ`, `hardy_double_collapse`,
`collapseIter_NF`). Target (existential form, so E'/c' need not be tracked exactly):
```
theorem ewIterTower_dom_pad {g : ℕ → ℕ} {E : ONote} {c : ℕ} (hE : E.NF) (hE0 : E ≠ 0)
    (hg : ∀ x, g x ≤ hardy (Wpow E) (x + c)) (α : ONote) (hα : α.NF) :
    ∀ d, ∃ (E' : ONote) (c' : ℕ), E'.NF ∧ E' ≠ 0 ∧
      ∀ x, ewIterTower g d α x ≤ hardy (Wpow E') (x + c')
```
- Base `d=0`: `ewIterTower g 0 α = g` (rfl) ⟹ `⟨E, c, …, hg⟩`.
- Step `d+1`: `ewIterTower g (d+1) α = ewIter (ewIterTower g d α) (collapseIter d α)`. From the IH
  `⟨E_d, c_d, …⟩`, apply `ewIter_hardy_le_of_dom_pad` (needs `collapseIter d α` NF via `collapseIter_NF`)
  ⟹ DOUBLE hardy `H_{ω^A}(H_{ω^B}(y))`, `A = E_d+2+1+collapseIter d α`, `B = E_d+2`, `y = Nlog(collapseIter d α)+x+p_d`.
  Then: `hardy_double_collapse` (B<A: `E_d+2 < E_d+2+1+collapseIter d α` — prove at repr level, `1+γ>0`)
  ⟹ `H_{ω^A+ω^B}(y)`; raise ARGUMENT `y ≤ x + c'` (monotone) then `hardy_le_of_lt` with
  `Wpow_add_lt_Wpow_succ` (`ω^A+ω^B < ω^{A+1}`) at gate `norm(ω^A+ω^B) ≤ x + c'`.
  **Set `E' := A+1`, `c' := Nlog(collapseIter d α) + p_d + norm(Wpow A + Wpow B)`** (covers BOTH
  `y ≤ x+c'` since `Nlog+p_d ≤ c'` AND the gate `norm ≤ c' ≤ x+c'`). `E' = A+1 ≠ 0` (`…+1`), NF.
  ⚠️ The gate needs `norm(Wpow A + Wpow B)` which is NOT in `p_d` — that's exactly why `c'` explicitly
  adds it (don't try to pay the gate from `p_d`/`y` alone; `y` only carries `Nlog`, not `norm`).

**Then** (`wip/E1EmbeddingGrind.lean`, which has `Sslot`/`P*` — the two wip modules can't import each
other, so this half lives there or is stated with the tower bound as a hypothesis):
- `P*`-domination: `P* = gvb goodsteinBodyE`, a FIXED formula ⟹ `gvb` elementary in its arg ⟹
  `∀z, P* z ≤ H_{ω^{e₀}}(z + c_P)` (loose is fine).
- `Sslot_dom_pad`: `S* z = max(tower z, P* z)` ≤ max of the two Hardy bounds ≤ single `H_{ω^{E*}}(z+c*)`
  (take the larger level, `hardy_le_of_lt` to align).
- Then `ewIter_hardy_le_of_dom_pad` on `S*` ⟹ `goodsteinLength m ≤ n ≤ H_{fixed}(H_{fixed}(g m))`.
- **2b(c)** Sslot assembly (`Sslot_mono_slot` [may need proving] + `ewIter_mono_slot` [banked]).
- **2b(e)** EventuallyLE: one fixed `fastGrowing o` dominates via `hardy_le_fastGrowing` /
  `hardy_omega_pow_lt_fastGrowing`; contradiction with
  `WainerRoute.goodsteinLength_eventually_dominates_fixed_fastGrowing`.
- Then the ONE judge package. Do NOT self-ratify into `src`.

## Gotchas fresh this lap
- `Wpow`/`hardy`/`ewRootSlot`/`ewIterTower` all live in `wip/HardyMajorization.lean`'s import closure
  (it imports `GoodsteinPA.OperatorZef2`); a probe file importing only `GoodsteinPA` CANNOT see `Wpow`
  (wip-only) — develop these IN the wip file, or inline `oadd x 1 0` for `Wpow x`.
- `hardy` is in namespace `GoodsteinPA.FastGrowing`; `hardy ω 0 = hardy 1 0 = 1` (`ω[i]=ofNat(i+1)`).
- `ONote.repr_add` needs `[NF]` instances for BOTH summands — provide `haveI : (Wpow A).NF := Wpow_NF hA`.
- `a + 1 ≠ 0` (Ordinal): `(lt_of_lt_of_le zero_lt_one le_add_self).ne'`; after `rw [ONote.repr_one]` the
  `1` shows as a cast `↑1` — `push_cast at hh` first.
- `Nat.lt_two_pow` is GONE → `Nat.lt_two_pow_self : n < 2^n`. `Ordinal.zero_lt_one` unknown → `zero_lt_one`.
- `dsimp only at h` ERRORS if it makes no progress; Lean already beta-reduces `(fun z => …) x` in
  `have h := hEng x`, so omega sees `hardy e (max m x)` directly — don't add a dsimp.
