# HANDOFF — 2026-06-24 (lap 59) — FRESH-MIND review + crux-2 natural-sum order theory through **F1**

> **Branch** `plan` · **HEAD `04d10a7`** · tree **clean** · `lake build GoodsteinPA` **green (1318 jobs)**.

## ⭐⭐ F1 (strict `#`-monotonicity) DONE this lap — the deep §4 descent order fact
After foundations + `icmp_trans`, drove the crux all the way to **F1**: `inadd_left_mono`
(`icmp X Y = 0 → icmp (g#X)(g#Y) = 0`, NF g,X,Y). Chain (all green, axiom-clean, `wip/InternalNadd.lean`):
`icmp_zero_of_exp_zero`/`icmp_term_insTerm` (base) → `icmp_trans` (order spine) → `icmp_insTerm_left`
(ea≺eb, non-recursive 3×3 grid) → `icmp_insTerm_mono(_aux)` (pair order-induction; ea=eb recurses on
tails) → `inadd_left_mono` (induct on g). **This is the order fact Buchholz Lemma 4.1/Thm 4.2 consumes
in every descent case** (replace one summand by a ≺-smaller one ⟹ the natural sum drops). The old
"next lap = icmp_insTerm_mono" is now DONE; see updated NEXT below (F2/F3/F4).

> **earlier-this-lap HEAD `9139c2b`** · tree **clean** · `lake build GoodsteinPA` **green (1318 jobs)**.
> `wip/InternalNadd.lean` `lake env lean` **green, 0 sorries, axiom-clean** `[propext,choice,Quot.sound]`.
> Headline `peano_not_proves_goodstein` untouched (honest `sorry`; real `#print axioms` re-verified
> `[propext, sorryAx, choice, Quot.sound]`, **0 math axioms**, anti-fraud intact).
> Best-case honest endpoint unchanged: `[propext, choice, Quot.sound, PA_delta1Definable]`.
>
> 8 verified commits this lap (`4141ade`…`9139c2b`). All crux-2 `wip/InternalNadd.lean` + docs.

## ⭐ Post-review continuation (same lap): `icmp_trans` + strict-mono base cases LANDED
After the review, kept driving the crux. **Banked (all green, axiom-clean), `wip/InternalNadd.lean`:**
- **`icmp_trans`** — general `≺`-transitivity (`icmp a b=0→icmp b c=0→icmp a c=0`), which
  `InternalONote` had *deliberately avoided*. Lexicographic on `(exp,coeff,tail)`, strong induction
  recursing into exponents+tails. **This is the order spine** for everything below (and F2/comm).
- **`icmp_zero_of_exp_zero`** — lead exp `≺` decides `≺`.
- **`icmp_term_insTerm`** — the `A=0` dominance base case (`ω^e·n ≺ insTerm e n B`, NF `B≠0`).

**⟹ The crux `icmp_insTerm_mono` is now unblocked** (all prerequisites in hand). NEXT (below) supersedes
the old "discharge `icmp_insTerm_congr`" plan: target the strict-mono *direction* (avoids the full
congruence's harder mixed cases), now feasible with `icmp_trans`.

## Lap-59 = FRESH-MIND REVIEW (direction VALIDATED + one reprioritization)
Re-verified the kernel from real `#print axioms` (above). Direction is sound: crux 2 (`PRWO→Con`, Buchholz
ord/R inside PA) is the hard generational crux; lap 58's internal Hessenberg natural sum `#`
(`wip/InternalNadd.lean`) for Buchholz's `õ` is the right chip. STATUS.md + PENDING_WORK.md refreshed.

**Banked this lap (all green, axiom-clean), continuing `wip/InternalNadd.lean`:**
- NF preservation: `isNF_insTerm`, `isNF_inadd` (order-induction through the 3 `insTerm_ocOadd` branches).
- Unit/prepend: `insTerm_prepend`, `inadd_zero_right`.
- ω-power layer: `thenV_one_right`, `icmp_omega_pow` (`icmp(ω^α)(ω^β)=icmp α β`), `inadd_omega_pow`,
  `inadd_single_term` (`(ω^e·n)#b = insTerm e n b`), `cmpV_add_left` (comparison invariant under common
  left summand).

**Reprioritization (acted on): ORDER > iC for crux 2.**
- The §4 descent (Buchholz Thm 4.2 / Lemma 4.1, now read verbatim, `scratchpad/buchholz-gentzen.txt:781-822`)
  consumes `#`'s ORDER laws, NOT `iC (a#b)≤iC a+iC b`. The iC bound is crux-1 width control; at most C4.
- `iC_inadd` does **not** follow from the naive `insTerm`-fold (over-counts the inserted coeff vs the whole
  accumulator; the true bound needs the NF leading-exponent structure). Deferred.

## NEXT LAP (hardest-first) — F2, F3, then F4 (commutativity), then promote + C0/C1
F1 DONE (`inadd_left_mono`). Remaining §4 `#`-facts (pinned in `PENDING_WORK.md`), now short with
`icmp_trans` + `icmp_omega_pow` + the `#` infra in hand:
1. **F3** `ω^β·k ≺ ω^{β+1}` (Ind rule, case 4): `icmp (ocOadd β k 0) (ocOadd β' 1 0) = 0` where
   `β' = β+1` (ordinal successor of the pre-ordinal — pin its CODE: `iadd β (ocOadd 0 1 0)` or the
   `õ(d1)+1` shape; check `iadd_one_eq`). Reduces to the exponent compare `icmp β β' = 0` via
   `icmp_ocOadd` (heads differ) — i.e. `β ≺ β+1`. Likely a couple lines.
2. **F2** `αi0,αi1 ≺ αi → icmp (ω^{αi0} # ω^{αi1}) (ω^{αi}) = 0`. `ω^{αi0}#ω^{αi1} = insTerm αi0 1
   (ω^{αi1})` (`inadd_omega_pow`); its lead exp is `max(αi0,αi1) ≺ αi`, so `icmp_zero_of_exp_zero`
   (need its `ocExp` via `ocExp_insTerm` + `icmp_trans`). Two-term CNF below `ω^{αi}`.
3. **F4** commutativity `inadd a b = inadd b a` (NF) — the other hard nut (NF canonical-form
   uniqueness). `icmp_trans`/`inadd_left_mono` help; may be avoidable per-descent-case by reordering
   summands syntactically. Defer if the C-milestones don't need it.
4. Then **promote `InternalNadd` → `src/`** (+ `GoodsteinPA.lean` aggregator; it's a general
   `InternalONote` extension — `icmp_trans` especially belongs in `src/InternalONote.lean` eventually)
   and start **C0/C1** (arithmetize system Z + `iõ`/`idg`/`iord`), per `CRUX2-ORD-ASSIGNMENT-2026-06-24.md`.
- ALT (parallel worktree): start C0 (arithmetize system Z `ZDerivation : V→Prop`), independent of `#`.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/` is wip. Aristotle OFFLINE. WebFetch dead. ⚠️ NEVER `git checkout <wip file>` — `cp` from scratchpad.
Crux 1 (`seqDescent_dominated`, tractable milestone) untouched this lap — see lap-56 STATUS box.
