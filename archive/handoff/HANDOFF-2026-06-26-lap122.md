# HANDOFF — lap 122 · generalized redex finder ASSEMBLED; the "L-axiom caveat" REFUTED

> **RESUME BATON.** Branch `plan`, HEAD = this lap's commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint unchanged (only one additive theorem in
> `InternalZ`): no `sorry` touched, 11 disclosed sorries as in lap 121. `peano_not_proves_goodstein`
> still `[propext, sorryAx, choice, Quot.sound]` (0 math axioms); the new theorem verified
> `[propext, Classical.choice, Quot.sound]`.

## What this lap did (4 on-crux commits + docs)
Attacked THE crux (the `red`-stall / `false_of_ZDerivesEmpty`), executing the lap-121 program and then
consolidating the whole leaf-stall case. **Banked 4 axiom-clean theorems** (all `[propext, choice, Quot.sound]`),
in `InternalZ`, in dependency order:

1. **`inference_critical_pair_of_chain_reroute`** (after `exit_nonRep_of_reroute`) = `inference_critical_pair_of_chain`
   with its `hnperm` **isymRep clause replaced by `hreroute`** (`∀ i ≤ j0, tp(znth ds i) = isymRep →
   ∃ i' < i, chainAsucc ds i' = chainAsucc ds i`). Produces the genuine `(R_A, L^k_A)` redex with `0 < rk A ≤ r`.
   Step A = `exit_nonRep_of_reroute`; Step B = least left symbol over `tpSeq ds`; Step C = least cut-formula
   source via `least_number` on `chainAsucc ds · = B`.
2. **`iord_descent_iRcrit_of_chain_reroute`** (after the finder) — the reroute twin of `iord_descent_iRcrit_of_chain'`:
   manufactures the redex via the finder, feeds `iord_descent_iRcrit_of_redex`. Stall-free K-descent, modulo `hreroute`.
3. **`hreroute_of_leaves`** (before the finder) — packages `chainAsucc_threaded_of_leaf` into the `hreroute`
   shape; reduces `hreroute` to `hleaves` (every `isymRep` premise ≤ j0 is an atom/`zAx1` leaf).
4. **`inference_critical_pair_of_botChain`** (after the descent wiring) — THE consolidation: a `ZDerivation
   (zK s r ds)` with `seqAnt s = ∅`, `seqSucc s = ⊥` contains the genuine redex **provided `hleaves`**. ALL
   finder bookkeeping discharged internally from `ZDerivation`; `hnperm2` is FREE for a ⊥-chain. The leaf-stall
   redex existence now reduces to JUST `hleaves`.

## ⭐ THE decisive result — the lap-121 "L-axiom caveat" is REFUTED
The lap-121 baton flagged a genuine risk that could sink path (i): "the threading may bottom out at an
`isymLk` L-axiom, not an `isymR(B)` producer." **It cannot.** Because Step B takes the LEAST left symbol
`j`, the cut formula `B` re-routes to its least source `i' ≤ i < j`, strictly below `j`, where there are NO
left symbols by minimality. So the source is never an L-axiom; it is forced to `R_B`. Path (i) is sound on
that front.

## ⭐ ARCHITECTURE CLARIFICATION (late lap) — the chain/Ind residual is NOT a finder gap
A chain (tag-3) / Ind (tag-4) `isymRep` premise makes the ⊥-chain NON-critical. Buchholz handles
non-critical chains by case 5.2 (the SPLICE into the permissible premise — smaller ordinal), NOT by the
Lemma-3.1 redex finder (case 5.1). So the reroute finder is *supposed* to handle only the leaf-stall
(case 5.1, re-routable atom/`zAx1`); the chain/Ind case routes to the EXISTING splice machinery
(`ZDerivation_red_zK_splice` / `iord_descent_red` chain-REPLACE — already sorries). `hleaves` is the
criticality-modulo-leaves side of the case-5.1/5.2 dichotomy, not a hole inside the finder.

## Next-lap targets (in order)
1. **[lap 123] The case-5.1/5.2 dichotomy at the descent level.** Valid ⊥-chain: EITHER all `isymRep`
   premises ≤ j0 are leaves (→ `inference_critical_pair_of_botChain` → redex → `iord_descent_iRcrit_of_redex`,
   case 5.1 — BANKED modulo the 6 ρ-facts), OR ∃ a non-leaf chain/Ind `isymRep` premise (→ case 5.2 splice,
   the existing sorries). Build the dichotomy + wire the leaf branch into the endgame.
2. The 6 ρ-facts (redex-premise reduct soundness) — the remaining deep input on the leaf branch.
3. Re-point `false_of_ZDerivesEmpty` (M3) strict-descent disjunct onto the stall-free track.
4. Sibling: `zKValidF_iIndReduct_of_zInd` (lap-120: false as stated; fix via `zsubst`). Then
   `foundation_bot_to_Z_empty` (M2) + `gentzenDescentφ`.

## Definability gotcha banked (lap 122)
`simp only [isymIsL]; definability` on `isymIsL (tp (znth ds x))` TIMES OUT (aesop depth-blowup on `tp`).
Fix: search over the coded `tpSeq ds` via `znth (tpSeq ds) x` (`znth_tpSeq` bridge), AND
`set Is := tpSeq ds; clear_value Is` BEFORE `definability` (else it recurses into `tpSeq`'s `PR.Construction`).
Theorem also needs `set_option maxHeartbeats 1000000`. See memory `definability-aesop-depth-blowup`.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open piece (`hreroute` for chain/Ind, path α) is engine-internal, not a portable lemma.

## Pointers
`PENDING_WORK.md` lap-122 (full residual + attack paths α/β + definability recipe) ·
`HANDOFF-2026-06-26-lap121.md` (the Sub-lemma decomposition this lap executed) · `STATUS.md`.
