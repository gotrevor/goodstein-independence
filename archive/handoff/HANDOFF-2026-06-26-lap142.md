# Handoff: lap 142 — CRITICAL ∀-case soundness PROVEN via the genuine `iRKcCrit`, orbit-only (no `red`/`redSound`)

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (`lake build GoodsteinPA`).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms). New lemmas `#print axioms`-clean (`[propext, Classical.choice, Quot.sound]`, **no `sorryAx`**).

## ✅ THE advance — the operator's existence-form spike, REALIZED in-kernel for the dominant critical sub-case
`ZDerivation_iRKcCrit_critical_all` (`Crux2Blueprint`, **sorry-FREE**, `#print axioms`-clean) proves: for a
regular `∅→⊥` chain that is critical (`¬ permIdx < lh ds`) whose R-redex is an `I∀`, the GENUINE re-keyed
critical reduct `iRKcCrit (zK s r ds)` is a `ZDerivation`, derived **purely from the orbit**
(`ZDerivesEmptyR + criticality`), reusing the BANKED per-reduct soundness `ZDerivation_iRKcCrit_all`
(laps 112-119) **with NO dependence on `red`/`redSound`** and **NO selection-correctness campaign**.

This settles the operator's SOLE-OBJECTIVE spike — "reframe to the existence form reusing the banked per-reduct
soundness, obviating the selection campaign" — **decisively in kernel for the ∀-case** (the dominant critical
sub-case). The `red`-route's critical soundness `ZDerivation_red_zK_crit` (`:1108`) is FALSE-as-stated (lap-114
instance-`0` shadow); the iRKcCrit route SIDESTEPS it entirely.

## ⭐ What unblocked it — the lap-130 "exact-shape obstruction" was STALE
The lap-130 docstrings (`InternalZ:1553`, `Crux2Blueprint`) claimed `seqSucc sⱼ = cutFormula` (the `hAll` input
to `ZDerivation_iRKcCrit_*`) was "not yet derivable from `zKValid`" because the `zAxAll` ZPhi disjunct carried
only `∀p ∈ Γ`. **That strengthening already landed:** the `zAxAll` disjunct (`InternalZ:5372`) NOW carries
`zAxAllSuccWff s p k` (`= seqSucc s = F(k)`). The obstruction is GONE. Lap-142 mechanics:
- **`redZKReady_of_zKValid` extended** — its ∀-branch now ALSO yields `seqSucc sⱼ = cutFormula` (via
  `zDerivation_zAxAll_inv`'s `zAxAllSuccWff`; the instance index `k' = π₁(π₂(tp dⱼ))` matches `cutFormula`'s
  readout exactly). `zDerivation_zAxAll_inv` was HOISTED above `redZKReady`. 2 destructure sites updated.
- **`chainInf_redexI_data`** (NEW, refactored out of `irk_chainAsucc_redexI_le`) — exposes the `isChainInf`
  tip `j0` with `redexI < j0` + threading/rank up to `j0`. The ∀-case needs threading only up to `redexI`
  (`ZDerivation_iRcritG_corrected`'s `haux1` L-half takes NONE), so `redexI < j0` suffices.

## 🎯 THE remaining critical residual — the ¬-case needs `redexJ ≤ j0` (NOT free from `zKValid`)
The ¬-case (`I¬` R-redex + `axNeg` L-redex) routes through `ZDerivation_iRKcCrit_neg`, whose succedent half
`ZDerivation_corrected_haux0_neg` REPLACES premise `redexJ`, so it needs threading **up to `redexJ`**. From
`zKValid`'s `isChainInf` we only get threading up to the existential tip `j0`, and `redexJ ≤ j0` is **genuinely
not provable** in general — the minimal redex `⟪redexI, redexJ⟫` could have `redexJ > j0` (a redex `⟪0, big⟫`
with `big > j0` has smaller pair-code than a `j0`-region redex). The ∀-case dodges this (its threading-heavy
half is at `redexI < j0`, provable).

**NEXT ATTACK (hardest-first):**
1. **Prove `redexJ ≤ j0` / `j0 = lh ds − 1` for ⊥-orbit chains.** Genuine reducts build last-premise-tip chains
   (`isChainInf_of_last`; `isChainInf_iCritReductSeq`: source FIRST, cut-user LAST), so `j0 = lh ds − 1` and
   `redexJ < lh ds = j0+1`. `zKValid` stores only the existential `j0` — so either (a) strengthen the ⊥-orbit
   chain invariant to pin `j0 = lh ds−1` (hereditary through embedding + reducts), or (b) weaken
   `ZDerivation_corrected_haux0_neg` to need threading only up to `min(redexJ, j0)`.
2. Then `ZDerivation_iRKcCrit_botOrbit` discharges BOTH cases → re-route `descent_step_K_critical` to witness
   `iRKcCrit` (needs `ZSeqAnt_iRKcCrit` — derive next to the banked `ZRegular_iRKcCrit_of_zK`/`ZFresh_iRKcCrit`/
   `fstIdx_iRKcCrit` + the banked descent `iord_descent_iRKcCrit_corr/_neg`), dropping the critical case's
   dependence on the false `:1108`.
3. The Ind ROOT of `ZDerivesEmptyR_descent_step` also routes through `red`/`redSound`→ the false
   `zKValidF_iIndReduct_of_zInd` (`Crux2Blueprint:80`, lap-136): genuine fix = the recursive predecessor cut.
   Once BOTH critical-K (iRKcCrit) and Ind (corrected reduct) are off `red`, `redSound` LEAVES the existence-form
   termination path (`false_of_ZDerivesEmpty`), dropping its false 1108/82 + selection-campaign sorries from it.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT try to prove `ZDerivation_red_zK_crit` (`:1108`) or `zKValidF_iIndReduct_of_zInd` (`:80`) as stated —
  both are FALSE (instance-`0` / `k=1`-chain ordinal shadows). The genuine route is the iRKcCrit / corrected-Ind
  reduct, NOT the `red` engine.
- Do NOT revert the critical/non-critical restructure (lap 141) or re-attempt `redZKReady_of_zKValid` global
  shortcut / `redLeast`-μ (wrong polarity).

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `ZDerivation_iRKcCrit_critical_all` (the lap-142 ∀-case soundness, just
  before `descent_step_K_critical`); `ZDerivation_iRKcCrit_botOrbit` (both-case soundness, needs redexJ-threading);
  `descent_step_K_critical` (still via `red`, to be re-routed); `ZDerivation_red_zK_crit` (`:1108`, false-as-stated).
- `src/GoodsteinPA/InternalZ.lean`: `chainInf_redexI_data` + `irk_chainAsucc_redexI_le` (corollary);
  `redZKReady_of_zKValid` (extended ∀-branch with `seqSucc sⱼ = cutFormula`); `zDerivation_zAxAll_inv` (hoisted);
  `ZDerivation_iRKcCrit_all`/`_neg`/`_of_zKValid`/`_of_isChainInf`/`_botOrbit` (banked iRKcCrit soundness).
- `src/GoodsteinPA/RedZKDescent.lean`: `iord_descent_iRKcCrit_corr`/`_neg` (banked iRKcCrit descent).
- `src/GoodsteinPA/Zsubst.lean`: `ZRegular_iRKcCrit_of_zK` (2516), `ZFresh_iRKcCrit` (3344).
- `PENDING_WORK.md` lap-142 · `STATUS.md` lap-142 banner.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. The critical ∀-case
soundness is PROVEN via `iRKcCrit` orbit-only (`ZDerivation_iRKcCrit_critical_all`, sorry-free, axiom-clean). The
operator's existence-form spike is realized in kernel for the dominant sub-case. FIRST move: prove `redexJ ≤ j0`
(or `j0 = lh ds−1`) for ⊥-orbit chains to close the ¬-case, then re-route `descent_step_K_critical` to `iRKcCrit`,
dropping the critical case off `red`/false-`:1108`.**
