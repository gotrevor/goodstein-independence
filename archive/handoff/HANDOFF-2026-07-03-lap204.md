# Handoff: SERIES-3 lap 204 — TC PASS PORT COMPLETE: passAuxTC + rankToZero_TC green; E-seam down to the bounded readoff + splice

**Date**: 2026-07-03 · **Branch**: `plan` · **HEAD**: `139f35d` · **Tree**: CLEAN ·
**5 green commits this lap** (`71557dc` stepAnd/stepVerum, `7257be0` splice-free atomic cut,
`8572767` running ∀/∃ reduction + Zef2TCProv, `317e391` **passAuxTC**, `139f35d`
**rankToZero_TC + sound0_TC**).
**Build**: 🟢 bare `lake build` (1342 jobs). **Headline** unchanged/undrifted.
All new theorems `[propext, Classical.choice, Quot.sound]`, no sorryAx; all in
`wip/E1EmbeddingGrind.lean` (~4300 lines; `#print axioms` block at EOF re-verifies).

**Order in force**: `REBUILD-Z-SERIES-3-ORDER-2026-07-03.md`. **Directive (OUTRANKS this)**:
DIRECTION.md lap-201 block (Lane E mandate — this lap is its E-seam continuation, sanctioned
by the lap-203 port plan).

## ✅ This lap — the E-seam pass port (blocks 12b–12f), ALL green

Lap-203 discovered rung E forces `Zef2TC` while rungs P/R/D are proven over `Zef2`, and
`passAux`'s inert-shape discharge breaks in TC.  This lap PORTED THE WHOLE PIPELINE:

1. **`stepAnd_Zef2TC`** — ⋏/⋎ principal top-rank cut: block-12a inversions feed two nested
   lower-complexity cuts (on ψ then φ) at root `osucc (osucc (βφ + βψ))`; slot UNCHANGED;
   gate = one hypothesis `Nlog (βφ+βψ) + 2 ≤ f 0`.  ⋎-principal = same lemma, premises
   swapped (double-negation cleanup, like passAux's `exs` reusing `all`).
   **`stepVerum_Zef2TC`** — ⊤/⊥ principal cuts free via `falsum_erase`.
2. **TC atomic cut is SPLICE-FREE** (structural finding): exactly one of `rel`/`nrel` is
   `atomTrue`, so erase the FALSE literal from its own premise (`false_nrel_erase`/
   `false_rel_erase`: axL pairs collapse to `trueRel`/`trueNrel` leaves on the surviving true
   half; a truth leaf on the false literal is killed by `atomTrue_nrel_iff_not_rel`).
   `stepAtom_Zef2TC` closes at `osucc (βφ+βψ)` — no `atomCutRun` surgery, no `g∘f`.
3. **`Zef2TCProv`** (slack wrapper) + **`cutReduceAllAuxRunning_TC`** — the running-family
   ∀/∃ reduction over the full rule set (fresh root `α+γ`, slot `g∘f`); the 5 new TC rules are
   all head-inert for the erased `∃⁰ ∼φ` (truth leaves survive; `andI`/`orI` rebuild like
   `allω`).  **`stepAllωTC_bnd`** = `allω_inversion` + the reduction (mirror of Zf2).
4. **`passAuxTC`** — THE PASS over `Zef2TC`: `Zef2TC α e H f (c+1) Γ → Zef2TCProv (collapse α)
   e H (ewIter f α) c Γ` **plus one extra threaded conjunct `3 ≤ f 0`** (rel1-stable;
   `ewRootSlot e m 0 ≥ 3`), which pays the finite steps' `Nlog σ + 2` gates via `ewIter_low`
   headroom (`2·ewIter f βᵢ 0 + 1 ≤ ewIter f α 0`).  New `osucc` roots fit under
   `collapse α = ω^α` by additive principality + `1 < ω^α` (**`osucc_lt_collapse`**).
5. **`rankToZero_TC`** — rung-R descent (passAuxTC iterated `d → 0`; `3 ≤ f 0` survives the
   tower via `ewIter_base_le`).  **`sound0_TC`** — rank-0 truth core over the full rule set.

## 🚦 NEXT (the two remaining seam pieces)

1. **The BOUNDED rank-0 TC readoff** (the flagged hard piece; design against
   `readoff_delta0_Zef2` `OperatorZef2.lean:~1141` + `headline_readoff_Zef2`): from a rank-0
   `Zef2TC` derivation of `{∃⁰ φ}` (Δ₀-shaped φ), extract `∃ n ≤ ewIter f α 0,
   atomTrue (φ/[nm n])`.  `sound0_TC` (banked) gives the UNBOUNDED true member; the bound
   needs its own induction — `exI` carries `n ≤ f 0` only at the root rule, nested `allω`
   branches relativize the slot (`rel1 f k` bases only grow, so the bound should thread like
   the Zef2 original; the new TC rules are inert for a quantifier-spine singleton — cf.
   `spineHead` vacuity machinery if the context needs pruning).
2. **`wip/SpliceAssembly.lean` composition** (W-1 sanctions wip-vs-wip): compose
   `embedding_Zef2TC_V3` (lap 203) → `rankToZero_TC` → bounded TC readoff into the rung-W
   statement shape of `wainer_splice_Zef2` (`src/GoodsteinPA/WainerLadder.lean:41`, the sole
   live src sorryAx).  Check the ∃K/rel1-slot plumbing: the embedding outputs slot
   `rel1 (ewRootSlot e B) K` at rank d — EwF1/EwF2 + `3 ≤ f 0` all hold for it.
3. Judge pass ratifies rung-E statement + the Zef2TC amendment in parallel (ledger flags);
   do NOT self-ratify into src.

## Gotchas fresh this lap
- `cases χ` exposes RAW constructors: use `Semiformula.complexity_and'`/`_or'` (primed,
  constructor form) — the unprimed ⋏/⋎ simp lemmas make no progress after `cases`.
- omega mixes `Max.max` and `⊔` as DIFFERENT atoms — don't mix `le_max_left` with
  simp-produced `⊔` forms; simp the hypothesis copies and let omega atomize uniformly.
- `∼⊤ = ⊥`, `∼(φ⋏ψ) = ∼φ⋎∼ψ`, `∼rel = nrel` are ALL rfl — use typed `have := D` (defeq),
  NOT `rw [...] at D` (pattern not found).  `∼∼φ = φ` is NOT rfl (rw on goal works).
- `@axL`-style case patterns must include the `{ar}` binder (axL/trueRel/trueNrel).
- `ω`-notation unavailable here: write `Ordinal.omega0`; `Ordinal.left_le_opow _ h0` proves
  `ω ≤ ω^r` from `0 < r` (dodges the missing `one_le_iff_pos`).
- `Cl_of_NF _` with underscore NF arg fails to synthesize inside anonymous-constructor
  `exact ⟨…⟩` — pass the NF proof explicitly.
