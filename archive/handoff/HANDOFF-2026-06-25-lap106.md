# HANDOFF — lap 106 · conclusion-tracking `ZcDer` + the arithmetized Tait `ZInf` + the ∀-inversion recursion (principal case proven)

> **RESUME BATON.** Branch `plan`, HEAD `0c3ce25`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`,
> 1325). `src/` UNTOUCHED — headline footprint unchanged this lap: `peano_not_proves_goodstein =
> [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms; lone `sorryAx` = crux-2 via
> `Crux2Blueprint.lean`). All lap-106 work is in `wip/PathCOmega.lean` (bricks 5o–5q) and the NEW
> `wip/PathCInf.lean` (bricks 5r–5s); neither imported by `GoodsteinPA.lean`. 7 commits this lap.
>
> **ONE-LINE STATE:** the lap-105 NEXT prerequisite (conclusion-tracking) is DONE on the `ZcDer` layer, and
> the genuine deep core — the ∀-inversion / cut-elimination recursion — is STARTED on a fresh arithmetized
> one-sided Tait datatype `ZInf`, with its STRUCTURE in place and the **principal `allω` case + atomic bases
> machine-checked**; the commuting cases are a disclosed term-mode-perf `sorry` (not a math gap).

## What landed (all axiom-clean `[propext, Classical.choice, Quot.sound]` except the disclosed `sorry`)

### `wip/PathCOmega.lean` — the conclusion-tracking `ZcDer` layer (bricks 5o–5q)
- **`fstIdx_zAllOmega`/`fstIdx_zExOmega`** — the missing Path-C conclusion projections.
- **`inductive ZcDer`** — `ZcOK` refined so the ω-∀ node carries its conclusion data (succedent `^∀ p`,
  premise-`t` derives `Γ⟹p(t) = seqSetSucc s (substs1 t p)`, the banked `zAllOmega_concl` facts).
- **`ZcDer.toZcOK`** (forgetful — all lap-105 ordinal bricks apply), **`zcDer_iff`/`ZcPhiD`** (inversion
  vehicle), **`zcDer_allOmega_inv`** (first end-sequent recovery), **`zcDer_iord_descent_allOmega`** (the
  principal ∀-inversion step, CONCLUSION-faithful — new over lap-105's `zcOK_iord_descent_zAllOmega`).
- **`zIall_realizes_ZcDer`** — the embedding's I∀ image realizes a conclusion-tracking ω-∀ `ZcDer` node.
- **`zcDer_redAllExS_orbit_step`** — the principal ∀/∃-cut orbit step on the `ZcDer` layer.
- **`zcDer_{ex,cut}_inv` / `zcDer_sord_descent_z{ExOmega,CutOmega}`** — the per-node inversion-step family
  on `ZcDer`, complete (∀ 5o, ∃/cut 5q).

### `wip/PathCInf.lean` — NEW: the arithmetized one-sided Tait Z∞ derivation (bricks 5r–5s)
- **`inductive ZInf : V → Prop`** — V-internal port of `Zinfty.Deriv` (one-sided Tait, conclusion = a
  `Seq`-code `Γ` of formula-codes, membership `inAnt`/`seqCons`). 9 constructors mirror
  `axL/verumR/weak/andI/orI/allω/exI/cut`; each base carries `Seq Γ`. The `allω` ω-rule is strictly
  positive ⟹ full structural recursor `ZInf.rec` (verified) incl. an IH over the ω-premise family.
- **`ZInf.seq`** (`ZInf Γ → Seq Γ`), **`ZInf.weakening`**, membership helpers
  (`inAnt_seqCons_self/_of/_comm`, `inAnt_of_seqCons_ne`), structural weakening helpers
  (`ZInf.weaken_top/seqCons_comm/weaken_under`).
- **`ZInf.allInv`** — the ∀-inversion recursion (port of `Zinfty.allInvAux`). **PROVEN:** the PRINCIPAL
  `allω` case (last rule introduces exactly `^∀ φ` ⟹ select the premise at the inversion witness `t`,
  re-insert `^∀ φ` below via `weaken_under`) + the atomic bases `axL`/`verumR`. **DISCLOSED `sorry`:** the
  commuting cases `weak`/`andI`/`orI`/`exI`/`cut`/`allω`-side.

## ⚠ The disclosed `sorry` (the ONE open obligation this lap leaves) — a PERF bug, not a math gap
`ZInf.allInv`'s commuting cases time out (HFS `whnf`/`isDefEq`, even at `maxHeartbeats 1600000`). The math
is the verbatim `allInvAux` port (recurse into premises, re-apply the rule, reorder via the weakening
helpers) — I wrote it in full first and it elaborated to a timeout, so I replaced the commuting bodies with
`sorry` to keep a green build. **Suspected cause:** `seqCons_comm`/`weaken_*` take the consed formulas
IMPLICITLY, so Lean infers them by unifying `seqCons (seqCons Γ A) B` against the premise type — forcing
`lh`/`insert` whnf on big codes. The full (timing-out) proof bodies are in this lap's git history
(reverted within commit `0c3ce25`'s parent diff — recoverable from `git show` of the working edits, or just
re-derive from the case sketches in the `sorry` comments).

**FIX (next lap, hardest-first):**
1. Give `ZInf.seqCons_comm`/`weaken_top`/`weaken_under` EXPLICIT formula arguments (no inference), and/or
   prove a single **`ZInf.permCongr : Seq Δ → (∀ A, inAnt A Γ ↔ inAnt A Δ) → ZInf Γ → ZInf Δ`** ONCE,
   standalone (the helpers compile FAST outside `induction`). Then each commuting case = one `permCongr`
   call with a `tauto`-closed membership `↔`. This should discharge all six commuting `sorry`s. The case
   sketches (which premise to recurse into, which reorder) are in the in-file `sorry` comments and in
   `PENDING_WORK.md` lap-106 NEXT item 0.

## Then (the endgame after `allInv` closes)
2. Port `andInvAux`/`orInvAux` to `ZInf` (same structure), then `cutElimStep`/`cutElim`.
3. **Bridge `ZInf`-derivation-height ↔ engine `iord`** so the cut-elimination descent rides crux-1's
   PRWO(ε₀). `ZInf` is the proof-STRUCTURE carrier; the two-sided engine codes (`sord`/`iord`,
   `wip/PathCOmega.lean`) remain the ORDINAL carriers. This is the genuine remaining synthesis (NEXT_STEPS
   lap-106 box).
4. Wire to `false_of_ZDerivesEmpty` (`Crux2Blueprint.lean:588`) + the other crux-2 placeholders
   (`Crux2Blueprint.lean:455/533/575/588`) → `Reduction.goodstein_implies_consistency` → headline. ONLY
   when `#print axioms` clean.

## Calculus reference (pinned this lap, `scratchpad/buchholz-gentzen.txt:924-972`)
Two layers: (A) the engine/`ZcDer` two-sided Buchholz Z∞ (`Γ→C`, single succedent; `R_A`/`Lk_A`/`Cut_D`;
cut on `D` has premises `Γ,D→C` and `Γ→D` — NOT the loose "C/¬C" of pre-lap-106 handoffs). (B) the new
`ZInf` one-sided Tait (`Seq` of signed formulas, negation `neg ℒₒᵣ`), matching the `Zinfty.Deriv` META
template. The bridge between them (item 3) is the open architectural synthesis.

## Aristotle
Idle — CORRECT. `allInv`'s commuting cases + the `permCongr` perf fix are entangled with the HFS
`Seq`/`inAnt` API; not self-contained mathlib lemmas.

## Kernel state (unchanged this lap — no `src/` edit)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` = `[propext, choice, Quot.sound]`.
