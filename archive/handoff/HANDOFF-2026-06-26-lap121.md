# HANDOFF — lap 121 · the stall SPLITS; generalized-redex-finder half-built (Step A done)

> **RESUME BATON.** Branch `plan`, HEAD `5f896f5`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`,
> **1326**). Headline footprint unchanged this lap (only additive lemmas in `InternalZ`): no `sorry` touched,
> 11 disclosed sorries as in lap 120. `peano_not_proves_goodstein` still `[propext, sorryAx, choice,
> Quot.sound]` (0 math axioms); all 5 new lemmas verified `[propext, Classical.choice, Quot.sound]`.

## What this lap did (5 commits of real on-crux progress)
Attacked THE crux (the `red`-stall / `false_of_ZDerivesEmpty`), per lap-120's directive. **Banked 4
axiom-clean lemmas + 1 instance** that split the monolithic stall and half-build the genuine fix:

1. **`iord_descent_iR2_zK_of_validF_critUpTo`** (InternalZ, after `iord_descent_iR2_zK_of_valid`). The iR2
   K-descent uses `zKValid`'s criticality conjunct ONLY at indices `≤ j0`; re-stated with `zKValidF` +
   `hcrit : ∀ i ≤ j0, ¬iperm(tp dᵢ) s`. ⟹ a JUNK `isymRep` premise (spurious atom) BEYOND the chain exit
   `j0` is HARMLESS to the genuine descent. Splits lap-120's stall into junk-beyond-j0 [harmless] vs
   threaded-atom-≤-j0 [the real core].
2. **`chainAsucc_threaded_of_leaf`** (InternalZ, after `zDerivation_zAx1_inv`). A threaded atom/`zAx1` leaf
   at `k ≤ j0` of a ⊥-chain has `∃ i' < k, chainAsucc ds i' = chainAsucc ds k` — pins the axiom-cut redex
   source; gives `k > 0` (stall leaf never premise 0).
3. **`iord_descent_iRcrit_of_redex`** (InternalZ, before `iord_descent_iRcrit_of_chain'`, now a wrapper).
   The K-descent computation needs only an EXHIBITED redex pair + `1≤r` + the 6 ρ-facts, NOT global
   criticality. So any way of exhibiting the principal-cut redex drives descent, ignoring a threaded atom.
4. **`exit_nonRep_of_reroute`** (InternalZ, after `chainAsucc_threaded_of_leaf`) — **Sub-lemma A** of the
   generalized finder. The LEAST-indexed exit of a re-routing ⊥-chain is non-`isymRep` (re-route ⟹ smaller
   exit ⟹ ⊥ vs minimality). Recovers `inference_critical_pair` Step A structurally.
5. **`chainAsucc_definable' (Γ) : Γ-[m+1]-Function₂`** (InternalZ:1251) via `.of_sigmaOne` — the
   infrastructure that fixed the aesop depth-blowup on `chainAsucc` (see memory `definability-aesop-depth-blowup`).

## ⭐ THE decomposition (the genuine fix, half-built) — read `PENDING_WORK.md` lap-121
Read `inference_critical_pair` (InternalZ:507) line by line. Its `hnperm` (criticality ≤ j0) has THREE
clauses; for a ⊥-chain the `isymR(Cmain)` and `isymLk-at-Γmain` clauses are GENUINE and always hold (I-rules
have compound succ ≠ ⊥; `Γmain = ∅`). **Only the `isymRep` clause breaks** under a threaded atom — at two
spots: the exit `j0` (Step A) and the least-left-symbol's chain-source `i`. The fix: generalize the finder
by REPLACING the `isymRep` clause with re-routing (`chainAsucc_threaded_of_leaf`'s duplicate-succedent fact).
- **Sub-lemma A (exit recovery): ✅ DONE** = `exit_nonRep_of_reroute`.
- **Sub-lemma B (non-isymRep chain-source): OPEN, NEXT TARGET.** Re-route the least-left-symbol's source `i`
  through duplicates to a non-`isymRep` source (least-number). **⚠️ CAVEAT (genuine risk):** the threading
  may bottom out at an `isymLk` L-axiom (succ `B`), not an `isymR(B)` producer — then no `(isymR,isymLk)`
  redex on `B` exists, and the generalized finder approach FAILS for that sub-case. If B is blocked, PIVOT
  to atom-removal (lexicographic `(iord, chain-length)` measure) — recorded as path (ii) in PENDING_WORK.
- Then feed the exhibited redex to `iord_descent_iRcrit_of_redex` ⟹ descent, threaded-atom stall DISSOLVED.

## Next-lap targets (in order)
1. **[lap 122] Sub-lemma B** (`inference_critical_pair`-source re-route). FIRST settle whether the
   threading can bottom out at an L-axiom (the caveat) — if yes, the whole generalized-finder approach is
   sunk; pivot to atom-removal. If no, build B, then assemble the generalized finder, then wire to
   `iord_descent_iRcrit_of_redex`. This DECIDES the stall.
2. Sibling (needed regardless of stall resolution): **`zKValidF_iIndReduct_of_zInd`** — lap-120 found it
   FALSE as stated; fix = instance-correct Ind reduct via `zsubst` (analogous to lap-114's critical reduct).
3. Then the rest of the endgame: `false_of_ZDerivesEmpty` (M3) + `foundation_bot_to_Z_empty` (M2) + (C)
   `gentzenDescentφ`.

## Probes settled this lap (don't redo)
- `ZRegular` does NOT exclude the stall (it's eigenvar-freshness; `zRegNext` atom-branch = 0). NEGATIVE.
- iR2 descent needs criticality only up to j0 (lemma 1) — so junk-beyond-j0 is provably handled.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open pieces (Sub-lemma B, the stall) are engine-internal, not isolated portable lemmas.

## Pointers
`PENDING_WORK.md` lap-121 (full decomposition + the definability recipe + 3 attack paths) ·
`REFLECTION-2026-06-26-lap120.md` (the stall diagnosis this lap built on) · `STATUS.md`.
