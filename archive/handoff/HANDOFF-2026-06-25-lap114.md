# HANDOFF — lap 114 · FRESH-MIND REVIEW: the inversion PRIZE is feasible (re-principalization)

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**).
> Headline footprint re-verified in-kernel (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms;
> lone `sorryAx` = crux-2). New lemma `seqSucc_zsubst_zIall_premise` axiom-clean `[propext, choice, Quot.sound]`.

## ⭐⭐⭐ THE FINDING (kernel-grounded) — `ZDerivation_red_zK_crit` is FALSE as stated; the fix is contained
The crux-2 "prize" inversion `ZDerivation_red_zK_crit` (`Crux2Blueprint:100`), framed since lap 110 as the
multi-year cut-elim core (`Zinfty.allInv`) and the avoided piece (lap-111), is **unprovable for its current
`ρ = zAxReduct ∘ red`** — for a precise, FIXABLE reason. Full chain in
`ANALYSIS-2026-06-25-lap114-inversion-instance-mismatch.md`. Summary:

1. It delegates to `ZDerivation_iRcritG_of` (`InternalZ:8336`). The outer recombination chain's validity is
   AUTOMATIC (`zKValidF_iCritReductGen`, `InternalZ:3129` — "ds0/ds1 immaterial to the outer validity"), so
   the whole inversion reduces to the two stripped half-derivations `haux0`/`haux1` being `ZDerivation`s.
2. `haux0` concludes `Γ → cutFormula d`. `cutFormula d = F(k) = substs1 (numeral k) F` with
   `k = π₁(π₂(tp (redexJ d)))`, the L-redex (axAll) instance (`cutFormula_all`, `InternalZ:6517`).
3. `haux0`'s `isChainInf` threading FORCES its redexI premise to derive exactly `F(k)`.
4. But `ρ (redexI) = zAxReduct (red premise) = zsubst d0 a (numeral 0)` (instance **0**, `red_zIall`
   `InternalZ:7198`), deriving `F(0) ≠ F(k)`. ⟹ `haux0` is unprovable; **`red`'s critical reduct is unsound.**
5. Why it hid: instance-0 is CORRECT for the ordinal descent (`iord (zsubst d0 a n)` is instance-invariant),
   so `iord_descent_red` (laps 108–113) is sound and survives. The defect is purely on the SOUNDNESS side.
   This is lap-104's `red_redAllEx_eq` re-principalization finding, now pinned to the live post-lap-107 engine.

**FIX (Buchholz §3.2 case 5.1 re-principalization, building blocks BANKED):** the I∀ R-redex premise must be
`zsubst d0 a (numeral k)`, not `… (numeral 0)`. Then:
- succedent `= cutFormula d` by **`seqSucc_zsubst_zIall_premise`** (`Zsubst.lean`, NEW this lap, axiom-clean,
  generalizes `red_zIall_tpReduce` from `t=0` to any closed `t`), modulo freshness `fvSubst a (numeral k) p = p` (O3).
- derivability by `ZDerivation_zsubst_zIall_premise` (`Zsubst.lean:1879`, banked), modulo `maxEigen d0 < a` (O1).
- I¬ redex unchanged (`red_zIneg = d0`, no instance).

## ✅ LANDED THIS LAP (2 green commits, both axiom-clean `[propext, choice, Quot.sound]`)
- **`seqSucc_zsubst_zIall_premise`** (`Zsubst.lean`, after `red_zIall_tpReduce`): the ∀-inversion succedent
  identity at a general instance `t` — `seqSucc (fstIdx (zsubst d0 a t)) = substs1 t p`. Linchpin #1. ~6 lines
  (`fstIdx_zsubst` + `seqSucc_fvSubstSeqt` + `zIallWff.2.1` + `fvSubst_substs1` + `termFvSubst_fvar_self` +
  `hpfresh`).
- **`seqSucc_corrected_redexI_eq_cutFormula`** (`Zsubst.lean`, 2nd commit): linchpin #2 — the corrected
  reduct `zsubst d0 a (numeral k)` (k = `π₁(π₂(tp (redexJ d)))`, the SAME k `cutFormula` reads) derives
  succedent exactly `cutFormula d`. Combines linchpin #1 with `cutFormula_all`. This is the j₀=redexI
  succedent clause of `haux0` that `red`'s instance-0 reduct cannot give. ~4 lines.
- **`fstIdx_zsubst_zIall_premise`** (`Zsubst.lean`, 3rd commit): the corrected reduct's FULL end-sequent at
  general instance `t` — `fstIdx (zsubst d0 a t) = seqSetSucc s (substs1 t p)` (generalizes
  `red_zIall_tpReduce` off `t=0`). The antecedent half `haux0` needs.
- **`ZDerivation_corrected_haux0`** (`Crux2Blueprint.lean`, 3rd commit, axiom-clean): ⭐ **the R-side half of
  the corrected inversion, PROVEN.** Replacing the I∀ R-redex premise of a critical chain by `zsubst d0 a
  (numeral k)` and reducing the conclusion succedent to `cutFormula d` is a `ZDerivation` — discharged
  ENTIRELY via `ZDerivation_iCritReplaceReduce_of` + the linchpins, modulo orbit data (O1 `maxEigen d0 < a`,
  O3 `hpfresh`/`hΓfresh`, cut-formula wff, threading/rank up to `redexI`). Exact analogue of
  `ZDerivation_zK_replace_zIall_of` at instance `k` not `0`. This confirms the lap-114 fix works in-kernel:
  the inversion's hard R-half is sound for the re-principalized reduct.
- Docstring note on `ZDerivation_red_zK_crit` (`Crux2Blueprint:100`) flagging the finding + fix (no code change
  to the disclosed sorry — kept honest; corrected reduct is next-lap work).
- STATUS.md / PENDING_WORK.md (lap-114 box) / ANALYSIS doc.

## 🎯 NEXT-LAP TARGET (hardest-first, headline-critical) — R-half DONE, 3 pieces left
**The R-half `haux0` is PROVEN this lap** (`ZDerivation_corrected_haux0`, axiom-clean). Remaining:
1. **`haux1` — the symmetric L-side half.** Buchholz Thm 3.4(a) L-half: replacing the L-redex premise
   (redexJ, an axAll axiom) and reducing the conclusion to `cutFormula d, Γ → Δ` (`seqAddAnt (cutFormula d)
   s`). For the axAll L-redex the reduct is the axiom instance; the threading uses `seqAddAnt` (antecedent
   GAINS `cutFormula d`) — mirror `ZDerivation_iCritReplaceReduce_general` / the I¬ replace
   (`ZDerivation_zK_replace_zIneg_of`, which already does the antecedent-growth `isChainInf_reduceR_membership`
   pattern). The succedent stays `Δ` (the original conclusion). Likely a `ZDerivation_corrected_haux1`
   analogue. NB the L-redex is an *axiom* — its "reduct" may just be the axiom premise threaded, no subst.
2. **Implement the corrected critical reduct in `red`.** Re-key `iRNextG`/`iRNext` tag-4 critical branch so
   the I∀ R-redex premise substitutes `numeral k` (`k = π₁(π₂(tp (znth (zKseq d) (redexJ d))))`) instead of
   `red` (which gives 0). Leave I¬ and the `zAxReduct` axiom-collapse as is. Re-prove the `red_zK_crit`
   equation + `iRNextDef` `𝚺₁`-definability. Descent lemmas are instance-invariant ⟹ should transfer
   (re-check each `red_zK_*` rewrite). Only the *I∀* R-redex changes; redex finder/`cutFormula`/`redexJ`
   untouched (same `k`). THEN assemble `ZDerivation_red_zK_crit` = `ZDerivation_iRcritG_of` fed `haux0`/`haux1`.
3. **Thread O1/O3 freshness** (`hpfresh`/`hΓfresh`/`maxEigen d0 < a`) + the `redexI ≤ j₀` threading/rank data
   through the `redSound` motive — the same orbit data the replace branch already isolates (lap-99/100,
   `ZDerivation_zK_replace_zIall_of`); `redexI < j₀` is lap-113's `irk_chainAsucc_redexI_le` reasoning.
Then `ZDerivation_red_zK_crit` is REAL → `redSound` (modulo the other validity sorries :81/:270/:443/:529) →
M2/M3 → headline (wire ONLY when `#print axioms` clean).

**Banked linchpins to reuse** (all in `Zsubst.lean`/`Crux2Blueprint.lean`, axiom-clean):
`seqSucc_zsubst_zIall_premise`, `seqSucc_corrected_redexI_eq_cutFormula`, `fstIdx_zsubst_zIall_premise`,
`ZDerivation_corrected_haux0`.

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, 8 sorries — by NAME, lines drift)
- `zKValidF_iIndReduct_of_zInd` (Ind-reduct validity — the "more tractable wall", lap-71) ·
  `ZDerivation_red_zK_crit` (THE prize; FALSE for current ρ — needs the corrected reduct; **R-half `haux0`
  proven this lap**, remaining = `haux1` + `red` redefinition) · splice validity (`ZDerivation_red_zK_splice`) ·
  axNeg replace validity · redZKReady-class · `iord_descent_red` chain-REPLACE IH (the ⊥-orbit invariant,
  lap-113) · M2 `foundation_bot_to_Z_empty` · M3 `false_of_ZDerivesEmpty`.
  (`ZDerivation_corrected_haux0` is sorry-FREE — it is the discharged R-half, not yet wired into the
  `ZDerivation_red_zK_crit` sorry pending `haux1` + the corrected `red`.)

## TWO deep pieces remain (both now sharply characterized)
- **The inversion** (`ZDerivation_red_zK_crit`): contained re-principalization fix (lap-114). R-half `haux0`
  DONE; remaining = L-half `haux1` + `red` redefinition. Higher value (cut-elim content).
- **The ⊥-orbit invariant** (chain-REPLACE IH + M3 atom-stall + replace/splice validity): the hereditary
  "selected-`tp = isymRep` / ⊥-succedent" invariant threaded through `red` (lap-113 KEY findings).

## Aristotle
Idle — CORRECT. Both remaining pieces are engine-internal redesigns, not isolated open lemmas.
