# HANDOFF — lap 77 · `ZDerivation_zsubst` COMPLETE (rung-1 done, axiom-clean)

> Supersedes prior HANDOFF-*.md. Read order: this file → `PENDING_WORK.md` → `STATUS.md`.
> Branch `plan`. Build 🟢 green `lake build GoodsteinPA` = **1323 jobs**. Tree clean.
> HEAD `4cd5669`. (6 commits this lap from `37adde7`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**. Sole math
wall = crux 2, localized to `RedSound` (`InternalZ.lean`). Ladder: rung 0.5 ✅ → **rung 1 `zsubst` ✅
(THIS LAP)** → rung 2 (Ind reduct) → rung 3 (K/cut reduct) → rung 4 (dispatch).

## ✅ THIS LAP (6 commits) — rung 1 CLOSED
1. **`11a3807`** `fvSubst` freshness-identity: `termFvSubst_eq_self_of_le`, `termFvSubstVec_eq_self_of_le`,
   `fvSubst_eq_self_of_le` (FvSubst.lean). `^&a ↦ t` is the identity on any `IsUTerm`/`IsUFormula` ≤ a
   (because `^&a = ⟪1,a⟫+1 > a`, `var_lt_qqFvar`).
2. **`20c558d`** `isChainInf_zsubst` (Zsubst.lean): chain-structure transfers under substitution. Every
   `isChainInf` condition is POSITIVE (closed under applying `fvSubst`) → preserved, NOT vacuous. Only
   the rank conjunct consumes succedent formula-hood (`hcf`, via `irk_fvSubst`).
3. **`fa4f60e`** strengthen `zKValid` with `∀ i, IsUFormula (chainAsucc ds i)` (chain-succedent
   formula-hood). Via `isChainInf` cond-1 this also gives `IsUFormula (seqSucc s)`.
4. **`2a7550a`** `zKValid` carries conclusion sequent well-formedness: `IsUFormula (seqSucc s)` +
   antecedent formula-set `∀ k < lh (seqAnt s), IsUFormula (znth (seqAnt s) k)`. (zKValid is now 10
   conjuncts; only one positional destructure site needed updating; the Δ₁ `zKValidDef` σ/π cores were
   extended — wrap the per-premise `∀` in parens, append top-level seqSucc/seqAnt conjuncts.)
5. **`4cd5669`** **CLOSE the zK case ⟹ `ZDerivation_zsubst` is a full theorem,** `#print axioms` =
   `[propext, Classical.choice, Quot.sound]`. New transfer helpers (Zsubst.lean):
   `inAnt_fvSubstSeq_reflect`, `tp_zsubst_eq`, `iperm_zsubst_conclusion`,
   `zIallF/zInegF/zAxAllF/zAxNegF_zsubst`.

**Key correction of the lap-76 "criticality obstruction":** it was an over-cautious FALSE ALARM for the
`d ≤ a` statement. Under `d ≤ a`, every premise `dᵢ ≤ a` and the conclusion `s ≤ a`, so every principal
formula and sequent is `^&a`-free (a code containing `^&a` would exceed `a`). The spurious-collapse worry
(`^∀F(^&a)` vs `^∀F(t)`) requires `^&a` to occur — impossible. So criticality transfers cleanly.

## ⚠ CAVEAT — `ZDerivation_zsubst` as proven is VACUOUS; rung 2 needs a GENERALIZATION
The `d ≤ a` bound makes the substitution vacuous (`a` cannot occur in `d`). This is the lap-73 caveat,
now sharp: **rung 2's Ind reduct forms `d1(a/ν) = zsubst d1 at' ν`** where `at'` is the eigenvariable
of `d1` — and `at'` DOES occur in `d1` (in `F(a)`), while `d1 ≤ at'` is FALSE. So the `d ≤ a` version
does NOT directly apply to rung 2.

**What rung 2 needs:** `ZDerivation_zsubst` with `d ≤ a` RELAXED to a freshness PREDICATE
`a ∉ eigenvars(d)` (a may occur free in sequents, but is not an inner I∀/Ind eigenvariable — no capture).
Then the substitution is NON-vacuous and the criticality obstruction becomes REAL (a occurs in principal
formulas → `fvSubst` changes them → `^∀F(^&a)→^∀F(t)` can spuriously match the conclusion). This needs
the eigenvariable side-condition (Buchholz: the eigenvariable is fresh for the conclusion `s` and all
chain conclusions) to rule out the collapse.

**The good news — ALL the transfer machinery built this lap is reusable for the generalization:**
`isChainInf_zsubst`, `iperm_tp_zsubst` (positive), `iperm_zsubst_conclusion` (reflection vs the a-free
conclusion — still holds if `a ∉ FV(s)`), `tp_zsubst_eq` (will weaken: needs the principal formula
a-free, true under freshness-for-conclusion), the `z*F_zsubst` projections, and the `zKValid`
sequent-wf strengthening. The criticality core `iperm_zsubst_conclusion` only needs `a ∉ FV(s)` (not
`d ≤ a`), so it survives the generalization directly.

## ▶ NEXT — two viable fronts (pick per how-to-get-unblocked)
**(A) Generalize `ZDerivation_zsubst` to the freshness predicate** (the rung-2 enabler). Define a Δ₁
tree-freshness `aNotEigen d` (a is not an I∀/Ind eigenvariable of any node ≤ d). Re-prove
`ZDerivation_zsubst` with `(hfresh : aNotEigen d)` + `(hsfresh : a ∉ FV(s))`-style hyps replacing
`d ≤ a`. The 6 non-zK cases need the eigenvariable freshness to discharge `fvSubst_substs1_fvar`'s
`a'≠a` (currently from `e < d ≤ a`); zK uses the reusable helpers above. THE deep part: criticality
when `a` occurs in principal formulas — resolved by the eigenvariable-fresh-for-conclusion condition.

**(B) Start rung 2 (the genuine Ind reduct) directly** and discover the exact bound it forces. Buchholz
14.24: `d[0] := K^{rk F}⟨d0, d1(a/0),…,d1(a/k−1)⟩`, `k = ⟦induction term⟧`. Build the substituted-block
sequence (Σ₁ recursion reading `zsubst d1 at' i`), prove its `zKValid` (the THREADING is the genuine
content: premise `i+1`'s antecedent `F(i)` = premise `i`'s succedent; rank `irk(F(i)) ≤ r` via
`irk_substs1`/`irk_fvSubst`). This will tell you precisely which `zsubst` correctness statement to prove.

## Parallel / second front
`PA_delta1Definable` (Foundation `Incompleteness/Examples.lean:17`, still an `axiom`) — mandatory for
axiom-free. Advance when the ladder blocks.

ANTI-FRAUD: discharge `Reduction.lean:66` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound`/`zKValid` or fake the reduct.
