# Handoff: lap 146 — REVIEW + `zIndWff` strengthening + `descent_step_Ind` DROPPED (axiom-clean, off red)

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms). FOUR commits: `5d77bd5` (review/direction) + `a2b2a3a` (`zIndWff` strengthening) +
`aca7536` (assembly plan) + `59b339b` (**`descent_step_Ind` DROPPED**).

## 🎉 HEADLINE RESULT — `descent_step_Ind` is PROVEN (was a named sorry), axiom-clean, off `red`
`#print axioms descent_step_Ind = [propext, Classical.choice, Quot.sound]`. The Ind-root branch of
`ZDerivesEmptyR_descent_step` is now fully proven AND fully off `red`. Assembly (commit `59b339b`, mirroring
`descent_step_K_critical_all`): witness `zK s (irk ⊥) (iIndReductSeqG d0 d1 (π₁ at') 1) = ⟨d0, d1[a:=0]⟩`; on
the ⊥-orbit `p=⊥` (`eq_falsum_of_substs1_falsum`) collapses the telescope (antecedents `{⊥}`, succedents `⊥`),
and the strengthened `zIndWff` gives the EXACT antecedent shape that threads the chain; `zKValidF` via
`isChainInf_telescope` + `iperm_tp_fstIdx_of_ZDerivation` + NEW `zKValidF_leafconds_of_ZDerivation` (InternalZ,
the tag-{1,2,5,6} `IsUFormula` obligations from a ZDerivation premise); `ZDerivation` via `zDerivation_zK_intro`;
invariants via the `_zK_of` folds + `*_zsubst` preservation; descent = banked `iord_descent_iIndReductSeqG_one`.
**Live-path sorries now: `descent_step_K_noncritical` (:2139, §5.2) + (A) `gDef` (:2457).**

## What this lap DID (beyond the review)

**LANDED the `zIndWff` membership→shape ripple (`a2b2a3a`) — the lap-145-identified "whole remaining content".**
The step clause was MEMBERSHIP `inAnt (F(a)) (seqAnt(fstIdx prem1))`, which admits UNSOUND Ind nodes (a lax
`d1 ⊢ {⊥,X}→⊥` leaks stray `X` past the rule). Now SHAPE + bundled `Seq`:
`Seq (seqAnt (fstIdx d)) ∧ seqAnt(fstIdx prem1) = seqCons (seqAnt(fstIdx d)) (F(a)) ∧ <succ>` (Seq bundled like
lap-118 `zInegAntWff`, self-preserving under eigensubst). Edits: `zIndWff` body + `zIndWffDef` σ/π (`seqConsDef`
+ `seqDef`, `sas` already bound) + `zIndWff_defined` simp + `zsubst` preservation (`Zsubst` zInd case) + two
triple-destructure fixes (`Crux2Blueprint:146`, `:329`). Build green, no axiom drift. So `zDerivation_zInd_inv`
now yields, on the ⊥-orbit (Γ=∅, p=⊥), `seqAnt(fstIdx d1) = {⊥}` EXACTLY — the soundness gap is CLOSED.
`descent_step_Ind` (:2262) is STILL sorry'd; its sole remaining content is the **soundness ASSEMBLY**.

## What this review lap settled

1. **lap-143's mandate is DONE.** Laps 144/145 wired the live `false_of_ZDerivesEmpty` path FULLY off `red`.
   `ZDerivesEmptyR_descent_step` (`Crux2Blueprint:2270`) is sorry-free and dispatches Ind→`descent_step_Ind`
   (genuine `iIndReductSeqG`) / K→`descent_step_K_majorIdx` (critical off `red`, ¬-case CLOSED lap-144). NO
   `red`, NO kernel-FALSE soundness (:80/:1108) on the live path anymore.

2. **The live `false_of_ZDerivesEmpty` path now has exactly THREE live sorries**, all genuine, none
   generational, none routing through `red`:
   - `descent_step_Ind` (:2262) — Ind-root soundness, blocked on a `zIndWff` antecedent-shape gap.
   - `descent_step_K_noncritical` (:2139) — Buchholz §5.2 atomic reduct.
   - (A) `exists_sigma1_descending_step` (:2327) — the `gDef` Σ₁-Semisentence packaging.
   (Plus the M2-side `:1867` `∃ z, ZDerivesEmptyR z` provider, on a different sub-goal.)

3. **Validated lap-145's `zIndWff` diagnosis IN-KERNEL — it is REAL, not a stale-obstruction repeat.**
   `zIndWff` (`InternalZ:1681`): base clause :1682 is an EQUATION (`seqAnt(fstIdx prem0) = seqAnt(fstIdx d)`);
   step clause :1684 is MEMBERSHIP (`inAnt (F(a)) (seqAnt(fstIdx prem1))`). Genuine asymmetry.

4. **The strengthening is REQUIRED, not convenient.** Membership-only admits *unsound* Ind nodes — a lax
   `d1 ⊢ {⊥,X}→⊥` (valid, `⊥∈`ant) leaks stray `X` past the rule, so the conclusion `Γ→F(t)` doesn't follow.
   So the fix closes a latent SOUNDNESS gap AND is more faithful to Buchholz (step premise `F(a),Γ→F(Sa)`).

5. **Refuted two cheaper reframes:** (a) `ZSeqAnt = (zSeqAnt = 0)` only flags sequence-wellformedness, not
   antecedent content → no shape. (b) the `zIndWff` "strengthen-body-without-the-ZPhi-cascade" docstring is
   over-optimistic — `zIndWff` IS in the `zPhi` Ind disjunct (`InternalZ:5399, 5451`), so its `𝚫₁`-Def must
   change; but `zIndWff` is C-free, so `zphi_monotone`/`_strong_finite` are untouched (definability-dominated,
   NOT a 64-site cascade). `seqConsDef`/`seqAddAntDef` (:6318) are already available.

## 🎯 NEXT (hardest-first) — `descent_step_K_noncritical` (:2139, Buchholz §5.2), then (A) `gDef` (:2457)

`descent_step_Ind` is DONE. Two live-path sorries remain:
1. **`descent_step_K_noncritical`** (`Crux2Blueprint:2139`) — Buchholz §3.2 case 5.2: a NON-critical `∅→⊥`
   K-node has a sound strictly-descending reduct. The §5.2 atomic reduction (Buchholz Lemma 5.2 gives
   `o(d[n]) < o(d)` even for atomic leaves — the genuine resolution of the lap-129 `red`-fixpoint stall). The
   deepest remaining cut-elimination content. Now that the Ind reduct is assembled, its Ind-premise sub-case
   may reuse the same `iIndReductSeqG` + `zKValidF_leafconds_of_ZDerivation` machinery.
2. **(A) `exists_sigma1_descending_step`** (`Crux2Blueprint:2457`) — the `gDef` Σ₁-Semisentence packaging
   (Foundation-definability; wrong-polarity μ-route refuted lap-139; needs a primrec witness bound OR a
   deterministic constructive reduct now that the descent steps produce explicit witnesses).
Both genuine, neither generational. See `PENDING_WORK.md`.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any `ZDerivesEmptyR_descent_step` branch with `red` (live path is off it; lap-141 regression).
- Do NOT attack the now-off-path dead `red`-soundness sorries {:80, :1108, :1257-ish, :1367-ish, :1471-ish}
  AS STATED; relocate to `wip/` only as a DELIBERATE cleanup AFTER `descent_step_Ind` drops, never count-management.
- Do NOT re-try the ZSeqAnt or "no-cascade-docstring" reframes (both CHECKED + refuted lap-146).
- Do NOT jump to `descent_step_K_noncritical`/(A) before `descent_step_Ind` drops (Ind is teed-up + validates
  the red-free pivot end-to-end — keep focus).

## 📁 Key anchors
- `zIndWff` def `InternalZ:1681`; `zIndWffDef` σ/π `:1695`; `zIndWff_defined` `:1725`; ZPhi Ind disjunct
  `:5399/:5451`; `zDerivation_zInd_inv` (yields `zIndWff`) `:8749`; `seqAddAntDef` `:6318`.
- `zsubst` zIndWff-preservation `Zsubst.lean:3595/3604`.
- `descent_step_Ind` (named sorry) `Crux2Blueprint:2262`; banked descent `iord_descent_iIndReductSeqG_one`
  `:2218`; `p=⊥` collapse `eq_falsum_of_substs1_falsum` `:2165`.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-146) · `STATUS.md` lap-146 · `PENDING_WORK.md` lap-146.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`.
`descent_step_Ind` is DROPPED (axiom-clean, off `red`). Live-path remaining = `descent_step_K_noncritical`
(:2139, Buchholz §5.2) + (A) `gDef` (:2457). Attack §5.2 next (hardest-first). NEVER `red`.**
