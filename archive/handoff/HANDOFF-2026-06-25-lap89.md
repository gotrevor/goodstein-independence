# HANDOFF — lap 89 · FRESH-MIND REVIEW + tag-4 DISPATCH `iRK` defined & WIRED into `red`

> Supersedes prior HANDOFF-*.md. Read order: this → `STATUS.md` → `PENDING_WORK.md` (lap-89 box).
> Branch `plan`. HEAD `dfe9bd3`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Tree clean.

## ⭐ REVIEW FINDING (the lap-88 handoff MISSED this — act on it)
`peano_not_proves_consistency` is now **axiom-clean** (`[propext, choice, Quot.sound]`, verified
in-kernel). **`PA_delta1Definable` was discharged UPSTREAM** — Foundation now proves `𝗣𝗔.Δ₁` as a real
`noncomputable instance` (`.lake/packages/foundation/…/InductionSchemeDelta1.lean:1379`), no longer an
axiom. ⟹ **the entire lap-74/78/81 "second front" (`src/PADelta1.lean` Δ₁-recognizer campaign) is MOOT.**
**The headline `peano_not_proves_goodstein` now has EXACTLY ONE open blocker:**
`goodstein_implies_consistency` (`Reduction.lean:68`, a literal `sorry`) = crux-1 (DONE lap 57) ∘ crux-2 =
`redSound` = internalized cut-elimination. Memory `pa-delta1-discharged-upstream` written; STATUS.md
refreshed off the stale two-front framing.

## ✅ 6 GREEN COMMITS THIS LAP (axiom base untouched; headline still 0 math axioms)
All new lemmas axiom-clean `[propext, Classical.choice, Quot.sound]`. All in `InternalZ.lean` unless noted.

1. `af509c3` — **REVIEW + STATUS refresh** (PA_delta1Definable finding; header/where-it-stands/what's-
   happened/Outstanding/ledger all updated; HANDOFF symlink lap86→lap88; memory written).
2. `6c97930` — **dispatch bricks**: `permIdxDef`/`permIdx_defined` (Σ₁-def of the dispatch index, was
   MISSING) + `iRKr` (5.2.2 replace-reduct = `iCritAux d (permIdx d) (znth s dᵢ)`). ⭐ key insight: the
   genuine reduct halves come from the **recursive table lookup `red dᵢ = znth s dᵢ`**, NOT from
   `inference_critical_pair` — so every branch is a CLOSED definable term, no existential.
3. `f89eebb` — **tag-4 dispatch `iRK` DEFINED + Σ₁-def**. `iRKs` (5.2.1 splice = `zK (fstIdx d) r'
   (seqInsert (zKseq d) i dᵢ{0} dᵢ{1})`, halves `= znth (zKseq (znth s dᵢ)) {0,1}`, **rank `r' =
   max(irk(seqSucc(fstIdx dᵢ{0})), zKrank d)` — VERIFIED to be exactly the minimal `r'` the validity
   object `isChainInf_seqInsert` requires** `(irk(seqSucc(fstIdx a)) ≤ r' ∧ r ≤ r')`); `iRKc` (5.1,
   standalone extraction of the old tag-4 `iRcritG` branch); `iRK` (the 3-way dispatcher). `iRK` branches
   on the **Δ₀ sentinel `permIdx d < lh (zKseq d)`** (= criticality, via `permIdxAux_eq_self_of_none` /
   `_isPermPrem_of_lt`) instead of embedding Δ₁ `zKCriticalDef`; sub-dispatch on the same test for the
   selected premise. `iRK_defined` via nested `by_cases` (supply `not_le.mpr`/`not_lt.mp` of the tests).
4. `d913cf2` — PENDING_WORK lap-89 box.
5. `dfe9bd3` — **WIRE `iRK` into `iRNextG` tag-4** (HEAD). `red`'s tag-4 now genuinely dispatches
   5.1/5.2.1/5.2.2 instead of always `iRcritG`. Blast radius handled: `iRNextGDef` tag-4 → `!iRKDef`;
   `iRNextG_defined` simplified (dropped the 1.6M-heartbeat override); `red_zK` is now the raw dispatch
   equation `red (zK s r ds) = iRK (zK s r ds) (redTable (zK-1))`; added `red_zK_crit` (5.1 refinement =
   `iRcritG` with recursive premise reducts, under `hcrit : ¬ permIdx < lh`); dispatch invariants
   `fstIdx_iRK`/`zTag_iRK` (+ `_iRKr`/`_iRKs`); `not_zKCritical_red_zK` now hypothesised on criticality;
   `fstIdx_red_of_tag_Ind_or_K` K-case closes via `fstIdx_iRK`.

**Net:** the tag-4 DISPATCH (the lap-88 "NEXT — assembly" item 1) is COMPLETE. `red` is now the genuine
Buchholz Def-3.2 reduct (validity-faithful + correctly case-dispatched), total + Σ₁-definable, build green.

## 🎯 OBJECTIVE (operator, binding)
Discharge `peano_not_proves_goodstein` (`Statement.lean:22`) **axiom-free**. Headline re-verified clean:
`[propext, sorryAx, choice, Quot.sound]` (0 math axioms). SOLE blocker = crux-2 = `redSound`.

## ▶ NEXT — `redSound` + unconditional descent (PENDING_WORK lap-89 box, steps 2–3)
1. **Dispatch-equivalence lemmas** (small, do first): `permIdx (zK s r ds) = lh ds ↔ zKCritical s ds`
   (⟸ = sentinel-no-hit `permIdxAux_eq_self_of_none`; ⟹ = `permIdxAux_isPermPrem_of_lt` contradiction).
   Plus the analogous `permIdx (zK s r ds) < lh ds ↔ ¬ zKCritical s ds` (have `permIdx_lt_of_not_zKCritical`
   for one direction). These let `redSound`/the descent know which Buchholz branch `iRK` fired.
2. **`redSound` : ∀ d, ZDerivesEmpty d → ZDerivation (red d)** (`Crux2Blueprint.lean:54`, currently
   `sorry`). `zDerivation_induction` over `d`; tag-4 case split on the `permIdx < lh` sentinel:
   - critical (5.1): `red = iRcritG` (via `red_zK_crit`), use `ZDerivation_iRcritG_of` (+ its R2 aux IH).
   - 5.2.1 splice: `red = iRKs`, use `ZDerivation_seqInsert_of_zK`. Supply the genuine halves
     `a=dᵢ{0},b=dᵢ{1}` (they ARE `znth (zKseq (red dᵢ)) {0,1}` by the IH `red dᵢ` is a ZDerivation);
     discharge `isChainInf_seqInsert`'s end-sequent hyps (`ha_ant`/`ha_rank`/`hb_succ`/`hb_ant`) from the
     critical premise's `inference_critical_pair` + Thm-3.4(a).
   - 5.2.2 replace: `red = iRKr`, use `ZDerivation_iCritAux_of_zK` (validity banked; the at-index premise
     reduct = `red dᵢ` by IH).
3. **`iord_descent_red` UNCONDITIONAL** (`Crux2Blueprint.lean:58`): per-branch via the banked descents
   `iord_descent_iRcrit_of_chain` (5.1) / `iord_descent_seqInsert'_of_iSpliceDescent` (5.2.1, discharge
   `r' ≤ dg(parent)` i.e. `rk(A(dᵢ)) ≤ dg(parent)`) / `iord_descent_iCritAux_of_iRedDescent` (5.2.2).
   Then `iord_red_iterate_descends` (already assembled in `Crux2Blueprint`, unconditional) →
   `false_of_ZDerivesEmpty` → M2 bridge → `Reduction.lean:68` → headline (once `#print axioms` clean).
   ⚠ `Crux2Blueprint.lean` is the assembly file (imports `InternalZ`+`Reduction`); its leaves `redSound`,
   `iord_descent_red`, `foundation_bot_to_Z_empty` (M2), `false_of_ZDerivesEmpty` (M3) are the open chain.

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. src/ sorries: `Statement.lean:22`, `Reduction.lean:68`,
`DescentSemantic.lean:582` + the 4 `Crux2Blueprint` leaves (M1b `redSound`/`iord_descent_red`, M2, M3).
No uncommitted edits. To `#print axioms`, use `lake env lean <scratch.lean>` OUTSIDE the tree (never
append-then-checkout the source — see `never-git-checkout-uncommitted-edits` memory).

## Key new API (this lap, all in `InternalZ.lean`)
`permIdx_defined`; `iRKr`/`iRKs`/`iRKc` (+ `_defined`/`_definable`, `fstIdx_*`/`zTag_*`); `iRK`/`iRKDef`/
`iRK_defined`; `fstIdx_iRK`/`zTag_iRK` (dispatch invariants); `red_zK` (raw `iRK` form); `red_zK_crit`
(5.1 refinement). The 5.2.1/5.2.2 validity + descent + ZDerivation constructors are the lap-86/87/88
banked objects (`zKValidF_seqInsert`, `ZDerivation_seqInsert_of_zK`, `iord_descent_seqInsert'`,
`iCritAux`, `ZDerivation_iCritAux_of_zK`, `iord_descent_iCritAux`).
