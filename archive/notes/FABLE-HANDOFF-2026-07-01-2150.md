# FABLE HANDOFF — 2026-07-01 21:50 EDT (architect/judge session, token-limited)

> **From**: the Fable-5 deep-dive session that wrote the masterplan and judged SPIKE-W3.
> **To**: the fresh session Trevor loads this into. You are the **architect/judge** for the
> goodstein-independence full-discharge campaign. Repo `~/src/goodstein-independence`, branch `plan`,
> HEAD `ded4cc2`. Read this, then the docs in §5's order. Everything below `#print axioms`-relevant
> was verified in-kernel by me, not doc-trusted — you can build on it, but keep the
> verify-don't-trust discipline for anything NEW a box reports.

## 1. Where things stand (one paragraph)

Trevor's mandate is **full discharge or abandon**: headline `𝗣𝗔 ⊬ ↑goodsteinSentence` at
`[propext, Classical.choice, Quot.sound]` (+ the 12 documented `native_decide` base facts — he
explicitly OK'd those). The masterplan (`MASTERPLAN-2026-07-01-ZERO-AXIOMS.md`, `f85a42f`) lays out
the one-engine path (phases W0–W7). Two operator-commissioned deciding spikes were launched
(`4026b4c`): **SPIKE-W3 ran and PASSED** with a mandatory amendment (`83e4bca`), which I
judge-validated and **sharpened with a statement-level catch** (`ded4cc2`). **SPIKE-W4 is RUNNING
RIGHT NOW** (Trevor fired it ~21:40 EDT) in a `c-yolo` box **bind-mounted on this same working
tree** — do not edit `wip/` or race commits until it exits. (The dirty `blueprint/src/*.tex` files
are some other session's WIP — leave them.)

## 2. Your immediate job: judge SPIKE-W4 when its verdict lands

Same discipline as my W3 pass (template: `E-2026-07-02-JUDGE-spike-w3-validation.md`):

1. Re-verify yourself: `export PATH="/opt/homebrew/bin:$PATH"; lake env lean wip/SpikeW4CutElim.lean`
   (exit 0, sorries disclosed) + the in-file `#print axioms` (expect `sorryAx` + canonical, **no new
   `axiom` decls**). Check `git show --stat` scope (spike files only).
2. Scrutinize the design against `SPIKE-W4-CONTROL.md`'s PASS/FAIL bar: `raise e α` must be
   **family-uniform** (a function of `(e, α)`, never of the ω-branch index `n`), `expTower`/`raise`
   explicit ONote functions, `< ε₀` structural via ONote.
3. **⚠️ The known trap to check for (my W3 catch, which the W4 order PREDATES):** the W4 candidate
   step statement is phrased over `ZekdSomeK` = `∃K, ZekdProv …`. Inside the cut-elim recursion,
   traversing a NON-principal `allω` node, the IH applies per premise → per-branch opaque `K_n`
   (and possibly per-branch `α_n`) — the same `∀∃ ↛ ∃∀` failure W3 hit. If the box's verdict didn't
   confront this, its PASS is suspect; the likely fix is the same shape as W3's: an explicit
   index-hypothesis carrier (`ZekdProv` with the base index in the statement, running index
   `max K n` explicit) rather than `∃K`-inside. Hand-check the max-algebra composition like I did in
   §3 of the W3 judge doc before ratifying.
4. Write `E-<date>-JUDGE-spike-w4-validation.md`, commit (branch `plan`, docs-only commits are safe
   even while a box runs; `.lean` edits are NOT until the box exits).

## 3. After both verdicts

- **PASS+PASS** → report to Trevor: masterplan W0–W7 ratification is his call (the DIRECTION
  carve-out already says this). Then **W0 executes host-side** (you): reify the 7 PathB capstones as
  REAL statements — using the **corrected K-hypothesis form** (§4 below) for the embedding capstone
  and whatever W4's validated statement is for cut-elim — rewire `PathBProbe`'s ledger +
  `blueprint_audit` + `lean-axiom-gate` allowlist, prune the ~25k-line Route-A mass
  (`InternalZ`/`Crux2Blueprint`/`Zsubst`/`Internal*`) into a `GoodsteinPABanked` lib target off the
  per-lap build, and rewrite `DIRECTION.md`'s CURRENT DIRECTIVE onto the W-phase ladder (operator
  ratifies). Statements are ARCHITECT-owned (LOCK discipline) — never delegate statement-writing to
  grind laps.
- **Either FAIL** → T-W3/T-W4 fired: present Trevor the fallback-architecture-vs-abandon fork
  (fallback = Buchholz fully-operator-controlled derivations; masterplan §5/§8).

## 4. Load-bearing technical findings (don't re-derive)

- **Kernel-verified banked assets** (I printed these myself): `ZinftyGen.Deriv.Provable.cutElim`
  (full unbounded ω-rule cut-elim) and `Embedding.embedC` (complete unbounded PA↪Z∞ embedding) are
  **clean 3-axiom theorems**; so are the someK keystones `cutReduceAllAux_control`,
  `inductionLeaf_runningIndex_witnessBound`, `ofBoundedTruth`,
  `inductionLeaf_cutTowerStepWithTerm_someK_probe`, `ZekdSomeK.allInv`.
  `LowerBoundHardy.lowerBound_hardy_selfcontained` + `cichon_caicedo…` = 3 + the 12 native base.
  The import closure smuggles NO foreign axioms (footprints would show them).
- **Route unification**: the lap-171 "Wainer NOT Towsner" fork is false — Buchholz–Wainer 1987
  proves the classification BY ω-rule + cut-elim, so the `OperatorZinfty`/someK lane IS the
  Wainer-debt discharge substrate. One engine, two read-offs (Towsner `¬B` contradiction +
  `wainer_bound…` `axiom → theorem` corollary). Masterplan §3; DIRECTION has the scoped carve-out.
- **Capstone defect**: the 7 `pathB_*_capstone` axioms chain through **empty inductive Props** —
  each is logically equivalent to the whole headline, individually undischargeable. W0 reifies them.
- **The W3-validated master-statement shape** (Towsner §16; binding for W3 grind step (1) — re-state
  the skeleton on this and re-run the assembly BEFORE any case lemma):

  ```lean
  ∃ c d₀ K₀ (α e : ONote), α.NF ∧ e.NF ∧
    ∀ K env, (∀ i, env i ≤ K) →
      ZekdProv α e (max K₀ K) d₀ c (Γ.image (fun φ => Embedding.asg env ▹ φ))
  ```

  Composition: branch `n` has `env' = n :>ₙ env ≤ max K n`; `max K₀ (max K n) = max (max K₀ K) n` =
  `allω`'s base+running-index shape; constant family `β n = α`; single structural `e` (witnesses paid
  by the running index — `e` is raised only in W4). Headline: `γ` closed, `K := 0` → all structural.
  Hand-checked, NOT yet machine-checked.
- **W3 spike result**: candidate global induction elaborates; 8/10 cases mechanical vs the banked
  someK API; hard cases = `all` (the `EmbeddingBound` uniform-family port) + `axm` (a dispatcher over
  W1 `ofBoundedTruth` + W2 induction cut-tower once those land).
- **Full independence** (Trevor's "completely"): the `¬`-half `𝗣𝗔 ⊬ ∼γ` is cheap at W6 — Foundation
  soundness + `Bridge` + `goodstein_terminates_engine` (`Domination.lean:389`, in-repo).
- **Current odds** (stated to Trevor, post-calibration debate): engine ~50–60% on this architecture,
  ~70–75% with the fallback; median 50–75 laps; P(>100 laps or abandon) ~30–40%. W3's PASS supports
  but does not yet move these — W4 is the remaining coin-flip.

## 5. Read order for full context

1. This file. 2. `MASTERPLAN-2026-07-01-ZERO-AXIOMS.md`. 3. `SPIKE-W3-VERDICT.md` +
`E-2026-07-02-JUDGE-spike-w3-validation.md`. 4. `SPIKE-W4-CONTROL.md` (+ its verdict when it lands).
5. `DIRECTION.md` top blocks (mandate + spike carve-out). 6. Background as needed:
`ROUTE-SCOPE-REALITY-2026-07-01.md`, `ON-LINE-FINDINGS-2026-07-01-…md`,
`E-2026-07-01-JUDGE-lap171-route-review.md`, `STATUS.md` head.

## 6. Operational cautions

- `c-yolo` refuses to launch from inside Claude Code — Trevor fires boxes himself; never offer to.
- The box works in THIS directory (bind-mount). While one runs: docs-only commits OK, no `.lean`
  edits, no `wip/` touches.
- Boxes' handoffs/verdicts: verify in-kernel before ratifying. The estimates culture here runs 2–4x
  optimistic; the LEDGER (what compiles, what `#print axioms` says) is the only trustworthy layer.
- KB rows updated through the masterplan (projects/README goodstein + lean-gallery rows); the W3/W4
  spike outcomes are NOT yet in the KB — fold them in at the next natural point (or /wrap).
