# Handoff: lap 135 — existence-form PIVOT settled & PORTED TO SRC; `false_of_ZDerivesEmpty` decomposed

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `cd01adb` · tree CLEAN · build 🟢 green
(`lake build GoodsteinPA`, **1326**). Headline faithful & UNCHANGED:
`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms).
**src sorries: 11 → 12** (the monolithic `false_of_ZDerivesEmpty` split into 2 named, attackable
sub-sorries — PROGRESS per the lap-135 DIRECTION metric, not regress).

## 🎯 What this lap did (operator mandate: M1b-term, existence-form spike FIRST)
Ran the lap-132 existence / least-descending-reduct spike to a **decisive PIVOT**, then **ported the
decomposition into `src/GoodsteinPA/Crux2Blueprint.lean`** (the directive rewards named src sub-sorries
over a monolithic one). `false_of_ZDerivesEmpty` is **no longer a sorry** — it is a sorry-FREE composition.

## 🧠 Verdict (PIVOT) — precise scoping derived from the actual code
The existence form **BUYS** (real): removes the BLOCKING "fixpoint ⟹ cut-free" obstruction (the lap-129
refutation that drove laps 120-131) and the total-deterministic-engine requirement; the critical/
non-critical split **dissolves into one one-shot `majorIdx`** (`majorIdx_botOrbit_reducible` /
`firstBotPrem_reducible` / `majorPrem_zAx{All,Neg}_cutPartner` all BANKED, used as ∃-facts, not a threaded
engine — `majorIdx` never stalls on the ⊥-orbit).
It **KEEPS** (does NOT shed): the `hAll` cutFormula bridge (tag-5/6 + critical) and the tag-4 structural
recursion (relocated to a `<`-induction over derivations with non-empty antecedent). M3 plumbing either way.

## ✅ Landed in src (Crux2Blueprint.lean, all green)
- **`false_of_ZDerivesEmpty`** (~1444) — PROVEN: `prwo_forbids_existence_descent (fun _ hd =>
  ZDerivesEmptyR_descent_step hd) hz`.
- **(E') `ZDerivesEmptyR_descent_step`** — PROVEN modulo the K case (Ind root via `iord_descent_red_zInd`;
  K root reduces to `descent_step_K_majorIdx`).
- **SORRY-FREE infra:** `iRedDescent_zK_replace_explicit` + `iord_descent_zK_replace_explicit` (index-
  generic `red`-free REPLACE descent kernel — works at `majorIdx`, the banked kernel was keyed to `permIdx`
  via `hred`); `descent_K_majorIdx_Ind_descends` (tag-3 Ind-major DESCENT, from `red_zInd`+`iRedDescent_zInd`).

## 🎬 The two NEW named sub-sorries (the whole remaining termination content)
1. **`descent_step_K_majorIdx`** (Crux2Blueprint:**1398**) — per-step K-case math: regular `∅→⊥` K-node ⟹
   ∃ sound descending reduct. Dispatch on `dⱼ = znth ds (majorIdx (zK s r ds))` (`majorIdx_botOrbit_reducible`
   gives in-range / succ `⊥` / `zTag ∈ {3,4,5,6}`):
   - **tag 3 (Ind)**: DESCENT proven (`descent_K_majorIdx_Ind_descends`); residual = the SOUNDNESS witness
     (`ZDerivesEmptyR` of `zK s r (seqUpdate ds majorIdx (red dⱼ))`) → routes to `zKValidF_iIndReduct_of_zInd`
     (Crux2Blueprint:79) + replace-preservation (`ZRegular/ZFresh/ZSeqAnt_red_zK_replace`, banked but keyed to
     permIdx — need majorIdx variants, or generalize).
   - **tag 5/6 (∀/¬-axiom)**: the PRINCIPAL CUT at `(i', majorIdx)`, `i'` = upstream R-intro PINNED by
     `majorPrem_zAxAll_cutPartner`/`_zAxNeg_cutPartner` (InternalZ:9217/9245, BANKED). `iRKcCrit`-style;
     soundness = the SHARED `hAll` bridge (lap-134's `redZKReady` ∀-pair export + `ZRegular` thread — see the
     lap-134 box below, still on-path); descent = banked `iord_descent_iRcritG_*`.
   - **tag 4 (chain)**: the relocated structural `<`-recursion — generalize (E') to Z-derivations with
     NON-EMPTY antecedent (chain threading), induct on `<`. The deep core both routes share.
2. **`prwo_forbids_existence_descent`** (Crux2Blueprint:**1430**) — M3 plumbing: realize `redLeast d := μ d'.
   [ZDerivesEmptyR d' ∧ icmp (iord d')(iord d)=0]` as a `𝚺₁` least-witness (`least_number` over (E')'s
   predicate, like `firstBotPrem_reducible`), internalize `n ↦ iord (redLeast^[n] z)` as `gentzenDescentφ`,
   apply `PRWO(ε₀)`. Reuses `wip/GentzenCon` (`gentzenDescentφ`/`prwoInstance`). The `𝚺₁`-ness is load-bearing
   (`iord` not internally well-founded in nonstandard `V`).

## 📌 Recommended NEXT (hardest-first, on M1b-term)
The tractable entry to actually CLOSE a sub-sorry is **`zKValidF_iIndReduct_of_zInd`** (Crux2Blueprint:79) —
gates `descent_step_K_majorIdx`'s tag-3 soundness AND `redSoundGen`'s Ind branch. It is the genuine Buchholz
induction→cut-chain reduction: build `isChainInf s (irk p) (iIndReductSeq d0 d1 1)` where
`iIndReductSeq d0 d1 1 = ⟨d1, d0⟩` from `zIndWff` (base `d0`: `Γ→F(0)`; step `d1`: `Γ,F(at')→F(at'+1)`).
Banked support: `iseqReg_iIndReductSeq`/`zfresh_iIndReductSeq`/`zSeqAnt_iIndReductSeq` (Zsubst) already cover
the reg/fresh/seqAnt conjuncts; the `isChainInf` (j0 exit = `d0`, threading `F(at')→` earlier succ, rank
`irk p`) + the per-premise `iperm`/formula-hood are the open content. NOT a one-lapper, but self-contained.

## ⚠️ Gotchas
- Newest baton by NUMERIC sort: `ls HANDOFF-*-lap*.md | sort -t p -k2 -n | tail -1` (not `ls | tail`).
- DIRECTION.md CURRENT DIRECTIVE is altitude-owned — READ it, do NOT edit it. Its success metric was updated
  this lap (commit 0627d27): "advance the crux — sub-lemma CLOSED **or** crux DECOMPOSED into named src
  sub-sorries (raising the count is PROGRESS)." Off-path lemma-banking / count-gaming does NOT count.
- `Crux2Blueprint.lean` IS in the default build despite the stale "not imported" header comment.
- `(5:V) ≠ 4`-style numeral ineqs in model `V`: use `simp`, not `decide`.

## 📁 Key files
- `src/GoodsteinPA/Crux2Blueprint.lean` — existence endgame (the new block before `end`), soundness front
  (575/614/648), `redSoundGen` (1050) + its K sub-sorries (687/790/963), `zKValidF_iIndReduct_of_zInd` (79).
- `src/GoodsteinPA/InternalZ.lean` — `majorIdx`/`majorIdx_botOrbit_reducible` (9147/9155),
  `majorPrem_zAx{All,Neg}_cutPartner` (9217/9245), `redZKReady_of_zKValid` (8338, hAll target),
  `iIndReductSeq` (2955), `zIndWff` (1681), `zDerivation_zInd_inv`.
- `src/GoodsteinPA/RedZKDescent.lean` — `iRedDescent_red_zK_replace_eq` (334, the permIdx kernel I generalized).
- `PENDING_WORK.md` lap-135 box — full verdict + ordered next attacks.

---
**→ Next session: HEAD `cd01adb`, build green. The termination crux is now an honest 2-piece src decomposition
(`descent_step_K_majorIdx` + `prwo_forbids_existence_descent`). Don't re-litigate the PIVOT. Pick the tractable
`zKValidF_iIndReduct_of_zInd` (gates tag-3 soundness) OR push `descent_step_K_majorIdx`'s tag-5/6 via the
banked cutPartner + the shared `hAll` (lap-134 redZKReady export). Both are on M1b-term.**
