# Handoff: lane D read-off (lap 195) — residue NARROWED to the gated core + Route-B (growth-coupled) path identified

**Date**: 2026-07-03 · **Branch**: `plan` · **Build**: 🟢 **`lake build`** (both libs, 1341 jobs) —
**NOT** `lake build GoodsteinPA` (see gate note below). **Headline**:
`GoodsteinPA.peano_not_proves_goodstein = [propext, Classical.choice, goodstein_implies_consistency,
Quot.sound]` (sorryAx OFF, UNDRIFTED — re-verified via `#print axioms`). Tree CLEAN.

**Order in force**: `REBUILD-Z-SERIES-1-ORDER-2026-07-02.md` · **Binding directive**: `DIRECTION.md`
CURRENT DIRECTIVE **lap-192 block** (MANDATE = lane D; top-rank cut JUDGE-GATED, wip-only probes).
**Ledger**: `REBUILD-Z-SERIES-1-LEDGER.md`.

## ⚠️ BUILD-GATE CORRECTION (act on this before any Route-B green claim)
`OperatorZef2` is **NOT** in the `lake build GoodsteinPA` dependency graph — **nothing in the
`GoodsteinPA` lib root imports it** (it is the parallel Route-B calculus). It compiles only via the
**second lean_lib `GoodsteinPABlueprint`** (`src/GoodsteinPABlueprint.lean:10`). So
`lake build GoodsteinPA` (1328 jobs) reports 🟢 **without recompiling `OperatorZef2`** — editing that
file and running only that command verifies a **STALE olean** (caught this lap: source edited but the
olean stayed at a pre-edit timestamp). **Correct gate = `lake build` (1341 jobs)** or
`lake build GoodsteinPA.OperatorZef2` / `lake build GoodsteinPABlueprint`. Prior handoffs' "Build 🟢
`lake build GoodsteinPA`" line is INSUFFICIENT for Route-B edits.

## ✅ What this lap did (3 green commits) — mandated lane D, `readoffD_trapped` residue

1. **Branch-0 narrowing (in-kernel).** KEY structural fact **`rel1 f 0 = f`** (since `max 0 x = x`),
   so branch `0` of every `allω` node runs at the UN-relativized slot `f` with `rel1 f 0 0 = f 0`.
   `readoffD_aux`'s trapped case now recurses into branch 0 whenever `χ/[nm 0]` is FALSE, closing at
   the SHARP bound `f 0` with NO residue. The residue `readoffD_trapped` now carries the added
   hypothesis `h0 : atomTrue (χ/[nm 0])` — it survives ONLY for **non-monotone** Δ₀ matrices (`χ/[nm 0]`
   true while `∀⁰ χ` false, so every false branch sits at index `≥ 1` where the slot grew to
   `rel1 f k₀ 0 = f k₀ ≥ f 0`).
2. **`readoffD_trapped_of_mono` banked SORRY-FREE** (`[propext, Classical.choice, Quot.sound]`): under
   the local monotone-instance condition `atomTrue(χ/[nm 0]) → atomTrue(∀⁰χ)`, `h0` contradicts
   `hfalse` ⇒ residue discharged. Proves the branch-0 mechanism fully closes the downward-closed-guard
   fragment (as for Goodstein's bounded-`∀` step clauses) WITHOUT E–W's (Ax2). A ready building block
   for a monotone-guarded specialization of `readoff_delta0_Zef2`.
3. Build-gate fix + this baton.

## 🔑 Route-decisive finding (read Towsner Thm 17.1 + E–W Lemma 31 this lap)
The generic residue is **NOT a slick structural fix**. Towsner's cut-free lower bound (Thm 17.1)
proves its invariant with an explicit **growth coupling** — clause (ii) `∃y g_y(n)=0`, `n ≤ k`, **with
`𝒢(n) > h_α(k)`**. `readoffD_aux`'s falsity invariant carries NO coupling, so nothing ties the grown
budget `f k₀` back to `f 0`. E–W's Lemma 31 dodges the descent only via **(Ax2)** (verify true Δ₀
leaves SEMANTICALLY). So the residue closes by exactly two routes:
- **(A) add (Ax2)** to `Zef2` — architect-gated, SHARED with rung E's Ax2-adequacy (do NOT self-ratify;
  wip-only if at all).
- **(B) growth-coupled invariant** — re-cast `readoffD_aux`'s *internal* invariant (Towsner (ii)-style:
  tie the witness bound to `ewN α` / the `ewIter` tower + the banked Part-2 fast-growing lower bound).
  This reformulates MY OWN helper, NOT the ratified `readoff_delta0_Zef2` statement ⇒ **IN-SCOPE
  proof-only work**, but multi-lap and coupling to the Hardy/Wainer bank.

## 🚧 NEXT (lane D — Route B, the mandated proof-only continuation)
Begin the growth-coupled `readoffD_aux'`: state the invariant tying the `∃⁰φ` witness budget to the
node's `ewN α`/slot via the `ewIter`/`ewN` algebra; prove `axL`/`exI`/`wk`/`weak`/`cut` (mechanical,
mirror `readoffD_aux`); isolate the SINGLE coupling lemma at the `allω` node (the ordinal drop
`β k₀ < α` must compensate the slot growth `rel1 f k₀`). Cross-check against the banked Hardy Lemma-19
brackets in `src/GoodsteinPA/Hardy.lean` and `ewIter`/`ewN` lemmas in `OperatorZef2.lean`/`EwIter.lean`.
If Route B stalls at the coupling, that IS the escalation input to hand the series-end judge alongside
the (Ax2) decision (D and E share it).

## 📁 Key files
- `src/GoodsteinPA/OperatorZef2.lean` — `readoffD_aux` (branch-0 narrowing; `allω`/trapped case
  ~:1052ff), `readoffD_trapped` (residue `sorry`, narrowed, full diagnosis in docstring),
  `readoffD_trapped_of_mono` (PROVEN sorry-free), `readoff_delta0_Zef2` (wired). Other sorries:
  top-rank cut (judge-gated), `embedding_Zef2` rung E (architect-gated), `wainer_splice_Zef2`.
- `PENDING_WORK.md` — lap-195 block at top (build-gate note, mono lemma, Towsner grounding, Route B).
- `DIRECTION.md` — CURRENT DIRECTIVE lap-192 block (BINDING; mandate = lane D).
- No uncommitted edits after the doc commit.
