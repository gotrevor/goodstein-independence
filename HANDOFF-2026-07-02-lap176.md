# HANDOFF — 2026-07-02, lap 176 (REBUILD-Z Scope-A: CRUX FINDING + independent kernel re-audit)

> **Terminal state:** branch `plan`, working tree clean, full repo green (`lake build`, 1333
> jobs). `src/GoodsteinPA/OperatorZeh.lean` = exactly 3 `sorry`s (the §5 f-slot pins), no new
> `axiom`s. Everything else in the module is axiom-clean. **No `src/` code changed this lap** —
> the advance is a source-grounded statement-shape finding on the crux, plus a kernel re-audit.

**Headline advance: the P1 obstruction (the crux's hardest open piece) is very likely a
STATEMENT-SHAPE ARTIFACT, not a real threading wall.** Cross-checking the three NOT-LOCKED §5
pins against the primary source (Eguchi–Weiermann arXiv:1205.2879, Def 23 + Lemmas 24–31,
summary on disk) shows pins 1–2 conflate two distinct E-W lemmas, and that conflation is exactly
what makes the P1 conjunct read as "false unconditionally per K2b." Full writeup:
**`REBUILD-Z-LAP1-FINDING-2026-07-02-fslot-control-raise.md`** (read before the judge opens the
gate). `PENDING_WORK.md` lap-176 section carries the actionable summary.

## The finding in one paragraph

E-W **Lemma 25 (cut-reduction)** keeps the ordinal-operator control **FIXED** and updates the
numeric operator by **composition `f∘g`**; E-W **Lemma 30 (collapse)** **RAISES** the control
and **ITERATES** the numeric operator (`f ↦ f^{F^α(0)+1}`). Pins 1–2
(`cutReduceAllAuxRunning_Zf`:659, `stepAllω_Zf`:674) pair a control-**raise** (`raise e α = e+ω^α`,
line 58) with **composition** `f∘g` — Lemma 30's raise glued to Lemma 25's numeric update, which
matches no single E-W lemma. Pin 3 (`cutElimPass_Zf`:690) correctly pairs raise with iteration
(`f'`, ∃). The P1 conjunct `NormControlled (f∘g) (raise e α) m` unfolds to `∀x, hardy (raise e α)
(max m x) ≤ (f∘g) x` — asking **composition to dominate the faster-growing RAISED Hardy**, which
E-W discharges only by **iteration** (Lemma 19 bound `N(α) ≤ f^{F^α(0)}(0)`). So P1 is unprovable
*as stated* for a structural, source-identifiable reason.

**Recommended fix (Option A, judge-owned statement amendment):** confine all raise + iteration to
`cutElimPass_Zf`; restate the reduction/step conjuncts at fixed control (`NormControlled (f∘g) e m`
— provable from input domination via banked `NormControlled.mono` +
`seam1_bump_absorbed_by_composition`). P1 then **dissolves from the reduction**, and the secondary
R3 "once per pass vs per-step raise, no `mono_e` to unify" tension evaporates with it. Option B
(raise stays at reduction) forces the numeric slot to iteration → the reduction becomes a collapse
→ reject unless K1 info-free membership provably blocks E-W's fixed-`F` absorb step (~20% escape).
Confidence the tension is real + decision-relevant: **~80%.**

## Option A is KERNEL-FORCED (the ~20% Option-B escape is closed)

The finding's one residual doubt (could K1 info-free membership *force* a raise into the reduction →
Option B?) is closed by the existing `mono_e_membership_gate_refuted` (`OperatorZeh.lean:224`): a
reduction concluding at `raise e α` from `e`-premises must re-establish `exI` bounds `n ≤ hardy e m`
at `n ≤ hardy (raise e α) m`, which is impossible (`hardy (raise e α)` can be strictly smaller —
`hardy ω 0 = 1 < 5`; no `mono_e`). So pins 1–2's raise is **unsound as a reduction**, not merely
unfaithful — Option A is forced. The *pass* (pin 3) may raise only because it CONSTRUCTS at the
raised control with a fast-growing ITERATE `f'` that pays the new bounds (Lemma 30) — kernel-separating
reduce-compose-no-raise (Lemma 25) from pass-raise-iterate (Lemma 30), and re-confirming `f'` must be pinned.

## Option A VALIDATED in-kernel: `NormControlled.comp` banked (the only `src/` change this lap)

The lap-176 finding *claimed* Option A's fixed-control reduction conjunct is "provable from banked
plumbing"; this lap proved it. New banked lemma (§5, `[propext, choice, Quot.sound]`):
`NormControlled.comp : NormControlled g e m → (∀y, y ≤ f y) → NormControlled (f∘g) e m`. The
inflationary hypothesis `∀y, y ≤ f y` is exactly E-W's `(f.1)` (`2y+1 ≤ f y`). So under Option A the
reduction's numeric obligation collapses to a one-liner — confirming P1 was **mis-located, not
hard**. This is permitted sibling infrastructure (a fact about the stable `NormControlled` def;
consumes no f-slot pin; touches no gated body); it is the plumbing lap-2 will apply.

## Companion finding (kernel-checked): pin 3's existential `f'` is VACUOUS

`cutElimPass_Zf` concludes `∃ f', … ∧ NormControlled f' (raise e α') m`, but `ZehProv` is f-free,
so `f'` is decoupled from the derivation, and `∃ f', NormControlled f' e' m` is trivially true for
any `e', m` (kernel-checked: `⟨fun x => hardy e' (max m x), fun x => le_rfl⟩`, sorry-free). The
conjunct adds **no quantitative content** — stronger than verdict Q2's "abstraction choice": it is
vacuous and **read-off-breaking** (E-W Lemma 31's `witness ≤ f(0)` needs the *concrete* iterate).
Fix: pin `f'` to the E-W iterate of the input `f` (`f' = f^{…}`, Lemma 30), which is where Option A
relocates the real P1 work and where Lemma 19 makes it achievable. (Pins 1–2 are NOT vacuous —
their `f, g` are explicit params.) Minor open (not load-bearing, `ZehProv` slack absorbs it): pin 1's
`osucc(α+γ)` output vs E-W Lemma 25's clean `α+β` — likely Buchholz §19.6 bookkeeping (that summary
is not on disk; not pursued).

## Second advance: independent kernel re-audit of the lap-175 terminal state

Re-ran `#print axioms` directly (not trusting the verdict table): `allInv_Zeh`, `orInv_Zeh`,
`andInvL_Zeh`, `andInvR_Zeh`, `Zeh.mono_H`, `readoff_sigma1`, `probe_allomega_reassembly_Zf` are
all `[propext, Classical.choice, Quot.sound]` — no `sorryAx`. `sorryAx` appears only on the three
§5 pins (+ the seam-1 arm probe, whose sole sorry-dep is the reduction pin). Verdict claims hold.
Also confirmed A3 is complete: the banked `Zekd` inversion suite is exactly four theorems
(`OperatorZinfty.lean:221/326/404/484`), all four ported; the minimal `Zeh` core has no further
invertible rule, so the companion set is closed. Permitted-sibling plumbing surface exhausted.

## Verdict judge-question #3 resolved (source-grounded): keep `stepAllω_Zf` unified

Core check: `allω`=⋀ (∀, all branches relativized), `exI`=⋁ (∃, one bounded witness `n ≤ hardy e m`).
The principal cut `{∀χ, ∃¬χ}` is a **single ⋁-principal reduction** (E-W Lemma 25): witness from D₂
(exI), invert D₁ (∀-side) via the proven `allInv_Zeh` (Lemma 24), cut at lower complexity. **Do NOT
split** — ∀χ and ∃¬χ are the same (dual) cut; the ∀-side is never its own reduction. All three
verdict judge-questions now have source-grounded answers (Q1 = P1 artifact/Option A; Q2 = vacuous
`f'`, must pin; Q3 = keep unified) — see the finding doc's "Complete judge-input package" table.

## Rails (LOCK §2) + run boundary — held

No `src/` edit; no LOCK edit; no gated body touched (statement-shape audit is permitted Scope-A
faithfulness work, and §5 is NOT-LOCKED by design — this is input to the judge gate, not a
challenge to a locked form). `wip/SpikeW4*`/`SpikeZ1Seams.lean` untouched. The RUN is cap-bounded
(governor self-stop can't arm while Route-A sorries stand — don't thrash).

## Resume point

Judge ratification of `REBUILD-Z-LAP1-VERDICT.md` **plus** a ruling on the lap-176 finding
(Option A vs B). The finding makes the gate cheaper to clear: if Option A is adopted, lap 2's
reduction discharge should be near-immediate from banked plumbing, with the genuine threading work
relocated (correctly) to `cutElimPass_Zf`'s iterated slot. Do NOT grind any §5 pin
pre-ratification.
