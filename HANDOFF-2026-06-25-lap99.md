# HANDOFF — lap 99 · the non-Rep replace VALIDITY LAYER landed (both R-rule + L-rule constructors)

> Branch `plan`. HEAD = `74da23c`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs).
> Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (bare `sorry`;
> the lone math residual `sorryAx` = crux-2). `peano_not_proves_consistency` clean. All three new
> bricks axiom-clean (`[propext, Classical.choice, Quot.sound]`).

## ✅ LANDED lap 99 — the conclusion-reducing replace VALIDITY constructors (frontier item 2, validity half)
Three commits, each green 1325 + axiom-clean. These discharge the **validity layer** of the two open
`ZDerivation_red_zK` replace branches (`Crux2Blueprint.lean:206,214`) — the genuine cut-elimination step
the keep-`Π` `ZDerivation_iCritAux_of` cannot do.

1. **`ZDerivation_iCritReplaceReduce_of`** (`InternalZ.lean`, after `ZDerivation_iCritAux_of`) — the
   **R-rule** constructor (I∀/I¬ selected premise). Replacing premise `i` by a reduct `v` whose
   end-sequent IS the reduced sequent `s'` (same antecedent `chainAnt ds i`, reduced succedent
   `seqSucc s'`), with `s'` keeping the parent antecedent, yields `ZDerivation (zK s' r (seqUpdate ds i v))`.
   Assembles lap-98's `isChainInf_seqUpdate_reduceR` + the validity conjuncts via `zDerivation_zK_intro`.
   Threading/rank up to `i` (`hthread`/`hrank`) are CALLER-SUPPLIED hypotheses (the deep residual — see
   below).

2. **`ZDerivation_zK_seqAddAnt`** (+ `isChainInf_seqAddAnt`, `zKValidF_seqAddAnt`,
   `forall_IsUFormula_seqCons`) — the **L-rule axAll** constructor. `red dᵢ = dᵢ` (premises unchanged),
   conclusion gains the cut-formula instance `A(k)` in its ANTECEDENT (`tpReduce (isymLk …) Π 0 =
   seqAddAnt A(k) Π`). Validity is **pure conclusion-antecedent monotonicity** (same `j₀`, threading only
   RELAXES). **Cleanest of the three** — needs NO `i ≤ j₀`. Side hyps: `Seq (seqAnt s)`, `IsUFormula A`.

3. **`tpReduce_tp_zAxAll` / `tpReduce_tp_zAxNeg`** (`InternalZ.lean`, before `zDerivation_zAtom_inv`) —
   the axiom conclusion-tracking read-outs. **Completes frontier item 1 (conclusion-tracking for ALL node
   types):** atom/Ind (Rep, id), I∀ (`red_zIall_tpReduce`), I¬ (`red_zIneg_tpReduce`), axAll
   (`seqAddAnt F(k) Π`), axNeg (`seqSetSucc Π p`).

## ✅ LANDED lap 99 (7th commit) — atom/Ind portion of line 214 MACHINE-CHECKED
`ZDerivation_red_zK`'s non-chain replace branch (`Crux2Blueprint.lean:214`) now DISPATCHES on
`tp dᵢ = isymRep`: the **Rep sub-case (atom/Ind selected premise)** is closed via
`ZDerivation_red_zK_replace` + `red_rep_of_tp_isymRep` (`hsel` vacuous since `zTag dᵢ ≠ 4`). The fat sorry
is split — the residual sorry is now precisely the **non-Rep** cases (I∀/I¬/axAll/axNeg). Headline
unchanged (`[propext, sorryAx, choice, Quot.sound]`, 0 math axioms). The remaining sorry needs the
O3-freshness motive (PENDING_WORK lap-99 path A) + conclusion `Seq(seqAnt s)`/wff threading.

## ⭐⭐ LANDED lap 99 (12th commit) — I∀ non-`Rep` replace FULLY ASSEMBLED (`ZDerivation_zK_replace_zIall_of`)
THE de-risking capstone. The hardest non-`Rep` case (I∀ selected premise) now assembles **end-to-end** in
the kernel (axiom-clean): `red dᵢ = zsubst d0 a 0` feeds `ZDerivation_iCritReplaceReduce_of`, with EVERY
hypothesis discharged from banked lemmas (`red_zIall_tpReduce` conclusion-tracking, `tag_uformula_of_ZDerivation`
+ `iperm_tp_fstIdx_of_ZDerivation` for the reduct's wff, `seqAnt/seqSucc_seqSetSucc`). The **only**
un-discharged inputs are the genuine orbit data — O3 freshness (`hpfresh`/`hΓfresh`), threading/rank up to
`i` (`hthread`/`hrank`, from `permIdx ≤ j₀`), reduced-succedent wff (`hsucc_wff`). ⟹ **The non-`Rep` route
is mechanically COMPLETE given the invariants; the remaining work is PURELY supplying them via the
strengthened `redSoundGen` motive.** Supporting brick (11th commit): `tag_uformula_of_ZDerivation` (bundled
`hf1_v…hf6_v` discharge for any `ZDerivation`, via `IsSemiformula.isUFormula`).

## ✅ LANDED lap 99 (9th commit) — `isChainInf_reduceR_membership` (general `inAnt`-form `isChainInf`)
The fully general conclusion-reducing `isChainInf` at `j₀ = i`, threading premise `i`'s (possibly changed)
antecedent + lower premises into `s'` via MEMBERSHIP (`inAnt`), not exact equality. Subsumes
`isChainInf_seqUpdate_reduceR`; directly handles all four non-`Rep` antecedent/succedent shapes. **With this
+ `ZDerivation_iCritReplaceReduce_general`, the validity-layer infrastructure for the non-`Rep` replace is
COMPLETE.** What remains to close `Crux2Blueprint.lean:214`'s non-`Rep` sorry is ONLY the per-case
`hsucc_v`/`hant_thread`/`hlt_thread`/wff data — all of which need the threaded sequent-wellformedness
(`Seq (seqAnt s)`, confirmed NOT derivable: `seqAnt s = π₁ s`) + O3-freshness + `∃ permissible ≤ j₀`
invariants (the motive cascade, PENDING_WORK lap-99 path A).

## ✅ LANDED lap 99 (8th commit) — `ZDerivation_iCritReplaceReduce_general` (the unifying constructor)
Factors `isChainInf` OUT as a hypothesis (mirrors `ZDerivation_seqInsert_of`) so ALL four non-`Rep` cases
share ONE constructor despite distinct conclusion bookkeeping. Hypotheses: `hci : isChainInf s' r
(seqUpdate ds i v)` (case builds its own), `hsucc_wff`/`hant_wff` (conclusion wff), `hv_succ_wff` (v's
succedent wff), `hperm_v` + tag-formula-hood of v. Off-`i` conjuncts inherit from parent `zKValidF`. This is
THE clean abstraction the per-case wiring calls — the validity-layer infrastructure for the non-`Rep` replace
is now COMPLETE. The only remaining input per case is the `isChainInf` (needs O3/Seq/threading invariants).

## ⭐ KEY FINDING (lap 99): the non-Rep replace splits into FOUR cases, not three
Reading `tpReduce` against each `tp`: the lap-98 "three cases" (R/L/Rep) actually has the **L-rule split
in two** by the axiom polarity:
- **axAll** (`tp = isymLk k (∀p)`): `tpReduce = seqAddAnt F(k) Π` → conclusion ANTECEDENT weakening →
  `ZDerivation_zK_seqAddAnt`. **DONE.**
- **axNeg** (`tp = isymLk 0 (¬p)`): `tpReduce = seqSetSucc Π p` → conclusion SUCCEDENT REPLACEMENT (the cut
  formula `p` becomes the new succedent). **This is its OWN constructor** — `red dᵢ = dᵢ` keeps the premise
  succedent `seqSucc s`, so the R-rule `ZDerivation_iCritReplaceReduce_of` (which needs `seqSucc (fstIdx v)
  = seqSucc s'`) does NOT apply. **OPEN** — needs a succedent-replacement chain constructor; in Buchholz
  this reduction also restructures the premises (the §5 ¬-axiom cut), so it is genuinely subtler than axAll.

## ▶ THE REMAINING NUT, sharply localized (to wire `Crux2Blueprint.lean:206,214`)
The two replace branches now have all the VALIDITY constructors except axNeg. To wire them, two deep pieces
remain, both feeding the constructors' hypotheses:

1. **THE `i ≤ j₀` threading derivation (R-rule).** `ZDerivation_iCritReplaceReduce_of` needs `hthread`/
   `hrank` = threading/rank up to `i = permIdx (zK s r ds)`. The parent `isChainInf s r ds` gives these only
   up to ITS `j₀`. To take `j₀ := i` (the I-rule premise carries the conclusion succedent: for a selected
   I∀, `tp dᵢ = isymR(∀p)` permissible ⟹ `seqSucc s = ∀p = A_i`, so `i` IS a valid j₀-succedent index), we
   need threading `∀ i' ≤ i` + rank `∀ i' < i`, which hold IF `i ≤ j₀`.
   **✅ KEY STEP LANDED (lap 99, axiom-clean): `permIdx_le_of_isPermPrem`** — `permIdx` is the LEAST
   permissible index, so `permIdx ≤` ANY permissible premise index. Per Buchholz §5.2 (verbatim, PDF md
   line 86: "Let `i ≤ j₀` minimal such that `tp(dᵢ) ◁ Π`"), a Buchholz-non-critical chain HAS a permissible
   premise at index `≤ j₀`, hence **`permIdx ≤ j₀`**. ⚠ NOTE the repo's `zKCritical` quantifies over ALL
   `i < lh ds` (not `i ≤ j₀`), so repo-non-critical ⊉ Buchholz-non-critical in general; the wire must supply
   "∃ permissible premise at index `≤ j₀`" as the orbit hypothesis (true on the ⊥-orbit — the I∀ redex sits
   at-or-below the distinguished top). **REMAINING for this piece:** a `isChainInf`-extraction lemma that,
   given the parent witness `j₀` and `permIdx ≤ j₀`, RESTRICTS the `∀ i'≤j₀`/`∀ i'<j₀` threading/rank down to
   `permIdx` — pure logic (a `le_trans` under the binders), small.

2. **Motive strengthening of `redSoundGen`** (`Crux2Blueprint.lean:226`). The dispatch's `hred` gives only
   `ZDerivation (red dᵢ)`; the constructors need the CONCLUSION-TRACKING per premise (`fstIdx (red dᵢ) =
   tpReduce (tp dᵢ) (fstIdx dᵢ) 0`, i.e. the banked `red_zIall/zIneg_tpReduce` / `tpReduce_tp_zAx*` instantiated
   at dᵢ) PLUS, for I∀, the O3 freshness conditions (`a ∉ FV(p)`, `a ∉ FV(Γ)`) — which come from `ZRegular`.
   Strengthen the IH motive to carry these alongside `ZDerivation (red dᵢ)`.

3. **axNeg succedent-replacement constructor** (smaller than #1; mirror `ZDerivation_zK_seqAddAnt` but for
   `seqSetSucc Π p` — read Buchholz §5 ¬-axiom cut for the premise restructuring).

Downstream (after `ZDerivation_red_zK`): `iord_descent_red` (Crux2Blueprint:282), `foundation_bot_to_Z_empty`
(M2), `false_of_ZDerivesEmpty` (M3), wire → headline.

## Aristotle
Idle — CORRECT. The residuals (`i ≤ j₀` threading, motive strengthening) are entangled with the 7800-line
`InternalZ` API (isChainInf/zKValidF/permIdx/tpReduce), not self-contained mathlib lemmas.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. New bricks all `[propext, Classical.choice, Quot.sound]`.
`#print axioms peano_not_proves_goodstein` = `[propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms).
