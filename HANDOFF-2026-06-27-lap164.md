# Handoff: lap 164 — FRESH-MIND REVIEW · direction KEPT · `residual`/`axMajorResidual` NARROWED (tags 0/1/2/7 phantom)

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `7a999c8` · build 🟢 1326 (`lake build GoodsteinPA`, exit 0).
Review lap. One green commit. No src sorry dropped (still 46 in Crux2Blueprint); the open core SHRANK.

## Axiom ledger (real `#print axioms`, this lap — no drift)
- `peano_not_proves_goodstein` (headline) = `[propext, sorryAx, choice, Quot.sound]` — 0 math axioms, bare sorry.
- `false_of_ZDerivesEmpty` = `[propext, sorryAx, choice, Quot.sound]` — 0 math axioms.
- `peano_not_proves_consistency` + `goodsteinSentence_faithful` = `[propext, choice, Quot.sound]` — clean.

## What this lap did
1. **Review (binding):** `DIRECTION.md` CURRENT DIRECTIVE + `STATUS.md` refreshed with a lap-164 banner.
   Direction KEPT (lap-161 still current): finish `false_of_ZDerivesEmpty` = M1b-term; ex-falso (i) DONE
   lap-163; M2/M4 bridge (the dominant feasibility unknown) deferred BY DESIGN until the engine closes. The
   operator's "M1b-term only / existence-form spike first" kickoff is SATISFIED (spike adopted laps 132/138;
   M1b-term = `false_of_ZDerivesEmpty`, the lemma being driven). Crux-neglect check: laps 161-163 all hit the
   directive's named piece (ex-falso), not side-leaves.
2. **Crux-narrowing (verified in-kernel, axiom-clean):** `tryProducerClose` (in BOTH the `residual` anySucc
   copy AND the `axMajorResidual` ⊥-version) strengthened with 3 hyps the callers already supply —
   `zTag(znth ds m) ≠ 0`, `≠ 7` (NON-LEAF, from `collapse`), `π₁(tp(znth ds m)) ≠ 0` (NON-right-symbol, from
   the `else` of `by_cases π₁(tp)=0`). Tags 0/1/2/7 producers are now **PHANTOM** (closed by `absurd`):
   leaves (0/7) contradict non-leaf; R-intros (1/2: `tp = isymR …`, `π₁ = 0`) contradict non-right-symbol
   (they'd `isRedexPair` with `jstar`, already killed by `hnolow` one level up via `rightSym_producer_redex`).

## 🎯 NEXT — the genuine remaining residual core (ranked in PENDING_WORK lap-164)
After narrowing, `residual`/`axMajorResidual` are reached ONLY from:
- **(a) tag-8 ⊥-producer** (`tryProducerClose` tag-8): a `zAxBot` producer, `⊥ ∈ Γ_m` threads to `⊥ ∈ Γ`
  (→ `leafClose`/`exFalsoClose`, AVAILABLE since lap-163) OR an earlier non-leaf ⊥-exit `m' < m` (needs
  `tryProducerClose m'` RECURSIVELY — blocked: it's a non-recursive `have`). FIX = restructure `tryProducerClose`
  as a bounded strong-induction-on-`m` recursion, or factor a `botThread` helper. **Cheapest real DROP next.**
- **(b) tag-5 climb escape** `^∀G ∈ Γ`: generalize `axAllClose` (`^∀⊥∈Γ→Γ→⊥`) to `^∀G∈Γ→Γ→C` (the §5 L∀-axiom
  `zAxAll s G-matrix …`); shared by both copies (the `threadEscapeClose` the lap-161 directive names).
- **(c) tag-6 partial thread** (`inegF p'∈Γ`, `p'` doesn't thread): check the non-leaf `p'`-producer is reducible.
- **(d) anySucc C-exit R-intro replay** (`:3613`/`:3619`): least-exit major is itself a tag-1/2 R-intro of `C`;
  needs internal WEAKENING / chain-truncation (re-base to full `Γ→C`) — the deep general-cut piece; defer.

Closing (a)-(c) collapses `axMajorResidual` (⊥) entirely; with (d) it collapses `residual` (anySucc) →
DROPS `genReduct_chain_noRedex_anySucc` → thin-wraps `genReduct_anySucc` → DROPS `axMajorResidual` +
`descent_step_K_noncrit_axMajor`. gDef (:4384) separable.

## ⚠️ FORBIDDEN / corrections (per DIRECTION.md lap-161, unchanged)
`red` witnesses; `iord`-recursion for the construction; `redLeast`/μ-min for gDef; the refuted single-premise
`seqUpdate` splice; axMajor/gDef STANDALONE before the port; the off-path dead red-soundness sorries AS STATED;
**M2/M3/M4 wiring** until `false_of_ZDerivesEmpty` is sorry-free. ALTITUDE: a sorry-free `false_of_ZDerivesEmpty`
is NOT the headline — `goodstein_implies_consistency` (`Reduction.lean:68`) is a bare sorry, M2/M4 ~0%.

---
**→ Next session: restructure `tryProducerClose` as a bounded recursion to close the tag-8 ⊥-producer (next (a)),
then generalize `axAllClose` for the tag-5 climb escape (b). Keep build green, `#print axioms` clean.**
