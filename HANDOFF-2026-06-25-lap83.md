# HANDOFF — lap 83 (FRESH-MIND REVIEW) · crux-2 re-point needs a NEW reduct dispatch, not wiring

> Supersedes prior HANDOFF-*.md. Read order: this → `PENDING_WORK.md` (lap-83 box at top, then lap-82) →
> `ANALYSIS-2026-06-25-lap82-criticality-not-validity.md` → `src/GoodsteinPA/InternalZ.lean` §descent.
> Branch `plan`. HEAD `81ed692`. Build 🟢 green (`lake build GoodsteinPA`, per lap-82). Tree clean.

## 🎯 OBJECTIVE (operator, unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`). Two headline blockers stand:
1. **crux 2** `RedSound` (`InternalZ.lean:5188`) — internalized cut-elimination; the active frontier.
2. **`PA_delta1Definable`** (`inductionSchemeUnivDelta1`, `PADelta1.lean`) — parallel rest-point per the
   lap-82 operator redirect; leave alone.

## ✅ THIS LAP (fresh-mind review only — NO proof edits; one doc commit `81ed692`)
Re-read the crux-2 descent capstone `iord_descent_iR2_zK_of_valid` (`InternalZ.lean:4755`) end-to-end and
found the lap-82 KEY FINDING **overstated**. Recorded the correction in `PENDING_WORK.md` lap-83 box:

- The lap-82 handoff said step 2 ("K-descent dispatch") is "not new descent math — just wire the banked
  `iord_descent_iCritAux`/`_iSpliceEnd` into a dispatch." **That is too optimistic.** The capstone uses
  criticality (`hnperm`) to **FIND THE REDEX** via `inference_critical_pair_of_chain`, then
  `rw [iR2_zK_eq_iRcrit]` to make `iR2` *be* that critical reduct.
- In the **non-critical** case there is provably no such redex, and `iRcrit` (= the current unconditional
  `iR2_zK`) produces nothing useful. The banked `iord_descent_iCritAux` descends `zK s r (seqUpdate ds i v)`
  — but only if `iR2_zK` actually PRODUCES that reduct, which it does not (`iR2_zK_eq_iRcrit` is
  unconditional). So wiring the banked descent **requires the reduct function `iR2_zK` itself to branch**
  on `zKCritical s ds`. That is a **definitional change to the reduct**, breaking `iR2_zK_eq_iRcrit` and
  everything proved through it (the redex-finder route, the §5 `zAxReduct` bundles). The lap-82 plan's own
  step-3 escape hatch ("if `iR2` can't dispatch case 5.2, define a NEW reduct + re-point RedSound/descent")
  is the realistic route.
- **Net:** re-pointing `ZPhi` → `zKValidF` (step 1) cannot stay green by itself — it forces
  `iord_descent_iR2_zK_of_valid` to take only `zKValidF`, whose non-critical case has no banked *producer*.

## ▶ NEXT (recommended sequencing — each step lands green/committable, not an all-or-nothing swap)
1. **Standalone non-critical descent capstone** `iord_descent_iCritAux_zK_noncrit` — hypotheses
   `zKValidF s r ds` + `¬ zKCritical s ds` + the witnessing premise `i ≤ j₀` with `iperm (tp dᵢ) s`;
   conclusion `icmp (iord (zK s r (seqUpdate ds i (iR2 (znth ds i))))) (iord (zK s r ds)) = 0`. Reuse the
   banked `iord_descent_iCritAux` (`InternalZ.lean:3197`) + `iord_descent_iCritAux_of_ZDerivation` (4066).
   The genuinely-new content is *selecting* the witness `i` from `isChainInf` + ¬criticality.
2. **Standalone sub-critical splice capstone** likewise, via banked `iord_descent_iSpliceEnd` (3334).
3. **ONLY THEN** define the dispatching reduct (a new `iR2'`, or a `zKCritical`-guarded `iR2_zK`) and do
   the lap-82 step-1 re-point (`ZPhi`'s `zK` disjunct `zKValid` → `zKValidF`; `zblueprint`
   `zKValidDef.sigma/.pi` → `zKValidFDef.sigma/.pi`, already banked; `zphi_iff`; `zPhi_definable`). Then
   `zKValid_of_ZDerivation_zK` → returns `zKValidF`; forward constructors (5094/5125) take `zKValidF`.
4. **Prove `RedSound`** (Thm 3.4(b)/D₁) — the parallel validity invariant, by the same `ZDerivation`
   induction that drives descent, carrying `zKValidF`. THE remaining new math.

## Build / verify
`lake build GoodsteinPA` green (unchanged this lap; only `PENDING_WORK.md` edited). `src/` sorries:
`Statement.lean:22` (headline), `Reduction.lean:68`, `DescentSemantic.lean:582` (crux-2 chain),
`PADelta1.lean` (`inductionSchemeUnivDelta1` — parallel-box rest-point). `RedSound` is a `Prop`
hypothesis, not a sorry — the genuine remaining crux-2 math content.

Aristotle: idle — Foundation not in its mathlib-v4.28 env; all crux-2 work is Foundation-internal.

## Commits this lap (branch `plan`)
- `81ed692` docs(lap 83): fresh-mind refinement — re-point needs a NEW reduct dispatch, not proof-only wiring

Host requested a graceful STOP (no relaunch). Tree clean after this HANDOFF.
