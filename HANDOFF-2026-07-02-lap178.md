# HANDOFF — 2026-07-02, lap 178 (CRUX PROBE: additive-identity shortcut to P1 kernel-REFUTED)

> **BOUNDED-SUBSET RUN, wait-for-judge state (unchanged since lap 176).** Target = `REBUILD-Z-ORDER`
> **Scope-A ONLY**; everything beyond is judge-gated. Scope-A (A1/A2/A3) remains exhausted. This lap,
> under sustained governor push, did the mission's hardest-first move: probe the actual P1 crux with
> the smallest kernel-grounded experiment, rather than idle. **Resume = judge ratification of
> `REBUILD-Z-LAP1-VERDICT.md` + the two P1 findings (lap-176 fslot-control-raise + lap-178 absorption
> wall).** Do NOT grind the 3 gated §5 pins; do NOT touch Route-A sorries; do NOT self-amend pins.

**Terminal state:** branch `plan`, HEAD advances by this lap's commit only (docs +
`wip/HardyAddProbe.lean`, an out-of-build evidence artifact — no `src/` edit). Full repo green
(`lake build`, **1333 jobs**). `src/GoodsteinPA/OperatorZeh.lean` = exactly 3 `sorry`s (the §5
f-slot pins), no new `axiom`s. No drift.

## What this lap did (a real advance on the crux, not idle side-work)

Attacked the hardest open calculus obligation head-on: `cutElimPass_Zf`'s conjunct
`NormControlled f' (raise e α') m`, `raise e α' = e + ω^{α'}` (the P1 raised-control question). The
tempting shortcut: the classical additive Hardy identity `H_{e+β} = H_e ∘ H_β` would make
`f' := hardy e ∘ hardy(ω^{α'})` a concrete E–W iterate, collapsing pin-3's *existential* iterate
(judge Q2) to one already-defined composition.

**Result — the shortcut is DEAD (kernel-verified in `wip/HardyAddProbe.lean`, sorry-free):**

- Proved `fundamentalSequence_add_succ` — the SUCCESSOR fs homomorphism, UNCONDITIONAL (holds even
  under absorption; successors have unique predecessors, shape forced by `FundamentalSequenceProp`).
  A clean new general ONote lemma; the notation-level generalization of the banked finite
  `hardy_add_ofNat` (`Hardy.lean:1257`). Also `onote_add_zero`.
- Kernel `rfl` REFUTATION of the unconditional identity under absorption: `add_omega_absorbs`
  (`(1:ONote)+ω = ω`) and `fundamentalSequence_absorbs` (`fs(1+ω) = fs(ω)`). Hence `H_{1+ω} = H_ω`,
  while `H_1 ∘ H_ω = (·+1)∘H_ω ≠ H_ω`. The limit-branch fs homomorphism is FALSE.
- **Finding:** the cut-elim `raise` grows `ω^{α'}` into the ABSORBING regime, where additive
  decomposition sees nothing of the input control `hardy e`. So P1 is NOT an algebraic identity — it
  is the genuine fast-growing domination `hardy(ω^{α'}) ≤ (iterate of the input slot)` (E–W Lemma
  19). Pin-3's iterate index is *essential*; this corroborates the BW87 cut-free fallback and favors
  the per-instance headline locus (judge Q1). Full writeup:
  `REBUILD-Z-LAP178-FINDING-2026-07-02-additive-identity-absorption-wall.md`.

This SHARPENS lap-177 ("no option-independent pass infra exists"): lap 177 argued the E–W norm bound
can't be *stated* without forbidden machinery; lap 178 shows the one algebraic shortcut that would
sidestep it is kernel-false. The two are complementary evidence-backed dead-ends.

## Kernel audit (this lap, real `#print axioms` unchanged from 177)

- Route A `peano_not_proves_goodstein` = `[propext, choice, Quot.sound, goodstein_implies_consistency]`
  — sorryAx OFF. Route B / consistency headlines unchanged. `OperatorZeh`: 3 §5 pins +
  `probe_cut_all_arm_Zf` carry `sorryAx`; the rest clean. No drift.
- `wip/HardyAddProbe.lean` is NOT in a build target (wip/ is outside the `src/` lib glob); validated
  standalone via `lake env lean wip/HardyAddProbe.lean` (green, sorry-free; two harmless deprecation
  warnings on `Ordinal.add_succ`/`succ_ne_zero`).

## Resume point (for the next lap or the judge)

- **If the judge has NOT ruled:** still a wait-for-judge, cap-bounded run — confirm state (build
  green 1333, axioms clean, 3 pins), do NOT reopen finished work or grind gated pins, end the turn.
  The permitted-sibling surface is now genuinely mined out: every remaining path to a pin body needs
  the forbidden fast-growing/iterate machinery (lap-178 closed the last algebraic shortcut).
- **If the judge ratifies Option A + pin `f'`:** lap 2 discharges the FIXED-control reduction conjunct
  via the banked `NormControlled.comp`, then the genuine work is `cutElimPass_Zf`'s
  `NormControlled (f^{…}) (raise e α') m` — which lap 178 confirms is the fast-growing domination
  `fastGrowing α' (n+1) ≤ f^{iterate}` (E–W Lemma 19), NOT an additive identity.
  `fundamentalSequence_add_succ` is available to relocate to `Hardy.lean` if the successor
  homomorphism is needed outside the finite slice.

## Lap-178 continued (governor push) — lap-177's "no F" CORRECTED; a PERMITTED path REOPENED

Chasing the surviving route rather than idling, I checked lap-177's premise "the substrate has no
fast-growing `F` / iterate `f^k`" — **it is WRONG.** `ONote.fastGrowing` is a mathlib def with the
repo's full growth theory (`Hardy.lean` §Basic); `f^k` = `Function.iterate`. So E–W Lemma 19 is
stateable on STABLE defs — not forbidden cut-elim machinery. And the repo's B4 identity `H_{ω^α}=f_α`
(`Hardy.lean:1082`) is **kernel-FALSE as an equality** (a second convincing-identity trap): the exact
`native_decide`-anchored relation (α∈{0,1,2}) is `hardy(oadd α 1 0) n + 1 = fastGrowing α (n+1)`.

The B4 identity `H_{ω^α}=f_α` is kernel-false off TWO shifts: (1) `H_{ω^1}(3)=7≠6`, shifted `=` is
`hardy(ω^α) n + 1 = fastGrowing α (n+1)` (α∈{0,1,2}); (2) even that `=` holds only at
successor/finite exponents — at LIMITS it degrades to strict `<`. So the UNCONDITIONAL, P1-relevant
truth is the INEQUALITY.

Banked in `wip/HardyFastGrowingBridge.lean` (out of build target, `64a9ed9`): the `≤` target
`hardy_omega_pow_add_one_le` is **proven for base α=0, LIMIT, AND successor α=β+1** — the successor
case wired to the coefficient intermediate `hardy_omega_pow_coeff_le`
(`H_{ω^β·(m+1)}(n)+1 ≤ f_β^[m+1](n+1)`, parametrized by the outer IH at β; kernel-verified β∈{0,1}),
whose **base m=0 is proven**.  Corollary `hardy_omega_pow_lt_fastGrowing`
(`hardy(ω^{α'}) n < fastGrowing α'(n+1)`) derived; `native_decide` anchors incl. equality-is-false.

**SOLE remaining sorry = the coefficient-induction step** (`m→m+1` of `hardy_omega_pow_coeff_le`):
`H_{ω^β·(m+2)}(n)+1 ≤ f_β^[m+2](n+1)`.  The fund. seq. of `ω^β·(m+2)` branches on β's shape — β=0 is
a finite SUCCESSOR (easy, `hardy_succ`); β=succ/limit introduce a tail term `ω^δ·(i+1)` (the genuine
Cichoń–Wainer core, `reaches_coeff_step'` territory in `Hardy.lean`).

**Net:** P1's raised-control domination REDUCES to `fastGrowing α'(n+1) ≤ (iterate of input slot)`,
and the whole `≤` bridge (B4) is down to ONE isolated coefficient step — a PERMITTED,
calculus-independent grind available NOW, regardless of the judge gate. This reopens a concrete
attack path lap-177 wrongly declared closed. **Next lap (permitted): close the coefficient step**
(start with β=0, then β=succ/limit). Reduction discharge + Lemma-19 wiring into pin-3 stay judge-gated.
