# Handoff: lap 140 — ALTITUDE REVIEW: directive corrected + crux DECOMPOSED + tag-5/6 residual sharpened

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `d7ea4d5` · build 🟢 green (`lake build GoodsteinPA`, **1326**).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms; lone `sorryAx` = crux-2), re-verified in-kernel; `goodsteinSentence_faithful` clean.
**4 commits** (`dae6bae` decomposition + review → `793d7c0` diagnosis → `c2fabf7` handoff → `d7ea4d5` ∀-soundness).

## ✅ ALSO LANDED (green, axiom-clean) — explicit-pair ∀-case soundness `ZDerivation_iCritReductG_all_at`
lap-139 NEXT **step 1 is DONE** (`Crux2Blueprint`, before `ZDerivation_iRcritG_corrected`): the pair-parametric
assembly of the `_at` halves into a full `ZDerivation` of the closed critical reduct `iCritReductG` at an
ARBITRARY pair `(i,j)` — via `ZDerivation_iCritReductG_of` directly (NO `redexI/redexJ`, NO `iRcritG`). Takes
`hdi : znth ds i = zIall …` as a HYPOTHESIS, so it is INDEPENDENT of the cutPartner residual. The cut-rank strict
drop is `irk_substs1_lt_all` on the I∀ matrix. **This is the soundness SKELETON `descent_step_K_tag5` instantiates.**
⟹ tag-5 now reduces to: (i) the cutPartner residual (discharge `hdi`), (ii) orbit-data hyps (re-key the
deterministic suppliers to `(cutPartner, majorIdx)`), (iii) regularity `ZDerivesEmptyR` of the reduct, (iv) descent
`iord_descent_iRcrit_of_chain'`. Build (iii)+(iv) next — they are independent of the cutPartner residual.

## ⚖️ This was an altitude review lap — I corrected the BINDING directive (DIRECTION.md)
The lap-137 CURRENT DIRECTIVE was **materially stale** and a grind lap obeying it would have re-attacked a dead
end. Two mandates RETIRED:
- The orbit (B)/(B0) it called "the load-bearing neglected piece" is **DONE** (lap 138, `exists_sigma1_iterate`).
- The `redLeast` μ-min route it mandated for (A) is **REFUTED** (lap 139, wrong-polarity witness bound).

⟹ **The whole crux-2 termination now collapses to ONE lemma `descent_step_K_majorIdx`:** `false_of_ZDerivesEmpty`
(M3) is sorry-free given `InternalPRWO V` (crux-1's deliverable, a hypothesis) + the bare-∃ per-step descent; the
orbit (B) is proven; and (A) `exists_sigma1_descending_step` folds into it via the *concrete* `redStep` (lap-139
reconciliation). DIRECTION.md CURRENT DIRECTIVE (lap-140) now says this; **obey it.**

## ⭐ THE advance — `descent_step_K_majorIdx` DECOMPOSED by major-premise tag (committed `dae6bae`)
`majorPrem_tag_mem` (tag ∈ {3,4,5,6}) drives a **sorry-FREE dispatcher** onto four independently-attackable
per-tag named src sub-`sorry`s (Crux2Blueprint, ~line 1768):
- **`descent_step_K_tag3`** (Ind) — reduct = corrected `iIndReductSeqG`; residual = `isChainInf_iIndReductSeqG`.
- **`descent_step_K_tag4`** (chain) — relocated structural `<`-recursion into the sub-K-chain. Deep core.
- **`descent_step_K_tag5`** (∀-axiom) — principal `(cutPartner, majorIdx)` cut; built on `_at` halves.
- **`descent_step_K_tag6`** (¬-axiom) — dual.
Raises src count = progress (the crux decomposed into named sub-goals).

## 🔬 Two in-kernel findings that SHARPEN the tag-5/6 attack (committed `793d7c0`, in PENDING_WORK)
1. **The `redZKReady_of_zKValid` shortcut is REFUTED.** It delivers a principal R-intro critical pair directly —
   BUT needs `zKValid = zKValidF ∧ zKCritical`, and `zKCritical s ds = ∀ i, ¬ iperm (tp dᵢ) s` is **FALSE on the
   ⊥-orbit** (an isymRep premise — Ind/K/atom/Ax¹ — has `iperm isymRep s` unconditionally). The lap-129 criticality
   stall, restated. ⟹ the cutPartner route is genuinely necessary; do NOT re-try this shortcut.
2. **tag-5/6 deep content pinned to `tp(cutPartner) = isymR (^∀p)`.** Every premise has `iperm (tp dᵢ)(fstIdx dᵢ)`
   (`zKValidF`), so by the trichotomy `tp ∈ {isymR(seqSucc), isymLk k A, isymRep}`. For cutPartner `i'`
   (`chainAsucc ds i' = ^∀p` via `majorPrem_zAxAll_cutPartner`): `tp i' = isymR C ⟹ C = ^∀p` (`iperm_isymR_iff`),
   i.e. the zIall R-intro `haux0_at` needs, AND `(i', majorIdx)` is then a genuine `isRedexPair`. **So tag-5/6 =
   ruling out the OTHER two cases (isymLk-axiom / isymRep) for the ∀p-producer** — a LOCALIZED
   `inference_critical_pair` without global criticality.

## 🎯 NEXT ATTACK (hardest-first, per DIRECTION.md lap-140)
1. **DECIDE the tag-5/6 bridge by a Buchholz §5 source read** (`papers/`): is the cutPartner provably an R-intro
   — route (a), a leastness/threading argument (`firstBotPrem_reducible` + `chainAsucc_threaded_of_leaf` already
   rule out atom/Ax¹ leaves; residual = isymLk-axiom + Ind/K) — OR does it need a general internal **∀-INVERSION**
   `ZDerivation Γ→∀p ⟹ ZDerivation Γ→p[k]` — route (b), which handles ANY ∀p-producer? Build the chosen bridge.
2. The rest of tag-5 is the explicit-pair assembly: mirror `ZDerivation_iRcritG_corrected` (`Crux2Blueprint:783`)
   with the `_at` halves via `ZDerivation_iCritReductG_of` (NOT `iRcritG`), threading orbit data
   (freshness/`hthread`/`hrank`/wff) for `(cutPartner, majorIdx)` + descent (`iord_descent_iRcrit_of_chain'` is
   pair-parametric) + regularity (`ZDerivesEmptyR` of the reduct).
3. **tag-3** (secondary): assemble `isChainInf_iIndReductSeqG` (lap-138 NEXT, readouts banked; pin the
   `t=t'+1`-vs-`numeral k` exit subtlety FIRST). Do NOT use the `red dⱼ` single-replace witness (false sorry).

## ⚠️ Forbidden / do-NOT-undo
- `redLeast`/μ for (A) (wrong polarity, lap-139); engine re-key of `red`/`redexI`/`redexJ` (lap-139); the
  `redZKReady_of_zKValid` shortcut (needs `zKCritical`, FALSE on the ⊥-orbit, lap-140); `red dⱼ` single-replace
  for tag-3 (ordinal-shadow false sorry, lap-138).
- Do NOT revert the per-tag decomposition; the four leaves are the live decomposition of the crux.

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `descent_step_K_tag{3,4,5,6}` (~1768, the four new leaves) + the
  sorry-free `descent_step_K_majorIdx` dispatcher; the `_at` halves (`haux0_at` ~405, `haux1_at` ~490, the `_neg`
  twins); `ZDerivation_iRcritG_corrected` (~783, the deterministic assembly template).
- `src/GoodsteinPA/InternalZ.lean`: `majorPrem_tag_mem` (9285), `majorPrem_zAx{All,Neg}_cutPartner` (9217/9245),
  `iperm`/`iperm_isymR_iff`/`iperm_isymLk_iff` (979/997/1005), `zKValid_iff_zKValidF_and_zKCritical` (1428),
  `redZKReady_of_zKValid` (8338, the blocked shortcut), `ZDerivation_iCritReductG_of` (9736).
- `DIRECTION.md` CURRENT DIRECTIVE (lap-140, binding) · `STATUS.md` (lap-140 banner) · `PENDING_WORK.md` lap-140.

---
**→ Next session: HEAD = `793d7c0`, build green. Crux-2 termination = the ONE lemma `descent_step_K_majorIdx`,
now decomposed into four per-tag leaves. tag-5/6's deep residual is pinned to `tp(cutPartner) = isymR (^∀p)`
(rule out isymLk/isymRep). FIRST move: a Buchholz §5 read to decide route (a) leastness-R-intro vs (b)
∀-inversion, then build the bridge + the explicit-pair assembly. Obey DIRECTION.md lap-140.**
