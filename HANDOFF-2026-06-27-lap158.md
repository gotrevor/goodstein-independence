# Handoff: lap 158 — FRESH-MIND REVIEW + SPIKE; genReduct generalization closes by CODE-induction (fork SETTLED)

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `9ac1bf3` · build 🟢 green (1326 jobs,
`lake build GoodsteinPA`). In-kernel (real `#print axioms`): headline `peano_not_proves_goodstein` +
`false_of_ZDerivesEmpty` = `[propext, sorryAx, choice, Quot.sound]` (**0 math axioms**);
`peano_not_proves_consistency` + `goodsteinSentence_faithful` = `[propext, choice, Quot.sound]` (clean).
No drift. No uncommitted edits.

## What this lap was
A FRESH-MIND REVIEW lap (altitude) that ALSO executed the mandated design spike. Two commits:
1. `b58236a` — review: re-verified fidelity, set `DIRECTION.md` CURRENT DIRECTIVE (lap-158), refreshed
   `STATUS.md` + `PENDING_WORK.md`. **Verdict: direction SOUND, progress GENUINE** (steady crux narrowing
   153→157, not fixation). lap-155's SUCCEDENT-THREADING COLLAPSE is DONE + EXHAUSTED; the residual
   `axMajorResidual` (`Crux2Blueprint:3417`) is the IRREDUCIBLE {3,4}-producer cut-reduction.
2. `9ac1bf3` — SPIKE (`wip/GenReductAnySucc.lean`, typechecks via `lake env lean`).

## ⭐ The decisive spike result (the lap's deliverable)
**The genReduct generalization off `seqSucc=⊥` closes by the EXISTING CODE-induction — NOT an outer
degree-induction.** The refactor is "drop the `seqSucc=⊥` clause," not "add a degree parameter." Proven by
two non-sorried lemmas that typecheck:
- **`genReduct_anySucc`** (= `genReduct_botSucc` minus the `seqSucc(fstIdx d)=⊥` antecedent): its
  `zDerivation_sigma_induction` body typechecks unchanged. The `𝚺₁` motive `GenReductCert` is definable
  without the `⊥`-clause; code-IH threads; tag-3/4 delegate.
- **`noRedex_producer_closes`** (non-sorried body): the {3,4} PRODUCER `m` of ANY succedent closes via
  `Or.inl (certReplace_of_premise_cert_anySucc … (IH m …))` with the GENERAL IH (no `⊥`-clause). This is the
  exact move the lap-157 refutation doubted — VALIDATED. It sidesteps the refutation because
  `certReplace_of_premise_cert` (`Crux2Blueprint:3283`) is ALREADY general-succedent in `m`: its FLATTEN
  rank-headroom comes from the PREMISE's own `irk+1 ≤ idg(premise)` (line 3328-3331), NOT the chain's degree
  (which the refutation found un-derivable). The degree headroom is LOCAL to each principal-cut flatten
  (already proven in `genReduct_chain_hasRedex`), so NO global degree-induction.

## 🎯 NEXT (the PORT to src — de-risked; raises the src sorry count = progress)
Port `wip/GenReductAnySucc.lean` to src, replacing `axMajorResidual` by the producer-splice path. Exactly
THREE generalized sub-lemmas (full plan in `PENDING_WORK.md` lap-158 top, priority order):
1. **`certReplace_of_premise_cert_anySucc`** — generalize `certReplace_of_premise_cert` `hbot0` (`= ⊥`) to
   the C-exit disjunct `chainAsucc ds j0 = seqSucc s` (used only as `Or.inr hbot0` → flip to `Or.inl`).
   SMALLEST drop, start here.
2. **`ind_reduct_anySucc`** — generalize `ind_reduct_botSucc_of_fresh` off `⊥`; for the closed ∀-tower
   `C = ^∀^k⊥` the induction is VACUOUS (`p_ind = C`) so the reduct is `d0`. CHECK `zIndWff` admits it.
3. **`genReduct_anySucc_chain`** + `genReduct_chain_hasRedex`/`_noRedex` callees — off `⊥` to the C-exit
   (from `zKValidF`), drop the `⊥`-clause from the IH, replace `axMajorResidual` with `noRedex_producer_closes`.
Then thread the generalized entry into `genReduct_botSucc` (`⊥` is a special instance; existing call sites
like `descent_step_K_noncrit_repMajor`:3818 keep working). gDef (`:3980`) stays a separate downstream leaf.

## ⚠️ Forbidden / binding (DIRECTION.md lap-158)
Same-degree `õ`-drop for the {3,4} producer (refuted lap 157); `red`; `iord`-recursion (CODE-induction +
local-degree flatten ONLY — spike confirms code-induction suffices); `redLeast` for gDef; the refuted
`seqUpdate` splice; attacking `descent_step_K_noncrit_axMajor`:3857 / gDef:3980 standalone; off-path dead
red-soundness sorries {:82,:1257,:1367,:1563,:1653,:1765,:1868} AS STATED; M2/M4. ALTITUDE: M2
(Foundation→Z bridge) ~0% built — "only the crux is left" ≠ "almost done."

## 📁 Key anchors
- Spike: `wip/GenReductAnySucc.lean` (`genReduct_anySucc`, `noRedex_producer_closes`, the 3 sorried generals).
- src targets: `genReduct_botSucc` (`Crux2Blueprint:3682`), `genReduct_botSucc_chain` (`:3655`),
  `genReduct_chain_hasRedex` (`:3048`), `genReduct_chain_noRedex` (`:3365`, `axMajorResidual` at `:3417`),
  `certReplace_of_premise_cert` (`:3283`), `ind_reduct_botSucc_of_fresh`.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-158, binding) · `PENDING_WORK.md` lap-158 top (port plan) · `STATUS.md`.

---
**→ Next session: PORT the spike to src (start with `certReplace_of_premise_cert_anySucc`, the smallest
drop), generalizing off `seqSucc=⊥` to close `axMajorResidual`. Keep build green + headline 0 math axioms.**
