# HANDOFF — lap 68 · THE descent is now UNCONDITIONAL (zKValid wired into ZPhi)

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md` (top = live next-step plan),
> then `STATUS.md`. Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = 1321 jobs.
> Working tree: clean (all committed).

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**.
Route: `PA⊢γ →(crux-1, DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)` → Gödel II. **crux-2 (Buchholz-Z
ordinal analysis) is the sole remaining math wall.** The 3 `src/` sorries: `Statement.lean:22`
(headline), `Reduction.lean:68` (`goodstein_implies_consistency` = crux-2's girder),
`DescentSemantic.lean:582` (off-headline semantic route).

## ✅ WHAT LANDED THIS LAP (2 green commits, axiom-clean `[propext,choice,Quot.sound]`)
**THE descent MATH is now a hypothesis-free fact about `ZDerivation`s.** The lap-67 capstone
`iord_descent_iR2_zK_of_valid` was CONDITIONAL on `zKValid`; the bare `ZPhi` `zK` disjunct didn't carry
it. This lap closed that last structural gap:

1. **`isChainInfDef : 𝚫₁.Semisentence 3`** + `zKValidDef : 𝚫₁.Semisentence 3` — hand-built Δ₁
   arithmetizations of the Buchholz `K^r` chain side conditions (chain-structure `j0`/threading/rank;
   per-premise `iperm`/`¬iperm` + tag-gated `IsUFormula`). σ-core extracts function values positively
   (`∃ y, !fDef y args ∧ …`), π-core via the antecedent (`∀ y, !fDef y args → …`). Both `.Defined`
   instances axiom-clean. **GOTCHA banked:** `∀ x ≤ t` parses to `Semiformula.ballLE` which has NO
   `Hierarchy` simp lemma → the `.mkSigma`/`.mkPi` hφ auto-prover fails; use `∀ x <⁺ t` (`ballLTSucc`,
   = ≤) which has `ballLTSucc_iff`. Also: the `.sigma`/`.pi` eval reductions (`iff_delta_sigma`/
   `iff_delta_pi`) are `@[simp]` + instance-resolved, so embedded Δ₁ sub-predicates auto-reduce.
2. **Wired `∧ zKValid s r ds` into the `ZPhi` `zK` disjunct** + the full Fixpoint cascade
   (`zphi_monotone`/`zphi_strong_finite`/`zphi_iff`/`zblueprint` σ&π cores/`zPhi_definable` — all
   carry it trivially since `zKValid` has no `C`-dependence) + all ~9 `rcases`/destructure sites.
3. **`zKValid_of_ZDerivation_zK`** reads `zKValid` off a genuine `ZDerivation` chain.
4. **`iord_descent_iR2_struct` extended to tag 4** (`htag : =1∨2∨3∨4`), K-case discharged by
   `iord_descent_iR2_zK_of_valid`. (Tags 0/5/6 = atom/axiom NF, correctly excluded — irreducible.)

## ▶ NEXT PHASE — no-infinite-descent → `ZDerivesEmpty → False` → C0.5 bridge → `Reduction.lean:68`
The descent `ZDerivation d → (tag reducible) → o(iR2 d) ≺ o(d)` is banked. The assembly to the headline:
1. **Define `ZDerivesEmpty d := ZDerivation d ∧ <d derives the empty sequent>`** (currently only a
   docstring at `InternalZ.lean:3914`). Sequent `fstIdx d = ∅`/⊥-succedent — pin the exact shape.
2. **`iR2` preservation**: `ZDerivesEmpty d → ZDerivesEmpty (iR2 d)` AND `d`'s tag is reducible
   (1–4, never an atom/axiom leaf — an empty/⊥ sequent is never an axiom conclusion). Then each step
   strictly drops `iord` via `iord_descent_iR2_struct`.
3. **No infinite descent**: `n ↦ iord (iR2^[n] z)` strictly `≺`-descends in ε₀ ⟹ contradiction. THE
   subtlety (memory `seqdominated-vacuity-needs-realizer`): ε₀ is NOT internally WF in PA — the
   contradiction is with **PRWO(ε₀)** (crux-1 deliverable), Gentzen-style, NOT naive internal WF.
   This is where crux-1 (PRWO) weaves into crux-2. Decompose carefully.
4. **C0.5 bridge `foundation_bot_to_Z_empty`** (`InternalZ.lean:3914` docstring; ~<1k lines per
   `E-CRUX2-DECOMPOSITION §5`): `¬Con(PA) → ∃ Foundation ⊥-proof → ∃ z, ZDerivesEmpty z`. Z's native
   `Ind` rule maps PA-induction directly (skips Bryce–Goré's ω-rule sub-tower). Parallelizable.
5. **Wire** → discharge `Reduction.lean:68`. ANTI-FRAUD: only discharge once
   `#print axioms peano_not_proves_goodstein` is clean.

Hardest-first call: step 3 (the no-infinite-descent / PRWO weave) is the genuine remaining math;
steps 1–2 are concrete unblockers; step 4 is engineering. Start 1–2 to expose 3's exact interface.

## Pointers
- `E-CRUX2-DECOMPOSITION-2026-06-24.md` §5 (C0.5 bridge), §8 (leaf grind-list). `papers/` Buchholz.
- Reference corpus (read-only, NOT auto-loaded): `~/personal/claude/knowledge/core/projects/lean-journey/reference/`.
- Definability idioms: Foundation `HFS/Vec.lean:917` (`memVecDef` = the .mkDelta σ/π template),
  `Definability/Definable.lean:186-195` (`Defined.proper`/`iff_delta_sigma`/`iff_delta_pi` @[simp]),
  `Basic/Hierarchy.lean:211-220` (`ballLT_iff`/`ballLTSucc_iff` — the `<⁺` Hierarchy lemmas).

## Lap discipline
InternalZ is in `src/` (green-gated, sorry-scanned). Commit each green build; end the turn after each
so the governor can pace.
