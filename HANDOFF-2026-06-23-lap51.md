# HANDOFF — 2026-06-23 (lap 51) — standard-level Cor 3.4 architecture for crux 1 + seam guards

> **Branch** `plan` · **HEAD `78c9b57`** · build **green** (`lake build GoodsteinPA`, 1311 jobs) ·
> tree **clean** · headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, 0 math
> axioms). This lap touched `src/InternalCor34.lean` (2 new axiom-clean bricks) + `wip/` (StdCor34 +
> GentzenCon seam guards). 4 commits: `c662843`, `fb0edef`, `a25c408`, `78c9b57`.

## What this lap did — built the standard-level internal Cor 3.4 architecture for crux 1 (γ→PRWO)
Followed lap-50's KEY insight (crux 1 needs only STANDARD Grzegorczyk level — memory
`crux1-headline-needs-only-standard-level`) and its designated next action.

1. **`isNF_iadd_clean` + `isNF_icorAlpha`** (`src/InternalCor34.lean`, axiom-clean, IN the build,
   commit `c662843`) — the missing NF sibling of `icmp_iadd_clean`/`iC_iadd_clean`. Clean CNF append
   `iadd a b` (tail `b` clean below `a`'s spine via `iAbove (ocExp b) a`) preserves normal form.
   ⟹ the slowed term `ω^(l+1)·β + g` (`icorAlpha`) now has **ALL FOUR** Cor-3.4 properties
   (within / boundary / C-bound / **NF**).

2. **`wip/StdCor34.lean`** (green at 400k heartbeats, off the build target, commits `fb0edef`+`a25c408`)
   — the internal **global** Cor-3.4 assembly over an abstract bookkeeping/`ig`-tail interface:
   - `salpha l β blk off igt j := icorAlpha (β (blk j)) (igt (blk j) (off j)) l` (the slowed sequence).
   - `salpha_isNF` / `salpha_desc` / `salpha_C_le` prove `α` is NF + globally `icmp`-descending +
     `iC(α j) ≤ K·(j+1)` — **exactly the input `InternalThm35.bbeta` (Thm 3.5) consumes**.
   - `habove_of_igt_exp` discharges the 3-arg clean-append hypothesis from the per-`igt` top-exponent
     bound (`ocExp(igt n m) = 0 ∨ = finCode j, j ≤ l`) = the `g < ω^(l+1)` property, via existing
     `iAbove_ocExp_iVbigMul_fin/_inf`.
   - NEW non-vacuous content: ℕ-template `Grz.corAlpha_*` has only the per-step lemmas (global ∀j descent
     vacuous in ℕ); inside `V ⊧ 𝗜𝚺₁` it is a real nonstandard infinite descent.
   - **GOTCHA banked:** `iadd`/`icorAlpha` are semireducible ⟹ `isDefEq` whnf-loops on variable-level
     args even on identical terms; `attribute [local irreducible] iadd icorAlpha` makes defeq structural.
     And `habove`'s 3rd arg feeds `β (blk a)`, so boundary leads `β(blk(j+1))`/`β(blk j)` need `a=j+1`/`a=j`
     (NOT `blk j+1`), keeping `salpha(j+1)` un-`hb`-rewritten.

3. **Seam guards** (`wip/GentzenCon.lean`, operator-directed, commit `78c9b57`) — 3 `example`s that
   compile iff the route chains (green modulo the 2 disclosed crux sorries):
   - SEAM 1: crux-1 output = crux-2 input via the **identical** `prwoInstance gentzenDescentφ` def.
   - SEAM 2: assembly output → Gödel II (`peano_not_proves_consistency`) ⟹ `False` ⟹ Con is
     definitionally Foundation's `↑𝗣𝗔.consistent`.
   - SEAM 3: `not_proves_of_implies_consistency ∘ assembly = 𝗣𝗔 ⊬ ↑goodsteinSentence` = the
     `Reduction.lean` girder type.

## NEXT (hardest-first) — crux 1 reduced to 2 concrete bricks (see PENDING_WORK top)
Discharge the `wip/StdCor34` interface hypotheses:
1. **Block bookkeeping `blk`/`off`** — a state-machine recurrence `IsBlkRec` (advance offset within block
   / roll to next block when `off+1 ≥ W(blk)`, `W t = iC(β(t+1))`) AVOIDS internal `findGreatest`. The
   dichotomy + offset relations are pure arithmetic; `blk j + off j ≤ j` + the `wsum(blk j) ≤ j` (for
   `hβC`) need internal induction ⟹ `blk`/`off` must be **definable** ⟹ build via PR.Construction
   (mirror `InternalCor34.iAboveTable`; thread the abstract width `W` as a parameter Semisentence).
2. **The standard-level `ig` recursion `igt n m`** — internal Grzegorczyk `g` (`Grz.g`) over meta-`l`:
   `ig 0 = ig0` (built), `ig (l+1) n m = iblk (l+1) (n-i) (ig l (iF_l^i n) j)`; needs internal `iF_l`
   (standard l ⟹ fixed primrec, IΣ₁-total — **NO internal Ackermann**) + the F-block decomposition.
   Provides `higt_exp`/`higt_within`/`higtC`/NF/≠0.
Then wire `salpha` → `bbeta` → `DescentArith.nonterminating_internal` (Lemma 3.6) → contradicts γ =
`goodstein_implies_prwo` (crux 1).

**⚠️ DEFERRED DEEPER SEAM (verify when crux-1's BODY lands):** StdCor34 works on `icmp`/`isNF` codes;
`prwoInstance`/`precφ` order by `natCode`. Need the bridge `icmp a b = 0 ↔ natCode-order` (+ isNF ↔ valid
CNF) so the slow-down descent IS the descent `prwoInstance gentzenDescentφ` quantifies over. F-φ-flavoured.

## Hardest wall (unchanged) — crux 2 eq (5) `ord(R d) ≺ ord d`
Gentzen reduction ordinal-descent (Buchholz [6]); blocked until `ord`/`R` are arithmetized over
Foundation's `Theory.Derivation`. Foundation's Hauptsatz is meta-level only ⟹ from-scratch. THE deep wall.

## Aristotle: OFFLINE. WebFetch dead. LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS,
`goodsteinTerminates`, `Statement.lean` headline.
