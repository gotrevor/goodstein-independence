# HANDOFF — 2026-06-22 (lap 18, DEEP REFLECTION + F definability half)

> **Branch** `plan` · HEAD `79cc9b0` · build **green** (`lake build GoodsteinPA`, **1267 jobs**) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). Working tree clean.
> Deliverables: (1) reflection synthesis — STATUS refreshed through lap 17 + F attack plan (PENDING top);
> (2) GPT-5.5 advice doc annotated (`ARITHMETIZATION-SEAM-ADVICE`, Codex agreed in the comment log);
> (3) **F's definability half BUILT + axiom-clean** (`src/GoodsteinPA/EpsilonOrder.lean`, new).

## ✅ Lap-18 proof deliverable — F definability half DONE (`src/GoodsteinPA/EpsilonOrder.lean`)
All `#print axioms` clean. Reduces the F seam to a single undischarged obligation:
- `eval_lMap_structLX` — generalised X-free invariance (any assignment).
- `hprec_of_eval`, `hprec_of_lMap_defined` — discharge the EXACT Boundedness `hprec` for any
  `lMap`-definable order.
- `xpos_lMap`, `hprecXPos_lMap` — `hprecXPos` automatic for X-free `prec`.
- **`structure Seam`** (`GoodsteinPA.EpsilonOrder.Seam`): fields `lt` / `[wf]` / `φ` / `hφ` /
  `ge : ε₀ ≤ orderType lt`; methods `Seam.prec`/`hprec`/`hprecXPos` discharge BOTH seam hypotheses.
- **⟹ instantiating `Seam` now needs ONLY: (2) a concrete `φ` (codeOfREPred₂) + (3) `Seam.ge`
  (ε₀-completeness, the real wall).** See `PENDING_WORK.md` top, "Corrected F work order".
- Codex's comment-log note: also add an **E-facing hook** to `Seam` (don't instantiate an arbitrary
  `lt` until E commits to the same coding) — a future interface refinement.

## 🧭 Direction call (reaffirmed)
Stay on the **Buchholz Boundedness route**. As of lap 17 the *entire machine from D back is
machine-checked + `#print axioms`-clean* (verified this lap, real output): Boundedness, C₁ `cutElim`→cr0,
D `orderType_le_of_TIprovable`, C₂-structural `embedC_LX_gen`, M4 `embedC`, M5 `cutElim`. The honest
realistic endpoint = **headline reduced to two well-scoped girders (E + F) atop a fully-built axiom-clean
infinitary proof-theory core** — net-new in Lean, worth finishing.

## 🎯 Open obligations (priority order)
1. **C₂ glue `hax_paLX`** X-induction assembly (`EmbeddingX.lean:705`) — pure integration, recipe inlined
   (see lap-17 HANDOFF #1-5). ~1 lap. **Closing it ⟹ Thm 5.6 (`PA ⊬ TI(ε₀)`) axiom-clean modulo E+F.**
2. **F arithmetization seam** (`‖≺‖=ε₀` + `hprec`/`hprecXPos`) — the campaign wall. Corrected attack-plan
   now in **PENDING_WORK.md top** (lap-18 reflection). Key findings:
   - `hprec` reduces to definability (`hprec_of_lMap_defined`) — TRACTABLE first brick; `TruthSem.models_lMap`
     already exists. `codeOfREPred₂` via Foundation `codeOfPartrec'` — verified real.
   - **The real girder = ε₀-completeness of CNF notations** (`∀ o<ε₀, ∃ x:ONote, NF x ∧ repr x=o`).
     **mathlib LACKS it** (only the order-embedding `NONote↪ε₀`). ~1–3 laps, **mathlib-only ⟹ the one
     piece well-suited to ARISTOTLE** (no Foundation context needed).
   - F's order MUST be the same `≺` step **E** commits to (co-design). Do NOT reuse `toONote 2·`/`seqONote`
     (pullback order type ω, not ε₀).
3. **E** Goodstein⟹TI_≺(X) — not started; pins F's order.

## 🎬 NEXT lap (19): pick a front (F definability half is now DONE)
- **Hardest-first (F wall):** the ε₀-completeness girder = `Seam.ge` (`∀ o<ε₀, ∃ x:ONote, NF x ∧ repr x=o`).
  Pure mathlib ordinal arith, ZERO Foundation dep ⟹ **good Aristotle candidate** (reconsider lap-17 "poor
  fit"). Hook: mathlib `Ordinal.lt_epsilon_zero : o<ε₀ ↔ ∃ n, o<(ω^·)^[n] 0` (in `SetTheory/Ordinal/Veblen`).
- **Tractable (F step 2):** `codeOfREPred₂` + spec (Foundation-side, independent). Gives `Seam.φ`. Note
  the `Semisentence ℒₒᵣ 2 = Semiformula ℒₒᵣ Empty 2` ⟹ `Empty→ℕ` embed wrinkle to feed `Seam.hφ`.
- **Milestone-closer:** close `hax_paLX` (recipe inlined at `EmbeddingX.lean:705`) ⟹ Thm 5.6 clean.

## ⚠️ Locked / notes
- **LOCKED untouched:** `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `sorry`.
- **src/ sorries (3):** `Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A, off-path),
  `EmbeddingX.lean:705` (`hax_paLX` glue). EpsilonOrder.lean is **sorry-free**.
- Build: `lake build GoodsteinPA` (**1267** jobs). Ledger in `STATUS.md` (refreshed lap 18).

## 📊 Lap estimate to headline (honest, updated)
C₂ glue ~1 · Thm 5.6 wiring ~0.5 · E ~2–4 · **F ~3–8+ (dominant risk; ε₀-completeness is the core)** · G ~1.
**Total ~8–15 laps, gated by F.** Everything from D back is axiom-clean machine-checked.
