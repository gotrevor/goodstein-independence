# HANDOFF — 2026-06-23 (lap 48) — internal Cor 3.4: clean-append + MinExpGe chain COMPLETE

> **Branch** `plan` · **HEAD `7b36ad1`** · build **green** (`lake build GoodsteinPA`, 1311 jobs) ·
> tree **clean** · headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact,
> untouched; 0 math axioms).
> Commits this lap: `61c07e8` (icmp_iadd_clean + icmp_swap), `6a1b559` (iC_iadd_clean),
> `5ce3ebb` (iAbove_iomul + iAbove_zero_iomul), `7b36ad1` (MinExpGe iterate + finite-threshold
> weakening + iAbove_zero_iomul type-bug fix).

## DONE THIS LAP — the internal Cor 3.4 comparison/bound/MinExpGe bricks (all axiom-clean)
Discharged the lap-47 PENDING NEXT items 1 & 2 and the MinExpGe-preservation, **without** needing
a general `icmp` transitivity (the g-paddings have FINITE leading exponents, so path-2 works).

`InternalONote.lean` (general icmp infra):
- `oswap` involution + `oswap_thenV`/`thenV_assoc`/`thenV_one_left`/`thenV_zero_left`/`cmpV_swap`.
- **`icmp_swap : icmp c2 c1 = oswap (icmp c1 c2)`** (CNF antisymmetry, pair-index induction);
  `icmp_two_iff_swap_zero`; `icmp_pos_ocOadd_lt_exp`.
- **`icmp_finThresh_mono`**: dominates finite `l` ⟹ dominates finite `j ≤ l` (concrete, no transitivity).
- thenV/cmpV registered as `Γ-Function₂` (so `definability` composes them).

`InternalCor34.lean`:
- **`icmp_iadd_clean`** (= `Grz.corAlpha_within`+`_boundary` unified): clean tails both sides ⟹
  `icmp (iadd a₁ b₁)(iadd a₂ b₂) = thenV (icmp a₁ a₂)(icmp b₁ b₂)`; `icmp_iadd_clean_boundary`.
- **`iC_iadd_clean`** (= `Grz.C_add_clean`): `iC(iadd a b) ≤ max (iC a)(iC b)` under clean.
- **MinExpGe**: `iAbove_iomul` (step `iAbove e0 a → iAbove (1+e0)(ω·a)`), `iAbove_zero_iomul`
  (base), `isNF_oadd1iter`, **`iAbove_ibigMul_iter`** (`iAbove ((1+·)^[k] 0)(ω^(k+1)·β)`),
  **`iAbove_finThresh_mono`** (weaken threshold `l ↦ j ≤ l`).

## NEXT (hardest-first within crux 1) — assemble `icorAlpha` (mirror `Grz.corAlpha`)
The bricks are ALL in place. Remaining glue:
1. **Cast identity** `(iadd (ocOadd 0 1 0))^[k] 0 = ocOadd 0 (k:V) 0` for k ≥ 1 (and `= 0` for k=0):
   induction on k via `iadd_one_zero` (k=0→1) and `iadd_one_fin` (merge, both exp-0). Then
   `iAbove_ibigMul_iter` gives `iAbove (ocOadd 0 (l:V) 0)(ibigMul (l+1) β)`; `iAbove_finThresh_mono`
   weakens to `iAbove (ocExp g)(ibigMul (l+1) β)` once `ocExp g = ocOadd 0 j 0` with `j ≤ l`
   (g < ω^(l+1); g's exp is finite by the ig0/iblk construction — establish `ocExp_ig` is a fin code).
2. **`icorAlpha l β j := iadd (ibigMul (l+1) (β block)) (ig l n m)`** with: C-bound (`iC_iadd_clean`
   + `iC_ibigMul_le` + iC of `ig`), within-descent (`icmp_iadd_clean` same lead, `ig`-tail descends),
   boundary-descent (`icmp_iadd_clean_boundary` + `icmp_ibigMul` lifting β-descent). Blueprint =
   sorry-free `Grzegorczyk.lean` `corAlpha_C_bound`/`corAlpha_within`/`corAlpha_boundary`.
3. Then the deep internal `ig` level-`l:V` recursion (the f-recursion) — abstract-`f` parameterization
   (lap-45 path #2). Feeds `DescentSemantic.nonterminating_of_xDescent`.

## Crux 2 (OTHER girder, untouched): Gentzen Thm 2.8(i) `PRWO(ε₀) → Con(𝗣𝗔)` + formulate
`PRWO(ε₀)` as `Sentence ℒₒᵣ`. See `Reduction.lean` docstring (`goodstein_implies_consistency` sorry).

## PROCESS NOTE: a `git checkout <file>` (to revert a temp `#check`) wiped 3 uncommitted lemmas this
lap — recovered from in-context text. COMMIT before any inspection-revert (see memory
`never-git-checkout-uncommitted-edits`).

## Aristotle: OFFLINE. WebFetch dead. LOCKED untouched (anti-fraud): `Defs.lean`, `Bridge.lean` RHS,
`goodsteinTerminates`, `Statement.lean` headline.
