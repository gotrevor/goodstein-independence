# HANDOFF — 2026-06-23 (lap 48) — internal Cor 3.4 BOUNDARY clean-append COMPLETE

> **Branch** `plan` · **HEAD `61c07e8`** · build **green** (`lake build GoodsteinPA`, 1311 jobs) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, untouched).

## DONE THIS LAP — NEXT item 1 of lap-47 (the boundary half of internal Cor 3.4)
Closed the lap-47 PENDING NEXT #1 (`icmp_iadd_clean_boundary`), and more — a **unified**
within+boundary comparison law. All axiom-clean.

`InternalONote.lean` (new general icmp infra):
- `oswap` (order-code involution 0↔2, fix 1) + Σ₀ def; `oswap_thenV`, `thenV_assoc`, `cmpV_swap`.
- **`icmp_swap : icmp c2 c1 = oswap (icmp c1 c2)`** (CNF comparison antisymmetry), strong
  induction on the pair ⟪c1,c2⟫; `icmp_two_iff_swap_zero`.
- `icmp_pos_ocOadd_lt_exp`. thenV/cmpV registered as `Γ-Function₂` (so `definability` composes them).

`InternalCor34.lean`:
- **`icmp_iadd_clean`** : tails clean below *both* spines ⟹
  `icmp (iadd a₁ b₁)(iadd a₂ b₂) = thenV (icmp a₁ a₂)(icmp b₁ b₂)` (= `Grz.corAlpha_within`+`_boundary`).
  Strong induction on ⟪a₁,a₂⟫; predicate definability built by hand (4 𝚷₁ iAbove antecedents +
  `Definable.comp₂` 𝚺₁ equation — `· definability` overflows aesop depth-30 otherwise).
- **`icmp_iadd_clean_boundary`** : `icmp a₁ a₂ = 0 ⟹ icmp (iadd a₁ b₁)(iadd a₂ b₂) = 0`.

## NEXT (PENDING_WORK lap-47 §, hardest-first within crux 1):
2. **`iC_iadd_clean`** (`iC (iadd a b) ≤ max (iC a)(iC b)` under `iAbove (ocExp b) a`) = internal
   `Grz.C_add_clean` (the slowness C-split) + `iAbove`-preservation for `ibigMul`/`iomul`
   (internal `MinExpGe_bigMul`: every `ω^k·β`-spine exponent dominates `e0` when `e0 < ω^k`).
3. The abstract-`ig` interface + `icorAlpha` + the deep internal `ig` level-`l:V` recursion.

These three bricks (`icmp_iadd_clean*` ✓, `iC_iadd_clean`, `MinExpGe`-preservation) assemble into
`InternalCor34.icorAlpha` mirroring `Grz.corAlpha` (C-bound + within + boundary descent), the input
to `DescentSemantic.nonterminating_of_xDescent`. Blueprint = sorry-free `Grzegorczyk.lean`.

## Crux 2 (the OTHER girder, untouched this lap): Gentzen Thm 2.8(i) `PRWO(ε₀) → Con(𝗣𝗔)` +
formulate `PRWO(ε₀)` as `Sentence ℒₒᵣ`. See Reduction.lean docstring.

## Aristotle: OFFLINE. WebFetch dead (use WebSearch / ON-LINE-REQUEST.md). LOCKED untouched:
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
