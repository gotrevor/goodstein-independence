# HANDOFF — lap 91 · route-B keystone `tpReduce` built; I∀ obstructions mapped (O1/O2/O3)

> Supersedes prior HANDOFF-*.md. Read order: this → `PENDING_WORK.md` (lap-91 box) → `STATUS.md`.
> Branch `plan`. HEAD = this commit. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Headline
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms).

## Operator target
Discharge `peano_not_proves_goodstein` (`Statement.lean`) AXIOM-FREE. = crux-1 (done lap 57) ∘ crux-2
(`redSound` = internalized cut-elimination, `Reduction.goodstein_implies_consistency` `sorry`). Crux-2
is a multi-month wall; lap 90 proved it needs **route B** (faithful Buchholz reduct that REDUCES the
conclusion `tp(d)(Π,n)`, not the repo's `red` which keeps `Π` — faithful only for `Rep` chains).

## What this lap did (6 commits; 3 code, 3 doc — all green 1325)
Built route B's foundation in `InternalZ.lean`:
- ✅ **`tpReduce I s n`** = Buchholz reduced sequent `I(Π,n)` (PDF §2 14.23/14.252): `Rep→Π`,
  `R_∀xF→Θ→F(n)`, `R_¬A→A,Θ→⊥`, `L^k_∀xF→F(k),Θ→D`, `L^0_¬A→Θ→A`. ∀/¬ dispatch on `π₁(A∸1)=6`;
  ¬-body via `IsUFormula.neg_neg`. 5 per-symbol equations proved (`tpReduce_isymRep` `@[simp]`).
- ✅ **`tpReduceDef` / `tpReduce_defined` / `_definable` / `_definable'`** — `tpReduce` is `𝚺₁`-definable.
- ✅ **`fstIdx_red_eq_tpReduce_of_Rep`** — route-B conclusion invariant for the `Rep` case
  (`zTag ∈ {3,4}`), = the headline ⊥-orbit specialisation (existing `red` already meets it, no rewire).

## ⭐ THE FINDINGS (3 obstructions — the genuine route-B walls; read `PENDING_WORK.md`)
- **O1 — no eigenvariable freshness.** `zIallWff`/`zIndWff` (`InternalZ.lean:1542`) don't track
  `a ∉ FV(Γ)`. Route B's I∀ conclusion `Γ→F(0)` needs `Γ[a/0]=Γ`. Moot on ⊥-orbit (Γ=∅).
- **O2 — eigen-subst is NOT `ZDerivation_zsubst`.** The I∀ reduct `d₀(a/n)` substitutes the
  *eigenvariable* (small, occurs in `d₀`); `ZDerivation_zsubst` (`Zsubst.lean:834`) needs `d≤a` (fresh
  large slot), so it doesn't apply. This is the lap-78 substitution wall. **THE genuine next deep
  target** — and it's needed in the critical case 5.1 (`dᵢ[k]`), the real cut-elimination step.
- **O3 — invariant is antecedent-membership-equivalence, not raw `=`.** Sequents are sets; antecedents
  are `seqCons`+`inAnt`, tracked inconsistently (`zIallWff` exact, `zInegWff` membership). State the
  Thm-3.4(b) invariant as succedent-`=` + antecedent-`≈` to `tpReduce`. Chain threading (`isChainInf`
  via `inAnt`) is robust to it. Headline ⊥-orbit is exact, so O3 only bites the general induction.

## ▶ NEXT (dependency order — see `PENDING_WORK.md` lap-91)
1. **O2 eigen-subst lemma** (gates the I∀/critical reducts). Decompose: does `zsubst` compute the right
   term-substitution on the eigenvar, only lacking the `d≤a` discharge? Consider whether Buchholz §6's
   `Z^∞` embedding (where ∀ = the ω-rule, NO eigenvariables — cf. Bryce–Goré `~/src/Gentzen`, PA_ω) is
   the cleaner cut-elimination locus than finitary eigen-subst. (Big architectural call — Trevor's, not
   a lap's; flag, don't pivot unilaterally.)
2. O1 (freshness in Wff), 3. red rewire to emit `tpReduce`, 4. Thm 3.4(b) invariant (Rep cases done),
   5. `iord_descent_red` → `false_of_ZDerivesEmpty` → M2 → headline.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `#print axioms peano_not_proves_goodstein` =
`[propext, sorryAx, choice, Quot.sound]` (verified via `lake env lean` scratch, OUTSIDE the tree).
