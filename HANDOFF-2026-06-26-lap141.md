# Handoff: lap 141 — SPIKE DECIDED: existence/critical-pair reframe OBVIATES the tag-5/6 wall

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `0ee70e4` · build 🟢 green (`lake build GoodsteinPA`, **1326**).

> ⚠️ **CORRECTION (commit `0ee70e4`, supersedes the `iR2` framing below):** an in-kernel `#print axioms` showed
> the critical reduct must be the SOUND `red` (= `iRcritG`), NOT `iR2` (= the ordinal-shadow `iRcrit`, WRONG
> endsequents — `ZDerivesEmptyR (iR2 …)` is FALSE-RISK). `descent_step_K_critical` now witnesses with `red`:
> descent `iord_descent_red_zK_crit` (banked) + soundness `ZDerivesEmptyR_red` (routes through the PRE-EXISTING
> red-R2 `ZDerivation_red_zK_crit`, `Crux2Blueprint:1108`). The false-risk `descent_step_K_critical_soundness`
> sorry is DELETED; K-step is **4 tag sorries → 1** (`descent_step_K_noncritical`, the Buchholz 5.2 leaf). The
> dispatcher case-splits on the `permIdx` sentinel. Read the lap-141 `PENDING_WORK.md` entry for the corrected
> account; the `iR2`/`descent_step_K_critical_soundness`/"ONE obligation RedSound(iR2)" wording below is STALE.
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms), re-verified in-kernel this lap.

## ✅ THE result — the operator-mandated existence-form reframe spike, RUN and DECIDED in-kernel
`descent_step_K_critical` (`Crux2Blueprint`, **sorry-FREE, compiles green**) is the kernel proof that the
existence / critical-pair reframe **DOES obviate** the tag-5/6 "cutPartner-is-a-principal-R-intro" residual.
This **overturns lap-139's** "the existence reframe does not obviate the deep content" *for the tag-5/6 sub-case*.

**Mechanism (Buchholz §3.2, `scratchpad/buchholz-gentzen.txt:648-745` — read it):** Buchholz's K-rule reduction
(Def 3.2 case 5) splits on **CRITICAL vs NON-CRITICAL**, NOT on the major premise's inference tag. The lap-140
tag-{3,4,5,6} decomposition was a *non-Buchholz* framing; its tag-5/6 wall is an ARTIFACT of insisting on
reducing AT the major premise. In the critical case (5.1), **Lemma 3.1 (`inference_critical_pair`, banked)
returns a PRINCIPAL redex pair `(i,j)` with `tp dᵢ = R_{Aᵢ}` automatically** — principality falls out of
criticality, there is NO separate producer-principal proof. The critical-case DESCENT is already fully banked
(`iord_descent_iR2_zK_of_validF_critUpTo`, `InternalZ:8193`, sorry-free, needs only criticality up to `j0`).

Sidesteps the lap-140 `redZKReady_of_zKValid` refutation (which needed GLOBAL `zKCritical`, false on the
⊥-orbit): the reframe **case-splits** on criticality-up-to-`j0` — critical → reduct; non-critical → case 5.2.

## ⭐ THE advance — `descent_step_K_majorIdx` restructured critical/non-critical; K-step 4 sorries → 2
- **`descent_step_K_critical`** (sorry-FREE) — critical case = `iR2` reduct; descent banked; modulo soundness.
- **`descent_step_K_critical_soundness`** (sorry) — `ZDerivesEmptyR (iR2 …)` for a critical chain = Buchholz Thm
  3.4(b)/`RedSound`-restricted; recursive (the two `iCritReductG` auxiliaries are ZDerivations of their reduced
  endsequents; `ZDerivation_iCritReductG_of` recombines).
- **`descent_step_K_noncritical`** (sorry) — Buchholz case 5.2: replace/splice the minimal Π-permissible premise
  (5.2.2 replace via banked `iord_descent_zK_replace_explicit`; 5.2.1 splice). Structural recursion on `dᵢ`.
- Dispatcher `descent_step_K_majorIdx` is now sorry-FREE plumbing: reads `j0` off `isChainInf`
  (`(zKValidF_of_ZDerivation_zK hZ).1`), `by_cases hcrit : ∀ i ≤ j0, ¬ iperm (tp (znth ds i)) s`.
- DROPPED the four `descent_step_K_tag{3,4,5,6}` sorries; the producer-principal wall (laps 139/140) is GONE.

## 🎯 The remaining crux-2 termination wall (corrected) = red-R2 (1108) + non-critical 5.2 (1865)
After the `red`-not-`iR2` correction, the K-step's two residuals are BOTH Buchholz Thm 3.4(b):
- **red-R2 `ZDerivation_red_zK_crit`** (`Crux2Blueprint:1108`, PRE-EXISTING — the gate `descent_step_K_critical`
  and the whole `ZDerivesEmptyR_red` chain route through). ⚠️ **It is FALSE-as-stated for the current `red`**
  (lap-114 crux finding, docstring `Crux2Blueprint:1095`): red's I∀ critical reduct substitutes instance **0**
  (`red_zIall`), correct for DESCENT (`iord` instance-invariant) but WRONG for SOUNDNESS — `haux0`'s threading
  needs the L-redex instance **k**. **Genuine fix = re-principalize at k** (re-key red's tag-4 critical branch
  to `zsubst d0 a (numeral k)`; banked `seqSucc_zsubst_zIall_premise`/`ZDerivation_zsubst_zIall_premise`) OR
  route through `iRKcCrit`/`ZDerivation_iRKcCrit_all` (`Crux2Blueprint:961/993`). Descent survives 0→k.
- **non-critical 5.2 `descent_step_K_noncritical`** (`Crux2Blueprint:1865`, NEW this lap): dispatch the
  `permIdx`-selected Rep premise (mirror `iord_descent_red`, `Crux2Blueprint:1560`): I-rule/Ind → red-replace
  (banked); chain → recursion (cf. the ~1620 sorry); atom/`Ax¹` → §5 atomic reduct (`zAxReduct`/`zAx1`, banked
  bundles `iRedDescent_zAx1_zAx{All,Neg}` `InternalZ:7983`; Buchholz Lemma 5.2 `o(d[n])<o(d)` for atomic `d` =
  the genuine lap-129 stall fix).

**NEXT ATTACK (hardest-first):** the red-R2 instance-k re-principalization (1108) — it CLOSES the critical case
and is the standing red-soundness gap for the WHOLE orbit. Then `descent_step_K_noncritical` (1865), decomposed
by the `permIdx`-selected premise's tag into named src sub-`sorry`s. Both = Buchholz Thm 3.4 simultaneous
induction on the build-up of `d`.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT revert the critical/non-critical restructure to the tag-{3,4,5,6} split — the spike proved it inferior
  (re-introduces the obviated producer-principal wall).
- Do NOT re-attempt `redZKReady_of_zKValid` as a global shortcut (needs `zKCritical`, FALSE on ⊥-orbit, lap-140);
  the case-split on criticality-up-to-`j0` is the correct alternative.
- `redLeast`/μ for (A) (wrong polarity, lap-139); engine re-key of `red`/`redexI`/`redexJ` (lap-139); `red dⱼ`
  single-replace tag-3 witness (ordinal-shadow FALSE sorry, lap-138).

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `descent_step_K_critical` (~1830, sorry-free spike),
  `descent_step_K_critical` (~1836, `red`-based, no NEW sorry — routes through 1108),
  `descent_step_K_noncritical` (~1865, the ONE new sorry), the sorry-free dispatcher
  `descent_step_K_majorIdx` (~1877), and `ZDerivation_red_zK_crit` (1108, the red-R2 gate).
- `src/GoodsteinPA/RedZKDescent.lean`: `iord_descent_red_zK_crit` (86, critical-red descent BANKED).
- `src/GoodsteinPA/InternalZ.lean`: `inference_critical_pair`/`_of_chain_tp` (507/1807, Lemma 3.1),
  `ZDerivation_iRcritG_of`/`_iRcritGNeg_of` (9761/9780), `iRedDescent_zAx1_zAx{All,Neg}` (7983),
  `iord_iRcritG_eq_iRcrit` (7795, the genuine-vs-shadow `iord` bridge).
- `scratchpad/buchholz-gentzen.txt:648-745` — Def 3.2 case 5 (5.1 critical / 5.2 non-critical) + Thm 3.4.
- `PENDING_WORK.md` lap-141 · `STATUS.md` lap-141 banner · `ANALYSIS-2026-06-25-lap114-inversion-instance-mismatch.md`.

---
**→ Next session: HEAD = `0238c9c`, build green (1326), headline `[propext, sorryAx, Classical.choice,
Quot.sound]`. The tag-5/6 producer-principal wall is OBVIATED (spike-decided in-kernel). K-step decomposed
critical/non-critical: critical reuses `red`'s existing soundness gate `ZDerivation_red_zK_crit` (1108, the
lap-114 instance-0→k re-principalization), non-critical is the one new sorry `descent_step_K_noncritical`
(1865). FIRST move: the red-R2 instance-k re-principalization (1108) — it CLOSES the critical case and is the
standing red-soundness gap. Then decompose 1865 by the `permIdx`-selected premise's tag.**
