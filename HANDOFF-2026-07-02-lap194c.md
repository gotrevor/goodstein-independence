# Handoff: lane D read-off (lap 194) — DECOMPOSED + root-caused to the missing (Ax2) rule (shared with rung E)

**Date**: 2026-07-02 · **Branch**: `plan` · **HEAD**: `e588032` · **Build**: 🟢 `lake build GoodsteinPA`
1328 jobs · **Headline**: `GoodsteinPA.peano_not_proves_goodstein = [propext, Classical.choice,
goodstein_implies_consistency, Quot.sound]` (sorryAx OFF, UNDRIFTED — re-verified via `#print axioms`).
Tree CLEAN.

**Order in force**: `REBUILD-Z-SERIES-1-ORDER-2026-07-02.md` · **Binding directive**: `DIRECTION.md`
CURRENT DIRECTIVE **lap-192 block** (MANDATE = lane D; top-rank cut JUDGE-GATED, wip-only probes only).
**Ledger**: `REBUILD-Z-SERIES-1-LEDGER.md` (lap-194 / 194c rows at end).

## ✅ What this lap did (3 green commits) — mandated lane D

- **`c47401f` (194)** — rung-D read-off `readoff_delta0_Zef2` DECOMPOSED from a monolithic `sorry` to
  a `readoffD_aux` reduction. New in `src/GoodsteinPA/OperatorZef2.lean`, all axiom-clean except the
  one disclosed residue: `sound0` (rank-0 `Zef2` soundness), `valm_nm` (local numeral-eval,
  `simp [nm]`), `atomTrue_all_iff`/`atomTrue_ex_iff`, and **`readoffD_aux`** (the strengthened FALSITY
  invariant `∀ψ∈Γ, ψ=∃⁰φ ∨ ¬atomTrue ψ → ∃ n ≤ f 0, atomTrue(φ/[nm n])`) — PROVEN for
  `axL`/`wk`/`weak`/`exI`/`cut` and the `allω` NON-trapped branch. `readoff_delta0_Zef2` now proven
  modulo the single residue `readoffD_trapped`.
- **`f52229c` (194b)** — characterized the residue: branch-local data is INSUFFICIENT (an `f 0`
  witness can only come from an `exI` at the unrelativized slot `f`, absent in the trapped subtree);
  naive accumulation REFUTED (`exI` witnesses need not be true).
- **`e588032` (194c)** — read the **E–W Lemma 31 (Witnessing) proof** from the PDF and ROOT-CAUSED
  the residue (see below). Docstring + PENDING (194c) + ledger carry the full analysis.

## 🔑 Route-decisive finding (carry forward)

**`readoffD_trapped` traces to `Zef2` missing E–W's (Ax2) true-literal rule.** From the Lemma 31
proof: E–W code `∃`=`⋁`-type (`(⋁)` keeps operator `f`, witness `≤ f(0)`), `∀`=`⋀`-type (`(⋀)`
relativizes `f→f[N(ι)]`) — matching our `exI` / `allω`(`rel1`). They ALSO have **(Ax2): a true closed
literal closes the sequent**; `Zef2` has only `axL` (complementary PAIR). Lemma 31 extracts the
top-`∃` witnesses via `(⋁)` at operator `f` and verifies the Δ₀ matrix instances SEMANTICALLY
(soundness) — it NEVER structurally descends the matrix. Our `readoffD_aux` DOES descend via `allω`
(relativizing the slot) ⇒ the trap. Without (Ax2), a false `∀⁰χ` branch can't close on a true
literal, forcing it to consume the trapped `∃⁰φ` deep (bound `f n > f 0`).

**Adding (Ax2) is the architect-gated Ax2-adequacy ALREADY flagged for rung E** (`Zekd` has
`trueRel`/`trueNrel`, `Zef2` has none, E–W Def 23 has (Ax2)). **So the rung-D residue and rung E's
gate are ONE calculus-faithfulness decision** — not self-ratifiable (a judged amendment, cf. the
`rel1` redesign).

## 🚧 NEXT (lane D — pick one)

1. **(Architect-gated) mirror E–W:** add the (Ax2) true-literal rule to `Zef2`, then prove the
   read-off à la Lemma 31 — extract the top-`∃⁰ φ` witness via `exI` at slot `f`, verify `φ/[nm n]`
   truth via `sound0` (semantic), WITHOUT recursing into `allω` matrix branches. Unblocks rung E too.
   Do NOT add (Ax2) to `src` without the architect/judge ruling — probe wip-only if at all.
2. **(In-scope pure-proof alternative)** show trap-derivations do NOT EXIST in `Zef2`-without-(Ax2):
   with no true-literal closure, the false `∀⁰χ` branch may be underivable ⇒ residue vacuous. Needs a
   normalization/occurrence meta-argument on the singleton derivation (NOT the refuted branch-local
   induction). Substantial but not gated — the thread to start fresh.
3. If both stall, this is escalation input for the series-end judge: rung-D read-off needs the (Ax2)
   amendment; decide (Ax2)-adequacy for `Zef2` once, covering D and E.

## 📁 Key files
- `src/GoodsteinPA/OperatorZef2.lean` — `sound0`, `valm_nm`, `atomTrue_all_iff/ex_iff`, `readoffD_aux`
  (PROVEN), `readoffD_trapped` (residue `sorry`, full E–W diagnosis in docstring),
  `readoff_delta0_Zef2` (wired). Other sorries: `:655` top-rank cut (judge-gated), `embedding_Zef2`
  rung E (architect-gated), `wainer_splice_Zef2` (downstream).
- `PENDING_WORK.md` — lap-194c / 194b / 194 blocks at top.
- `REBUILD-Z-SERIES-1-LEDGER.md` — lap-194 / 194c rows.
- `DIRECTION.md` — CURRENT DIRECTIVE lap-192 block (BINDING).
- No uncommitted edits (tree clean at `e588032`).
