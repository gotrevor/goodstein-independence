# HANDOFF — lap 92 · DEEP REFLECTION · the ω-rule pivot (route C)

> Supersedes prior HANDOFF-*.md. Read order: `REFLECTION-2026-06-25-lap92.md` (primary) → this →
> `STATUS.md` (lap-92 box) → `PENDING_WORK.md` (lap-92 box). Branch `plan`. HEAD = this commit.
> Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Headline
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms);
> `peano_not_proves_consistency` clean; `goodsteinSentence_faithful` clean (re-audited, no drift).

## Operator target (unchanged)
Discharge `peano_not_proves_goodstein` (`Statement.lean`) AXIOM-FREE = crux-1 (DONE lap 57) ∘ crux-2
(`goodstein_implies_consistency`, `Reduction.lean:68`, the lone `sorry`) = `redSound` = internalized
cut-elimination.

## What this lap decided (no proof code — synthesis is the deliverable)
**Direction KEPT; sub-route PIVOT recommended (route C = the ω-rule).** The laps-78–91 stall is an
artifact of arithmetizing Buchholz's **finitary eigenvariable** system. There cut-elimination needs
`d₀(a/n)` eigenvariable substitution that *preserves validity* — the O2 / lap-78 wall. Route A
(keep-Π `red`) was refuted lap-90; route B (`tpReduce`, laps 90–91) **still hits the same substitution
wall**, and lap-90 proved there is no validity-free bypass.

**The fix: arithmetize the infinitary ω-rule system** (Buchholz §6 `Z^∞`/Schütte `PA_ω`), exactly as
(a) the repo's own meta engine `Zinfty.lean` does, axiom-clean, and (b) Bryce–Goré's complete Coq
`Con(PA)` does. An ω-rule `∀xF`-node has a premise *family* `{dₙ ⊢ Γ→F(n)}`; a critical cut **selects
`dₙ`** (already deriving `Γ→F(n)`) instead of substituting an eigenvariable. ⟹ **O1 (freshness),
O2 (eigen-subst), and route-B's `tpReduce` conclusion-tracking all collapse at once.**

## ▶ NEXT (de-risked — do NOT rewrite InternalZ yet)
1. **`wip/InternalZomega.lean` SPIKE (the riskiest-assumption probe).** Define an internal ω-rule ∀-node
   `zAllOmega s g` (`g : V` codes the Σ₁ premise-notation; premise-n = `appPrem g n` derives `Γ→F(n)`),
   reusing the seq/notation machinery (`zK`/`zKseq`/`iIndReductSeq`/`iRepeatSeq`). State its
   `ZDerivation`-clause + the **critical-cut reduct** for a `∀xF`-cut and *confirm by elaboration* the
   reduct is `appPrem g n` with **zero substitution obligations**. This is one bounded, self-contained
   lemma — good Aristotle fodder once stated. (Aristotle is IDLE; all jobs done. Feed it this once crisp.)
2. **Decision gate.** Spike clean + reduct substitution-free → commit route C: port the axiom-clean
   `iord`/`icmp`/`idg`/`iõ`/ω-tower engine, rebuild `red`/`redSound` over the ω-rule with cut-elim by
   recursion on `iord<ε₀` (licensed by PRWO). New wall instead → genuine finding, fall back to route-B O2.
3. **Keep the reusable route-B leaves** (`red_zK_rep/_splice`, `tp_eq_isymRep_of_zTag`,
   `red_rep_of_tp_isymRep`, `tp_isymRep_of_emptyAnt_botSucc`): the `tp`-dispatch survives route C; only the
   eigenvariable substitution is replaced by premise selection.

## STOP
No more finitary `tpReduce` conclusion-tracking or `Zsubst`/`ZDerivation_zsubst` eigenvariable-substitution
lemmas (O1/O2). Do not re-derive the "substitution must preserve validity" wall a fourth time.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `#print axioms peano_not_proves_goodstein` =
`[propext, sorryAx, choice, Quot.sound]` (verified via `lake env lean` scratch, OUTSIDE the tree, this lap).
