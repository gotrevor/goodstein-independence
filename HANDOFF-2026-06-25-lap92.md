# HANDOFF — lap 92 · DEEP REFLECTION · the ω-rule pivot (route C)

> Supersedes prior HANDOFF-*.md. Read order: `REFLECTION-2026-06-25-lap92.md` (primary) → this →
> `STATUS.md` (lap-92 box) → `PENDING_WORK.md` (lap-92 box). Branch `plan`. HEAD = this commit.
> Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Headline
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms);
> `peano_not_proves_consistency` clean; `goodsteinSentence_faithful` clean (re-audited, no drift).

## Operator target (unchanged)
Discharge `peano_not_proves_goodstein` (`Statement.lean`) AXIOM-FREE = crux-1 (DONE lap 57) ∘ crux-2
(`goodstein_implies_consistency`, `Reduction.lean:68`, the lone `sorry`) = `redSound` = internalized
cut-elimination.

## What this lap decided (no proof code — synthesis is the deliverable)
**Direction KEPT; sub-route PIVOT recommended (route C = the ω-rule).** The laps-78–91 stall is an
artifact of arithmetizing Buchholz's **finitary eigenvariable** system. There cut-elimination needs
`d₀(a/n)` eigenvariable substitution that *preserves validity* — the O2 / lap-78 wall. Route A
(keep-Π `red`) was refuted lap-90; route B (`tpReduce`, laps 90–91) **still hits the same substitution
wall**, and lap-90 proved there is no validity-free bypass.

**The fix: arithmetize the infinitary ω-rule system** (Buchholz §6 `Z^∞`/Schütte `PA_ω`), exactly as
(a) the repo's own meta engine `Zinfty.lean` does, axiom-clean, and (b) Bryce–Goré's complete Coq
`Con(PA)` does. An ω-rule `∀xF`-node has a premise *family* `{dₙ ⊢ Γ→F(n)}`; a critical cut **selects
`dₙ`** (already deriving `Γ→F(n)`) instead of substituting an eigenvariable. ⟹ **O1 (freshness),
O2 (eigen-subst), and route-B's `tpReduce` conclusion-tracking all collapse at once.**

## ⭐ CORRECTION (later this lap — read `ANALYSIS-2026-06-25-lap92-criticality-wall-is-gone.md`)
Verified in-kernel that `ZPhi` ALREADY uses criticality-free `zKValidF` and `ZDerivation_zsubst` is green.
**⟹ the lap-78 "substitution wall" is GONE** (its CE-1/CE-2 attack only the criticality conjunct, no longer
in validity). Lap-91's **O2 is mislabeled** — not the lap-78 wall. The real residual is the **O1↔O2
freshness/eigensubst coupling** intrinsic to finitary ∀. This gives TWO honest paths (not just the ω-rule):
- **Path X (stay finitary, lower risk, no longer known-blocked):** add eigenvariable freshness to
  `zIallWff`/`zIndWff` (O1) + prove `ZDerivation_zsubst_eigen` (eigenvariable→closed numeral, `aNotEigen`,
  preserving `zKValidF`) (O2).
- **Path C (ω-rule pivot):** dissolves O1+O2+`tpReduce` permanently; bigger one-time rebuild.

## ⭐⭐ DECISION (end of lap 92 — `DECISION-2026-06-25-lap92-path-X-favored.md`): Path X favored
The ω-rule precedents (`Zinfty.lean`, Bryce–Goré) are META-level; they do NOT de-risk the *arithmetized*
ω-rule (Σ₁ `Z*` notations), the un-precedented hard part. Path X finishes the invested finitary
arithmetization, and O1 is now tractable + **maintainable through `red`** via the key insight: **`zsubst`
(closed term) preserves eigenvariable indices**, so a freshness invariant on eigenvariable indices
(`maxEigen premise < eigenvar`) is reduction-stable (the code-bound `d≤a` was not). O2 is already banked.

## ▶ NEXT (Path X foundation lemmas — concrete, reuse the `idg` template)
1. **✅ DONE (commit `51d17b1`):** `maxEigen d` (largest eigenvariable index) defined + `𝚺₁`-definable in
   `Zsubst.lean` via the `idg`/`PR.Construction` template (`maxEigenNext`/`maxEigenTable`/`maxEigenDef`),
   axiom-clean, green 1325.
2. **✅ DONE (lap 93, `Zsubst.lean`, axiom-clean, green 1325): recursion equations + stability.**
   (a) `maxEigen_zAtom`/`_zIall`(`max a (maxEigen d0)`)/`_zIneg`/`_zInd`(`max (π₁ at') (max …)`)/`_zAxAll`/
   `_zAxNeg`/`_zAx1`/`_zK`(`iseqMaxEigen ds`) via the `idg` structural-correctness template
   (`maxEigen_eq_maxEigenNext` + `znth_maxEigenTable_eq_maxEigen` + the `iseqMaxEigen` fold, mirror `idg_zK`).
   (b) **`maxEigen_zsubst (a t) : ∀ d, ZDerivation d → maxEigen (zsubst d a t) = maxEigen d`**, by
   `zDerivation_induction` + (a) + `zsubst_zIall`/`zInd` keeping the eigenvariable + fold-congruence
   `iseqMaxEigenAux_congr` (chain case). `#print axioms` = `[propext, choice, Quot.sound]`.
3. **✅ DONE (lap 93, `Zsubst.lean`, axiom-clean, green 1325): `ZDerivation_zsubst` reformulated** from
   `d ≤ a` to **`maxEigen d < a`**. Moved the whole `maxEigen` block ABOVE `ZDerivation_zsubst`; added
   fold-bound `le_iseqMaxEigen`; each case derives `e ≠ a` + premise bound from the recursion eqs. Dead
   `hsa` removed. Corollaries `ZDerivation_zsubst_zIall_premise`/`_zInd_premise1` now take
   `maxEigen d0 < a` / `maxEigen d1 < π₁ at'`.
4. **✅ DONE (lap 93): O1 foundation via additive `zReg`** (NOT a `zIallWff` edit — that reshapes the
   fixpoint; large blast radius). Standalone `𝚺₁` hereditary-freshness `zReg d` (violation count, `0` iff
   regular) on the `maxEigen` template + recursion eqs + `ZRegular`; route-B bridges
   `maxEigen_lt_of_regular_zIall`/`_zInd`; substitution step `zReg_zsubst`. All axiom-clean, green 1325.
5. **← START HERE: thread `ZRegular` through `red`/`redSound`** — embedding ⟹ regular; `red` preserves
   `ZRegular` (hard: splice/replace reducts; `zsubst` step is done). Then route-B
   reducts discharge (banked corollaries `ZDerivation_zsubst_zIall_premise`/`_zInd_premise1`). Probe framing
   superseded by the Path-X DECISION:
1. **`wip/InternalZomega.lean` SPIKE (the riskiest-assumption probe).** Define an internal ω-rule ∀-node
   `zAllOmega s g` (`g : V` codes the Σ₁ premise-notation; premise-n = `appPrem g n` derives `Γ→F(n)`),
   reusing the seq/notation machinery (`zK`/`zKseq`/`iIndReductSeq`/`iRepeatSeq`). State its
   `ZDerivation`-clause + the **critical-cut reduct** for a `∀xF`-cut and *confirm by elaboration* the
   reduct is `appPrem g n` with **zero substitution obligations**. This is one bounded, self-contained
   lemma — good Aristotle fodder once stated. (Aristotle is IDLE; all jobs done. Feed it this once crisp.)
2. **Decision gate.** Spike clean + reduct substitution-free → commit route C: port the axiom-clean
   `iord`/`icmp`/`idg`/`iõ`/ω-tower engine, rebuild `red`/`redSound` over the ω-rule with cut-elim by
   recursion on `iord<ε₀` (licensed by PRWO). New wall instead → genuine finding, fall back to route-B O2.
3. **Keep the reusable route-B leaves** (`red_zK_rep/_splice`, `tp_eq_isymRep_of_zTag`,
   `red_rep_of_tp_isymRep`, `tp_isymRep_of_emptyAnt_botSucc`): the `tp`-dispatch survives route C; only the
   eigenvariable substitution is replaced by premise selection.

## STOP
No more finitary `tpReduce` conclusion-tracking or `Zsubst`/`ZDerivation_zsubst` eigenvariable-substitution
lemmas (O1/O2). Do not re-derive the "substitution must preserve validity" wall a fourth time.

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `#print axioms peano_not_proves_goodstein` =
`[propext, sorryAx, choice, Quot.sound]` (verified via `lake env lean` scratch, OUTSIDE the tree, this lap).
