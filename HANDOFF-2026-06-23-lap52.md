# HANDOFF — 2026-06-23 (lap 52) — crux-1 internal-Grzegorczyk substrate built end-to-end

> **Branch** `plan` · **HEAD `66a0e7b`** · build **green** (`lake build GoodsteinPA`, 1313 jobs) ·
> tree **clean** · headline `peano_not_proves_goodstein` = honest `sorry`
> (`#print axioms` = `[propext, sorryAx, Classical.choice, Quot.sound]`, anti-fraud intact, verified
> this lap). 11 commits `c9fc6ea..66a0e7b`. Two modules PROMOTED to `src/` (BlkRec, IIter); one new
> wip file (InternalGrz). All new lemmas axiom-clean (`[propext, choice, Quot.sound]`), sorry-free.

## What this lap did — built the WHOLE internal-Grzegorczyk substrate for crux-1 brick 2
Followed lap-51 designated NEXT (discharge the `wip/StdCor34` interface). Crux 1 = `γ → PRWO`; its
internal Cor-3.4 slow-down `salpha` (`wip/StdCor34.lean`) needs two interface obligations:
**(brick 1)** block bookkeeping `blk`/`off`, and **(brick 2)** the standard-level `ig`-tail recursion.
Both substrates now exist, axiom-clean:

### `src/GoodsteinPA/BlkRec.lean` — brick 1 COMPLETE (promoted to src/, in the build)
Internal `𝚺₁` state machine (`boStep`: advance offset / roll to next block when `off+1 ≥ W(blk)`) over
an abstract width *sequence code* `wseq` (`znth wseq b`), sidestepping internal `findGreatest`. Proves:
- `blk_succ_dich` (= `salpha_desc`'s `hblk_dich`), `off_succ_of_blk_eq` (within-block off-advance,
  behind `higt_within`), `blk_add_off_le`/`blk_le` (block count).
- **`wsumc` + `wsumc_blk_le`** (codex review): the elapsed-*WIDTH* invariant `wsumc(blk j) ≤ j`
  (`wsumc_blk_add_off : wsumc(blk j)+off j = j`), the honest `salpha_C_le.hβC` input — `blk_le`
  (block count) alone was an OVERCLAIM. `off_lt_width` (offset < current width).
- **`blk_prefix_congr`/`off_prefix_congr`** (codex wseq-seam route b): `blk`/`off` depend only on
  `wseq`'s prefix `[0,j]` ⟹ a long-enough prefix code of the true widths suffices.
- ⚠️ `off_lt_width`/`wsumc_blk_add_off`/`wsumc_blk_le` take **prefix** positivity `∀ b ≤ j, 1 ≤ znth wseq b`
  (so a FINITE code, `znth=0` past length, works once it covers `[0,j]`). Signature: `(wseq) ∀ j, (hpos) → …`.

### `src/GoodsteinPA/IIter.lean` — brick 2 substrate COMPLETE (promoted to src/, in the build)
- `iIter fDef f hf x c = f^[c] x` — internal iteration of a FIXED `𝚺₁`-fn `f` (graph `fDef`) at an
  INTERNAL count `c`, as a `PR.Construction` + recurrence laws + definability + `iIter_natCast`.
- `iF : ℕ → (V→V)` — standard-level Grzegorczyk hierarchy by **meta-recursion on level** (Subtype
  bundle `iFwith` carries function+Def+proof): `iF_zero`/`iF_succ`/`iF_defined`/`iF_natCast`. NO
  internal Ackermann (level stays meta-`ℕ`).
- `ipsum f n i = Σ_{t=1}^i f^[t] n` (`Grz.psum` internal): `ipsum_zero`/`_succ`/`_defined`/monotone.
- **GOTCHA:** `iIter`/`ipsum`/`iwseq` are parameterized by `(fDef,f,hf)` so their definability stays a
  LEMMA, not instance (the `Def` doesn't determine `f`/`hf`). Downstream re-expose as instances when
  `f`/`hf` are fixed section vars (`InternalGrz` does `instance iIter_inst'/ipsum_inst'`).

### `wip/InternalGrz.lean` — brick-2 block decomposition (wip, off build target)
- `iwseq f n N = ⟨f^[1]n,…,f^[N]n⟩` width code: `iwseq_seq`/`_lh`/`znth_iwseq` (`znth = f^[k+1] n`).
- **`wsumc_iwseq : BlkRec.wsumc (iwseq f n N) i = ipsum f n i`** — THE bridge: turns the whole BlkRec
  invariant suite into the classical `Grz.psum`-based decomposition for free.
- `iblockIdx/iblockOff := BlkRec.blk/off (iwseq f n (m+1)) m`; `iIter_pos`/`one_le_znth_iwseq`
  (positive widths from positivity-preserving `f`, `n≥1`); the two decomposition laws:
  `ipsum_iblockIdx_add_iblockOff : ipsum f n (iblockIdx) + iblockOff = m` and
  `iblockOff_lt_width : iblockOff < f^[iblockIdx+1] n`.
- **GOTCHA banked:** `iIter_succ` is `@[simp]`; inside `definability` it rewrites `iIter n (k+1) ↦
  f (iIter n k)`, exposing abstract `f` (no standalone definability) ⟹ `attribute [-simp] IIter.iIter_succ`.

## NEXT (hardest-first) — assemble `ig` + port the `g`-properties into the `StdCor34.igt` interface
1. **`ig : ℕ → V → V → V`** meta-recursion (mirror `Grz.g`, `Grzegorczyk.lean:343`):
   `ig 0 = ig0` (built, `InternalCor34`); `ig (l+1) n m = iblk (l+1) (n - iblockIdx (iFDef l) (iF l) (iF_defined l) n m)
   (ig l (iIter (iFDef l) (iF l) (iF_defined l) n (iblockIdx …)) (iblockOff …))` for `m < iF (l+1) n` else `0`.
   `iblk` is in `InternalCor34` (`ω^k·c + x`, all props). Needs positivity-preservation of each `iF l`
   (`∀ x, 1 ≤ x → 1 ≤ iF l x`) — prove by meta-induction on l via `iF_succ`/`iIter_pos`.
2. **Port `Grz.g_NF`/`g_lt`/`g_desc`/`g_C_bound`** (`Grzegorczyk.lean:363/390/426/599`) to `ig`, using the
   `InternalGrz` decomposition laws + the `iblk`/`ig0` brick props (`InternalCor34`). These ARE the
   `StdCor34.igt` interface: `higtNF`/`higt0`/`higt_within`/`higtC`/`higt_exp`. Set `igt n m := ig l₀ n m`
   for the Lemma-3.2 standard level `l₀`.
3. **Wire** `BlkRec.blk/off` (outer, widths = `iC(β(t+1))` via a prefix code) + `igt` into
   `StdCor34.salpha_*` → `InternalThm35.bbeta` → `DescentArith.nonterminating_internal` (Lemma 3.6) ⟹
   `goodstein_implies_prwo` body (`wip/GentzenCon.lean`, crux 1).
4. ⚠️ **DEFERRED DEEPER SEAM** (verify when crux-1 body lands): `icmp`-code ↔ `natCode`-order bridge
   (`StdCor34` orders by `icmp`/`isNF`; `prwoInstance`/`precφ` by `natCode`). F-φ-flavoured. ANTI-FRAUD:
   re-`#print axioms` the route as the `sorry` fills.

## Hardest wall (unchanged) — crux 2 eq (5) `ord(R d) ≺ ord d`
Gentzen reduction (Buchholz [6]); blocked until `ord`/`R` arithmetized over Foundation's
`Theory.Derivation` (Hauptsatz is meta-only). The deep from-scratch wall. Seam guards (crux1↔crux2↔Gödel
II) are machine-checked in `wip/GentzenCon.lean` (lap 51 SEAM 1/2/3).

## Aristotle: OFFLINE. WebFetch dead. LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS,
`goodsteinTerminates`, `Statement.lean` headline. M1 (`goodsteinTerminates_re`) already axiom-clean.
