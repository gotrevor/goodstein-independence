# HANDOFF — lap 102 · Probe 2 EXECUTED · the fork is SETTLED (Path C, stored ordinals)

> Branch `plan`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). `src/` unchanged (headline footprint
> intact). All new work is in `wip/InternalZomega.lean` (NOT imported by `GoodsteinPA.lean`), axiom-clean.
> **READ:** the `wip/InternalZomega.lean` Probe-2 section + verdict (primary), then `NEXT_STEPS.md`.

## What this lap settled
Lap-101 ended pointing at **Probe 2** (the decisive fork test): does the ω-rule retire the `redZKReady`
chain motive on the node that actually walls? This lap ran it, in-kernel, and got a clean verdict **with a
refinement that overturns lap-101's cost estimate**.

**(A) chain motive RETIRED — already proven by `Zinfty.lean`.** The meta ω-rule calculus `ZinftyF.Deriv`
does the full Gentzen cut-elimination (Towsner §19) with NO chain rule and NO `redZKReady`-style
conclusion-tracking motive — the only multi-premise rule is `allω`, ∀/∃-cuts reduce by premise SELECTION.
So the lap-92 thesis (selection dissolves conclusion-tracking) is CONFIRMED: the ω-calculus has no chain to
track. `Zinfty.lean` IS the in-kernel certificate.

**(B) the SURPRISE — the induction ω-node's ordinal is a genuine LIMIT** (3 new axiom-clean lemmas):
- `iotil_zK_iIndReduct` — the induction `k`-step unfolding's õ = `ω^{õ d1}·k # ω^{õ d0}` (closed form;
  coefficient `k` IS the unfolding depth).
- `ocOadd_coeff_strictMono` — `ω^e·k₁ ≺ ω^e·k₂` for `k₁<k₂`.
- **`iotil_zK_iIndReduct_strictMono`** (the key result) — the induction premise-family ordinal STRICTLY
  INCREASES in depth `k`. So `⨆_k o(premise k) = ω^{õ d1 + 1} # ω^{õ d0}` is a genuine limit, NOT a finite
  successor — Probe 1's "constant ⟹ finite `iord d0 + 1`" escape does **not** extend to induction. The
  repo's computed `iord = iotower (iotil) (idg)` is a finite-`#`-fold with no sup operation ⟹ it cannot
  assign the induction ω-node its ordinal.

`#print axioms iotil_zK_iIndReduct_strictMono = [propext, Classical.choice, Quot.sound]` (verified).

## The refined NET CALL (supersedes lap-101)
Fork settled IN FAVOUR of **Path C (ω-rule)** — but lap-101's "reuse the ordinal engine" estimate was wrong
on one axis. The ω-rule retires the chain motive (good), but the **ORDINAL LAYER must be REPLACED, not
reused**: the induction ω-node ordinal is a limit the computed `iord` cannot express. Path C = **Buchholz
operator-controlled derivations with STORED ordinals** (`Deriv` carries `o` as node data + side condition
`∀ premise, o(premise) ≺ o(node)`), exactly as `ZinftyF.Deriv`/`o` does at the meta level. This is MORE
arithmetizable than computing a sup (no limit operation needed), but it is a from-scratch ordinal/derivation
datatype, not a graft onto `InternalZ`'s `iotower`/`iotil`/`idg`.

- **Path X (finitary) disfavoured AND likely BROKEN.** Its linchpin `redZKReady`'s hereditary-Rep invariant
  does NOT follow from Cor 2.1 down a nested-chain spine: a sub-chain's selected premise is permissible
  w.r.t. the sub-chain's OWN conclusion `Γᵢ→Aᵢ` (growing antecedent, arbitrary succedent), which CAN be an
  I-rule/axiom (non-Rep). So `fstIdx (red dᵢ) = fstIdx dᵢ` — the keep-Π replace precondition the repo's
  `red` DEFINITION commits to for non-critical nested chains — can FAIL, making `red` unfaithful there.
  Cor 2.1 fires only at the ∅→⊥ TOP node, not hereditarily. (Lap-101's worry, now with a concrete reason.)

## NEXT LAP — begin the Path-C arithmetized datatype
Define the Σ₁-coded stored-ordinal ω-derivation, porting `ZinftyF.Deriv`/`o`/`cr` from `Finset Seq`/
`Ordinal` to `V`/`iord`-CNF. **First milestone:** the arithmetized `allω` node (`zconstruction` tag;
premise family by `zsubst`/iterated-step code; ordinal carried as data) + `iord` AS STORED + the single
`cutElimStep` ordinal drop on it — reusing this spike's `zOmegaPrem`/`iord_descent_zOmegaPrem` for the
∀-cut case, and `Zinfty.cutElimStep`/`cutElimPrincipal` as the proof template. crux-2's
`false_of_ZDerivesEmpty` then becomes: cut-bearing ⊥-derivation has stored ordinal `< ε₀`; `red` = one
`cutElimStep` strictly dropping it; ⊥ has no cut-free proof ⟹ `red` never terminates ⟹ infinite ε₀-descent
⟹ contradicts PRWO(ε₀). Keep `InternalZ`/`Crux2Blueprint` (Path X) green in `src/` as fallback until Path C
reaches `false_of_ZDerivesEmpty`.

## Kernel state (unchanged this lap)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — bare `sorry`, 0 math
  axioms (the lone `sorryAx` = crux-2).
- `peano_not_proves_consistency = [propext, choice, Quot.sound]` — clean. `goodsteinSentence_faithful` — clean.

## Aristotle
Idle — CORRECT. The Path-C datatype work is entangled with the `InternalZ`/`Zinfty` API; not a
self-contained mathlib lemma.

## ALSO landed this lap — Path-C brick 1 (`wip/PathCOmega.lean`, axiom-clean)
Began PRIORITY 1 immediately. The stored-ordinal ω-∀-node datatype, first brick:
- `zAllOmega s d0 a α` (tag 7) + `zAllOmegaValid` (premise family valid ∧ `∀-premise, iord ≺ stored α`).
- **`zIall_realizes_zAllOmegaValid`** — a regular finitary `zIall` realizes the ω-node with stored ordinal =
  the node's OWN `iord`, the descent side-condition discharged by the banked `iord_descent_zIall` (via
  `iord_zsubst`). The existing I∀ embedding produces a valid Path-C ω-node for free.
- `zAllOmega_cut_valid` / `zAllOmega_cut_descends` — the ∀-cut invariant (validity-preserving + ordinal
  drop below the stored α), read off the node data. The ∀-cut case of the `Zinfty.cutElimStep` analogue.
`#print axioms zIall_realizes_zAllOmegaValid = [propext, choice, Quot.sound]`. NEXT bricks (2–4) listed in
the file footer + `PENDING_WORK.md` lap-102.

## This lap's commits
(1) `wip/InternalZomega.lean` — Probe 2 (3 axiom-clean lemmas + verdict). (2) `wip/PathCOmega.lean` —
Path-C brick 1 (stored-ordinal ω-∀-node, axiom-clean). No `src/` change; `lake build GoodsteinPA` green
1325, headline footprint unchanged.
