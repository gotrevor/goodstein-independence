# HANDOFF — 2026-06-24 (lap 63) — cut-elim nut's ORDINAL SIDE complete; iR skeleton; ZDerivation descent

> **Branch** `plan` · **HEAD** `ad3ca5f` · `lake build GoodsteinPA` **green (1320 jobs)** · headline
> `peano_not_proves_goodstein` untouched (honest `sorry`, 0 math axioms, anti-fraud intact).
>
> **Lap arc:** the cut-elimination nut (Buchholz Lemma 4.1(b)(ii) case 5.1) was the charted hard core. This
> lap closed its **entire ordinal side** — every inequality it needs is now machine-checked, axiom-clean —
> and built the `iR` reduction skeleton + the `ZDerivation`-induction descent for the I-rules. The genuine
> remaining content is **object construction** (`iR`'s Ind/critical-K reducts = eigenvariable substitution +
> sequence-building on codes) plus L3.1/T3.4 (the redex finder / rank bound) and the C0.5 bridge.

## What landed (8 commits, all axiom-clean `[propext, choice, Quot.sound]`)
1. **Housekeeping** — harvested Bryce–Goré findings → `archive/findings/`; banked judge §8 leaf-breakdown;
   closed `ON-LINE-REQUEST.md`; added `papers/bryce-gore-gentzen/` stub.
2. **`icmp_iotower_height_le`** (`src/InternalTower.lean`) — general tower height-monotonicity
   `ω_m(β) ≼ ω_{m'}(β)` for `m ≤ m'`. **The nut's N4(iii)** (degree can drop by an arbitrary gap, so the
   single-step `icmp_iotower_lt_succ_of_le` was insufficient — judge validation item confirmed a real gap).
   Built on a strict-gap induction `icmp_iotower_lt_add_succ`.
3. **`iord_descent_cut`** (`wip/InternalZ.lean`) — **the nut's N4 combine**: `õ(e) ≺ ω^{õ(d)}` + degree drop
   `dg(e)+1 ≤ dg(d)` ⟹ `o(e) ≺ o(d)`, via base-mono + base-shift (`iotower_omega_pow`) + height-mono. The
   complete ordinal tail of case 5.1.
4. **`iR : V → V` skeleton** (`wip/InternalZ.lean`) — `zTag` dispatch; I¬/I∀ structural branches (reduct =
   premise); atom/Ind/K placeholders. `iRDef` 𝚺₀ graph + compute lemmas + `iord_descent_iR_z{Ineg,Iall}`.
5. **`isNF_iadd_one_right`** + `ocExp_iadd_self_one` (`src/InternalNadd.lean`) — right-successor NF
   (`isNF a → isNF (a+1)`), the brick for õ-NF of every I/Ind-rule derivation.
6. **õ-NF lemmas** `isNF_iotil_z{Iall,Ineg,Ind}` + **`icmp_iotil_ind_reduct`** (LH4 Ind ordinal core, F1+F3)
   (`wip/InternalZ.lean`).
7. **`iord_iR_descent_I`** (`wip/InternalZ.lean`) — structural descent over `ZDerivation` for the I-rules
   (`zTag ∈ {1,2}`), via the C0 Fixpoint `zDerivation_induction`. **V-level machine-checked analogue of
   `GentzenCon`'s placeholder `ord_R_descends`**, restricted to rules whose reduct `iR` already constructs.
8. **`inadd_right_mono`** (`src/InternalNadd.lean`) — F1-mirror (right-monotonicity via comm/F4), for chain
   cases where the reduced premise is not leftmost (N2).

## NEXT (hardest-first; the residual is OBJECT CONSTRUCTION, the judge's flagged "volume" risk)
1. **`iR`'s Ind reduct** `d[0] = K^r(d0, d1(0),…,d1(k−1))` — needs (a) eigenvariable substitution `sub : V→V→V`
   on derivation codes (primrec on codes) with **substitution-invariance** `õ(sub d1 j)=õ d1`, `dg(sub d1 j)=
   dg d1` (Buchholz Remark p.10), and (b) a sequence-builder `[d0, sub d1 0,…]` (model on `iCTable`'s
   `PR.Construction` in `InternalONote.lean`). Then `iotil(reduct)=ω^{õ d0}#ω^{õ d1}·k` connects to
   `icmp_iotil_ind_reduct` (DONE) and the idg side to `idg_zInd`/`idg_zK`, lifting LH4 to a full `iord`
   descent. **THE next object brick.**
2. **L3.1 redex finder** (judge §8.1) — a 𝚺₁ search over the chain's premise inference-symbols for the least
   critical `(i,j,k)`; finite `zTag`/inference-symbol combinatorics, NO ordinals. Needs the inference-symbol
   structure (note `irk` is still a STUB `:=0` — the real formula-rank recursion is a prereq for T3.4/T4).
3. **T3.4 rank bound + iR critical branch** (the nut's object side) — builds `d{0}=K^r(i/d_i[k])`,
   `d{1}=K^r(j/d_j[0])`, `d[0]=K^{r−1}d{0}d{1}`; then N3a (ℕ-max), N3b (F2), feed `iord_descent_cut` (DONE).
4. **C0.5 Foundation→Z bridge** (parallelizable, worktree) — `(𝗣𝗔).DerivationOf d ⊥ → ∃ z, ZDerivesEmpty z`,
   three-layer per Bryce–Goré `Peano.v` (`archive/findings/…bryce-gore-gentzen.md` §1; B1/B2/B3 in
   `E-CRUX2-DECOMPOSITION §5`). Z's native `Ind` rule skips their biggest sub-tower — <1k lines.
5. **PARALLEL:** `PA_delta1Definable` (mandatory per hardened endpoint; still an upstream `axiom` — §4 findings).

## Ordinal side of the nut — DONE inventory (don't rebuild)
`icmp_iotower_mono` (base), `iotower_omega_pow` (base-shift), `icmp_iotower_height_le` (height, N4iii),
`iord_descent_cut` (N4 combine), `iord_descent_{samedeg,dgdrop,I}`, F1 `inadd_left_mono`, F1-mirror
`inadd_right_mono`, F2 `icmp_omega_pow_nadd_lt`, F3 `icmp_term_lt_omega_succ`, F4 `inadd_comm`,
`icmp_iotil_ind_reduct` (LH4), õ-NF `isNF_iotil_z*` + `isNF_iadd_one_right`. The nut is now "two F-lemmas +
a tower step + object construction" — and the F-lemmas/tower step are all banked.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
Aristotle OFFLINE. Crux 1 DONE (axiom-clean lap 57). All crux-2 work stays in `wip/` until the cruxes are
real (anti-fraud). `src/` additions this lap are pure ordinal-algebra assets, sorry-free + axiom-clean.
