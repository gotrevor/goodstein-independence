# HANDOFF — lap 69 · crux-2 back-half reduced to ONE obligation (RedSound) + PRWO weave

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md`, then `STATUS.md`.
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = 1321 jobs. Tree clean.

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**.
Route: `PA⊢γ →(crux-1 DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)` → Gödel II. Sole math wall = crux-2
(Buchholz-Z ordinal analysis, the `Reduction.lean:68` girder `goodstein_implies_consistency`).

## ✅ WHAT LANDED THIS LAP (4 green commits, all axiom-clean `[propext,choice,Quot.sound]`)
Closed the **soundness gap** in the system-Z encoding and assembled the whole descent back-half down
to a single obligation. The fix: the `ZPhi` leaf + I-rule disjuncts were UNCONSTRAINED, so e.g.
`zAtom (∅→⊥)` was an irreducible normal form "deriving" ⊥. Added the **faithful Buchholz side
conditions** and re-ran the full Σ₁/Δ₁ Fixpoint cascade twice (leaf pass, then I-rule pass):
- **leaves** (tags 0/5/6): atom = identity axiom `C ∈ Γ`; §5 ∀-axiom `∀xF ∈ Γ`; §5 ¬-axiom `¬A ∈ Γ`
  (all `inAnt _ (seqAnt s)`).
- **I-rules** (tags 1/2): succedent permissibility `seqSucc s = ^∀ p` / `= inegF p` (the R-symbol
  Buchholz condition).
Cascade touched: `ZPhi`, `zphi_monotone`, `zphi_strong_finite`, `zphi_iff`, `zblueprint` σ&π cores,
`zPhi_definable`, + all ~13 downstream `rcases zDerivation_iff` sites (GOTCHA: anonymous-constructor
`_` absorbs a trailing conjunct, but a *named* hyp binding the membership must be split, and the atom
`⟨s, h⟩` pattern needs `⟨s, h, _⟩` or `congrArg` breaks).

New lemmas (end of `InternalZ.lean`, all axiom-clean):
- `ZDerivesEmpty d := ZDerivation d ∧ seqAnt(fstIdx d)=∅ ∧ seqSucc(fstIdx d)=⊥`.
- `zTag_reducible_of_emptyAnt` (empty antecedent ⟹ tag∈{1,2,3,4}) and the sharper
  `zTag_Ind_or_K_of_ZDerivesEmpty` (⊥-succedent rules out I-rules ⟹ tag∈{3,4}).
- `fstIdx_iR2_of_tag_Ind_or_K` — `iR2` preserves the end-sequent on tags 3,4 (both reducts are
  `zK (fstIdx d) …`).
- `iord_descent_iR2_of_ZDerivesEmpty` — every `iR2`-step strictly lowers `iord`.
- `RedSound : Prop := ∀ d, ZDerivesEmpty d → ZDerivation (iR2 d)` — **the sole remaining obligation**.
- `ZDerivesEmpty_iR2` (preservation, modulo `RedSound`), `ZDerivesEmpty_iterate`, and
  **`iord_iR2_iterate_descends`** : under `RedSound`, `n ↦ iord(iR2^[n] z)` `≺`-descends at every step
  — the infinite ε₀-descent (V-internal analog of `wip/GentzenCon.gentzenDescent_descends`).

## ▶ NEXT — the two pieces between here and `Reduction.lean:68`
1. **`RedSound` (reduction-soundness) — THE deep residual.** Prove `ZDerivation (iR2 d)` for a
   `ZDerivesEmpty d` (tags 3,4). For tag 3 `iR2(zInd…) = zK s (irk p) (iIndReductSeq d0 d1 1)`; tag 4
   `iR2(zK…) = iCritReduct… = zK s (r-1) (iCritReductSeq …)`. Each must satisfy the `zK` disjunct:
   `Seq` (have: `iIndReductSeq_seq`/`iCritReductSeq_seq`) ∧ premises ∈ `ZDerivation` (d0,d1 are, via
   `zDerivation_zK_inv`/Ind inversion) ∧ **`zKValid s r' (reductSeq)`** ← the Buchholz reduction-lemma
   content (isChainInf + per-premise iperm/¬iperm + formula-hood for the produced chain). This is the
   genuine multi-lap nut. Decompose: premise-membership (tractable) vs `zKValid`-of-reduct (deep).
2. **C0.5 bridge + internalize the iteration.** `¬Con(M) → ∃ z:M, ZDerivesEmpty z` (Foundation ⊥-proof
   → Z ⊥-derivation, `InternalZ.lean:3913` docstring), then present `n ↦ iord(iR2^[n] z)` as the `𝚺₁`
   graph `gentzenDescentφ` and prove the per-model `gentzen_descent_of_inconsistent` (currently the
   disclosed axiom in `wip/GentzenCon.lean`). That + `gentzen_reduction_internalized` (already a
   theorem) discharges crux-2.

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean.

## Pointers
- `wip/GentzenCon.lean` — the abstract-axiom scaffold the InternalZ objects realize: `derivesEmpty`≈
  `ZDerivesEmpty`, `R`≈`iR2`, `ord`≈`iord`, `ord_R_descends`≈`iord_descent_iR2_of_ZDerivesEmpty` ✓,
  `R_preserves_empty`≈`ZDerivesEmpty_iR2`. Deep axiom = `gentzen_descent_of_inconsistent` (per-model).
- `E-CRUX2-DECOMPOSITION-2026-06-24.md` §5 (C0.5), §8 (leaf grind-list). `papers/buchholz-on-gentzens-*`.
- Reference corpus (read-only): `~/personal/claude/knowledge/core/projects/lean-journey/reference/`.

## Lap discipline
InternalZ is `src/` (green-gated, sorry-scanned). Commit each green build; end the turn after each so
the governor can pace.
