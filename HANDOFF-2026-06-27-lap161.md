# Handoff: lap 161 — DEEP REFLECTION (every-9th). Directive RE-SYNCED; {3,4}-producer core is CLOSED.

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `d4ce7e3` (docs-only lap, no src change) · build 🟢 green
(1326 jobs, `lake build GoodsteinPA`, exit 0). Real `#print axioms` (in-kernel, re-verified this lap):
headline `peano_not_proves_goodstein` = `[propext, sorryAx, choice, Quot.sound]` (**0 math axioms**, bare-sorry
headline); `peano_not_proves_consistency` + `goodsteinSentence_faithful` = `[propext, choice, Quot.sound]`
(clean); `false_of_ZDerivesEmpty` = `[propext, sorryAx, choice, Quot.sound]` (0 math axioms). No drift.

## This was the altitude lap. The synthesis IS the deliverable. Read these:
- **`DIRECTION.md` CURRENT DIRECTIVE (lap-161)** — binding, OUTRANKS this handoff. Read it before any proof work.
- **`REFLECTION-2026-06-27-lap161.md`** — the full direction call + reasoning.
- **`STATUS.md`** — refreshed (header, lap-161 blockquote, Where it stands, What's happened, Outstanding, ledger).

## The one thing that changed the picture
The lap-158 directive was **stale + internally contradictory**: its review half mandated an "outer
degree-induction design spike FIRST," but its OWN spike (`9ac1bf3`) **refuted** that hypothesis. **Laps 159-160
then WIRED + CLOSED the {3,4}-producer cut-reduction core in-kernel** — `repProducerClose` (`Crux2Blueprint:3455`)
`:= Or.inl (certReplace_of_premise_cert … (IH m …))`, non-sorried, **consumed in the live dispatch at `:3578-3581`**
(tag-3 zInd / tag-4 zK producers). Headroom is LOCAL to each flatten (premise's own `irk+1 ≤ idg(premise)`), NOT
the chain's degree ⟹ no outer degree-induction. **So "the irreducible heart of crux-2" (Buchholz Thm 2.1) is CRACKED.**

## The 6 live sorries (all in Crux2Blueprint), and how they collapse
The open `residual` (`:3451`) / `axMajorResidual` (`:3735`) now hold ONLY the **STRUCTURAL escape set**:
(i) **⊥-exit ex-falso** (`⊥∈Γ`/`^∀⊥∈Γ ⟹ Γ→C`, NO single Z-rule — needs an internal **⊥-elim/weakening** lemma,
the ONE new piece); (ii) **C-exit R-intro replay**; (iii) **tag-5/6 thread-escape** (shared — prove once).
- `genReduct_chain_noRedex_anySucc`/`residual` (`:3407`/`:3451`) — the lead domino.
- `ind_reduct_anySucc` (`:3382`), `genReduct_chain_hasRedex_anySucc` (`:3391`) — the other 2 anySucc leaves.
- `genReduct_chain_noRedex`/`axMajorResidual` (`:3683`/`:3735`) + `descent_step_K_noncrit_axMajor` (`:4180`) —
  **DROP from the port** (once the 3 anySucc leaves close, `genReduct_botSucc` thin-wraps `genReduct_anySucc`).
- `gDef exists_sigma1_descending_step` (`:4309`) — separable fifth (witness-bound / constructive reduct, NOT μ-min).

**One ex-falso/weakening lemma + the anySucc port collapses FOUR of the six.**

## ✅ DONE THIS LAP (grind delta, commit `78c3776`)
Isolated the ⊥-exit ex-falso out of the context-free `residual` into a NAMED sub-`sorry`
`exFalsoClose : inAnt (^⊥ : V) (seqAnt s) → GenReductCert (zK s r ds)` (after `leafCloseC`, ~`:3471`); rewired
the two ⊥-exit sites (tag-0, tag-7) to it. Build 🟢 1326, 0 math axioms. **KEY FINDING:** the ex-falso is a
DESIGN question — NO single Z-rule fires on bare `⊥∈Γ`, and naive structural induction on `C` FAILS at the atom
base case (atoms have no R-intro; this Z-system has NO ⊥-left rule). So `⊥∈Γ ⟹ Γ→C` is not admissible as-is.

## 🎯 NEXT — build `exFalsoClose` by ROUTE 4 (induction on `C = seqSucc s`); it's pure ASSEMBLY (infra all present)
Route 1 (vacuity) REFUTED (⊥ enters antecedents as a prior ⊥-exit's cut formula). Route 4 = induction on `C`,
which AVOIDS the heavy route-2 `zAxBot` datatype ripple. **The dominant cut-formula shape `^∀^k⊥` (lap-155) has
matrix `⊥`, not an atom ⟹ closes cleanly** as `zIall^k (zAtom)` (innermost premise `Γ+eigen → ⊥` is a leaf).
**All builders verified present** (`zReg_zIneg`/`zFresh_zIneg`/`zSeqAnt_zIneg` + `zIall` variants +
`fstIdx`/`idg`/`iotil`); cert template = `axNegCloseGen` (`Crux2Blueprint:3480`). Full per-case build plan +
infra inventory in PENDING_WORK lap-161. **Execution order:** (1) `inegF p` case (no recursion, cleanest:
`zIneg s p (zAtom …)`, `zInegWff` `InternalZ:1525`); (2) `^∀^k⊥` via the `zIall` recursion bottoming at `⊥`;
(3) atom-matrix case → show vacuous (`atom succedent ⟹ atom∈Γ ⟹ leafCloseC`) or narrow sub-`sorry`. All three →
`exFalsoClose` DROPS. Then (ii) C-exit R-intro replay + (iii) tag-5/6 thread-escape still in `residual` (:3451);
closing all three DROPS the leaf → thin-wraps `genReduct_botSucc` → DROPS `axMajorResidual` +
`descent_step_K_noncrit_axMajor`. (Route 2 `zAxBot` = fallback ONLY if the atom-matrix case is both non-vacuous
AND reachable.)

## ⚠️ FORBIDDEN (DIRECTION.md lap-161)
Re-spiking the {3,4} producer / outer degree-induction (DONE + refuted — do NOT rebuild); `red` witnesses;
`iord`-recursion for the construction (CODE-induction + local-degree flatten ONLY); `redLeast`/μ-min for gDef;
the refuted single-premise `seqUpdate` splice; attacking `descent_step_K_noncrit_axMajor`/gDef STANDALONE before
the port wires them; off-path dead red-soundness sorries {:82,:1257,:1367,:1563,:1653,:1765,:1868} AS STATED;
**M2/M3/M4 wiring**. ALTITUDE: `false_of_ZDerivesEmpty` sorry-free is NOT the headline —
`goodstein_implies_consistency` (`Reduction.lean:68`) is a bare sorry, NOT YET wired to it; M2/M4 embedding
bridge ~0% built. "Only the crux left" ≠ "almost done."

---
**→ Next session: build the internal ex-falso/weakening Z-lemma, wire it to close `residual` (`Crux2Blueprint:3451`).
Keep build green + headline 0 math axioms. Obey `DIRECTION.md` CURRENT DIRECTIVE (lap-161).**
