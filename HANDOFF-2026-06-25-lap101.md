# HANDOFF — lap 101 · DEEP REFLECTION · the sub-route fork is reopened (run the skipped spike)

> Branch `plan`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). This is a reflection lap — the
> deliverable is the synthesis, not a proof step. No `src/` code changed; build state unchanged from lap 100.
> **READ FIRST:** `REFLECTION-2026-06-25-lap101.md` (primary), then `NEXT_STEPS.md` (corrected priorities).

## Kernel state (re-verified in-kernel this lap, `lake env lean`)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — bare `sorry`, **0 math
  axioms**, anti-fraud intact.
- `peano_not_proves_consistency = [propext, choice, Quot.sound]` — clean.
- `not_proves_of_implies_consistency` — clean. `goodsteinSentence_faithful` — clean.
- Statement re-audited vs paper: `goodsteinSeq m (k+1) = bump (base k) (goodsteinSeq m k) - 1`, `base k =
  k+2`, `bump` recurses on base-`b` exponents = genuine hereditary base bump. **No transcription drift.**

## The call (why this lap matters)
Destination KEEP (`𝗣𝗔 ⊬ Goodstein`, axiom-free, Rathjen/Gentzen). crux-2 target KEEP (`redSound` =
internalized cut-elimination = the sole open math content). **Sub-route fork REOPENED.**

The trajectory that triggered it: the lap-92 reflection diagnosed the *finitary eigenvariable* presentation
as the wall-source and recommended the ω-rule pivot (Path C) **after a de-risk spike**. Lap-95 overruled to
Path X (finitary, surgical `iRK` gate) **without running the spike** — which was never written. Laps 95–100
ground Path X: real mechanical progress (the gate; the I∀/I¬/axAll non-Rep replace cases assembled) but the
wall **relocated** (eigensubst O2 → the `redZKReady` "hereditary all-Rep selected spine" motive), exactly
the conclusion-tracking the ω-rule makes free. And the motive's hard core is shaky: a ∅→⊥ chain's premises
have **growing** antecedents `{A₀..A_{i-1}}→Dᵢ`, so Cor 2.1 (selected-premise-is-Rep, first level) does NOT
directly reapply down the spine ⟹ "hereditary all-Rep" is a repo artifact (keeping `Π` + `tpReduce`) that
may not even hold as stated. This is the lap-92 pattern repeating.

## NEXT LAP START HERE — PRIORITY 1: the skipped de-risk spike
**Write `wip/InternalZomega.lean`** (self-contained, NOT imported by `GoodsteinPA.lean`). Full spec +
decision rule in `NEXT_STEPS.md`. In brief:
1. Internal ω-rule ∀-node `zAllω s x h` — premise-`n` := `zsubst h x (numeral n)` (Buchholz §6 `Z*`;
   reuse axiom-clean `zsubst`). Validity: `∀ n, ZDerivation (premise n) ∧ fstIdx (premise n) = Γ→F(n)`.
2. Critical-cut reduct SELECTS premise `t` = `premise t`. No new substitution in the reduction step.
3. Confirm by elaboration the reduct is a `ZDerivation` with NO appeal to eigenvariable-substitution
   validity (contrast the finitary O2 wall). Probe `iord (zAllω …)` assignability (the arithmetization risk).
- **Elaborates clean → PIVOT to Path C** (retires motive/axNeg/Ind/5.1/5.2.1/ordinal-K at once; math
  doubly-proven by Bryce–Goré Coq + the repo's own axiom-clean meta `Zinfty.lean`).
- **Walls on Σ₁ arithmetization → commit to Path X** (PRIORITY 2 below) with the evidence.
- Template to mirror: `src/GoodsteinPA/Zinfty.lean` (the meta ω-rule cut-elimination, axiom-clean).

## PRIORITY 2 (fallback — only if the spike walls)
Resume Path X: drive `redZKReady` (`Crux2Blueprint.redSoundGen` K-case `sorry`). First sub-lemma
`redZKReady_of_emptyAnt_botSucc`, but FIRST settle whether the hereditary-Rep invariant even holds (growing
antecedents). Then axNeg, `zKValidF_iIndReduct_of_zInd` (Ind), `ZDerivation_red_zK_crit` (5.1),
`ZDerivation_red_zK_splice` (5.2.1), `iord_descent_red` (ordinal K). See `PENDING_WORK.md` lap-100 box.

## PRIORITY 3 (path-independent, after the fork settles)
M2 `foundation_bot_to_Z_empty` (Bryce–Goré `Peano.v` bridge, ~1k lines) · M3 `false_of_ZDerivesEmpty`
(Σ₁-internalize descent + PRWO) · wire `Crux2Blueprint` → `Reduction.goodstein_implies_consistency` →
headline (only when `#print axioms` clean — anti-fraud).

## ALSO LANDED this lap — the spike's first concrete brick (`wip/InternalZomega.lean`, axiom-clean)
Started PRIORITY 1 in-kernel. Two lemmas, both `[propext, choice, Quot.sound]` (verified):
- **`zOmegaPrem_valid`** — the ω-rule premise family `t ↦ zsubst d0 a t` from a valid finitary I∀ node is
  UNIFORMLY a family of `ZDerivation`s, discharged PURELY by the construction-time freshness bound
  `maxEigen d0 < a` (= `ZRegular`, which `red` already maintains) — NO criticality / conclusion-tracking
  motive. The ∀-cut reduct = the SELECTION `zOmegaPrem d0 a t`; its validity is this lemma (a
  hypothesis-level fact about the well-formed ∀-node), NOT a cut-elimination obligation. **In-kernel
  evidence that the ω-rule view retires the `redZKReady` wall: the substitution-validity moves to
  node-construction, off the cut-elimination recursion.**
- **`zOmegaPrem_concl`** — the selected premise's conclusion is COMPUTED (`= Γ→F(t)`), not threaded through
  a motive. Generalizes `red_zIall_tpReduce` (its `t=0` instance) to the whole family.

**Probe 1 RESOLVED this lap (arithmetization-risk de-risk, both axiom-clean):**
- **`iord_zOmegaPrem`** — every ω-rule premise has the SAME ordinal `iord d0` (the eigensubst preserves
  `iord`, via the banked `iord_zsubst`).
- **`iord_zOmegaPrem_constant`** — the premise-family ordinal is constant in the index, so the feared
  `sup_t (iord (premise t))` over an INFINITE family collapses to `iord d0`; the ω-node's ordinal is the
  FINITE `iord d0 + 1`, assignable with the existing `iord`/ω-tower engine, NO new primitive.

**Capstone `zIall_realizes_omega` (axiom-clean):** a regular `zIall s a p d0` (`ZDerivation` + freshness
bound + O3 eigenvariable side-condition) realizes the FULL ω-rule ∀-node — for every closed `t`, premise
`zsubst d0 a t` is a `ZDerivation` of `Γ→F(t)` with ordinal `iord d0`. So a Path-C rebuild reuses the
existing I∀ embedding wholesale (PA's ∀-intro already produces such a node); the ω-node carries the SAME
finite data as `zIall` (premise-`t` computed on demand, never stored — so no `Fixpoint.StrongFinite` issue).

**NET CALL (updated by the spike): the evidence now runs IN FAVOUR of the Path-C pivot.** Both of the
ω-node's hard properties — premise validity (`zOmegaPrem_valid`, motive-free) and ordinal assignment
(`iord_zOmegaPrem_constant`, finite) — are in-kernel facts on the EXISTING engine, and they are exactly
the two obligations the finitary route turns into the open `redZKReady` motive + `iord_descent_red` K-case.
**Per-step ∀-cut invariant now in-kernel too (`iord_descent_zOmegaPrem`, axiom-clean):** selecting the
premise for witness `t` strictly drops the ordinal (`iord d0 ≺ iord (zIall …)`) UNIFORMLY in `t` — composing
banked `iord_descent_zIall` with `iord_zOmegaPrem`. With `zOmegaPrem_valid` this is the FULL cut-elimination
invariant for the ∀-cut (validity-preserving AND ordinal-decreasing), on the existing nodes.

The single remaining open piece (Probe 2) is ASSEMBLING the cut-elimination RECURSION (Schütte/Tait, on
`iord` + cut-rank), which needs the ω-node datatype (new tag in the `zconstruction` Fixpoint) +
`ZPhi`/`iord`/`tp` extension — the ~2–3k-line but TEMPLATED rebuild (`Zinfty.lean` is the meta template).
Every load-bearing sub-question is now answered in-kernel (validity, conclusion, ordinal, realizability,
per-step descent); what remains is engineering, not unsolved math. **NEXT LAP:** commit to the pivot —
start the `zAllω` datatype (tag 7, same data shape as `zIall`) + its `red`/`iord` recursion equations. See
`wip/InternalZomega.lean` footer + `NEXT_STEPS.md`.

## Aristotle
Idle — CORRECT. The residuals are entangled with the 8000-line `InternalZ`/`Zinfty` API, not self-contained
mathlib lemmas. (The spike is too entangled for Aristotle too.)

## This lap's commits
(1) Docs synthesis (`REFLECTION-2026-06-25-lap101.md` + `NEXT_STEPS.md` + this baton + STATUS/PENDING_WORK
refresh). (2) `wip/InternalZomega.lean` — the de-risk spike's first axiom-clean brick. No `src/` change;
`lake build GoodsteinPA` green 1325, headline footprint unchanged.
