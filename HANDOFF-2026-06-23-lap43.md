# HANDOFF — 2026-06-23 (lap 42 close, **`IterPrefix_lxDef` DISCHARGED — descent exists unconditionally**)

> **Branch** `plan` · HEAD `16552e3` · build **green** (`lake build GoodsteinPA`, **1308 jobs**) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, **0 math axioms**).
> **Tree clean**. New lemmas `#print axioms`-clean (`[propext, choice, Quot.sound]` + the accepted
> F-φ `native_decide` artifact). Girder `peano_not_proves_TI` re-verified **unchanged**.

## What lap 42 did (1 commit)
The lap-41 handoff named `IterPrefix_lxDef` the "lone wall … genuine multi-lap infra." **It was not.**
The membership-form trick (`DescentConstruction.isDescent_iff_mem`: render the descent so the `X`-atom
sits on a *bound* variable, not a `znth`-function-term) that lap 35 used for the `Mlt`-descent applies
verbatim to lap-41's `descentR` route. So:
- **`minClause_lxDef`** (`DescentConstruction.lean`) — the ONE new clause: the `descentR` minimality
  `∀ z<x', ¬(Mlt f z x ∧ ¬MX z)` in membership form, via Foundation `Semiformula.ballLT` + `eval_ballLT`.
- **`IterPrefix_lxDef`** — `IterPrefix`'s four clauses (`skel`/`descentMlt`/`minClause`/`xclause`) are
  binary-`LX`-definable; conjoin (`lxDef2_and`) + `∃`-close (`lxDef_exists`); membership↔`znth` via
  `Seq.znth`/`znth_eq_of_mem`. The `descentR`'s `¬MX y` part folds into the global `xclause`
  (`i<k ⟹ i+1≤k`). Discharges the `hPdef` hypothesis of `descent_iterate_seq_exists`.
- **`descent_iterate_seq_total : ∀ k:M, IterPrefix hM f a₀ k`** — **UNCONDITIONAL**. The canonical
  `Mlt`-descent prefix from `a₀` exists at every length `k:M`, hypothesis-free.

## ⚠️ FRESH-MIND COURSE-CORRECTION — the real crux is the SLOW-DOWN (read this before grinding)
The lap-41 decomposition (its piece 1) said the descent `α` comes "with `iC(α k)≤K(k+1)`". **Wrong.**
`descentR` picks the `<`-least `¬MX` code below `αₖ`; its coefficient `C` is **uncontrolled**. Rathjen
2014 (`papers/rathjen-2014…pdf`, p.11–12) gets the bound ONLY via:
- **Lemma 3.3 / Cor 3.4 — the Grzegorczyk `g`-padding**: pad an arbitrary ε₀-descent into a *slow* one
  (`|αᵢ|≤K(i+1)`). `g(n,m)>g(n,m+1)` for `m<f(n)`, `|g(n,m)|≤K(n+m+1)`; Lemma 3.2 = mathlib
  `exists_lt_ack_of_nat_primrec`. **NOT STARTED — this is the genuine remaining wall.**
- **Thm 3.5 — reindex** `β_{K(n+1)+i}=ω·αₙ+(K-i)` ⟹ `C(βᵣ)≤r+1`. The lap-41 `InternalONote` toolkit
  (`iC_iomul`/`iC_iadd_finite`/`icmp_betaTail_within`/`_boundary`/`isNF_iadd_finite`) is exactly this
  stage's code arithmetic — but it is **downstream of Cor 3.4**, not a substitute for it.

Also: `no_min_descent_absurd_of_goodstein`'s `hbound` `sorry` (`DescentSemantic.lean:569`) still asks
for a `𝚺₁-Function₁ b`, which is **unachievable** (`b` is `X`-dependent). When β is built, **refactor
`hCD` through lap-41's `nonterminating_of_xDescent`** (the `lx_nonterminating`/`X`-essential path) and
delete the dead `𝚺₁` `hbound` + `DescentArith.nonterminating_internal` call.

## Done since this baton was written (lap 42, 2nd commit `747fe5d`)
**Piece 2 (extract `α`) is DONE.** `descent_alpha_exists` (`DescentConstruction.lean`): `α : M → M` with
`α 0=a₀`, `∀k ¬MX(α k)`, `∀k descentR f (α k)(α(k+1))`, via `IterPrefix_agree` (prefix agreement by
X-free `sigma1_succ_induction` + `descentR_functional`). Axiom-clean, green 1308. The descent is now a
usable meta-function. **`α` is NOT yet known slow** (`iC(αₖ)≤K(k+1)`) — that bound is piece 1.

## NEXT (hardest-first) — see `PENDING_WORK.md` lap-42 for the plan
1. **(CRUX) Rathjen Cor 3.4 slow-down**, internalized on `M`-codes (multi-lap). Decompose: (a) an
   ℕ-template `g : ℕ²→ONote` with its descent + `|g|≤K(n+m+1)` bound — **self-contained, Aristotle-
   eligible**; (b) internalize as `M`-code recursion. Lemma 3.2 = `Mathlib.Computability.Ackermann`.
3. **(plumbing) `Mlt`→`icmp` decode** (the route-b seam): `prec`(=`Mlt`, X-free) ⟺ `icmp · = 0` on the
   ε₀-code reading; `isNF (α k)`.
4. **(toolkit ready) Thm 3.5 reindex** `α(slow)→β` — `iCanon(r+1)`, `icmp`-descent, `isNF`.
5. **`hPdef'` + close** — LX-def of `ievalNat(k+1)(βₖ)≤igoodstein m₀ k` (`lxDef_of_reduct`) ⟹
   `nonterminating_of_xDescent` ⟹ `hCD` ⟹ `hbound`. ANTI-FRAUD: `#print axioms peano_not_proves_goodstein`
   MUST be `[propext,choice,Quot.sound]` (+ only if genuinely chained) — else leave the `sorry`.

## Aristotle
Idle. Best fit now = the **ℕ-template `g` (piece 1a)**: a self-contained `ℕ²→ONote` Grzegorczyk-padding
construction with its strict-descent + coefficient-bound lemmas (no `M`, no `X`). If you bound a clean
sub-lemma, feed it.

## LOCKED untouched (anti-fraud): `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline `sorry`.
## src/ sorries: `Statement.lean:22` (headline, locked), `Reduction.lean:52` (off-path), the `hbound`
`sorry` in `DescentSemantic.no_min_descent_absurd_of_goodstein` (the lone genuine wall).
`DescentConstruction` is sorry-free (incl. the new `IterPrefix_lxDef`/`descent_iterate_seq_total`).
