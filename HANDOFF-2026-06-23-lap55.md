# HANDOFF — 2026-06-23 (lap 55) — crux-1 frontier collapsed to TWO clean inputs; iF growth bricks; model-theoretic route mapped

> **Branch** `plan` · **HEAD `c9ca087`** · tree clean · `lake build GoodsteinPA` **green (1315 jobs)** ·
> headline `peano_not_proves_goodstein` = honest `sorry` (`#print axioms` = trust base only, anti-fraud
> intact). **6 commits** `d23a60b..c9ca087`. All new lemmas axiom-clean `[propext, choice, Quot.sound]`,
> sorry-free. `wip/StdCor34.lean` checked via `lake env lean wip/StdCor34.lean` (green).

## What this lap did — closed crux-1's `hdef`, reduced `hdom`, built the Lemma-3.2 iF substrate

Crux 1 = `crux1_internal_run` (lap 54) modulo THREE named hyps (`hdef`/`hdom`/input `β`). This lap
**discharged `hdef`** and **reduced `hdom`** to a clean width-domination, collapsing the frontier to
two inputs.

### (1) `hdef` — definability — FULLY DISCHARGED
- `src/GoodsteinPA/InternalIg.lean`: `ig0_definable` (ite-comp), **`ig_definable`** (meta-induction on
  the standard level `l`, proved at `𝚺₁` so the IH + sub-args live on the `𝚺` side `…comp` wants),
  `igtTot_definable`. (`d23a60b`)
- `wip/StdCor34.lean`: `bbtail_definable`, `bbeta_definable`, `salpha_definable` — the whole
  `bbeta∘salpha` tower is a fixed `𝚺₁`-definable composition of code arithmetic. **`hdef_of_beta_definable`**
  discharges `crux1_internal_run`'s `hdef` from a single `𝚺₁-Function₁ β` premise;
  **`crux1_internal_run_of_beta_def`** is the end-to-end girder modulo (definable NF descent β) + `hdom`.
  (`ce74007`)

### (2) `hdom` — domination — reduced to WIDTH-domination
- `src/GoodsteinPA/BlkRec.lean`: `off_succ_lt_width_of_blk_eq` (within a block, `off j + 1 < width`).
- `wip/StdCor34.lean`: `hdom_of_width_dom` + **`crux1_internal_run_of_width_dom`** — the domination
  premise is now exactly `∀ n, znth wseq n ≤ iF l₀ n` (concrete instance of Rathjen 2014 Lemma 3.2).
  (`325d4f1`)

### (3) iF growth bricks (Lemma 3.2 substrate) — `src/GoodsteinPA/IIter.lean`
- `self_le_iIter`/`self_le_iF`: inflationary `n ≤ iF l n`. (`47b66a1`)
- `le_iIter_add` (iterate monotone in count), `iF_le_succ_level`, `iF_mono_level` (`l ≤ l' ⟹ iF l n ≤
  iF l' n`, n≥1), `iF_one` (`iF 1 n = n + n`). (`c9ca087`)

## NEXT (hardest-first) — the model-theoretic route for `goodstein_implies_prwo`
**Crux-1 frontier = exactly two inputs to `crux1_internal_run_of_width_dom`:** (a) definable ≺-descending
NF `β`, (b) width-domination `∀ n, znth wseq n ≤ iF l₀ n`.

**KEY ROUTE (see PENDING_WORK ⭐ lap 55):** `LO.FirstOrder.complete_iff : T ⊨ φ ↔ T ⊢ φ`
(`Foundation/.../Completeness/Completeness.lean:69`) makes `goodstein_implies_prwo` provable **per model**,
with NO ord/R arithmetization. `𝗣𝗔 ⊢ prwoInstance seq` ⟺ holds in every `V ⊧ 𝗣𝗔`; given `𝗣𝗔 ⊢ γ`,
each such `V ⊧ γ`; a `V`-internal `seq`-descent ⟹ build `β` + width-dom ⟹ `crux1_internal_run_of_width_dom`
⟹ `V ⊭ γ`, contradiction. The deep content concentrates in ONE per-model obligation:
*internal `seq`-descent ⟹ (NF `β` + standard-`l₀` width-domination)*.
- **NEXT LAP:** set up the `complete_iff` skeleton in `wip/GentzenCon.lean`, isolating that single
  obligation as the lone sorry (verify the `consequence`/soundness API names + the `𝗣𝗔`-model coercion).
- Width-domination is where **primrec-only** bites (`Grz.F_diag_not_dominated`): only the concrete
  `seq = gentzenDescentφ` instance is standard-`l₀` dominated (`[[crux1-headline-needs-only-standard-level]]`).
  Thread a standard-`l₀` domination certificate through `goodstein_implies_prwo`, or specialize it.

## Hardest wall (unchanged) — crux 2 eq (5) `ord(R d) ≺ ord d` (🟠 generational, cited)
`wip/GentzenCon.lean` isolates it to the single `ord_R_descends` axiom. Not the active thread.
Direction KEEP (drive crux 1).

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/StdCor34.lean`/`wip/GentzenCon.lean` are wip (crux-1 assembly over named hyps + crux-2 scaffold).
Aristotle OFFLINE. WebFetch dead.
