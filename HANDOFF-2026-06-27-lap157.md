# Handoff: lap 157 — `climb_to_rep_producer` PROVEN (axiom-clean); tag-5 zAxAll producer collapses

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `34ff2b3` · build 🟢 green (1326 jobs,
`lake build GoodsteinPA`). In-kernel: headline `GoodsteinPA.peano_not_proves_goodstein` =
`[propext, sorryAx, choice, Quot.sound]` (**0 math axioms**); `false_of_ZDerivesEmpty` =
`[propext, sorryAx, choice, Quot.sound]`. The new lemma `climb_to_rep_producer` =
`[propext, choice, Quot.sound]` (**sorry-FREE, axiom-clean**). No drift. No uncommitted edits.

## ✅ DONE THIS LAP — the §14.254b WEDGE (the handoff's attack #1), PROVEN + wired
1. **`climb_to_rep_producer` (`InternalZ:~9166`, axiom-clean, commit `34ff2b3`).** A left-axiom major
   (`tp = isymLk kk F`, `F` a `^∀`-formula, `F ∈ chainAnt ds jstar`) has its cut formula `F` ALWAYS produced
   by a `zInd`/`zK` (tag 3/4) or `zAxNeg` (tag 6) node, OR threads to a `^∀`-formula in `Γ`. Proof: a local
   `producerStep` threads the active formula of a left-axiom node → escape (`^∀ … ∈ Γ`) or NON-LEAF producer
   (`leastSucc_in_ant_or_nonleaf`); an R-intro producer is killed by `hnolow` (`rightSym_producer_redex`).
   The **`zAxAll` (tag-5) producer is NOT a dead end**: its active `^∀ p'` threads one rank UP, so a
   `least_number` search over "`x ≤ j0 ∧ tag ∈ {3,4,5,6} ∧ chainAsucc ds x` is a `^∀`-formula" gives a LEAST
   producer that cannot be `zAxAll` (else a strictly-earlier `^∀`-producer exists, contradicting minimality).
2. **Wired into `genReduct_chain_noRedex`.** Factored `closeZAxNeg` (the reusable lap-156 zAxNeg-thread-and-
   close); `tryProducerClose`'s tag-5 (zAxAll) case now CLIMBS → `closeZAxNeg` on a `zAxNeg` landing (CLOSES),
   `axMajorResidual` on {3,4}/escape. **The tag-5 producer is GONE from the residual** (strict superset of
   cases closed vs lap-156).

## 🔻 The open M1b-term residual: `axMajorResidual` (`Crux2Blueprint:~3417`) NARROWED to {zInd(3), zK(4)}
Now covers ONLY: a `zInd`/`zK` producer of a `^∀^k⊥` (k≥1, a NON-⊥ succedent), plus the general
`^∀^k⊥ ∈ Γ` escape. (The `descent_step_K_noncrit_axMajor` sorry `:~3826` and `exists_sigma1_descending_step`
`:~3977` are the other two live M1b-term sorries; see PENDING_WORK.)

## ⛔ REFUTED THIS LAP (kernel-grounded) — local õ-drop is the WRONG engine for the {3,4} producer
Reducing a producer of `^∀^k⊥` by a same-end-sequent `certReplace` or a 2-cut `certFlatten` needs
`irk(cutFormula)+1 ≤ idg(zK s r ds)`, which is **NOT derivable**: `isChainInf` records only
`irk(chainAsucc ds i) ≤ r` (`InternalZ:1182`); `idg_zK = max r (iseqMaxIdg ds − 1) ≥ r`. The cut formula's
rank can be `= r = idg` (no headroom; e.g. `r=1`, `idg=1`, `irk(^∀⊥)=1`). **Degree-trade** (lap-150/151
echo): the {3,4} producer reduction must drop the DEGREE (`iord`, eliminating the `^∀^k⊥` cut lowers max cut
rank), NOT `õ` (`iRedDescent`). `genReduct_botSucc` is `⊥`-succedent-ONLY (`hsucc : seqSucc (fstIdx d) = ⊥`),
so it CANNOT reduce a producer deriving `Γ_m → ^∀^k⊥`. Also refuted: degenerate `zInd → d0`
(`idg_zInd = max(max(idg d0 − 1, …), irk p)` can RAISE the degree).

## 🎯 NEXT (priority order — full tree in `PENDING_WORK.md` lap-157 top)
1. **Wire `climb_to_rep_producer` at `descent_step_K_noncrit_axMajor` (Γ=∅).** Escape is VACUOUS at
   `seqAnt s = ∅` → tag-5 major collapses to a {3,4,6}-producer cleanly; {6} closes; {3,4} is the sharp
   residual. DECOMPOSES the monolithic `:~3826` sorry into "reduce a {3,4} producer of `^∀^k⊥`".
2. **Generalize `genReduct_botSucc`/`genReduct_chain_noRedex`/`GenReductCert` OFF `seqSucc = ⊥` to general
   succedent `C`** — the principled Buchholz route, the ONLY way to reduce the {3,4} producer (`Γ_m → ^∀^k⊥`).
   Reuse the `iRKcCrit`/`genReduct_chain_hasRedex` `iord`-DEGREE-drop engine (per the refutation), NOT
   `iRedDescent`. Big refactor; strict-headroom available (eliminate highest-rank cuts first).
3. **CHECK** the general `^∀^k⊥ ∈ Γ` escape via a k-fold instantiation-cut chain (degree `≤ r ≤ idg`); at Γ=∅
   it's vacuous so #1 dodges it.

## ⚠️ Forbidden / do-NOT-undo (per DIRECTION lap-155, still binding)
`red` witnesses; `iord`-recursion for CONSTRUCTION (CODE induction via `zDerivation_sigma_induction` ONLY);
`redLeast`/μ-min for gDef; the refuted single-premise `seqUpdate` splice; the refuted local õ-drop for {3,4}
producers (THIS lap); attacking `descent_step_K_noncrit_axMajor`/gDef STANDALONE without the climb; the
off-path dead `red`-soundness sorries AS STATED; M2/M4 wiring. ALTITUDE: M2 (Foundation→Z bridge) ~0% built.

## 📁 Key anchors (this lap)
- `climb_to_rep_producer` (`InternalZ:~9166`, `set_option maxHeartbeats 1600000`), local `producerStep`.
- `closeZAxNeg` (`Crux2Blueprint:~3462`, factored), `tryProducerClose` tag-5 climb (`~:3500`).
- The kit it reuses: `leastSucc_in_ant_or_nonleaf` (`InternalZ:9076`), `rightSym_producer_redex` (`:9151`),
  `pi2_tp_eq_seqSucc_of_pi1_zero` (`:9127`), `zDerivation_zAxAll_inv` (`:8488`), `tp_zAxAll` (`:701`).
- `DIRECTION.md` (lap-155, binding) · `PENDING_WORK.md` lap-157 top · prior baton `HANDOFF-2026-06-27-lap156.md`.

---
**→ Next session: wire the climb at `descent_step_K_noncrit_axMajor` (Γ=∅, escape vacuous → clean tag-5
collapse, decomposes the `:3826` sorry), then start the general-succedent generalization (the {3,4}-producer
reduction, DEGREE-drop). Keep build green + headline 0 math axioms.**
