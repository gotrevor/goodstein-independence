# HANDOFF — lap 65 · DRIVE TO COMPLETION + FARM FROM THE A/B ARMS

> **This baton supersedes all prior HANDOFF-*.md.** Read it first, then `STATUS.md` + `PENDING_WORK.md`
> + `E-CRUX2-DECOMPOSITION-2026-06-24.md` (§8 = your grind-list). Written by the judge (Ren) after a
> verified post-mortem of the overnight A/B effort experiment. Operator: Trevor.

## 🎯 THE MAIN OBJECTIVE (the only thing that counts)
Discharge the headline `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`),
**axiom-free (trust base `[propext, Classical.choice, Quot.sound]` only) or ABANDON.** No
Gentzen-as-axiom, no PRWO-as-axiom on the headline (that cop-out is forbidden and has been pitched +
rejected twice). `PA_delta1Definable` (Foundation axiom surfaced by Gödel II) must ALSO be discharged.

The route is settled (Rathjen Cor 3.7): `PA⊢γ →(§3) PA⊢PRWO(ε₀) →(Gentzen Thm 2.8) PA⊢Con(PA)` → Gödel II.
- **crux-1 (γ→PRWO): DONE + axiom-clean (lap 57).**
- **crux-2 (PRWO→Con): the only math wall left.** It is FULLY DECOMPOSED in `E-CRUX2-DECOMPOSITION-2026-06-24.md`.

The 3 `src/` sorries are the whole job: `Statement.lean:22` (headline), `Reduction.lean:68`
(`goodstein_implies_consistency` = crux-2's conclusion, the girder), `DescentSemantic.lean:582` (the
slowed code-descent β feeding crux-1). The 7 `wip/` sorries (Bounding/Embedding/WitnessBound/Zinfty)
are in files **`src/` does not import** — they are OFF the headline path; do not chase them.

## ⚠️ POST-MORTEM: why the overnight A/B made ZERO progress on the objective (judge-verified)
Both arms ran 8 laps. **Neither closed a single sorry** (src 3 / wip 7, byte-identical to the fork
`2beac51`). The cause is structural, and you must NOT repeat it:
1. **Both arms poured 100% of their output into ONE file, `wip/InternalZ.lean`** (med +2567 lines / 94
   commits; xhigh +2401 / 49). Zero `src/` edits.
2. **`wip/InternalZ.lean` is NOT a lake target and is imported by NOTHING.** The lakefile builds only
   `GoodsteinPA` (`srcDir=src`). So `lake build` — and therefore the pre-commit hook AND the treadmill
   green-gate — **never compiled it.** Every "axiom-clean wip" commit claim was UNVERIFIED by the repo's
   own gates. (The judge hand-verified it DOES compile and the named capstones ARE axiom-clean — but you
   have been flying blind, and the metric is dead until this is fixed.)
3. The crux-2 island was **never wired toward `Reduction.lean:68`.** The box optimized "add an
   axiom-clean lemma to the island" (always green, always succeeds) over "wire a hole" (hard, risks a
   red lap). That is the failure mode. **A lap that only adds lemmas to a non-built, un-imported file
   with no wiring is a NON-LAP.** Every lap must move toward compiling the crux-2 work under the
   green-gate OR shrinking distance to discharging a `src/` hole.

## PRIORITY 0 — make the crux-2 work actually compile under the green-gate (do this FIRST)
Add `wip/` as a compiled lake target so `lake build` checks `InternalZ.lean` every lap. Either add a
second `[[lean_lib]]` (e.g. `name = "GoodsteinWip"`, `srcDir = "wip"`) and put it in `defaultTargets`,
or import the crux-2 root into the `GoodsteinPA` library. Until this lands, "axiom-clean" is a claim,
not a check, and you can silently regress.

## PRIORITY 1 — FARM the proven crux-2 machinery from the two A/B arms (don't reinvent it)
Two CoW clones forked from `2beac51` each built ~2400 lines of crux-2 machinery in `wip/InternalZ.lean`.
**Judge-verified: both compile (`lake env lean`), both capstones are axiom-clean**
(`[propext, Classical.choice, Quot.sound]`). They are two INDEPENDENT extensions of the same base, so
reconcile/dedup — do not blind-merge. Take **xhigh's recursive reduction as the spine**, graft **med's
atomic-case leaves**. Both arms are READ-ONLY reference; harvest lemmas + proofs into THIS repo's
`wip/InternalZ.lean`.

- **`~/src/goodstein-ab-xhigh`** (HEAD `5aeb097`) — **DEPTH / the architectural keystone**: the
  *recursive* `iR2` total Σ₁ course-of-values reduction: `iR2`, `iRTable`, `znth_iRTable_eq_iR2`,
  `iR2_eq_iRNext`, `iR2_zK`, `iR2_zK_eq_iRcrit`, `iord_descent_iR2_struct`,
  `iRedDescent_iR2_of_tp_isymR` (axiom-clean ✓), plus Σ₁-definability of the reduct objects and the
  Thm-4.2 `iRedDescent` structural-induction interface (`iRedDescent`, `iord_descent_of_iRedDescent`,
  `iRedDescent_zIall/zIneg/zInd`, `iord_descent_iCritAux_of_iRedDescent`).
- **`~/src/goodstein-ab-med`** (HEAD `312ed88`) — **BREADTH / the atomic critical case, substitution-free**:
  `ZDerivation_critical_iR_atomic_constructed`, `ZDerivesEmpty_descends_critical_atomic` (axiom-clean ✓),
  `iord_descent_iCritReduct_of_ZDerivation` (axiom-clean ✓), the `zAx1`/`zAxInst` atomic reduct codes,
  faithful §5 atomic õ (`iotil_zAxInst = 2·rk(C)−1`), the N1 leaves (`N1_zIall/N1_zIneg`, `N1_zAxR`,
  `N1_zAxInst`), and the substitution-free critical nut chain.

Diff each arm against the base to see exactly what to lift:
`git -C ~/src/goodstein-ab-xhigh log --oneline 2beac51..HEAD` (and `-ab-med`).

## PRIORITY 2 — the genuinely-new remaining content (§8 of the decomposition; only 4 small leaves)
Per `E-CRUX2-DECOMPOSITION-2026-06-24.md` §8, the entire hard part reduces to four new leaves + F1/F2
(in flight) + one tower lemma. Most of these the arms already started — finish + assemble them:
1. **L3.1 redex finder (L1–L4)** — the Σ₁ search producing the least redex pair `(i,j,k)` from a chain
   code (finite `zTag` combinatorics, no ordinals). xhigh has `redex finder as a DEFINABLE FUNCTION`.
2. **T2/T3** — the one reusable "replace a premise of a valid `Kʳ`-chain by a same-endsequent reduct ⟹
   still a valid `Kʳ`-chain" lemma.
3. **T4(a)** — rank-substitution-invariance `rk(F(t)) = rk(F)` (one small structural lemma); plus T4(c)
   chain-rank invariant `rk(cut formula) ≤ r` if ranks aren't already carried by `ZDerivation`.
4. **the `d{0}/d{1}` object construction** = the `iR` critical branch (`d{0}=Kʳ(i/dᵢ[k])`,
   `d{1}=Kʳ(j/dⱼ[0])`, `d[0]=Kʳ⁻¹ d{0} d{1}` verbatim — VALIDATE the construction matches Buchholz 3.2
   case 5.1; a different reduct changes N2–N3).
- F1 (`icmp_insTerm_congr`/left-cancel summand swap), F2 (`ω^α0 # ω^α1 ≺ ω^α`), and the tower lemma
  N4(iii) (`icmp_iotower_lt_succ_of_le` — VALIDATE it delivers height-monotonicity at fixed base).
Then assemble the **UNCONDITIONAL** Thm 4.2 structural induction → `ZDerivesEmpty d → False` (no
empty-sequent Z-derivation) → `PRWO(ε₀) → Con(PA)`. The arms' nuts are CONDITIONAL (descent *given* the
redex data + IH); turning them unconditional via L3.1 + the induction is the real remaining math.

## PRIORITY 3 — WIRE into the headline (the step both arms never attempted)
1. `C0.5` Foundation→Z bridge: a Foundation ⊥-proof → a Buchholz-Z ⊥-derivation (`foundation_bot_to_Z_empty`
   in the arms; Z has a native `Ind` rule so PA-induction maps directly — blueprint Bryce-Goré `Peano.v`,
   do NOT port their infinitary `cut_elim.v`).
2. Chain crux-1 (γ→PRWO, done) + crux-2 (PRWO→Con) → **discharge `Reduction.lean:68`**
   `goodstein_implies_consistency`. Then the headline falls out of `not_proves_of_implies_consistency`.
3. Discharge `DescentSemantic.lean:582` (the slowed code-descent β, Rathjen §3 Cor 3.4 + Thm 3.5
   internalized) and `PA_delta1Definable`.
4. Gate of done: `src/` sorry-clean AND `#print axioms peano_not_proves_goodstein` shows only the trust
   base. ANTI-FRAUD: do not discharge `Reduction.lean:68` until `#print axioms` is clean.

## Pointers
- `E-CRUX2-DECOMPOSITION-2026-06-24.md` — §8 leaf grind-list (the prize). `E-EQ5-ROUTE-FINDING-2026-06-23.md`
  — eq-5 faithfulness + the bridge seam + feasibility. `STATUS.md` / `PENDING_WORK.md` — box state. `HARVEST.md` — spin-offs.
- `papers/` Buchholz [6] (crux-2 source: §3 reduction, §4 assignment, Lemma 4.1/Thm 4.2 = eq 5), Rathjen
  2014, Kirby-Paris; Bryce-Goré Coq in `scratchpad/Gentzen-bg/` (feasibility precedent, arXiv:2603.00487).
- Buchholz §4: `o(d)=ω_{dg(d)}(õ(d))`; box's `iord`/`idg`/`iõ` = this (judge-verified faithful).

## Lap discipline (the anti-island rule)
Each lap, before adding ANY lemma, ask: "does this move the crux-2 work toward compiling under the
green-gate, or toward discharging a `src/` hole?" If neither, it's the wrong lap. Refresh `STATUS.md`
with a real `#print axioms` of the headline + `Reduction.lean:68` each review lap. Aristotle is available
for bounded well-posed leaves (the §8 finite combinatorics + F1/F2 are good candidates).
