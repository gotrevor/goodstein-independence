# HANDOFF — 2026-07-02, lap 175 (REBUILD-Z lap 1 — Scope-A: seed + f-slot statements + pin 1)

**Verdict: PASS — Scope-A complete, T-R(i) does NOT fire.**  Read
`REBUILD-Z-LAP1-VERDICT.md` first (the binding record + the judge gate for laps 2+).
Repo green (`lake build`, 1333 jobs); `src/GoodsteinPA/OperatorZeh.lean` carries exactly
3 `sorry`s (the §5 statement pins), no new `axiom`s.

## What happened (Scope-A A1 + A2 + A3)

- **A1 — verbatim seed.**  `src/GoodsteinPA/OperatorZeh.lean` created from
  `wip/SpikeZ1Seams.lean` §0–§2 + `mono_H` + `ZehProv` + §5 read-off, namespace
  `SpikeZ1 → OperatorZeh` (LOCK §1).  Wired into `GoodsteinPABlueprint` build tree.
- **A3 — the inversion suite DISCHARGED (Z1 pin 1 + companions), all axiom-clean.**
  `allInv_Zeh` (§4): was a statement pin; now a real six-case induction mirroring
  `Zekd.allInv`, the numeric `max k/d`-inert bookkeeping re-keyed to the stage axis
  (`max m n₀`) and relativization axis (`adjoin H n₀`); new bookkeeping =
  `adjoin_swap`/`adjoin_idem`/`adjoin_base_mono` (operator-side analogs of `Zekd`'s
  max-reshuffle).  The three companion inversions `orInv_Zeh`/`andInvL_Zeh`/`andInvR_Zeh`
  (§7): keep the same `(α,e,H,m,c)` (no re-key); the minimal core has no `andI`/`orI`
  intro, so ∧/∨ is never principal and every case threads (strictly shorter than `Zekd`'s).
  All four `#print axioms` = `[propext, choice, Quot.sound]` — no `sorryAx`.
- **A2 — f-slot statements + seam re-checks.**  §5 drafts the E–W function-slot suite
  (`cutReduceAllAuxRunning_Zf`, `stepAllω_Zf`, `cutElimPass_Zf`, bodies `sorry`) with the
  `NormControlled` carrier (`hardy e` at root, `rel1` at ω-nodes, `f∘g` at cuts).  §6
  re-runs both Z1 seams in the f-form as REAL proofs: `probe_allomega_reassembly_Zf`
  (seam 2, sorry-FREE) and `probe_cut_all_arm_Zf` (seam 1, real modulo the reduction pin
  only, consuming the now-proven `allInv_Zeh`).  Both compose ⟹ T-R(i) does not fire.

## Permitted-sibling infrastructure built ahead this lap (NOT judge-gated, all axiom-clean)

While the reduction crux is gated, built the assembly plumbing so the discharge lap is pure
assembly (per `how-to-get-unblocked`): `Zeh.mono_c` (rank monotonicity, §8);
`NormControlled.mono`/`.stage_antitone` + `rel1_mono` (slot/stage domination);
`osucc_add_NF`/`osucc_add_lt_osucc_add`/`add_lt_osucc_add`/`osucc_add_mem` (the §19.6
ordinal-splice descent bricks the `osucc (α+γ)` output cites).  Next permitted sibling if
resumed pre-ratification: the `ZehProv`-level splice combinator's *ordinal* bookkeeping is
now complete — remaining un-built work is either the gated reduction body itself or the P1
hardy-domination-under-raise facts (which should NOT be pre-built until the judge ratifies
the §5 statement shape, since judge-questions #1–#3 may revise it).

## Next (laps 2+, JUDGE-GATED behind `REBUILD-Z-LAP1-VERDICT.md`)

1. **Lap 2–4 — discharge the reduction pins.**  `cutReduceAllAuxRunning_Zf` first (port
   the W4B running-family reduction; `allInv_Zeh` is now a real tool for the arm).  Expect
   the P1 hardy-domination-under-raise question at the `NormControlled (f∘g) (raise e α) m`
   conjunct — if the threading is kernel-refuted, switch to the BW87 cut-free read-off
   fallback (`readoff_sigma1`, proven), do NOT grind against it.
2. **Lap 5–7 — cut-elimination assembly.**  `stepAllω_Zf` then `cutElimPass_Zf`
   (rank `c+1 → c` at towered control, f-slot iterated).
3. **Lap 8–10 — Δ₀ read-off extension** (LOCK P2/J3, Towsner-5.4 pattern).

Three judge questions raised in the verdict §"For the judge": (1) the P1 conjunct's locus,
(2) `cutElimPass_Zf`'s existential iterated slot, (3) whether `stepAllω_Zf` should split ∀/∃.

## Rails (LOCK §2) — all held

R1 (no numeric via membership), R2 (∃ at root only), R3 (`e` constant; control at statement
level, no `mono_e`), R4 (budgets function-valued), R5 (no axioms; 3 disclosed pins).  Work
stayed in `src/GoodsteinPA/OperatorZeh.lean`; `wip/SpikeW4*/SpikeZ1Seams.lean` untouched
(read-only evidence).  The RUN is cap-bounded (Scope-A exhausted → baton + end lap; the
governor's `src`-sorry-clean self-stop cannot pass while Route-A material is banked).
