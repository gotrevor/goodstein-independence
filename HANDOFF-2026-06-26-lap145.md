# Handoff: lap 145 — `descent_step_Ind` cracked open: `k=⟦t⟧` blocker DISSOLVED, descent PROVEN, real blocker = `zIndWff` antecedent gap

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms). Two NEW banked lemmas, both `#print axioms = [propext, Classical.choice, Quot.sound]`.

## What this lap did to the hardest open leaf `descent_step_Ind` (the Ind-root soundness/descent)

The lap-144 baton named `descent_step_Ind` the #1 hardest-first leaf and its "lone genuine prerequisite" the
**internal term-evaluation `k = ⟦t⟧`** (the `hexit` clause `substs1 (numeral k) p = substs1 t p = ⊥`). This
lap took it apart and found that framing was **a phantom on the ⊥-orbit**, then PROVED the descent half and
pinned the actual obstruction.

### 1. `k = ⟦t⟧` blocker DISSOLVED — the ⊥-orbit forces `p = ⊥` (`eq_falsum_of_substs1_falsum`, banked)
On a `∅→⊥` orbit, `zIndWff` gives `seqSucc s = substs1 t p` and the orbit gives `seqSucc s = ⊥`, so
`substs1 t p = ⊥`. For a 1-ary semiformula `p`, substitution preserves the top connective (`substs_rel/
_nrel/_and/_or/_all/_ex/_verum/_falsum` + `IsSemiformula.case_iff`), and `^⊥` is the only constructor whose
substitution is `^⊥`, so **`p = ⊥`**. Then EVERY premise of the Ind reduct `iIndReductSeqG` carries succedent
`substs1 _ ⊥ = ⊥`, so the `hexit` clause holds for ANY `k` — **no term-evaluation needed**. The witness is now
pinned to the minimal **`k = 1`** reduct `⟨d0, d1[a:=0]⟩`.

### 2. DESCENT half DONE (`iord_descent_iIndReductSeqG_one`, banked, axiom-clean)
`icmp (iord (zK s (irk p) (iIndReductSeqG d0 d1 (π₁ at') 1))) (iord (zInd s at' p d0 d1)) = 0`. The genuine
substituted `k=1` reduct's `iord` equals the ordinal shadow `iIndReductSeq d0 d1 1 = ⟨d1,d0⟩`'s (banked
`iord_descent_iIndReduct`): both are 2-element seqs over the SAME premise-ordinal multiset (`idg`/`iotil` are
substitution-invariant, `idg_zsubst`/`iotil_zsubst`), so the folds differ by a **single `inadd`/`max`
commute** — crucially **no `inadd_assoc`** (the repo deliberately lacks it for general `k`; the `red` Ind
reduct `iRInd = zK s (irk p) (iIndReductSeq d0 d1 1)` is the 2-element SHADOW for exactly this reason — it
descends but is unsound, lap-136). Picking `k=1` keeps the descent in the commutativity-only regime.

### 3. SOUNDNESS BLOCKED — the REAL obstruction is a `zIndWff` faithfulness GAP (lap-145 finding)
`ZDerivesEmptyR (zK s (irk p) (iIndReductSeqG d0 d1 (π₁ at') 1))` needs `⟨d0, d1[a:=0]⟩` to thread
(`isChainInf`): every formula in `d1[a:=0]`'s antecedent must be `∈ Γ=∅` or `= chainAsucc 0 = ⊥`, i.e.
`seqAnt(fstIdx d1) ⊆ {⊥}`. **But `zIndWff` only requires `inAnt (F(a)) (seqAnt(fstIdx d1))` — MEMBERSHIP, not
shape.** A lax Ind node can have `d1` = a `zAtom` deriving `{⊥,X}→⊥` (valid, since `⊥ ∈` antecedent), whose
reduct is NOT a valid chain. So **the soundness goal is genuinely FALSE for lax nodes**, not merely unprovable
— do NOT sorry it as a flat lemma. The lap-144 plan ("step antecedent `Γ,F(i)` threads") silently assumed the
shape `zIndWff` does not deliver.

**FIX (the concrete next-attack) = strengthen the `zIndWff` step clause to pin
`seqAnt(fstIdx d1) = seqAddAnt (F(a)) (seqAnt s)`** (faithful Buchholz Ind rule: step premise antecedent
EXACTLY `Γ,F(a)`). This is a faithfulness ripple of exactly the lap-115 (`zAx1` 8th ZPhi disjunct) /
lap-118 (`zAxNeg` 4th conjunct) kind: edit the ZPhi Ind disjunct + `zphi_monotone`/`_strong_finite`/`zphi_iff`/
`zblueprint` σ+π / `zPhi_definable` + the `rcases zDerivation_iff.mp` sites (and `zDerivation_zInd_inv`/
`zIndWff` itself). After it: `seqAnt(fstIdx d1) = {F(a)} = {⊥}` (Γ=∅, p=⊥), the telescope `hstep` holds, and
soundness closes via `zDerivation_zK_intro` + `isChainInf_telescope` (banked) + premise `ZDerivation`s
(`ZDerivation_zsubst`, needs `maxEigen d1 < π₁ at'` from `ZRegular (zInd …)`) + the invariants
`ZRegular/ZFresh/ZSeqAnt` of the reduct (premise-hereditary). The DESCENT (item 2) is ALREADY proven for that
exact witness — so the strengthening is the WHOLE remaining content of `descent_step_Ind`.

## 🎯 NEXT (hardest-first)
1. **`zIndWff` step-premise antecedent strengthening** (the lap-145 finding's fix). Pin
   `seqAnt(fstIdx d1) = seqAddAnt (F(a)) (seqAnt s)`. Ripple per lap-115/118. Then assemble `descent_step_Ind`
   from the banked descent (`iord_descent_iIndReductSeqG_one`) + the now-provable soundness. ⟹ DROPS
   `descent_step_Ind`. (Caveat: re-check `seqAddAnt`/`inAnt` exact shapes vs the Buchholz §3.2 Ind rule before
   the ripple — the `zAxNeg` strengthening took two laps partly from shape mismatches.)
2. `descent_step_K_noncritical` (Buchholz 5.2 atomic reduct, GENUINE).
3. (A) `exists_sigma1_descending_step` (gDef packaging / concrete `redStep`/witness-bound).

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any `ZDerivesEmptyR_descent_step` branch with `red` (whole live path is off it).
- Do NOT re-introduce the `k = ⟦t⟧` term-evaluation framing for the Ind exit — DISSOLVED (`p = ⊥`).
- Do NOT sorry `ZDerivesEmptyR (zK … iIndReductSeqG …)` as a flat lemma — FALSE for lax `zIndWff` nodes until
  the step-antecedent is pinned.
- Do NOT attack `redSoundGen`/`ZDerivation_red_zK_crit`/`zKValidF_iIndReduct_of_zInd` AS STATED (dead, off-path).

## 📁 Key anchors (all in `src/GoodsteinPA/Crux2Blueprint.lean`)
- `eq_falsum_of_substs1_falsum` (NEW, the ⊥-orbit `p=⊥` collapse) — `~:2165`.
- `iord_descent_iIndReductSeqG_one` (NEW, the descent half, `k=1`, RED-FREE) + privates
  `iIndReductSeqG_one`/`idg_zK_iIndReductSeqG_one_eq`/`iotil_zK_iIndReductSeqG_one_eq`.
- `descent_step_Ind` (named sorry; docstring now records all three lap-145 findings + the fix) — `~:2248`.
- Telescope/read-offs banked: `isChainInf_telescope`, `chainAnt_/chainAsucc_iIndReductSeqG_*`,
  `seqSucc_zsubst_zInd_step` (the F(t+1) succedent). `zIndWff`/`zDerivation_zInd_inv` in `InternalZ.lean`.
- `PENDING_WORK.md` lap-145 · `STATUS.md` lap-145.

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. `descent_step_Ind`'s
descent half + `p=⊥` collapse are BANKED; the whole remaining content is the `zIndWff` step-premise antecedent
strengthening (lap-115/118-style ripple). Do THAT (hardest-first) → it drops `descent_step_Ind`. NEVER `red`.**
