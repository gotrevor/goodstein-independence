# Handoff: lap 206 — DEEP REFLECTION (every-9th) + route-(c) gadget probe PASSED + Gated/value kit GREEN

**FINAL LAP STATE (5 green commits)**: reflection synthesis (`b33192f`) · gadget probe
(`6bcea22`, `wip/ReadoffValueGadgetProbe.lean`) · `Gated` predicate (`c6a9fce`) · value kit
(`1c887ba`) — all sorry-free, kernel-clean. **Resume point = §NEXT item 1 below** (the master
root lemma `gated_of_sigma1`); items 2-3 follow. Tree CLEAN at end of lap.

**Date**: 2026-07-03 · **Branch**: `plan` · **Tree**: CLEAN · **Build**: 🟢 bare `lake build`
(1342 jobs, re-run this lap); `wip/E1EmbeddingGrind.lean` + `wip/ReadoffValueGadgetProbe.lean`
compile. **Headline** unchanged/undrifted: `peano_not_proves_goodstein = [propext,
Classical.choice, goodstein_implies_consistency, Quot.sound]`, sorryAx OFF (re-verified real
`#print axioms`); Route-B headline = girder + 12 native_decide, sorryAx OFF.

**Directive (OUTRANKS this)**: `DIRECTION.md` CURRENT DIRECTIVE **lap-206 block** (set this lap).

## ✅ This lap (deep reflection + probe)

### Reflection synthesis (commit 1) — ROUTE VERDICT: CONTINUE, piece-1 route RETARGETED
Full synthesis in `PENDING_WORK.md` (lap-206 top). Independently re-verified EVERYTHING (build,
headlines + statements re-read vs the source claim, rungs P/R/D, the whole wip bank — kernel-clean
except exactly the ONE `readoffTC_core` trap sorry). Central finding:
- **Trap-fix routes (a) mono-guard and (b) φ-shape vacuity are DEAD on the real matrix**:
  `igoodsteinDef` is the machine-generated `PR.Blueprint.resultDef` computation-history formula
  (`InternalGoodstein.lean:46`); its bounded-∀ coherence clauses falsify the mono-guard and sit on
  the instantiation spine. Do NOT grind (a)/(b).
- **Source diagnosis (E–W §4–5)**: E–W value-relativize controls (`f[m+k]`, `f[N(ι)]`) and gate
  every branch index by rule side conditions; `Zef2TC.allω` has no gate ⟹ the trap is a
  calculus/invariant MISMATCH artifact; E–W's own exit bound is an α-indexed iterate
  (`s^α(m₀+⋯)`), not constant `f(0)`.
- **MANDATE = route (c), the value-budget read-off** (design + step order in PENDING lap-206):
  subformula closure from the singleton root (junk can't enter: weakening shrinks going up, cut
  impossible at c=0) + ball value gate `k₀ < val(bounding term) ≤ P_φ V` + master bound
  `BND V α := ewIter S α (S V)`, `S x := max (f₀ x) (P_φ x)`. Exposed bound may loosen — the
  ratified splice target `∃ o, o.NF ∧ …` has total ordinal freedom (piece 2b absorbs).
- Ledger re-grade: Route-B girder **🟠 → 🟡**. STATUS/DIRECTION/literature-review all refreshed.

### Gadget probe (commit 2) — `wip/ReadoffValueGadgetProbe.lean`, sorry-free, kernel-clean
The mandated step-(1) probe PASSED: `T3_descent` (the decisive threading inequality — ordinal
descent + budget bump absorbed by `BND`, from banked `ewIter_monotone`/`ewIter_lower` alone),
`T1_extract`, `T4_slot_read` (slot stays `rel1 f₀ j`, `j ≤ V`, via `rel1_rel1`; gates and reads
convert to T3/T1 shapes), `S_max_mono_infl`. All `[propext, Classical.choice, Quot.sound]` or
less. **(c′) — the `allω`-gate calculus amendment — is OFF the table.**

## ✅ Also this lap (commit 3) — `wip/ReadoffValueGate.lean`: the `Gated` predicate GREEN
The derivation-side half of step (2) is BANKED (sorry-free, compiles clean): **`Gated P V ψ`** —
the hereditary semantic value gate the V-threaded invariant tracks (defined by WF recursion on
`complexity`, `Semiformula.complexity_rew` pays the `χ/[nm k]` recursion) — plus the four
accessor iffs (`Gated_and_iff`/`or`/`all`/`exs`) and **`Gated_mono`** (budget monotonicity —
old members stay gated when V bumps). ARCHITECTURE NOTE: `Gated` replaces the planned syntactic
`InstOf φ V` closure — the derivation induction only manipulates `Gated` (semantic, no syntax);
the syntactic ball/`P_φ` work discharges `Gated` ONCE at the pipeline root. The tracked `∃⁰ φ`
member is ALSO just required `Gated` (its ∃-clause is exactly the tracked-`exI` false-instance
need), so the invariant is uniform: every member `Gated`, non-φ members additionally false.

## ✅ Also this lap (commit 4, `1c887ba`) — the tvB/gvb value kit GREEN
`wip/ReadoffValueGate.lean` now ALSO banks (all `[propext, Classical.choice, Quot.sound]`):
**`valm_mono`** (ℒₒᵣ standard-model term values monotone in both envs; the func casing needs
`cases f` + a `show`-cast from raw `ORing.Func` constructors to the class projections
`Language.Zero.zero`/… that `val_func₀`/`Structure.add_eq_of_lang` are keyed on — tactic `match
k, f` does NOT refine the goal, `cases f` does); **`tvB t B`** (`valm ℕ (fun _ => B) (fun _ => 0)`)
+ `tvB_mono`/`tvB_bShift`/`tvB_nm`; **`gvb ψ B`** (max value of atomic-arg terms — ball guards
included) + `gvb_mono`; **`gvb_rew_le`** (contraction under numeral-or-variable rewrites; class
stable under `ω.q` via `q_bvar_zero`/`q_bvar_succ`/`q_fvar`; constructors pushed by
`Semiformula.rew_rel`/`rew_nrel` + `rfl`-shows) and its corollary **`gvb_substs_le`**:
`gvb (χ/[nm k]) B ≤ gvb χ (max B k)` — the exact `allω`/`exs` descent shape.
GOTCHA: it is `Rew.subst` (not `substs`); `φ/[t] = Rew.subst ![t] ▹ φ` (abbrev, defeq);
`omega` atomizes `valm` vs `val (standardModel ℕ)` separately — use defeq-`exact`
(`Nat.add_le_add`) instead.

## 🚦 NEXT (grind laps, in order — DIRECTION lap-206 block)
1. **The ROOT discharge `gated_of_sigma1`** (the remaining half of step (2), design settled):
   `Hierarchy 𝚺 1 ψ → (∀ B, gvb ψ B ≤ P (max V B)) → Gated P V ψ` — note the COUPLED hypothesis
   shape `P (max V B)`: it self-threads through instantiation (inner guards may mention outer
   variables; `gvb_substs_le` + max-algebra re-establishes it at budget `max V k`). Induct on
   complexity (or on the `Hierarchy` derivation) at arity 0/1: the `all`-head inverts to `ball`
   ONLY (`cases` on the `Hierarchy` hypothesis — `all`/`pi`/`dummy_sigma` are level-blocked at
   𝚺 1); the gate: a false instance of the guarded `∀⁰[“x. x < !!t”] χ'` forces
   `atomTrue (guard/[nm k])` = `k < val(t) ≤ gvb ψ 0 ≤ P V`; `t.Positive` (bound var not in `t`)
   makes `val(t)` instance-independent. `Hierarchy.rew` threads Hierarchy-ness through instances;
   `smul_ball` pushes rewrites through the guarded quantifier. At the pipeline root, choose
   `P := fun B => gvb φ_root (max V_root B)` (monotone via `gvb_mono`).
2. **V-threaded re-grind of `readoffTC_core`** against `BND V α := ewIter S α (S V)`: invariant =
   `(∃⁰φ) ∈ Γ ∧ ∀ ψ ∈ Γ, Gated P V ψ ∧ (ψ = ∃⁰φ ∨ ¬atomTrue ψ)` + slot frame `g = rel1 f₀ j`,
   `j ≤ V`; the 8 closed cases survive with V-edits (`Gated_mono` for bumps); the trap case
   descends into `Gated_all_iff`'s gated false branch `k₀ ≤ P V` (T3 absorbs). Copy `Gated` +
   the T-lemmas into `E1EmbeddingGrind.lean` at wiring time (it is not importable).
3. **Piece 2a structural wiring** (bound-shape-independent, may interleave): root plumbing →
   `rankToZeroAuxTC` (EwLow entry; `three_le_rel1_rootSlot`/`ewIterTower_infl` banked lap 205).
4. Then 2b growth conversion; rung-E statement + `Zef2TC` + (Ax2) + read-off (c) shape → next
   judge pass as ONE package. **Do NOT self-ratify into `src`.**

## Gotchas fresh this lap
- `ewIter_lower`'s gate at seed `m` is `Nlog β ≤ S (Nlog α + m)` — always reach it from the
  calculus's `Nlog β ≤ (slot) 0` via `T4` + one `S`-application + monotonicity (`by omega` on the
  `Nlog α + m` argument). The one-`S`-ahead seed (`S V`, not `V`) is what makes the exI/trap bumps
  absorb — do NOT try to thread the bare-`V` seed (the gate can't reach `S (S V)` from `f₀ j'`
  otherwise).
- `SS_le_ewIter` (`S (S x) ≤ ewIter S β x`, `β ≠ 0`) is `ewIter_lower` at `β := 0` — no
  inflationarity needed (`ewIter_zero` + `simpa`).
- `igoodsteinDef` is 𝚺₁ (∃-prefixed), so the pipeline φ handed to the read-off may itself be
  Σ₁-matrixed, not Δ₀ — route (c)'s gate only needs "all ∀-subformulas ball-shaped", which
  `Hierarchy 𝚺 1` supplies; don't force a Δ₀ hypothesis the pipeline can't discharge.
