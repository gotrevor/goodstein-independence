# HANDOFF — lap 96 · route-B conclusion-reducing `iRKr` LANDED in-kernel

> Supersedes prior HANDOFF-*.md. Branch `plan`. HEAD = `b74f7a0`. Build 🟢 green
> (`lake build GoodsteinPA`, 1325 jobs). Headline `peano_not_proves_goodstein =
> [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms, re-verified in-kernel);
> `peano_not_proves_consistency` axiom-clean. Lone math obligation is still crux-2
> (`goodstein_implies_consistency`, `Reduction.lean:68`) = internalized Buchholz cut-elim.

## Operator target (unchanged)
Discharge `peano_not_proves_goodstein` AXIOM-FREE = crux-1 (DONE lap 57) ∘ crux-2 (`redSound` =
internalized Buchholz cut-elimination). Route B (conclusion-reducing reduct) is the path (lap-90).

## ⭐ KEY FINDING (lap 96): the route-B rewire is SURGICAL — `iord`/`zReg` are conclusion-independent
`iotil_zK` = `iseqNaddIdg ds` and `idg_zK` = `max r (iseqMaxIdg ds - 1)` read ONLY rank + premise
sequence, NOT the conclusion `fstIdx`. Likewise `zReg (zK …) = iseqReg ds`. So changing `iRKr`'s
CONCLUSION (route B) leaves the entire lap-82..95 ordinal-descent + regularity investment UNTOUCHED;
only the handful of `fstIdx`-asserting lemmas needed threading. This dissolves the "huge blast radius"
fear that deferred the rewire for laps.

## What lap 96 LANDED (in-kernel, green 1325, axiom-clean)
- **`iRKr` (`InternalZ.lean:5804`) now emits the reduced conclusion** `tpReduce (tp dᵢ) (fstIdx d) 0`
  (Buchholz Def 3.2 case 5.2.2) instead of keeping `Π`. `iRKrDef` definability re-proved.
- **`fstIdx_iRKr`** now states the reduced form; **`fstIdx_iRKr_of_Rep` / `fstIdx_iRK_of_Rep`** recover
  the keep-`Π` form when the selected premise is `Rep`.
- **Cor 2.1 promoted to InternalZ:** `tp_isymRep_of_emptyAnt_botSucc` (permissible premise of a ∅→⊥
  chain is Rep) + **`tp_selected_isymRep_of_emptyAnt_botSucc`** (selected premise of a ∅→⊥ chain is Rep)
  + **`fstIdx_red_of_emptyAnt_botSucc`** (red keeps the ∅→⊥ conclusion on the ⊥-orbit). All axiom-clean.
- old **false** `fstIdx_red_eq_tpReduce_of_Rep` REMOVED (it used repo `tp = Rep` for chains, but
  Buchholz's 5.2.2 sets `tp(d) := tp(dᵢ)`; the invariant must read the SELECTED premise's tp).
- `Zsubst`/`Crux2Blueprint` `red_zK_rep`/`_rep_nonchain` restated to the reduced form (regularity
  proofs unaffected). **`ZDerivation_red_zK_replace` now PROVED** on the reduced conclusion modulo the
  route-B conclusion-tracking IH `fstIdx (red dᵢ) = fstIdx dᵢ ∧ tp (red dᵢ) = isymRep`.

## ▶ NEXT (START HERE) — the route-B soundness induction `redSoundF`
The piecemeal K-helpers are now obsolete; the clean target is the conclusion-tracking induction
(lap-90 route B, refined):

  `redSoundF : ∀ d, ZDerivation d → ZDerivation (red d) ∧ fstIdx (red d) = redConcl d`

where `redConcl d` = the genuine Buchholz `tp(d)(end d, 0)` — for a NON-CRITICAL chain in case 5.2.2
that is `tpReduce (tp dᵢ) (fstIdx d) 0` (dᵢ the SELECTED premise), NOT `tpReduce (repo-tp d) …` (repo
`tp` of a chain is always Rep, which is WRONG for 5.2.2 — this is the `tp`-faithfulness subtlety that
killed the naive invariant). Provable by plain structural induction (no orbit restriction); the
conclusion-tracking IH feeds `ZDerivation_red_zK_replace`'s `hredfst`/`hredtp`. Then specialise to the
⊥-orbit (`fstIdx_red_of_emptyAnt_botSucc`, already proved) where every reached chain's selected premise
is Rep (Cor 2.1) ⟹ conclusion stays `∅→⊥` ⟹ `ZDerivesEmpty` preserved.

Concretely:
1. Define `redConcl` (or fold it into the invariant) handling the chain 5.2.2 selected-premise tp.
2. Prove `redSoundF` by `zDerivation_induction` — the I/Ax/atom cases are trivial; Ind needs
   `zKValidF_iIndReduct_of_zInd` (Crux2Blueprint sorry, also route-B conclusion+count tracking, see
   lap-90 note); the chain case dispatches 5.1/5.2.1/5.2.2 feeding the now-proved replace lemma + the
   splice (`ZDerivation_red_zK_splice`, still sorry) + crit (`ZDerivation_red_zK_crit`, still sorry).
3. `iord_descent_red` (Crux2Blueprint:325) — assemble from the banked per-branch descents under the
   faithful dispatch (iord is conclusion-independent, so the descent lemmas apply directly).
4. Wire `Crux2Blueprint → false_of_ZDerivesEmpty → goodstein_implies_consistency → headline`.

## Open crux-2 sorries (Crux2Blueprint, all route-B residuals)
`zKValidF_iIndReduct_of_zInd`, `ZDerivation_red_zK_crit`, `ZDerivation_red_zK_splice`,
`ZDerivation_red_zK` (chain-replace + non-chain-replace branches), `iord_descent_red`,
`foundation_bot_to_Z_empty` (M2 bridge), `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle (deeply-entangled residuals, poor self-contained fodder — same as lap 95).

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `#print axioms peano_not_proves_goodstein` =
`[propext, sorryAx, Classical.choice, Quot.sound]`. New lap-96 lemmas all axiom-clean.
