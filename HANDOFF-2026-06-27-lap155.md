# Handoff: lap 155 — FRESH-MIND REVIEW; tag-5/6 COURSE-CORRECTED off the lap-136 wall

**Date**: 2026-06-27 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Re-verified in-kernel: headline `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]`
(**0 math axioms**); `goodsteinSentence_faithful` + `peano_not_proves_consistency` = `[propext, choice,
Quot.sound]`; `false_of_ZDerivesEmpty = [propext, sorryAx, choice, Quot.sound]`. Statement re-audited — no
drift. **This was a review lap: no proof code, the deliverable is the binding course-correction in
`DIRECTION.md` CURRENT DIRECTIVE (lap-155).**

## The call: direction KEPT, tag-5/6 sub-case REFRAMED
- lap-152's mandate is **DONE**: MASTER KEY #1 `genReduct_chain_hasRedex` DROPPED (lap 153), MASTER KEY #2
  `genReduct_chain_noRedex` 6/8 tag-branches PROVEN (lap 154). The whole M1b-term termination crux is now
  the **SINGLE open leaf `axMajorClose`** (`Crux2Blueprint:3418`) — the tag-5/6 L-axiom cut-partner.
- The operator's standing "do the lap-132 existence-form spike first" directive is STALE (adopted ~laps
  138-141; we are 14+ laps downstream). Do NOT re-run it.

## ⭐ The course-correction (kernel-grounded, the reason for this lap)
The lap-154 handoff + lap-154b refinement frame `axMajorClose`'s hard sub-case (b) as the **lap-136
general-succedent induction reduct** (`⟨d0, d1[a:=0..k-1]⟩`, `k=⟦t⟧`) — the repo's single hardest target,
kernel-refuted at face value (`ind-reduct-false-target-lap136`). **The review judges that PESSIMISTIC +
PREMATURE** and mandates the cheaper SUCCEDENT-THREADING COLLAPSE first.

Grounding (verified by reading the actual machinery, not memory):
1. **Cut formula is `^∀⊥`.** A tag-5 ⊥-exit major `zAxAll s' p' k'` satisfies `zAxAllSuccWff`
   (`seqSucc s' = substs1 k' p'`); the ⊥-exit forces `seqSucc s' = ⊥`, so `eq_falsum_of_substs1_falsum`
   gives `p' = ⊥`. (Finding 1, kept from lap-154b.)
2. **The active `^∀⊥` already splits via the chain's `hthread`.** The existing
   `majorPrem_zAxAll_cutPartner` (`InternalZ:9329`) takes `inAnt (^∀⊥) (seqAnt s')` (the major's antecedent,
   from `zDerivation_zAxAll_inv`) and applies `hthread` → `^∀⊥ ∈ Γ = seqAnt s` ∨ `∃ i'<jstar, ^∀⊥ =
   chainAsucc ds i'`. At `seqAnt s = ∅` it kills the first by `simp [inAnt, lh_empty]`; **at `Γ≠∅` the first
   disjunct is the ESCAPE (sub-case a).**
3. **Under `hnolow` a direct R-intro of `^∀⊥` below j0 is IMPOSSIBLE.** `isRedexPair` (`InternalZ:4820`)
   requires: i-end right-symbol (`π₁(tp)=0`), j-end left-symbol (`π₁(tp)=1`), SAME cut formula — exactly the
   shape a `(zIall ^∀p, zAxAll ^∀p)` pair has. So a `zIall` introducing `^∀⊥` at any `i' < jstar ≤ j0` would
   be an `isRedexPair` with jstar, contradicting `hnolow`. **`^∀⊥` is therefore never CREATED below j0; it
   only threads from Γ** ⟹ sub-case (b) collapses to (a). This OBVIATES lap-136 for tag-5/6.

## 🎯 NEXT — the mandated attack tree (full detail in `PENDING_WORK.md` lap-155 top)
Decompose `axMajorClose` into named src sub-`sorry`s (raising the count = progress):
1. **[teed-up first DROP] 2 reusable ordinal lemmas** — `w < ω^w` (`icmp w (ocOadd w 1 0) = 0`, NF `w<ε₀`,
   order-induction on the CNF spine; cf. the `iseqNaddIdg ≠ 0` positivity proof already at
   `Crux2Blueprint:3395-3410`) + summand-≤-fold (`icmp (ocOadd (iotil (znth ds jstar)) 1 0) (iseqNaddIdg ds)
   = 0`). Self-contained; REUSED by every nonzero-õ reduct from here.
2. **sub-case (a) `axLeafClose`** (the `^∀⊥∈Γ` analogue of `leafClose`, `Crux2Blueprint:3420`) — generalize
   `majorPrem_zAxAll_cutPartner`/`_zAxNeg_cutPartner` off `seqAnt s = ∅` so the threading RETURNS the
   `^∀⊥∈Γ` disjunct; then the fresh axiom `zAxAll s ⊥ k'` (NOT `zAtom` — `^∀⊥`, not `⊥`, is in Γ) derives
   `Γ→⊥` directly, õ-dropping via #1. Reg/fresh/seqAnt for `zAxAll` are free (mirror `zReg_zAtom` etc.).
3. **collapse sub-case (b)** — case-split i''s constructor: leaf → `chainAsucc_threaded_of_leaf` (EXISTS,
   used at `InternalZ:9292`); direct-R-intro → `hnolow` (impossible, verified); zK sub-chain → recurse the
   threading; **lone residual = a `zInd` concluding `^∀⊥`** — FIRST check whether a sound `zInd` can even
   conclude a `^∀`-formula (its succedent is `substs1 t p_ind` per `zDerivation_zInd_inv`). If not, the
   collapse is TOTAL. Only if it survives does lap-136 return, and then as a NARROW `Γ→^∀⊥` target.

**Success = a src sorry drops on this path** (lemma 1 + sub-case (a) is the bar). Closing all of
`axMajorClose` ⟹ `genReduct_chain_noRedex` LAST leaf gone ⟹ `genReduct_botSucc` PROVEN ⟹ outer
`descent_step_K_noncrit_axMajor` (`:3507`) + gDef (`:3630`) collapse.

## ⚠️ Forbidden / do-NOT-undo (per DIRECTION lap-155, binding)
Building the lap-136 general-succedent reduct as the PRIMARY attack BEFORE the collapse is tested to
exhaustion; `red` witnesses; `iord`-recursion for construction (CODE induction via
`zDerivation_sigma_induction` ONLY); `redLeast`/μ-min for gDef; the refuted single-premise `seqUpdate`
splice; attacking `descent_step_K_noncrit_axMajor`/gDef STANDALONE; the off-path dead `red`-soundness
sorries AS STATED; M2/M4 wiring.

## 📁 Key anchors
- Target: `axMajorClose` (`Crux2Blueprint:3418`, inside `genReduct_chain_noRedex` `:3365`); the
  `genReduct_chain_noRedex` body's `leafClose` (`:3420`) is the reusable `⊥∈Γ` template; `hnolow`/`hthread0`
  are its hyps.
- Cut-partner lemmas to generalize: `majorPrem_zAxAll_cutPartner` (`InternalZ:9329`), `_zAxNeg_cutPartner`
  (`:9357`); both currently `hant : seqAnt s = ∅`. `chainAsucc_threaded_of_leaf` (used `:9292`).
- Redex machinery: `isRedexPair` (`InternalZ:4820`), `redexPair_tp` (`:4831`).
- Ordinal kit: `iotil_zAxAll` (`InternalZ:2428`, the nonzero õ), `iotil_zK`, `iseqNaddIdg`, `inadd_left_mono`
  (`InternalNadd:827`), `icmp_pos_zero`/`icmp_zero_pos` (`InternalONote`), `isNF_omega_pow`, `ocOadd`.
- `DIRECTION.md` CURRENT DIRECTIVE (lap-155, binding) · `PENDING_WORK.md` lap-155 top · prior baton
  `HANDOFF-2026-06-27-lap154.md`.

---
**→ Next session: prove the 2 ordinal lemmas (`w<ω^w` + summand-≤-fold), then sub-case (a) `axLeafClose`, to
DROP the first half of `axMajorClose`. Then collapse sub-case (b) via the leaf/`hnolow`/zK case-split. Keep
build green + headline 0 math axioms.**
