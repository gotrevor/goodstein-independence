# HANDOFF — lap 128 · the FRESHNESS CAMPAIGN completed + ∀-case soundness of the re-keyed reduct LANDED

> **RESUME BATON.** Branch `plan`, HEAD = this lap's commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint unchanged: `peano_not_proves_goodstein =
> [propext, sorryAx, choice, Quot.sound]` (0 math axioms). All new lemmas additive + axiom-clean
> `[propext, choice, Quot.sound]`; no `sorry` touched (11 disclosed sorries, as lap 127).

## What this lap DID — freshness front: substrate → orbit-invariant → SOUNDNESS

A seven-commit arc that takes the eigenvariable-freshness invariant from "red-stability half-done"
(lap 127) all the way to **the ∀-case soundness of the engine-swap critical reduct, with freshness
discharged from the orbit**. The lap-114 "instance-`0`-vs-`k`" obstruction is now CLOSED on the supply
side.

### The arc (each a green, axiom-clean commit)
1. **`ZFresh_red`** (`Zsubst.lean`) — `red` preserves `ZFresh` (full `zK` chain dispatch; lap 127 had only
   structural+Ind). `zFresh(zK)` is the pure premise max-fold, so the family mirrors `ZRegular_red_zK`.
2. **`ZFresh` threaded into `ZDerivesEmptyR`** (`Crux2Blueprint:933`) — 3rd conjunct; `ZDerivesEmptyR_red`
   discharges it via `ZFresh_red`. Freshness is now a `red`-orbit invariant.
3. **Target-3 suppliers** (`Zsubst.lean`) — `fvSubst_numeral_eq_self_of_zfresh_zIall_at` /
   `fvSubstSeq_…_at`: lift the stored `numeral 0` witness to ANY cut instance `k`.
4. **`ZFresh_iRKcCrit`** (+ `ZFresh_zsubst_zIallPrem` / `ZFresh_zInegPrem`) — O3 front of the engine swap:
   `iRKcCrit d` preserves `ZFresh` (mirror of the landed O1 `ZRegular_iRKcCrit`).
5. **`zfresh_critReductCorr_freshness`** (`Zsubst.lean`) — packages the exact `⟨hpfresh, hΓfresh⟩` pair
   `ZDerivation_iRcritG_critReductCorr` needs at `k = π₁(π₂(tp dⱼ))`, from just `ZFresh (zK s r ds)` + the
   R-redex form + matrix wff.
6. **`ZDerivation_iRKcCrit_all`** (`Crux2Blueprint.lean`, after `ZDerivation_iRcritG_critReductCorr`) — ⭐
   THE soundness payoff. `iRKcCrit (zK s r ds)` is a genuine `ZDerivation` in the ∀ case, with
   `hpfresh`/`hΓfresh` discharged INTERNALLY from the orbit `ZFresh` (no longer bare hypotheses). Delegates
   to the PROVEN `ZDerivation_iRcritG_critReductCorr` through `iRKcCrit_eq_corr`. Sorry-free.

### Net state of the engine swap's critical reduct `iRKcCrit`
All three invariants now LANDED additively (no touch to `red`/`iRK`, `src/` green throughout):
- **O1 regularity:** `ZRegular_iRKcCrit` (lap 119).
- **O3 freshness:** `ZFresh_iRKcCrit` (this lap).
- **Soundness (∀):** `ZDerivation_iRKcCrit_all` (this lap) — freshness self-supplied from the orbit.

## Next-lap targets (in order)
1. **¬-case soundness `ZDerivation_iRKcCrit_neg`** — the twin of `ZDerivation_iRKcCrit_all` for `zTag dᵢ
   ≠ 1` (I¬ R-redex), delegating to the PROVEN `ZDerivation_iRcritGNeg_critReductNeg` via `iRKcCrit_eq_neg`.
   No freshness needed (the ¬-reduct is the red-free `zInegPrem` child); the bundle is `hdi = zIneg`, `hdj =
   zAxNeg`, `hcut`, `hd0ant`, threading/rank/Seq. Contained mirror of target 6 above.
2. **Discharge the non-freshness plumbing** — `hCwff`/`hSeqs`/`hSeqsj`/`hsj`/`hthread`/`hrank`/`hrankI`
   from `zKValid`/`isChainInf`. `redZKReady_of_zKValid` (`InternalZ:8268`) already gives `hi`/`hj`/`hIJ` +
   the polarity dispatch (∀-pair `hdi`/`hdj` + rank relation `irk(∀pj)=irk(cutFormula)+1`). Threading/rank:
   restrict the parent `isChainInf` (carried in `zKValid`) to `≤ redexI` via `thread_rank_restrict_of_le`
   (`InternalZ:5112`) — BUT this needs the index bound `redexI ≤ j0` (j0 = the `isChainInf` tip). **Open
   sub-question:** is `redexI < j0` (for `hrankI`) provable from the redex-pair / chain structure? Probe
   `redexPair`/`isRedexPair` lemmas + how `j0` (tip carrying succedent) relates to the redex indices.
3. **Assemble `ZDerivation_red_zK_crit`** (`Crux2Blueprint:517`, sorry) post-swap = `red_zK_crit` (re-keyed
   to `iRKcCrit`) + `ZDerivation_iRKcCrit_all`/`_neg` + the plumbing from target 2. NOTE: this lemma's
   signature must take the orbit bundle (`ZFresh` + threading + rank), since plain `ZDerivation` doesn't
   supply freshness — that's why `redSound` consumes `ZDerivesEmptyR` (now carrying `ZFresh`).
4. **THE atomic engine swap** — re-key `iRK`'s `else` branch `iRKc d s ↦ iRKcCrit d` + `iRKDef`/`iRNextGDef`
   (the `iRKcCritDef` `𝚺₁` graph is already built, `InternalZ:7484`); re-point `red_zK_crit`; re-key the
   DESCENT (`iord_descent_red` crit case ↦ `iord_iRcritG_eq_iRcrit` family, `RedZKDescent`). This is the
   load-bearing red-build-mid-way change; do it LAST, once targets 1-3 make every downstream re-proof a
   one-liner. See `engine-swap-not-pure-wiring-lap119` memory for the 3-front scoping.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open pieces are engine-internal assembly, not portable single-lemma targets.

## Pointers
`PENDING_WORK.md` lap-128 · `STATUS.md` · soundness: `Crux2Blueprint.lean` (`ZDerivation_iRKcCrit_all`,
`ZDerivation_iRcritG_critReductCorr`, `ZDerivation_iRcritGNeg_critReductNeg`) · freshness:
`Zsubst.lean` (`ZFresh_red`, `ZFresh_iRKcCrit`, `zfresh_critReductCorr_freshness`,
`fvSubst*_numeral_eq_self_of_zfresh_zIall_at`) · engine: `InternalZ.lean` (`iRKcCrit`, `iRKcCrit_eq_corr`,
`critReductCorr`, `redZKReady_of_zKValid:8268`, `thread_rank_restrict_of_le:5112`).
