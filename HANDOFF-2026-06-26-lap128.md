# HANDOFF — lap 128 · `ZFresh_red` PROVEN (red preserves the freshness invariant, full zK dispatch)

> **RESUME BATON.** Branch `plan`, HEAD = this lap's commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint unchanged: `peano_not_proves_goodstein =
> [propext, sorryAx, choice, Quot.sound]` (0 math axioms). All new lemmas additive + axiom-clean
> `[propext, choice, Quot.sound]`; no `sorry` touched (11 disclosed sorries, as lap 127).

## What this lap DID — `ZFresh_red` is now COMPLETE (lap-127 target 1 closed)

Lap 127 landed `zFresh_zsubst` + `ZFresh_red_of_not_zK` (structural + Ind cases of red-stability). Lap 128
closed the remaining frontier — the `zK` chain dispatch — so the **full downward red-stability theorem**

```
theorem ZFresh_red : ∀ d : V, ZDerivation d → ZFresh d → ZFresh (red d)
```

is PROVEN and axiom-clean `[propext, Classical.choice, Quot.sound]` (verified in-kernel via `#print axioms`).

### Why it went fast: `zFresh(zK)` is a pure premise max-fold
The chain node carries **no own freshness flag** (`zFresh_zK = iseqMaxTab …` over premises), so every branch
of the `iRK` dispatch collapses to "every reduct premise is `ZFresh`", closed by `zfresh_zK_of`. This made
the whole family a near-mechanical mirror of `ZRegular_red_zK` (no `zsubst`/`zReg`-table reasoning needed at
the chain node — that was already discharged inside `ZFresh_red_of_not_zK`'s I∀ leaf last lap).

### ✅ LANDED (`Zsubst.lean`, after `ZFresh_red_of_not_zK`)
Structural blocks (line-for-line analogues of the `ZRegular_*` versions):
- `ZFresh_zAxReduct` (zAx1 on tags 5/6, identity else) · `ZFresh_zK_of_premises` / `ZFresh_zK_premise`
  (wrappers of `zfresh_zK_of` / `zfresh_zK_premise`) · `ZFresh_zK_of_seqUpdate` ·
  `ZFresh_zK_of_iCritReductSeq` · `ZFresh_zK_of_seqInsert` · `ZFresh_iRcritG_premise`.

Branch lemmas (the `iRK` dispatch):
- `ZFresh_red_zK_replace` (5.2.2) · `ZFresh_red_zK_crit` (5.1) · `ZFresh_red_zK_splice` (5.2.1, halves
  given) · `ZFresh_red_zK_splice_of_chain` (5.2.1, halves derived from `zTag dᵢ = 4` + IH) ·
  `ZFresh_red_zK` (the full gated dispatch) · `ZFresh_red` (the `zDerivation_induction` assembly).

## Next-lap targets (in order) — unchanged from lap-127 plan, target 1 now DONE

1. **Thread `∧ ZFresh d` into `ZDerivesEmptyR`** (`Crux2Blueprint.lean:933`:
   `def ZDerivesEmptyR (d : V) : Prop := ZDerivesEmpty d ∧ ZRegular d` → add `∧ ZFresh d` as 3rd conjunct).
   Mechanical ripple, all pieces banked:
   - `ZDerivesEmptyR_red` (`Crux2Blueprint:1097`): add the 3rd component `ZFresh_red d h.1.1 h.2.2` (after
     re-indexing `h.2` → `h.2.1` for the `ZRegular` slot). The `fstIdx_red` transfer is unchanged.
   - `redSound` (`:938`): `fun d h => redSoundGen d h.1.1 h.2.1` (re-index only — `redSound` doesn't consume
     `ZFresh` yet; it's the M-level orbit invariant carried FOR target 2).
   - The embedding `foundation_bot_to_Z_empty` (M2, `:1131`, sorry) supplies the base `ZFresh`: real
     formula antecedents ⟹ `seqWffFlag = 0`; eigenvariables chosen fresh ⟹ the two fresh-eqs. Add the
     `ZFresh z` conjunct to its `∃ z, ZDerivesEmptyR z` conclusion (it's already a sorry, so just widen the
     target type — no new proof obligation lands until M2 is attacked).
   - Audit every `.2`/`.1.2` projection on a `ZDerivesEmptyR` across `Crux2Blueprint.lean` +
     `RedZKDescent.lean` after the def change (grep `\.2\b` near `ZDerivesEmptyR`/`hz`/`hd`). The
     `ZRegular`-slot consumers (`iord_descent_red`, `ZRegular_zK_premise` calls) move from `.2` to `.2.1`.
2. **Close LEFT-branch ∀-soundness** via `ZDerivation_iRcritG_critReductCorr` (`Crux2Blueprint:470`): feed
   `hpfresh = fvSubst_numeral_transfer (matrix UFormula) (fvSubst_numeral_eq_self_of_zfresh_zIall
   (zfresh_zK_premise … the chain's ZFresh …))` and `hΓfresh` likewise (`fvSubstSeq_*`). THE payoff — the
   freshness front feeds the cut-elimination ∀-inversion the whole crux-2 endgame rests on. (Now that
   `ZFresh` is orbit-invariant after target 1, `zfresh_zK_premise` extracts the per-cut-instance freshness
   exactly where `critReductCorr` needs it.)
3. ¬-case (`iRcritGNeg`, lap-117) — same substrate covers it.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open pieces (target-1/2/3 threading + the M2/M3 endgame) are engine-internal, not
portable single-lemma targets.

## Pointers
`PENDING_WORK.md` lap-128 · `STATUS.md` · `Zsubst.lean` (search `ZFresh_red`, `ZFresh_red_zK`,
`ZFresh_zK_of_seqUpdate`) · `Crux2Blueprint.lean:933` (`ZDerivesEmptyR` def, target-1 site) ·
`ZRegular_red` skeleton `Zsubst.lean:2433` (the mirror that `ZFresh_red` followed).
