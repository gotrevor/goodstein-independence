# HANDOFF — lap 84 · crux-2 RedSound: D₁ validity toolkit banked + judge blueprint wired

> Supersedes prior HANDOFF-*.md. Read order: this → `PENDING_WORK.md` (lap-84 boxes) →
> `Crux2Blueprint.lean` (the leaf decomposition) → `E-CRUX2-ROADMAP-2026-06-24.md` (M1–M4).
> Branch `plan`. HEAD `891bbf8`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Tree clean.

## 🎯 OBJECTIVE (operator, binding)
Discharge `peano_not_proves_goodstein` (`Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`) **or ABANDON** — no Gentzen/PRWO-as-axiom. Front-2
(`PA_delta1Definable`) is GIFTED upstream (Foundation@e6e1ad1) — done, leave alone. SOLE blocker =
**crux-2**: the 3 src sorries (`Statement.lean:22`, `Reduction.lean:66`, `DescentSemantic.lean:550`),
which all reduce to **`RedSound`** (the genuine validity-preserving cut-elimination reduct).

## ✅ THIS LAP (15 green commits, axiom base untouched) — D₁ (validity) of RedSound, all 3 cases
The judge unlock (verified vs Buchholz both papers): validity is a **PARALLEL invariant** (Thm
3.4(b)/6.2 = our `zKValidF`/D₁), proved alongside the BANKED descent (Lemma 4.1/Thm 4.2 = D₃) over the
same reduct — NOT recovered post-hoc. `RedSound`-on-`iR2` is false only because `iR2` is ordinal-first.

Banked in `InternalZ.lean` (all kernel-checked, `#print axioms` clean):
- **T2/T3 "replace-a-premise stays valid"**: `isChainInf_congr`, `isChainInf_seqUpdate`,
  `zKValidF_seqUpdate`, and concrete non-critical (5.2.2) `zKValidF_seqUpdate_iR2` (+ reduct-shape
  `iR2_eq_zK_of_tag_Ind_or_K`, `zTag_iR2_…=4`, `tp_iR2_…=isymRep`).
- **Critical (5.1)**: `isChainInf_iCritReductSeq` → `zKValidF_iCritReductSeq` (validity given Thm-3.4(a)
  cut-threading), then the GENUINE-reduct version `zKValidF_iCritReductGen` where the **threading is
  AUTOMATIC** from the genuine sequent ops. VERIFIED FAITHFUL to Buchholz §2 p.6 (`d{0}⊢Θ→A(d)`,
  `d{1}⊢A(d),Θ→D`; cut formula `A(d)` = `hthread1`'s `B=seqSucc(fstIdx d0)` disjunct).
- **Genuine reduct objects**: sequent ops `seqSetSucc s C` (`Θ→C`), `seqAddAnt A s` (`A,Θ→D`) +
  `inAnt_seqCons`/`inAnt_seqAddAnt`; `iCritReductG` (named genuine critical reduct) + read-outs;
  **`ZDerivation_iCritReductG_of`** — `iCritReductG` is a `ZDerivation` MODULO exactly two named
  residuals (see NEXT). Splice (5.2.1) prereqs: `znth_seqCons_seqUpdate_top/_lt`, `lh_seqCons_seqUpdate`.

Judge blueprint (`Crux2Blueprint.lean`, judge seed): elaborates clean vs HEAD (zero signature drift),
imported into `GoodsteinPA.lean` (green-gated, not an island). Discharged its **connectives**
(`ZDerivesEmpty_red`, `ZDerivesEmpty_red_iterate`, `iord_red_iterate_descends`) — now PROVEN from the
deep leaves. Blueprint sorries 8→6.

## ▶ NEXT (priority — RedSound critical case is reduced to TWO residuals; see PENDING lap-84 FINAL box)
1. **R2 — auxiliary IH** (independent of re-point; pure Thm-3.4(a)): extract `A(d)` from the redex
   (`A_i = chainAsucc ds i`; `^∀ p ⟹ A(d)=substs1 (num k) p`, `inegF q ⟹ A(d)=q`), prove the two
   auxiliaries `zK (seqSetSucc s (A(d))) r ds0` / `zK (seqAddAnt (A(d)) s) r ds1` are `ZDerivation`s of
   those endsequents (d{0}'s `isChainInf` uses distinguished premise `j0'=i`). Rank drop BANKED
   (`irk_cut_lt_rank_forall`/`_neg`).
2. **R1 — the `ZPhi` re-point** `zKValid → zKValidF` (the `hZPhiK` hypothesis of
   `ZDerivation_iCritReductG_of` made real, ~6 sites — blast radius measured lap-82). ⚠️ INTERLOCKED:
   `zDerivation_zK_inv` then yields only `zKValidF`, breaking `iord_descent_iR2_zK_of_valid` (uses
   criticality to FIND the redex). Must land WITH a descent that supplies `zKCritical` separately at the
   reduction site (`zKValid_iff_zKValidF_and_zKCritical` is the bridge). Do as a FOCUSED turn.
3. **M1a `red`** (5-case reduct, Buchholz Def 3.2) + `fstIdx_red`; critical case = `iCritReductG`.
   **M1b `redSound`** = the D₁ structural induction (uses the banked per-case validity above) ∥ banked
   D₃. Then blueprint connectives are already done ⟹ `iord_red_iterate_descends` is live.
4. **M2 `foundation_bot_to_Z_empty`** (Bryce–Goré `Peano.v` B1–B3, PA-induction→Z `Ind`).
   **M3 `false_of_ZDerivesEmpty`** (internalize `n↦iord(red^[n] z)` as Σ₁ graph `gentzenDescentφ` via
   the now-proven `iord_red_iterate_descends`) ⟹ `GentzenCon.gentzen_descent_of_inconsistent` ⟹ headline
   via existing `Reduction.lean` scaffolding (no new top-level wiring).

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. `lake build GoodsteinPA.Crux2Blueprint` 🟢 (6 leaf sorries).
src/ sorries: `Statement.lean:22`, `Reduction.lean:66`, `DescentSemantic.lean:550` (the 3 crux) +
6 disclosed blueprint leaves (M1a/M1b/M2/M3). `RedSound` is a `Prop`, not a sorry.

Aristotle: idle (Foundation not in its mathlib-v4.28 env; all crux-2 work is Foundation-internal).
Judge: in judge mode, will check after a few laps whether `red`/`redSound` materialize as genuine
(not iR2 re-skins) + run the deferred `#print axioms PA_delta1Definable` kernel check between laps.

## Commits this lap (branch `plan`, 520c058..891bbf8)
isChainInf_seqUpdate · zKValidF_seqUpdate · zKValidF_seqUpdate_iR2 · isChainInf_congr ·
isChainInf_iCritReductSeq · zKValidF_iCritReductSeq · splice read-outs · seqSetSucc/seqAddAnt ·
zKValidF_iCritReductGen · iCritReductG + ZDerivation_iCritReductG_of · blueprint import ·
blueprint connectives discharged · (+ 2 PENDING/docs).
