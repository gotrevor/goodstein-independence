# HANDOFF — 2026-07-02, lap 174 (SPIKE-Z1: the `Zᵉ` seam + Σ₁-instance spike)

**Verdict: PASS — T-Z1 does NOT fire.** `SPIKE-Z1-VERDICT.md` + `wip/SpikeZ1Seams.lean`
(green, exactly 2 sorries = the two disclosed §3 statement pins; no new `axiom`s, no
`src/` edits). Read the verdict first; it is the binding record.

## What happened

- **Q1 (both W4B seams REVERSE, kernel-proven at the seams):** seam 1 — the arm's only
  output obligation is `Cl`-membership of `osucc (αf + γf)`, derived by 2 constructor
  steps (`seam1_membership_absorbed`); seam 2 — the reduction-output class over the
  branch-unbounded `ω·(n+1)` family re-enters `Zeh.allω` sorry-free
  (`probe_allomega_reassembly_Zeh`, `two_level_config_Zeh`). Rail held (no sorried
  membership anywhere; pins consumed only by `probe_cut_all_arm_Zeh`).
- **Q2 (read-off PROVEN, not just statable):** `readoff_sigma1`/`headline_readoff` —
  the BW87 Bounding-Lemma analog at rank 0, ∀-free Σ₁ shape, witness `≤ hardy e m` from
  the judgment's control+stage, per-instance, no evaluator. **The "Σ₁-definability of
  concrete `H`" headline risk DISSOLVES**: the read-off consumes no `H`-data.
- **Kernel findings forcing amendments:** (K1) `Cl_of_NF` — any set closed under the
  pin's 4 ops contains every NF `ONote` (membership is information-free at ε₀);
  (K2) `mono_e_membership_gate_refuted` — `raise (ofNat 5) 1 = ω` and
  `hardy ω 0 = 1 < 5`: membership CANNOT gate `mono_e`; (K3) norm-balls aren't
  `+`-closable. → **A1**: `exI`'s designated argument is a judgment-carried STAGE `m`
  (`H ∩ ℕ` designation refuted); **A2**: step motives hold IHs at ONE control
  (per-branch raise-then-unify has no `Zeh` analog).
- **§6 (the real fork shape, landed mid-session):** Eguchi–Weiermann arXiv:1205.2879
  (operator supplied the PDF live; `papers/eguchi-weiermann-2012-operator-controlled-id1.{pdf,md}`,
  host commit `f411cf4`) — the literature's PA-level operator control carries a
  NUMBER-THEORETIC FUNCTION `f : ℕ→ℕ` (witness ≤ `f 0`; ω-premises at `f[N(ι)]`
  max-adjunction; **cut-reduction composes `f ∘ g`**). Kernel-checked the absorbing
  algebra: `rel1_comp` (relativization commutes with composition, axiom-free),
  `seam2_function_slot_payable`, `seam1_bump_absorbed_by_composition` — the non-affine
  function-slot is what the `(k,d)` counter could never be.

## Next (rebuild lap 1, gated on judge ratification)

1. Re-pin the reduction/step statements on **function-valued slots** (E–W form: compose
   at principal cuts, max-relativize at ω-nodes, `hardy e` at the root) folding A1/A2.
2. `mono_e` redesign: once-per-pass raise, per-instance hardy-domination certificates
   (W4B caveat (ii) still open at the pin). Fallback ALREADY validated: don't thread —
   read off cut-free (BW87 discipline; `readoff_sigma1` is that read-off, proven).
3. Wants: Weiermann BSL 12(2) 2006 (PA proper), Buchholz MLQ 47 (2001) finitary
   treatment (the in-kernel-friendly shape) — flagged in the paper summary.

Estimate unchanged ~7–11 laps; risk moved off Σ₁-of-`H` onto witness-threading design
(bounded by the validated fallback). Do NOT start re-threading before the judge pass.
