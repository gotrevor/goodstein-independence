# HANDOFF — 2026-07-02, global lap 183 = REBUILD-Z lap 3 (resolution (2) crux verified; output slot is `g∘f`)

> Baton filename uses the GLOBAL lap number (183). The REBUILD-Z sub-campaign "lap 3" is the
> DIRECTION laps-2–4 counter.

> **CHECKPOINT:** branch `plan`, build 🟢 (`lake build`, **1333 jobs**), headline
> `peano_not_proves_goodstein` = `[propext, Classical.choice, goodstein_implies_consistency,
> Quot.sound]` — NO DRIFT. §6 seam probes green. src pins unchanged from lap 2 (the lap-2
> escalation left them; resolution (2) is a LOCK §1 amendment, not a grind-lap edit).

## What this lap settled (the crux advance)

Lap 2 localized pins 1–2 to ONE gap (principal-`exI` running-family stage can't be lowered) and
named **resolution (2)** — a function-slot `exI` bound in the judgment (faithful E–W shape) — as
the fix, but flagged it "viability unverified, architect-level."  **This lap verified resolution
(2)'s crux in-kernel and corrected the composition order.**

Decisive kernel probe: `wip/ZefResolutionProbe.lean` (compiles clean, all lemmas axiom-clean).
Finding: `REBUILD-Z-LAP3-FINDING-2026-07-02-gof-order-resolves-slot-reduction.md`.

**The pins' output slot `f∘g` is BACKWARDS; the correct order is `g∘f`.**  E–W Lemma 25's
`f∘g` = `(¬C-slot)∘(C-slot)` = `(∀-family-slot)∘(∃-side-slot)`; the pins name the ∀-family slot
`g` and the ∃-side slot `f`, so E–W's `f∘g` = **`g∘f` in pin naming**.

- `reslot_fog_FAILS` `[propext,choice,Quot.sound]` — with `f = hardy ω = 2x+1`, `g = x²+2x+1`
  (both `NormControlled` at `(ω,0)`), witness `n = 1 ≤ f 0`: family budget `(rel1 g n) 0 = g 1 = 4`
  overflows `(f∘g) 0 = f(g 0) = 3`.  The `f∘g` re-slot is impossible.
- `reslot_gof_family` `[propext]` + `reslot_gof_exside` — the `g∘f` output DOMINATES both cut
  premises for ANY monotone+inflationary (hence any `NormControlled`) slots: `rel1 g n ≤ g∘f`
  (given `n ≤ f 0`) and `f ≤ g∘f`.  Re-slot by a plain `mono_f`.
- `gof_normControlled` — `NormControlled (g∘f) e m` still dischargeable (`normControlled_comp_running`,
  roles swapped).

**Whole reduction ports, not just the cut.** Lap-2 `redDeriv` already closes every case but the
principal `exI` in the STAGE calculus; those port mechanically (slot passes through like the
stage).  The two axis-critical points both hold: principal `exI` closes via the `g∘f` re-slot;
the `allω` reassembly threads because `rel1_comp : rel1 (g∘f) n = g ∘ rel1 f n` (ALREADY in
`OperatorZeh.lean:642`) re-expresses each branch's composed output slot `g ∘ rel1 f n` as exactly
`rel1 (g∘f) n` — the slot the output ω-node's `n`-th branch must carry.  So **resolution (2)
makes pins 1–2 TRUE with arbitrary `NormControlled` slots** — no E–W `(f.1)/(f.2)` growth class
needed for the REDUCTION (only the composition order `g∘f`).  The growth class is still needed
for pin 3's iterate (Lemma 30) — untouched.

## NEXT (the concrete next step — no judge ruling required to START the wip capstone)

The definitive proof that pins 1–2 are provable is now a bounded build:

1. **Build the minimal slot calculus `Zef` in `wip/`** — clone `Zeh` with the stage `m` replaced
   by a carried slot `f : ℕ → ℕ`: `exI` bound `n ≤ f 0`; `allω` branch slot `rel1 f n`; other
   rules pass the slot through.  Prove `mono_f` (pointwise-`≤` slot weakening; `exI` via
   `n ≤ f 0 ≤ f' 0`, `allω` via `rel1_mono`), `change_H`, `weakening`, `allInv_Zef` (port
   `allInv_Zeh`, `max m n₀ → rel1 f n₀`, `adjoin` unchanged).
2. **Port `redDeriv` → `redDeriv_slot`** with output slot `g∘f`; the principal `exI` closes with
   the `g∘f` re-slot (this lap's `reslot_gof_*`), the `allω` with `rel1_comp`.  Target: the FULL
   reduction sorry-free in the slot calculus = kernel proof pins 1–2 are provable.
3. **Hand the working `Zef` calculus to the architect** as the concrete LOCK §1 amendment to
   ratify (carry the slot, `exI` bound `n ≤ f 0`, `allω` branch `rel1 f n`, pins output `g∘f`),
   then port into `src` behind the ratification.

If the wip capstone (steps 1–2) closes sorry-free, that IS the discharge of pins 1–2 modulo the
mechanical `src` migration — a major crux advance, and it removes the "viability unverified"
caveat entirely.

## UPDATE (same lap, continued): the reduction now CLOSES SORRY-FREE in the slot calculus

`wip/ZefSlotCalculus.lean` — the minimal slot calculus `Zef` (`Zeh` with stage `m` ⤳ slot
`f : ℕ → ℕ`) + **`redDeriv_slot`**, the FULL running-family §19.6 cut-reduction, output slot
`g∘f`, **closing SORRY-FREE** (`#print axioms redDeriv_slot = [propext, Classical.choice,
Quot.sound]`, NO `sorryAx`).  This is the lap-2 gap — "unprovable for `e > 0`" in the
fixed-`hardy e m` calculus — now DISCHARGED as a real theorem in the resolution-(2) calculus.
Built: inductive `Zef`, `Zef.mono_f` (slot weakening), `Zef.change_H`, `ZefProv`, the re-slot
domination facts (`reslot_family`/`reslot_exside`, `rel1_monotone`/`rel1_infl`), and the reduction
port.  Slots carried monotone + inflationary (every `NormControlled`/E–W operator is; `rel1`, `∘`
preserve both) — threaded through the induction as the sole extra bookkeeping vs the stage port.

**So pins 1–2 are PROVABLE — BOTH now mirrored SORRY-FREE in the slot calculus.**
`wip/ZefSlotCalculus.lean` now carries the COMPLETE slot-calculus mirror, all axiom-clean
(`[propext, Classical.choice, Quot.sound]`, no `sorryAx`):
- `redDeriv_slot` — pin 1's core (`cutReduceAllAuxRunning_Zf`'s first conjunct = lap-2 `redDeriv`).
- `allInv_Zef` — ∀-inversion (port of `allInv_Zeh`, `max m n₀ → rel1 f n₀`; operator threading
  free via `mono_Hf`/`change_H`; `exI` bound raise needs `f` monotone).
- `stepAllω_Zef` — pin 2 (`stepAllω_Zf`): invert the ∀-side → `redDeriv_slot`, output slot `g∘f`.
- supporting: `Zef` inductive, `Zef.mono_f`, `Zef.change_H`, `Zef.mono_Hf`, `ZefProv`,
  `reslot_family`/`reslot_exside`, `rel1_monotone`/`rel1_infl`.
- `readoff_sigma1_Zef` + `headline_readoff_Zef` — the §3 EXIT ported to the slot calculus: a
  rank-0 `Zef` derivation of `{∃⁰φ}` yields a witness `n ≤ f 0`.  Closes the end-to-end viability
  loop (reduction → inversion → step → EXIT, all sorry-free) AND matches E–W's Witnessing Lemma
  31 (`witness ≤ f(0)`) VERBATIM — the slot IS the read-off budget (vs `Zeh`'s `hardy e m`, the
  canonical slot at 0).  Independent of cut-elimination / pin 3.

The remaining REAL work is NOT math — it is the architect's LOCK §1 amendment (carry the slot;
`exI` bound `n ≤ f 0`; `allω` branch `rel1 f n`; pins output `g∘f`) + the mechanical `src`
migration behind that ratification.  The math obstruction the lap-2 escalation flagged is GONE:
the running-family reduction AND the ∀-inversion feeder both close sorry-free.

### NEXT (revised)
1. Hand the working `Zef` calculus (`wip/ZefSlotCalculus.lean`) to the architect as the concrete
   LOCK §1 amendment to ratify (slot judgment, `exI` bound `n ≤ f 0`, `allω` branch `rel1 f n`,
   pins output `g∘f`, `NormControlled` slots are monotone+inflationary — the two carried side
   conditions).
2. Behind ratification: migrate `Zeh → Zef` in `src/GoodsteinPA/OperatorZeh.lean`, discharge
   pins 1–2 sorry-free (the wip proofs port verbatim modulo namespace), retire the lap-2 escalation.
3. Pin 3 (`cutElimPass_Zf`, Lemma 30 iterate) stays the lap-5 entrance gate — untouched; that is
   where the E–W `(f.1)/(f.2)` growth class + control-raise live.

## Resume-state invariants (do NOT re-derive / re-attempt)

- The fixed-`hardy e m` STAGE bound genuinely cannot carry the running-family reduction (lap 2,
  kernel-localized).  The fix is the SLOT judgment (resolution 2), NOT a stage-growing restatement
  (resolution 1 fails: the stage-bump doesn't commute past `allω` — see lap-2 finding) and NOT
  grinding the gap as written.
- The reduction output slot is **`g∘f`** (∀-family slot ∘ ∃-side slot), NOT `f∘g`.  `f∘g` is
  kernel-refuted (`reslot_fog_FAILS`).
- `rel1_comp` (src `OperatorZeh.lean:642`) is the reassembly brick that threads the composed slot
  through `allω` — already banked, anticipated this exact need.
- Pin 3 (`cutElimPass_Zf`) discharge as written stays FORBIDDEN (vacuous `∃ f'`, lap-5 mini-lock).
- Route-A / Δ₀ read-off extension stay FORBIDDEN.
- Lap-2's `change_H`, `normControlled_comp_running`, `principal_witness_exceeds_stage` stand.

## Kernel evidence (real `#print axioms`, build 🟢 1333)

```
reslot_fog_FAILS          [propext, Classical.choice, Quot.sound]   -- f∘g refuted
reslot_gof_family         [propext]                                  -- g∘f dominates family
reslot_gof_exside         [propext]  (trivial)                       -- g∘f dominates ∃-side
gof_normControlled        [propext, Classical.choice, Quot.sound]    -- conjunct dischargeable
peano_not_proves_goodstein [propext, Classical.choice, goodstein_implies_consistency, Quot.sound]  -- NO DRIFT
```
