# HANDOFF — lap 109 · the K-case branch-descent TRIO banked + critical sub-branch wired in place

> **RESUME BATON.** Branch `plan`, HEAD `7371573`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`,
> **1326**). Headline footprint intact, re-verified in-kernel (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms; lone
> `sorryAx` = crux-2); `iord_descent_red = [propext, sorryAx, Classical.choice, Quot.sound]`. 3 commits this
> lap (`9e86a26` critical sub-branch, `8138b91` replace descent, `7371573` splice descent).

## What landed this lap (109)

### 1. `iord_descent_red`'s K-case CRITICAL sub-branch — PROVEN IN PLACE (`9e86a26`)
`iord_descent_red` (`Crux2Blueprint.lean:540`) now dispatches the K/cut case on the `permIdx` criticality
sentinel. The **critical branch** (`¬ permIdx < lh ds`) is discharged by the banked
`iord_descent_red_zK_crit` (`RedZKDescent`), with full `zKValid` assembled from the `ZDerivation`-supplied
`zKValidF` + `zKCritical_of_not_permIdx_lt hcrit`. **This resolves lap-108's flagged "zKValid-from-orbit"
gating concern: criticality is supplied FREE by the branch dispatch — no "wire `zKValid` into `ZPhi`" phase
needed for the critical brick.** Crux2Blueprint now imports RedZKDescent. The residual `sorry` is the
NON-critical (splice/replace) branch.

### 2. The two NON-critical branch descents — BANKED as `_eq` lemmas (`8138b91`, `7371573`)
Both in `RedZKDescent.lean`, axiom-clean `[propext, Classical.choice, Quot.sound]`, green-gated. Each
reduces the `red`-level `iord` descent to its recursive sub-facts:

- **`iord_descent_red_zK_replace_eq`** (case 5.2.2). Both replace dispatches (`red_zK_rep` /
  `red_zK_rep_nonchain`, same `K^r(i/red dᵢ)` reduct shape) ⟹ descent GIVEN the premise IH
  `iRedDescent (red dᵢ) dᵢ`. Via `iotil_zK_lt_replace` (F1 strict `#`-mono) + `idg_zK_le_replace` +
  `iord_descent_le`. Stated against the explicit reduct equation `hred`, so one lemma covers both dispatches.
- **`iord_descent_red_zK_splice_eq`** (case 5.2.1). The splice reduct `K^{r'}(seqInsert ds i a b)`
  (`a,b = dᵢ`'s critical-reduct halves, `r' = max{rk(A(dᵢ)),r}`) ⟹ descent GIVEN each half's `õ`/`idg` bound
  below `dᵢ` (`ha/hb/hag/hbg`) + the faithful rank bound `r' ≤ dg(parent)` (`hr'`). Via the banked
  rank-general `iord_descent_seqInsert'` (F2 rotation kernel `iotil_seqInsert_lt` + `idg_seqInsert_le'`).

**Net:** the K-case branch-descent TRIO is complete — critical (`zK_crit`), replace (`replace_eq`), splice
(`splice_eq`) — each collapsing the genuine reduct's `iord` descent to its IH/sub-facts. What remains is
purely the RECURSION that supplies those sub-facts.

### 3b. Non-critical K-branch — three reducible sub-cases CLOSED in place (`afb4b69`, `b0a3732`)
`iord_descent_red`'s non-critical K-branch now dispatches on the selected premise's tag and closes the
non-recursive reducible cases by feeding banked `iRedDescent` bundles to `iord_descent_red_zK_replace_eq`:
**I¬** (`red dᵢ=d0`, `iRedDescent_zIneg`), **Ind** (`red dᵢ=iRInd dᵢ`, `iRedDescent_zInd`), **I∀**
(`red dᵢ=zsubst d0 a 0`, NEW `iRedDescent_red_zIall` — eigensubst-invariant bundle, no regularity). The
residual `sorry` is now FOUR documented sub-cases: **atom/axAll/axNeg** (the FIXPOINT defect, `red dᵢ=dᵢ`)
and **chain** (the recursive core, needs the strong-induction IH via `replace_eq`/`splice_eq`).

### 3. The atom-selection FIXPOINT defect — FORMALIZED (`123fbcd`)
`red_zK_fixpoint_of_atom_selected` (`RedZKDescent`, axiom-clean): when `permIdx` selects a bare
identity-atom premise, `red (zK s r ds) = zK s r ds` — a genuine fixpoint. This pins the recursion wall in
kernel: an atom-selected node has NO `iord` descent, the orbit stalls. See `PENDING_WORK.md` lap-109 for the
three resolution paths (refined: the `isymRep` tag conflates atoms with reducible Ind/chains; the fix is an
`isPermPrem`/`permIdx` engine refinement to skip atoms, or an atom-free embedding).

## NEXT (hardest-first) — supply the branch IHs by restructuring `iord_descent_red` as a strong induction
The non-critical `sorry` (`Crux2Blueprint.lean:556`) needs the premise descents fed to the two `_eq` lemmas.
1. **Strong-induction restructure.** Re-prove `iord_descent_red` (or a general `iRedDescent (red d) d` over
   ZDerivation+regular) by `zDerivation_induction` (mirror `redSoundGen`, `Crux2Blueprint.lean:456`), so the
   IH `iRedDescent (red dᵢ) dᵢ` is available for the structurally-smaller premise `dᵢ = znth ds (permIdx)`.
   - REPLACE: feed the IH straight to `iord_descent_red_zK_replace_eq`.
   - SPLICE: `dᵢ` is a critical chain (`zTag dᵢ = 4` ∧ critical), so `red dᵢ = iRcritG dᵢ ρ`; the two halves
     `a,b` are its critical-reduct premises. Their per-half `õ`/`idg` bounds below `dᵢ` (`ha/hb/hag/hbg`) are
     intermediate facts of `iord_descent_red_zK_crit`/`iord_descent_iRcrit_of_chain'` — extract them as a
     reusable "critical halves descend" lemma. The rank bound `hr' : r' ≤ dg(parent)` is the genuine
     residual (the cut-rank `rk(A(dᵢ))` is absorbed by `dg(d) = max(r,…)` — provable from `zKValidF`'s rank
     conjunct `hrank`).
2. **The one genuine SUBTLETY (settle in-kernel, prove or refute).** The IH's STRICT `otil_lt` requires the
   selected/permissible premise `dᵢ` to be REDUCIBLE — a normal-form leaf (`tp = isymRep` atom, or
   axiom tag 5/6) has `red dᵢ = dᵢ` ⟹ NO strict descent ⟹ replace would STALL. Whether `permIdx`
   (= least `i` with `isPermPrem ds s i := iperm (tp dᵢ) s`) can ever select such a leaf is the crux: it
   should be excluded by the `zKValidF`/`isChainInf` chain structure (the chain's permissible premise is the
   one carrying the cut formula non-principally — an R/L inference, not a bare axiom). Verify against
   `isChainInf` + `iperm`/`tp` of leaves; this is shared with `redSoundGen`'s `redZKReady` motive wall.
3. **`redZKReady` motive strengthening** (`Crux2Blueprint.lean:488/493` sorry; `redZKReady` def at 340).
   Soundness `ZDerivation (red d)` for the K-case needs the 7-field per-node orbit-invariant bundle carried
   hereditarily. SHARED wall with the descent recursion (both need `red dᵢ` to be a genuine ZDerivation that
   strictly descends). The lone localized residual inside is **axNeg** (`ZDerivation_red_zK_nonRep`,
   `Crux2Blueprint.lean:404`): a ¬-axiom premise reduct is a SUCCEDENT replacement (`tpReduce (tp zAxNeg) s 0
   = seqSetSucc s p`, `Γ→p`) needing Buchholz's genuine ¬-axiom cut (premise restructuring), not the
   membership-`isChainInf` route. Genuinely deep — needs the Buchholz Def-3.2 ¬-axiom case faithfully.
4. Then `false_of_ZDerivesEmpty` (`Crux2Blueprint.lean:619`) via `iord_red_iterate_descends` + PRWO; the
   embedding `foundation_bot_to_Z_empty` (`607`); then wire crux-1 ∘ crux-2 →
   `Reduction.goodstein_implies_consistency` → headline. ONLY when `#print axioms` clean.

## Aristotle
Idle — CORRECT. The branch IHs / rank bound / leaf-reducibility are entangled with the `InternalZ` HFS
engine API (`isChainInf`/`iperm`/`tp`/`zKValidF`), not self-contained mathlib lemmas.

## Kernel state
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — 0 math axioms.
- `iord_descent_red_zK_crit` / `_replace_eq` / `_splice_eq` = `[propext, Classical.choice, Quot.sound]`.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` = `[propext, Classical.choice, Quot.sound]`.
