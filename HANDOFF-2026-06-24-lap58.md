# HANDOFF — 2026-06-24 (lap 58) — crux 2 reframed + Buchholz ord/R grounded + natural-sum brick started

> **Branch** `plan` · **HEAD `a2ede6d`** · tree **clean** · `lake build GoodsteinPA` **green (1318 jobs)**.
> `wip/GentzenCon.lean` + `wip/InternalNadd.lean` both `lake env lean` **green, 0 sorries**.
> Headline `peano_not_proves_goodstein` untouched (honest `sorry`, anti-fraud intact).
> Best-case honest endpoint unchanged: `[propext, choice, Quot.sound, PA_delta1Definable]`.
>
> 5 verified commits this lap (`21a7318`…`a2ede6d`). All crux-2 work.

## Lap-58 arc (3 fronts, all committed green)

### 1. Crux 2 reframed to the model-theoretic route (`21a7318`)
`gentzen_reduction_internalized` (was an OPAQUE object-level **axiom** `𝗣𝗔 ⊢ (prwoInstance gentzenDescentφ
🡒 Con(𝗣𝗔))`) is now a **THEOREM**, proved via Foundation completeness `provable_of_models 𝗣𝗔` +
`Semantics.Imp.models_imply` + `Bootstrapping.consistent.defined (T := 𝗣𝗔) (V := M)`, from a clean
**per-model semantic axiom** `gentzen_descent_of_inconsistent` (`¬𝗣𝗔.Consistent M` ⟹ the gentzen descent
everywhere `icmp`-descends in `M` — Gentzen eq-(5) at model level). The assembly
`goodstein_implies_consistency_via_gentzen` `#print axioms` = `[propext,choice,Quot.sound,
PA_delta1Definable]` + **4 crux-2 axioms** (count unchanged; deep axiom swapped to the proven crux-1 shape):
`gentzenDescentφ`, `gentzenDescentφ_dominated`, `gentzenDescentφ_realized`, `gentzen_descent_of_inconsistent`.

### 2. Buchholz ord/R grounded VERBATIM + calculus-mismatch finding (`9697cdf`, `cbe30d1`)
`CRUX2-ORD-ASSIGNMENT-2026-06-24.md` (text `scratchpad/buchholz-gentzen.txt`): §4 assignment
`o(d) = ω_{dg(d)}(õ(d))`, the `õ`/`dg` recursions (I∀/I¬/Ind/K rules), §2 consistency argument.
**⚠️ LOAD-BEARING:** Buchholz's `ord`/`R` are over **his system Z** (`K^r`/`Ind`/`Rep`/`tp`/`d[n]`),
NOT Foundation's **Tait+cut** `Theory.Derivation`; `Hauptsatz` is meta-level (no shortcut). ⟹ **Route A**:
arithmetize system Z + a translation `𝗣𝗔-Tait-⊥-deriv → Z-⊥-deriv`. The C0–C5 milestones are in the doc.

### 3. Crux-2 brick 1 — internal natural (Hessenberg) sum `inadd` (`7669f64`, `a2ede6d`)
`õ` needs the natural sum `#` (ordinary `iadd` absorbs smaller leading terms, ≠ `#`). New file
`wip/InternalNadd.lean` (`lake env lean` green, axiom-clean `[propext,choice,Quot.sound]`):
- **Equational core (`7669f64`):** `insTerm e n b` (insert `ω^e·n` into NF `b`) + `inadd a b =
  insTerm (ocExp a)(ocCoeff a)(inadd (ocTail a) b)`, two single-arg course-of-values recursions
  mirroring `iomul`/`iadd`. Recurrences `insTerm_ocOadd`/`inadd_ocOadd`/`*_zero`. Both fully
  `𝚺₁`-definable (tables + `*_defined`/`*_definable`/`*_definable'`).
- **Property helpers (`a2ede6d`):** `icmp_tri` (comparison ∈ {0,1,2}, order-induction via
  `cmpV_tri`/`thenV_tri`) + `icmp_eq_zero_of_ne`; `insTerm_pos`/`insTerm_ne_zero`; `ocExp_insTerm`
  (lead exp = `e` unless `e ≺ lead-exp b`, then `ocExp b`).

**GOTCHAS banked (PENDING):** open `LO.FirstOrder.Arithmetic.HierarchySymbol` for
`DefinableFunction₂/₃.comp`/`Definable.comp₁`/`ball_le`; 2-param `PR.Blueprint` `resultDef` natural order
is `(result, index, param₀, param₁)` ⟹ `insTermTableDef := …resultDef.rew (Rew.subst ![#0,#3,#1,#2])`;
V-numeral disequalities (`0≠1`,`0≠2`,…) discharge by `simp` (not `decide`/`omega`).

## NEXT LAP (hardest-first, all `wip/InternalNadd.lean`, then promote to `src/`)
1. **`isNF_insTerm`** (`isNF e → n≠0 → isNF b → isNF (insTerm e n b)`) — order-induction on `b`; the
   `e≺e'` branch's NF side-condition `icmp (ocExp(insTerm e n r')) e' = 0` follows from `ocExp_insTerm`
   (lead-exp of `insTerm e n r'` is `e` or `ocExp r'`, both `≺ e'`). NF char: `isNF_ocOadd`
   (`n≠0 ∧ isNF ec ∧ isNF rc ∧ (rc=0 ∨ icmp (ocExp rc) ec = 0)`). icmp antisym: `icmp_swap`/`oswap`;
   eq: `icmp_eq_imp_eq` (bounded).
2. **`isNF_inadd`** (fold `isNF_insTerm` over `a`'s terms, order-induction on `a`).
3. **`icmp`-monotonicity of `#`** (commutativity + strict monotone in each NF arg) and **`iC_inadd`**
   (`iC (a # b) ≤ iC a + iC b`, mirror `iC_iadd`-style). These are what `õ`'s descent (Lemma 4.1) consumes.
4. Then **promote `InternalNadd` → `src/`** (add to `GoodsteinPA.lean` aggregator) and start **C0**
   (arithmetize system Z) → **C1** (`iõ`/`idg`/`iord` by strong-recursion, using `inadd` + ω-tower).

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/` is wip. Aristotle OFFLINE. WebFetch dead. ⚠️ NEVER `git checkout <wip file>` — `cp` from scratchpad.
