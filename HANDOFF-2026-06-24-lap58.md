# HANDOFF — 2026-06-24 (lap 58) — CRUX 2 REFRAMED to the model-theoretic route (matches crux 1)

> **Branch** `plan` · tree clean after commit · `lake build GoodsteinPA` **green (1318 jobs)**.
> `wip/GentzenCon.lean` `lake env lean` **green, 0 sorries**. Headline `peano_not_proves_goodstein`
> untouched (honest `sorry`, anti-fraud intact).
>
> **Lap-58 landmark:** `gentzen_reduction_internalized` (was an opaque object-level **axiom**
> `𝗣𝗔 ⊢ (prwoInstance gentzenDescentφ 🡒 Con(𝗣𝗔))`) is now a **THEOREM**, discharged via Foundation's
> arithmetic completeness (`provable_of_models`) from a clean **per-model semantic axiom**
> `gentzen_descent_of_inconsistent`. This puts crux 2's deep content in the *same* model-theoretic shape
> that the proven crux-1 route uses — the right shape for the `ord`/`R` arithmetization to plug into.
> The assembly `goodstein_implies_consistency_via_gentzen` `#print axioms` is unchanged in count:
> `[propext, choice, Quot.sound, PA_delta1Definable]` + **4 crux-2 axioms**, but the deep one swapped:
> `gentzenDescentφ`, `gentzenDescentφ_dominated`, `gentzenDescentφ_realized`,
> **`gentzen_descent_of_inconsistent`** (replacing `gentzen_reduction_internalized`).
>
> ⚠️ FirstOrder object-level implication arrow is `🡒` (not `➝`); `⨀` = modus ponens on `𝗣𝗔 ⊢`.
> Foundation API used: `provable_of_models 𝗣𝗔`, `Semantics.Imp.models_imply`,
> `Bootstrapping.consistent.defined (T := 𝗣𝗔) (V := M)` (`↔ 𝗣𝗔.Consistent M`),
> `ModelsTheory.of_provably_subtheory' M 𝗜𝚺₁ 𝗣𝗔`.

## What changed (lap 58)
`wip/GentzenCon.lean` Step-3 block (lines ~166–212):
- **NEW per-model axiom** `gentzen_descent_of_inconsistent` — for every `M ⊧ₘ* 𝗜𝚺₁`, `M ⊧ₘ* 𝗣𝗔`:
  `¬ 𝗣𝗔.Consistent M → ∀ n y z, gentzenDescentφ[y,n] → gentzenDescentφ[z,n+1] → icmp z y = 0`.
  (= Gentzen eq-(5) at the model level: an `M`-internal `⊥`-derivation ⟹ the descent
  `n ↦ ord(Rⁿd₀)` is everywhere `≺`-descending in `M`.)
- **NEW theorem** `gentzen_consistent_of_prwo (M) [IΣ₁] [PA]` — contrapositive: `M ⊧ prwoInstance
  gentzenDescentφ → 𝗣𝗔.Consistent M`. (`by_contra` + `prwoInstance_models_iff` + the axiom.)
- **`gentzen_reduction_internalized` is NOW A THEOREM** — `provable_of_models 𝗣𝗔 _`, then
  `Semantics.Imp.models_imply` + `consistent.defined.mpr (gentzen_consistent_of_prwo …)`.
- Footer added: the **C1–C5 milestones** to discharge `gentzen_descent_of_inconsistent` (arithmetize
  `iord`/`iR` over `Theory.Derivation`; C3 = eq-(5) in `M` is the deep core). The 4 crux-2 axioms all
  collapse out of C1–C5.
- The meta-`ℕ` `ord`/`R`/`derivesEmpty`/`R_preserves_empty`/`ord_R_descends` axioms remain as
  documentation; **not** on the `#print axioms` path (confirmed).

## ALSO lap 58 — Buchholz ord/R GROUNDED + calculus-mismatch finding (`9697cdf`)
Extracted Buchholz §4 ord assignment VERBATIM (`o(d)=ω_{dg(d)}(õ(d))`) → `CRUX2-ORD-ASSIGNMENT-
2026-06-24.md` (+ `scratchpad/buchholz-gentzen.txt`). **Load-bearing finding:** Buchholz's `ord`/`R`
are over his **system Z** (`K^r`/`Ind`/`Rep`), NOT Foundation's **Tait+cut** `Theory.Derivation`;
`Hauptsatz` is meta-level (no shortcut). ⟹ **Route A**: arithmetize Z + translate `𝗣𝗔-⊥-deriv → Z-⊥-deriv`.
**First concrete brick next lap:** InternalONote **natural (Hessenberg) sum `inadd`** — the ONE missing
ordinal primitive `õ` needs (`ω^c`=`ocOadd c 1 0`; ω-tower=meta-iterate; `iadd`/`iomul`/`ibigMul` exist).
Build in a new `src/GoodsteinPA/InternalNadd.lean`, mirror the `iadd` lemma set, sorry-free + axiom-clean.
Then C0 (arithmetize system Z) → C1 (`iõ`/`idg`/`iord` by strong-recursion) → C2/C3 (`iR` + Thm 4.2).

## NEXT LAP (hardest-first) = the C1–C5 arithmetization (crux 2, 🟠 generational)
Discharge `gentzen_descent_of_inconsistent` by building, inside `M ⊧ 𝗣𝗔`:
1. **C1 `iord : M → M`** — `𝚺₁` ordinal assignment by `<`-strong-recursion on coded derivations
   (`Bootstrapping/Syntax/Proof/Basic.lean`: `cutRule`/`andIntro`/… with `d₁_lt_cutRule` giving the
   well-founded recursion; per-rule ε₀-code, cut ↦ `ω`-bump on cut-rank). Substrate = the repo's
   `PR.Construction` idiom (cf. `BlkRecF`/`iIter`) adapted to subterm recursion.
2. **C2 `iR : M → M`** + `iR`-preserves-`⊥` (`𝗣𝗔.DerivationOf d ⊥ → 𝗣𝗔.DerivationOf (iR d) ⊥`).
3. **C3 eq-(5) in `M`**: `𝗣𝗔.DerivationOf d ⊥ → icmp (iord (iR d)) (iord d) = 0` — THE deep core,
   ground in Buchholz [6] (`papers/buchholz-on-gentzens-first-consistency-proof.pdf` + `siders-*.pdf`).
4. **C4** NF/nonzero of `iord` on `⊥`-derivations (⟹ `gentzenDescentφ_realized`).
5. **C5** `gentzenDescentφ` = graph of `n ↦ iord(iR^[n] d₀)`, `d₀ := μ d. 𝗣𝗔.Proof d ⌜⊥⌝`.
First action: read `Basic.lean` `Theory.Derivation` recursion/inversion API (`case_iff`, the `induction`
principle line ~552, the `*_lt_*` subterm-code lemmas) and write a `wip/GentzenOrd.lean` scaffold for C1.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/GentzenCon.lean` is wip. Aristotle OFFLINE. WebFetch dead. Honest endpoint unchanged: best-case
headline `[propext, choice, Quot.sound, PA_delta1Definable]`.
⚠️ NEVER `git checkout <wip file>` to undo edits — `cp` from scratchpad.
