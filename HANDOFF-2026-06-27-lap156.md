# Handoff: lap 156 — §14.254b SUCCEDENT-THREADING COLLAPSE executed to exhaustion; zAxNeg producer CLOSED

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `eb878b7` · build 🟢 green (1326 jobs,
`lake build GoodsteinPA`). Re-verified in-kernel: headline `peano_not_proves_goodstein` =
`[propext, sorryAx, choice, Quot.sound]` (**0 math axioms**); `false_of_ZDerivesEmpty` =
`[propext, sorryAx, choice, Quot.sound]`. No drift. **No uncommitted edits.**

## ✅ DONE THIS LAP (7 commits — the directive's mandated collapse, executed to exhaustion)
1. **tag-6 sub-case (a) PROVEN** (`51312d2`): dual of lap-155's tag-5(a). `zDerivation_zAxNeg_inv` gives
   BOTH `inegF p' ∈ Γ'` and `p' ∈ Γ'`; thread both via `hthread0`; both in Γ → fresh §5 ¬-axiom `zAxNeg s p'`
   derives `Γ→⊥` directly, õ-dropping via `finHead_iotil_lt_iseqNaddIdg`.
2. **Succedent-threading COLLAPSE** (`d4ecee7`): new reusable `leastSucc_in_ant_or_nonleaf`
   (`InternalZ:~9065`) — general-Γ analogue of `chainAsucc_threaded_of_leaf` that RETURNS the `F ∈ Γ`
   disjunct (instead of killing it via `seqAnt s = ∅`). The LEAST-indexed premise `m ≤ n` concluding cut
   formula `F` is a LEAF (threads to Γ; leastness kills the earlier-premise disjunct) or NON-LEAF
   (`zTag ∉ {0,7}`). Wired via the shared `collapse` helper: tag-5 (`F=^∀⊥`) + tag-6 (`F=inegF p'` AND
   `F=p'`) sub-case (b) leaf cut-partner chains COLLAPSE to Γ → the proven sub-case (a) reduct.
3. **R-intro killed by `hnolow`** (`a0c1524`): new `pi2_tp_eq_seqSucc_of_pi1_zero` (a right-symbol premise
   carries its succedent as the `tp`-formula) + `rightSym_producer_redex` (a RIGHT-symbol producer
   `m < jstar` of a left-axiom major's cut formula `F` forms `isRedexPair ⟪m,jstar⟫ ≤ j0` → contradicts
   `hnolow`). Wired into tag-5 + tag-6-inegF. **The directive's "R-intro→`hnolow`" claim is now
   machine-verified: `^∀⊥` is never CREATED below j0, only threaded.** Collapse TESTED TO EXHAUSTION ⟹ the
   lap-136 general-succedent reduct is now UN-FORBIDDEN for the residual.
4. **zAxNeg PRODUCER base case CLOSED** (`7905f35`): generalized `collapse` to any `i ≤ j0` (thread a
   producer's own antecedent); `axNegCloseGen` (general `zAxNeg s q` reduct, witness index supplies õ);
   `tryProducerClose` dispatches a Rep producer by constructor — the `zAxNeg` producer (itself a ¬-axiom
   `Γₘ→anything` from `¬q,q∈Γₘ`) threads BOTH `¬q` and `q` to Γ → `zAxNeg s q` derives `Γ→⊥` directly.

## 🔻 The ONE open M1b-term leaf: `axMajorResidual` (`Crux2Blueprint:~3417`, inside `genReduct_chain_noRedex`)
**NARROWED from "any tag-5/6 cut-partner" to {zInd (3), zK (4), zAxAll (5)} producers + recursion.** It now
covers ONLY: a NON-LEAF producer `m < jstar` concluding the cut formula `F` with `zTag ∈ {3,4,5}` (the
zAxNeg=6 producer is CLOSED), PLUS the case where threading a zAxNeg producer's `¬q`/`q` hits a DEEPER
producer (the `_` discards in `tryProducerClose`). Net sorry count 1→1; the open core is now precisely the
deep general-succedent constructions (narrowed `Γ→^∀⊥` / `Γ→inegF p'` targets, NEVER "arbitrary C").

## 🎯 NEXT — close the {3,4,5}-producer residual (full tree in `PENDING_WORK.md` lap-156 top)
1. **zAxAll producer (tag 5) — the rank-climbing WEDGE.** `zAxAll s_m (^∀⊥) k_m` concluding `^∀⊥` has matrix
   `^∀⊥` (forced by `substs1 k_m p_m = ^∀⊥`), active formula `^∀(^∀⊥)` (rank+1) in its antecedent.
   `collapse m` threads it → `^∀(^∀⊥)∈Γ` (or deeper producer). Rank is bounded by `r` (`hrank0`), so the
   climb terminates at `^∀^k⊥ ∈ Γ`. Reduct = a 2-cut `zK [d_a, d_b]`: `d_a=zAxAll` derives `Γ→^∀⊥`,
   `d_b=zAxAll s ⊥ 0` derives `^∀⊥,Γ→⊥`; the cut composes to `Γ→⊥`. Build as `certReplace`/`certFlatten` +
   õ-descent of the 2-cut chain. **Start with the ONE-STEP case** (`^∀(^∀⊥)∈Γ` directly).
2. **zInd producer (tag 3) concluding `^∀⊥`** — `substs1 t p_ind = ^∀⊥` forces `p_ind=^∀⊥` (degenerate
   induction). The reduct is the lap-136 unfolding `⟨d0, d1[a:=0..k-1]⟩` (un-forbidden, narrowed to `Γ→^∀⊥`).
   CHECK FIRST whether `zIndWff` even ADMITS a sound zInd concluding a closed ∀-sentence (may be vacuous →
   free kill).
3. **zK producer (tag 4)** — sub-chain concluding `^∀⊥`; recurse the chain reduction at general succedent.
   Likely needs `genReduct_chain_noRedex`/`genReduct_botSucc` generalized off `seqSucc s = ⊥`. Hardest;
   defer behind 1+2.
4. **Optional: make `tryProducerClose` recurse (strong induction on `m`)** to close the zAxNeg-producer
   CHAIN (threading `¬q` hits another zAxNeg producer, etc.). Marginal — the index strictly decreases, so
   well-founded — but zAxAll/zInd/zK still block, so it doesn't drop the sorry alone.

**Closing all of {3,4,5} producers ⟹ `axMajorResidual` DROPS ⟹ `genReduct_chain_noRedex` LAST leaf gone ⟹
`genReduct_botSucc` PROVEN ⟹ outer `descent_step_K_noncrit_axMajor` + gDef collapse ⟹ M1b-term DONE.**

## ⚠️ Forbidden / do-NOT-undo (per DIRECTION lap-155, still binding)
The lap-136 general-succedent reduct was forbidden "BEFORE the collapse is tested to exhaustion" — that
test is now DONE (leaves→Γ, R-intro→hnolow, zAxNeg→closed), so it is UN-FORBIDDEN for the {3,4,5} residual,
but ONLY as the narrowed `Γ→^∀⊥` target, never "arbitrary succedent C". Still forbidden: `red` witnesses;
`iord`-recursion for construction (CODE induction via `zDerivation_sigma_induction` ONLY); `redLeast`/μ-min
for gDef; the refuted single-premise `seqUpdate` splice; attacking `descent_step_K_noncrit_axMajor`/gDef
STANDALONE; the off-path dead `red`-soundness sorries AS STATED; M2/M4 wiring. ALTITUDE CAUTION: M2
(Foundation→Z bridge) ~0% built — "only the crux left" ≠ "almost done."

## 📁 Key anchors (this lap's new assets)
- `leastSucc_in_ant_or_nonleaf` (`InternalZ:~9065`), `pi2_tp_eq_seqSucc_of_pi1_zero` (`~:9122`),
  `rightSym_producer_redex` (`~:9145`) — the collapse + R-intro-kill kit.
- `genReduct_chain_noRedex` (`Crux2Blueprint:3365`): in-body helpers `collapse` (`~:3433`, now `∀ i ≤ j0`),
  `axNegCloseGen` (`~:3443`), `tryProducerClose` (`~:3460`); `axMajorResidual` (`~:3417`); tag-5 branch
  (`~:3505`, `axAllClose` + `hjL`), tag-6 branch (`~:3540`, `axNegClose` + `hjLneg`).
- Ordinal kit: `finHead_iotil_lt_iseqNaddIdg` (`InternalZ:3353`), `iotil_zAxAll`/`iotil_zAxNeg` (`:2428/2433`).
- `DIRECTION.md` CURRENT DIRECTIVE (lap-155, binding) · `PENDING_WORK.md` lap-156 top · prior baton
  `HANDOFF-2026-06-27-lap155.md`.

---
**→ Next session: attack the zAxAll-producer rank-climbing 2-cut reduct (start one-step `^∀(^∀⊥)∈Γ` case),
or first CHECK zInd-concluding-`^∀⊥` derivability (possible free kill). Keep build green + headline 0 math
axioms.**
