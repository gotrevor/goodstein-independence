# REVIEW DIRECTIVE — goodstein-independence

> ⚠️ **WRITE-ACCESS: review & reflection (altitude) laps ONLY.** Grind laps READ this and work
> strictly within it; they MUST NOT edit it. The operator may also set it directly.
> This file **OUTRANKS** any `HANDOFF` "NEXT" pointer or in-flight campaign momentum — it is the
> binding strategic directive that holds *between* altitude laps. Keep it SHORT; detail lives in
> `PENDING_WORK.md` / the dated `REFLECTION-*.md`. This is a *current orders* layer, distinct from
> `DIRECTION.md` (the standing charter, rarely changes) and `HANDOFF-*.md` (per-lap tactics).

## CURRENT DIRECTIVE · set: pre-lap-135 (operator + judge)

**THE objective (only this):** **M1b-term** — resolve the `red`-engine **STALL / termination** of
crux-2. Make a `red`-fixpoint on a ∅→⊥ orbit yield a contradiction, so the ordinal descent
terminates and the fixpoint branch of `false_of_ZDerivesEmpty` (`Crux2Blueprint:1144`) closes.

**Success metric:** a `src/` `sorry` actually **DROPS** on this path. NOT "lemmas banked." (src has
been pinned at 11 for ~30 laps while lemmas accumulated — that pattern ends here.)

**Mandated FIRST move (do not skip):** run the **lap-132 existence-form reframe** as a cheap `wip/`
spike BEFORE any more invariant-fold work. Reframe the endgame from "`red` is a total Σ₁ function that
descends on every orbit step" to the **existence / least-descending-reduct** form, where
"fixpoint ⟹ cut-free" is *definitional*. If it holds it obviates the
`majorIdx`/`firstBotPrem`/`zReg`/`zFresh`/`zSeqAnt` selection-correctness campaign and reuses the
banked per-reduct soundness (laps 112–119). Settle it decisively like the lap-101 Path-X/C spike, then
proceed on whichever side it proves.

**FORBIDDEN until the spike is decided:** more `zReg`/`zFresh`/`zSeqAnt` invariant folds; treating the
engine swap as the goal; closing off-critical-path easy `sorry`s; any work on M2 / M3 / M4. If you judge
M1b-term genuinely blocked, record in `PENDING_WORK.md` exactly why + one concrete attack — do not
silently switch threads.

**Why (the diagnosis):** two altitude laps (120, 132) found the project circling — reduct-soundness and
an engine swap got polished while the load-bearing TERMINATION question (does cut-elimination actually
reach a contradiction, or stall?) has sat open since lap 104. Each grind lap reverted to the in-flight
fold campaign because it read the handoff, not the reflection. This directive layer is the fix: it binds
the grind laps until an altitude lap changes it.

## DIRECTIVE HISTORY (newest first; append one line per altitude lap — never delete)

- **pre-lap-135** (operator + judge): focus to **M1b-term only**; existence-form spike FIRST; success = a `src/` sorry drops, not lemmas banked.
