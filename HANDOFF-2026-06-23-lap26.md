# HANDOFF — 2026-06-23 (lap 26, **E-core(b) THE WALL: internal `bump`/`goodsteinSeq` BUILT inside `V`**)

> **Branch** `plan` · HEAD = `90348f8` (lap-26 tip) · build **green** (`lake build GoodsteinPA`, **1280 jobs**) ·
> headline `GoodsteinPA.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). Tree clean.

A hardest-first lap that **cracked the gating prerequisite of E-core(b)** (the lap-25 handoff's "THE WALL"):
make `bump`/`goodsteinSeq` `𝚺₁`-definable *inside* an arbitrary `V ⊧ₘ* 𝗜𝚺₁`. That is now **built and
machine-checked** — the deep-machinery doubt is resolved. Durable overview = `STATUS.md`; attack paths =
`PENDING_WORK.md`; E map = `DESCENT-PLAN.md §3`.

## ✅ Lap-26 deliverables (9 green commits, all `[propext,choice,Quot.sound]`-clean)

Five **new** files, all wired into `src/GoodsteinPA.lean`, building the internal Goodstein substrate from
Foundation's `PR.Construction` engine (`HFS/PRF.lean`) — since Foundation's `Exponential` is **base-2 only**:

1. **`InternalPow.lean`** — `ipow b x` (variable-base power) via `PR.Construction`, `𝚺₁-Function₂` instance,
   power laws (`one_le_ipow`, `ipow_pos`, `ipow_add`, `ipow_le_ipow_right`, `ipow_lt_ipow_right`) by internal
   `𝗜𝚺₁` succ-induction.
2. **`InternalDigits.lean`** — `idigit b n i = (n/b^i)%b`, `idigit_lt`, `idigit_succ_exp` (digit shift),
   `𝚺₁-Function₃` (via `definability`).
3. **`InternalLog.lean`** — `ilog b n` (base-`b` log) via least-number principle (mirrors Foundation base-2
   `log`); `ilog_spec` (`b^(ilog)≤n<b^(ilog+1)`), `ilog_graph`, **`ilog_defined : 𝚺₁-Function₂`** (the gating
   prereq, hand-written `ilogDef`).
4. **`InternalBump.lean`** — the hereditary base-change `bump` (course-of-values) via the **table reduction**:
   `bumpNext` (table step, hand `bumpNextDef`), `ibumpTable` (the `PR.Construction` table), `ibump b n :=
   znth (ibumpTable b n) n`, all `𝚺₁`-definable; structural correctness (`ibumpTable_seq`, `ibumpTable_lh`,
   `znth_ibumpTable_eq_ibump` stability, `ibump_zero`); and **`ibump_succ`** — the proven peel recursion
   `ibump b (n+1) = (n+1)/b^e·(b+1)^(ibump b e)+ibump b r` (`e=ilog`, `r=(n+1)%b^e`), exactly `Defs.bump`.
5. **`InternalGoodstein.lean`** — `igoodstein m₀ k` (the run `k↦mₖ`) via `PR.Construction` on `ibump`,
   `𝚺₁-Function₂`, recursion eqns matching `Defs.goodsteinSeq`. **This is the concrete `m : V → V` that
   `DescentArith.ineq6_internal` abstracts over.**
6. **`InternalBridge.lean`** (started) — standard-model faithfulness: **`ipow_nat`** (`=b^n`),
   **`ilog_nat`** (`=Nat.log b n`) over `ℕ`.

## ⚠️ Key gotchas resolved (save the next lap from re-deriving)
- **`definability`/aesop CANNOT handle predicates over `ibumpTable`/`ibump`** — its `PR.result`
  definability leaf makes `isDefEq`/aesop blow up (depth-30 fail; depth-80 heartbeat-timeout). FIX:
  split the predicate with `HierarchySymbol.Definable.comp₁`/`comp₂`/`ball_le`, then supply the FUNCTION
  definability as **explicit composition terms** (`def_ibumpTable`/`def_ibump` helpers = `DefinableFunction₂.comp (F := …) (.const b) (.var i)`), NOT via search. Also need general
  `Γ-[m+1]-Function₂` instances (`…_definable'` via `.of_sigmaOne`) so `0+1` unifies.
- **`PR.Construction` blueprint formulas** reference component Defs (`!bumpNextDef`, `!seqConsDef`,
  `!mkSeq₁Def`); `zero`/`succ`-defined proofs are `.mk fun v ↦ by simp [blueprint, …_defined.iff]`. `!⟦0⟧`
  needs `emptyset_def` in the simp set.
- **LE instance diamond on `ℕ`**: Foundation's scoped `≤` is `x=y ∨ x<y` (`instLE_foundation`), ≠ defeq
  `Nat.le`. The `<` underneath IS `Nat.lt` (shared). Convert with `LO.FirstOrder.Arithmetic.le_def`, then
  `omega`/Nat lemmas. (`ring`/`le_or_lt` are NOT available in `V`; use `add_assoc`/`by_cases`.)

## 🎯 Open obligations (E-core, hardest-first) — resume HERE
1. **Finish `InternalBridge.lean`** (faithfulness, anti-fraud-critical): `ibump_nat (b n : ℕ) (hb : 2≤b) :
   ibump b n = Defs.bump b n` by `Nat.strong_induction_on` (use `ibump_succ` + `ilog_nat`/`ipow_nat`, then
   `ih` at `Nat.log b (m+1) < m+1` and `(m+1)%b^log < m+1` — the `Defs.bump` `decreasing_by` bounds; unfold
   RHS `bump` with `conv_rhs => rw [bump]`). Then `igoodstein_nat (m k : ℕ) : igoodstein m k = goodsteinSeq m k`
   by induction on `k` (base `k+2≥2`). **Bridges internal machinery ⟷ audited `Defs`.**
2. **THE remaining math content — the internal `ineq6_step`** (Rathjen Lemma 3.6 slow-down): build the
   `b`-side bound `b k = T̂^{k+2}_ω(βₖ)` as a `𝚺₁`-function, prove base `b 0 ≤ m 0` and the internal step
   `b k ≤ m k → b (k+1) ≤ m (k+1)` (Δ₀ numeral form of `DescentCore.ineq6_step`), then plug `m=igoodstein`,
   `b` into **`DescentArith.ineq6_internal`** (already built) to get `𝗣𝗔 ⊢ ∀k, b k ≤ m k`. This is the genuine
   non-vacuous descent; ground it in `papers/` (Rathjen 2014 §3).
3. **Back-end** (Route A `PRWO⟹Con(PA)`+Gödel-II, or Route B integrated paLX) — deferred; see `PENDING_WORK.md`
   lap-24 correction.
4. **F-φ** `rePred_ltPull_natCode` — ON ARISTOTLE (`aris_onotecmp`, `16c9fc79-…`). On return: verify in-kernel
   + `#print axioms`, port → makes Thm 5.6 fully axiom-clean.

## Notes
- **At next lap start:** poll `aristotle list` for F-φ; check for `ON-LINE-FINDINGS-*.md`.
- **LOCKED untouched:** `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `sorry`.
- **src/ sorries (2):** `Statement.lean:22` (headline, locked), `Reduction.lean:50` (Route-A, off-path).
- Discharge headline `sorry` ONLY when E is real AND `#print axioms peano_not_proves_goodstein` clean.
