# HANDOFF — lap 132 · DEEP REFLECTION: the STALL is an engine-formulation artifact; course-TEST the existence form

> **RESUME BATON.** Branch `plan`, HEAD = lap-132 reflection commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint re-verified in-kernel & UNCHANGED:
> `peano_not_proves_goodstein = [propext, sorryAx, choice, Quot.sound]` (0 math axioms). No `src/`
> proof code touched this lap (reflection lap — docs only). 11 disclosed headline sorries, unchanged.

## What this lap DID — the every-9th-lap altitude pass

Read STATUS + the lap-126→131 batons + PENDING_WORK + NEXT_STEPS + the git trajectory (no treadmill
jsonl exists); re-verified the build green and the headline/faithfulness in-kernel; re-audited the
statement vs source; then judged the whole project from altitude. **Primary deliverable:
`REFLECTION-2026-06-26-lap132.md`** (read it — this baton is the short form).

### The finding (the value of the altitude lap)
crux-2's diagnosis is CONVERGED and CORRECT (laps 92/101/111/120/129 all re-pin it to the `red`-engine
on the ⊥-orbit). But the SELECTION/STALL sub-goal that has consumed laps 120→131 — `firstBotPrem_reducible`,
the `majorIdx` re-key, the tag-5/6 critical dispatch, the `zReg`/`zFresh`/`seqAntSeq` invariant folds, the
ZPhi exact-shape strengthenings (`zAx1` 115, `zAxNeg` 118, `zAxAll` 131, `zIneg` pending) — exists **ONLY
because the endgame demands `red` be a total deterministic Σ₁ FUNCTION that descends on every orbit step.**
The fixpoint branch of `false_of_ZDerivesEmpty` then needs "`red`-fixpoint ⟹ cut-free", which lap 129
refuted for the permIdx engine, forcing the whole no-stall campaign.

### The reframe (course-TEST, not a declared win)
Define the iteration as the Σ₁ **least *descending* reduct** instead of a fixed selector:
```
redLeast(d) := least d'  with  [ ZDerivesEmptyR d' ∧ icmp (iord d') (iord d) = 0 ∧ d' a cut-reduct of d ]
            := d          if no such d' exists
```
Then **"`redLeast d = d`"  ⟺  "no descending reduct"  ⟹  "`d` cut-free"** by the contrapositive of one
existence lemma **(E)**: `ZDerivesEmptyR d ∧ ¬cutfree d ⟹ ∃ d', ZDerivesEmptyR d' ∧ iord d' ≺ iord d ∧
d' a cut-reduct of d`. "fixpoint ⟹ cut-free" becomes DEFINITIONAL — no faithful-selector proof. The
endgame closes: descend-while-cut + PRWO(ε₀) forbids infinite descent ⟹ reach a fixpoint ⟹ cut-free
∅→⊥ ⟹ Cor 2.1 absurd.

(E) is dischargeable at the ⊥-root from **already-banked** facts:
- every ⊥-derivation's root is Ind/K — `zTag_Ind_or_K_of_ZDerivesEmpty` (`InternalZ:8636`);
- K-critical reduct SOUND (laps 112-119, `ZDerivation_iRKcCrit_all` + ¬-twin) + DESCENDS
  (`iord_descent_iRKcCrit_corr`/`_neg`, `RedZKDescent:580/597`); Ind reduct sound + `iord_descent_red_zInd`
  (`Crux2Blueprint:1116`);
- cut-free ∅→⊥ absurd — Cor 2.1 (`tp_selected_isymRep_of_emptyAnt_botSucc`, `InternalZ:7684`);
- the dichotomy `iRcrit_descends_or_zInd_zK_premise` (`RedZKDescent:734`) is essentially (E) at the root.

**KEEP (both forms need it):** per-reduct soundness (112-119); invariant folds `ZRegular_red`/`ZFresh_red`
+ the pending `seqAntSeq` fold (so `redLeast d` stays in `ZDerivesEmptyR`); per-reduct descent lemmas.
**OBVIATE (pending spike):** the fixed-engine permIdx→`majorIdx` swap, `firstBotPrem_reducible`, the
tag-5/6 dispatch, the ZPhi exact-shape strengthenings whose only consumer is the fixed engine's soundness
derivation. **Honest caveat:** the redex-pair combinatorics survive — but as a one-shot `∃`, not a total
function. The spike must confirm (E) is clean at the root without re-importing the stall (the "real cut vs
structurally-cut-free chain" determination is exactly Cor 2.1's job).

## NEXT (turnkey — full detail in `NEXT_STEPS.md` lap-132 box + `PENDING_WORK.md` lap-132)
1. **`wip/ExistenceEndgame.lean` spike** (self-contained, NOT imported by `GoodsteinPA.lean` — cannot
   affect the green gate). Define `redLeast`; state (E); assemble the existence-form
   `false_of_ZDerivesEmpty` with signatures pinned against the real API, bodies sorried where the banked
   lemmas plug in. Get it to ELABORATE — that alone is the evidence (mirror lap-101's fork spike).
   **Decision rule:** (E) closes cleanly at the root ⟹ PIVOT the endgame, drop the stall campaign.
   (E) re-imports the stall ⟹ fall back to the lap-129 engine swap WITH that evidence (its facts are all
   still valid; NEXT_STEPS keeps it as the FALLBACK route).
2. Then the PRWO wiring + M2 embedding — reused verbatim, only the iterator changes (`red` → `redLeast`).

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open work is the spike (a self-contained design test against the in-repo API), not a
portable lemma. Do NOT submit `false_of_ZDerivesEmpty` or (E) — they depend on the whole `InternalZ`/`iord`
substrate, which Aristotle cannot see.

## Pointers
`REFLECTION-2026-06-26-lap132.md` (primary) · `STATUS.md` (refreshed: header, lap-132 box, Where-it-stands,
What's-happened bullet, ledger note) · `NEXT_STEPS.md` lap-132 box (existence form; lap-129 swap kept as
FALLBACK) · `PENDING_WORK.md` lap-132 · endgame surface: `Crux2Blueprint.lean:1281-1314`
(`iord_red_iterate_descends` / `false_of_ZDerivesEmpty`) · banked (E)-suppliers: `RedZKDescent.lean:580/597/734`,
`Crux2Blueprint.lean:1116`, `InternalZ.lean:7684/8636`.
