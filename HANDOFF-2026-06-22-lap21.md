# HANDOFF — 2026-06-22 (lap 21, REVIEW: Thm 5.6 ASSEMBLED + D' gap surfaced + chip 1)

> **Branch** `plan` · HEAD `91375e3` · build **green** (`lake build GoodsteinPA`, **1271 jobs**) ·
> headline `GoodsteinPA.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). Tree clean.

## ✅ Lap-21 deliverables (3 green commits)
1. **`cbbbe1e` — Thm 5.6 ASSEMBLED** (`src/GoodsteinPA/Thm56.lean`, NEW). The entire Buchholz §5 girder
   is now ONE theorem: **`peano_not_proves_TI : IsEmpty (Derivation2 paLX {TI prec})`** (Gentzen 1943
   sharpness, `𝗣𝗔 ⊬ TI_≺(X)`). Chain: C₂ `embedC_LX`+`hax_paLX` → `asgX_TI_fix` collapses the assignment
   image (`TI prec` fvar-free: `prec = lMap (emb precφ)`, `precφ` a `Semisentence`) → C₁+D
   `orderType_le_of_TIprovable` → F `seam.ge`; contradiction `ε₀ ≤ ‖≺‖ ≤ 2^(ω_c^α) < ε₀`. Plus
   **`peano_not_proves_goodstein_of_descent (hE : DescentE) : 𝗣𝗔 ⊬ ↑goodsteinSentence`** — the headline
   reduced to ONE wall **E**, with E's exact interface pinned (`DescentE := 𝗣𝗔 ⊢ γ → Nonempty
   (Derivation2 paLX {TI prec})`). `#print axioms peano_not_proves_TI = [propext, choice, Quot.sound,
   sorryAx, rePred_ltPull_natCode]`. Headline UNTOUCHED.
2. **`91375e3` — D' chip 1** (`src/GoodsteinPA/EmbeddingBound.lean`, NEW): `provable_em_x_bdd` —
   bounded `Z∞` excluded middle, **finite** ordinal `≤ 2·complexity`, axiom-clean.

## ⚠️ THE KEY REVIEW FINDING — D' is a real gap the lap-20 handoff MISSED
Lap-20 claimed "Thm 5.6 axiom-clean modulo E+F". **Incomplete.** The contradiction needs the embedded
ordinal **`α < ε₀`**, but `embedC_LX` only gives `∃ α` (no bound). `α < ε₀` is *the* Gentzen content
(a finite PA-proof embeds to a `Z∞`-proof of height `< ε₀` — PA cannot certify heights up to ε₀ itself).
Isolated as the disclosed `sorry` **`Thm56.embed_TI_bounded`** (D'). **Walls are now E + F-φ + D'.**

## 🎯 Open obligations (priority order)
1. **D' — discharge `embed_TI_bounded`** (the immediate open `sorry`; tractable, Foundation-light, NO
   literature). Re-run the embedding with a **uniform** bound `∃ c, ∃ B<ε₀, ∀ e, PXFc B c (…)` (B OUTSIDE
   `∀ e` ⟹ ω-rule family is constant ⟹ `⨆ₙ const = const`). **Chips left** (in `EmbeddingBound.lean`),
   dependency order:
   - `provable_true_x_bdd` (ω-completeness leaf; bound complexity-determined like em).
   - `exI_closed_bdd` (uses `provable_em_cong_gen_x`; ordinal = α + finite bumps).
   - `metaInduction_cong_bdd` — the cut-tower; chain `β(n) ≤ E_ψ + ω` uniformly (em bound `E_ψ` is
     complexity-determined, so the chain only grows by `+1` per step) ⟹ `⨆ₙ β(n) < ε₀`. THE one with a
     non-constant family; bound it by an explicit `< ε₀` ordinal.
   - `PXFc_allClosure_bdd` (uniform family ⟹ uniform output).
   - `embedC_LX_gen_bdd` (10 structural cases, mirror `EmbeddingX.lean:411`; bound bumps `+1`/`max+1`/`sup+1`).
   - `hax_paLX_bdd` (mirror `EmbeddingX.lean:744`: X-free axioms via `provable_true_x_bdd`, X-induction
     via `metaInduction_cong_bdd` + `PXFc_allClosure_bdd`).
   - wire `embed_TI_bounded` ⟹ **Thm 5.6 axiom-clean modulo only F-φ**.
   *Note:* `provable_em_x_bdd` shows the pattern — return an explicit complexity-determined ordinal,
   `mono` leaves up to it, `PXFc.allω (β := fun _ => const)` + `iSup_const_ord` for ω-rule cases.
   Cast tips: `ring`/`ring_nf` FAIL on `Ordinal` (non-commutative); use `omega` for ℕ arithmetic then
   `Nat.cast_add`/`Nat.cast_one`; `0 ≤ x` via `(by simp)` (`Ordinal.zero_le` is gone).
2. **F-φ** `rePred_ltPull_natCode` (`SeamDefinability.lean`). **ON ARISTOTLE** (`aris_onotecmp`, UUID
   `16c9fc79-ae8b-4b04-8b83-2e8e9e5f38db`, RUNNING since ~lap 20). On return: VERIFY in-kernel + `#print
   axioms`, port. Crux = `Primcodable ONote` (structural) + `Primrec₂ ONote.cmp`.
3. **E** `DescentE` (interface pinned). The Goodstein→`TI(ε₀)` descent in PA — the deep crux, **needs
   `papers/` reading** (Cichoń/Rathjen-2014/Agboola/Towsner). Map the wall, state per-step lemmas,
   formalize the first prerequisite. ~2–4 laps.
4. **G — done** (`peano_not_proves_goodstein_of_descent`). Discharge the headline `sorry` ONLY when E is
   real AND `#print axioms peano_not_proves_goodstein` is clean.

## ⚠️ Locked / notes
- **LOCKED untouched:** `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `sorry`.
- **src/ sorries (3):** `Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A, off-path),
  `Thm56.lean` `embed_TI_bounded` (D', disclosed). `EmbeddingBound.lean` is sorry-free.
- STATUS.md refreshed this lap (ledger + Outstanding + What's-happened).

## 📊 Lap estimate to headline
D' ~1–3 (tractable, in progress) · F-φ ~0–1 (Aristotle) · E ~2–4 (dominant, needs literature) · G done.
Everything from D back + C₂ + the §5 assembly is machine-checked.
