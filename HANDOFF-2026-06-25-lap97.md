# HANDOFF — lap 97 · ⚠ the lap-96 I∀ rewire plan is ARCHITECTURALLY IMPOSSIBLE; `ZRegular_red` banked

> Branch `plan`. HEAD = `3a8720b`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs).
> Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
> (0 math axioms; `sorryAx` = crux-2 residuals). New `ZRegular_red` is axiom-clean
> (`[propext, Classical.choice, Quot.sound]`).

## ⛔ CORRECTION: the lap-96 "immediate concrete step" (rewire `iRNextG` tag-1 to emit `zsubst …`) CANNOT BE DONE
`iRNextG` / `red` / `redTable` / `iRK` live in **`InternalZ.lean`**. `zsubst` (and `iord_zsubst`,
`ZDerivation_zsubst`, `zReg_zsubst`) live in **`Zsubst.lean`, which `import GoodsteinPA.InternalZ`**
(strictly downstream). So `iRNextG` literally cannot name `zsubst` — the lap-96 plan would fail to
elaborate (`Unknown identifier zsubst`). Verified by attempting it (revert clean). Closing this off:
- Moving `red`'s table machinery DOWN into Zsubst means dragging **37+ downstream `red`/`fstIdx_red`/
  `red_zK_*`/`red_eq_iR2`/`red_zIneg_tpReduce` lemmas** (InternalZ:6360–7350+) with it — huge.
- Moving `zsubst` UP into InternalZ means hauling `fvSubst`/`fvSubstSeqt`/the whole substitution substrate
  ahead of `ZDerivation` — also huge and risky.
- `iord_zsubst` (lap 96) is real and axiom-clean, but it is NOT wireable into `iRNextG`. Don't retry this.

## ⭐ KEY FINDING: the I∀ conclusion-rewire is NOT NEEDED for the ⊥-orbit endgame
On the ⊥-orbit, Buchholz Cor 2.1 (`tp_selected_isymRep_of_emptyAnt_botSucc`, InternalZ, lap 96) gives:
every reached chain's **selected** premise `dᵢ` is `Rep` (`tp dᵢ = isymRep`) — hence atom/Ind/chain,
**never an I∀ node** (I∀ has `tp = isymR ≠ isymRep`). So `red`'s I∀ conclusion-tracking is never
exercised on the orbit. The conclusion-tracking actually needed — for a **Rep** selected premise — is
already `red_rep_of_tp_isymRep` (PROVED, `Crux2Blueprint.lean:158`), and there `tpReduce isymRep = id`
(no genuine reduction). So `red` keeps the `∅→⊥` conclusion on the orbit (`fstIdx_red_of_emptyAnt_botSucc`).

## ⭐⭐ THE WALL, FINALLY DIAGNOSED: `red` cannot do the eigenvariable substitution (architectural)
The genuine residual is `ZDerivation_red_zK` **case 2** (non-chain selected premise). When the selected
premise `dᵢ` is an **I∀ node** (∀-principal-cut), Buchholz reduces the conclusion to `Θ→F(0)` (`tpReduce
(isymR ∀p) s 0`) AND must put **`zsubst d0 a (numeral 0)`** (`d0(a/0)`) at position `i`, NOT `d0`. The
repo's `red` puts `red(zIall) = d0` (derives `Γ→F(a)`), so `red (chain) = zK (Θ→F(0)) r (seqUpdate ds i d0)`
is **genuinely unsound** — d0 derives F(a)≠F(0). The VALUE is wrong; no downstream proof fixes it.

**Why unfixable in place:** `red`/`iRNextG`/`iRKr` are in InternalZ; `zsubst` is downstream in Zsubst. And
`red`'s block is tangled through InternalZ's tail (6190–7409) with `iR2`/`ZDerivesEmpty`/`zDerivation_*_inv`.

**Hereditary Cor 2.1 is FALSE** (settled by reasoning): `red` recurses into the Rep selected premise's OWN
selected premise (permissible for its non-⊥ conclusion), where I∀/axiom non-Rep selected premises occur. So
the eigensubst case is genuinely reachable — the route-B `tpReduce` machinery cannot be avoided.

**This supersedes the lap-96 "rewire iRNextG" plan.** The fix is a real architectural pivot — see
`PENDING_WORK.md` (lap 97) for the three options (recommended: relocate the `red` block downstream of
`zsubst`, change tag-1 to the eigensubst; `iord_zsubst`/`ZRegular_red`/`zReg_zsubst` transfer the banked
descent + regularity unchanged since both are conclusion-independent).

## ✅ What lap 97 LANDED (in-kernel, green 1325, axiom-clean)
- **`ZRegular_red` (`Zsubst.lean`, after `ZRegular_red_zK`):**
  `∀ d, ZDerivation d → ZRegular d → ZRegular (red d)`, by `zDerivation_induction` assembling the
  existing axiom-clean pieces (`ZRegular_red_of_not_zK` + unconditional `ZRegular_red_zK`, per-premise IH
  via `ZRegular_zK_premise`). `#print axioms = [propext, Classical.choice, Quot.sound]`. This is the full
  O1 half of "red preserves valid + regular" as one clean statement — banked for the regular-orbit assembly.

## ▶ PIVOT IN PROGRESS — checkpoint A DONE, checkpoint B next
**✅ Checkpoint A (commit `4161dee`, green 1325):** the `zsubst` DEFINITION block is relocated upstream
into InternalZ (`import GoodsteinPA.FvSubst` added; fvSubstSeq/tblMapSeq/zIallEig/zsubstNext/zsubstTable/
zsubst + definability moved from `Zsubst.lean`; hard theorems `ZDerivation_zsubst`/`iord_zsubst`/
`zReg_zsubst` stayed in Zsubst, referencing the upstream def). `zsubst` is now ABOVE `red`/`iRNextG`.

**▶ Checkpoint B (NEXT) — the in-place I∀ eigensubst rewire:**
1. `iRNextG` (InternalZ ~6556 after the move) tag-1 = `zsubst (zIallPrem d) (zIallEig d) (numeral 0)`;
   re-prove `iRNextG_defined` (add `zsubst_defined.iff`/`zIallEig_defined.iff`/`numeralGraph` to simp),
   `red_zIall = zsubst d0 a (numeral 0)` (simp `zIallPrem_zIall, zIallEig_zIall`).
2. Fix `ZRegular_red_of_not_zK` zIall case (use `zReg_zsubst _ _ _ hd0`, name `hd0`); fix `redSoundGen`
   zIall case — thread `ZRegular d` into the motive so `ZDerivation_zsubst` gets `maxEigen d0 < a`
   (motive `P d := ZRegular d → ZDerivation (red d)`; `definability` should handle the Δ₁ implication; the
   zK case feeds `ZRegular_zK_premise` into the IH `.2`). NOTE: this regularity-threading is the one
   non-trivial sub-step — `redSound` then needs `ZRegular` from its orbit hypothesis (thread it via a
   `ZDerivesEmptyR := ZDerivesEmpty ∧ ZRegular` predicate; M2/M3 are sorries so their sigs can absorb it).
3. Then `ZDerivation_red_zK` case 2 is SOUND for the I∀ selected premise (replacement `zsubst d0 a 0`
   derives `Γ→F(0)`, matching `Θ→F(0)`). Axiom (isymLk) sub-case = analogous L-cut, second hardest leaf.
4. Banked `iord_zsubst`/`ZRegular_red`/`zReg_zsubst` transfer unchanged (conclusion-independent).
5. Then `iord_descent_red`, M2 (`foundation_bot_to_Z_empty`), M3 (`false_of_ZDerivesEmpty`), wire to headline.
**⚠ land green or stash to wip/ — never leave InternalZ red across a turn boundary.**

## Open crux-2 sorries (Crux2Blueprint, unchanged)
`zKValidF_iIndReduct_of_zInd`, `ZDerivation_red_zK_crit`, `ZDerivation_red_zK_splice`,
`ZDerivation_red_zK` (2 replace branches), `iord_descent_red`, `foundation_bot_to_Z_empty`,
`false_of_ZDerivesEmpty`.

## Aristotle
Idle (deeply-entangled residuals; the open hereditary-Cor-2.1 question is a reading task, not Aristotle fodder).

## Build / verify
`lake build GoodsteinPA` 🟢 1325. `#print axioms peano_not_proves_goodstein` =
`[propext, sorryAx, Classical.choice, Quot.sound]`. `ZRegular_red` axiom-clean.
