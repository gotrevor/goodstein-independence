# HANDOFF — 2026-06-23 (lap 41, **slow-down code toolkit complete + the Lemma-3.6 engine**)

> **Branch** `plan` · HEAD `ee65554` · build **green** (`lake build GoodsteinPA`, **1308 jobs**) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, **0 math axioms**).
> **Tree clean**. All new lemmas `#print axioms`-clean `[propext, choice, Quot.sound]`.

## Context: the lone wall is `hbound` (`DescentSemantic.lean:416`)
Same as lap 40: the open `sorry` is `hbound` inside `no_min_descent_absurd_of_goodstein`, an
existential producing a non-terminating internal special-Goodstein run from the M-internal ≺-descent
(`no_min`). After lap 41 the **code-level slow-down toolkit + the Lemma-3.6 assembly engine are
complete**; what remains is the **SEAM** (build the 𝚺₁ `β : M → M` from `no_min` with the 3 props).

## ✅ Done this lap (4 feat commits) — axiom-clean
- `f7f6922` **`icmp_iomul`** (`InternalONote.lean`): `icmp (ω·a)(ω·b) = icmp a b` on NF codes (block
  comparison). Bounded order-induction down the shared spine, head via `icmp_one_add`, tail via IH. +`iomul_ne_zero`.
- `2d10030` **`icmp_betaTail_boundary`**: cross-block descent `icmp (ω·a+s)(ω·b+t) = 0` from `icmp a b = 0`.
  +`iadd_iomul_ocOadd_finite` (spine-walk of appending a finite term to ω·c), +`thenV_zero_mono`.
- `f04f0ed` **`isNF_iomul`** + **`isNF_iadd_finite`**: NF preservation for `ω·c` and `ω·α + m` (positive
  finite `m`). +helpers `icmp_right_zero_ne_zero`, `finHead_NF_tail_zero`, `isNF_iadd_one`.
- `ee65554` **`DescentSlowdown.lean`** (NEW FILE): **`hbound_of_slowdown`** — abstracting over the slowed
  code seq `β : V → V` (`𝚺₁`, `∀k isNF (β k)`, `∀k iCanon (k+1)(β k)`, `∀k icmp (β(k+1))(β k)=0`), produces
  EXACTLY the `hbound` tuple (seed `m₀ = ievalNat 1 (β 0)`, bound `b k = ievalNat (k+1)(β k)`, base/step/hpos;
  `step` = the proved `ineq6_step_internal`). **`nonterminating_of_slowdown`** composes with
  `DescentArith.nonterminating_internal`. Imported into root `GoodsteinPA.lean`.

## What the slow-down code toolkit now provides (all proved, axiom-clean, in `InternalONote.lean`)
- `iadd`, `iomul` (internal CNF `+`, `ω·`); `iC_iomul`, `iC_iadd_finite` ⟹ `C(βₖ) ≤ k+1`.
- within-block descent `icmp_betaTail_within`; cross-block descent `icmp_betaTail_boundary`.
- NF preservation `isNF_iomul`, `isNF_iadd_finite`.
- `ineq6_step_internal` (lap 39, the Rathjen (6) step). **Everything `hbound_of_slowdown` consumes.**

## NEXT (hardest-first) — the SEAM (only remaining piece of `hbound`)
Build the slowed-down `β : M → M` (in M's `ℒₒᵣ`-reduct, `[ORingStructure M] [M ⊧ₘ* 𝗜𝚺₁]`) and feed
`DescentSlowdown.nonterminating_of_slowdown`, then close `hbound` (anti-fraud: re-`#print axioms` the
headline — must stay `sorryAx`-only — after the seam edit; the seam touches the proved girder).
1. **M-internal `Mlt`-descent `αₖ : M → M`** from `no_min` + `ha₀`: definable least-`y` recursion via
   `M`'s LX least-number principle (`Arithmetic.leastNumber`/`succInd`, generic over `[LX.ORing]`).
   **Must be M-internal/definable**, not metatheoretic `choice` (see `PENDING_WORK.md` ⚠). This is the
   hard sub-wall — produces `αₖ` with `Mlt`-descent + each `¬MX (αₖ)`.
2. **`αₖ` (the `Mlt`/`precφ` order) → codes** (route b of lap-40 seam): decode `Mlt` to `icmp`/codes so
   the abstract descent becomes `icmp (αₖ₊₁)(αₖ) = 0` on NF codes, with `isNF (αₖ)` + a coefficient
   bound `iC αₖ ≤` (Rathjen `|αₖ| ≤ K(k+1)`).
3. **Rathjen reindexing → `βᵣ`**: `βᵣ = ω·αₙ + (K−i)` with global step `r = K(n+1)+i`, `i < K` (block
   `n`, offset `i`). Gives `iCanon (r+1) βᵣ` (from `iC_iomul`+`iC_iadd_finite`, `C_betaTail_le` ℕ-template
   in `DescentCore.lean:294`) and the `icmp`-descent (within `icmp_betaTail_within` + boundary
   `icmp_betaTail_boundary`). The reindexing arithmetic + its 𝚺₁-definability is the assembly.
4. **Feed the X-ESSENTIAL run, not the 𝚺₁ one.** ⚠ CORRECTION (lap 41): the descent `β` is
   **X-dependent** (its `¬MX` condition is the free predicate `X`), so `b k = ievalNat (k+1)(β k)` is
   NOT an `ℒₒᵣ`-`𝚺₁` function — `DescentArith.nonterminating_internal` / `nonterminating_of_slowdown`
   (𝚺₁) do NOT apply. The right tool is **`DescentSemantic.lx_nonterminating`** (already proved, line
   259): it runs the inequality-(6) iteration by `lx_succ_induction` (X-essential), needing only that
   the COMPARISON `b k ≤ igoodstein m₀ k` is **LX-definable** (`hPdef`), plus base/step/hpos.
   `DescentSlowdown.slowdown_run_facts` (lap 41) hands you base/step/hpos for free (X-agnostic code
   arithmetic). **So the seam reduces to: (i) build X-definable `β : M → M`; (ii) prove `hPdef` (the
   comparison is LX-definable — via `lxDef_of_reduct` on the `𝚺₁` `igoodstein`/`ievalNat` graphs +
   the X-formula for `β`); then `lx_nonterminating ... ⟹ ∀ k 0 < igoodstein m₀ k`** = `hCD`. Suggested:
   add a new lemma `nonterminating_of_xDescent` in `DescentSemantic` packaging (i)+(ii)+slowdown_run_facts
   +lx_nonterminating (NEW lemma, do NOT edit the girder; re-`#print axioms peano_not_proves_TI` after).
   The hard remaining sub-wall is purely (i): the M-internal **X-definable recursion** building `β`
   (codes via the reindexing) from the `descent_step` selector — Foundation has `lx_succ_induction`
   but likely no ready X-definable recursion theorem; that infrastructure is the multi-lap core.

## Aristotle
Idle. The slow-down code lemmas are all closed locally; the genuinely-open work now is the SEAM
(M-internal definable descent), which is model-theoretic (not a self-contained ℕ-code lemma) — poor
Aristotle fit. If a bounded code lemma surfaces in step 3 (e.g. the reindexing `βᵣ` 𝚺₁-def), feed that.

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`.

## src/ sorries: `Statement.lean:22` (headline, locked), `Reduction.lean:52` (off-path Route-A),
`DescentSemantic.lean:416` `hbound` (the lone genuine wall). `InternalONote`, `DescentSlowdown` sorry-free.
