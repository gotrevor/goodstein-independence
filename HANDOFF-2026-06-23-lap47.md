# HANDOFF — 2026-06-23 (lap 47) — REVIEW + internal Thm 3.5 COMPLETE + Cor 3.4 clean-append begun

> **Branch** `plan` · **HEAD `5ecd56c`** · build **green** (`lake build GoodsteinPA`, 1311 jobs) ·
> headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, untouched; real
> `#print axioms` = `[propext, sorryAx, choice, Quot.sound]`, 0 math axioms).
> Commits this lap: `c8ba8a6` (ω-tower cofinality + `bbeta_desc_exists` ⟹ internal Thm 3.5 complete),
> `5a75ba2` (STATUS re-anchor onto Route A + lap-47 docs), `5ecd56c` (internal Cor 3.4 clean-append infra).
> Untracked (NOT mine, leave alone): `E-ARCHITECTURE-RESPONSE-2026-06-23.md` (Codex), `scratchpad/rathjen.txt`.

## DONE THIS LAP (part 2) — internal Cor 3.4 clean-append infrastructure (`InternalCor34.lean`, axiom-clean)
Cor 3.4's slowed term `αⱼ = ω^(l+1)·βₙ + g(l,n,m)` needs a GENERAL clean append (`g` is a genuine ordinal
`< ω^(l+1)`, not the finite `ocOadd 0 m 0` tail the `betaTail` lemmas handle). Built the internal analog of
`Grz.AllExpAbove`/`C_add_clean`/`corAlpha_within`:
- `iadd_clean_step` — `iadd` takes its `gt` branch under `icmp ea (ocExp b) = 2` (head exp dominates tail).
- `iAbove e0 a` — Σ₁-flag predicate via a parameterized course-of-values table (mirrors `isNFb`/`iaddTable`;
  `via`-index convention: `v 0`=output, `v (i+1)`=i-th input — needed `open …HierarchySymbol` + `Classical`):
  "every leading exponent down `a`'s spine `≻ e0`" (internal `MinExpGe`). Laws `iAbove_zero`, `iAbove_ocOadd`,
  recursion `iAboveb_pos`.
- `icmp_iadd_clean_within` — two clean appends onto the SAME head compare by their tails
  (`icmp (iadd a b₁)(iadd a b₂) = icmp b₁ b₂`) = internal `corAlpha_within`. Plus `ocExp_iadd_clean`.

## NEXT (PENDING_WORK lap-47 §, hardest-first within crux 1):
1. `icmp_iadd_clean_boundary` (head drops; internal `corAlpha_boundary`) — shared-prefix recursion, cleanest
   with `isNF a₁/a₂` + the `icmp = 1 ⟹ equal-code` fact. A unified `icmp (iadd a₁ b₁)(iadd a₂ b₂) =
   thenV (icmp a₁ a₂)(icmp b₁ b₂)` subsumes within+boundary.
2. `iC_iadd_clean` (`iC (iadd a b) ≤ max (iC a)(iC b)` under `iAbove (ocExp b) a`) = internal `C_add_clean`
   (the slowness C-split) + `iAbove`-preservation for `ibigMul`/`iomul` (internal `MinExpGe_bigMul`).
3. The abstract-`ig` interface + `icorAlpha` + the deep internal `ig` level-`l:V` recursion (the f-recursion).

## Fresh-mind review (done first this lap)
- Build green; headline honest `sorry`, 0 math axioms. `Thm56.peano_not_proves_TI` clean (trust base + 1
  🟢 native_decide). `not_proves_of_implies_consistency` + `peano_not_proves_consistency` carry
  `PA_delta1Definable` (the Route-A Gödel-II hook).
- **Direction KEEP.** Route A (Rathjen Cor 3.7 = the original DIRECTION Gödel-II plan) is the settled route
  (lap 46). The free-X Buchholz `peano_not_proves_TI` is axiom-clean but OFF the headline path (wrong
  direction). STATUS.md was badly stale (framed around free-X; said "Route A REJECTED") — **re-anchored
  onto Route A this lap** (header / Where-it-stands / Outstanding / ledger / pointers).

## DONE THIS LAP — internal Thm 3.5 is now HYPOTHESIS-FREE (`src/GoodsteinPA/InternalThm35.lean`)
The lap-46 disclosed boundary hypothesis `hbdry : icmp (bbtail K α K) (iwtower s) = 0` (β_K ≺ ωₛ) is
discharged via **ω-tower cofinality**:

  `iwtower_cofinal (c : V) : ∃ s, icmp c (iwtower s) = 0`   (axiom-clean)

**Key simplification over the lap-46 sketch:** NO NF hypothesis is needed. `icmp_ocOadd_lt_exp` decides
`≺` from the *leading exponent alone*, and `iwtower (sₑ+1) = ocOadd (iwtower sₑ) 1 0` has exponent `ωₛₑ`,
so the strong-induction hypothesis (`sigma1_order_induction` at `ocExp c < c`) giving `e ≺ ω_{sₑ}` decides
the whole code directly — no `c < ω^{e+1} ≤ ω^{ω_sₑ}` detour. Witness `s` = the iterated-exponent depth.
Then `bbeta_desc_exists (hK) (hNF) (hdesc) : ∃ s, ∀ r, icmp (bbeta K s α (r+1)) (bbeta K s α r) = 0`
gives the full descending Thm 3.5 sequence unconditionally (NF + tight `C(βᵣ)≤r+1` hold for the same `s`).

## OPEN DEEP CRUXES (hardest-first; multi-lap; see `PENDING_WORK.md` lap-47 §) — the live work
The headline reduces (axiom-clean) to `Reduction.goodstein_implies_consistency : 𝗣𝗔⊢γ → 𝗣𝗔⊢Con(𝗣𝗔)`
(disclosed `sorry`), = two girders:
1. **Internal Cor 3.4** (THE harder crux). Slow internal descent `α : V → V` (`iC(αₙ)≤K(n+1)`, `isNF`,
   `icmp`-descent) from a raw primrec ε₀-descent. Internal level `l:V` ⟹ Ackermann ⟹ NOT IΣ₁-total ⟹
   needs the PA substrate `ReductModel.reduct_models_PA`, NOT the IΣ₁ `PR.Construction` toolkit.
   **Recommended first attack (lap-45 path #2): parameterize over an abstract internal `f`** (its recursion
   eqns + Lemma-3.2 domination as hypotheses), build `ig`/`icorAlpha`/descent+bound relative to it,
   discharge `f`'s existence separately. Blueprint = sorry-free ℕ-template `Grzegorczyk.lean` (Lemma 3.3 +
   Cor 3.4 bricks `corAlpha_C_bound`/`_within`/`_boundary`). Standard-`l` `InternalCor34`
   (`ig0`/`iblk`/`ibigMul`) = reusable bricks (special case), NOT the generic route.
2. **Gentzen Thm 2.8(i) `PRWO(ε₀) → Con(𝗣𝗔)`** + formulate **`PRWO(ε₀)` as a `Sentence ℒₒᵣ`** (∀ primrec
   ε₀-descent → finite). Primrec `ord` + reduction `R`, `ord(R D)<ord D`, over Foundation `Derivation`.
   `Con(PA)` = `¬ T.Provable ⌜⊥⌝` (Foundation `Incompleteness/Consistency.lean`). The deep girder.
3. **`PA_delta1Definable`** (🟡) rides Gödel II on this route — a true theorem disclosed as a Foundation-pin
   axiom; the headline inherits it unless burned down upstream (DIRECTION anti-fraud #1 to reconcile).

## NEXT: start crux 1 via the abstract-`f` parameterization (path #2). It lets the genuine Grzegorczyk
`g`-padding math land green NOW without first building internal Ackermann. Read `Reduction.lean` docstring
+ STATUS "Where it stands". Do NOT resume free-X §3-on-X (dead, `not_dominated_of_diag_le`).

## src/ sorries (unchanged): `Statement.lean:22` (headline, LOCKED), `Reduction.lean`
`goodstein_implies_consistency` (Route-A girder), `DescentSemantic:582` (free-X β-wall, OFF-path,
`wip/` candidate), `Thm56` D' (off-headline), `Embedding.lean:70`, `EmbeddingX:410`, `DescentConstruction:576`.
## LOCKED untouched (anti-fraud): `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
## Aristotle: OFFLINE this run. No jobs. WebFetch dead (use WebSearch / ON-LINE-REQUEST.md if needed).
