# HANDOFF — 2026-06-23 (lap 49) — generic-level (non-standard `l:V`) internal Cor 3.4 lead + `icorAlpha` COMPLETE

> **Branch** `plan` · **HEAD `5b5929c`** · build **green** (`lake build GoodsteinPA`, 1311 jobs) ·
> tree **clean** · headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact, 0 math axioms).
> 7 commits this lap: `773cce9`, `6d3d512`, `d002a0a`, `f95061c`, `ad4d167`, `0168ef0`, `5b5929c`
> (+ docs commits `918a99f`, `968acad`).

## ORIENTATION — M1 & Phase-1 were ALREADY done
The operator kickoff named **M1 (`Encoding.goodsteinTerminates_re`)** as the first target. It is
**already complete & axiom-clean** (`#print axioms = [propext, Classical.choice, Quot.sound]`, backed by
`primrec_bump`/`primrec_goodsteinSeq` — no `sorry`/axiom). **Phase 1 (M2)** `not_proves_of_implies_consistency`
is also done & axiom-clean. The ONLY open obligation toward the headline is the deep Phase-2 girder
`Reduction.goodstein_implies_consistency` (= crux 1 + crux 2 below) and the live `sorry` at
`DescentSemantic.lean:582` (produce the slowed code-descent `β : M → M`).

## DONE THIS LAP — `InternalCor34.lean`, all axiom-clean — the generic Cor 3.4 lead + `icorAlpha`
The meta-iterate `ibigMul (k:ℕ)` was only the **standard-level** lead; the generic Rathjen §3 route needs
**`l : V` (non-standard** — the Grzegorczyk domination level is computed from an *internal* primrec descent
index, hence internal). Built the V-indexed analog bottom-up:
- **`iVbigMul β l = (ω·)^l β`** — `ω^l·β` as a genuine `𝚺₁` primitive recursion (`PR.Construction`, mirror
  of `iAboveTable`), with `isNF_iVbigMul`/`icmp_iVbigMul`/`iC_iVbigMul_le` by `sigma1_succ_induction`.
  **`iVbigMul` is now `attribute [irreducible]`** — its `construction.result` never reduces on a variable
  level, so any `whnf`/`isDefEq` meeting `iVbigMul β (l+1)` loops to the heartbeat limit.
- **`iAbove_finCode_iVbigMul`** — V-indexed MinExpGe (lead clean above finite code). Motive-definability
  needed an **explicit `Definable.comp₂` term** (`DefinableFunction₃.comp (hF := ocOadd_definable.of_sigmaOne)`)
  — plain `definability` aesop blows past depth/heartbeats on the `iAbove` relation rule (memory
  `definability-aesop-depth-blowup`).
- **clean-append discharge**: `iAbove_code_iVbigMul`, `iAbove_ocExp_iVbigMul_fin/_inf` produce
  `iAbove (ocExp g) (iVbigMul β (l+1))` for the `g`-tail `< ω^(l+1)` (finite or infinite top exponent).
- **`icorAlpha β g l := iadd (iVbigMul β (l+1)) g`** (the Cor 3.4 slowed term) with its three portable
  properties (mirror sorry-free `Grz.corAlpha_*`): **`icorAlpha_within`** (`icmp_iadd_clean_within`),
  **`icorAlpha_boundary`** (`icmp_iadd_clean_boundary`+`icmp_iVbigMul`), **`icorAlpha_C_le`**
  (`iC_iadd_clean`+`iC_iVbigMul_le`). ⚠ The full 4-hyp `icmp_iadd_clean` blows up on unification — use
  the `_within`/`_boundary` wrappers.
- **`icorAlpha_ig0_within`** — end-to-end validation at concrete level 0 (lead `ω·β`, tail `ig0`): the
  abstract interface composes with a real `g`. Plus `ig0_ne_zero`, `ocExp_ig0`, `iconst_add_le_mul`.

## NEXT (hardest-first) — crux-1 step 3, TWO deep isolated pieces (both cross-layer)
The generic-`V` per-step content is now COMPLETE. The `icorAlpha_*` lemmas are exactly the interface the
remaining pieces must satisfy. Both remaining pieces leave the generic-`V` `InternalCor34` layer:
1. **Internal `ig` f-recursion over level `l:V`** — `ig (l+1) n m = iblk (l+1) (…) (ig l (f^[blk] n) (off))`,
   bottoming at the internal Grzegorczyk **`F` (Ackermann-level)**. BLOCKER: needs (a) **internal iteration**
   `f^[k] n` for internal `k` (no infra yet — needs a `𝚺₁`/PA recursion table) and (b) the **full-PA reduct**
   `reduct_models_PA` (F is not IΣ₁-total). Foundation has NO Ackermann/fast-growing infra (checked). Provide
   the abstract-`ig` interface (the `icorAlpha_*` hyps) and discharge `f` separately.
2. **X-definable block bookkeeping** (`corBlk`/`corOff` over the descent's C-widths `corW β t = iC(β(t+1))`)
   — assembles the global slow `α : V → V`. **KEY FINDING lap 49: NOT cleanly IΣ₁** — `W = corW β` is
   X-definable (β lives in the LX `DescentConstruction` layer), so `iwsum`/`iwidx`/`iwoff` must be built
   X-definably THERE (mirror `Grz.wsum`/`widx`/`woff`, Grzegorczyk.lean:159-217), feeding `bbeta` (Thm 3.5,
   DONE) → `nonterminating_of_xDescent` → kills `DescentSemantic:582`.

## Crux 2 (OTHER girder) — Gentzen Thm 2.8(i) `PRWO(ε₀)→Con(𝗣𝗔)` — GROUNDED lap 49
Read Rathjen 2014 pp.8–11; full paper-grounded lemma-by-lemma decomposition in
**`CRUX2-GENTZEN-2026-06-23.md`** (PRWO formulation = the hinge; primrec `ord`/`R` over Foundation's
arithmetized `Theory.Derivation : V→Prop`; `ord(R D)≺ord D` = the Gentzen reduction core). **Independent of
crux 1, NOT blocked on Ackermann** — the recommended next thread if crux-1's internal-`F` infra stays
blocked. Keep crux-2 scaffold sorries in `wip/`.

## Aristotle: OFFLINE. WebFetch dead. LOCKED untouched (anti-fraud): `Defs.lean`, `Bridge.lean` RHS,
`goodsteinTerminates`, `Statement.lean` headline.
