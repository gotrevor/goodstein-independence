# HANDOFF — 2026-06-23 (lap 31, **reduct→𝗜𝚺₁ bridge DONE + equality architecture correction**)

> **Branch** `plan` · HEAD = `a8211c3` · build **green** (`lake build GoodsteinPA`, **1303 jobs**) ·
> headline `GoodsteinPA.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). **Tree clean.**

Durable overview = **`STATUS.md`**. Attack map + the lap-31 corrections = **`PENDING_WORK.md` lap-31 top**
(read FIRST). Lone wall unchanged: `DescentSemantic.no_min_descent_absurd_of_goodstein`.

## Lap-31 deliverable (green, axiom-clean `[propext, choice, Quot.sound]`)
`src/GoodsteinPA/ReductModel.lean` (NEW, wired into `GoodsteinPA.lean`). Presents `M`'s `ℒₒᵣ`-reduct as
`[ORingStructure M] [M ⊧ₘ* 𝗜𝚺₁]` — the substrate (`igoodstein`) carrier the lap-30 plan needs:
- `reductORing` — `ORingStructure M` read off `M`'s `LX`-interpretation of ring/order symbols.
- `reduct_eq_standardModel : inst.lMap Φ = @standardModel M reductORing` (via `standardModel_unique`;
  template = Foundation `TA/Nonstandard.lean`). Needs `[Structure.Eq LX M]`.
- `reduct_models_PA` / `reduct_models_isigma1` — `M ⊧ paLX ⟹ reduct ⊧ 𝗣𝗔 ⟹ ⊧ 𝗜𝚺₁`.

## The lap-31 finding (corrects the lap-30 plan — both in `PENDING_WORK` lap-31 §1/§2)
1. **Equality subtlety.** Tait `completeness_of_encodable` models have `=` = arbitrary relation, NOT
   real equality; the substrate needs real `=`. Honest precondition `[Structure.Eq LX M]`. SUPPLY it by
   re-routing `descentE` through `EQ.provOf` (Eq-models) — sound since `TI prec` is closed. **TASK A.**
2. **Opaque blob.** `hgood` is `reduct ⊧ ∀⁰ codeOfREPred goodsteinTerminates` (opaque r.e.-code), not
   `∃N, igoodstein m N = 0`. Bridging them IΣ₁-internally is the #4 arithmetization wall. **TASK B.**

## Next actions (hardest-first)
- **TASK A** (bounded): add `[Structure.Eq LX M]` to `paLX_models_TI_of_PA_provable`; re-route `descentE`
  via `EQ.provOf` + `𝗘𝗤 ⪯ paLX` (prove) + `Semiformula.toEmpty` of `TI prec`. Keep build green.
- **TASK B** (deep): connect `codeOfREPred goodsteinTerminates` to `igoodstein`-termination inside `M`.
  Option (ii) — transparent `goodsteinSentence` via `InternalBridge` (ℕ-faithful, same locked RHS) — is
  cleaner but touches the audit surface; needs anti-fraud review, do NOT do silently.
- **TASK C/D**: the M-internal descent (`no_min` → LX least-number) and slow-down+ineq6 internalization.

## LOCKED untouched (anti-fraud)
`Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `Statement.lean` `sorry`.

## src/ code sorries (3, unchanged)
`Statement.lean` (headline, locked), `Reduction.lean:52` (Route-A hook, off path),
`DescentSemantic.no_min_descent_absurd_of_goodstein` (**THE wall**, disclosed). ReductModel is `sorry`-free.

## Aristotle
Idle/correct (no isolated self-contained OPEN lemma yet; Task A/B/C are instance- or design-heavy).
