# HANDOFF — 2026-06-24 (lap 59) — FRESH-MIND review + crux-2 natural-sum order foundations

> **Branch** `plan` · **HEAD `20b5116`** · tree **clean** · `lake build GoodsteinPA` **green (1318 jobs)**.
> `wip/InternalNadd.lean` `lake env lean` **green, 0 sorries, axiom-clean** `[propext,choice,Quot.sound]`.
> Headline `peano_not_proves_goodstein` untouched (honest `sorry`; real `#print axioms` re-verified
> `[propext, sorryAx, choice, Quot.sound]`, **0 math axioms**, anti-fraud intact).
> Best-case honest endpoint unchanged: `[propext, choice, Quot.sound, PA_delta1Definable]`.
>
> 5 verified commits this lap (`4141ade`…`20b5116`). All crux-2 `wip/InternalNadd.lean` + docs.

## Lap-59 = FRESH-MIND REVIEW (direction VALIDATED + one reprioritization)
Re-verified the kernel from real `#print axioms` (above). Direction is sound: crux 2 (`PRWO→Con`, Buchholz
ord/R inside PA) is the hard generational crux; lap 58's internal Hessenberg natural sum `#`
(`wip/InternalNadd.lean`) for Buchholz's `õ` is the right chip. STATUS.md + PENDING_WORK.md refreshed.

**Banked this lap (all green, axiom-clean), continuing `wip/InternalNadd.lean`:**
- NF preservation: `isNF_insTerm`, `isNF_inadd` (order-induction through the 3 `insTerm_ocOadd` branches).
- Unit/prepend: `insTerm_prepend`, `inadd_zero_right`.
- ω-power layer: `thenV_one_right`, `icmp_omega_pow` (`icmp(ω^α)(ω^β)=icmp α β`), `inadd_omega_pow`,
  `inadd_single_term` (`(ω^e·n)#b = insTerm e n b`), `cmpV_add_left` (comparison invariant under common
  left summand).

**Reprioritization (acted on): ORDER > iC for crux 2.**
- The §4 descent (Buchholz Thm 4.2 / Lemma 4.1, now read verbatim, `scratchpad/buchholz-gentzen.txt:781-822`)
  consumes `#`'s ORDER laws, NOT `iC (a#b)≤iC a+iC b`. The iC bound is crux-1 width control; at most C4.
- `iC_inadd` does **not** follow from the naive `insTerm`-fold (over-counts the inserted coeff vs the whole
  accumulator; the true bound needs the NF leading-exponent structure). Deferred.

## NEXT LAP (hardest-first) — discharge `icmp_insTerm_congr` (THE crux nut)
The §4 `#`-facts are pinned as **F1–F4** in `PENDING_WORK.md` (lap-59 section, top). The deep one **F1**
(strict `#`-mono) is sharply isolated:
1. **`icmp_insTerm_congr` (NF A,B): `icmp (insTerm e n A) (insTerm e n B) = icmp A B`.** ← attack THIS.
   Pair order-induction on `m=⟪A,B⟫` (mirror `InternalONote.icmp_swap_aux`); motive carries `isNF`. Case
   grid on `icmp e (ocExp A)`×`icmp e (ocExp B)` ∈{0,1,2}² + A/B=0. Branch recipes (prepend/merge/recurse/
   mixed/base-dominance) are written out in PENDING. The base A=0,B≠0 needs a small dominance sub-lemma
   `insTerm_ge_term` (`icmp (ω^e·n)(insTerm e n B)=0` for B≠0).
2. **`inadd_left_cancel`** (NF g): `icmp (inadd g X)(inadd g Y)=icmp X Y` — induct on `g` via
   `inadd_ocOadd`+`inadd_single_term`, each step = `icmp_insTerm_congr`. ⟹ strict-mono corollary (F1).
3. **F2** (`αi0,αi1≺αi → icmp (ω^{αi0}#ω^{αi1})(ω^{αi})=0`) and **F3** (`ω^β·k≺ω^{β+1}`): short with
   `icmp_omega_pow`/`icmp_ocOadd` once F1 is in hand.
4. **F4** commutativity (NF canonical-form uniqueness): the other hard nut; defer behind F1.
5. Then **promote `InternalNadd` → `src/`** (+ `GoodsteinPA.lean` aggregator) and start **C0/C1** (arithmetize
   system Z + `iõ`/`idg`/`iord`), per `CRUX2-ORD-ASSIGNMENT-2026-06-24.md`.
- ALT (if F1 stalls): start C0 in a worktree (independent of the `#` order algebra).

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/` is wip. Aristotle OFFLINE. WebFetch dead. ⚠️ NEVER `git checkout <wip file>` — `cp` from scratchpad.
Crux 1 (`seqDescent_dominated`, tractable milestone) untouched this lap — see lap-56 STATUS box.
