# HANDOFF — 2026-06-23 (lap 54) — crux-1 internal girder built end-to-end + `hdef` chain started

> **Branch** `plan` · **HEAD `021091b`** · tree **clean** · `lake build GoodsteinPA` **green (1315 jobs)** ·
> headline `peano_not_proves_goodstein` = honest `sorry`
> (`#print axioms` = `[propext, sorryAx, choice, Quot.sound]`, 0 math axioms, anti-fraud intact,
> re-verified this lap). **8 commits** `cb12499..021091b`. One module promoted to `src/` (`InternalIg`).
> All new lemmas axiom-clean (`[propext, choice, Quot.sound]`), sorry-free.

## What this lap did — the WHOLE internal-Grzegorczyk crux-1 girder, machine-checked end-to-end

Started from lap-53's "3/5 igt props built" and drove crux 1 (`goodstein_implies_prwo` route) far:

### (1) ALL 5 igt-interface props (`cb12499`) — `src/GoodsteinPA/InternalIg.lean`
- **`higt_within` — THE deep brick = internal `Grz.g_desc`** (`m < iF l n → icmp (ig l n (m+1)) (ig l n m)
  = 0`). Meta-induction; base `icmp_ig0_desc`; step via the **new `iblock_step` dichotomy** (within:
  `iblockOff↦+1`, fixed index ⟹ `icmp_iblk_within` + IH with `iblockOff_lt_width`; boundary:
  `iblockOff↦0`, index `+1`, coeff strict drop via `iblockIdx_lt` + monus arith ⟹ `icmp_iblk_boundary`;
  exhaustion: `ig(m+1)=0 ≺` via `icmp_zero_ocOadd`). KEY insight: `iblockIdx … n m` vs `n (m+1)` read
  DIFFERENT width codes; `iblockIdx_common`/`iblockOff_common` re-express the `m`-state on the longer
  common code `iwseq…(m+1+1)` (prefix-invariance) so the `BlkRec` step laws apply.
- **`ig_ne_zero` = `higt0`** (in-range nonzero). Supporting generic bricks: `iblockIdx_common`/
  `iblockOff_common`/`iblock_step`/`ipsum_le_add`/`ipsum_le_of_le`/`iter_le_ipsum_diag`(=`Grz.F_succ_le_psum`)
  /`iblockIdx_lt`(=`Grz.blockIdx_lt`).

### (2) Totalized `igtTot` (`9c190c0`) — unconditional props
`igtTot l n m := if m < iF l n then ig l n m else ig0 0 0`. Resolves the lap-53 `higt0` reconciliation:
`isNF_igtTot`/`igtTot_ne_zero`/`higt_exp_igtTot`/`iC_igtTot_bound` are now **UNCONDITIONAL** (the
`salpha_*` interface demands them ∀ n m; raw `ig` is 0 out of range). `igtTot_within` stays in-range —
the single seam where domination (Lemma 3.2) is consumed.

### (3) Promoted `InternalIg` → `src/` (`5e63402`); aggregator updated; build 1314→1315.

### (4)+(5) Cor 3.4 → Thm 3.5 assembly (`ff15574`,`567ceca`) — `wip/StdCor34.lean`
- **`salpha_igtTot_spec l₀ (0<l₀) …`**: instantiates `salpha (↑l₀) β blk off (igtTot l₀)`, proves the
  NF + (∃K, tight iC-bound) + ≺-descent triple. The 4 unconditional `igtTot` props discharge
  `salpha_isNF`/`salpha_C_le` outright; `salpha_desc` reduces to the **single domination input `hdom`**
  (`off j + 1 < iF l₀ (blk j)`) via `igtTot_within`.
- **`bbeta_of_igtTot`**: feeds the triple into `InternalThm35.bbeta_*` ⟹ full Thm 3.5 sequence.
- **`bbeta_of_igtTot_blkRec`**: specializes `blk/off := BlkRec.blk/off wseq`, discharging the bookkeeping
  (`hblk_dich`/`hoff_adv`/`hnm`) from the src `BlkRec` laws.

### (6) `crux1_internal_run` (`a1803dd`) — WHOLE girder → non-terminating Goodstein run
`nonterminating_of_bbeta_facts` repackages the `bbeta` triple as `DescentSlowdown.nonterminating_of_slowdown`'s
input (`iCanon (r+1) = iC ≤ r+1`, definitional). **`crux1_internal_run`** chains `igtTot → salpha → bbeta
→ Lemma 3.6` ⟹ `∃ m₀, ∀ k, 0 < igoodstein m₀ k` (non-terminating internal Goodstein run = the
contradiction). The entire internal-Grzegorczyk crux-1 girder is now machine-checked, with EXACTLY THREE
named-hypothesis gaps remaining (see below).

### (7)+(8) `hdef` definability chain STARTED (`e5dc43b`,`021091b`) — `src/GoodsteinPA/InternalIg.lean`
- Leaves `iblk_definable`/`iblockIdx_definable`/`iblockOff_definable` (explicit `DefinableFunction₂/₃.comp`
  terms — `definability` aesop blows depth on nested `ocOadd`/`iwseq`, memory `definability-aesop-depth-blowup`).
- **`definableFunction_ite`** — the KEY UNLOCK: `fun v ↦ if P v then f v else g v` definable from
  Δ-definable `P` (both `P`,`¬P`) + definable `f`,`g`, via graph disjunction. Foundation has NO direct helper.

## NEXT (hardest-first) — finish `hdef`, then the two deep bricks
Crux 1 = `crux1_internal_run` modulo exactly THREE named hypotheses:
1. **`hdef`** — `𝚺₁-Function₁ (bbeta K s (salpha (↑l₀) β (BlkRec.blk wseq) (BlkRec.off wseq) (igtTot l₀)))`.
   **Most tractable; resume HERE.** Leaves + `definableFunction_ite` are built. Remaining links:
   - `ig0_definable`: `show DefinableFunction (fun v : Fin 2 → V => if v 1 < v 0 + 2 then ocOadd 0 (v0+2-v1) 0
     else 0)` (defeq to `fun v => ig0 (v0)(v1)` since `ig0` unfolds to the `if`), then
     `definableFunction_ite` with `P := fun v => v 1 < v 0 + 2` (hP/hnP via `<`/`≤` definability —
     shallow, `by definability` likely OK), `f := ocOadd 0 (v0+2-v1) 0` (explicit `ocOadd`-comp + a
     `sub`/`add`-comp for `v0+2-v1`), `g := const 0`.
   - `ig_definable : ∀ l : ℕ, 𝚺₁-Function₂ (ig l)` — **meta-induction on `l`** (NOT internal): base
     `ig 0 = ig0` (ig0_definable); step `ig (l+1) = if m < iF(l+1)n then iblk(l+1) (max 1 (n-iblockIdx))
     (ig l (iIter…) (iblockOff)) else 0` — `definableFunction_ite` with the recursive `ig l` (IH) +
     `iblk`/`iblockIdx`/`iblockOff`/`iIter`/`iF` leaves + `max`/`sub` comps.
   - `igtTot_definable` (ite over `ig l`), `bbtail_definable` (no ite — `iadd`/`iomul`/`ocOadd`/`/`/`%`
     comp), `bbeta_definable` (ite over `iwtower`/`bbtail`), `icorAlpha_definable`/`salpha_definable`
     (`iVbigMul`+`iadd`+`igtTot`+`blk`/`off` comp). Then assemble `hdef` for the full `bbeta∘salpha`.
   - Watch: `salpha`/`icorAlpha` were made `[local irreducible]` in `wip/StdCor34.lean` (lap-49 `iVbigMul`
     defeq-loop note) — definability proofs may need the real defs; prove them in `InternalIg`/a new src
     module where they're reducible, or `unfold` locally.
2. **`hdom` — domination (Rathjen 2014 Lemma 3.2)**: `off j + 1 < iF l₀ (blk j)`, i.e. the primrec width
   `iC(β(t+1)) ≤ iF l₀ t`. DEEP, tied to the concrete gentzen β + standard level l₀. mathlib `ack`/
   `exists_lt_ack` gives the ℕ-template (per `Grzegorczyk.lean:38`); `not_dominated_of_diag_le`/
   `F_diag_not_dominated` are the NEGATIVE direction already in src. Note the `n=0` degeneracy
   (`F l 0 = 0`) ⟹ Rathjen's `max(2,·)` prefix (`Grzegorczyk.lean:748-756`).
3. **Input `β`**: the gentzen ε₀-descent `n ↦ ord(Rⁿ d)` as a ≺-descending NF V-sequence with
   `iC(β(blk j)) ≤ Cβ + j`. Tied to the still-placeholder `ord`/`R`/`gentzenDescentφ` encodings
   (`wip/GentzenCon.lean`). Plus the final REFLECTION lift from `crux1_internal_run`'s V-internal run to
   the PA-provability statement `𝗣𝗔 ⊢ prwoInstance seq` (`goodstein_implies_prwo` `sorry`).

## Hardest wall (unchanged) — crux 2 eq (5) `ord(R d) ≺ ord d` (🟠 generational, cited)
`wip/GentzenCon.lean` isolates it to the single `ord_R_descends` axiom. Chip opportunistically; not the
active thread. Direction KEEP (drive crux 1).

## Aristotle OFFLINE. WebFetch dead. LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS,
`goodsteinTerminates`, `Statement.lean` headline. `wip/StdCor34.lean`/`wip/GentzenCon.lean` are wip
(crux-1 assembly over named hyps + crux-2 scaffold). `src/GoodsteinPA/InternalIg.lean` is the lap's home.
