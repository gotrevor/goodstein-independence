# Handoff: lap 139 — pair-parametric critical-cut half layer (dissolves the tag-5/6 engine re-key)

**Date**: 2026-06-26 · **Branch**: plan · **HEAD**: `d5e9e44` · build 🟢 green (`lake build GoodsteinPA`, **1326**).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms; lone `sorryAx` = crux-2). **3 green commits this lap** (`bde9e72` cutFormulaAt + haux0_at →
`a4076c6` haux1_at → `d5e9e44` ¬-case halves). **No src sorry dropped** — none was feasible (see verdict);
the advance is a structural unlock + a decisive reconciliation of the primary/secondary fronts.

## ⚖️ DIRECTION reconciliation (READ FIRST — settles the lap-137 "hardest-first (A)" vs the tag-5/6 work)
DIRECTION.md (lap-137) makes the **Σ₁ ε₀-descent / (A) `exists_sigma1_descending_step`** the hardest-first
PRIMARY, with `descent_step_K_majorIdx` the *permitted secondary front*. **These are the SAME deep content:**
- **(A) via DIRECTION's `redLeast` μ-min** (`g w := μ d'. [ZDerivesEmptyR d' ∧ icmp(iord d')(iord w)=0]`) is
  **BLOCKED on a wrong-polarity witness bound.** The μ graph is `𝚫₁`, BUT a TOTAL `g : V → V` (which
  `IIter`/(B0) needs) requires completing `g` off the orbit; the completion's graph has a `¬∃d', P(w,d')`
  disjunct = **Π₁** (unbounded), so `g`'s graph is `Σ₁ ∨ Π₁`, NOT `Σ₁`. Fix needs a primrec WITNESS BOUND
  `∃ d' ≤ B(w), P(w,d')` (then bounded-μ is `𝚫₁`-total). No such `B` is built; `wip/WitnessBound.lean` is an
  unrelated lap-4 Towsner artifact.
- **(A) via the concrete `redStep`** (the HANDOFF route): `redStep` is a total `𝚺₁` PR.Construction →
  definability + `gDef` are FREE, no μ, no bound. Its descent clause `∀ w ∈ ZDerivesEmptyR, ZDerivesEmptyR
  (redStep w) ∧ icmp(iord(redStep w))(iord w)=0` IS exactly `ZDerivesEmptyR_descent_step` / `descent_step_K_majorIdx`.
- **⟹ The secondary front (`descent_step_K_majorIdx`, tag-5/6 + tag-3) IS the tractable path to the primary (A).**
  Crux-1 delivers the SEQUENCE-form `InternalPRWO` (no `𝚺₁` ε₀-descent), NOT an ε₀-LNP, so a least-element
  restructure cannot shed (A) either (it would itself need to build the descending sequence = (A)). The
  existence-form reframe does NOT obviate the deep content — re-confirmed this lap from these fresh angles.

## ⭐ THE advance — the FULL critical-cut half-derivation layer is now PAIR-PARAMETRIC (Crux2Blueprint)
The lap-129 STALL forced the cut onto a deterministic redex finder (`redexI`/`redexJ`); the existence form
PICKS the pair `(cutPartner, majorIdx)` one-shot. The soundness was already pair-parametric at the
`iCritReductG` level (`ZDerivation_iCritReductG_of`, InternalZ:9736, takes the modified premise seqs
explicitly) and the REPLACE workhorse `ZDerivation_iCritReplaceReduce_of`/`_general` is index-parametric. The
LONE obstruction to building the principal cut at an ARBITRARY pair WITHOUT the forbidden engine re-key was
that `cutFormula d` (InternalZ:6648) reads `redexI d`/`redexJ d`. **Landed (all proven, axiom-clean, 0 new sorries):**
- **`cutFormulaAt i j d`** (Crux2Blueprint, before `haux0`) — `cutFormula` with the redex pair abstracted;
  `cutFormula d = cutFormulaAt (redexI d) (redexJ d) d` by `rfl` (`cutFormulaAt_redex`, `@[simp]`).
  Readouts `cutFormulaAt_all` (∀: `F(k)`-instance) / `cutFormulaAt_neg` (¬: `A`).
- **All four half-derivations, explicit-pair `_at` twins** (originals re-derived as the `(redex,…)` instance):
  - `ZDerivation_corrected_haux0_at {i j}` (∀ R-half, cut formula `cutFormulaAt i j d`)
  - `ZDerivation_corrected_haux1_at {j Cc}` (∀ L-half, abstract cut formula `Cc` — `haux1` only USES it via `hsj`/`hCwff`)
  - `ZDerivation_corrected_haux1_neg_at {i Cc}` (¬ antecedent half)
  - `ZDerivation_corrected_haux0_neg_at {j Cc}` (¬ succedent half)
  ⟹ the full soundness SUPPLY for tag-5 (`zAxAll`) AND tag-6 (`zAxNeg`) at any cut pair, NO `redexI/redexJ`.

## 🎯 NEXT ATTACK (the tag-5/6 existence-form assembly, then wire to `descent_step_K_majorIdx`)
1. **Explicit-pair `iCritReductG` soundness at an arbitrary pair** — assemble `haux0_at`+`haux1_at` (∀) and
   `haux0_neg_at`+`haux1_neg_at` (¬) via `ZDerivation_iCritReductG_of` directly (NOT `iRcritG`, which bakes
   `redexI/redexJ`). Reduct = `iCritReductG (fstIdx d) (cutFormulaAt i j d) (zKrank d−1) (zKrank d) (zKrank d)
   (seqUpdate (zKseq d) i (haux0-reduct)) (seqUpdate (zKseq d) j (haux1-reduct))`. Thread the cut-rank drop
   `irk (cutFormulaAt i j d) ≤ zKrank d − 1` (∀: `irk_cutFormula_lt`-style on `cutFormulaAt_all` + `irk_substs1`).
2. **Wire `(cutPartner, majorIdx)`:** at a tag-5/6 major premise (succedent `^⊥`), `majorPrem_zAxAll/zAxNeg_cutPartner`
   (banked, InternalZ:9217/9245) give `i' < majorIdx` with `chainAsucc ds i' = ^∀ p` / `inegF p`.
   ⚠️ **GAP (the genuine residual):** the cutPartner `i'` has SUCCEDENT `^∀ p`, but `haux0_at` needs `znth ds i'
   = zIall sᵢ a p d0` (a genuine R-INTRO of `∀p`, not merely "some premise with succedent `∀p`"). Proving the
   cutPartner is a PRINCIPAL R-intro is the open content (Buchholz criticality: the R-side of the principal
   cut is the I-rule introducing the cut formula). This is where the deep tag-5/6 soundness now concentrates.
3. **Descent** of the explicit-pair reduct: `iord_descent_iRcrit_of_chain'` is already pair-parametric (reads
   the supplier via six per-redex bundle facts); re-key its callers off `redexI/redexJ` to `(i', majorIdx)`.
4. Then `descent_step_K_majorIdx` tag-5/6 = (soundness 1+2) + (descent 3), and (A) follows via the concrete
   `redStep` (Ind = `red d`; K tag-5/6 = this explicit-pair reduct; K tag-3 = `iIndReductSeqG`; K tag-4 = recursion).

## ⚠️ Forbidden / do-NOT-undo
- Do NOT re-key `red`/`redexI`/`redexJ` to `majorIdx` (the engine swap — DIRECTION-forbidden). The pair-parametric
  layer is the CORRECT alternative: pick `(cutPartner, majorIdx)` directly, no finder.
- Do NOT revert the `_at` lemmas to redexI/redexJ-only; the originals remain as instances (`cutFormulaAt_redex`).
- Do NOT attack (A) via the `redLeast` μ-min without first building the primrec witness bound (wrong polarity).

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: `cutFormulaAt`/readouts + 4 `_at` half-lemmas (search `_at {`); the
  `(redexI,redexJ)` originals delegate just below each. `descent_step_K_majorIdx` (sorry, the K-case math),
  `exists_sigma1_descending_step` (A, sorry).
- `src/GoodsteinPA/InternalZ.lean`: `ZDerivation_iCritReductG_of` (9736, pair-parametric soundness),
  `ZDerivation_iCritReplaceReduce_of`/`_general` (5830, index-parametric REPLACE), `cutFormula` (6648),
  `iRcritG` (6781), `majorPrem_zAx{All,Neg}_cutPartner` (9217/9245), `majorIdx_botOrbit_reducible` (9155).
- `DIRECTION.md` CURRENT DIRECTIVE (lap-137, binding) · `PENDING_WORK.md` lap-139 · `STATUS.md`.

---
**→ Next session: HEAD = `d5e9e44`, build green. Full pair-parametric half layer DONE. The deep tag-5/6
soundness now concentrates in ONE gap: the cutPartner is a principal R-intro (step 2 above). Assemble the
explicit-pair `iCritReductG` soundness (step 1), then close that gap, then descent (step 3). (A) follows via
the concrete `redStep` — do NOT chase `redLeast`/μ without a witness bound.**
