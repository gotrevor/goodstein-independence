# Handoff: lap 149 — FRESH-MIND REVIEW + tag-3 freshFlag DROPPED; next = the code-recursion crux

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms).
Operator SOLE-OBJECTIVE = **M1b-term** (crux-2 termination).

## ✅ DONE THIS LAP — tag-3 freshFlag sorry DROPPED (a live-path src sorry; the operator's bar)
After the review (below) set the mandate, executed it end-to-end. The residual
`freshFlag (π₁ at'') p' (seqAnt s') = 0 := sorry` at `Crux2Blueprint` (descent_step_K_noncrit_repMajor tag-3)
is GONE — **`descent_step_K_noncrit_repMajor`'s tag-3 (`zInd`) branch is now fully sorry-free** (its only
remaining sorry is the tag-4 forward-ref to `descent_step_K_noncrit_repMajor_K`). Re-verified in-kernel:
`ind_reduct_botSucc_of_fresh` + `zFresh_zsubst` are now **axiom-clean (no sorryAx)**: `[propext, choice,
Quot.sound]`; headline + `false_of_ZDerivesEmpty` unchanged `[propext, sorryAx, choice, Quot.sound]` (0 math
axioms, no drift).

**How (the mechanism that worked):** strengthened `zFreshNext`'s tag-3 branch (`Zsubst.lean`) to carry
`freshFlag (zIndEig d) ⊥ (seqAnt (fstIdx d))` — a **dummy `^⊥` matrix**. KEY INSIGHT: the real zInd matrix is
`IsSemiformula 1` (induction var), which would force a level-1 generalization of `fvSubst_numeral_fresh_subst`
(it wants `IsUFormula`); but `ind_reduct_botSucc_of_fresh` consumes ONLY the antecedent part of the flag
(`freshFlag_snd`/`_wff`), never the matrix part — so a vacuous `^⊥` matrix (a `UFormula`) carries exactly
what's needed and reuses `freshFlag` + its definability with ZERO new boilerplate and no level-1 yak-shave. On
the ⊥-orbit the conclusion formula is `⊥` anyway (`eq_falsum_of_substs1_falsum`), so antecedent-only freshness
IS the complete eigenvariable condition — faithful, not a hack. The 9-edit ripple (zFreshNext body + Def
σ-clause + defined simp set + `zFresh_zInd` + `zFresh_zsubst` tag-3 + 5 max-reprojection sites + the
hypothesis matrix `p→⊥` + the residual discharge) is the EXACT shape of the lap-146 `zIndWff` ripple.

## 🎯 NEXT (per DIRECTION.md lap-149) — the CRUX, no more leaf-hunting
The only live crux-2 sorries are now: **tag-4 K-recursion** `descent_step_K_noncrit_repMajor_K`
(`Crux2Blueprint:2938`), **tag-5/6 cut-partner** `descent_step_K_noncrit_axMajor`:3012, **(A) gDef**:3135.
Leaves 2938+3012 ARE the genuine crux — the **general `Γ→⊥` cut-reduction by strong induction on derivation
CODE** (Buchholz Thm 2.1 / §14.253-254; NOT `iord`-recursion, PRWO/Gödel-barred). START it: decompose into the
code-induction skeleton (`ZDerivation`-valued same-end-sequent descending `Rep`-reduct of a structurally-
smaller premise). A disclosed sub-`sorry` decomposition is a successful crux lap.

---

## (The review verdict — context for the above)
This was an **altitude review lap**; the deliverable is the refreshed binding direction (DIRECTION.md lap-149).

## ⭐ The review verdict
Direction is **SOUND and KEPT** (existence-form pivot, off `red`, structural/code induction). lap-146's
directive mandate is **DONE** — `descent_step_Ind` DROPPED (lap 146); laps 147-148 correctly advanced into
Buchholz §5.2 noncritical (has-redex half PROVEN lap 147; no-redex decomposed faithfully per §14.254a/b
lap 148, tag-3 repMajor proven modulo a freshFlag residual). **No harmful repetition** — each of laps 144-148
closed or precisely decomposed a real piece.

**Re-verified in-kernel** (`lake env lean`, throwaway module): `false_of_ZDerivesEmpty` (M3),
`ZDerivesEmptyR_descent_step` (E'), `descent_step_K_noncrit_recurse` (§5.2 no-redex dispatcher), AND the
headline all `= [propext, sorryAx, Classical.choice, Quot.sound]` — **0 custom math axioms**. The entire
crux-2 chain is reduced to **four disclosed live `sorryAx` leaves**:
1. **tag-3 freshFlag** residual — `Crux2Blueprint:2974` (the LAST tractable leaf).
2. **tag-4 K-recursion** — `descent_step_K_noncrit_repMajor_K`:2934.
3. **tag-5/6 cut-partner** — `descent_step_K_noncrit_axMajor`:3002.
4. **(A) gDef** — `exists_sigma1_descending_step`:3125.

**The one concern flagged:** a *crux-neglect signal is forming*. Leaves (2)+(3) ARE the genuine deepest crux —
the **general `Γ→⊥` cut-reduction by strong induction on derivation CODE** (Buchholz Thm 2.1 / §14.253-254;
NOT `iord`-recursion, PRWO/Gödel-barred) — and it has stayed untouched while laps closed the *surrounding*
machinery (Ind reducts, `descent_step_K_replace` plumbing, dispatchers). After tag-3 freshFlag (the last easy
leaf), the residual is purely the two genuine cruxes, so the grind MUST turn to them — no more leaf-hunting.

## ▶ MANDATED next move (DIRECTION.md lap-149) — DROP tag-3 freshFlag. A real src-sorry DROP.
Strengthen `zFreshNext`'s tag-3 (`zInd`) branch to carry the I∀-style eigenvariable freshness, **mirroring the
tag-1 clause** (`Zsubst.lean:1671`, which already carries `freshFlag`). This is the **EXACT shape** of the
lap-146 `zIndWff` membership→shape ripple that DROPPED `descent_step_Ind`: focused, definability-dominated, NOT
a 64-site cascade (`zFresh` is C-free). The full 7-step ripple (body :1673 → Def σ-clause :1683 → simp set
:1691 → `zFresh_zInd` :1812 → `zFresh_zsubst` tag-3 :1947 + new `freshFlag_eq_zero_of_zfresh_zInd` → re-project
the 4 max-extraction sites → discharge :2974) is spelled out in **`PENDING_WORK.md` lap-149**. After it,
`descent_step_K_noncrit_repMajor`'s tag-3 branch is sorry-free.

## ▶ THEN — the CRUX
General `Γ→⊥` reduction by code-induction (leaves 2934 + 3002) + gDef (3125). START the general reduction:
decompose into the code-induction skeleton (`ZDerivation`-valued same-end-sequent descending `Rep`-reduct of a
structurally-smaller premise, recursion on the finite code). A disclosed sub-`sorry` decomposition of THIS is a
successful crux lap even with nothing closed. Re-read `scratchpad/buchholz-gentzen.txt` §14.253-254.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any descent branch with `red`; do NOT re-introduce criticality into the `iRKcCrit` path.
- Do NOT attempt the general step by `iord`-recursion (PRWO/Gödel-barred) — structural/code induction ONLY.
- Do NOT use `redLeast`/μ-min for (A) gDef (refuted lap-139).
- Do NOT collapse the repMajor/axMajor §14.254a/b split (judge-C2/C3 endorsed).
- Do NOT jump to the general recursion / gDef BEFORE freshFlag drops (it is teed-up and validates the no-redex
  machinery end-to-end). Do NOT pursue `zReg`/`zFresh`/`zSeqAnt` folds as a *goal* — the tag-3 fold is mandated
  ONLY because it directly drops :2974.
- Do NOT touch the off-path dead `red`-soundness sorries {:82,:1257,:1367,:1563,:1653,:1765,:1868} AS STATED.

## 📁 Key anchors
- Residual: `freshFlag (π₁ at'') p' (seqAnt s') = 0 := sorry` at `Crux2Blueprint:2974` (in
  `descent_step_K_noncrit_repMajor`, tag-3 branch).
- Pattern to mirror: tag-1 I∀ `zFreshNext` clause `Zsubst.lean:1671`; extractor
  `freshFlag_eq_zero_of_zfresh_zIall` :1872; `zFresh_zsubst` tag-1 case :1935-1941.
- Consumer (already takes the freshness): `ind_reduct_botSucc_of_fresh` `Crux2Blueprint:2770`.
- `freshFlag` def `Zsubst.lean:1613`; `freshFlag_eq_zero` :1647; `freshFlag_zsubst_eq_zero` :1661.
- Accessors: `zIndEig (zInd …)=π₁ at'` `InternalZ:1654/1658`; `zIndP (zInd …)=p` `InternalZ:169/236`.
- The crux leaves: 2934, 3002; gDef 3125. Source: `scratchpad/buchholz-gentzen.txt` §14.253-254.

---
**→ Next session: execute the tag-3 freshFlag DROP per PENDING_WORK lap-149 (the operator's bar — a src sorry
drops), keep build green + headline `[propext, sorryAx, Classical.choice, Quot.sound]`, THEN turn to the
general code-recursion crux. NEVER `red`; NEVER `iord`-recursion for the general step.**
