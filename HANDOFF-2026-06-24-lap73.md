# HANDOFF — lap 73 · RUNG 1 STEP 1 DONE + STEP 2 SUBSTRATE COMPLETE

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md` (top), then `STATUS.md`.
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = **1323 jobs**. Tree clean. HEAD `70a3dfb`.
> (4 green commits this lap; HEAD started at `e9f89a8`.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**.
Sole math wall = crux-2, localized to **`RedSound`** (`InternalZ.lean`): genuine internalized
cut-elimination. Lap-70 ladder: rung 0.5 ✅ → **rung 1 `zsubst`** (DEFINED lap 72, CORRECTNESS lap 73) →
rung 2 (Ind reduct) → rung 3 (K/cut reduct) → rung 4 (`RedSound` tag-dispatch).

## ✅ THIS LAP — rung-1 step 1 (`fstIdx_zsubst`) + step-2 substrate (full subst-commutation)
All axiom-clean `[propext, Classical.choice, Quot.sound]`, green 1323. Commits:
- `0356643` **step 1**: `zsubst` table structural correctness + 7 recursion eqs + **`fstIdx_zsubst`**
  (`ZDerivation d → fstIdx (zsubst d a t) = fvSubstSeqt a t (fstIdx d)`).
- `91ae176` step-2 substrate pt 1: `IsUTerm.termFvSubst`, `IsUFormula.fvSubst`, `fvSubst_neg`,
  `inAnt_fvSubstSeq`, `fvSubst_inegF`.
- `e4d57b3` **`termFvSubst_termSubst`** (term-level subst lemma; closedness in fvar `x=a` case).
- `70a3dfb` **`fvSubst_subst`** (formula subst lemma, `pi1_structural_induction`) + binder substrate
  (`termBShift_eq_self_of_closed`, `termFvSubst_termBShift`, `termFvSubstVec_qVec`) +
  **`fvSubst_substs1_fvar`** (`a'≠a → fvSubst a t (substs1 ^&a' p) = substs1 ^&a' (fvSubst a t p)` —
  Buchholz regularity, the zIall/zInd succedent transfer).

The genuine hard math of rung 1 (the freshness/closedness-gated substitution lemma) is DONE.

## ▶ NEXT — `ZDerivation_zsubst` assembly (rung-1 step 2 proper)
See `PENDING_WORK.md` lap-73 box for the full plan. Two resolved findings:
- **(A) Freshness = `d ≤ a` code-bound** (no tree predicate): every internal eigenvariable `e < n ≤ d`,
  so `d ≤ a ⟹ e ≠ a` discharges `fvSubst_substs1_fvar`. Motive `P d := ∀ a, d ≤ a → ∀ t, IsSemiterm 0 t
  → ZDerivation (zsubst d a t)` over `zDerivation_induction`, build via `zDerivation_iff.mpr` 7-tag.
  Caveat: rung-2's `zsubst d1 at' j` may force generalizing to a real "a∉eigenvars(d)" predicate.
- **(B) WELL-FORMEDNESS GAP (the blocker)**: `ZPhi`'s I∀/I¬/Ind disjuncts lack principal-formula
  `IsSemiformula`/`IsUFormula` that the commutation lemmas need. Fix = lap-71 cascade: add
  `IsSemiformula ℒₒᵣ 1 p` to `zIallWff`/`zIndWff`, `IsUFormula ℒₒᵣ p` to `zInegWff`. Small blast radius
  (ZPhi plumbing threads `…Wff` opaquely); only `…WffDef`/`_defined` change. Start with `zInegWff`.

Then **step 3** `iotil_zsubst = iotil` (õ subst-invariance), then **rung 2**.

## Parallel front (independent of the reduct)
- `PA_delta1Definable` upstream Foundation `axiom` (`Incompleteness/Examples.lean:17`) — mandatory for
  axiom-free; bounded arithmetization, no deep math.

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. Do NOT axiomatize `RedSound` or fake the reduct.
