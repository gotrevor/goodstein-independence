# HANDOFF — lap 111 · DEEP REFLECTION (every-9th) · direction KEPT, structural lever identified

> **RESUME BATON.** Branch `plan`, tree CLEAN. Build 🟢 green (`lake build GoodsteinPA`, **1326**). Headline
> footprint re-verified in-kernel this lap (`lake env lean`):
> `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` (0 math axioms; lone
> `sorryAx` = crux-2); `goodstein_implies_consistency` = same; `peano_not_proves_consistency` /
> `not_proves_of_implies_consistency` / `goodsteinSentence_faithful` all axiom-clean.

## ⭐ GRIND ADDENDUM (lap 111, after the synthesis commit) — step (1) STARTED + atom branch CLOSED
The governor resumed; I began NEXT step (1), the disjunctive reformulation. **LANDED (green 1326):**
- `iord_descent_red` retyped to **`red d = d ∨ icmp (iord (red d)) (iord d) = 0`**; all working branches
  (Ind / critical / I∀ / I¬ / Ind-replace) wrapped in `Or.inr`. `iord_red_iterate_descends` retyped to the
  matching per-step **dichotomy** `red^[n+1] z = red^[n] z ∨ icmp ≺ = 0`.
- **Atom branch (tag 0) genuinely CLOSED** via `Or.inl (red_zK_fixpoint_of_atom_selected …)` — it is a TRUE
  node-fixpoint (`tp = isymRep`, `tpReduce_isymRep s 0 = s`, premises unchanged). One fewer `sorry`.
- **KEY in-kernel finding (sharpens the obstruction):** axAll/axNeg (tags 5/6) are **NOT** clean fixpoints.
  `red dᵢ = dᵢ` but `tp dᵢ = isymLk`, so `red_zK_rep_nonchain` STRIPS the conclusion (`tpReduce isymLk s 0 ≠
  s`) while `iord` (premise-only, lap-110) is UNCHANGED ⟹ **neither disjunct holds** at those nodes. They
  close ONLY via the **SELECTION INVARIANT** (a valid ⊥-orbit K-node's `permIdx` never selects a lone axiom
  L-leaf ⟹ branch vacuous). This is the genuine remaining obstruction, now precisely located.

**Residual `sorry`s in `iord_descent_red` (4, down from 5):** chain-REPLACE IH (strong-induction recursion) ·
splice `hr'` (lap-110 cut-formula strip) · axAll · axNeg (both = the selection invariant). Headline footprint
re-verified clean post-change: `[propext, sorryAx, choice, Quot.sound]`.

## NEXT (updated by the grind) — the SELECTION INVARIANT is now the named crux
The disjunctive reformulation revealed the single honest obstruction: **`permIdx` of a valid ⊥-orbit K-node
never selects a lone axiom L-leaf (axAll/axNeg/atom).** Prove this (then axAll/axNeg branches are vacuous and
atom is covered) — likely from `zKValid`/`hvalid` + the K-rule critical-pair structure (a lone `L^k_A` axiom
without a matching `R_A` is not the redex). Then: the chain-REPLACE strong induction (recursion on derivation
code), then the splice `hr'` via the lap-110 cut-formula strip, then the ∀/¬-inversion (the prize), then M3/M2.

## What this lap was (reflection, not grind)
Every-9th deep reflection. Primary deliverable = `REFLECTION-2026-06-25-lap111.md` (read it). No proof
mechanics attempted before the synthesis. Statement re-audited vs the paper — **no drift**. One doc-drift
fixed (`Reduction.lean` no longer claims `peano_not_proves_consistency` carries `PA_delta1Definable` — clean
since lap 89; kernel confirms).

## The call (full reasoning in REFLECTION + NEXT_STEPS lap-111 box)
Direction KEPT: axiom-free `𝗣𝗔 ⊬ Goodstein`, carrier = the Σ₁ engine `red`/`iord`. Three findings:
1. **The `iord_descent_red` FIXPOINT branches are a SELECTION bug, not descent gaps.**
   `Crux2Blueprint:568/610/612` (atom/axAll/axNeg-selected, `red d = d`) + `:594` (chain-REPLACE IH, false at
   atom-fixpoints) are all the engine selecting an axiom LEAF as redex (no cut → stalls). Same defect diagnosed
   laps 104/107, re-surfacing branch by branch. **STOP grinding per-branch descents for these.**
2. **Effort imbalance.** Descent side (`iord_descent_red`) ~80% done; the ∀/¬-INVERSION
   (`ZDerivation_red_zK_crit`, `:96/100`) ≈0% on the engine and AVOIDED (only attempt `ZInf.allInv` killed
   VACUOUS lap 107). The inversion is the prize.
3. **Lap 110 was real progress** (`iCritReductG` principal-vs-stripped cut-formula root cause — correct).

## NEXT (ordered — start here)
1. **(do FIRST, structural — collapses ~4 sorries)** Reformulate `iord_descent_red` to disjunctive
   `red d = d ∨ icmp (iord (red d)) (iord d) = 0`; reformulate `false_of_ZDerivesEmpty` (`Crux2Blueprint:673`)
   to "orbit terminates at a `red`-fixpoint (PRWO well-foundedness) ⟹ that fixpoint is cut-free ⟹ a cut-free
   `∅→⊥` is absurd." Fixpoint branches close via the LEFT disjunct (banked `red_zK_fixpoint_of_atom_selected`).
   Real obligation collapses to `red d = d ⟹ d cut-free` (selection correctness) + `no cut-free ∅→⊥`. Contained:
   `iord_descent_red` is consumed only at `:644` (`iord_red_iterate_descends`) → `:673`. **Verify the engine
   actually gives `red d = d` on the fixpoint branches before committing to the rewrite** (it should — that's
   the lap-109/110 finding); if it resists, record why and fall back.
2. **(mechanical — lap-110 plan)** `iCritReductG` cut formula → STRIPPED `A(d)`; close splice `hr'` (`:608`)
   via `irk_cut_lt_rank_forall`/`_neg` (`InternalZ:409/415`). Descent lemmas immune. See
   `ANALYSIS-2026-06-25-lap110-iCritReductG-cut-formula-strip.md`.
3. **(THE PRIZE, hardest-first)** ∀/¬-INVERSION on the engine (`ZDerivation_red_zK_crit`): two halves derive
   the stripped endsequents. Template `Zinfty.allInv`/`andInv`/`orInv` (`src/Zinfty.lean`, axiom-clean, META,
   one-sided Tait). Bridge to the two-sided V-code engine (the lap-106 fork). Multi-year core — do not defer.

Then downstream (path-independent): M3 `false_of_ZDerivesEmpty` Σ₁-graph + crux-1 PRWO; M2
`foundation_bot_to_Z_empty` (`:660`) Foundation→Z embedding; wire crux-1∘crux-2 → `Reduction.lean:68` →
headline (ONLY when `#print axioms` clean — anti-fraud, `Statement.lean:22` stays `sorry` until then).

## Open-sorry inventory (crux-2, `Crux2Blueprint.lean`, build-line numbers from `lake build`)
- `:79` `zKValidF` Ind-reduct validity · `:96` `ZDerivation_red_zK_crit` (critical SOUNDNESS = the inversion)
- `:197` splice validity · `:370` axNeg replace validity · `:456` (redZKReady-class) · `:541` `iord_descent_red`
  (5 internal sorries: 3 fixpoint + chain-REPLACE IH + splice `hr'`) · `:660` M2 embedding · `:673` M3.

## Aristotle
Idle — CORRECT. Everything open is entangled with the `InternalZ` HFS engine API (`red`/`iRK`/`iord`/`irk`/
`zKValidF`), not self-contained mathlib lemmas. The inversion port (NEXT 3) is the first candidate that could
be carved into an engine-agnostic sub-lemma, but only after its V-code statement is pinned.

## Kernel state (re-verified in-kernel lap 111)
- `peano_not_proves_goodstein` / `goodstein_implies_consistency` = `[propext, sorryAx, choice, Quot.sound]`.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` / `goodsteinSentence_faithful` =
  `[propext, choice, Quot.sound]` (clean).
