# HANDOFF — lap 126 · FRESH-MIND REVIEW: freshness transfer substrate LANDED; mechanism course-corrected

> **RESUME BATON.** Branch `plan`, HEAD = this lap's commit, tree CLEAN. Build 🟢 green
> (`lake build GoodsteinPA`, **1326**). Headline footprint unchanged: `peano_not_proves_goodstein =
> [propext, sorryAx, choice, Quot.sound]` (0 math axioms). The 4+2 new lemmas in `Zsubst.lean` are
> additive + axiom-clean `[propext, choice, Quot.sound]`; no `sorry` touched (11 disclosed sorries, as lap 125).

## What this lap did (review + a banked substrate)
The fresh-mind review validated the **direction** (close the LEFT-branch ∀-soundness gap = the I∀
eigenvariable condition, lap-125's pin) but **corrected the mechanism**, and landed the reusable core.

### ⭐ The course-correction (the review's main value)
Lap 125's NEXT plan was: *strengthen the `ZPhi` I∀ disjunct with `fvSubst a · p = p ∧ fvSubstSeq a ·
(seqAnt s) = seqAnt s`* — a 64-site atomic ripple (à la lap-115 `zAx1` / lap-118 `zAxNeg`). **That is the
wrong mechanism**, for two kernel-grounded reasons:
1. It contradicts the repo's **own lap-93 additive-O1 architecture** (`Zsubst.lean:947`, verbatim):
   "rather than baking freshness into `zIallWff` (which would shrink the `ZDerivation` fixpoint and force
   the embedding to re-prove it), `zReg` is a standalone `𝚺₁` function threaded *alongside* `ZDerivation`."
2. Baking freshness into `zIallWff`/`ZPhi` breaks the **proven, load-bearing `ZDerivation_zsubst`**: a code
   bound `p ≤ a` is not `zsubst`-stable (lap-92), and even the semantic form forces re-proving the fixpoint's
   `𝚫₁` definability + every `rcases zDerivation_iff` site.

**The principled fix = a standalone `zFresh` invariant** (exact parallel of `zReg`), storing the `𝚫₁`,
`zsubst`-stable *semantic* non-occurrence `fvSubst a (numeral 0) p = p` (⟺ `^&a ∉ FV p`). Semantic
non-occurrence IS `zsubst`-stable (substituting a *different* var by a *closed* numeral introduces no new
`^&a`), unlike the code bound — that is precisely why it can be threaded through `red`.

### ✅ Banked this lap — the freshness transfer substrate (`Zsubst.lean`, after `isSemiterm_succVar`)
The storable witness is at `numeral 0`, but `ZDerivation_iRcritG_critReductCorr` (Crux2Blueprint:470) needs
it at the *cut instance* `numeral k'` (= `π₁(π₂(tp(znth ds redexJ)))`, unknown at the I∀ node). Bridged by
the **double-substitution-collapses** identity (a numeral contains no `^&a`), all axiom-clean:
- `termFvSubst_numeral_idem`, `termFvSubstVec_numeral_idem`, `fvSubst_numeral_idem` — collapse
  `fvSubst a s (fvSubst a (numeral m) ·) = fvSubst a (numeral m) ·`.
- **`fvSubst_numeral_transfer`** : `IsUFormula p → fvSubst a (numeral m) p = p → fvSubst a (numeral k) p = p`.
- **`fvSubstSeq_numeral_idem` / `fvSubstSeq_numeral_transfer`** : the `seqAnt`-sequence analogues
  (hypothesis: entries `IsUFormula`).

These plug DIRECTLY into the consumer: `hpfresh = fvSubst_numeral_transfer hp h0`,
`hΓfresh = fvSubstSeq_numeral_transfer hΓ h0Γ`.

### ✅ ALSO LANDED — the standalone `zFresh` invariant (`Zsubst.lean`, after `zReg_zsubst`)
Full mirror of the `zReg` table machinery, axiom-clean `[propext, choice, Quot.sound]`, build 🟢 1326:
`eqFlag` (`𝚺₀`) → `freshFlag a p Γ = max (eqFlag (fvSubst a (numeral 0) p) p) (eqFlag (fvSubstSeq a
(numeral 0) Γ) Γ)` (`𝚺₁`, via `numeralGraph`/`fvSubstGraph`/`fvSubstSeqDef`) → `zFreshNext` (flag at tag-1
I∀ only; tags 2/3/4 fold premises) → `zFreshTable`/`zFresh` → recursion eqns (`zFresh_zIall`/`zIneg`/`zInd`/…)
→ **`ZFresh d := zFresh d = 0`** + per-node extraction **`fvSubst_numeral_eq_self_of_zfresh_zIall`** /
**`fvSubstSeq_numeral_eq_self_of_zfresh_zIall`** (the `maxEigen_lt_of_regular_zIall` analogues) +
**`zfresh_zIallPrem`**. **The freshness front is now DEFINED + per-node-extractable + transfer-ready
end-to-end** — `h0 = fvSubst_numeral_eq_self_of_zfresh_zIall (hfresh : ZFresh (zIall …))`.

## Next-lap targets (in order) — these REPLACE lap-125's ZPhi-ripple plan
1. **`zFresh_zsubst` stability** (mirror `zReg_zsubst`, Zsubst:1329 via `zDerivation_induction`, 8 cases):
   `zFresh (zsubst d a t) = zFresh d`. The I∀ case needs `freshFlag a' (fvSubst a t p) (fvSubstSeq a t Γ) =
   freshFlag a' p Γ` for `a' ≠ a` — needs a small commutation `fvSubst a' (numeral 0) (fvSubst a t p) =
   fvSubst a t (fvSubst a' (numeral 0) p)` (distinct fresh vars commute; pure `FvSubst` induction).
   → `red`-stability `ZFresh_red`.
2. **Thread `∧ ZFresh d` into `ZDerivesEmptyR`** (Crux2Blueprint:933); the embedding
   `foundation_bot_to_Z_empty` supplies it (eigenvariables chosen fresh). Then LEFT-branch ∀-soundness
   closes via `ZDerivation_iRcritG_critReductCorr` (feed `hpfresh = fvSubst_numeral_transfer (…)
   (fvSubst_numeral_eq_self_of_zfresh_zIall …)`) → assemble the LEFT successor + restructure
   `false_of_ZDerivesEmpty` off the `red`-orbit onto the lap-124 dichotomy successor + WF(`iord`).
3. ¬-case (`iRcritGNeg`, lap-117) needs the same freshness — the transfer + extraction substrate covers it.

## Open-sorry inventory (11 disclosed, unchanged) — by NAME
Headline `peano_not_proves_goodstein` (LOCKED) · `goodstein_implies_consistency` (girder) ·
`no_min_descent_absurd_of_goodstein` (DescentSemantic, off-path) · 8 Crux2Blueprint:
`zKValidF_iIndReduct_of_zInd` · `ZDerivation_red_zK_crit` · axNeg replace validity ·
`ZDerivation_red_zK_splice` · redZKReady-class · `iord_descent_red` chain-REPLACE IH ·
`foundation_bot_to_Z_empty` (M2) · `false_of_ZDerivesEmpty` (M3).

## Aristotle
Idle — CORRECT. The open pieces (`zFresh` invariant, endgame restructure) are engine-internal, not portable.

## Pointers
`PENDING_WORK.md` lap-126 (full mechanism rationale + `zFresh` design) · `STATUS.md` lap-126 box ·
`Zsubst.lean:947` (the lap-93 additive-O1 note that grounds the correction) · `HANDOFF-2026-06-26-lap125.md`.
