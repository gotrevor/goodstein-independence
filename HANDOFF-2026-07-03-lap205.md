# Handoff: lap 205 — FRESH-MIND REVIEW + E-seam piece 1 REALIZED (bounded rank-0 `Zef2TC` read-off, modulo one trap)

**Date**: 2026-07-03 · **Branch**: `plan` · **HEAD**: (this lap's commit) · **Tree**: CLEAN ·
**Build**: 🟢 bare `lake build` (1342 jobs); `wip/E1EmbeddingGrind.lean` compiles.
**Headline** unchanged/undrifted: `peano_not_proves_goodstein = [propext, Classical.choice,
goodstein_implies_consistency, Quot.sound]`, sorryAx OFF (re-verified real `#print axioms`).

**Directive (OUTRANKS this)**: `DIRECTION.md` CURRENT DIRECTIVE **lap-205 block** (set this lap) —
MANDATE = the two E-seam wip pieces: (1) the bounded rank-0 read-off, then (2) SpliceAssembly.

## ✅ This lap

### Review (fresh-mind)
Independently re-verified: headline undrifted, bare `lake build` 🟢 1342, and in wip
`embedding_Zef2TC_V3` / `budgetedEmbeddingV3` (V3 ladder 10/10) / `passAuxTC` / `rankToZero_TC` /
`sound0_TC` ALL kernel-clean `[propext, Classical.choice, Quot.sound]`. Direction SOUND — laps
202–204 CLOSED the lap-201 "V3 predicate" mandate AND ported the whole cut-elim pass to `Zef2TC`.
No un-escalated route trigger fired. Retargeted `DIRECTION.md` CURRENT DIRECTIVE (lap-205 block) +
refreshed `STATUS.md` to the two remaining E-seam wip pieces. (commit 1)

### E-seam piece 1 — `readoff_delta0_Zef2TC` (the flagged hard piece) (commit 2)
`wip/E1EmbeddingGrind.lean`, appended before the final `end`. From a rank-0 `Zef2TC` derivation of
the SINGLETON `{∃⁰ φ}`, extract `∃ n ≤ ewIter f α 0, atomTrue (φ/[nm n])` (the R-4′ bound).
Realizes **E–W Witnessing Lemma 31** exactly as the retirement note mandates (`ReadoffDAuxRetired.lean`):
- **`readoffTC_core`** — invariant induction, bound `f 0`. Invariant = `∃⁰ φ ∈ Γ ∧ every OTHER
  member standard-false`. Extract the top `∃⁰ φ` witness via `exI` at slot `f` (`n ≤ f 0`), verify
  the matrix instance SEMANTICALLY via `sound0_TC`, NO structural descent into `allω` branches.
  **8/9 rule-cases close**: axL/trueRel/trueNrel/verumR are vacuous under the invariant;
  `weak`/`wk`/`andI`/`orI`/`exI` recurse at slot `f` (`sound0_TC` kills empty contexts + the
  vacuous-`exI` re-witness); `cut` is rank-0.
- **`f0_le_ewIter`** — the root weakening `f 0 ≤ ewIter f α 0` (fully clean `[propext, choice, Quot.sound]`).
- **`readoff_delta0_Zef2TC`** — applies the core to the singleton (trivial invariant) + weakens.
- Footprint: `readoffTC_core`/`readoff_delta0_Zef2TC` = `[propext, sorryAx, Classical.choice, Quot.sound]`
  — the SINGLE disclosed `sorry` below.

## 🚧 THE ONE residual (disclosed `sorry`, `readoffTC_core` `allω` case)
`∀⁰ χ` standard-false (forced by the invariant) yet `χ/[nm 0]` TRUE — the non-monotone-matrix trap.
Then `rel1 f 0 = f`'s sharp branch-0 recursion is unavailable and the semantic false-branch index
overflows the budget (kernel-refuted in general; trap-derivations EXIST in `Zef2TC` — (Ax2) doesn't
help when `∃⁰ φ` is the only true context member). EXACTLY the fragment `readoffD_trapped_of_mono`
(`src/GoodsteinPA/OperatorZef2.lean`) closes under goodstein's downward-closed guard.

## 🚦 NEXT
1. **Close the residual** — pick the statement-shape route (judge-gated choice):
   (a) thread the downward-closed guard `atomTrue (χ/[nm 0]) → atomTrue (∀⁰ χ)` (goodstein's
   bounded-`∀` guards satisfy it), OR (b) show `goodsteinBodyE`'s matrix has no leading substituted
   bounded-`∀` so the vacuous-`exI`→`allω` path that buries `∃⁰ φ` never arises (inspect
   `goodsteinBody`; if it holds, the trap is vacuous for the pipeline). Detail in `PENDING_WORK.md`
   (lap-205 top).
2. **E-seam piece 2** — `wip/SpliceAssembly.lean`: compose `embedding_Zef2TC_V3` → `rankToZero_TC`
   → `readoff_delta0_Zef2TC` into `wainer_splice_Zef2`'s shape. Embedding outputs slot
   `rel1 (ewRootSlot e B) K` (EwF1 ⇒ inflationary ⇒ feeds `f0_le_ewIter`).
3. rung-E statement + `Zef2TC` amendment + read-off statement shape → next judge pass. **Do NOT
   self-ratify into `src`.**

## Gotchas fresh this lap
- `∀⁰ φ = Semiformula.all φ`, `∃⁰ φ = Semiformula.exs φ` (constructors, per `spineHead_all/_exs` rfl).
  So `by simp` (reduceCtorEq) proves cross-constructor `≠` (`(χ₁⋏χ₂) ≠ ∃⁰φ`, `∀⁰χ ≠ ∃⁰φ`); and
  `by simpa using (h : ∃⁰χ = ∃⁰φ)` gives `χ = φ`. `ne_of_ne_complexity` only works when complexities
  DIFFER (rel/nrel/⊤ vs ∃⁰φ) — NOT for ⋏/⋎ vs ∃⁰ (equal complexity).
- `simp [atomTrue]` unfolds `atomTrue (χ₁⋏χ₂)`→`atomTrue χ₁ ∧ atomTrue χ₂` (and ⋎→∨) directly — do
  NOT pass `Semiformula.eval_and`/`eval_or` (those constants don't exist here).
- `ewIter_zero f : ewIter f 0 = f` (simp). `rel1 f 0 0 = f 0` via `simp [rel1]` (`max 0 0 = 0`).
- `atomTrue_ex_iff`/`atomTrue_all_iff`/`atomTrue_nrel_iff_not_rel` are the semantic bridges (in
  `OperatorZef2`/`OperatorZinfty`, opened).
