# HANDOFF — 2026-06-23 (lap 24, **REVIEW + E-core grounded end-to-end**)

> **Branch** `plan` · HEAD `6f75db4` · build **green** (`lake build GoodsteinPA`, **1271 jobs**) ·
> headline `GoodsteinPA.peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact). Tree clean.

This was a fresh-mind review lap that also delivered real E-core progress. Durable overview =
**`STATUS.md`** (refreshed). Attack paths = **`PENDING_WORK.md`** (lap-24 top, read first). E map =
**`DESCENT-PLAN.md`** (now has §1 CORRECTION + §3a Σ₁-reframe). Bare **`HANDOFF.md`** = thin pointer.

## ✅ Lap-24 deliverables (6 green commits, `afc145c`…`6f75db4`)

1. **Review / STATUS refresh** (`afc145c`). Re-validated direction against the REAL kernel:
   `peano_not_proves_TI` carries exactly `[propext, choice, Quot.sound, rePred_ltPull_natCode]` — **D'
   was already discharged lap 22** (no `sorryAx`); F-φ is the SOLE remaining math axiom on the Thm 5.6
   route. `aris_emcong` was CANCELED (target already proved). Walls reduced **E + F-φ + D' → E-core + F-φ**.
2. **E-core kernel** (`b1f5260`, `src/GoodsteinPA/DescentCore.lean`, axiom-clean). `Dom.ineq6_step` =
   the non-vacuous Π₁ heart of Rathjen Lemma 3.6 (one special Goodstein step from `m ≥ T̂^{k+2}_ω(βₖ)`
   lands `≥ T̂^{k+3}_ω(β_{k+1})`) + `lemma36_ineq6`/`lemma36_nonterminating` (the `∀k` iteration —
   **semantic shadow only**, vacuous hyps since ε₀ is well-founded; documents the induction the
   arithmetization encodes). Weakened `Domination.canon_repr` `2≤b → 1≤b` (base-2 `T̂²_ω` = `evalNat 1`).
3. **E back-end CORRECTION** (`8435312`, docs). The DESCENT-PLAN's "`PRWO ⟹ TI prec` = one X-instance"
   understated Route B: Rathjen's `PRWO` is *primrec*; a counterexample to the free-X `TI prec` gives an
   X-*definable* descent, so primrec-`PRWO` can't refute it directly. Honest Route-B bridge = carry out
   §3 *inside paLX* with the free-X descent. **`Goodstein ⟹ PRWO(ε₀)` (Rathjen §3) is SHARED by both
   back-ends** → focus on §3, defer the Route A/B choice. Lit request filed (`ON-LINE-REQUEST` lap 24).
4. **§3 brick + arithmetization scoping** (`6f75db4`). `Dom.C : ONote → ℕ` (Rathjen max-coefficient) +
   `Canon_iff_C_le` (`Canon b o ↔ C o ≤ b`), axiom-clean. PENDING_WORK: GROUNDED the arithmetization —
   see below.

## 🎯 Open obligations (two walls)

1. **E-core — the deep wall** (Rathjen §3, `DescentCore.lean`/new files). Route-independent target:
   `𝗣𝗔 ⊢ goodsteinSentence → 𝗣𝗔 ⊢ PRWO(ε₀)`. **Arithmetization grounded (the dominant wall):**
   - Σ₁ glue is FREE: `sigma_one_completeness {σ} (hσ : Hierarchy 𝚺 1 σ) : ℕ ⊧ₘ σ → T ⊢ σ`
     (`Foundation/.../R0/Basic.lean:146`, `[𝗥₀ ⪯ T]`). All Δ₀/Σ₁ computations are free.
   - Inequality (6)'s `∀k` is Π₁ → needs a PA-induction. Foundation idiom = internalized `V ⊧ 𝗜𝚺₁` +
     `sigma1_pos_succ_induction` (`Arithmetic/Induction.lean`).
   - **KEY de-risk: arithmetize over base-b NUMERALS (Rathjen Lemma 2.2(ii) digit comparison = Δ₀),
     NOT internalized ONote.** The ordinal/`repr`/ε₀ detour is only the semantic-side proof convenience.
   - Prereq chain: (i) `goodsteinSeq` already arithmetized (`Encoding`); (ii) `βₖ`/`T̂^{k+2}` as
     `𝚺₁`/primrec numeral fns; (iii) arithmetized `ineq6_step` (Δ₀ numeral comparison); (iv) internal
     induction; (v) back-end (deferred). **(ii)–(iv) = the multi-lap wall.**
   - **Next concrete bricks:** the slow-down constructions Rathjen Lemma 3.3 / Cor 3.4 / Thm 3.5 as
     ℕ/ONote *step*-facts using the new `C` (Lemma 3.2 = mathlib `exists_lt_ack_of_nat_primrec`); OR
     start the numeral-based `ineq6_step` in `IΣ₁`.
2. **F-φ** `rePred_ltPull_natCode` (`SeamDefinability.lean`). **ON ARISTOTLE** (`aris_onotecmp`, UUID
   `16c9fc79-ae8b-4b04-8b83-2e8e9e5f38db`, RUNNING). On return: VERIFY in-kernel + `#print axioms`, port
   → makes Thm 5.6 entirely axiom-clean. Local fallback: `Primcodable.ofDenumerable`.

## ⚠️ Notes
- **At next lap start:** check for `ON-LINE-FINDINGS-*.md` answering the lap-24 back-end question; poll
  `aristotle list` for F-φ. `GREP Domination.lean` before any semantic ONote/Goodstein lemma.
- **LOCKED untouched:** `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, headline `sorry`.
- **src/ sorries (2):** `Statement.lean:22` (headline, locked), `Reduction.lean:52` (Route-A, off-path).
- Discharge the headline `sorry` ONLY when E is real AND `#print axioms peano_not_proves_goodstein` clean.
