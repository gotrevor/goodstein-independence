# HANDOFF — 2026-06-23 (lap 57) — crux-1 SOUNDNESS FIX: realizer threading (false→true lemma)

> **Branch** `plan` · continues lap 56 · `wip/GentzenCon.lean` `lake env lean` **green** (2 expected
> sorries: crux-2 `gentzen_prwo_implies_consistency`, crux-1 width-construction in `seqDescent_dominated`).
> Headline `peano_not_proves_goodstein` untouched (still honest `sorry`, no `src/` change this lap).

## Lap 57 — caught & fixed a FALSE wip lemma; reduced crux-1 to the width bookkeeping
**Bug found (fresh-mind audit):** lap-56's `seqDescent_dominated` was **FALSE at M=ℕ**. Its conclusion
`SeqDominated M` asserts `∃ β:M→M, … ∧ ∀n, icmp(β(n+1))(β n)=0` — an infinite ε₀-descent, impossible in
ℕ — while its hypotheses `_hdesc`/`_hstdom` are **vacuously satisfiable** by an empty/partial `seq`
(graph never realized ⟹ both hyps trivially hold, conclusion false). A `sorry` on a false statement is a
dead end (can never honestly discharge) and would have silently broken the crux-1 chain.

**Fix — `SeqRealized` threading.** Added
`SeqRealized seq M := ∃ β:M→M, (∀n, M⊧/![β n,n] seq) ∧ (∀n, isNF(β n)) ∧ (∀n, β n≠0) ∧ 𝚺₁-Function₁ β`
(the honest "seq is the graph of a total primrec NF-valued function" content). `seqDescent_dominated`
now takes `hreal : SeqRealized seq M`; from it the realizer's descent `icmp(β(n+1))(β n)=0` is **real**
(= `hdesc` applied to `β`'s graph-realizations), so the hypotheses are jointly **unsatisfiable in ℕ**
(vacuously true there, exactly like `StdCor34.crux1_internal_run_of_width_dom`) and substantive only in
nonstandard M. The β-parts of `SeqDominated` (NF, ≠0, descent, definability) are now **discharged
directly**; the lone remaining gap is the Cor-3.4 **width bookkeeping**. Threaded `hreal` up through
`nonterminating_of_seq_descent` / `prwoInstance_models_of_goodstein` / `goodstein_implies_prwo`; supplied
at `gentzenDescentφ` by the new disclosed axiom `gentzenDescentφ_realized` (ord/R primrec ⟹ total NF 𝚺₁
branch — joins `gentzenDescentφ`/`_dominated`, NOT on the headline `#print axioms` path).

## NEXT (hardest-first): the `BlkRec`-over-function refactor (SHARPENED — see ANALYSIS lap-57)
**Second lap-57 finding (committed as docs):** the remaining `seqDescent_dominated` gap is NOT "build a
finite width code `wseq`" — **no finite `wseq` works**. `nonterminating_of_slowdown` needs the slow-down
descent + `iC(β' k) ≤ k+1` for ALL `k:V`; a finite `wseq : V` returns `znth = 0` past `lh wseq`
(`Seq.znth_prop_not`) ⟹ `blk wseq j ∼ j` for large `j` ⟹ `hβC : iC(β(blk wseq j)) ≤ Cβ + j` fails for the
complexity-growing descents Cor 3.4 exists to absorb. **The width must be a `𝚺₁` FUNCTION
`W := fun t => iC(β(t+1))`** (mirroring the sorry-free ℕ-template `Grz.corW`/`C_le_wsum_corW`). Full
argument + fix in `ANALYSIS-2026-06-23-lap57-width-code-wall.md`. The girder is still vacuously TRUE (no
false kernel content), but it is a DEAD architecture — crux 1 cannot be completed through the finite-code
packaging. Refactor (additive, keeps `src/` green):
1. **DONE (`21d1856`)** — `src/GoodsteinPA/BlkRecF.lean`: `blkF`/`offF`/`wsumcF` over a width FUNCTION
   `W:V→V` (baked via `WDef`, mirrors `iwseq`), sorry-free + axiom-free, in the build (1317 jobs). All 4
   bookkeeping facts + `wsumcF_blkF_add_offF`/`wsumcF_blkF_le` (global positive widths) ported.
2. **DONE (`2f8f72e`)** — `src/GoodsteinPA/StdCor34F.lean`: `crux1_internal_run_F` drives the internal
   run through `BlkRecF.blkF` (width `W = fun t => iC(β(t+1))`). Sorry-free + axiom-free, in build (1318
   jobs). The C-bound `iC(β(blkF j)) ≤ iC(β 0)+j` and within-block domination are DISCHARGED internally
   (`iC_le_wsumcF` = internal `Grz.C_le_wsum_corW`, + `wsumcF_blkF_le`, + `offF_succ_lt_width`). **No
   `hβC` hypothesis, no `Cβ`/`wseq` to supply.** Inputs: `𝚺₁`-definable (explicit `βDef`) `≺`-descending
   NF-nonzero `β` with `iF l₀`-dominated width `iC(β(n+1)) ≤ iF l₀ n`.
3. **DONE (`2199982`) — CRUX 1 CLOSED.** `wip/GentzenCon.lean` `nonterminating_of_seq_descent` now calls
   `StdCor34F.crux1_internal_run_F` directly; the dead `SeqDominated`/`seqDescent_dominated` finite-`wseq`
   girder is gone. `SeqRealized` carries explicit `βDef`+`DefinedFunction₁`; `SeqStdBounded` is the width
   form. `crux1_internal_run_F` `#print axioms = [propext, choice, Quot.sound]`. The crux-1 sorry is GONE.

## ⭐⭐ STATE: WHOLE Gentzen chain SORRY-FREE; crux 1 axiom-clean; crux 2 = 4 disclosed axioms
`wip/GentzenCon.lean` is **sorry-free** (`lake env lean` green, zero sorry warnings). The full assembly
`goodstein_implies_consistency_via_gentzen : 𝗣𝗔 ⊢ γ → 𝗣𝗔 ⊢ Con(𝗣𝗔)` `#print axioms` =
`[propext, choice, Quot.sound, PA_delta1Definable]` + exactly **4 disclosed crux-2 axioms**:
- `gentzenDescentφ : Semisentence ℒₒᵣ 2` — the `ℒₒᵣ` graph of `n ↦ ord(Rⁿd₀)`.
- `gentzenDescentφ_realized` — it's a total `𝚺₁` NF-nonzero branch (with explicit `βDef`).
- `gentzenDescentφ_dominated` — Rathjen Lemma 3.2 width bound (`iC(β(n+1)) ≤ iF l₀ n`).
- `gentzen_reduction_internalized : 𝗣𝗔 ⊢ (prwoInstance gentzenDescentφ 🡒 Con(𝗣𝗔))` — Gentzen's
  `PRWO(ε₀) → Con(𝗣𝗔)` internalized as one `𝗣𝗔`-theorem. `gentzen_prwo_implies_consistency` is now a
  real modus-ponens proof on it.
Crux 1 (`goodstein_implies_prwo`) is fully proved, axiom-clean. The meta `ord`/`R`/`ord_R_descends`(eq-5)
axioms are KEPT as documentation of *why* `gentzen_reduction_internalized` holds, but are NOT depended on.

### NEXT (hardest-first) = crux 2, the Gentzen ordinal arithmetization (🟠 generational)
Discharge the 4 disclosed axioms by building the real objects (all interlocked):
1. **`ord`/`R` as `ℒₒᵣ` primrec functions** over Foundation's `Theory.Derivation` (coded `𝗣𝗔`
   derivations, `Bootstrapping/Syntax/Proof/Basic.lean`). Buchholz [6] (`papers/buchholz-on-gentzens-
   first-consistency-proof.pdf`) + `papers/siders-*.pdf`. Decompose eq-(5) `ord(R d) ≺ ord d` lemma-by-
   lemma.
2. **`gentzenDescentφ`** = arithmetize `n ↦ ord(Rⁿd₀)` as a graph; then `_realized`/`_dominated`
   discharge from `ord`/`R`'s fixed primrec build tree (Rathjen 3.2 gives the standard `l₀`).
3. **`gentzen_reduction_internalized`** = formalize Gentzen's reduction inside `𝗣𝗔`: `¬Con` ⟹ `⊥`-deriv
   `d₀` ⟹ the descent contradicts the PRWO instance. This is the deep `TI(ε₀) ⊢ Con(PA)` content.
This is multi-lap. Headline stays a `sorry` until all 4 are real (anti-fraud); GentzenCon stays wip.

## State of the crux-1 chain (`wip/GentzenCon.lean`)
`goodstein_implies_consistency = goodstein_implies_prwo gentzenDescentφ … ∘ gentzen_prwo_implies_consistency`.
- **`goodstein_implies_prwo`** (crux 1) — PROVED modulo the ONE sorry in `seqDescent_dominated` (now the
  width construction only). Honest: realizer + certificate are hypotheses, supplied for the Gentzen
  instance by disclosed axioms.
- **`seqDescent_dominated`** — TRUE conditional lemma (vacuous at ℕ); β-parts discharged; sorry = the
  Cor-3.4 width bookkeeping.
- **`gentzen_prwo_implies_consistency`** (crux 2) — bare sorry + `ord`/`R`/`ord_R_descends`/`derivesEmpty`/
  `gentzenDescentφ`/`gentzenDescentφ_dominated`/`gentzenDescentφ_realized` placeholder axioms. 🟠 generational.

## LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates`, `Statement.lean` headline.
`wip/GentzenCon.lean` is wip. Aristotle OFFLINE. WebFetch dead. Honest endpoint unchanged: best-case
headline `[propext, choice, Quot.sound, PA_delta1Definable]`.
⚠️ NEVER `git checkout <wip file>` to undo edits — `cp` from scratchpad.
