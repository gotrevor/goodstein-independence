# Handoff: lap 165 — KEYSTONE `closeNonRepProducer` PROVEN + WIRED; both residuals narrowed to R1/R2

**Date**: 2026-06-27 · **Branch**: plan · **HEAD**: `6732eb2` · build 🟢 1326 (`lake build GoodsteinPA`, exit 0).
M1b-term lap, 3 commits (`9a3d78e`,`5f03aee`,`6732eb2`). No src sorry dropped (still 46 in Crux2Blueprint),
but the keystone least-number RECURSION the lap-164 handoff named is now **in-kernel, sorry-free, and wired
into both copies** — the open `residual`/`axMajorResidual` core is reduced to exactly two general-cut escapes.

## Axiom ledger (real `#print axioms`, this lap — no drift)
- `peano_not_proves_goodstein` (headline) = `[propext, sorryAx, choice, Quot.sound]` — 0 math axioms.
- `false_of_ZDerivesEmpty` = `[propext, sorryAx, choice, Quot.sound]` — 0 math axioms.
- **`closeNonRepProducer` = `[propext, choice, Quot.sound]` — sorry-FREE** (the keystone recursion is real).
- `peano_not_proves_consistency` + `goodsteinSentence_faithful` = `[propext, choice, Quot.sound]` — clean.

## What this lap did
1. **`climb_to_rep_producer` exposes `m < jstar`** (`InternalZ.lean`, was `m ≤ j0`). The least ∀-producer
   `m₀ ≤ n₀ < jstar` (seed `n₀` threads from `jstar`), now in the signature — lets `n*`-leastness kill the
   climb's `{6,8}` landings in the recursion. Both `tryProducerClose` call sites recover `m ≤ j0`.
2. **`closeNonRepProducer`** — top-level lemma (`Crux2Blueprint.lean`, after `zDerivation_zAxBot`,
   `set_option maxHeartbeats 1600000`). Parameterized by the no-redex chain data + per-case closers
   (`hrepClose`/`hleafClose`/`haxNegClose`/`hcollapse`) + a residual cert. ANY L-axiom producer
   (tag∈{5,6,8})≤j0 → `GenReductCert` via a GLOBAL `least_number` over `{5,6,8}`-producers: the least `n*`'s
   active formula threads (`collapse`/∀-`climb`) to `m' < n*`, which `n*`-leastness forces into `{3,4}`
   (→`hrepClose`) or a right-symbol R-intro forming an `isRedexPair` with `n*` (→`hnolow`, ⊥). Sorry-free.
3. **Wired into BOTH** `genReduct_chain_noRedex` (⊥, `axMajorResidual`) and `_anySucc` (`residual`) via a
   local `cnrp` wrapper: `closeZAxNeg` → thin wrapper, `tryProducerClose` tag-5/6/8 → `cnrp`, main tag-6
   jstar block → one `cnrp jstar`. Net **−134 lines**.

## 🎯 NEXT — the genuine remaining open core (sharply pinned)
`axMajorResidual` / `residual` are now reached ONLY from:
- **(R1)** tag-6 `inegF q ∈ Γ`, `q` produced by an R-intro (no redex — `n*` left-consumes `inegF q`, not `q`).
- **(R2)** tag-5 ∀-climb landing `^∀G ∈ Γ`.
- **(anySucc only)** main tag-5 jstar escape `^∀p' ∈ Γ` (R2-twin) + **Family-C** C-exit R-intro replay.
- The ⊥-version `axMajorResidual` is **PURELY R1/R2** (no C-exit).

**R1, R2, and the tag-5 escape ALL need a formula in `Γ`, hence are VACUOUS for the headline `∅→⊥`.** The
obstruction to a DROP: `genReduct_chain_noRedex` is Γ-GENERAL; the recursion descends into Rep producers whose
`Γ_sub` is non-empty (formulas thread to earlier outer succedents, not to ∅), so R1/R2 are reachable there.

**NEXT (ranked, full detail in `PENDING_WORK.md` lap-165):**
- **(a) GLOBAL-THREADING REFRAME — the drop path.** Strengthen the `GenReductCert` recursion motive to carry
  "every Γ_sub formula traces (via the ROOT `hthread0`) to outer Γ=∅ or an earlier outer succedent." Then
  R1/R2 trace to root-∅ → vacuous → `hresidual` never hit → `axMajorResidual` DROPS. The wall:
  `genReduct_botSucc`'s `zDerivation_sigma_induction` code-IH does not carry the outer threading. 1-2 laps;
  cleanest path to the first real drop since the narrowing began.
- **(b)** handle R1/R2 as genuine Γ-general cut-reductions (R1 = cut on `q`, R2 = ∀-cut on `^∀G`); deeper.
- **(c)** Family-C C-exit (anySucc): internal weakening to re-base the tag-1/2 R-intro to `Γ→C`; defer.

## ⚠️ FORBIDDEN / ALTITUDE (per DIRECTION.md lap-161/164, unchanged)
`red` witnesses; `iord`-recursion for the construction; `redLeast`/μ-min for gDef; the refuted `seqUpdate`
splice; axMajor/gDef STANDALONE before the port; **M2/M3/M4 wiring** until `false_of_ZDerivesEmpty` is
sorry-free. **ALTITUDE:** a sorry-free `false_of_ZDerivesEmpty` is NOT the headline — `goodstein_implies_
consistency` (`Reduction.lean:68`) is a bare sorry, M2/M4 ~0%; the closing lap HANDS to altitude to re-plan M2.

---
**→ Next session: the GLOBAL-THREADING REFRAME (a) — strengthen the `GenReductCert` recursion motive to carry
the root-Γ threading invariant, making R1/R2 vacuous → DROP `axMajorResidual`. Keep build green, axioms clean.**
