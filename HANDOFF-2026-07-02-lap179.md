# HANDOFF — 2026-07-02, lap 179 (P1 bridge DISCHARGED + full E–W Lemma 19 comparison banked)

> **WIND-DOWN CHECKPOINT (lap budget reached):** branch `plan`, HEAD `655d3a8`, tree CLEAN (all
> committed). Full build green (`lake build`, **1333 jobs**); headline `peano_not_proves_goodstein`
> = `[propext, choice, goodstein_implies_consistency, Quot.sound]` (sorryAx OFF, no drift);
> `OperatorZeh.lean` = the same 3 §5 judge-gated pins, no new axioms.
>
> **Lap-179 net:** discharged the P1 bridge sorry `hardy_omega_pow_coeff_comp` (it was ~90% already
> in-repo — `Hardy.lean:1536 hardy_oadd_coeff_step` verbatim modulo `β≠0`; only the β=0 finite arm
> was missing), then built out the ENTIRE E–W Lemma 19 comparison of the Hardy and fast-growing
> hierarchies (previously absent), all in `src/GoodsteinPA/Hardy.lean`, all `[propext,choice,Quot.sound]`:
>   • `hardy_omega_pow_add_one_le` / `_lt_fastGrowing` — upper `H_{ω^α}(n)+1 ≤ f_α(n+1)` (∀ α)
>   • `fastGrowing_le_hardy_omega_pow` + `hardy_omega_pow_bracket` — the two-sided `ω^α` sandwich
>   • `hardy_omega_pow_coeff_comp/_coeff_le`, `fastGrowing_iterate_le_hardy_coeff`,
>     `hardy_coeff_add_one_le`, `hardy_omega_pow_coeff_bracket` — the coefficient-general family
>   • `fastGrowingε₀_le_hardy_tower_succ`, `hardy_tower_succ_lt_fastGrowing` — the ε₀-diagonal capstone
>
> **NEXT (judge-gated):** the crux `wainer_bound_of_pa_proves_goodstein` via the `Zeh` calculus is
> blocked on judge ratification of `REBUILD-Z-LAP1-VERDICT.md`; then ORDER lap 2 (A3 inversion suite
> already proven → reduction discharge next). The permitted calculus-independent growth lane is now
> comprehensively mined (see `PENDING_WORK.md` crux map). Route-A machinery stays FORBIDDEN.

> **CHECKPOINT (original):** branch `plan`, tree committed. Full build green (`lake build`, **1333 jobs**).
> This lap CLOSED the sole remaining sorry of the P1 fast-growing-domination bridge and relocated
> the whole (now sorry-free) bridge from `wip/` into `src/GoodsteinPA/Hardy.lean`. No new axioms;
> headline audit surfaces unchanged (no drift). `OperatorZeh.lean` = the same 3 §5 judge-gated pins.

> **BOUNDED-SUBSET RUN, wait-for-judge state (unchanged framing since lap 176).** Target =
> `REBUILD-Z-ORDER` **Scope-A ONLY**; reduction discharge + calculus wiring stay judge-gated. This
> lap did the mission's hardest-first move (advance the P1 crux with the smallest kernel-grounded
> step) rather than idle, and it PAID OFF: the E–W Lemma 19 upper bound is now a real, banked,
> kernel-clean theorem. **Resume = judge ratification of `REBUILD-Z-LAP1-VERDICT.md` + the P1
> findings.** Do NOT grind the 3 gated §5 pins; do NOT wire the bridge into pin-3 (that's reduction
> discharge, forbidden in Scope-A); do NOT touch Route-A sorries.

## What this lap did (a real crux advance, kernel-verified)

Attacked the lap-178 hand-off's named next target — `hardy_omega_pow_coeff_comp`, "the SOLE
remaining open obligation" of the P1 bridge (the equal-exponent additive composition
`H_{ω^β·(k+2)}(n) = H_{ω^β·(k+1)}(H_{ω^β}(n))`).

**Finding: it was already 90% proven in the repo.** `Hardy.lean:1536` `hardy_oadd_coeff_step` is
`hardy_omega_pow_coeff_comp` *verbatim* modulo a `β ≠ 0` hypothesis (proven via the banked
non-absorbing additive law `hardy_oadd_tail`/`hardy_add_comp`). The lap-178 baton planned a fresh
non-absorbing LIMIT fs-homomorphism proof, not realizing the successor/limit content was banked. The
only genuine gap was the `β = 0` finite case:
`H_{ofNat(k+2)}(n) = H_{ofNat(k+1)}(H_{ofNat 1}(n))`, discharged by `ofNat_succ` (to rewrite
`oadd 0 m.succPNat 0 = ofNat (m+1)`) + `hardy_ofNat` (`H_{ofNat c}(x)=x+c`) + `omega`.

With that, the whole bridge is sorry-free. **Relocated `wip/HardyFastGrowingBridge.lean` → a new
section of `src/GoodsteinPA/Hardy.lean`** ("B4 at an arbitrary transfinite exponent", right before
`end`, generalizing the finite/successor `hardy_omega_pow_ofNat`/`_omega` already there). Deleted the
wip file (fully subsumed). `wip/HardyAddProbe.lean` (the absorption-wall REFUTATION record) is left
in place — distinct evidence content.

### Banked in `src/` this lap (all `#print axioms` = `[propext, Classical.choice, Quot.sound]`)

- `hardy_omega_pow_coeff_comp` — equal-exponent additive composition, **unconditional in `β`**.
- `hardy_omega_pow_coeff_le` — Cichoń–Wainer coefficient intermediate
  `H_{ω^β·(m+1)}(n)+1 ≤ f_β^[m+1](n+1)` (induction on the coefficient `m`).
- `hardy_omega_pow_add_one_le` — **the E–W Lemma 19 upper bound
  `H_{ω^α}(n)+1 ≤ f_α(n+1)`, UNCONDITIONAL over every `α : ONote`** (WF recursion on `α`:
  base = equality, successor exponent = coeff intermediate at the IH, limit = IH + `fastGrowing`
  index-monotonicity across the fundamental sequence).
- `hardy_omega_pow_lt_fastGrowing` — the strict corollary `H_{ω^α}(n) < f_α(n+1)` P1 consumes.
- `native_decide` anchors (blessed base, cf. Domination.lean policy): the `+1` shift at α∈{0,1,2},
  the falsity of the bare equality, and a genuine-limit anti-vacuity `H_{ω^ω}(1) < f_ω(2)`.

## Lap-179 continued (governor push) — the E–W Lemma 19 bracket is now TWO-SIDED

Under continued governor push, closed the matching LOWER half of the bridge (calculus-independent,
pin gate untouched): `fastGrowing_le_hardy_omega_pow` — `f_α(n) ≤ H_{ω^α}(n)`, unconditional over
every `α : ONote` (WF recursion: α=0 trivial; limit = IH verbatim since both sides pick index `α[n]`
at argument `n`; successor = iterate domination `(f_β)^[n](n) ≤ (H_{ω^β})^[n](n) ≤ (H_{ω^β})^[n+1](n)`
via `hardy_oadd_coeff` + a new `iterate_le_iterate_of_le` helper + `hardy_monotone`/`le_hardy`).
Combined with the upper bound this yields **`hardy_omega_pow_bracket`**: the two-sided sandwich
`f_α(n) ≤ H_{ω^α}(n) < f_α(n+1)` — H_{ω^α} sits within one `f_α`-step of `f_α`, at every `α`. Banked
in `src/Hardy.lean`, `[propext, choice, Quot.sound]`, full build green (1333). This is the complete
E–W Lemma 19 comparison of the two hierarchies at `ω^α`, a clean reusable growth-theory result.

## Lap-179 continued — coefficient-general bracket (family complete)

Extended the `ω^α` bracket to general coefficient (α≠0), banked in `src/Hardy.lean`
(`[propext,choice,Quot.sound]`, build 1333): `hardy_omega_pow_coeff_bracket` —
`(f_α)^[k+1](n) ≤ H_{ω^α·(k+1)}(n) < (f_α)^[k+1](n+1)` — via the lower bound
`fastGrowing_iterate_le_hardy_coeff` + the new upper `hardy_coeff_add_one_le` (which carries the `+1`
shift through `k+1` iterates with the `≤`-generalized `iterate_offset_le`). The Hardy/fast-growing
comparison at `ω^α·(k+1)` is now two-sided too. This closes out the permitted calculus-independent
growth lane: the E–W Lemma 19 comparison is complete at every `ω^α` and every coefficient.

## Lap-179 continued — the ε₀-diagonal capstone

Applied the `ω^α` bracket at `α = tower i` (using `tower(i+1)=ω^{tower i}` and
`fastGrowingε₀ i = f_{tower i}(i)`) to pin the ε₀ fast-growing diagonal against the Hardy tower —
the ε₀-tier reading of E–W Lemma 19, the level where `goodsteinLength` itself lives. Banked in
`src/Hardy.lean` (`[propext,choice,Quot.sound]`, build 1333): `fastGrowingε₀_le_hardy_tower_succ`
(`fastGrowingε₀ i ≤ H_{tower(i+1)}(i)`) and `hardy_tower_succ_lt_fastGrowing`
(`H_{tower(i+1)}(i) < f_{tower i}(i+1)`). The E–W comparison now reaches from `ω^α` through general
coefficients up to the ε₀ diagonal.

## What this means for P1 (the crux the headline rests on)

The raised-control obligation P1 (`NormControlled f' (raise e α') m`, `raise e α' = e + ω^{α'}`)
reduces, via `hardy_omega_pow_lt_fastGrowing`, to the **iterate-domination half**
`f_{α'}(n+1) ≤ f^{[iterate]}(n+1)`: the raised control `≈ hardy(ω^{α'})` (absorbing regime, the
lap-178 finding) is now KERNEL-dominated by `f_{α'}` at the shifted argument. The Lemma-19 *upper*
half is DONE; pin-3's essential iterate index (lap 178) is exactly the residual iterate-domination
half, which is calculus-side reduction discharge — **judge-gated**, forbidden in Scope-A.

## Kernel audit (this lap)

- Full repo `lake build` green, **1333 jobs**.
- `peano_not_proves_goodstein` = `[propext, Classical.choice, goodstein_implies_consistency,
  Quot.sound]` — sorryAx OFF, **no drift** from lap 178.
- The four new Hardy lemmas: `[propext, Classical.choice, Quot.sound]` (native_decide anchors are
  separate `example`s and do not enter the theorem footprints).
- `OperatorZeh.lean`: the same 3 §5 f-slot pins, no new axioms, no drift.

## Resume point

- **If the judge has NOT ruled:** wait-for-judge, cap-bounded. Confirm state (build green 1333,
  axioms clean, 3 pins, no drift), do NOT grind gated pins or wire the bridge into the calculus.
  Scope-A remains exhausted; the P1 upper-bound spadework is now banked, so the next *permitted*
  calculus-independent Hardy growth-theory targets are thin — inventory `wip/HardyAddProbe.lean` and
  the finite-B4 neighbours if a further permitted brick is wanted, but the substantive residual
  (iterate domination) is judge-gated.
- **If the judge ratifies Option A + pin `f'`:** lap 2 discharges the FIXED-control reduction
  conjunct via banked `NormControlled.comp`, then the genuine calculus work in `cutElimPass_Zf` now
  has its upper-bound brick ready: `hardy_omega_pow_lt_fastGrowing` supplies `hardy(ω^{α'}) < f_{α'}`,
  leaving the iterate-domination half `f_{α'}(n+1) ≤ f^{[iterate]}` (E–W Lemma 19 lower half) to wire
  into pin-3.
