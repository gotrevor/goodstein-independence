# HANDOFF — lap 70 · Option B REFUTED in-kernel; genuine validity-preserving reduct (Option A) is forced

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md`, then `STATUS.md`.
> Branch: `plan`. Build: 🟢 green, `lake build GoodsteinPA` = 1321 jobs. Tree clean. HEAD `e1a6147`.
> (7 green commits this lap; HEAD started at `f17d8d1`, see full list at bottom.)

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free**.
Route: `PA⊢γ →(crux-1 DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)` → Gödel II. Sole math wall = crux-2,
now sharply localized to **`RedSound`** (`InternalZ.lean:4703`): the `iR2`-reduct of a contradiction
derivation must itself be a genuine `ZDerivation`.

## ⛔ THE DECISIVE LAP-70 FINDING — Option B is DEAD (do NOT re-attempt it)
The lap-69 analysis recommended **Option B** (`ZPreDerivation`: weaken the iterate class so the
**ordinal-faithful** `iR2` preserves it, sidestepping the eigenvariable nut). **This is refuted, and the
refutation is now a kernel-checked theorem** (`not_zKValid_iCritReduct`, axiom-clean):

- The critical reduct `iCritReduct d i j v w = zK (fstIdx d) (zKrank d−1) ⟨iCritAux d i v, iCritAux d j w⟩`.
- Its premises are `iCritAux _ = zK …` — themselves **chain nodes**. Every chain node has `tp = isymRep`
  (`tp_zK`, line 703), and `isymRep` is **permissible for every conclusion** (`iperm_isymRep`).
- But the descent's K-case (`iord_descent_iR2_zK_of_valid`) consumes `zKValid`, whose **criticality**
  conjunct demands `∀ i, ¬iperm (tp dᵢ) s` — the very hypothesis the L3.1 redex finder
  (`inference_critical_pair_of_chain`) needs to force a genuine R/L redex pair. A chain with a `Rep`
  premise can **never** be `zKValid`. ⟹ `iR2` cannot preserve descent-readiness; `RedSound` is **false**
  for the current ordinal-faithful `iR2`. No weakening of `zKValid` that still drives L3.1 escapes this:
  the descent needs a redex, a redex needs a non-permissible premise, the reduct has none.
- **Cross-checked against Bryce–Goré** (arXiv:2603.00487, clone at `~/src/Gentzen`, findings in
  `archive/findings/ON-LINE-FINDINGS-2026-06-24-bryce-gore-gentzen.md`): their `cut_elimination`
  (`Logic/cut_elim.v:231`) is **genuinely validity-preserving** (`cut_elimination_valid:387`),
  **shape-dispatched on the cut formula** (`_atom`/`_neg`/`_lor`). The ordinal-faithful `iCritReduct`
  is an ordinal-tracking *shadow*, NOT that object.

## ✅ WHAT LANDED THIS LAP (1 green commit `f17d8d1`, all axiom-clean `[propext,choice,Quot.sound]`)
Rule-inversion (peeling) primitives — the foundational building blocks a genuine validity-preserving
reduct consumes to peel the redex premises — plus the obstruction formalized:
- `zDerivation_zIall_inv` / `zDerivation_zIneg_inv` (R-redex peel: premise `d0` is a `ZDerivation` +
  succedent = principal formula), `zDerivation_zAxAll_inv` / `zDerivation_zAxNeg_inv` (L-redex peel:
  `IsUFormula p` + the `∀p`/`¬p ∈ Γ` side condition), `zDerivation_zAtom_inv`.
- `not_zKValid_of_zK_premise` (a `K^r` chain with any `Rep`-tagged premise is never `zKValid`) +
  `not_zKValid_iCritReduct` (the concrete obstruction; guards against re-chasing Option B).
- helpers `iCritReductSeq_lh`, `znth_iCritReductSeq_zero`.

## ✅ ALSO LANDED THIS LAP (commits 4–7, all axiom-clean, green 1321)
- `ZDerivation_iR2_zIall`/`_zIneg` — the clean I-rule `RedSound` fragment (`iR2 = d0`, valid by
  inversion; I¬ is Buchholz 14.23 `d[0]:=d0` verbatim).
- `isChainInf_of_last` — chain-validity reduced to premise-local threading (last premise = `j0`); the
  reusable tool the genuine Ind/critical reducts use to establish `isChainInf`.
- `zIallWff`/`zInegWff` — the Buchholz I∀/I¬ **premise-sequent** conditions (`d0 ⊢ Γ→F(a)` /  `A,Γ→⊥`),
  the additive **start of rung 0.5** (rule-faithful `ZPhi`). Compile via `substs1`/`qqFvar`.
- **Findings pinned in `PENDING_WORK.md` (lap-70 section, READ IT):** the genuine Buchholz reductions
  (Def 3.2 / 14.23–14.25), the foundational `zsubst` brick, the Foundation substitution API map
  (`subst`/`substs1`/`shift`/`free`; free vars `^&i = qqFvar`), and the **rung-0.5 design decision**
  (decode `at' = ⟪a,t⟫`; `at'` is currently OPAQUE/unused — Ind semantics entirely unencoded).

## ▶ NEXT — finish rung 0.5, then Option A's genuine reduct (the deep crux-2 core)
**Immediate (rung 0.5, mechanical now that it's scoped):**
1. `zIndWff` (Ind premise sequents) — needs the `Sa = a+1` ℒₒᵣ term-code (no successor symbol in ℒₒᵣ;
   build `^&a ⊹ 1`-code via the addition function-symbol code) + the `at' = ⟪a,t⟫` decode.
2. Δ₁-definability instances for `zIallWff`/`zInegWff`/`zIndWff` (mirror `zKValidDef`).
3. Wire them as conjuncts into the `ZPhi` I∀/I¬/Ind disjuncts — the atomic Σ₁/Δ₁ cascade (laps 66/69
   pattern: `ZPhi` def, `zphi_monotone`/`_strong_finite`/`_iff`, `zblueprint` σ&π cores, `zPhi_definable`,
   + every `rcases zDerivation_iff` site gains the extra conjunct). Then `zDerivation_zInd_inv` upgrades
   to also hand the premise-sequent data.


This is real cut-elimination for the V-internal system Z, internalized in IΣ₁ — a major multi-lap
undertaking (Bryce–Goré spent ~420 lines on `cut_elim_aux0` alone, in a *meta*-level Coq dev; the
IΣ₁-internalization is harder still). Realistic lap cadence = one genuine rung per lap. Blueprint =
`~/src/Gentzen/theories/Logic/cut_elim.v` (port the SHAPE, not the infinitary internals — see findings §2).

**The genuine reduct is shape-dispatched on the cut formula** (the redex pair (i,j) found by L3.1: premise
`i` has `tp = isymR A` — an I-rule R-intro of cut formula `A`; premise `j` has `tp = isymLk k A` — a §5
L-axiom). For each principal connective of `A`, the reduct connects the peeled sub-derivations:
1. **Define the genuine `iCritReduct'`** (replacing the ordinal shadow): on a critical chain, peel the
   R-redex (via `zDerivation_zIall_inv`/`_zIneg_inv`) and the L-redex (via `zDerivation_zAxAll_inv`/
   `_zAxNeg_inv`), then splice — producing a chain whose premises are GENUINE sub-derivations (non-`Rep`
   tps), so `zKValid` of the result is reachable. Start with the SIMPLEST cut shape.
2. **Prove `zKValid` of the genuine reduct** (the validity half — Bryce–Goré `cut_elimination_valid`).
3. **Re-fit the ordinal descent** (`iord_descent_iCritReduct_object` is built on the shadow; the õ/dg
   facts `iotil_iCritAux_lt`/`idg_iCritAux_le` should largely survive — they are stated on `seqUpdate`
   read-outs that the genuine reduct also produces). Then `RedSound` becomes provable and
   `iord_iR2_iterate_descends` (already assembled, modulo `RedSound`) closes the descent.

**The Ind case (tag 3) may be the most tractable first rung:** unlike the critical reduct, the Ind reduct
`iRInd → iIndReductSeq d0 d1 1 = ⟨d1, d0⟩` has premises that are the ORIGINAL sub-derivations `d0,d1`
(genuine tps, not `Rep`). What is still wrong is the **count** (genuine is `⟦t⟧`, not 1) and threading
(`isChainInf`). Evaluate whether a genuine Ind→K unfolding with the right count is provable-`zKValid`
before tackling the harder critical-cut shapes.

## Parallel front (when the reduct blocks)
- `PA_delta1Definable` is STILL an upstream `axiom` (findings §4) — mandatory for axiom-free. Independent.
- C0.5 bridge `¬Con(M) → ∃ z, ZDerivesEmpty z` (three-layer shape from Bryce–Goré `Peano.v`, findings §1).

ANTI-FRAUD: discharge `Reduction.lean:68` / `Statement.lean:22` only when `#print axioms
peano_not_proves_goodstein` is clean. `RedSound` is the lone InternalZ obligation; do NOT axiomatize it
or fake the reduct.

## Lap discipline
InternalZ is `src/` (green-gated, sorry-scanned). Commit each green build; end the turn after each so
the governor can pace.

## Commit log (lap 70, branch `plan`)
- `f17d8d1` rule-inversion peeling primitives + Option-B obstruction formalized
- `bbd1b99` HANDOFF — Option B refuted in-kernel, Option A forced
- `c156a9b` clean I-rule RedSound fragment + Option-A executable ladder
- `0d46982` isChainInf_of_last + refined ladder (ZPhi rule-faithfulness prereq)
- `44aace1` pin rung-0.5 design (decode at'=<a,t>) + exact Buchholz Ind conditions
- `e1a6147` zIallWff/zInegWff — I-rule premise-sequent conditions (rung-0.5 start)
