# Handoff: lap 166 — NAMED-AXIOM BLUEPRINT adopted; `sorryAx` is OFF the headline

**Date**: 2026-06-28 · **Branch**: plan · **HEAD**: `6b99382` · build 🟢 1326 (`lake build GoodsteinPA`, exit 0).
**OPERATOR-DIRECTED LEDGER CONVERSION** (not a math-direction change). 6 commits. ⚠️ **This lap is a
VISIBILITY change, NOT mathematical progress** — see ALTITUDE below.

## What this lap did (sorries → NAMED AXIOMS, per KB `named-axiom-blueprint`)
Trevor's stance shifted (KB note, 2026-06-27): a faithful, declared, allowlisted milestone `axiom` is the
*clearer, more honest* blueprint node than one opaque `sorryAx`. Adopted that discipline here:

1. **Headline wired (`deff4c5`).** `goodstein_implies_consistency` (the Phase 2–3 girder γ→Con(PA) inside
   PA) promoted `theorem … := sorry` → **named `axiom`** (`Reduction.lean:75`). `Statement.lean` now
   `import`s `Reduction` and the headline `peano_not_proves_goodstein` is a **REAL proof**:
   `:= not_proves_of_implies_consistency goodstein_implies_consistency`.
2. **Crux-2 milestones (`90a8faf`).** `exists_sigma1_descending_step` (the (A) Σ₁ step, bare-sorry leaf)
   → `axiom` (no `_builder` — a `_builder := sorry` would only re-add a parked sorry). `false_of_ZDerivesEmpty`
   (M3 Gentzen-False interface) → `axiom` + drop-in **`false_of_ZDerivesEmpty_builder`** at the IDENTICAL
   type carrying the real composition.
3. **Trust-base docs (`23bf62b`).** Rewrote DIRECTION.md "ANTI-FRAUD guard" (exact-match → allowlist
   policy), Statement.lean docstring, STATUS.md ledger. The old "a custom axiom on the headline = smuggling"
   rule is SUPERSEDED: a *faithful/declared/allowlisted/intended-to-discharge* axiom is the honest node; the
   fraud guard now bites on *undeclared* / off-allowlist / false axioms, `native_decide`, vacuous restatement.
4. **CI gate (`6b99382`).** New `.github/workflows/ci.yml` — allowlist axiom-clean gate (wrap-tolerant):
   headline NEVER `sorryAx`, axiom set ⊆ {3 canonical + `goodstein_implies_consistency`}. Logic validated
   locally (PASS + negative tests). NOT runnable from the box (no GitHub egress).

## Axiom ledger (real `#print axioms`, verified this lap — no drift)
- **`peano_not_proves_goodstein` (headline) = `[propext, Classical.choice, Quot.sound,
  goodstein_implies_consistency]` — `sorryAx` OFF the headline.** ✅ (was `[…, sorryAx, …]` through lap 165.)
- `exists_sigma1_descending_step` / `false_of_ZDerivesEmpty` = `[3 canonical + self]` (clean milestone interfaces).
- `false_of_ZDerivesEmpty_builder` = `[propext, sorryAx, choice, Quot.sound, exists_sigma1_descending_step]`
  — the in-motion crux-2 engine debt, parked off the milestone ledger.
- The **only 3** `axiom` decls in `src/`: `goodstein_implies_consistency`, `exists_sigma1_descending_step`,
  `false_of_ZDerivesEmpty`. Gate (host): `lean-axiom-gate . -t GoodsteinPA.peano_not_proves_goodstein
  -a goodstein_implies_consistency` ⇒ ✓.

## ⚠️ ALTITUDE (UNCHANGED, binding) — do not mistake this for progress
The KB is explicit: *"the honesty comes from visibility, not structure; a blueprint axiom left undischarged
forever is a cop-out, just labeled."* The headline is clean ONLY modulo the named girder axiom, whose
**construction is still ~0–partial**: the M2/M4 embedding (PA-proof → `ZDerivesEmptyR`) is ~0% built, and
the crux-2 engine `false_of_ZDerivesEmpty_builder` is still sorry-full (the in-motion descent engine).
"Only the crux left" ≠ "almost done." DISCHARGE MAP (allowlist → 3 canonical): (a) close the crux-2 engine
(`ZDerivesEmptyR_descent_step` chain) → drop in `false_of_ZDerivesEmpty_builder`; (b) build (A)
`exists_sigma1_descending_step`; (c) build M2/M4 to discharge `goodstein_implies_consistency`. Each `axiom`
→ `theorem` removes it from the allowlist; the day all three land, flip CI to `--exact`.

## 🎯 NEXT — the math direction is UNCHANGED from lap-165 (the ledger conversion did not touch it)
Close the remaining `residual` (Crux2Blueprint, the R1/R2 general-cut escapes) via the **GLOBAL-THREADING
REFRAME** (carry "Γ_sub traces to root-Γ=∅" through `genReduct_botSucc`'s code-induction → R1/R2 vacuous →
DROP `axMajorResidual` — the first real src-sorry DROP since narrowing began). Full detail:
`PENDING_WORK.md` lap-165, `HANDOFF-2026-06-27-lap165.md`. When the crux-2 engine closes, drop in
`false_of_ZDerivesEmpty_builder` (axiom → theorem) and the milestone ledger self-discharges.

## ⚠️ FORBIDDEN / guardrails (unchanged from lap-161/164/165)
`red` witnesses in live proofs; `iord`-recursion for the construction; `redLeast`/μ-min for gDef; the
refuted `seqUpdate` splice. **NEVER axiomatize the dead `red` cluster** (`ZDerivation_red_zK_crit`,
`_splice`, `_nonRep`, `redSoundGen`, `iord_descent_red*`, `zKValidF_iIndReduct_of_zInd`) — they are
documented-FALSE; a false `axiom` proves `False`. They STAY as clearly-labeled refuted sorries (verified
still `theorem`s this lap). Never axiomatize a statement you don't believe true + faithfully-stated.

---
**→ Next session: resume the math (close `residual` → DROP `axMajorResidual`). The ledger is now
named-axiom clean; keep it that way (CI allowlist gate enforces it). Push from the host — the box has no
GitHub egress, so this branch + handoff is the baton.**
