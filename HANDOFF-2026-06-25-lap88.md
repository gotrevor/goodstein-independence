# HANDOFF — lap 88 · crux-2: 5.2.1 genuine-object stack COMPLETE (rank-faithful) + 5.2 dispatch index

> Supersedes prior HANDOFF-*.md. Read order: this → `STATUS.md` → `PENDING_WORK.md` (lap-88 box) →
> `ANALYSIS-2026-06-25-lap86-criticality-resolved.md` (the dispatch spec). Branch `plan`. HEAD `6cce9a6`.
> Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Tree clean.

## ✅ 6 GREEN COMMITS THIS LAP (axiom base untouched; headline still 0 math axioms)
All new lemmas axiom-clean `[propext, Classical.choice, Quot.sound]`. All in `InternalZ.lean`.

1. `5aa47b7` — **5.2.1 descent on the GENUINE object.** `iord_descent_seqInsert`:
   `o(K^r(seqInsert ds j a b)) ≼ o(K^r ds)`, the ordinal descent on the in-place ordered-insert object
   that `zKValidF_seqInsert` validity lives on — *replacing* the end-append model
   `iord_descent_iSpliceEnd`. Rotation kernel `icmp_iseqNaddIdg_seqInsert` (J-shifted induction carrying
   `isNF` of the partial fold; base = F2 `icmp_omega_pow_nadd_lt`; suffix entries fold identically via
   `inadd_right_mono` — **NO permutation / `inadd_assoc` plumbing**, which the lap-87 plan had feared
   necessary). Plus `idg`-side `iseqMaxIdg_seqInsert_le`/`idg_seqInsert_le` + helper
   `iseqMaxIdgAux_le_of_all`.
2. `9453346` — **descent wrappers** `iord_descent_seqInsert_of_ZDerivation` /
   `_of_iSpliceDescent`: plug the genuine descent into the SAME `iSpliceDescent` IH interface the
   end-append model used, NF side conditions discharged from the chain's `ZDerivation`.
3. `b0f347e` — **5.2.1 ZDerivation constructor** `ZDerivation_seqInsert_of` / `_of_zK` (analogue of
   `ZDerivation_iCritAux_of`): the spliced chain is a genuine `ZDerivation` from the two half
   derivations + spliced `isChainInf` + per-half well-formedness. New reusable `forall_znth_seqInsert`
   (pointwise entry-predicate, handles the suffix-shift predecessor decomposition ONCE).
4. `ff9d420` — **5.2 dispatch index** `permIdxAux`/`permIdx`: first-hit bounded search (single-index
   analogue of `redexAux`) for the least premise `i` with `iperm (tp dᵢ) s` (Buchholz Def 3.2 case 5.2's
   minimal `i`). Full spec stack + Σ₁-definability + `permIdx_lt_of_not_zKCritical` (a non-critical chain
   lands `permIdx` on a real permissible premise `< lh ds`).
5. `6cce9a6` — **RANK-GENERAL 5.2.1 descent.** Caught a gap: the genuine reduct rank
   `r' = max{rk(A(dᵢ)), r}` (paper md line 25) can EXCEED `r`, which the same-rank `iord_descent_seqInsert`
   did not cover. Added `idg_seqInsert_le'` / `iord_descent_seqInsert'` / `_of_iSpliceDescent` under the
   faithful hyp `r' ≤ dg(parent)` (`dg` already absorbs every chain cut rank). **This is the descent form
   the dispatch's 5.2.1 case must call** — building on the same-rank version would have hit an unprovable
   obligation when `r' > r`. ⚠ The dispatch must discharge `r' ≤ dg(zK s r ds)`, i.e. `rk(A(dᵢ)) ≤
   dg(parent)` (plausible: A(dᵢ) is dᵢ's own redex cut formula, dᵢ a premise of the chain — but PROVE it).

**Net:** the 5.2.1 branch is now COMPLETE at the object level — validity (`zKValidF_seqInsert`, lap 87),
descent (`iord_descent_seqInsert`), and ZDerivation (`ZDerivation_seqInsert_of`) all CO-LOCATED on the
same genuine `seqInsert` object. The lap-87 order-sensitivity finding is fully resolved. The dispatch
index that selects 5.2.1 vs 5.2.2 is built and specced.

## 🎯 OBJECTIVE (operator, binding)
Discharge `peano_not_proves_goodstein` (`Statement.lean:22`) **axiom-free**. Headline re-verified clean:
`[propext, sorryAx, choice, Quot.sound]` (0 math axioms). SOLE blocker = crux-2 = `redSound` =
internalized cut-elimination. The genuinely-new content per lap-86 finding: `red`'s tag-4 must DISPATCH
5.1 / 5.2.1 / 5.2.2; each branch's validity + descent.

## State of the three dispatch branches (now ALL object-complete)
- **5.1 critical** — `iCritReductG` / `ZDerivation_iCritReductG_of` (laps 84–85). Descent
  `iord_descent_iRcrit_of_chain`. Built.
- **5.2.2 replace** — `iCritAux` + `ZDerivation_iCritAux_of`/`_of_zK` + `zKValidF_seqUpdate` +
  `iord_descent_iCritAux` (lap 86). Built.
- **5.2.1 splice** — `seqInsert` + `ZDerivation_seqInsert_of`/`_of_zK` + `zKValidF_seqInsert` +
  `iord_descent_seqInsert` (+ wrappers) (laps 87–88). **Built this lap.**
- **dispatch index** — `permIdx` + `permIdx_lt_of_not_zKCritical` (lap 88). Built.

## ▶ NEXT — the tag-4 DISPATCH definition, then `redSound` (`PENDING_WORK.md` lap-88 box)
The remaining work is **assembly**, not new descent/validity math:
1. **Rewrite `iRNextG`'s tag-4 branch** (`InternalZ.lean:~5800`) to DISPATCH (currently always `iRcritG`):
   `if zKCritical (fstIdx d) (zKseq d) then iRcritG d ρ else` — at `i := permIdx d`, sub-dispatch on
   whether `znth (zKseq d) i` is itself critical → 5.2.1 `seqInsert`-reduct / 5.2.2 `iCritAux`-reduct.
   ⚠ This ripples: `iRNextGDef` (the big Σ₁ formula), `iRNextG_defined` (heartbeat-heavy), `red_zK`, and
   the lap-86 `not_zKCritical_*` lemmas (now apply only to the 5.1 sub-case). LARGE blast radius — do it
   as one careful change; keep `iRNextG` total + definable. Consider building the dispatched reduct as a
   separate definable `iRNextGD` first to contain the radius, then swap `redTable` to it.
2. **Wire the genuine halves**: for 5.2.1, `a = dᵢ{0}`, `b = dᵢ{1}` from `inference_critical_pair`
   applied to the critical premise `dᵢ`; discharge `isChainInf_seqInsert`'s `ha_ant`/`ha_rank`/`hb_succ`/
   `hb_ant` from the Thm-3.4(a) end-sequents; build `iSpliceDescent` for the descent. The halves are
   `zK`-chains ⟹ use `ZDerivation_seqInsert_of_zK` + `iord_descent_seqInsert_of_iSpliceDescent`.
3. **`redSound`** = `zDerivation_induction`, tag-4 split 5.1/5.2.1/5.2.2 → each ZDerivation constructor;
   then `iord_descent_red` UNCONDITIONAL (drops the now-unsatisfiable `hcrit`). Wire
   `Crux2Blueprint.redSound`/`iord_descent_red` → `false_of_ZDerivesEmpty` → headline (M2/M3 leaves remain).

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. src/ sorries unchanged from lap 87: `Statement.lean:22`,
`Reduction.lean:68`, `DescentSemantic.lean:582` + the 4 `Crux2Blueprint` leaves. No uncommitted edits.
Aristotle: idle (Foundation not in its v4.28 env).

## ⚠ Process note
Lost lap-88 commit-1 edits once to a reflexive `git checkout src/...InternalZ.lean` (the exact hazard in
the `never-git-checkout-uncommitted-edits` memory) — reconstructed from context. To `#print axioms`,
use `lake env lean <scratch.lean>` on a file OUTSIDE the tree; never append-then-checkout the source.
