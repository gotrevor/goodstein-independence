# HANDOFF — lap 105 · the natural-sum `#` RESOLUTION of the lap-104 `imax` ordinal tension

> Branch `plan`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). `src/` UNTOUCHED — headline footprint
> intact (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`, 0 math axioms,
> lone `sorryAx` = crux-2; `peano_not_proves_consistency` / `not_proves_of_implies_consistency` clean —
> unchanged since lap 104, no `src/` edit this lap). All new work in `wip/PathCOmega.lean`, axiom-clean
> `[propext, Classical.choice, Quot.sound]`.

## What this lap did (Brick 5c — `wip/PathCOmega.lean`, axiom-clean)

Lap 104's 2nd in-kernel finding said the `imax` stored ordinal is INSUFFICIENT for the cut node's
operator-control (the `cut` constructor needs `sord premise ≺ stored`, but the max-ACHIEVING premise EQUALS
`imax`, never `≺` — `icmp` irreflexive), and framed the fix as Gentzen's rank-aware `ω`-tower, deferred as
"genuinely multi-month". **This lap shows that deferral is unnecessary for the principal ∀/∃ step.** The
natural (Hessenberg) sum `inadd` (`#`) — already in the repo (`InternalNadd.lean`, used by the induction
node's `indOmegaStoredOrd`) — supplies BOTH the operator-control AND the descent obligations at once, in
five new axiom-clean lemmas:

- **`lt_inadd_self_right`** `: isNF X → isNF g → icmp 0 g = 0 → icmp X (inadd X g) = 0` — `X ≺ X # g` for
  `g ≻ 0`. The operator-control fact `imax` could NOT provide (strict self-domination on the right summand).
  One-liner from `inadd_left_mono` + `inadd_zero_right`.
- **`lt_inadd_self_left`** `: isNF X → isNF g → icmp 0 X = 0 → icmp g (inadd X g) = 0` — `g ≺ X # g` for
  `X ≻ 0`. From `inadd_right_mono` + `inadd_zero_left`.
- **`inadd_strict_mono`** `: a≺a' → b≺b' → a#b ≺ a'#b'` (all NF) — `#` strictly monotone in BOTH arguments.
  `icmp_trans'` over `inadd_right_mono` then `inadd_left_mono`.
- **`redAllExN`** — the `#`-stored ∀/∃-cut reduct: identical to lap-103's `redAllEx` but the stored ordinal
  is `(sord selected-∀-premise) # (sord ∃-premise)`, not their `imax`. It sums the reduced premises' OWN
  `sord` (not `iord`), correct even when a premise is itself a cut/ω-node (general Path-C), unlike `imax`'s
  `iord` left field.
- **`zcOK_redAllExN`** — **principal ∀/∃-cut `hinv`, FULL closure, `imax`-free.** The `#`-stored reduct of a
  `ZcOK` cut (left = ω-∀-node, right = ∃-node) is `ZcOK`. **Operator-control is DISCHARGED** from the
  premises' positivity + NF alone (`lt_inadd_self_right`/`_left`) — NO externally-supplied `hLctrl`/`hRctrl`.
  Contrast lap-104's `zcOK_redAllEx_of_ctrl`, which had to ASSUME `hLctrl`/`hRctrl` and could not prove them
  for `imax`. This closes the operator-control half of `hinv` for the principal ∀/∃ step.
- **`sord_redAllExN_lt`** — the `#`-stored reduct STRICTLY drops the stored ordinal **against a `#`-stored
  parent**: if the reduct's two premises each have `sord ≺` the parent's corresponding premise ordinals
  (`αAll`, `αEx`), then `(sord …) # (sord …) ≺ αAll # αEx` (the parent's own `#`-stored ordinal), by
  `inadd_strict_mono`. **No additive-principality of the parent ordinal needed** — which is the obstruction
  that drove lap 104 to `imax`. The parent's `#`-stored ordinal is itself the operator-controlled value, and
  strict-monotonicity carries the descent. So BOTH operator-control AND descent hold for the SAME `#` choice.

### Why this is the right resolution (not a shortcut)

This is exactly the standard Schütte `#`-bookkeeping (Towsner's META cut-elimination in `src/Zinfty.lean`
combines cut premises by natural sum). Lap 104's `imax`-vs-`inadd` tension was real but the conclusion ("need
the tower NOW") over-shot: `imax` gives descent-vs-ARBITRARY-`α` but no operator-control; `inadd` gives both,
because a properly operator-controlled parent stores `#` of ITS premises, so the descent is against a
`#`-value and strict-monotonicity (not principality) carries it. The reduct is NOT principal so its own
ordinal need not be principal — only strictly below the parent's, which `#`-monotonicity delivers.

## Brick 5d ALSO landed (the `#`-resolution is UNIFORM across ω-nodes)

`redIndExN` + `zcOK_redIndExN` + `sord_redIndExN_lt` — the induction/∃-cut analogue of brick 5c, the SAME
`lt_inadd_self_*`/`inadd_strict_mono` argument applied to PA's induction ω-node (the genuinely-PA-specific
rule). Confirms the lap-105 insight is structural to the cut node, not to one cut-formula shape: both
ω-nodes (∀ and induction) get operator-control + descent from the `#`-storage with no additive-principality.
Premises' `ZcOK`-hood taken as hypotheses (same provenance as brick 5c; the induction-node constructor for
`ZcOK` is the small datatype extension that makes them automatic). Both axiom-clean.

## Brick 5e — the COMPLETE cut ordinal `max(a,b)+1` (supersedes 5c/5d's `#`, no positivity)

Bricks 5c/5d's `#`-storage closes operator-control only when BOTH premises have POSITIVE ordinal
(`X ≺ X#g` needs `g ≻ 0`). An axiom LEAF has ordinal `0` (Schütte's `o(axiom)=0`), so a cut whose premise
is an axiom breaks `#` operator-control. **Brick 5e closes the gap with the textbook Tait/Schütte cut
ordinal `o(cut) = max(o(dL), o(dR)) + 1`** (`inc (imax …)`, `inc α := α # 1`):
- **`zcOK_redAllExS`** — operator-control UNCONDITIONAL: each premise `≼ imax ≺ imax+1`
  (`lt_imax_inc_left/right`), NO positivity hypothesis (the `+1` supplies the strictness `imax` itself
  lacked — the exact lap-104 gap). Holds even when a reduced premise is an axiom leaf (ordinal `0`).
- **`sord_redAllExS_lt`** — descent against an ARBITRARY `max+1`-stored parent (no additive-principality —
  the lap-104 `imax` virtue, now WITH operator-control): `a'≺a, b'≺b ⟹ imax a' b' + 1 ≺ imax a b + 1`
  (`inc_imax_strict_mono`, via the linear-order `icmp_imax_lt` + `inc_strict_mono`).
- Helpers (all axiom-clean): `inc`, `isNF_inc`, `lt_inc`, `inc_strict_mono`, `isNF_imax`,
  `lt_imax_inc_left/right`, `lt_imax_of_lt_left/right`, `inc_imax_strict_mono`.

**`redAllExS` is the canonical reduct.** It supersedes lap-104's bare `imax` (no op-control) AND lap-105's
`#` bricks 5c/5d (positivity-gated). The `#` lemmas remain valid (true under positivity) and the `#`
infrastructure (`inadd_strict_mono`, `lt_inadd_self_*`) underpins `inc`. With `max+1`, the principal ∀/∃
cut step is FULLY closed (operator-control + descent, no side conditions beyond NF). The genuinely-deep
content remaining is purely the rank-mixing tower for COMPOUND cut formulas, off the principal ω-cut step.

## Brick 5f — leaf `sord` NF discharged (the cut bricks' NF hypotheses are now automatic for leaves)

`isNF_sord_of_ZDerivation : ZDerivation d → isNF (sord d)` (unconditional), via `sord_eq_iord_of_ZDerivation`
(a leaf's `sord` = its `iord`, the dispatch falling through tags 7/8/9/10 — `zTag_ne_{seven,eight,nine,ten}`)
+ `isNF_iord_of_ZDerivation` (`iord = iotower (iotil ·) (idg ·)`, `iotil` NF via `isNF_iotil_of_ZDerivation`,
`iotower` NF-preserving). New helper `zTag_ne_eight_of_ZDerivation` rounds out the engine-tag exclusions.
Corollary **`zcOK_redAllExS_leaf`**: the principal ∀/∃-cut reduct is `ZcOK` with ZERO side conditions when
both reduced premises are engine `ZDerivation`s (the embedding's image) — NF auto, positivity not needed
(brick 5e). This is the cleanest statement of the principal ∀/∃ operator-control. All axiom-clean.

## What remains (sharply isolated now)

The genuinely-deep `ω`-tower content of `Zinfty.cutElimStep` is NOT gone — but it is now pinned to its true
locus: **rank-mixing across COMPOUND cut formulas.** A single cut on `A ∧ B` (rank `|A∧B|`) reduces to TWO
cuts on `A`, `B` (lower rank) — a pure single-`#`-ordinal measure does NOT strictly decrease across a
rank-lowering step; that is where `omegaTower (cr) (o)` collapses `(rank, ord)` into one ordinal. The ∀/∃
PRINCIPAL step (this brick) does NOT need it. So the NEXT obligations, hardest-first:

1. **General ∀-inversion `redInv∀`** (front carried from lap 104) — the recursion that RE-PRINCIPALIZES a
   left premise that is NOT literally an ω-∀-node (cut/∃/leaf), pushing the inversion through the last rule
   (Schütte; `Zinfty.allInvAux` is the META template). Principal case = `zcOK_omegaAll_inv` (premise
   selection, banked). With `redInv∀` + `zcOK_redAllExN` + `sord_redAllExN_lt`, the GENERAL ∀/∃ cut step is
   `hinv`+`hdrop`-closed.
2. **The internal `ω`-tower `iomegaTower`** (replaces front-2's "rank-aware sord" framing) — for the COMPOUND
   cut formulas only. Port `Zinfty.omegaTower` (`ω_0^α = α`, `ω_{c+1}^α = ω_c^(ω^α)`) to the ε₀-codes
   (`ocOadd α 1 0` = `ω^α`, `iotower` already exists — see `indOmegaStoredOrd`). The single-ordinal measure
   the ∧/∨ cut reductions descend in. NOT needed for the ∀/∃ step.
3. Then the genuine `red_iterate_descends` with `P = ZcOK ∧ derives ∅→⊥`; embedding (`hz`); arithmetize
   `red` + `gentzenDescentφ` → `gentzen_descent_of_inconsistent` (`wip/GentzenCon.lean`) + crux-1 PRWO →
   `goodstein_implies_consistency` → headline (ONLY when `#print axioms` clean).

## Kernel state (unchanged this lap — no `src/` edit)
- `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]` — bare `sorry`, 0 math axioms.
- `peano_not_proves_consistency` / `not_proves_of_implies_consistency` = `[propext, choice, Quot.sound]` — clean.

## Aristotle
Idle — CORRECT. The `redInv∀` recursion and the `iomegaTower` port are entangled with the
`InternalZ`/`Zinfty`/`InternalNadd` API; not self-contained mathlib lemmas.

## This lap's commit
`wip/PathCOmega.lean` only (Brick 5c: 5 new axiom-clean lemmas + the `#`-stored reduct), STATUS/PENDING_WORK/
NEXT_STEPS refreshed. `lake build GoodsteinPA` green 1325; `lake env lean wip/PathCOmega.lean` clean, all new
decls `[propext, Classical.choice, Quot.sound]`; `src/` untouched.
