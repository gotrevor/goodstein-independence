# Handoff: lap 138 — (B0) iteration linchpin DISCHARGED via the repo's own `IIter`; Σ₁-orbit half VALIDATED

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (`lake build GoodsteinPA`, **1326**).
Headline footprint UNCHANGED (`peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
— 0 math axioms; lone `sorryAx` = crux-2).

## ⭐ THE advance (a real src `sorry` dropped on the termination path)
**(B0) `exists_sigma1_iterate` is PROVEN** (was `sorry`) — and with it **(B) `exists_sigma1_descent_of_sigma1_step`
is now fully PROVEN** (it was proven-modulo-(B0)). Both axiom-clean `[propext, Classical.choice, Quot.sound]`.

**How:** the lap-137 baton said "no generic Foundation iteration combinator exists (checked: HFS has none)" and
framed (B0) as a from-scratch `PR.Construction`. **That missed that the repo already built exactly this combinator
for crux-1:** `IIter.iIter fDef f hf x c = f^[c] x` (`src/GoodsteinPA/IIter.lean`) — a genuine `𝚺₁`
`PR.Construction` over an arbitrary parameter-free-defined `f`, with `iIter_zero`/`iIter_succ`/`iIter_definable'`
all banked and axiom-clean. So `orbit n := IIter.iIter gDef g hg z n` discharges (B0) in three lines:
`iIter_zero` ⟹ `orbit 0 = z`, `iIter_succ` ⟹ `orbit (n+1) = g (orbit n)`, and `𝚺₁-Function₁ orbit` via
`DefinableFunction₂.comp` on `iIter_definable'`.

**The lap-137 parameter-free SUBTLETY was real and is now resolved by the statement, not worked around:** `PR.Blueprint.succ`
needs a parameter-free `Semisentence`, so (B0)'s hypothesis is the explicit `hg : 𝚺₁.DefinedFunction₁ g gDef`
(parameter-free `gDef`), NOT the parametrized `𝚺₁-Function₁ g`. `gDef` threads back through (B) and (A).
(Added `import GoodsteinPA.IIter` to `Crux2Blueprint.lean` — acyclic: IIter's deps are crux-1
Cor34/Grzegorczyk, never InternalZ/Crux2.)

**Why this matters (de-risks the existence-form pivot):** the handoff flagged (B0) as the decisive test —
"if the `𝚺₁`-orbit can't be built, the whole pivot is hollow." **It is NOT hollow: the Σ₁ orbit builds cleanly.**
The orbit→NF→descent→PRWO machinery (B0)+(B) is now entirely closed; the termination half's remaining content is
purely the per-step descent + packaging it as a concrete function.

## 📌 Termination path — exact open state now
`false_of_ZDerivesEmpty` (`:1754`) → `prwo_forbids_existence_descent` → `exists_sigma1_descent_of_step` (`:1730`,
sorry-free) → **(A)** + (B)+(B0 PROVEN); and `ZDerivesEmptyR_descent_step` → **`descent_step_K_majorIdx`**.
**Two open sorries remain on this path:**
- **(A) `exists_sigma1_descending_step`** (`Crux2Blueprint:1669`) — a *total concrete* `𝚺₁` step `g : V → V`
  **with an explicit `gDef : 𝚺₁.Semisentence 2`** (conclusion strengthened this lap to demand the `gDef` that
  (B0) consumes), descending on every `ZDerivesEmptyR` point.
- **`descent_step_K_majorIdx`** (`Crux2Blueprint:1614`) — the deep per-step `𝗜𝚺₁` cut-reduction descent (K-case).

## 🎯 NEXT ATTACK (recorded for the crux) — (A) collapses onto a concrete reduct + `descent_step_K_majorIdx`
(A) and `descent_step_K_majorIdx` **share one need:** a CONCRETE deterministic descending reduct *function*
(not a bare `∃`). The clean route:
1. Define a concrete `redStep : V → V` (parameter-free `𝚺₁`, hence explicit `redStepDef`): on an **Ind root**
   `redStep d := red d` (already concrete; descent = `iord_descent_red_zInd`, PROVEN); on a **K root**
   `redStep d :=` the major-premise-replace reduct `seqUpdate`/`zKseq` with the chosen premise replaced by its
   reduct (built from `red`, `majorIdx`, `zKseq` — all concrete `𝚺₁`).
2. (A) is then immediate: `g := redStep`, `gDef := redStepDef`, descent = `ZDerivesEmptyR_descent_step` re-cast
   to return `redStep d` (Ind branch already does; K branch = `descent_step_K_majorIdx` stating the descent of the
   concrete `redStep` rather than a bare `∃`).
3. The genuine deep content stays `descent_step_K_majorIdx` (Gentzen cut-reduction: tag-3 Ind soundness via the
   corrected `iIndReductSeqG`, tag-5/6 principal-cut via the banked `cutPartner`/`hAll`, tag-4 chain `<`-recursion).

So the productive ordering is: **(i) define the concrete `redStep`/`redStepDef`** (mechanical, banks (A)'s shell),
**(ii) grind `descent_step_K_majorIdx`** (the deep core). Avoid the `μ`/witness-bound route for (A) — the concrete
engine reduct sidesteps the wrong-polarity totality guard entirely.

### ⚠️ lap-138 structural findings (two traps to avoid; the genuine tag-3 entry)
- **`g := red` is NOT (A)'s step.** `red` is valid everywhere (`ZDerivesEmptyR_red`, PROVEN) and descends on **Ind
  roots** (`iord_descent_red_zInd`) but **STALLS on K roots** — so it fails the descent clause there. (A) genuinely
  couples to `descent_step_K_majorIdx` (the majorIdx-keyed K-reduct); no `red`-only shortcut.
- **`descent_step_K_majorIdx` tag-3 is NOT "descent-proven + soundness-sorry".** The banked
  `descent_K_majorIdx_Ind_descends` proves descent for the single-replace witness `zK s r (seqUpdate ds m (red dⱼ))`,
  but that is the **ORDINAL SHADOW**: `iord` is instance-invariant so it descends, yet its chain-VALIDITY is exactly
  the lap-136 obstruction (`zKValidF_iIndReduct_of_zInd` FALSE; refuted in-kernel by
  `zKValidF_iIndReduct_forces_degenerate`). So `ZDerivesEmptyR` of that witness is FALSE for a genuine Ind node —
  **do NOT decompose tag-3 with the `red dⱼ` witness (it plants a false sorry).**
- **THE genuine tag-3 entry:** the lap-136 corrected substituted chain `iIndReductSeqG d0 d1 a k = ⟨d0, d1[a:=0], …,
  d1[a:=k-1]⟩` (infra BUILT at `Crux2Blueprint:196-270`) has **no validity yet** — assemble
  `isChainInf s (irk p) (iIndReductSeqG d0 d1 a k)` (exit at last premise `d1[a:=k-1]` deriving `F(k)=seqSucc s`;
  threading `F(i)→F(i+1)` via the banked `znth_iIndReductSeqG_step` + `seqSucc_zsubst_zInd_step`) and the
  `iord`-descent of that chain. Building blocks (`znth_iIndReductSeqG_zero/_step`, `seqSucc_zsubst_zInd_step`) are
  banked; the `isChainInf` assembly is the multi-lemma deep core both tag-3 and (via (A)) the termination half need.

## ⚠️ Forbidden / do-NOT-undo
- Do NOT undo the lap-137 PRWO-seam type-correction (re-stating `→ False` in bare `𝗜𝚺₁` is Gödel-barred).
- Do NOT revert (B0) to the parametrized `𝚺₁-Function₁ g` hypothesis — `PR.Blueprint.succ` genuinely needs the
  parameter-free `gDef`; the explicit-`gDef` statement is the provable one and (A) supplies it.
- Do NOT re-derive a fresh `PR.Construction` for the orbit — `IIter.iIter` already is it.

## 📁 Key anchors
- `src/GoodsteinPA/Crux2Blueprint.lean`: (B0) `exists_sigma1_iterate` (`:1685`, PROVEN via IIter), (B)
  `exists_sigma1_descent_of_sigma1_step` (`:1707`, PROVEN), `exists_sigma1_descent_of_step` (`:1730`, sorry-free),
  (A) `exists_sigma1_descending_step` (`:1669`, sorry — strengthened to produce `gDef`), `descent_step_K_majorIdx`
  (`:1614`, sorry).
- `src/GoodsteinPA/IIter.lean`: `iIter` (`:45`), `iIter_zero`/`iIter_succ` (`:50`/`:53`), `iIter_definable'` (`:68`).
- `DIRECTION.md` CURRENT DIRECTIVE (lap-137, still binding) · `PENDING_WORK.md` lap-138 · `STATUS.md` lap-138 box.

---
**→ Next session: HEAD = this commit, build green. (B0)/(B) DONE. Termination half = (A) [concrete `redStep` +
`gDef`] + `descent_step_K_majorIdx` [the deep Gentzen descent]. Define `redStep`/`redStepDef` first (banks (A)),
then grind `descent_step_K_majorIdx`. Do NOT chase the `μ`/witness-bound route for (A).**
