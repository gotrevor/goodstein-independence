# HANDOFF — 2026-06-22

Branch `plan`, HEAD `b809c81`. Build **green**: `lake build GoodsteinPA` (1204 jobs, offline).

## What this lap did (M1 + M2 + Phase-2 decomposition)

1. **M1 — `Encoding.goodsteinTerminates_re` PROVED.** Phase 0 is now axiom-clean.
   New `src/GoodsteinPA/Computability.lean` proves by hand (mathlib won't auto-derive WF-recursive
   `Computable`):
   - `primrec_natLog` — `Nat.log` primitive recursive (strong-recursion equation `natLog_rec`).
   - `primrec_bump` — `bump` primitive recursive (top-power peeling; both recursive calls land at
     indices `< n`, recovered from the `nat_strong_rec` value table via `Option.iget`).
   - `primrec_goodsteinSeq` — `goodsteinSeq` primitive recursive (structural `nat_rec'`).
   Then `goodsteinTerminates_re` chains `PrimrecPred → ComputablePred → .to_re → REPred.projection`
   (∃ N). Verified: `#print axioms goodsteinSentence_faithful = [propext, Classical.choice,
   Quot.sound]` (no `sorryAx`).

2. **M2 — Phase 1 Gödel II hook.** New `src/GoodsteinPA/Reduction.lean`:
   - `peano_not_proves_consistency : 𝗣𝗔 ⊬ ↑𝗣𝗔.consistent` (Foundation's `consistent_unprovable`).
   - `not_proves_of_implies_consistency : (𝗣𝗔 ⊢ γ → 𝗣𝗔 ⊢ Con(𝗣𝗔)) → 𝗣𝗔 ⊬ γ` — **axiom-clean**
     meta-reduction (collapses the headline to one implication).
   - `goodstein_implies_consistency : 𝗣𝗔 ⊢ γ → 𝗣𝗔 ⊢ Con(𝗣𝗔)` — disclosed `sorry` (the girder).
   - **Finding:** Gödel II for `𝗣𝗔` needs `𝗣𝗔.Δ₁`, which Foundation supplies as the **axiom**
     `PA_delta1Definable` (TODO in `Incompleteness/Examples.lean`). So Route A's profile carries
     that axiom. Documented; see O3.

3. **Phase 2 decomposition (operator's explicit ask).**
   - `PHASE2-DECOMPOSITION.md` — lemma-by-lemma ladder (M3…M7) grounded in
     `papers/towsner-goodstein-epsilon0-unprovability.pdf` (read in full this lap). Two routes:
     **A** (our Gödel II hook, inherits `PA_delta1Definable`) vs **B / Towsner** (direct `𝗣𝗔 ⊬ γ`
     via `Z_∞` ω-rule embedding + cut-elimination + Hardy domination — **no Con(PA), no axiom**).
     **Recommends Route B as the spine.**
   - `wip/Zinfty.lean` — **compile-checked** prototype of the M3 crux (encoding E2): the
     infinitely-branching ω-rule `inductive Deriv`, well-defined `Ordinal` bound + `ℕ∞` cut-rank
     measures, `o_allI_gt` (ω-bound dominates each premise), `Provable.mono`, `Provable.weakening`
     (ordinal-free). Validates the infinitary calculus IS formalizable in Lean/mathlib.

## State of the headline (ANTI-FRAUD)
`Statement.peano_not_proves_goodstein` is **still a literal `sorry`** (untouched). Per
`DIRECTION.md`, discharge only when `#print axioms` is clean AND it genuinely chains. It does not
yet. `Defs.lean`, `Bridge.lean` RHS, `goodsteinTerminates` — all LOCKED, untouched.

## Next lap — start here (hardest-first)
Open obligations + 3 attack paths each are in **`PENDING_WORK.md`**. Priority order:
1. **O2 / Phase-2 crux.** Continue `wip/Zinfty.lean`. Best next move per the decomposition: either
   (a) refine `AForm` to a real free-variable + numeral-substitution layer (so `rk` is genuinely
   finite, replacing the `ℕ → AForm` family), or (b) **attack cut-elimination M5** (Towsner §19:
   inversions 19.2–19.4, reductions 19.5–19.6, the `ω^α` step 19.7, iteration 19.9) directly on
   the abstract `Deriv` — it is the self-contained heart and the true feasibility test.
2. **O2 / M6 (parallel).** Hardy domination — coordinate with Track 1
   (`~/src/lean-formalizations` `Logic/FastGrowing`); reuse, don't duplicate. Only M6.4 (Thm 17.1,
   the cut-free lower bound) is new here.
3. **O3** — if staying Route A, `PA_delta1Definable` must eventually be discharged; Route B avoids
   it. Could file an `ON-LINE-REQUEST.md` to check whether a newer Foundation rev fills the TODO.

No open `ON-LINE-REQUEST.md` (Towsner is self-contained for Route B). No Aristotle job in flight
(`--no-aristotle` / offline assumed).

## Build / file map
- `src/GoodsteinPA/{Defs,Encoding,Bridge,Statement}.lean` — original (Statement headline `sorry`).
- `src/GoodsteinPA/Computability.lean` — NEW (M1, axiom-clean).
- `src/GoodsteinPA/Reduction.lean` — NEW (M2; one disclosed `sorry` = the girder).
- `src/GoodsteinPA.lean` — root, imports all of the above.
- `wip/Zinfty.lean` — Phase-2 prototype (OUTSIDE build target; check with
  `lake env lean wip/Zinfty.lean`).
- `PHASE2-DECOMPOSITION.md`, `PENDING_WORK.md` — the plan + open-work map.
