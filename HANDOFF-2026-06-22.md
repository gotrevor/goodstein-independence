# HANDOFF — 2026-06-22 (lap 2)

> **NEXT LAP FIRST ACTION:** attack the **cut-reduction lemma** (Towsner §19.5–19.7) in
> `wip/ZinftyF.lean` — the ordinal-arithmetic heart of the lone open `cutElimStep`. All three
> inversion lemmas it needs are now PROVED. See `PENDING_WORK.md` O2 "NEXT (hardest-first)".

Branch `plan`, HEAD = latest `feat(Phase 2): prove ∧-inversion…`. Headline build **green**
(`lake build GoodsteinPA`; `wip/` is outside the build target, so it never affects the headline).
Headline `Statement.peano_not_proves_goodstein` is **still a literal `sorry`** (untouched, per
ANTI-FRAUD — it does not yet chain). `Defs.lean`/`Bridge.lean` RHS/`goodsteinTerminates` LOCKED.

## What this lap did — built the real `Z_∞` calculus + the whole inversion layer

New file **`wip/ZinftyF.lean`** (654 lines, `lake env lean wip/ZinftyF.lean` clean except the one
`cutElimStep` sorry). This **supersedes** the abstract-`AForm` prototype `wip/Zinfty.lean` (kept
for history; do NOT delete). Implements the HANDOFF KEY FINDING: build `Z_∞` on Foundation's real
`SyntacticFormula ℒₒᵣ`.

1. **`Z_∞` calculus over real ℒₒᵣ syntax.** `inductive Deriv : Seq → Type` with the ω-rule
   `allω` (one premise per numeral `n`, via `φ/[nm n]`). Measures: ordinal bound `o : Deriv → Ordinal`
   (ω-node = `(⨆ₙ o) + 1`), cut rank `cr : Deriv → ℕ∞`. `Provable α c Γ := ∃ d, o d ≤ α ∧ cr d ≤ c`.
   - **The `ℕ∞/⊤` blocker is dissolved:** Foundation's `complexity : Form → ℕ` is always finite, so
     `Provable α (c:ℕ)` non-vacuously bounds *quantified* cut formulas (impossible on `AForm`).

2. **Structural decision — set sequents.** `Seq := Finset Form`. Matches Towsner (his `Γ` is a
   finite *set*). ⇒ **contraction is free** (`insert` idempotent), so there is **no `contr` rule**.
   This is load-bearing: an explicit `contr` rule makes the inversion lemmas intractable (the
   principal-contraction case must re-invert a *remaining* copy, breaking both structural and
   ordinal induction). Sets dissolve that case. `Provable.contr` is then a free corollary.

3. **Full predicate-level inference API, all proved:** `axL`, `verumR`, `andI`, `orI`, `exI`,
   `allω` (sup-bound ω-rule), `cut`, `mono`, `weakening`, `cast`.

4. **All three inversion lemmas PROVED** (Towsner §19.2–19.4 — the syntactic content of
   cut-elimination), each by structural induction on `Deriv`, all 8 constructor cases, preserving
   both ordinal bound and cut rank:
   - `orInvAux` / `Provable.orInv` — §19.2 (∨).
   - `andInvAux` / `Provable.andInvL` / `Provable.andInvR` — §19.3 (∧, both conjuncts).
   - `allInvAux` / `Provable.allInv` — §19.4 (ω/∀, the distinctive infinitary one; principal `allω`
     case supplies the instance directly from the ω-indexed premise family).

5. `cutElim` (Thm 19.9) is *proved* by induction reducing entirely to `cutElimStep` (Thm 19.7),
   the lone `sorry`.

## State of the headline (ANTI-FRAUD)
`Statement.peano_not_proves_goodstein` still a literal `sorry`. The whole `wip/ZinftyF.lean` layer
is Phase-2 frontier work toward Route B; it does not yet connect to the headline (that's M4
embedding + M6 lower bound + M7). Discharge the headline only when `#print axioms` is clean AND it
genuinely chains.

## Next lap — start here (hardest-first)
1. **Cut-reduction lemma (Towsner §19.5–19.7)** in `wip/ZinftyF.lean` → discharges `cutElimStep`.
   The inversions are done; the reduction is the ordinal-arithmetic heart. Sketch in
   `PENDING_WORK.md` O2 "NEXT". First decide whether the `(α,c)` indexing suffices or a numeric
   Hardy `k` parameter must be threaded through `Provable` (Towsner uses `h_{ω^α}(k)` in 19.6/19.7).
2. After cut-elimination closes: **M4 embedding** (finitary PA proof ↪ `Z_∞` at bound `< ε₀`,
   Towsner Thm 16.7/18.1) and **M6.4 lower bound** (Thm 17.1, no cut-free `Z_∞` proof of the
   Goodstein sentence at any `α < ε₀`). Then M7 connect to `goodsteinSentence`, M20.1 contradiction.
3. When a milestone in `ZinftyF.lean` is genuinely closed and self-contained, promote it from `wip/`
   to `src/` (add to `GoodsteinPA.lean` imports).

No open `ON-LINE-REQUEST.md` (Towsner self-contained). No Aristotle job (offline).

## Build / file map
- `src/GoodsteinPA/{Defs,Encoding,Bridge,Statement}.lean` — original (headline `sorry`).
- `src/GoodsteinPA/{Computability,Reduction}.lean` — M1 (axiom-clean) + M2 (Gödel II hook).
- `wip/ZinftyF.lean` — **Phase-2 Z_∞ calculus + inversions (THIS LAP), outside build target.**
- `wip/Zinfty.lean` — superseded `AForm` prototype (history; keep).
- `PHASE2-DECOMPOSITION.md`, `PENDING_WORK.md` — plan + open-work map.
