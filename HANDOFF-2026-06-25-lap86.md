# HANDOFF — lap 86 · crux-2: gating question RESOLVED + 5.2 dispatch leaves built

> Supersedes prior HANDOFF-*.md. Read order: this → `STATUS.md` (lap-86 box) → `PENDING_WORK.md`
> (lap-86 box) → `ANALYSIS-2026-06-25-lap86-criticality-resolved.md` → `Crux2Blueprint.lean` (leaves).
> Branch `plan`. HEAD `1c61bb1`. Build 🟢 green (`lake build GoodsteinPA`, 1325 jobs). Tree clean.

## ✅ 5 GREEN COMMITS THIS LAP (axiom base untouched, headline still 0 math axioms)
1. `e82b47b` — **gating finding** `not_zKCritical_red_zK` (+ `_iRcritG`/`_iCritReductG`): the critical-only
   reduct is non-critical after one step ⟹ the 5.2 dispatch is MANDATORY (see below / the ANALYSIS doc).
2. `0b8b6c1` — `zKCritical` is Δ₁ (`zKCriticalDef` + `zKCritical_defined`/`_definable`): the dispatch
   branch predicate, so `iRNextG`'s tag-4 can test criticality and stay Σ₁.
3. `8c5d793` — **5.2.2 VALIDITY leaf** `ZDerivation_iCritAux_of` (replace premise `i` of a valid chain by
   a same-end-sequent valid reduct ⟹ valid chain). Next to banked descent
   `iord_descent_iCritAux_of_ZDerivation` ⟹ the 5.2.2 case is complete at the lemma level.
4. `cdb2fb0` — **dispatch-ready 5.2.2 corollary** `ZDerivation_iCritAux_of_zK` (reduct is a K-chain ⟹
   own-perm + formula-hood hyps collapse; only the end-sequent match remains).
5. `1c61bb1` — **Buchholz Lemma 3.3** `iperm_tp_fstIdx_of_ZDerivation` (own-permissibility of any
   ZDerivation): uniform discharge of `hperm_v` across the leaves; needed for the 5.2.1 spliced halves.

## 🎯 OBJECTIVE (operator, binding)
Discharge `peano_not_proves_goodstein` (`Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`). Headline re-verified this lap: `[propext, sorryAx, choice,
Quot.sound]` (0 math axioms, honest sorry, anti-fraud intact). SOLE blocker = **crux-2** = the blueprint
nut **`redSound`** (`Crux2Blueprint.lean:54`) = internalized cut-elimination.

## ✅ THIS LAP (FRESH-MIND REVIEW) — resolved the lap-85 gating question
The lap-85 NEXT priority 2 asked: *does a `ZDerivesEmpty` K-chain always have a critical redex?*
("decide this first — it gates whether `red` is complete.") **ANSWER: NO**, proved in-kernel.

- **`not_zKCritical_red_zK`** (+ `not_zKCritical_iRcritG`, `not_zKCritical_iCritReductG`) in
  `InternalZ.lean` (after `red_zK`, axiom-clean `[propext, choice, Quot.sound]`): the critical-only
  reduct `red (zK s r ds) = iRcritG …` is ITSELF a non-critical chain — its `⊥`-half premise (index 1)
  is a `K`-chain (`tp = isymRep`, permissible everywhere via `iperm_isymRep`). So
  `zKCritical (fstIdx (red d)) (zKseq (red d))` FAILS.
- **Consequence:** the iterate-descent `iord_iR2_iterate_descends`'s `hcrit` hypothesis (a `zKCritical`
  on every iterate) is **UNSATISFIABLE after one reduction step**. The critical-only `red`/`iR2`
  (Buchholz Def 3.2 case **5.1 only**) cannot drive the ε₀-descent on its own.

This is the lap-86 analog of lap-70's `not_zKValid_iCritReduct` (an in-kernel obstruction that redirects
the design). It does NOT waste laps 84–85: `red`/`iRcritG`/`ZDerivation_iRcritG_of` ARE the needed 5.1
case. The finding is that 5.1 is one of three branches.

## ▶ NEXT — the corrected `red` (Buchholz Def 3.2 case 5 DISPATCH; descent banked, lap-82)
`iRNextG`'s tag-4 (`InternalZ.lean:~4870`) is `iRcritG d ρ` unconditionally. Make it dispatch on
`zKCritical (fstIdx d) (zKseq d)` (now Δ₁): 5.1 critical → `iRcritG`; else the 5.2 cases.

1. **5.2.1 splice VALIDITY — THE NEXT HARD PIECE.** `red d = zK s r' (seqCons (seqUpdate ds i dᵢ{1}) dᵢ{0})`.
   Descent banked (`iord_descent_iSpliceEnd` over exactly this shape). ⚠ Validity unbuilt: only read-outs
   (`znth_seqCons_seqUpdate_top`/`_lt`, `lh_seqCons_seqUpdate`) — NO `isChainInf`/`zKValidF` for the spliced
   chain. Build the spliced-chain threading: the new chain `seqCons (seqUpdate ds i a) b` re-establishes
   `isChainInf` (distinguished `j₀`, `Γₖ ⊆ Γ,A₀…A_{k-1}` threading with the two spliced halves — `a=dᵢ{1}`
   at `i` carries succedent `Aᵢ`, `b=dᵢ{0}` at the end carries the cut formula `A(dᵢ)` threaded back to its
   prior occurrence, `rk ≤ r'`). This is REQUIRED (not avoidable by 5.2.2-replace) because for a critical
   `dᵢ` the pre-ordinal `õ(red dᵢ)` BLOWS UP (Lemma 4.1(b)) — `iord_descent_iCritAux` needs `õ(v) ≺ õ(dᵢ)`,
   which fails; the splice instead incorporates `dᵢ`'s halves (smaller `õ`) directly. Use
   `iperm_tp_fstIdx_of_ZDerivation` (lap 86) for the halves' own-perm.
2. **5.2.2 replace VALIDITY — DONE (lap 86).** `ZDerivation_iCritAux_of` / `_of_zK` + banked descent. For
   ⊥-chains the chosen premise is a `Rep` so `tp(dᵢ)(Π,n)=Π` (conclusion + rank unchanged); the general
   reduced-succedent op `tp(dᵢ)(s,n)` only matters if `red` is total on non-⊥ chains.
3. **Dispatch DEF + recursion eqs** — redefine `iRNextG` tag-4 to branch on `zKCritical`, find the minimal
   permissible-premise index `i` (mirror `redexAux`'s bounded first-hit `PR.Construction`), and select
   5.1 / 5.2.1 / 5.2.2; re-prove `red_zK` to the dispatching form, re-check `red_eq_iR2_of_tag_ne_four`,
   `fstIdx_red_of_tag_Ind_or_K`, and the lap-86 `not_zKCritical_*` lemmas (the obstruction lemmas should
   now apply only to the critical sub-case). Then `iRNextGDef` definability (uses `zKCriticalDef`).
4. **`redSound`** = `zDerivation_induction`, tag-4 split 5.1/5.2.1/5.2.2 → each a `zKValidF` chain;
   `iord_descent_red` becomes UNCONDITIONAL (drops `hcrit`). The lap-85 R2 (`haux0`/`haux1` of
   `ZDerivation_iRcritG_of`) + the `zAx1` tag-7 issue still apply to the 5.1 sub-case.

## ⚠ Note on lap-85 priority-1
`iord (red x) = iord (iR2 x)` unconditional is necessary but NOT sufficient — it inherits `iR2`'s
descent, which is itself gated on the now-false criticality. Pursue it only as part of the dispatch, not
as a standalone closer of `iord_descent_red`.

## Build / verify
`lake build GoodsteinPA` 🟢 1325 jobs. src/ sorries unchanged: `Statement.lean:22`, `Reduction.lean:68`,
`DescentSemantic.lean:582` + 4 blueprint leaves (`redSound`, `iord_descent_red`, `foundation_bot_to_Z_empty`,
`false_of_ZDerivesEmpty`). Aristotle: idle (Foundation not in its v4.28 env).
