# Handoff: lap 148 — §14.254 REPLACE plumbing banked + no-redex residual re-split (judge-C3 aligned)

**Date**: 2026-06-26 · **Branch**: plan · build 🟢 green (1326 jobs, `lake build GoodsteinPA`).
Headline UNCHANGED `peano_not_proves_goodstein = [propext, sorryAx, Classical.choice, Quot.sound]`
(0 math axioms). ONE commit. Operator SOLE-OBJECTIVE = **M1b-term** (crux-2 termination).

## What drove this lap
A host session flagged `E-2026-06-26-JUDGE-codex-review.md` ("probably worth taking a peek"). Its point
**C3** (converged with an outside Codex review) is a direct course-correction on the lap-147 handoff's
"write `descent_step_general` first" plan: **prove the two no-redex major-premise cases SEPARATELY first +
a shared replace tool; do NOT write `descent_step_general` blind — verify they share a motive first.**
This lap did exactly that, grounded in the Buchholz source.

## 🎯 The deliverables (all green, headline clean)
1. **Source read — Buchholz §14.25** (`scratchpad/buchholz-gentzen.txt:262-490`). The decisive facts:
   - **Major premise is NEVER in endform** — a GENERAL argument (`Γⱼ ⊆ Θ` + `Aⱼ ≈ ⊥`: were the major premise
     endform, the endsequent `Θ→D` would be too, contra the standing non-endform hypothesis). Valid for ANY
     `Γ`, not just `∅` — so the recursion into a sub-premise stays reducible. (`majorPrem_tag_mem` proves the
     {3,4,5,6} tag dichotomy for `Γ=∅` specifically; Buchholz's endform argument is the general version.)
   - **has-redex = §14.253** principal cut → PROVEN lap-147 (`descent_step_K_hasRedex`).
   - **no-redex = §14.254a ∪ §14.254b**, BOTH "replace a `Rep` premise with its SAME-end-sequent reduct":
     §14.254a (`tp(dₘ)=Rep`) replaces the major premise; §14.254b (`tp(dₘ)=Lₖ_V`, `V∉Θ`) replaces the upstream
     cut-partner `i′`. In the NO-redex case `i′` is NOT an R-intro (else `⟪i′,m⟫` is a redex `≤ j0`), so
     `tp(d_{i′})=Rep`. **The two cases provably share ONE motive — the judge-C3 gate is SATISFIED.**
   - **CORRECTION** to lap-147's PENDING note: no-redex `axMajor` is §14.254b (reduce the Rep partner), NOT
     §14.253 (the principal cut — that IS the has-redex case, already proven). The cut-partner is NOT a
     principal R-intro here; it is a Rep node (chain/Ind).
2. **`descent_step_K_replace`** (`Crux2Blueprint:2475`, axiom-clean `[propext, choice, Quot.sound]`) — the
   shared §14.254 replace step, off `red`, off criticality. Given a chain `zK s r ds` and ANY premise `i`
   with a same-end-sequent (`fstIdx v = fstIdx (znth ds i)`) strictly-`iord`-descending regular/fresh/seqAnt
   `ZDerivation` reduct `v`, `zK s r (seqUpdate ds i v)` is a strictly-descending `ZDerivesEmptyR`. Pure
   assembly over banked `ZDerivation_iCritAux_of` (validity via `zKValidF_seqUpdate`) +
   `iord_descent_iCritAux_of_ZDerivation` (the N1-IH descent) + `ZRegular/ZFresh/ZSeqAnt_zK_of_seqUpdate`; the
   reduct wff side-conditions discharge uniformly from `ZDerivation v` (`iperm_tp_fstIdx_of_ZDerivation` +
   `zKValidF_leafconds_of_ZDerivation`). This is Buchholz §14.254 `d[0] = Kʳ_Θ(i ∕ dᵢ[0])` at the assembly layer.
3. **Re-split `descent_step_K_noncrit_recurse`** — lap-147's final commits collapsed the no-redex residual to
   ONE sorry with a docstring that WRONGLY claimed "all cases replace the major premise" (false for the
   tag-5/6 `red`-FIXPOINT L-axioms, lap-130 finding `InternalZ:9281`). Restored the faithful §14.254a/b split,
   re-aligning with the judge-C2 three-leaf M1b path `{repMajor, axMajor, gDef}`:
   - `descent_step_K_noncrit_repMajor` (`Crux2Blueprint:2527`, tags 3/4) — §14.254a, replace the major premise.
   - `descent_step_K_noncrit_axMajor` (`Crux2Blueprint:2545`, tags 5/6) — §14.254b, replace the Rep cut-partner.
   - `descent_step_K_noncrit_recurse` now a sorry-FREE dispatcher (routes via `majorPrem_tag_mem`).

## ⚠️ Honest accounting — no sorry DROPPED this lap
The operator's strict bar is a dropped src sorry; this lap decomposed (+1 live sorry) and banked the shared
tool instead. Both leaves bottom out in the GENERAL `Γ→⊥` reduction (a structurally-smaller premise's
`Rep`-reduct, `Γ′` possibly nonempty), closure via strong induction on the derivation CODE (Buchholz Thm 2.1
— NOT `iord`-recursion, which is PRWO/Gödel-barred). That recursion is genuinely multi-lap; no single leaf is
honestly closeable without it. The advance is real: the surrounding replace plumbing is now discharged
(`descent_step_K_replace`), the residual of each leaf is PRECISE (one smaller-premise reduct), and the
source-grounded §14.254a/b structure is pinned.

## 🎯 CONCRETE NEXT ATTACK (hardest-first → an actual DROP)
Build the GENERAL reduction as a **`ZDerivation`-valued** lemma producing a smaller premise's same-end-sequent
descending reduct, by strong induction on code. Tractable FIRST target = **tag-3 repMajor** (`zInd` major
premise): generalize lap-146 `descent_step_Ind` off `Γ=∅` to a `ZDerivation`-valued `Γₘ→⊥` Ind reduct
(`iIndReductSeqG`, same end-sequent, descending/regular/fresh/seqAnt), then `descent_step_K_replace` at
`i = majorIdx (zK s r ds)` DROPS the tag-3 part of repMajor. The `Γ=∅`-dependence in `descent_step_Ind` is
LOCALIZED to `hAnt1` (the antecedent collapse, `Crux2Blueprint:2782`): for nonempty `Γₘ` the Ind-reduct
telescope must thread `Γₘ` through each premise's antecedent. (tag-4 `zK` major premise needs the recursion
proper / `iord_descent_red_zK_replace`-style splice; axMajor needs the cut-partner-Rep dichotomy via
`majorPrem_zAxAll_cutPartner`/`_zAxNeg_cutPartner` + the recursion.)

## ⚠️ Forbidden / do-NOT-undo
- Do NOT witness any descent branch with `red`; do NOT re-introduce criticality into the `iRKcCrit` path.
- Do NOT attack the off-path dead `red`-soundness sorries {:82,:1257,:1367,:1563,:1653,:1765,:1868} AS STATED.
- Do NOT attempt the general reduction by `iord`-recursion (PRWO/Gödel-barred); STRUCTURAL/code induction only.
- Do NOT collapse repMajor/axMajor back into one residual (the judge-C2/C3 split is the endorsed structure).
- (A) `exists_sigma1_descending_step` (`:2913`, gDef): keep the graph coupled to the single-STEP reduct; do
  NOT revive the orbit-level `redLeast`/μ-min (refuted lap-139, judge C4).

## 📁 Key anchors
- Shared replace: `descent_step_K_replace` `Crux2Blueprint:2475`. Banked underpinnings:
  `ZDerivation_iCritAux_of` `InternalZ:5863`, `iord_descent_iCritAux_of_ZDerivation` `:5843`,
  `zKValidF_seqUpdate` `InternalZ:3576`, `ZRegular/ZFresh/ZSeqAnt_zK_of_seqUpdate` `Zsubst:2465/2933/3136`.
- Leaves: `descent_step_K_noncrit_repMajor` `:2527`, `descent_step_K_noncrit_axMajor` `:2545`, dispatcher
  `descent_step_K_noncrit_recurse` `:2563`; has-redex `descent_step_K_hasRedex` `:2346`.
- Major-premise machinery: `majorPrem_tag_mem` `InternalZ:9369`, `majorIdx_botOrbit_reducible` `:9239`,
  `majorPrem_zAxAll_cutPartner`/`_zAxNeg_cutPartner` `:9301/9329`.
- Ind reduct to generalize: `descent_step_Ind` `Crux2Blueprint:2716` (lap 146), `Γ=∅`-dep at `hAnt1` `:2782`.
- Source: `scratchpad/buchholz-gentzen.txt` §13.4 endform `:84`, §14.25 chain rule `:262-490`.
- Judge review: `E-2026-06-26-JUDGE-codex-review.md` (C2 three-leaf, C3 separate-then-share, C4 gDef).

---
**→ Next session: build green, headline `[propext, sorryAx, Classical.choice, Quot.sound]`. The shared
§14.254 replace plumbing (`descent_step_K_replace`) is banked + axiom-clean; the no-redex residual is the
faithful §14.254a/b split {`repMajor`, `axMajor`}. To DROP a sorry: generalize `descent_step_Ind` off `Γ=∅`
(a `ZDerivation`-valued `Γₘ→⊥` Ind reduct) → tag-3 repMajor falls via `descent_step_K_replace`. NEVER `red`;
NEVER `iord`-recursion for the general step.**
