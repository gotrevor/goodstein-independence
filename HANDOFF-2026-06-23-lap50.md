# HANDOFF — 2026-06-23 (lap 50) — REVIEW + crux-2 PRWO formulation BUILT & faithfulness-certified

> **Branch** `plan` · **HEAD** = lap-50 commit (after `1ef8e1e`) · build **green** (`lake build
> GoodsteinPA`, 1311 jobs) · headline `peano_not_proves_goodstein` = honest `sorry` (anti-fraud intact,
> 0 math axioms). `src/` **untouched** this lap — the deliverable is `wip/GentzenCon.lean` (off-target).

## Fresh-mind review verdict — direction KEEP, pivot to the unblocked crux
Route A (Rathjen Cor 3.7: `γ→PRWO(ε₀)→Con(𝗣𝗔)→Gödel II`) is sound and re-validated against the real
kernel. The headline's single open obligation is `Reduction.goodstein_implies_consistency`, = two deep
girders. **Crux 1** (§3 `γ→PRWO`, internal Cor 3.4) bottoms at the internal Grzegorczyk `F`
(Ackermann-level) — **blocked on infra Foundation lacks** (no internal iteration table / fast-growing
hierarchy; F not IΣ₁-total). So this lap advanced the **unblocked crux 2** (Gentzen `PRWO→Con`), exactly
as the lap-49 handoff recommended.

## DONE this lap — `wip/GentzenCon.lean` (type-checks; 2 disclosed crux sorries; off the build target)
**Foundation map (Explore):** there is **NO universal evaluator / Kleene-T** in Foundation — `code` /
`codeOfPartrec'` / `codeOfREPred` all encode a *meta-level* function into a *fixed* formula. ⟹ **PRWO is
a per-formula schema**, which is precisely what the proof needs (crux 1 proves every instance; crux 2
uses the single instance for the Gentzen descent). Memory `crux2-prwo-schema-no-universal-evaluator`.
- **`prwoInstance seq := “¬∀ n y z, (!seq y n ∧ !seq z (n+1)) → !precφ z y”`** — the PRWO sentence,
  reusing the repo's existing ε₀-ordering formula `SeamDefinability.precφ`. No `isNF` predicate needed:
  `natCode : ℕ ≃ NONote` bijects onto ALL CNF notations, so every `n:ℕ` already denotes a `<ε₀` ordinal.
- **`prwoInstance_faithful` PROVED** — the std-model audit `ℕ⊧prwoInstance seq ↔ ¬∀n y z, seq[y,n] →
  seq[z,n+1] → natCode z < natCode y`. Axioms = `[propext, choice, Quot.sound, ONoteComp…native_decide]`
  (trust base + 1 🟢 F-φ artifact). **The formulation is kernel-certified faithful.**
- `gentzenDescent_descends` / `derivesEmpty_iterate` PROVED (the `n↦ord(Rⁿd)` descent).
- `goodstein_implies_consistency_via_gentzen = crux2 ∘ crux1` type-checks = the `Reduction.lean` girder
  interface — the two-girder architecture is now concrete and validated.

DSL std-model eval recipe (the fiddly part, banked in the memory): the sentence head is `∼`, so connective
HomClass lemmas must be in the simp set (eval_all alone → "no progress"); `Structure.Add.add` reduces
`op(+)` for `(x+1)`; finish the ∀-body match by curry/uncurry.

## NEXT (hardest-first) — crux-2 deep cores, all in `wip/GentzenCon.lean`
1. **`ord_R_descends` (eq 5)** — Gentzen's reduction lowers the assigned ordinal. THE ordinal-analysis
   content; ground in Buchholz [6] (`papers/buchholz-on-gentzens-first-consistency-proof.pdf` +
   `papers/siders-gentzen-consistency-proofs-arithmetic.pdf`). Foundation's Hauptsatz is **meta-level
   only** (`Foundation/FirstOrder/Hauptsatz.lean` proves `Derivation.IsCutFree` on `Derivation2`, NOT an
   arithmetized cut-elim) — so there is no shortcut; this is a from-scratch arithmetization.
2. **`ord`/`R` as arithmetized primrec functions** over `Theory.Derivation : V→Prop`
   (`Bootstrapping/Syntax/Proof/Basic.lean:459`) + `R_preserves_empty` + `gentzenDescentφ` (the ℒₒᵣ graph
   of `n↦ord(Rⁿ d₀)`, `d₀`=least ⊥-proof).
3. **`gentzen_prwo_implies_consistency` (crux 2 assembly)** — inside 𝗣𝗔: `¬Con ⟹` derivation `d₀` of ⊥ ⟹
   `gentzenDescent d₀` is an infinite ≺-descent ⟹ contradicts `prwoInstance gentzenDescentφ`. The subtlety:
   `prwoInstance` must be applied to the *internal* `d₀` (this is why PRWO is a closed sentence with an
   internally-definable descent, not a meta-implication).
4. (separate girder, crux 1) **`goodstein_implies_prwo`** — Rathjen §3 internal Cor 3.4 (internal
   Ackermann, blocked; lead = lap-49 `InternalCor34.icorAlpha` tower).

## Promotion gate
When crux 2's `ord_R_descends`/`ord`/`R` are real (and crux 1 lands), the `wip/GentzenCon` chain replaces
`Reduction.goodstein_implies_consistency`'s sorry — but ONLY if `#print axioms peano_not_proves_goodstein`
is `[propext, choice, Quot.sound, PA_delta1Definable]` (the Route-A disclosed upstream axiom) or cleaner.
Until then `src/` stays as-is (anti-fraud).

## Aristotle: OFFLINE. WebFetch dead. LOCKED untouched: `Defs.lean`, `Bridge.lean` RHS,
`goodsteinTerminates`, `Statement.lean` headline.
