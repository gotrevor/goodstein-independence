# HANDOFF — lap 67 · THE tag-4 K-CASE DESCENT ASSEMBLED + definability cascade underway

> Supersedes all prior HANDOFF-*.md. Read this, then `PENDING_WORK.md` (top = live next-step plan),
> then `STATUS.md`. Branch: `plan`. HEAD: `f3bea74`. Build: 🟢 green, `lake build GoodsteinPA` = 1321
> jobs. Working tree: clean (all committed).

## 🎯 OBJECTIVE (unchanged)
Discharge `peano_not_proves_goodstein` (`src/GoodsteinPA/Statement.lean:22`) **axiom-free** (trust base
`[propext, Classical.choice, Quot.sound]`). Route: `PA⊢γ →(crux-1, DONE) PA⊢PRWO(ε₀) →(crux-2) PA⊢Con(PA)`
→ Gödel II. **crux-2 (Buchholz-Z ordinal analysis in PA) is the sole remaining math wall.**
The 3 `src/` sorries: `Statement.lean:22` (headline), `Reduction.lean:66` (`goodstein_implies_consistency`
= crux-2's girder), `DescentSemantic.lean:550` (the slowed code-descent β).

## ✅ WHAT LANDED THIS LAP (8 green commits, all axiom-clean `[propext,choice,Quot.sound]`)
**THE prize: `iord_descent_iR2_zK_of_valid` (commit `46ae441`) — the tag-4 (K/cut-rule) ordinal
descent is now machine-checked.** `o(iR2 (zK s r ds)) ≺ o(zK s r ds)` for a valid K^r chain whose
premises are ZDerivations, conditional on `zKValid s r ds`. This was THE remaining crux-2 descent nut;
the descent MATH is now complete across all rules. Supporting substrate (all in `src/InternalZ.lean`):
1. `tp_cases` (tp-trichotomy R/L/Rep), `tp_eq_isymR_of_pi₁_zero`/`tp_eq_isymLk_of_pi₁_one`, `isymIsR`,
   `pi₁_isym*` ⟹ **`redexPair_tp`**: read `tp(redexI)=R_A` ∧ `tp(redexJ)=L^k_A` off the bare
   `isRedexPair` finder least-pair (so the prime nut's six ρ-facts at redexI/redexJ are dischargeable).
2. **`iRedDescent_zAxReduct_of_iRedDescent`** (wrap collapse via `icmp_trans`; handles the I-rule
   sub-derivation itself being an axiom leaf) + `iRedDescent_zAxReduct_iR2_of_tp_isymR` (i-side) /
   `_isymLk` (j-side) ⟹ the six ρ-facts at `ρ = zAxReduct∘iR2`.
3. **`zKValid` Prop bundle** = `isChainInf` ∧ per-premise `iperm`(perm) ∧ `¬iperm`(crit) ∧ per-tag
   principal-formula `IsUFormula` (tags 1,2,5,6) — exactly the deferred hyps the prime nut consumes.
4. **Definability cascade (in progress)** toward arithmetizing `zKValid` into `ZPhi`:
   `seqAntDef`/`seqSuccDef`/`chainAsuccDef`/`chainAntDef`/`zAxAllFDef`/`zAxNegFDef` (function Defs),
   `inAntDef` (𝚺₀), `iperm_iff_proj` + `ipermDef` (𝚺₀), `isChainInf_iff_idx` (bounded-index reform,
   eliminates the unbounded `∀ B`).

## ▶ NEXT STEP — finish the definability cascade, then make the descent UNCONDITIONAL
The tag-4 descent is conditional on `zKValid`; the bare `ZPhi` `zK` disjunct doesn't yet carry it. To
make `iord_descent_iR2_struct` unconditional on tag 4 (⟹ the unconditional `ZDerivation d → icmp (iord
(iR2 d)) (iord d)=0`), wire `zKValid` into `ZPhi`. Remaining cascade steps (see PENDING_WORK top for
full detail):
1. **`isChainInfDef : 𝚫₁.Semisentence 3`** — `.mkDelta (.mkSigma …) (.mkPi …)` matching
   `isChainInf_iff_idx`. IDIOM (confirmed from Foundation `Vec.lean:919`, `isFormulaSet`): σ extracts
   each function value via `∃ v, !vDef v args ∧ …`; π via `∀ v, !vDef v args → …`; bounded quantifiers
   (`∃ j0 < l`, `∀ i ≤ j0`, `∀ k < lh …`, `∃ i' < i`) appear IDENTICALLY in both (bounded ∃ IS allowed
   in `.mkPi` — see `RestrictedProvability.lean:27`). Only `irk ≤ r` differs: σ `∃ rk, !irkDef rk ca ∧
   rk ≤ r`, π `∀ rk, !irkDef rk ca → rk ≤ r`. Functions znth/fstIdx/lh/seqAnt/seqSucc are all 𝚺₀.
   Prove `.Defined` by `.mk; constructor <;> intro v <;> simp [isChainInf_iff_idx, …all _defined.iff…]`.
2. **`zKValidDef : 𝚫₁.Semisentence 3`** bundling `isChainInfDef` + bounded-∀ `ipermDef`/`¬ipermDef` +
   bounded-∀ per-tag `IsUFormula` (`(isUFormula ℒₒᵣ).sigma`/`.pi` on `zIallF`/`zInegF`/`zAxAllF`/
   `zAxNegF`, gated by `zTagDef = 1/2/5/6`). All pieces now defined.
3. **ZPhi `zK`-disjunct cascade** (one atomic pass, build at end): add `∧ zKValid s r ds` to the `zK`
   disjunct in `ZPhi` def + `zphi_monotone` (trivial — no `C`) + `zphi_strong_finite` (trivial) +
   `zphi_iff` (both dirs) + `zblueprint` Σ AND Π cores (`!zKValidDef.sigma`/`.pi s r ds`) +
   `zPhi_definable` simp set; then the ~6 `rcases zDerivation_iff.mp` sites gain a `hvalid` in the zK
   pattern. (Lap-66 did the analogous §5-leaf cascade — pattern known.)
4. **Make tag-4 unconditional**: in `iord_descent_iR2_struct`, replace the zK case's `simp [zTag_zK] at
   htag` with `exact iord_descent_iR2_zK_of_valid hds hmem hvalid` (premises ZDerivation + zKValid from
   `zDerivation_zK_inv`), drop the `htag` restriction.
Then: no-infinite-descent (ε₀ WF) ⟹ `ZDerivesEmpty d → False` ⟹ C0.5 Foundation→Z bridge
(`foundation_bot_to_Z_empty`, blueprint Bryce-Goré `Peano.v`) ⟹ discharge `Reduction.lean:66` ⟹
headline. ANTI-FRAUD: only discharge `Reduction.lean:66` once `#print axioms peano_not_proves_goodstein`
is clean.

## Pointers
- `E-CRUX2-DECOMPOSITION-2026-06-24.md` §8 = leaf grind-list. `papers/` Buchholz [6].
- Reference corpus (read-only, NOT auto-loaded): `~/personal/claude/knowledge/core/projects/lean-journey/reference/`.
- Foundation idiom refs used this lap: `HFS/Seq.lean` (`znth_prop_not`, `lh_bound`),
  `Bootstrapping/.../Proof/Basic.lean` (blueprint `.mkDelta`/`.sigma`/`.pi` shape),
  `Bootstrapping/.../isFormulaSet` (Δ₁ `.mkDelta` with `.sigma`/`.pi` sub-predicates), `HFS/Vec.lean:919`
  (function-value extraction inside `.mkPi` via `∀ v, !vDef → …`).

## Lap discipline reminder
Every lap must compile crux-2 work under the green-gate (InternalZ is in `src/`). Commit each green
build, end the turn after each so the governor can pace.
